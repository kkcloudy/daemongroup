关于eag2.0上配置实时生效的处理

1、对配置的应用直接复制到相应的session，这样保证在修改后，已经认证的用户配置不用变，也不会下线 。
    除了nasip 和 端口 的修改需要下线，其他配置修改都不影响现有用户。
    
2、1.3版本配置兼容问题
    vtysh里面有一个全局变量boot_flag，
    如果为1，表明当前是系统起来的时候加载配置执行命令。
    如果为0，表明是命令行执行命令。
    2.0上保留1.3当前已经实现的命令，但是加上对boot_flag的判断，如果是系统启动加载配置，就执行（其实就是写配置文件）
        否则就提示，这个命令已经废弃了，保留它只是为了能兼容老版本的配置文件。
        然后修改最后start 服务的命令，其中读取1.3的命令生成的配置文件，转换成2。0的dbus调用。
        将1.3配置命令的showrun 去掉。
    2.0所有的命令都重新实现。

    可以和1.3的命令在相同的节点下。
    
