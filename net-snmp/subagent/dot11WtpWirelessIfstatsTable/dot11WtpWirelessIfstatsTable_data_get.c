/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpWirelessIfstatsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpWirelessIfstatsTable get routines.
 * TODO:240:M: Implement dot11WtpWirelessIfstatsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpWirelessIfstatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpWirelessIfstatsTable is subid 5 of wtpInterface.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpWirelessIfstatsTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11WtpWirelessIfstatsTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfIndex
 * wtpWirelessIfIndex is subid 1 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.3.1.1
 * Description:
Wireless interface index.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessIfIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessIfIndex_dot11WtpWirelessIfstatsTable_map(long *mib_wtpWirelessIfIndex_val_ptr, long raw_wtpWirelessIfIndex_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessIfIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wtpWirelessIfIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessIfIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessIfIndex_val_ptr) = raw_wtpWirelessIfIndex_val;

    return MFD_SUCCESS;
} /* wtpWirelessIfIndex_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpWirelessIfstatsTable_indexes_set_tbl_idx(dot11WtpWirelessIfstatsTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpWirelessIfIndex_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:dot11WtpWirelessIfstatsTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wtpWirelessIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wtpWirelessIfIndex = wtpWirelessIfIndex_val;
    

    return MFD_SUCCESS;
} /* dot11WtpWirelessIfstatsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpWirelessIfstatsTable_indexes_set(dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpWirelessIfIndex_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:dot11WtpWirelessIfstatsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpWirelessIfstatsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpWirelessIfIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpWirelessIfstatsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpWirelessIfstatsTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfAvgRxSignalStrength
 * wirelessIfAvgRxSignalStrength is subid 1 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.1
 * Description:
The average received signal strength.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfAvgRxSignalStrength.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfAvgRxSignalStrength_map(long *mib_wirelessIfAvgRxSignalStrength_val_ptr, long raw_wirelessIfAvgRxSignalStrength_val)
{
    netsnmp_assert(NULL != mib_wirelessIfAvgRxSignalStrength_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfAvgRxSignalStrength_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfAvgRxSignalStrength mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfAvgRxSignalStrength_val_ptr) = raw_wirelessIfAvgRxSignalStrength_val;

    return MFD_SUCCESS;
} /* wirelessIfAvgRxSignalStrength_map */

/**
 * Extract the current value of the wirelessIfAvgRxSignalStrength data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfAvgRxSignalStrength_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfAvgRxSignalStrength_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, long * wirelessIfAvgRxSignalStrength_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfAvgRxSignalStrength_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfAvgRxSignalStrength_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfAvgRxSignalStrength data.
 * set (* wirelessIfAvgRxSignalStrength_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfAvgRxSignalStrength_val_ptr ) = rowreq_ctx->data.wirelessIfAvgRxSignalStrength;

    return MFD_SUCCESS;
} /* wirelessIfAvgRxSignalStrength_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfHighestRxSignalStrength
 * wirelessIfHighestRxSignalStrength is subid 2 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.2
 * Description:
Receive the maximum signal strength.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfHighestRxSignalStrength.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfHighestRxSignalStrength_map(long *mib_wirelessIfHighestRxSignalStrength_val_ptr, long raw_wirelessIfHighestRxSignalStrength_val)
{
    netsnmp_assert(NULL != mib_wirelessIfHighestRxSignalStrength_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfHighestRxSignalStrength_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfHighestRxSignalStrength mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfHighestRxSignalStrength_val_ptr) = raw_wirelessIfHighestRxSignalStrength_val;

    return MFD_SUCCESS;
} /* wirelessIfHighestRxSignalStrength_map */

/**
 * Extract the current value of the wirelessIfHighestRxSignalStrength data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfHighestRxSignalStrength_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfHighestRxSignalStrength_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, long * wirelessIfHighestRxSignalStrength_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfHighestRxSignalStrength_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfHighestRxSignalStrength_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfHighestRxSignalStrength data.
 * set (* wirelessIfHighestRxSignalStrength_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfHighestRxSignalStrength_val_ptr ) = rowreq_ctx->data.wirelessIfHighestRxSignalStrength;

    return MFD_SUCCESS;
} /* wirelessIfHighestRxSignalStrength_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfLowestRxSignalStrength
 * wirelessIfLowestRxSignalStrength is subid 3 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.3
 * Description:
Receive the minimum signal strength.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfLowestRxSignalStrength.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfLowestRxSignalStrength_map(long *mib_wirelessIfLowestRxSignalStrength_val_ptr, long raw_wirelessIfLowestRxSignalStrength_val)
{
    netsnmp_assert(NULL != mib_wirelessIfLowestRxSignalStrength_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfLowestRxSignalStrength_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfLowestRxSignalStrength mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfLowestRxSignalStrength_val_ptr) = raw_wirelessIfLowestRxSignalStrength_val;

    return MFD_SUCCESS;
} /* wirelessIfLowestRxSignalStrength_map */

/**
 * Extract the current value of the wirelessIfLowestRxSignalStrength data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfLowestRxSignalStrength_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfLowestRxSignalStrength_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, long * wirelessIfLowestRxSignalStrength_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfLowestRxSignalStrength_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfLowestRxSignalStrength_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfLowestRxSignalStrength data.
 * set (* wirelessIfLowestRxSignalStrength_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfLowestRxSignalStrength_val_ptr ) = rowreq_ctx->data.wirelessIfLowestRxSignalStrength;

    return MFD_SUCCESS;
} /* wirelessIfLowestRxSignalStrength_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfUpdownTimes
 * wirelessIfUpdownTimes is subid 4 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.4
 * Description:
Wireless interface updown times.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfUpdownTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfUpdownTimes_map(u_long *mib_wirelessIfUpdownTimes_val_ptr, u_long raw_wirelessIfUpdownTimes_val)
{
    netsnmp_assert(NULL != mib_wirelessIfUpdownTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfUpdownTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfUpdownTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfUpdownTimes_val_ptr) = raw_wirelessIfUpdownTimes_val;

    return MFD_SUCCESS;
} /* wirelessIfUpdownTimes_map */

/**
 * Extract the current value of the wirelessIfUpdownTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfUpdownTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfUpdownTimes_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfUpdownTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfUpdownTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfUpdownTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfUpdownTimes data.
 * set (* wirelessIfUpdownTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfUpdownTimes_val_ptr ) = rowreq_ctx->data.wirelessIfUpdownTimes;

    return MFD_SUCCESS;
} /* wirelessIfUpdownTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfChStatsNumStations
 * wirelessIfChStatsNumStations is subid 5 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.5
 * Description:
The terminals number which use the channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfChStatsNumStations.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfChStatsNumStations_map(u_long *mib_wirelessIfChStatsNumStations_val_ptr, u_long raw_wirelessIfChStatsNumStations_val)
{
    netsnmp_assert(NULL != mib_wirelessIfChStatsNumStations_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsNumStations_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfChStatsNumStations mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfChStatsNumStations_val_ptr) = raw_wirelessIfChStatsNumStations_val;

    return MFD_SUCCESS;
} /* wirelessIfChStatsNumStations_map */

/**
 * Extract the current value of the wirelessIfChStatsNumStations data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfChStatsNumStations_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfChStatsNumStations_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfChStatsNumStations_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfChStatsNumStations_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsNumStations_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfChStatsNumStations data.
 * set (* wirelessIfChStatsNumStations_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfChStatsNumStations_val_ptr ) = rowreq_ctx->data.wirelessIfChStatsNumStations;

    return MFD_SUCCESS;
} /* wirelessIfChStatsNumStations_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxSignalPkts
 * wirelessIfTxSignalPkts is subid 6 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.6
 * Description:
The number of signaling packets which.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxSignalPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxSignalPkts_map(u_long *mib_wirelessIfTxSignalPkts_val_ptr, u_long raw_wirelessIfTxSignalPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxSignalPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxSignalPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxSignalPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxSignalPkts_val_ptr) = raw_wirelessIfTxSignalPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfTxSignalPkts_map */

/**
 * Extract the current value of the wirelessIfTxSignalPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxSignalPkts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxSignalPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfTxSignalPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxSignalPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxSignalPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxSignalPkts data.
 * set (* wirelessIfTxSignalPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxSignalPkts_val_ptr ) = rowreq_ctx->data.wirelessIfTxSignalPkts;

    return MFD_SUCCESS;
} /* wirelessIfTxSignalPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxSignalPkts
 * wirelessIfRxSignalPkts is subid 7 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.7
 * Description:
The number of signaling packets which received.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxSignalPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxSignalPkts_map(u_long *mib_wirelessIfRxSignalPkts_val_ptr, u_long raw_wirelessIfRxSignalPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxSignalPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxSignalPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxSignalPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxSignalPkts_val_ptr) = raw_wirelessIfRxSignalPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfRxSignalPkts_map */

/**
 * Extract the current value of the wirelessIfRxSignalPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxSignalPkts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxSignalPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfRxSignalPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxSignalPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxSignalPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxSignalPkts data.
 * set (* wirelessIfRxSignalPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxSignalPkts_val_ptr ) = rowreq_ctx->data.wirelessIfRxSignalPkts;

    return MFD_SUCCESS;
} /* wirelessIfRxSignalPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxDataPkts
 * wirelessIfTxDataPkts is subid 8 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.8
 * Description:
The number of data packets which transmited.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxDataPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxDataPkts_map(u_long *mib_wirelessIfTxDataPkts_val_ptr, u_long raw_wirelessIfTxDataPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxDataPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxDataPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxDataPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxDataPkts_val_ptr) = raw_wirelessIfTxDataPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfTxDataPkts_map */

/**
 * Extract the current value of the wirelessIfTxDataPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxDataPkts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxDataPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfTxDataPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxDataPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxDataPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxDataPkts data.
 * set (* wirelessIfTxDataPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxDataPkts_val_ptr ) = rowreq_ctx->data.wirelessIfTxDataPkts;

    return MFD_SUCCESS;
} /* wirelessIfTxDataPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxDataPkts
 * wirelessIfRxDataPkts is subid 9 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.9
 * Description:
The number of data packets which received.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxDataPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxDataPkts_map(u_long *mib_wirelessIfRxDataPkts_val_ptr, u_long raw_wirelessIfRxDataPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxDataPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxDataPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxDataPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxDataPkts_val_ptr) = raw_wirelessIfRxDataPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfRxDataPkts_map */

/**
 * Extract the current value of the wirelessIfRxDataPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxDataPkts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxDataPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfRxDataPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxDataPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxDataPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxDataPkts data.
 * set (* wirelessIfRxDataPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxDataPkts_val_ptr ) = rowreq_ctx->data.wirelessIfRxDataPkts;

    return MFD_SUCCESS;
} /* wirelessIfRxDataPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxDataOctets
 * wirelessIfRxDataOctets is subid 10 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.10
 * Description:
Number of bytes received.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxDataOctets.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxDataOctets_map(unsigned long long *mib_wirelessIfRxDataOctets_val_ptr, unsigned long long raw_wirelessIfRxDataOctets_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxDataOctets_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxDataOctets_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxDataOctets mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxDataOctets_val_ptr) = raw_wirelessIfRxDataOctets_val;

    return MFD_SUCCESS;
} /* wirelessIfRxDataOctets_map */

/**
 * Extract the current value of the wirelessIfRxDataOctets data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxDataOctets_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxDataOctets_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wirelessIfRxDataOctets_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxDataOctets_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxDataOctets_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxDataOctets data.
 * set (* wirelessIfRxDataOctets_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxDataOctets_val_ptr ) = rowreq_ctx->data.wirelessIfRxDataOctets;

    return MFD_SUCCESS;
} /* wirelessIfRxDataOctets_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxDataOctets
 * wirelessIfTxDataOctets is subid 11 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.11
 * Description:
Number of bytes sent.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxDataOctets.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxDataOctets_map(unsigned long long *mib_wirelessIfTxDataOctets_val_ptr, unsigned long long raw_wirelessIfTxDataOctets_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxDataOctets_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxDataOctets_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxDataOctets mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxDataOctets_val_ptr) = raw_wirelessIfTxDataOctets_val;

    return MFD_SUCCESS;
} /* wirelessIfTxDataOctets_map */

/**
 * Extract the current value of the wirelessIfTxDataOctets data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxDataOctets_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxDataOctets_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wirelessIfTxDataOctets_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxDataOctets_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxDataOctets_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxDataOctets data.
 * set (* wirelessIfTxDataOctets_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxDataOctets_val_ptr ) = rowreq_ctx->data.wirelessIfTxDataOctets;

    return MFD_SUCCESS;
} /* wirelessIfTxDataOctets_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfChStatsDwlinkTotRetryPkts
 * wirelessIfChStatsDwlinkTotRetryPkts is subid 12 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.12
 * Description:
Retransmission packets number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfChStatsDwlinkTotRetryPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfChStatsDwlinkTotRetryPkts_map(u_long *mib_wirelessIfChStatsDwlinkTotRetryPkts_val_ptr, u_long raw_wirelessIfChStatsDwlinkTotRetryPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfChStatsDwlinkTotRetryPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsDwlinkTotRetryPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfChStatsDwlinkTotRetryPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfChStatsDwlinkTotRetryPkts_val_ptr) = raw_wirelessIfChStatsDwlinkTotRetryPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfChStatsDwlinkTotRetryPkts_map */

/**
 * Extract the current value of the wirelessIfChStatsDwlinkTotRetryPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfChStatsDwlinkTotRetryPkts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfChStatsDwlinkTotRetryPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfChStatsDwlinkTotRetryPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfChStatsDwlinkTotRetryPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsDwlinkTotRetryPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfChStatsDwlinkTotRetryPkts data.
 * set (* wirelessIfChStatsDwlinkTotRetryPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfChStatsDwlinkTotRetryPkts_val_ptr ) = rowreq_ctx->data.wirelessIfChStatsDwlinkTotRetryPkts;

    return MFD_SUCCESS;
} /* wirelessIfChStatsDwlinkTotRetryPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfChStatsUplinkUniFrameCnt
 * wirelessIfChStatsUplinkUniFrameCnt is subid 13 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.13
 * Description:
The total number of uplink uniframes on wireless channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfChStatsUplinkUniFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfChStatsUplinkUniFrameCnt_map(u_long *mib_wirelessIfChStatsUplinkUniFrameCnt_val_ptr, u_long raw_wirelessIfChStatsUplinkUniFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfChStatsUplinkUniFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsUplinkUniFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfChStatsUplinkUniFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfChStatsUplinkUniFrameCnt_val_ptr) = raw_wirelessIfChStatsUplinkUniFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfChStatsUplinkUniFrameCnt_map */

/**
 * Extract the current value of the wirelessIfChStatsUplinkUniFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfChStatsUplinkUniFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfChStatsUplinkUniFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfChStatsUplinkUniFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfChStatsUplinkUniFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsUplinkUniFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfChStatsUplinkUniFrameCnt data.
 * set (* wirelessIfChStatsUplinkUniFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfChStatsUplinkUniFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfChStatsUplinkUniFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfChStatsUplinkUniFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfChStatsDwlinkUniFrameCnt
 * wirelessIfChStatsDwlinkUniFrameCnt is subid 14 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.14
 * Description:
The total number of downlink uniframes on wireless channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfChStatsDwlinkUniFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfChStatsDwlinkUniFrameCnt_map(u_long *mib_wirelessIfChStatsDwlinkUniFrameCnt_val_ptr, u_long raw_wirelessIfChStatsDwlinkUniFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfChStatsDwlinkUniFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsDwlinkUniFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfChStatsDwlinkUniFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfChStatsDwlinkUniFrameCnt_val_ptr) = raw_wirelessIfChStatsDwlinkUniFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfChStatsDwlinkUniFrameCnt_map */

/**
 * Extract the current value of the wirelessIfChStatsDwlinkUniFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfChStatsDwlinkUniFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfChStatsDwlinkUniFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfChStatsDwlinkUniFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfChStatsDwlinkUniFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsDwlinkUniFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfChStatsDwlinkUniFrameCnt data.
 * set (* wirelessIfChStatsDwlinkUniFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfChStatsDwlinkUniFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfChStatsDwlinkUniFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfChStatsDwlinkUniFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfUpChStatsFrameNonUniFrameCnt
 * wirelessIfUpChStatsFrameNonUniFrameCnt is subid 15 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.15
 * Description:
The total number of uplink nonuniframes on wireless channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfUpChStatsFrameNonUniFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfUpChStatsFrameNonUniFrameCnt_map(u_long *mib_wirelessIfUpChStatsFrameNonUniFrameCnt_val_ptr, u_long raw_wirelessIfUpChStatsFrameNonUniFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfUpChStatsFrameNonUniFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfUpChStatsFrameNonUniFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfUpChStatsFrameNonUniFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfUpChStatsFrameNonUniFrameCnt_val_ptr) = raw_wirelessIfUpChStatsFrameNonUniFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfUpChStatsFrameNonUniFrameCnt_map */

/**
 * Extract the current value of the wirelessIfUpChStatsFrameNonUniFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfUpChStatsFrameNonUniFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfUpChStatsFrameNonUniFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfUpChStatsFrameNonUniFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfUpChStatsFrameNonUniFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfUpChStatsFrameNonUniFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfUpChStatsFrameNonUniFrameCnt data.
 * set (* wirelessIfUpChStatsFrameNonUniFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfUpChStatsFrameNonUniFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfUpChStatsFrameNonUniFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfUpChStatsFrameNonUniFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfDownChStatsFrameNonUniFrameCnt
 * wirelessIfDownChStatsFrameNonUniFrameCnt is subid 16 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.16
 * Description:
The total number of downlink nonuniframes on wireless channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfDownChStatsFrameNonUniFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfDownChStatsFrameNonUniFrameCnt_map(u_long *mib_wirelessIfDownChStatsFrameNonUniFrameCnt_val_ptr, u_long raw_wirelessIfDownChStatsFrameNonUniFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfDownChStatsFrameNonUniFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfDownChStatsFrameNonUniFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfDownChStatsFrameNonUniFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfDownChStatsFrameNonUniFrameCnt_val_ptr) = raw_wirelessIfDownChStatsFrameNonUniFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfDownChStatsFrameNonUniFrameCnt_map */

/**
 * Extract the current value of the wirelessIfDownChStatsFrameNonUniFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfDownChStatsFrameNonUniFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfDownChStatsFrameNonUniFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfDownChStatsFrameNonUniFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfDownChStatsFrameNonUniFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfDownChStatsFrameNonUniFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfDownChStatsFrameNonUniFrameCnt data.
 * set (* wirelessIfDownChStatsFrameNonUniFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfDownChStatsFrameNonUniFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfDownChStatsFrameNonUniFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfDownChStatsFrameNonUniFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfChStatsPhyErrPkts
 * wirelessIfChStatsPhyErrPkts is subid 17 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.17
 * Description:
The total number of physical error packets on wireless channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfChStatsPhyErrPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfChStatsPhyErrPkts_map(u_long *mib_wirelessIfChStatsPhyErrPkts_val_ptr, u_long raw_wirelessIfChStatsPhyErrPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfChStatsPhyErrPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsPhyErrPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfChStatsPhyErrPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfChStatsPhyErrPkts_val_ptr) = raw_wirelessIfChStatsPhyErrPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfChStatsPhyErrPkts_map */

/**
 * Extract the current value of the wirelessIfChStatsPhyErrPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfChStatsPhyErrPkts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfChStatsPhyErrPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfChStatsPhyErrPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfChStatsPhyErrPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsPhyErrPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfChStatsPhyErrPkts data.
 * set (* wirelessIfChStatsPhyErrPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfChStatsPhyErrPkts_val_ptr ) = rowreq_ctx->data.wirelessIfChStatsPhyErrPkts;

    return MFD_SUCCESS;
} /* wirelessIfChStatsPhyErrPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfChStatsMacFcsErrPkts
 * wirelessIfChStatsMacFcsErrPkts is subid 18 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.18
 * Description:
The total number of Fcs mac error packets on wireless channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfChStatsMacFcsErrPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfChStatsMacFcsErrPkts_map(u_long *mib_wirelessIfChStatsMacFcsErrPkts_val_ptr, u_long raw_wirelessIfChStatsMacFcsErrPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfChStatsMacFcsErrPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsMacFcsErrPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfChStatsMacFcsErrPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfChStatsMacFcsErrPkts_val_ptr) = raw_wirelessIfChStatsMacFcsErrPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfChStatsMacFcsErrPkts_map */

/**
 * Extract the current value of the wirelessIfChStatsMacFcsErrPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfChStatsMacFcsErrPkts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfChStatsMacFcsErrPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfChStatsMacFcsErrPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfChStatsMacFcsErrPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsMacFcsErrPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfChStatsMacFcsErrPkts data.
 * set (* wirelessIfChStatsMacFcsErrPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfChStatsMacFcsErrPkts_val_ptr ) = rowreq_ctx->data.wirelessIfChStatsMacFcsErrPkts;

    return MFD_SUCCESS;
} /* wirelessIfChStatsMacFcsErrPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfChStatsMacMicErrPkts
 * wirelessIfChStatsMacMicErrPkts is subid 19 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.19
 * Description:
The total number of Mic mac error packets on wireless channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfChStatsMacMicErrPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfChStatsMacMicErrPkts_map(u_long *mib_wirelessIfChStatsMacMicErrPkts_val_ptr, u_long raw_wirelessIfChStatsMacMicErrPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfChStatsMacMicErrPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsMacMicErrPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfChStatsMacMicErrPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfChStatsMacMicErrPkts_val_ptr) = raw_wirelessIfChStatsMacMicErrPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfChStatsMacMicErrPkts_map */

/**
 * Extract the current value of the wirelessIfChStatsMacMicErrPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfChStatsMacMicErrPkts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfChStatsMacMicErrPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfChStatsMacMicErrPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfChStatsMacMicErrPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsMacMicErrPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfChStatsMacMicErrPkts data.
 * set (* wirelessIfChStatsMacMicErrPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfChStatsMacMicErrPkts_val_ptr ) = rowreq_ctx->data.wirelessIfChStatsMacMicErrPkts;

    return MFD_SUCCESS;
} /* wirelessIfChStatsMacMicErrPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfChStatsMacDecryptErrPkts
 * wirelessIfChStatsMacDecryptErrPkts is subid 20 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.20
 * Description:
The total number of decryption failed mac error packets on wireless channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfChStatsMacDecryptErrPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfChStatsMacDecryptErrPkts_map(u_long *mib_wirelessIfChStatsMacDecryptErrPkts_val_ptr, u_long raw_wirelessIfChStatsMacDecryptErrPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfChStatsMacDecryptErrPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsMacDecryptErrPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfChStatsMacDecryptErrPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfChStatsMacDecryptErrPkts_val_ptr) = raw_wirelessIfChStatsMacDecryptErrPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfChStatsMacDecryptErrPkts_map */

/**
 * Extract the current value of the wirelessIfChStatsMacDecryptErrPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfChStatsMacDecryptErrPkts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfChStatsMacDecryptErrPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfChStatsMacDecryptErrPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfChStatsMacDecryptErrPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsMacDecryptErrPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfChStatsMacDecryptErrPkts data.
 * set (* wirelessIfChStatsMacDecryptErrPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfChStatsMacDecryptErrPkts_val_ptr ) = rowreq_ctx->data.wirelessIfChStatsMacDecryptErrPkts;

    return MFD_SUCCESS;
} /* wirelessIfChStatsMacDecryptErrPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfChStatsFrameErrorCnt
 * wirelessIfChStatsFrameErrorCnt is subid 21 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.21
 * Description:
The total number of error frames which the channel received.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfChStatsFrameErrorCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfChStatsFrameErrorCnt_map(u_long *mib_wirelessIfChStatsFrameErrorCnt_val_ptr, u_long raw_wirelessIfChStatsFrameErrorCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfChStatsFrameErrorCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsFrameErrorCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfChStatsFrameErrorCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfChStatsFrameErrorCnt_val_ptr) = raw_wirelessIfChStatsFrameErrorCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfChStatsFrameErrorCnt_map */

/**
 * Extract the current value of the wirelessIfChStatsFrameErrorCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfChStatsFrameErrorCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfChStatsFrameErrorCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfChStatsFrameErrorCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfChStatsFrameErrorCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsFrameErrorCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfChStatsFrameErrorCnt data.
 * set (* wirelessIfChStatsFrameErrorCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfChStatsFrameErrorCnt_val_ptr ) = rowreq_ctx->data.wirelessIfChStatsFrameErrorCnt;

    return MFD_SUCCESS;
} /* wirelessIfChStatsFrameErrorCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfChStatsFrameRetryCnt
 * wirelessIfChStatsFrameRetryCnt is subid 22 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.22
 * Description:
The total number of retry frames on the channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfChStatsFrameRetryCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfChStatsFrameRetryCnt_map(u_long *mib_wirelessIfChStatsFrameRetryCnt_val_ptr, u_long raw_wirelessIfChStatsFrameRetryCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfChStatsFrameRetryCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsFrameRetryCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfChStatsFrameRetryCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfChStatsFrameRetryCnt_val_ptr) = raw_wirelessIfChStatsFrameRetryCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfChStatsFrameRetryCnt_map */

/**
 * Extract the current value of the wirelessIfChStatsFrameRetryCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfChStatsFrameRetryCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfChStatsFrameRetryCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfChStatsFrameRetryCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfChStatsFrameRetryCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfChStatsFrameRetryCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfChStatsFrameRetryCnt data.
 * set (* wirelessIfChStatsFrameRetryCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfChStatsFrameRetryCnt_val_ptr ) = rowreq_ctx->data.wirelessIfChStatsFrameRetryCnt;

    return MFD_SUCCESS;
} /* wirelessIfChStatsFrameRetryCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfApChStatsFrameFragRate
 * wirelessIfApChStatsFrameFragRate is subid 23 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.23
 * Description:
Channel sub-frame rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfApChStatsFrameFragRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfApChStatsFrameFragRate_map(u_long *mib_wirelessIfApChStatsFrameFragRate_val_ptr, u_long raw_wirelessIfApChStatsFrameFragRate_val)
{
    netsnmp_assert(NULL != mib_wirelessIfApChStatsFrameFragRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfApChStatsFrameFragRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfApChStatsFrameFragRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfApChStatsFrameFragRate_val_ptr) = raw_wirelessIfApChStatsFrameFragRate_val;

    return MFD_SUCCESS;
} /* wirelessIfApChStatsFrameFragRate_map */

/**
 * Extract the current value of the wirelessIfApChStatsFrameFragRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfApChStatsFrameFragRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfApChStatsFrameFragRate_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfApChStatsFrameFragRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfApChStatsFrameFragRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfApChStatsFrameFragRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfApChStatsFrameFragRate data.
 * set (* wirelessIfApChStatsFrameFragRate_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfApChStatsFrameFragRate_val_ptr ) = rowreq_ctx->data.wirelessIfApChStatsFrameFragRate;

    return MFD_SUCCESS;
} /* wirelessIfApChStatsFrameFragRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxMgmtFrameCnt
 * wirelessIfRxMgmtFrameCnt is subid 24 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.24
 * Description:
Received the number of management frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxMgmtFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxMgmtFrameCnt_map(u_long *mib_wirelessIfRxMgmtFrameCnt_val_ptr, u_long raw_wirelessIfRxMgmtFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxMgmtFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxMgmtFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxMgmtFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxMgmtFrameCnt_val_ptr) = raw_wirelessIfRxMgmtFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfRxMgmtFrameCnt_map */

/**
 * Extract the current value of the wirelessIfRxMgmtFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxMgmtFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxMgmtFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfRxMgmtFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxMgmtFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxMgmtFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxMgmtFrameCnt data.
 * set (* wirelessIfRxMgmtFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxMgmtFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfRxMgmtFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfRxMgmtFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxCtrlFrameCnt
 * wirelessIfRxCtrlFrameCnt is subid 25 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.25
 * Description:
Received the number of control frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxCtrlFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxCtrlFrameCnt_map(u_long *mib_wirelessIfRxCtrlFrameCnt_val_ptr, u_long raw_wirelessIfRxCtrlFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxCtrlFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxCtrlFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxCtrlFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxCtrlFrameCnt_val_ptr) = raw_wirelessIfRxCtrlFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfRxCtrlFrameCnt_map */

/**
 * Extract the current value of the wirelessIfRxCtrlFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxCtrlFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxCtrlFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfRxCtrlFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxCtrlFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxCtrlFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxCtrlFrameCnt data.
 * set (* wirelessIfRxCtrlFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxCtrlFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfRxCtrlFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfRxCtrlFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxDataFrameCnt
 * wirelessIfRxDataFrameCnt is subid 26 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.26
 * Description:
Received the number of data frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxDataFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxDataFrameCnt_map(u_long *mib_wirelessIfRxDataFrameCnt_val_ptr, u_long raw_wirelessIfRxDataFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxDataFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxDataFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxDataFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxDataFrameCnt_val_ptr) = raw_wirelessIfRxDataFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfRxDataFrameCnt_map */

/**
 * Extract the current value of the wirelessIfRxDataFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxDataFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxDataFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfRxDataFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxDataFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxDataFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxDataFrameCnt data.
 * set (* wirelessIfRxDataFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxDataFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfRxDataFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfRxDataFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxAuthenFrameCnt
 * wirelessIfRxAuthenFrameCnt is subid 27 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.27
 * Description:
Received the number of certification frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxAuthenFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxAuthenFrameCnt_map(u_long *mib_wirelessIfRxAuthenFrameCnt_val_ptr, u_long raw_wirelessIfRxAuthenFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxAuthenFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxAuthenFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxAuthenFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxAuthenFrameCnt_val_ptr) = raw_wirelessIfRxAuthenFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfRxAuthenFrameCnt_map */

/**
 * Extract the current value of the wirelessIfRxAuthenFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxAuthenFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxAuthenFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfRxAuthenFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxAuthenFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxAuthenFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxAuthenFrameCnt data.
 * set (* wirelessIfRxAuthenFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxAuthenFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfRxAuthenFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfRxAuthenFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxAssociateFrameCnt
 * wirelessIfRxAssociateFrameCnt is subid 28 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.28
 * Description:
Received the number of associated frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxAssociateFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxAssociateFrameCnt_map(u_long *mib_wirelessIfRxAssociateFrameCnt_val_ptr, u_long raw_wirelessIfRxAssociateFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxAssociateFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxAssociateFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxAssociateFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxAssociateFrameCnt_val_ptr) = raw_wirelessIfRxAssociateFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfRxAssociateFrameCnt_map */

/**
 * Extract the current value of the wirelessIfRxAssociateFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxAssociateFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxAssociateFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfRxAssociateFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxAssociateFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxAssociateFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxAssociateFrameCnt data.
 * set (* wirelessIfRxAssociateFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxAssociateFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfRxAssociateFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfRxAssociateFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxMgmtFrameCnt
 * wirelessIfTxMgmtFrameCnt is subid 29 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.29
 * Description:
Send the number of management frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxMgmtFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxMgmtFrameCnt_map(u_long *mib_wirelessIfTxMgmtFrameCnt_val_ptr, u_long raw_wirelessIfTxMgmtFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxMgmtFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxMgmtFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxMgmtFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxMgmtFrameCnt_val_ptr) = raw_wirelessIfTxMgmtFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfTxMgmtFrameCnt_map */

/**
 * Extract the current value of the wirelessIfTxMgmtFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxMgmtFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxMgmtFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfTxMgmtFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxMgmtFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxMgmtFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxMgmtFrameCnt data.
 * set (* wirelessIfTxMgmtFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxMgmtFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfTxMgmtFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfTxMgmtFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxCtrlFrameCnt
 * wirelessIfTxCtrlFrameCnt is subid 30 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.30
 * Description:
Send the number of control frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxCtrlFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxCtrlFrameCnt_map(u_long *mib_wirelessIfTxCtrlFrameCnt_val_ptr, u_long raw_wirelessIfTxCtrlFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxCtrlFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxCtrlFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxCtrlFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxCtrlFrameCnt_val_ptr) = raw_wirelessIfTxCtrlFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfTxCtrlFrameCnt_map */

/**
 * Extract the current value of the wirelessIfTxCtrlFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxCtrlFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxCtrlFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfTxCtrlFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxCtrlFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxCtrlFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxCtrlFrameCnt data.
 * set (* wirelessIfTxCtrlFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxCtrlFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfTxCtrlFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfTxCtrlFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxDataFrameCnt
 * wirelessIfTxDataFrameCnt is subid 31 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.31
 * Description:
Sent the number of data frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxDataFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxDataFrameCnt_map(u_long *mib_wirelessIfTxDataFrameCnt_val_ptr, u_long raw_wirelessIfTxDataFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxDataFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxDataFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxDataFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxDataFrameCnt_val_ptr) = raw_wirelessIfTxDataFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfTxDataFrameCnt_map */

/**
 * Extract the current value of the wirelessIfTxDataFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxDataFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxDataFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfTxDataFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxDataFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxDataFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxDataFrameCnt data.
 * set (* wirelessIfTxDataFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxDataFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfTxDataFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfTxDataFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxAuthenFrameCnt
 * wirelessIfTxAuthenFrameCnt is subid 32 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.32
 * Description:
Send the number of certification frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxAuthenFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxAuthenFrameCnt_map(u_long *mib_wirelessIfTxAuthenFrameCnt_val_ptr, u_long raw_wirelessIfTxAuthenFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxAuthenFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxAuthenFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxAuthenFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxAuthenFrameCnt_val_ptr) = raw_wirelessIfTxAuthenFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfTxAuthenFrameCnt_map */

/**
 * Extract the current value of the wirelessIfTxAuthenFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxAuthenFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxAuthenFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfTxAuthenFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxAuthenFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxAuthenFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxAuthenFrameCnt data.
 * set (* wirelessIfTxAuthenFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxAuthenFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfTxAuthenFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfTxAuthenFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxAssociateFrameCnt
 * wirelessIfTxAssociateFrameCnt is subid 33 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.5.1.33
 * Description:
Send the link of the number of frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxAssociateFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxAssociateFrameCnt_map(u_long *mib_wirelessIfTxAssociateFrameCnt_val_ptr, u_long raw_wirelessIfTxAssociateFrameCnt_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxAssociateFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxAssociateFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxAssociateFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxAssociateFrameCnt_val_ptr) = raw_wirelessIfTxAssociateFrameCnt_val;

    return MFD_SUCCESS;
} /* wirelessIfTxAssociateFrameCnt_map */

/**
 * Extract the current value of the wirelessIfTxAssociateFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxAssociateFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxAssociateFrameCnt_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfTxAssociateFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxAssociateFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxAssociateFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxAssociateFrameCnt data.
 * set (* wirelessIfTxAssociateFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxAssociateFrameCnt_val_ptr ) = rowreq_ctx->data.wirelessIfTxAssociateFrameCnt;

    return MFD_SUCCESS;
} /* wirelessIfTxAssociateFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxManagePkts
 * wirelessIfTxManagePkts is subid 34 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.34
 * Description:
The number of send management pkts.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxManagePkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxManagePkts_map(u_long *mib_wirelessIfTxManagePkts_val_ptr, u_long raw_wirelessIfTxManagePkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxManagePkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxManagePkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxManagePkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxManagePkts_val_ptr) = raw_wirelessIfTxManagePkts_val;

    return MFD_SUCCESS;
} /* wirelessIfTxManagePkts_map */

/**
 * Extract the current value of the wirelessIfTxManagePkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxManagePkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxManagePkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfTxManagePkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxManagePkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxManagePkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxManagePkts data.
 * set (* wirelessIfTxManagePkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxManagePkts_val_ptr ) = rowreq_ctx->data.wirelessIfTxManagePkts;

    return MFD_SUCCESS;
} /* wirelessIfTxManagePkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxManagePkts
 * wirelessIfRxManagePkts is subid 35 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.35
 * Description:
The number of received management pkts.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxManagePkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxManagePkts_map(u_long *mib_wirelessIfRxManagePkts_val_ptr, u_long raw_wirelessIfRxManagePkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxManagePkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxManagePkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxManagePkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxManagePkts_val_ptr) = raw_wirelessIfRxManagePkts_val;

    return MFD_SUCCESS;
} /* wirelessIfRxManagePkts_map */

/**
 * Extract the current value of the wirelessIfRxManagePkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxManagePkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxManagePkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfRxManagePkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxManagePkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxManagePkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxManagePkts data.
 * set (* wirelessIfRxManagePkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxManagePkts_val_ptr ) = rowreq_ctx->data.wirelessIfRxManagePkts;

    return MFD_SUCCESS;
} /* wirelessIfRxManagePkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxManageOctets
 * wirelessIfTxManageOctets is subid 36 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.36
 * Description:
The number of send management bytes.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxManageOctets.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxManageOctets_map(unsigned long long *mib_wirelessIfTxManageOctets_val_ptr, unsigned long long raw_wirelessIfTxManageOctets_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxManageOctets_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxManageOctets_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxManageOctets mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxManageOctets_val_ptr) = raw_wirelessIfTxManageOctets_val;

    return MFD_SUCCESS;
} /* wirelessIfTxManageOctets_map */

/**
 * Extract the current value of the wirelessIfTxManageOctets data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxManageOctets_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxManageOctets_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wirelessIfTxManageOctets_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxManageOctets_val_ptr );

/*
 * TODO:231:o: |-> copy wirelessIfTxManageOctets data.
 * get (* wirelessIfTxManageOctets_val_ptr ).low and (* wirelessIfTxManageOctets_val_ptr ).high from rowreq_ctx->data
 */
  //  (* wirelessIfTxManageOctets_val_ptr ).high = rowreq_ctx->data.wirelessIfTxManageOctets.high;
  //  (* wirelessIfTxManageOctets_val_ptr ).low = rowreq_ctx->data.wirelessIfTxManageOctets.low;
	* wirelessIfTxManageOctets_val_ptr  = rowreq_ctx->data.wirelessIfTxManageOctets;

    return MFD_SUCCESS;
} /* wirelessIfTxManageOctets_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxManageOctets
 * wirelessIfRxManageOctets is subid 37 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.37
 * Description:
The number of received management bytes.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxManageOctets.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxManageOctets_map(unsigned long long *mib_wirelessIfRxManageOctets_val_ptr, unsigned long long raw_wirelessIfRxManageOctets_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxManageOctets_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxManageOctets_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxManageOctets mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxManageOctets_val_ptr) = raw_wirelessIfRxManageOctets_val;

    return MFD_SUCCESS;
} /* wirelessIfRxManageOctets_map */

/**
 * Extract the current value of the wirelessIfRxManageOctets data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxManageOctets_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxManageOctets_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wirelessIfRxManageOctets_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxManageOctets_val_ptr );

/*
 * TODO:231:o: |-> copy wirelessIfRxManageOctets data.
 * get (* wirelessIfRxManageOctets_val_ptr ).low and (* wirelessIfRxManageOctets_val_ptr ).high from rowreq_ctx->data
 */
   // (* wirelessIfRxManageOctets_val_ptr ).high = rowreq_ctx->data.wirelessIfRxManageOctets.high;
   // (* wirelessIfRxManageOctets_val_ptr ).low = rowreq_ctx->data.wirelessIfRxManageOctets.low;
	* wirelessIfRxManageOctets_val_ptr = rowreq_ctx->data.wirelessIfRxManageOctets;

    return MFD_SUCCESS;
} /* wirelessIfRxManageOctets_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxTotalPkts
 * wirelessIfTxTotalPkts is subid 38 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.38
 * Description:
.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxTotalPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxTotalPkts_map(u_long *mib_wirelessIfTxTotalPkts_val_ptr, u_long raw_wirelessIfTxTotalPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxTotalPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxTotalPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxTotalPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxTotalPkts_val_ptr) = raw_wirelessIfTxTotalPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfTxTotalPkts_map */

/**
 * Extract the current value of the wirelessIfTxTotalPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxTotalPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxTotalPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfTxTotalPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxTotalPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxTotalPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxTotalPkts data.
 * set (* wirelessIfTxTotalPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxTotalPkts_val_ptr ) = rowreq_ctx->data.wirelessIfTxTotalPkts;

    return MFD_SUCCESS;
} /* wirelessIfTxTotalPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxTotalPkts
 * wirelessIfRxTotalPkts is subid 39 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.39
 * Description:
.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxTotalPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxTotalPkts_map(u_long *mib_wirelessIfRxTotalPkts_val_ptr, u_long raw_wirelessIfRxTotalPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxTotalPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxTotalPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxTotalPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxTotalPkts_val_ptr) = raw_wirelessIfRxTotalPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfRxTotalPkts_map */

/**
 * Extract the current value of the wirelessIfRxTotalPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxTotalPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxTotalPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfRxTotalPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxTotalPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxTotalPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxTotalPkts data.
 * set (* wirelessIfRxTotalPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxTotalPkts_val_ptr ) = rowreq_ctx->data.wirelessIfRxTotalPkts;

    return MFD_SUCCESS;
} /* wirelessIfRxTotalPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxTotalOctets
 * wirelessIfTxTotalOctets is subid 40 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.40
 * Description:
The total number of radio send packet bytes.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxTotalOctets.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxTotalOctets_map(unsigned long long *mib_wirelessIfTxTotalOctets_val_ptr, unsigned long long raw_wirelessIfTxTotalOctets_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxTotalOctets_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxTotalOctets_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxTotalOctets mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxTotalOctets_val_ptr) = raw_wirelessIfTxTotalOctets_val;

    return MFD_SUCCESS;
} /* wirelessIfTxTotalOctets_map */

/**
 * Extract the current value of the wirelessIfTxTotalOctets data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxTotalOctets_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxTotalOctets_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wirelessIfTxTotalOctets_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxTotalOctets_val_ptr );

/*
 * TODO:231:o: |-> copy wirelessIfTxTotalOctets data.
 * get (* wirelessIfTxTotalOctets_val_ptr ).low and (* wirelessIfTxTotalOctets_val_ptr ).high from rowreq_ctx->data
 */
  //  (* wirelessIfTxTotalOctets_val_ptr ).high = rowreq_ctx->data.wirelessIfTxTotalOctets.high;
  //  (* wirelessIfTxTotalOctets_val_ptr ).low = rowreq_ctx->data.wirelessIfTxTotalOctets.low;
	* wirelessIfTxTotalOctets_val_ptr = rowreq_ctx->data.wirelessIfTxTotalOctets;

    return MFD_SUCCESS;
} /* wirelessIfTxTotalOctets_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxTotalOctets
 * wirelessIfRxTotalOctets is subid 41 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.41
 * Description:
The total number of radio received packet bytes.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxTotalOctets.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxTotalOctets_map(unsigned long long *mib_wirelessIfRxTotalOctets_val_ptr, unsigned long long raw_wirelessIfRxTotalOctets_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxTotalOctets_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxTotalOctets_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxTotalOctets mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxTotalOctets_val_ptr) = raw_wirelessIfRxTotalOctets_val;

    return MFD_SUCCESS;
} /* wirelessIfRxTotalOctets_map */

/**
 * Extract the current value of the wirelessIfRxTotalOctets data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxTotalOctets_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxTotalOctets_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wirelessIfRxTotalOctets_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxTotalOctets_val_ptr );

/*
 * TODO:231:o: |-> copy wirelessIfRxTotalOctets data.
 * get (* wirelessIfRxTotalOctets_val_ptr ).low and (* wirelessIfRxTotalOctets_val_ptr ).high from rowreq_ctx->data
 */
  //  (* wirelessIfRxTotalOctets_val_ptr ).high = rowreq_ctx->data.wirelessIfRxTotalOctets.high;
  //  (* wirelessIfRxTotalOctets_val_ptr ).low = rowreq_ctx->data.wirelessIfRxTotalOctets.low;
	* wirelessIfRxTotalOctets_val_ptr  = rowreq_ctx->data.wirelessIfRxTotalOctets;

    return MFD_SUCCESS;
} /* wirelessIfRxTotalOctets_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxDropPkts
 * wirelessIfRxDropPkts is subid 42 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.42
 * Description:
The total number of radio received drop packets.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxDropPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxDropPkts_map(u_long *mib_wirelessIfRxDropPkts_val_ptr, u_long raw_wirelessIfRxDropPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxDropPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxDropPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxDropPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxDropPkts_val_ptr) = raw_wirelessIfRxDropPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfRxDropPkts_map */

/**
 * Extract the current value of the wirelessIfRxDropPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxDropPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxDropPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfRxDropPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxDropPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxDropPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxDropPkts data.
 * set (* wirelessIfRxDropPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxDropPkts_val_ptr ) = rowreq_ctx->data.wirelessIfRxDropPkts;

    return MFD_SUCCESS;
} /* wirelessIfRxDropPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxDropPkts
 * wirelessIfTxDropPkts is subid 43 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.43
 * Description:
The total number of radio send drop packets.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxDropPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxDropPkts_map(u_long *mib_wirelessIfTxDropPkts_val_ptr, u_long raw_wirelessIfTxDropPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxDropPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxDropPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxDropPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxDropPkts_val_ptr) = raw_wirelessIfTxDropPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfTxDropPkts_map */

/**
 * Extract the current value of the wirelessIfTxDropPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxDropPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxDropPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfTxDropPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxDropPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxDropPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxDropPkts data.
 * set (* wirelessIfTxDropPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxDropPkts_val_ptr ) = rowreq_ctx->data.wirelessIfTxDropPkts;

    return MFD_SUCCESS;
} /* wirelessIfTxDropPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfRxErrPkts
 * wirelessIfRxErrPkts is subid 44 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.44
 * Description:
The total number of radio received error packets.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfRxErrPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfRxErrPkts_map(u_long *mib_wirelessIfRxErrPkts_val_ptr, u_long raw_wirelessIfRxErrPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfRxErrPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxErrPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfRxErrPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfRxErrPkts_val_ptr) = raw_wirelessIfRxErrPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfRxErrPkts_map */

/**
 * Extract the current value of the wirelessIfRxErrPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfRxErrPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfRxErrPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfRxErrPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfRxErrPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfRxErrPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfRxErrPkts data.
 * set (* wirelessIfRxErrPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfRxErrPkts_val_ptr ) = rowreq_ctx->data.wirelessIfRxErrPkts;

    return MFD_SUCCESS;
} /* wirelessIfRxErrPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wirelessIfTxErrPkts
 * wirelessIfTxErrPkts is subid 45 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.45
 * Description:
The total number of radio send error packets.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessIfTxErrPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessIfTxErrPkts_map(u_long *mib_wirelessIfTxErrPkts_val_ptr, u_long raw_wirelessIfTxErrPkts_val)
{
    netsnmp_assert(NULL != mib_wirelessIfTxErrPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxErrPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wirelessIfTxErrPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wirelessIfTxErrPkts_val_ptr) = raw_wirelessIfTxErrPkts_val;

    return MFD_SUCCESS;
} /* wirelessIfTxErrPkts_map */

/**
 * Extract the current value of the wirelessIfTxErrPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessIfTxErrPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wirelessIfTxErrPkts_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, u_long * wirelessIfTxErrPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wirelessIfTxErrPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wirelessIfTxErrPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessIfTxErrPkts data.
 * set (* wirelessIfTxErrPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wirelessIfTxErrPkts_val_ptr ) = rowreq_ctx->data.wirelessIfTxErrPkts;

    return MFD_SUCCESS;
} /* wirelessIfTxErrPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfstatsEntry.wtpNeighborSSIDList
 * wtpNeighborSSIDList is subid 46 of dot11WtpWirelessIfstatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.5.1.46
 * Description:
The wtp Neighbor SSID List.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNeighborSSIDList.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNeighborSSIDList_map(char **mib_wtpNeighborSSIDList_val_ptr_ptr, size_t *mib_wtpNeighborSSIDList_val_ptr_len_ptr, char *raw_wtpNeighborSSIDList_val_ptr, size_t raw_wtpNeighborSSIDList_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpNeighborSSIDList_val_ptr);
    netsnmp_assert((NULL != mib_wtpNeighborSSIDList_val_ptr_ptr) && (NULL != mib_wtpNeighborSSIDList_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wtpNeighborSSIDList_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpNeighborSSIDList non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpNeighborSSIDList_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpNeighborSSIDList_val_ptr_ptr) || (*mib_wtpNeighborSSIDList_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpNeighborSSIDList_val_ptr_ptr = realloc( *mib_wtpNeighborSSIDList_val_ptr_ptr, converted_len * sizeof(**mib_wtpNeighborSSIDList_val_ptr_ptr));
        if(NULL == *mib_wtpNeighborSSIDList_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpNeighborSSIDList_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpNeighborSSIDList_val_ptr_ptr, raw_wtpNeighborSSIDList_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpNeighborSSIDList_map */

/**
 * Extract the current value of the wtpNeighborSSIDList data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNeighborSSIDList_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpNeighborSSIDList_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpNeighborSSIDList.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpNeighborSSIDList_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpNeighborSSIDList_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpNeighborSSIDList_get( dot11WtpWirelessIfstatsTable_rowreq_ctx *rowreq_ctx, char **wtpNeighborSSIDList_val_ptr_ptr, size_t *wtpNeighborSSIDList_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpNeighborSSIDList_val_ptr_ptr) && (NULL != *wtpNeighborSSIDList_val_ptr_ptr));
   netsnmp_assert( NULL != wtpNeighborSSIDList_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessIfstatsTable:wtpNeighborSSIDList_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpNeighborSSIDList data.
 * set (* wtpNeighborSSIDList_val_ptr_ptr ) and (* wtpNeighborSSIDList_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpNeighborSSIDList data
     */
    if ((NULL == (* wtpNeighborSSIDList_val_ptr_ptr )) ||
        ((* wtpNeighborSSIDList_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpNeighborSSIDList_len * sizeof((* wtpNeighborSSIDList_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpNeighborSSIDList data
         */
        (* wtpNeighborSSIDList_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpNeighborSSIDList_len * sizeof((* wtpNeighborSSIDList_val_ptr_ptr )[0]));
        if(NULL == (* wtpNeighborSSIDList_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpNeighborSSIDList_val_ptr_len_ptr ) = rowreq_ctx->data.wtpNeighborSSIDList_len * sizeof((* wtpNeighborSSIDList_val_ptr_ptr )[0]);
    memcpy( (* wtpNeighborSSIDList_val_ptr_ptr ), rowreq_ctx->data.wtpNeighborSSIDList, (* wtpNeighborSSIDList_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpNeighborSSIDList_get */


/** @} */
