/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11SSIDRateStatsTable.h"
#include "dot11SSIDRateStatsTable_data_access.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "../mibs_public.h"


/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11SSIDRateStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11SSIDRateStatsTable is subid 8 of wtpWlan.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.8, length: 12
*/

/**
 * initialization for dot11SSIDRateStatsTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11SSIDRateStatsTable_reg
 *        Pointer to dot11SSIDRateStatsTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11SSIDRateStatsTable_init_data(dot11SSIDRateStatsTable_registration_ptr dot11SSIDRateStatsTable_reg)
{
    DEBUGMSGTL(("verbose:dot11SSIDRateStatsTable:dot11SSIDRateStatsTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11SSIDRateStatsTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11SSIDRateStatsTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11SSIDRateStatsTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11SSIDRateStatsTable:dot11SSIDRateStatsTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11SSIDRateStatsTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11SSIDRateStatsTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11SSIDRATESTATSTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11SSIDRateStatsTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11SSIDRateStatsTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11SSIDRateStatsTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11SSIDRateStatsTable_cache_load(netsnmp_container *container)
{
	
	snmp_log(LOG_DEBUG, "enter dot11SSIDRateStatsTable_cache_load\n");
	
    dot11SSIDRateStatsTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
         */
        /** 128 - 1(entry) - 1(col) - 1(other indexes) = 111 */
   char   wtpMacAddr[20];
   size_t      wtpMacAddr_len = 0;
        /*
         * wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   wlanCurrID = 0;

    DEBUGMSGTL(("verbose:dot11SSIDRateStatsTable:dot11SSIDRateStatsTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
      //  return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11SSIDRateStatsTable container.
     * loop over your dot11SSIDRateStatsTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */

    char SSIDInDropRate[255] = { 0 };
	size_t SSIDInDropRate_len = 0;
	char SSIDOutDropRate[255] = { 0 };
	size_t SSIDOutDropRate_len = 0;
	char SSIDDownBandWidthRate[255] = { 0 };
	size_t SSIDDownBandWidthRate_len = 0;
	char SSIDUpBandWidthRate[255] = { 0 };
	size_t SSIDUpBandWidthRate_len = 0;
	
    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_wtp_config_of_all_cmd\n");
    messageHead = list_connection_call_dbus_method(show_wtp_config_of_all_cmd, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_wtp_config_of_all_cmd,messageHead=%p\n", messageHead);
	
	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    struct WTP_CONFIG_INFORMATION *WTPconfig = messageNode->message;
		    if(WTPconfig)
		    {
		        struct WTP_CONFIG_INFORMATION *WTPconfig_show = NULL;
    			for(WTPconfig_show = WTPconfig->wtp_config_list; NULL != WTPconfig_show; WTPconfig_show = WTPconfig_show->next)
    			{
    				memset(wtpMacAddr,0,sizeof(wtpMacAddr));
    				if(WTPconfig_show->wtpMacAddr)
    				{
    					snprintf(wtpMacAddr,
    							 sizeof(wtpMacAddr)-1,
    							 "%02X:%02X:%02X:%02X:%02X:%02X",
    							 WTPconfig_show->wtpMacAddr[0],
    							 WTPconfig_show->wtpMacAddr[1],
    							 WTPconfig_show->wtpMacAddr[2],
    							 WTPconfig_show->wtpMacAddr[3],
    							 WTPconfig_show->wtpMacAddr[4],
    							 WTPconfig_show->wtpMacAddr[5]);
    				}

    				if(WTPconfig_show->wlan_info_head)
    				{   
    				    int i  =0;
    				    struct WLAN_INFO *wlan_config = NULL;
    					for(i=0,wlan_config = WTPconfig_show->wlan_info_head;
    						((i<WTPconfig_show->apply_wlan_num)&&(NULL != wlan_config));
    						i++,wlan_config = wlan_config->next)
    					{
    						wlanCurrID = local_to_global_ID(messageNode->parameter, wlan_config->Wlanid, WIRELESS_MAX_NUM);
					    /*
					    ***************************************************
					    ***             START EXAMPLE CODE              ***
					    ***---------------------------------------------***/
					    /*
					     * get a line (skip blank lines)
					     */
					    //do {
					      //  if (!fgets(line, sizeof(line), filep)) {
					            /* we're done */
					        //    fclose(filep);
					          //  filep = NULL;
					       // }
					   // } while (filep && (line[0] == '\n'));

					    /*
					     * check for end of data
					     */
					    //if(NULL == filep)
					      //  break;

					    /*
					     * parse line into variables
					     */
					    /*
					    ***---------------------------------------------***
					    ***              END  EXAMPLE CODE              ***
					    ***************************************************/

					        /*
					         * TODO:352:M: |   |-> set indexes in new dot11SSIDRateStatsTable rowreq context.
					         */
					        rowreq_ctx = dot11SSIDRateStatsTable_allocate_rowreq_ctx();
					        if (NULL == rowreq_ctx) {
					            snmp_log(LOG_ERR, "memory allocation failed\n");
                                free_dbus_message_list(&messageHead, Free_show_wtp_config_of_all_cmd);
					            return MFD_RESOURCE_UNAVAILABLE;
					        }
							wtpMacAddr_len = strlen(wtpMacAddr);
					        if(MFD_SUCCESS != dot11SSIDRateStatsTable_indexes_set(rowreq_ctx
					                               , wtpMacAddr, wtpMacAddr_len
					                               , wlanCurrID
					               )) {
					            snmp_log(LOG_ERR,"error setting index while loading "
					                     "dot11SSIDRateStatsTable cache.\n");
					            dot11SSIDRateStatsTable_release_rowreq_ctx(rowreq_ctx);
					            continue;
					        }

					        /*
					         * TODO:352:r: |   |-> populate dot11SSIDRateStatsTable data context.
					         * Populate data context here. (optionally, delay until row prep)
					         */
					    /*
					     * TRANSIENT or semi-TRANSIENT data:
					     * copy data or save any info needed to do it in row_prep.
					     */
					    /*
					     * setup/save data for SSIDInDropRate
					     * SSIDInDropRate(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
					     */
					    /*
					     * TODO:246:r: |-> Define SSIDInDropRate mapping.
					     * Map values between raw/native values and MIB values
					     *
					     * if(MFD_SUCCESS !=
					     *    SSIDInDropRate_map(&rowreq_ctx->data.SSIDInDropRate, &rowreq_ctx->data.SSIDInDropRate_len,
					     *                SSIDInDropRate, SSIDInDropRate_len, 0)) {
					     *    return MFD_ERROR;
					     * }
					     */
					    /*
					     * make sure there is enough space for SSIDInDropRate data
					     */
					    /*if ((NULL == rowreq_ctx->data.SSIDInDropRate) ||
					        (rowreq_ctx->data.SSIDInDropRate_len < (SSIDInDropRate_len * sizeof(rowreq_ctx->data.SSIDInDropRate[0])))) {
					        snmp_log(LOG_ERR,"not enough space for value\n");
					        return MFD_ERROR;
					    }*/
					    char wtpRxPacketLossRate[10] = { 0 };				
						memset(wtpRxPacketLossRate,0,sizeof(wtpRxPacketLossRate));				
					    if(wlan_config->intotlePkts==0)
							strncpy(wtpRxPacketLossRate,"0.00%",sizeof(wtpRxPacketLossRate)-1);
						else
		                {
							snprintf(wtpRxPacketLossRate,sizeof(wtpRxPacketLossRate)-1,"%.2f",((float)wlan_config->indorpPkts/(float)wlan_config->intotlePkts)*100);
			            	strncat(wtpRxPacketLossRate,"%",sizeof(wtpRxPacketLossRate)-strlen(wtpRxPacketLossRate)-1);
		                }
						
					    SSIDInDropRate_len = MIN(strlen(wtpRxPacketLossRate),sizeof(SSIDInDropRate)-1);
						memset(SSIDInDropRate,0,sizeof(SSIDInDropRate));
						memcpy(SSIDInDropRate,wtpRxPacketLossRate,SSIDInDropRate_len);
			
					    rowreq_ctx->data.SSIDInDropRate_len = SSIDInDropRate_len * sizeof(rowreq_ctx->data.SSIDInDropRate[0]);
					    memcpy( rowreq_ctx->data.SSIDInDropRate, SSIDInDropRate, rowreq_ctx->data.SSIDInDropRate_len );
					    
					    /*
					     * setup/save data for SSIDOutDropRate
					     * SSIDOutDropRate(2)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
					     */
					    /*
					     * TODO:246:r: |-> Define SSIDOutDropRate mapping.
					     * Map values between raw/native values and MIB values
					     *
					     * if(MFD_SUCCESS !=
					     *    SSIDOutDropRate_map(&rowreq_ctx->data.SSIDOutDropRate, &rowreq_ctx->data.SSIDOutDropRate_len,
					     *                SSIDOutDropRate, SSIDOutDropRate_len, 0)) {
					     *    return MFD_ERROR;
					     * }
					     */
					    /*
					     * make sure there is enough space for SSIDOutDropRate data
					     */
					    /*if ((NULL == rowreq_ctx->data.SSIDOutDropRate) ||
					        (rowreq_ctx->data.SSIDOutDropRate_len < (SSIDOutDropRate_len * sizeof(rowreq_ctx->data.SSIDOutDropRate[0])))) {
					        snmp_log(LOG_ERR,"not enough space for value\n");
					        return MFD_ERROR;
					    }*/
						char wtpTxPacketLossRate[10] = { 0 };				
						memset(wtpTxPacketLossRate,0,sizeof(wtpTxPacketLossRate));
					    if(wlan_config->outtotlePkts==0)
							strncpy(wtpTxPacketLossRate,"0.00%",sizeof(wtpTxPacketLossRate)-1);
						else
		                {
							snprintf(wtpTxPacketLossRate,sizeof(wtpTxPacketLossRate)-1,"%.2f",((float)wlan_config->outdorpPkts/(float)wlan_config->outtotlePkts)*100);
			            	strncat(wtpTxPacketLossRate,"%",sizeof(wtpTxPacketLossRate)-strlen(wtpTxPacketLossRate)-1);
		                }
						
					    SSIDOutDropRate_len = MIN(strlen(wtpTxPacketLossRate),sizeof(SSIDOutDropRate)-1);
						memset(SSIDOutDropRate,0,sizeof(SSIDOutDropRate));
						memcpy(SSIDOutDropRate,wtpTxPacketLossRate,SSIDOutDropRate_len);
						
					    rowreq_ctx->data.SSIDOutDropRate_len = SSIDOutDropRate_len * sizeof(rowreq_ctx->data.SSIDOutDropRate[0]);
					    memcpy( rowreq_ctx->data.SSIDOutDropRate, SSIDOutDropRate, rowreq_ctx->data.SSIDOutDropRate_len );
					    
					    /*
					     * setup/save data for SSIDDownBandWidthRate
					     * SSIDDownBandWidthRate(3)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
					     */
					    /*
					     * TODO:246:r: |-> Define SSIDDownBandWidthRate mapping.
					     * Map values between raw/native values and MIB values
					     *
					     * if(MFD_SUCCESS !=
					     *    SSIDDownBandWidthRate_map(&rowreq_ctx->data.SSIDDownBandWidthRate, &rowreq_ctx->data.SSIDDownBandWidthRate_len,
					     *                SSIDDownBandWidthRate, SSIDDownBandWidthRate_len, 0)) {
					     *    return MFD_ERROR;
					     * }
					     */
					    /*
					     * make sure there is enough space for SSIDDownBandWidthRate data
					     */
					    /*if ((NULL == rowreq_ctx->data.SSIDDownBandWidthRate) ||
					        (rowreq_ctx->data.SSIDDownBandWidthRate_len < (SSIDDownBandWidthRate_len * sizeof(rowreq_ctx->data.SSIDDownBandWidthRate[0])))) {
					        snmp_log(LOG_ERR,"not enough space for value\n");
					        return MFD_ERROR;
					    }*/
					    char wtpDownBandwidthUtilization[10] = { 0 };
						memset(wtpDownBandwidthUtilization,0,sizeof(wtpDownBandwidthUtilization));
						snprintf(wtpDownBandwidthUtilization,sizeof(wtpDownBandwidthUtilization)-1,"%d",wlan_config->SSIDDownBandWidthRate);
						strncat(wtpDownBandwidthUtilization,"%",sizeof(wtpDownBandwidthUtilization)-strlen(wtpDownBandwidthUtilization)-1);
						
					    SSIDDownBandWidthRate_len = MIN(strlen(wtpDownBandwidthUtilization),sizeof(SSIDDownBandWidthRate)-1);
						memset(SSIDDownBandWidthRate,0,sizeof(SSIDDownBandWidthRate));
						memcpy(SSIDDownBandWidthRate,wtpDownBandwidthUtilization,SSIDDownBandWidthRate_len);
						
					    rowreq_ctx->data.SSIDDownBandWidthRate_len = SSIDDownBandWidthRate_len * sizeof(rowreq_ctx->data.SSIDDownBandWidthRate[0]);
					    memcpy( rowreq_ctx->data.SSIDDownBandWidthRate, SSIDDownBandWidthRate, rowreq_ctx->data.SSIDDownBandWidthRate_len );
					    
					    /*
					     * setup/save data for SSIDUpBandWidthRate
					     * SSIDUpBandWidthRate(4)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
					     */
					    /*
					     * TODO:246:r: |-> Define SSIDUpBandWidthRate mapping.
					     * Map values between raw/native values and MIB values
					     *
					     * if(MFD_SUCCESS !=
					     *    SSIDUpBandWidthRate_map(&rowreq_ctx->data.SSIDUpBandWidthRate, &rowreq_ctx->data.SSIDUpBandWidthRate_len,
					     *                SSIDUpBandWidthRate, SSIDUpBandWidthRate_len, 0)) {
					     *    return MFD_ERROR;
					     * }
					     */
					    /*
					     * make sure there is enough space for SSIDUpBandWidthRate data
					     */
					    /*if ((NULL == rowreq_ctx->data.SSIDUpBandWidthRate) ||
					        (rowreq_ctx->data.SSIDUpBandWidthRate_len < (SSIDUpBandWidthRate_len * sizeof(rowreq_ctx->data.SSIDUpBandWidthRate[0])))) {
					        snmp_log(LOG_ERR,"not enough space for value\n");
					        return MFD_ERROR;
					    }*/
					    char wtpUpBandwidthUtilization[10] = { 0 };
						memset(wtpUpBandwidthUtilization,0,sizeof(wtpUpBandwidthUtilization));
						snprintf(wtpUpBandwidthUtilization,sizeof(wtpUpBandwidthUtilization)-1,"%d",wlan_config->SSIDUpBandWidthRate);
						strncat(wtpUpBandwidthUtilization,"%",sizeof(wtpUpBandwidthUtilization)-strlen(wtpUpBandwidthUtilization)-1);
						
					    SSIDUpBandWidthRate_len = MIN(strlen(wtpUpBandwidthUtilization),sizeof(SSIDUpBandWidthRate)-1);
						memset(SSIDUpBandWidthRate,0,sizeof(SSIDUpBandWidthRate));
						memcpy(SSIDUpBandWidthRate,wtpUpBandwidthUtilization,SSIDUpBandWidthRate_len);
						
					    rowreq_ctx->data.SSIDUpBandWidthRate_len = SSIDUpBandWidthRate_len * sizeof(rowreq_ctx->data.SSIDUpBandWidthRate[0]);
					    memcpy( rowreq_ctx->data.SSIDUpBandWidthRate, SSIDUpBandWidthRate, rowreq_ctx->data.SSIDUpBandWidthRate_len );
					    
					        
					        /*
					         * insert into table container
					         */
				        if(CONTAINER_INSERT(container, rowreq_ctx))
						{
							dot11SSIDRateStatsTable_release_rowreq_ctx(rowreq_ctx);
						}
				        ++count;
				    }			
		        }
    		}	}
    	}
	    free_dbus_message_list(&messageHead, Free_show_wtp_config_of_all_cmd);
	}
	

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
      //  fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11SSIDRateStatsTable:dot11SSIDRateStatsTable_cache_load",
               "inserted %d records\n", count));
	
	snmp_log(LOG_DEBUG, "exit dot11SSIDRateStatsTable_cache_load\n");

    return MFD_SUCCESS;
} /* dot11SSIDRateStatsTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11SSIDRateStatsTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11SSIDRateStatsTable:dot11SSIDRateStatsTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11SSIDRateStatsTable cache.
     */
} /* dot11SSIDRateStatsTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11SSIDRateStatsTable_row_prep( dot11SSIDRateStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SSIDRateStatsTable:dot11SSIDRateStatsTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11SSIDRateStatsTable_row_prep */

/** @} */
