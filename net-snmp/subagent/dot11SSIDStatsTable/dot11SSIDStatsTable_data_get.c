/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11SSIDStatsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11SSIDStatsTable get routines.
 * TODO:240:M: Implement dot11SSIDStatsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11SSIDStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11SSIDStatsTable is subid 6 of wtpWlan.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11SSIDStatsTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11SSIDStatsTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wlanCurrID
 * wlanCurrID is subid 1 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.1
 * Description:
The wlan's ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wlanCurrID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wlanCurrID_dot11SSIDStatsTable_map(long *mib_wlanCurrID_val_ptr, long raw_wlanCurrID_val)
{
    netsnmp_assert(NULL != mib_wlanCurrID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:wlanCurrID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wlanCurrID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wlanCurrID_val_ptr) = raw_wlanCurrID_val;

    return MFD_SUCCESS;
} /* wlanCurrID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11SSIDStatsTable_indexes_set_tbl_idx(dot11SSIDStatsTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:dot11SSIDStatsTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wlanCurrID = wlanCurrID_val;
    

    return MFD_SUCCESS;
} /* dot11SSIDStatsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11SSIDStatsTable_indexes_set(dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:dot11SSIDStatsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11SSIDStatsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wlanCurrID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11SSIDStatsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11SSIDStatsTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDTxSignalPkts
 * SSIDTxSignalPkts is subid 1 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.1
 * Description:
The total number of transmitted signaling packet.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDTxSignalPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDTxSignalPkts_map(u_long *mib_SSIDTxSignalPkts_val_ptr, u_long raw_SSIDTxSignalPkts_val)
{
    netsnmp_assert(NULL != mib_SSIDTxSignalPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxSignalPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDTxSignalPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDTxSignalPkts_val_ptr) = raw_SSIDTxSignalPkts_val;

    return MFD_SUCCESS;
} /* SSIDTxSignalPkts_map */

/**
 * Extract the current value of the SSIDTxSignalPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDTxSignalPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDTxSignalPkts_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDTxSignalPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDTxSignalPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxSignalPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDTxSignalPkts data.
 * set (* SSIDTxSignalPkts_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDTxSignalPkts_val_ptr ) = rowreq_ctx->data.SSIDTxSignalPkts;

    return MFD_SUCCESS;
} /* SSIDTxSignalPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDRxSignalPkts
 * SSIDRxSignalPkts is subid 2 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.2
 * Description:
The total number of received signaling packet.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDRxSignalPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDRxSignalPkts_map(u_long *mib_SSIDRxSignalPkts_val_ptr, u_long raw_SSIDRxSignalPkts_val)
{
    netsnmp_assert(NULL != mib_SSIDRxSignalPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxSignalPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDRxSignalPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDRxSignalPkts_val_ptr) = raw_SSIDRxSignalPkts_val;

    return MFD_SUCCESS;
} /* SSIDRxSignalPkts_map */

/**
 * Extract the current value of the SSIDRxSignalPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDRxSignalPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDRxSignalPkts_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDRxSignalPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDRxSignalPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxSignalPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDRxSignalPkts data.
 * set (* SSIDRxSignalPkts_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDRxSignalPkts_val_ptr ) = rowreq_ctx->data.SSIDRxSignalPkts;

    return MFD_SUCCESS;
} /* SSIDRxSignalPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDTxDataPkts
 * SSIDTxDataPkts is subid 3 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.3
 * Description:
The total number of transmitted data packet.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDTxDataPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDTxDataPkts_map(u_long *mib_SSIDTxDataPkts_val_ptr, u_long raw_SSIDTxDataPkts_val)
{
    netsnmp_assert(NULL != mib_SSIDTxDataPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxDataPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDTxDataPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDTxDataPkts_val_ptr) = raw_SSIDTxDataPkts_val;

    return MFD_SUCCESS;
} /* SSIDTxDataPkts_map */

/**
 * Extract the current value of the SSIDTxDataPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDTxDataPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDTxDataPkts_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDTxDataPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDTxDataPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxDataPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDTxDataPkts data.
 * set (* SSIDTxDataPkts_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDTxDataPkts_val_ptr ) = rowreq_ctx->data.SSIDTxDataPkts;

    return MFD_SUCCESS;
} /* SSIDTxDataPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDRxDataPkts
 * SSIDRxDataPkts is subid 4 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.4
 * Description:
The total number of received signaling packet.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDRxDataPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDRxDataPkts_map(u_long *mib_SSIDRxDataPkts_val_ptr, u_long raw_SSIDRxDataPkts_val)
{
    netsnmp_assert(NULL != mib_SSIDRxDataPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxDataPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDRxDataPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDRxDataPkts_val_ptr) = raw_SSIDRxDataPkts_val;

    return MFD_SUCCESS;
} /* SSIDRxDataPkts_map */

/**
 * Extract the current value of the SSIDRxDataPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDRxDataPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDRxDataPkts_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDRxDataPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDRxDataPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxDataPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDRxDataPkts data.
 * set (* SSIDRxDataPkts_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDRxDataPkts_val_ptr ) = rowreq_ctx->data.SSIDRxDataPkts;

    return MFD_SUCCESS;
} /* SSIDRxDataPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDUplinkDataOctets
 * SSIDUplinkDataOctets is subid 5 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.5
 * Description:
Number of bytes received.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDUplinkDataOctets.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDUplinkDataOctets_map(U64 *mib_SSIDUplinkDataOctets_val_ptr, U64 raw_SSIDUplinkDataOctets_val)
{
    netsnmp_assert(NULL != mib_SSIDUplinkDataOctets_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDUplinkDataOctets_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDUplinkDataOctets mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDUplinkDataOctets_val_ptr) = raw_SSIDUplinkDataOctets_val;

    return MFD_SUCCESS;
} /* SSIDUplinkDataOctets_map */

/**
 * Extract the current value of the SSIDUplinkDataOctets data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDUplinkDataOctets_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDUplinkDataOctets_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, unsigned long long * SSIDUplinkDataOctets_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDUplinkDataOctets_val_ptr );

/*
 * TODO:231:o: |-> copy SSIDUplinkDataOctets data.
 * get (* SSIDUplinkDataOctets_val_ptr ).low and (* SSIDUplinkDataOctets_val_ptr ).high from rowreq_ctx->data
 */
    //(* SSIDUplinkDataOctets_val_ptr ).high = rowreq_ctx->data.SSIDUplinkDataOctets.high;
    //(* SSIDUplinkDataOctets_val_ptr ).low = rowreq_ctx->data.SSIDUplinkDataOctets.low;    
	* SSIDUplinkDataOctets_val_ptr = rowreq_ctx->data.SSIDUplinkDataOctets;

    return MFD_SUCCESS;
} /* SSIDUplinkDataOctets_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDDwlinkDataOctets
 * SSIDDwlinkDataOctets is subid 6 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.6
 * Description:
Number of bytes sent.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDDwlinkDataOctets.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDDwlinkDataOctets_map(U64 *mib_SSIDDwlinkDataOctets_val_ptr, U64 raw_SSIDDwlinkDataOctets_val)
{
    netsnmp_assert(NULL != mib_SSIDDwlinkDataOctets_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDDwlinkDataOctets_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDDwlinkDataOctets mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDDwlinkDataOctets_val_ptr) = raw_SSIDDwlinkDataOctets_val;

    return MFD_SUCCESS;
} /* SSIDDwlinkDataOctets_map */

/**
 * Extract the current value of the SSIDDwlinkDataOctets data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDDwlinkDataOctets_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDDwlinkDataOctets_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, unsigned long long * SSIDDwlinkDataOctets_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDDwlinkDataOctets_val_ptr );

/*
 * TODO:231:o: |-> copy SSIDDwlinkDataOctets data.
 * get (* SSIDDwlinkDataOctets_val_ptr ).low and (* SSIDDwlinkDataOctets_val_ptr ).high from rowreq_ctx->data
 */
    //(* SSIDDwlinkDataOctets_val_ptr ).high = rowreq_ctx->data.SSIDDwlinkDataOctets.high;
    //(* SSIDDwlinkDataOctets_val_ptr ).low = rowreq_ctx->data.SSIDDwlinkDataOctets.low;
	* SSIDDwlinkDataOctets_val_ptr = rowreq_ctx->data.SSIDDwlinkDataOctets;

    return MFD_SUCCESS;
} /* SSIDDwlinkDataOctets_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDChStatsDwlinkTotRetryPkts
 * SSIDChStatsDwlinkTotRetryPkts is subid 7 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.7
 * Description:
Downlink packet retransmission number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDChStatsDwlinkTotRetryPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDChStatsDwlinkTotRetryPkts_map(u_long *mib_SSIDChStatsDwlinkTotRetryPkts_val_ptr, u_long raw_SSIDChStatsDwlinkTotRetryPkts_val)
{
    netsnmp_assert(NULL != mib_SSIDChStatsDwlinkTotRetryPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDChStatsDwlinkTotRetryPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDChStatsDwlinkTotRetryPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDChStatsDwlinkTotRetryPkts_val_ptr) = raw_SSIDChStatsDwlinkTotRetryPkts_val;

    return MFD_SUCCESS;
} /* SSIDChStatsDwlinkTotRetryPkts_map */

/**
 * Extract the current value of the SSIDChStatsDwlinkTotRetryPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDChStatsDwlinkTotRetryPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDChStatsDwlinkTotRetryPkts_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDChStatsDwlinkTotRetryPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDChStatsDwlinkTotRetryPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDChStatsDwlinkTotRetryPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDChStatsDwlinkTotRetryPkts data.
 * set (* SSIDChStatsDwlinkTotRetryPkts_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDChStatsDwlinkTotRetryPkts_val_ptr ) = rowreq_ctx->data.SSIDChStatsDwlinkTotRetryPkts;

    return MFD_SUCCESS;
} /* SSIDChStatsDwlinkTotRetryPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDChStatsUplinkUniFrameCnt
 * SSIDChStatsUplinkUniFrameCnt is subid 8 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.8
 * Description:
Uplink unicast frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDChStatsUplinkUniFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDChStatsUplinkUniFrameCnt_map(u_long *mib_SSIDChStatsUplinkUniFrameCnt_val_ptr, u_long raw_SSIDChStatsUplinkUniFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDChStatsUplinkUniFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDChStatsUplinkUniFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDChStatsUplinkUniFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDChStatsUplinkUniFrameCnt_val_ptr) = raw_SSIDChStatsUplinkUniFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDChStatsUplinkUniFrameCnt_map */

/**
 * Extract the current value of the SSIDChStatsUplinkUniFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDChStatsUplinkUniFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDChStatsUplinkUniFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDChStatsUplinkUniFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDChStatsUplinkUniFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDChStatsUplinkUniFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDChStatsUplinkUniFrameCnt data.
 * set (* SSIDChStatsUplinkUniFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDChStatsUplinkUniFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDChStatsUplinkUniFrameCnt;

    return MFD_SUCCESS;
} /* SSIDChStatsUplinkUniFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDChStatsDwlinkUniFrameCnt
 * SSIDChStatsDwlinkUniFrameCnt is subid 9 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.9
 * Description:
Downlink unicast frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDChStatsDwlinkUniFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDChStatsDwlinkUniFrameCnt_map(u_long *mib_SSIDChStatsDwlinkUniFrameCnt_val_ptr, u_long raw_SSIDChStatsDwlinkUniFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDChStatsDwlinkUniFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDChStatsDwlinkUniFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDChStatsDwlinkUniFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDChStatsDwlinkUniFrameCnt_val_ptr) = raw_SSIDChStatsDwlinkUniFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDChStatsDwlinkUniFrameCnt_map */

/**
 * Extract the current value of the SSIDChStatsDwlinkUniFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDChStatsDwlinkUniFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDChStatsDwlinkUniFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDChStatsDwlinkUniFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDChStatsDwlinkUniFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDChStatsDwlinkUniFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDChStatsDwlinkUniFrameCnt data.
 * set (* SSIDChStatsDwlinkUniFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDChStatsDwlinkUniFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDChStatsDwlinkUniFrameCnt;

    return MFD_SUCCESS;
} /* SSIDChStatsDwlinkUniFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDUpChStatsFrameNonUniFrameCnt
 * SSIDUpChStatsFrameNonUniFrameCnt is subid 10 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.10
 * Description:
Uplink unicast frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDUpChStatsFrameNonUniFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDUpChStatsFrameNonUniFrameCnt_map(u_long *mib_SSIDUpChStatsFrameNonUniFrameCnt_val_ptr, u_long raw_SSIDUpChStatsFrameNonUniFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDUpChStatsFrameNonUniFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDUpChStatsFrameNonUniFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDUpChStatsFrameNonUniFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDUpChStatsFrameNonUniFrameCnt_val_ptr) = raw_SSIDUpChStatsFrameNonUniFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDUpChStatsFrameNonUniFrameCnt_map */

/**
 * Extract the current value of the SSIDUpChStatsFrameNonUniFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDUpChStatsFrameNonUniFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDUpChStatsFrameNonUniFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDUpChStatsFrameNonUniFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDUpChStatsFrameNonUniFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDUpChStatsFrameNonUniFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDUpChStatsFrameNonUniFrameCnt data.
 * set (* SSIDUpChStatsFrameNonUniFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDUpChStatsFrameNonUniFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDUpChStatsFrameNonUniFrameCnt;

    return MFD_SUCCESS;
} /* SSIDUpChStatsFrameNonUniFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDDownChStatsFrameNonUniFrameCnt
 * SSIDDownChStatsFrameNonUniFrameCnt is subid 11 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.11
 * Description:
Downlink unicast frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDDownChStatsFrameNonUniFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDDownChStatsFrameNonUniFrameCnt_map(u_long *mib_SSIDDownChStatsFrameNonUniFrameCnt_val_ptr, u_long raw_SSIDDownChStatsFrameNonUniFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDDownChStatsFrameNonUniFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDDownChStatsFrameNonUniFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDDownChStatsFrameNonUniFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDDownChStatsFrameNonUniFrameCnt_val_ptr) = raw_SSIDDownChStatsFrameNonUniFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDDownChStatsFrameNonUniFrameCnt_map */

/**
 * Extract the current value of the SSIDDownChStatsFrameNonUniFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDDownChStatsFrameNonUniFrameCnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDDownChStatsFrameNonUniFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDDownChStatsFrameNonUniFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDDownChStatsFrameNonUniFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDDownChStatsFrameNonUniFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDDownChStatsFrameNonUniFrameCnt data.
 * set (* SSIDDownChStatsFrameNonUniFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDDownChStatsFrameNonUniFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDDownChStatsFrameNonUniFrameCnt;

    return MFD_SUCCESS;
} /* SSIDDownChStatsFrameNonUniFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDApChStatsFrameFragRate
 * SSIDApChStatsFrameFragRate is subid 12 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.12
 * Description:
Sub-frame rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDApChStatsFrameFragRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDApChStatsFrameFragRate_map(u_long *mib_SSIDApChStatsFrameFragRate_val_ptr, u_long raw_SSIDApChStatsFrameFragRate_val)
{
    netsnmp_assert(NULL != mib_SSIDApChStatsFrameFragRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDApChStatsFrameFragRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDApChStatsFrameFragRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDApChStatsFrameFragRate_val_ptr) = raw_SSIDApChStatsFrameFragRate_val;

    return MFD_SUCCESS;
} /* SSIDApChStatsFrameFragRate_map */

/**
 * Extract the current value of the SSIDApChStatsFrameFragRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDApChStatsFrameFragRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDApChStatsFrameFragRate_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDApChStatsFrameFragRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDApChStatsFrameFragRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDApChStatsFrameFragRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDApChStatsFrameFragRate data.
 * set (* SSIDApChStatsFrameFragRate_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDApChStatsFrameFragRate_val_ptr ) = rowreq_ctx->data.SSIDApChStatsFrameFragRate;

    return MFD_SUCCESS;
} /* SSIDApChStatsFrameFragRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDRxCtrlFrameCnt
 * SSIDRxCtrlFrameCnt is subid 13 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.13
 * Description:
Received the number of control frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDRxCtrlFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDRxCtrlFrameCnt_map(u_long *mib_SSIDRxCtrlFrameCnt_val_ptr, u_long raw_SSIDRxCtrlFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDRxCtrlFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxCtrlFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDRxCtrlFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDRxCtrlFrameCnt_val_ptr) = raw_SSIDRxCtrlFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDRxCtrlFrameCnt_map */

/**
 * Extract the current value of the SSIDRxCtrlFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDRxCtrlFrameCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDRxCtrlFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDRxCtrlFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDRxCtrlFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxCtrlFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDRxCtrlFrameCnt data.
 * set (* SSIDRxCtrlFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDRxCtrlFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDRxCtrlFrameCnt;

    return MFD_SUCCESS;
} /* SSIDRxCtrlFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDRxDataFrameCnt
 * SSIDRxDataFrameCnt is subid 14 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.14
 * Description:
Received the number of data frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDRxDataFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDRxDataFrameCnt_map(u_long *mib_SSIDRxDataFrameCnt_val_ptr, u_long raw_SSIDRxDataFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDRxDataFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxDataFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDRxDataFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDRxDataFrameCnt_val_ptr) = raw_SSIDRxDataFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDRxDataFrameCnt_map */

/**
 * Extract the current value of the SSIDRxDataFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDRxDataFrameCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDRxDataFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDRxDataFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDRxDataFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxDataFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDRxDataFrameCnt data.
 * set (* SSIDRxDataFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDRxDataFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDRxDataFrameCnt;

    return MFD_SUCCESS;
} /* SSIDRxDataFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDRxAuthenFrameCnt
 * SSIDRxAuthenFrameCnt is subid 15 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.15
 * Description:
Received certification of the number of frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDRxAuthenFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDRxAuthenFrameCnt_map(u_long *mib_SSIDRxAuthenFrameCnt_val_ptr, u_long raw_SSIDRxAuthenFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDRxAuthenFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxAuthenFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDRxAuthenFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDRxAuthenFrameCnt_val_ptr) = raw_SSIDRxAuthenFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDRxAuthenFrameCnt_map */

/**
 * Extract the current value of the SSIDRxAuthenFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDRxAuthenFrameCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDRxAuthenFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDRxAuthenFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDRxAuthenFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxAuthenFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDRxAuthenFrameCnt data.
 * set (* SSIDRxAuthenFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDRxAuthenFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDRxAuthenFrameCnt;

    return MFD_SUCCESS;
} /* SSIDRxAuthenFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDRxAssociateFrameCnt
 * SSIDRxAssociateFrameCnt is subid 16 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.16
 * Description:
Received the number of frames associated.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDRxAssociateFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDRxAssociateFrameCnt_map(u_long *mib_SSIDRxAssociateFrameCnt_val_ptr, u_long raw_SSIDRxAssociateFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDRxAssociateFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxAssociateFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDRxAssociateFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDRxAssociateFrameCnt_val_ptr) = raw_SSIDRxAssociateFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDRxAssociateFrameCnt_map */

/**
 * Extract the current value of the SSIDRxAssociateFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDRxAssociateFrameCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDRxAssociateFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDRxAssociateFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDRxAssociateFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDRxAssociateFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDRxAssociateFrameCnt data.
 * set (* SSIDRxAssociateFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDRxAssociateFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDRxAssociateFrameCnt;

    return MFD_SUCCESS;
} /* SSIDRxAssociateFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDTxCtrlFrameCnt
 * SSIDTxCtrlFrameCnt is subid 17 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.17
 * Description:
Send the number of control frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDTxCtrlFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDTxCtrlFrameCnt_map(u_long *mib_SSIDTxCtrlFrameCnt_val_ptr, u_long raw_SSIDTxCtrlFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDTxCtrlFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxCtrlFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDTxCtrlFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDTxCtrlFrameCnt_val_ptr) = raw_SSIDTxCtrlFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDTxCtrlFrameCnt_map */

/**
 * Extract the current value of the SSIDTxCtrlFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDTxCtrlFrameCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDTxCtrlFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDTxCtrlFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDTxCtrlFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxCtrlFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDTxCtrlFrameCnt data.
 * set (* SSIDTxCtrlFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDTxCtrlFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDTxCtrlFrameCnt;

    return MFD_SUCCESS;
} /* SSIDTxCtrlFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDTxDataFrameCnt
 * SSIDTxDataFrameCnt is subid 18 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.18
 * Description:
Sent the number of data frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDTxDataFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDTxDataFrameCnt_map(u_long *mib_SSIDTxDataFrameCnt_val_ptr, u_long raw_SSIDTxDataFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDTxDataFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxDataFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDTxDataFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDTxDataFrameCnt_val_ptr) = raw_SSIDTxDataFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDTxDataFrameCnt_map */

/**
 * Extract the current value of the SSIDTxDataFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDTxDataFrameCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDTxDataFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDTxDataFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDTxDataFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxDataFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDTxDataFrameCnt data.
 * set (* SSIDTxDataFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDTxDataFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDTxDataFrameCnt;

    return MFD_SUCCESS;
} /* SSIDTxDataFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDTxAuthenFrameCnt
 * SSIDTxAuthenFrameCnt is subid 19 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.19
 * Description:
Send the number of frames of the certification.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDTxAuthenFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDTxAuthenFrameCnt_map(u_long *mib_SSIDTxAuthenFrameCnt_val_ptr, u_long raw_SSIDTxAuthenFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDTxAuthenFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxAuthenFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDTxAuthenFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDTxAuthenFrameCnt_val_ptr) = raw_SSIDTxAuthenFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDTxAuthenFrameCnt_map */

/**
 * Extract the current value of the SSIDTxAuthenFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDTxAuthenFrameCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDTxAuthenFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDTxAuthenFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDTxAuthenFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxAuthenFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDTxAuthenFrameCnt data.
 * set (* SSIDTxAuthenFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDTxAuthenFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDTxAuthenFrameCnt;

    return MFD_SUCCESS;
} /* SSIDTxAuthenFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDTxAssociateFrameCnt
 * SSIDTxAssociateFrameCnt is subid 20 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.20
 * Description:
Send the link of the number of frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDTxAssociateFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDTxAssociateFrameCnt_map(u_long *mib_SSIDTxAssociateFrameCnt_val_ptr, u_long raw_SSIDTxAssociateFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDTxAssociateFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxAssociateFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDTxAssociateFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDTxAssociateFrameCnt_val_ptr) = raw_SSIDTxAssociateFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDTxAssociateFrameCnt_map */

/**
 * Extract the current value of the SSIDTxAssociateFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDTxAssociateFrameCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDTxAssociateFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDTxAssociateFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDTxAssociateFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDTxAssociateFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDTxAssociateFrameCnt data.
 * set (* SSIDTxAssociateFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDTxAssociateFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDTxAssociateFrameCnt;

    return MFD_SUCCESS;
} /* SSIDTxAssociateFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDApChStatsNumStations
 * SSIDApChStatsNumStations is subid 21 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.21
 * Description:
Current SSID to connect to the terminal number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDApChStatsNumStations.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDApChStatsNumStations_map(u_long *mib_SSIDApChStatsNumStations_val_ptr, u_long raw_SSIDApChStatsNumStations_val)
{
    netsnmp_assert(NULL != mib_SSIDApChStatsNumStations_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDApChStatsNumStations_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDApChStatsNumStations mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDApChStatsNumStations_val_ptr) = raw_SSIDApChStatsNumStations_val;

    return MFD_SUCCESS;
} /* SSIDApChStatsNumStations_map */

/**
 * Extract the current value of the SSIDApChStatsNumStations data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDApChStatsNumStations_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDApChStatsNumStations_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDApChStatsNumStations_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDApChStatsNumStations_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDApChStatsNumStations_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDApChStatsNumStations data.
 * set (* SSIDApChStatsNumStations_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDApChStatsNumStations_val_ptr ) = rowreq_ctx->data.SSIDApChStatsNumStations;

    return MFD_SUCCESS;
} /* SSIDApChStatsNumStations_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDDwlinkTotFrameCnt
 * SSIDDwlinkTotFrameCnt is subid 22 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.22
 * Description:
SSID DOWN TOTLE of frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDDwlinkTotFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDDwlinkTotFrameCnt_map(u_long *mib_SSIDDwlinkTotFrameCnt_val_ptr, u_long raw_SSIDDwlinkTotFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDDwlinkTotFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDDwlinkTotFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDDwlinkTotFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDDwlinkTotFrameCnt_val_ptr) = raw_SSIDDwlinkTotFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDDwlinkTotFrameCnt_map */

/**
 * Extract the current value of the SSIDDwlinkTotFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDDwlinkTotFrameCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDDwlinkTotFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDDwlinkTotFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDDwlinkTotFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDDwlinkTotFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDDwlinkTotFrameCnt data.
 * set (* SSIDDwlinkTotFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDDwlinkTotFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDDwlinkTotFrameCnt;

    return MFD_SUCCESS;
} /* SSIDDwlinkTotFrameCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDUplinkTotFrameCnt
 * SSIDUplinkTotFrameCnt is subid 23 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.23
 * Description:
SSID UP TOTLE of frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDUplinkTotFrameCnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDUplinkTotFrameCnt_map(u_long *mib_SSIDUplinkTotFrameCnt_val_ptr, u_long raw_SSIDUplinkTotFrameCnt_val)
{
    netsnmp_assert(NULL != mib_SSIDUplinkTotFrameCnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDUplinkTotFrameCnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDUplinkTotFrameCnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDUplinkTotFrameCnt_val_ptr) = raw_SSIDUplinkTotFrameCnt_val;

    return MFD_SUCCESS;
} /* SSIDUplinkTotFrameCnt_map */

/**
 * Extract the current value of the SSIDUplinkTotFrameCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDUplinkTotFrameCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDUplinkTotFrameCnt_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDUplinkTotFrameCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDUplinkTotFrameCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDUplinkTotFrameCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDUplinkTotFrameCnt data.
 * set (* SSIDUplinkTotFrameCnt_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDUplinkTotFrameCnt_val_ptr ) = rowreq_ctx->data.SSIDUplinkTotFrameCnt;

    return MFD_SUCCESS;
} /* SSIDUplinkTotFrameCnt_get */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDStatsEntry.SSIDAssociatedUserNum
 * SSIDAssociatedUserNum is subid 24 of dot11SSIDStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.6.1.24
 * Description:
SSID UP TOTLE of frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDAssociatedUserNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDAssociatedUserNum_map(u_long *mib_SSIDAssociatedUserNum_val_ptr, u_long raw_SSIDAssociatedUserNum_val)
{
    netsnmp_assert(NULL != mib_SSIDAssociatedUserNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDAssociatedUserNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDAssociatedUserNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDAssociatedUserNum_val_ptr) = raw_SSIDAssociatedUserNum_val;

    return MFD_SUCCESS;
} /* SSIDAssociatedUserNum_map */

/**
 * Extract the current value of the SSIDAssociatedUserNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDAssociatedUserNum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDAssociatedUserNum_get( dot11SSIDStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDAssociatedUserNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDAssociatedUserNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDStatsTable:SSIDAssociatedUserNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:241:o: |-> Extract the current value of the SSIDAssociatedUserNum data.
 * set (* SSIDAssociatedUserNum_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDAssociatedUserNum_val_ptr ) = rowreq_ctx->data.SSIDAssociatedUserNum;

    return MFD_SUCCESS;
} /* SSIDAssociatedUserNum_get */


/** @} */
