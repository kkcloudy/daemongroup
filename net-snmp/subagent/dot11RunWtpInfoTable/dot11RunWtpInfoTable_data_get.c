/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id: dot11RunWtpInfoTable_data_get.c,v 1.1 2010/07/02 03:37:27 qiaojie Exp $
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11RunWtpInfoTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11RunWtpInfoTable get routines.
 * TODO:240:M: Implement dot11RunWtpInfoTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11RunWtpInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11RunWtpInfoTable is subid 8 of wtpStats.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.8, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11RunWtpInfoTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RunWtpInfoEntry.RunWtpMacAddr
 * RunWtpMacAddr is subid 1 of dot11RunWtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.8.1.1
 * Description:
Run AP MAC.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RunWtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RunWtpMacAddr_map(char **mib_RunWtpMacAddr_val_ptr_ptr, size_t *mib_RunWtpMacAddr_val_ptr_len_ptr, char *raw_RunWtpMacAddr_val_ptr, size_t raw_RunWtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_RunWtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_RunWtpMacAddr_val_ptr_ptr) && (NULL != mib_RunWtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RunWtpInfoTable:RunWtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement RunWtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_RunWtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_RunWtpMacAddr_val_ptr_ptr) || (*mib_RunWtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_RunWtpMacAddr_val_ptr_ptr = realloc( *mib_RunWtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_RunWtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_RunWtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_RunWtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_RunWtpMacAddr_val_ptr_ptr, raw_RunWtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* RunWtpMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11RunWtpInfoTable_indexes_set_tbl_idx(dot11RunWtpInfoTable_mib_index *tbl_idx, char *RunWtpMacAddr_val_ptr,  size_t RunWtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11RunWtpInfoTable:dot11RunWtpInfoTable_indexes_set_tbl_idx","called\n"));

    /* RunWtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->RunWtpMacAddr_len = sizeof(tbl_idx->RunWtpMacAddr);
    /*
     * make sure there is enough space for RunWtpMacAddr data
     */
    if ((NULL == tbl_idx->RunWtpMacAddr) ||
        (tbl_idx->RunWtpMacAddr_len < (RunWtpMacAddr_val_ptr_len * sizeof(tbl_idx->RunWtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->RunWtpMacAddr_len = RunWtpMacAddr_val_ptr_len * sizeof(tbl_idx->RunWtpMacAddr[0]);
    memcpy( tbl_idx->RunWtpMacAddr, RunWtpMacAddr_val_ptr, tbl_idx->RunWtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11RunWtpInfoTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11RunWtpInfoTable_indexes_set(dot11RunWtpInfoTable_rowreq_ctx *rowreq_ctx, char *RunWtpMacAddr_val_ptr,  size_t RunWtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11RunWtpInfoTable:dot11RunWtpInfoTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11RunWtpInfoTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , RunWtpMacAddr_val_ptr, RunWtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11RunWtpInfoTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11RunWtpInfoTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RunWtpInfoEntry.RunWtpID
 * RunWtpID is subid 2 of dot11RunWtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.8.1.2
 * Description:
Run AP ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RunWtpID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RunWtpID_map(long *mib_RunWtpID_val_ptr, long raw_RunWtpID_val)
{
    netsnmp_assert(NULL != mib_RunWtpID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RunWtpInfoTable:RunWtpID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RunWtpID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_RunWtpID_val_ptr) = raw_RunWtpID_val;

    return MFD_SUCCESS;
} /* RunWtpID_map */

/**
 * Extract the current value of the RunWtpID data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RunWtpID_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
RunWtpID_get( dot11RunWtpInfoTable_rowreq_ctx *rowreq_ctx, long * RunWtpID_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != RunWtpID_val_ptr );


    DEBUGMSGTL(("verbose:dot11RunWtpInfoTable:RunWtpID_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RunWtpID data.
 * set (* RunWtpID_val_ptr ) from rowreq_ctx->data
 */
    (* RunWtpID_val_ptr ) = rowreq_ctx->data.RunWtpID;

    return MFD_SUCCESS;
} /* RunWtpID_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RunWtpInfoEntry.RunWtpIP
 * RunWtpIP is subid 3 of dot11RunWtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.8.1.3
 * Description:
Run AP IP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RunWtpIP.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RunWtpIP_map(u_long *mib_RunWtpIP_val_ptr, u_long raw_RunWtpIP_val)
{
    netsnmp_assert(NULL != mib_RunWtpIP_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RunWtpInfoTable:RunWtpIP_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RunWtpIP mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_RunWtpIP_val_ptr) = raw_RunWtpIP_val;

    return MFD_SUCCESS;
} /* RunWtpIP_map */

/**
 * Extract the current value of the RunWtpIP data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RunWtpIP_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
RunWtpIP_get( dot11RunWtpInfoTable_rowreq_ctx *rowreq_ctx, u_long * RunWtpIP_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != RunWtpIP_val_ptr );


    DEBUGMSGTL(("verbose:dot11RunWtpInfoTable:RunWtpIP_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RunWtpIP data.
 * set (* RunWtpIP_val_ptr ) from rowreq_ctx->data
 */
    (* RunWtpIP_val_ptr ) = rowreq_ctx->data.RunWtpIP;

    return MFD_SUCCESS;
} /* RunWtpIP_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RunWtpInfoEntry.RunWtpSN
 * RunWtpSN is subid 4 of dot11RunWtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.8.1.4
 * Description:
Run AP SN.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RunWtpSN.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RunWtpSN_map(char **mib_RunWtpSN_val_ptr_ptr, size_t *mib_RunWtpSN_val_ptr_len_ptr, char *raw_RunWtpSN_val_ptr, size_t raw_RunWtpSN_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_RunWtpSN_val_ptr);
    netsnmp_assert((NULL != mib_RunWtpSN_val_ptr_ptr) && (NULL != mib_RunWtpSN_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RunWtpInfoTable:RunWtpSN_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement RunWtpSN non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_RunWtpSN_val_ptr_len; /* assume equal */
    if((NULL == *mib_RunWtpSN_val_ptr_ptr) || (*mib_RunWtpSN_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_RunWtpSN_val_ptr_ptr = realloc( *mib_RunWtpSN_val_ptr_ptr, converted_len * sizeof(**mib_RunWtpSN_val_ptr_ptr));
        if(NULL == *mib_RunWtpSN_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_RunWtpSN_val_ptr_len_ptr = converted_len;
    memcpy( *mib_RunWtpSN_val_ptr_ptr, raw_RunWtpSN_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* RunWtpSN_map */

/**
 * Extract the current value of the RunWtpSN data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RunWtpSN_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param RunWtpSN_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by RunWtpSN.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*RunWtpSN_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update RunWtpSN_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
RunWtpSN_get( dot11RunWtpInfoTable_rowreq_ctx *rowreq_ctx, char **RunWtpSN_val_ptr_ptr, size_t *RunWtpSN_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != RunWtpSN_val_ptr_ptr) && (NULL != *RunWtpSN_val_ptr_ptr));
   netsnmp_assert( NULL != RunWtpSN_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RunWtpInfoTable:RunWtpSN_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RunWtpSN data.
 * set (* RunWtpSN_val_ptr_ptr ) and (* RunWtpSN_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for RunWtpSN data
     */
    if ((NULL == (* RunWtpSN_val_ptr_ptr )) ||
        ((* RunWtpSN_val_ptr_len_ptr ) < (rowreq_ctx->data.RunWtpSN_len * sizeof((* RunWtpSN_val_ptr_ptr )[0])))) {
        /*
         * allocate space for RunWtpSN data
         */
        (* RunWtpSN_val_ptr_ptr ) = malloc(rowreq_ctx->data.RunWtpSN_len * sizeof((* RunWtpSN_val_ptr_ptr )[0]));
        if(NULL == (* RunWtpSN_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* RunWtpSN_val_ptr_len_ptr ) = rowreq_ctx->data.RunWtpSN_len * sizeof((* RunWtpSN_val_ptr_ptr )[0]);
    memcpy( (* RunWtpSN_val_ptr_ptr ), rowreq_ctx->data.RunWtpSN, (* RunWtpSN_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* RunWtpSN_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RunWtpInfoEntry.RunWtpState
 * RunWtpState is subid 5 of dot11RunWtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.8.1.5
 * Description:
Run AP state.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 1/8. Values:  run(5)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RunWtpState.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RunWtpState_map(u_long *mib_RunWtpState_val_ptr, u_long raw_RunWtpState_val)
{
    netsnmp_assert(NULL != mib_RunWtpState_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RunWtpInfoTable:RunWtpState_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RunWtpState enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_RunWtpState_val) {
        case INTERNAL_RUNWTPSTATE_RUN:
             *mib_RunWtpState_val_ptr = RUNWTPSTATE_RUN;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for RunWtpState\n", raw_RunWtpState_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* RunWtpState_map */

/**
 * Extract the current value of the RunWtpState data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RunWtpState_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
RunWtpState_get( dot11RunWtpInfoTable_rowreq_ctx *rowreq_ctx, u_long * RunWtpState_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != RunWtpState_val_ptr );


    DEBUGMSGTL(("verbose:dot11RunWtpInfoTable:RunWtpState_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RunWtpState data.
 * set (* RunWtpState_val_ptr ) from rowreq_ctx->data
 */
    (* RunWtpState_val_ptr ) = rowreq_ctx->data.RunWtpState;

    return MFD_SUCCESS;
} /* RunWtpState_get */



/** @} */
