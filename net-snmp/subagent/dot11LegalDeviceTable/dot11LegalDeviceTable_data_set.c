/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11LegalDeviceTable.h"
#include "wcpss/wid/WID.h"
#include "ws_dcli_ac.h"
#include "autelanWtpGroup.h"
#include "ws_init_dbus.h"
#include "ws_dbus_list_interface.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11LegalDeviceTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11LegalDeviceTable is subid 1 of dot11WhiteBlackList.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11LegalDeviceTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11LegalDeviceTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11LegalDeviceTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11LegalDeviceTable_undo_setup( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:dot11LegalDeviceTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11LegalDeviceTable undo.
     * set up dot11LegalDeviceTable undo information, in preparation for a set.
     * Undo storage is in (* legalRowStatus_val_ptr )*
     */

    return rc;
} /* dot11LegalDeviceTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11LegalDeviceTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11LegalDeviceTable_undo( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:dot11LegalDeviceTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11LegalDeviceTable undo.
     * dot11LegalDeviceTable undo information, in response to a failed set.
     * Undo storage is in (* legalRowStatus_val_ptr )*
     */

    return rc;
} /* dot11LegalDeviceTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11LegalDeviceTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11LegalDeviceTable_undo_cleanup( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:dot11LegalDeviceTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11LegalDeviceTable undo.
     * Undo storage is in (* legalRowStatus_val_ptr )*
     */

    return rc;
} /* dot11LegalDeviceTable_undo_cleanup */

static int
check_create_ready(dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx) {
    if(17 == strlen(rowreq_ctx->data.legalMacAddress)) {
        return MFD_SUCCESS;
    }

    return MFD_ERROR;
}

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11LegalDeviceTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11LegalDeviceTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11LegalDeviceTable_commit( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:dot11LegalDeviceTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;
	
    if(RS_NOTREADY == rowreq_ctx->data.legalRowStatus && MFD_SUCCESS == check_create_ready(rowreq_ctx)) {
        rowreq_ctx->data.legalRowStatus = RS_NOTINSERVICE;
    }    

    /*
     * commit dot11LegalDeviceTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_LEGALMACADDRESS_FLAG) {
       save_flags &= ~COLUMN_LEGALMACADDRESS_FLAG; /* clear legalMacAddress */
       /*
        * TODO:482:o: |-> commit column legalMacAddress.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11LegalDeviceTable column legalMacAddress commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo legalMacAddress
             */
            rowreq_ctx->column_set_flags |= COLUMN_LEGALMACADDRESS_FLAG;
       }
    }

    if (save_flags & COLUMN_LEGALROWSTATUS_FLAG) {
        save_flags &= ~COLUMN_LEGALROWSTATUS_FLAG; /* clear legalRowStatus */
        switch(rowreq_ctx->data.legalRowStatus) {
            case RS_ACTIVE:
                {
					int ret = 0;
					void *connection = NULL;
				    if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3)) {
						ret = set_mac_whitelist(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.legalMacAddress);
						snmp_log(LOG_DEBUG, "dot11LegalDeviceTable_commit: set_mac_whitelist: ret = %d\n", ret);						
						if(ret == 1)
						{
							rc = MFD_SUCCESS;
                            break;
						}
						else if(SNMPD_CONNECTION_ERROR == ret) 
						{
							close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
						}
				    }
                }
                rc = -1;
                break;

            case RS_CREATEANDWAIT:
                rowreq_ctx->data.legalRowStatus = RS_NOTREADY;
                rc = MFD_SUCCESS;
                break;
                
            case RS_DESTROY:  
                {
					int ret = 0;
					void *connection = NULL;
				    if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3)) {
						ret = delete_mac_whitelist(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.legalMacAddress );
						snmp_log(LOG_DEBUG, "dot11LegalDeviceTable_commit: delete_mac_whitelist: ret = %d\n", ret);
						if(ret == 1)
						{								
							rc = MFD_SUCCESS;
                            break;
						}
						else if(SNMPD_CONNECTION_ERROR == ret) 
						{
                            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
                        }
				    }
                }
                rowreq_ctx->rowreq_flags &= ~MFD_ROW_DELETED;
                rc = -1;
                break;
                
            default:
                break;
        }           
       /*
        * TODO:482:o: |-> commit column legalRowStatus.
        */
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11LegalDeviceTable column legalRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo legalRowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_LEGALROWSTATUS_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11LegalDeviceTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11LegalDeviceTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11LegalDeviceTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11LegalDeviceTable_undo_commit( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:dot11LegalDeviceTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11LegalDeviceTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11LegalDeviceTable_undo_commit */

/*
 * TODO:440:M: Implement dot11LegalDeviceTable node value checks.
 * TODO:450:M: Implement dot11LegalDeviceTable undo functions.
 * TODO:460:M: Implement dot11LegalDeviceTable set functions.
 * TODO:480:M: Implement dot11LegalDeviceTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11LegalDeviceEntry.legalMacAddress
 * legalMacAddress is subid 2 of dot11LegalDeviceEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.1.1.2
 * Description:
Legal MAC address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param legalMacAddress_val_ptr
 *        A char containing the new value.
 * @param legalMacAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by legalMacAddress_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11LegalDeviceTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.legalMacAddress).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
legalMacAddress_check_value( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx, char *legalMacAddress_val_ptr,  size_t legalMacAddress_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:legalMacAddress_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != legalMacAddress_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid legalMacAddress value.
     */
	
    if(RS_NOTINSERVICE == rowreq_ctx->data.legalRowStatus || RS_NOTREADY == rowreq_ctx->data.legalRowStatus)
    	return MFD_SUCCESS; 
	else
		return MFD_ERROR;  /* legalMacAddress value not illegal */
} /* legalMacAddress_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11LegalDeviceTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11LegalDeviceTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
legalMacAddress_undo_setup( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:legalMacAddress_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup legalMacAddress undo.
     */
    /*
     * copy legalMacAddress and legalMacAddress_len data
     * set rowreq_ctx->undo->legalMacAddress from rowreq_ctx->data.legalMacAddress
     */
    memcpy( rowreq_ctx->undo->legalMacAddress, rowreq_ctx->data.legalMacAddress,
            (rowreq_ctx->data.legalMacAddress_len * sizeof(rowreq_ctx->undo->legalMacAddress[0])));
    rowreq_ctx->undo->legalMacAddress_len = rowreq_ctx->data.legalMacAddress_len;


    return MFD_SUCCESS;
} /* legalMacAddress_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param legalMacAddress_val_ptr
 *        A char containing the new value.
 * @param legalMacAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by legalMacAddress_val_ptr
 */
int
legalMacAddress_set( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx, char *legalMacAddress_val_ptr,  size_t legalMacAddress_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:legalMacAddress_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != legalMacAddress_val_ptr);

    /*
     * TODO:461:M: |-> Set legalMacAddress value.
     * set legalMacAddress value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.legalMacAddress, legalMacAddress_val_ptr, legalMacAddress_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.legalMacAddress_len = legalMacAddress_val_ptr_len / sizeof(legalMacAddress_val_ptr[0]);

    return MFD_SUCCESS;
} /* legalMacAddress_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
legalMacAddress_undo( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:legalMacAddress_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up legalMacAddress undo.
     */
    /*
     * copy legalMacAddress and legalMacAddress_len data
     * set rowreq_ctx->data.legalMacAddress from rowreq_ctx->undo->legalMacAddress
     */
    memcpy( rowreq_ctx->data.legalMacAddress, rowreq_ctx->undo->legalMacAddress,
            (rowreq_ctx->undo->legalMacAddress_len * sizeof(rowreq_ctx->data.legalMacAddress[0])));
    rowreq_ctx->data.legalMacAddress_len = rowreq_ctx->undo->legalMacAddress_len;

    
    return MFD_SUCCESS;
} /* legalMacAddress_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11LegalDeviceEntry.legalRowStatus
 * legalRowStatus is subid 3 of dot11LegalDeviceEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.1.1.3
 * Description:
This variable is used to create, modify, and/or delete a row
	        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param legalRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11LegalDeviceTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
legalRowStatus_check_value( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx, u_long legalRowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:legalRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid legalRowStatus value.
     */

    return MFD_SUCCESS; /* legalRowStatus value not illegal */
} /* legalRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11LegalDeviceTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11LegalDeviceTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
legalRowStatus_undo_setup( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:legalRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup legalRowStatus undo.
     */
    /*
     * copy legalRowStatus data
     * set rowreq_ctx->undo->legalRowStatus from rowreq_ctx->data.legalRowStatus
     */
    rowreq_ctx->undo->legalRowStatus = rowreq_ctx->data.legalRowStatus;


    return MFD_SUCCESS;
} /* legalRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param legalRowStatus_val
 *        A long containing the new value.
 */
int
legalRowStatus_set( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx, u_long legalRowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:legalRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set legalRowStatus value.
     * set legalRowStatus value in rowreq_ctx->data
     */

    int ret = MFD_SUCCESS;
    
    switch(legalRowStatus_val) {
    
        case RS_ACTIVE:         
        case RS_DESTROY:
            rowreq_ctx->data.legalRowStatus = legalRowStatus_val;
            ret = MFD_SUCCESS;
            break;
    
        case RS_NOTINSERVICE:
        case RS_NOTREADY:
        case RS_CREATEANDGO:
            ret = MFD_ERROR;
            break;
    
        case RS_CREATEANDWAIT:
            if(RS_NONEXISTENT == rowreq_ctx->data.legalRowStatus) {
				global_to_local_ID(&(rowreq_ctx->data.parameter), rowreq_ctx->tbl_idx.legalMacID, WIRELESS_MAX_NUM);
                
				rowreq_ctx->data.legalRowStatus = legalRowStatus_val;
                ret = MFD_SUCCESS;
            }
            else {
                ret = MFD_ERROR;
            }
            break;
    
        default:
            snmp_log(LOG_WARNING, "unknow %s in legalRowStatus_set", legalRowStatus_val);
            ret = MFD_ERROR;
            break;
    }

    return ret;
} /* legalRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
legalRowStatus_undo( dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11LegalDeviceTable:legalRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up legalRowStatus undo.
     */
    /*
     * copy legalRowStatus data
     * set rowreq_ctx->data.legalRowStatus from rowreq_ctx->undo->legalRowStatus
     */
    rowreq_ctx->data.legalRowStatus = rowreq_ctx->undo->legalRowStatus;

    
    return MFD_SUCCESS;
} /* legalRowStatus_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11LegalDeviceTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11LegalDeviceTable if you don't have dependencies)
 */
int
dot11LegalDeviceTable_check_dependencies(dot11LegalDeviceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11LegalDeviceTable:dot11LegalDeviceTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11LegalDeviceTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & COLUMN_LEGALROWSTATUS_FLAG) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->legalRowStatus,
                                         rowreq_ctx->data.legalRowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_DESTROY ==  rowreq_ctx->data.legalRowStatus) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;
            }
            else if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.legalRowStatus) {
                if ((rowreq_ctx->column_set_flags & DOT11LEGALDEVICETABLE_REQUIRED_COLS)
                    != DOT11LEGALDEVICETABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11LegalDeviceTable",
                                "required columns missing (0x%0x != 0x%0x)\n",
                                rowreq_ctx->column_set_flags, DOT11LEGALDEVICETABLE_REQUIRED_COLS));
                    return MFD_CANNOT_CREATE_NOW;
                }
                rowreq_ctx->data.legalRowStatus = ROWSTATUS_ACTIVE;
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.legalRowStatus) {
                if (rowreq_ctx->column_set_flags & ~COLUMN_LEGALROWSTATUS_FLAG) {
                    DEBUGMSGTL(("dot11LegalDeviceTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;

            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11LegalDeviceTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11LegalDeviceTable_check_dependencies */

/** @} */
