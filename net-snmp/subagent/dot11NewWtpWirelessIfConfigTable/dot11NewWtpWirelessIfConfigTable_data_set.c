/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "ws_dcli_ac.h"
#include "autelanWtpGroup.h"
/* include our parent header */
#include "dot11NewWtpWirelessIfConfigTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11NewWtpWirelessIfConfigTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11NewWtpWirelessIfConfigTable is subid 9 of wtpInterface.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11NewWtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11NewWtpWirelessIfConfigTable_undo_setup( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:dot11NewWtpWirelessIfConfigTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11NewWtpWirelessIfConfigTable undo.
     * set up dot11NewWtpWirelessIfConfigTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11NewWtpWirelessIfConfigTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11NewWtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11NewWtpWirelessIfConfigTable_undo_cleanup( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:dot11NewWtpWirelessIfConfigTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11NewWtpWirelessIfConfigTable undo.
     */

    return rc;
} /* dot11NewWtpWirelessIfConfigTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11NewWtpWirelessIfConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11NewWtpWirelessIfConfigTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11NewWtpWirelessIfConfigTable_commit( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:dot11NewWtpWirelessIfConfigTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11NewWtpWirelessIfConfigTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_NEWWTPWIRELESSIFBEACONINTVL) {
       save_flags &= ~FLAG_NEWWTPWIRELESSIFBEACONINTVL; /* clear NewwtpWirelessIfBeaconIntvl */
       /*
        * TODO:482:o: |-> commit column NewwtpWirelessIfBeaconIntvl.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11NewWtpWirelessIfConfigTable column NewwtpWirelessIfBeaconIntvl commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewwtpWirelessIfBeaconIntvl
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWWTPWIRELESSIFBEACONINTVL;
       }
    }

    if (save_flags & FLAG_NEWWTPWIRELESSIFDTIMINTVL) {
       save_flags &= ~FLAG_NEWWTPWIRELESSIFDTIMINTVL; /* clear NewwtpWirelessIfDtimIntvl */
       /*
        * TODO:482:o: |-> commit column NewwtpWirelessIfDtimIntvl.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11NewWtpWirelessIfConfigTable column NewwtpWirelessIfDtimIntvl commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewwtpWirelessIfDtimIntvl
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWWTPWIRELESSIFDTIMINTVL;
       }
    }

    if (save_flags & FLAG_NEWWTPWIRELESSIFSHTRETRYTHLD) {
       save_flags &= ~FLAG_NEWWTPWIRELESSIFSHTRETRYTHLD; /* clear NewwtpWirelessIfShtRetryThld */
       /*
        * TODO:482:o: |-> commit column NewwtpWirelessIfShtRetryThld.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11NewWtpWirelessIfConfigTable column NewwtpWirelessIfShtRetryThld commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewwtpWirelessIfShtRetryThld
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWWTPWIRELESSIFSHTRETRYTHLD;
       }
    }

    if (save_flags & FLAG_NEWWTPWIRELESSIFLONGRETRYTHLD) {
       save_flags &= ~FLAG_NEWWTPWIRELESSIFLONGRETRYTHLD; /* clear NewwtpWirelessIfLongRetryThld */
       /*
        * TODO:482:o: |-> commit column NewwtpWirelessIfLongRetryThld.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11NewWtpWirelessIfConfigTable column NewwtpWirelessIfLongRetryThld commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewwtpWirelessIfLongRetryThld
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWWTPWIRELESSIFLONGRETRYTHLD;
       }
    }

    if (save_flags & FLAG_NEWWTPWIRELESSIFMAXRXLIFETIME) {
       save_flags &= ~FLAG_NEWWTPWIRELESSIFMAXRXLIFETIME; /* clear NewwtpWirelessIfMaxRxLifetime */
       /*
        * TODO:482:o: |-> commit column NewwtpWirelessIfMaxRxLifetime.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11NewWtpWirelessIfConfigTable column NewwtpWirelessIfMaxRxLifetime commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewwtpWirelessIfMaxRxLifetime
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWWTPWIRELESSIFMAXRXLIFETIME;
       }
    }

    if (save_flags & FLAG_NEWWTPPREAMBLELEN) {
       save_flags &= ~FLAG_NEWWTPPREAMBLELEN; /* clear NewWtpPreambleLen */
       /*
        * TODO:482:o: |-> commit column NewWtpPreambleLen.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11NewWtpWirelessIfConfigTable column NewWtpPreambleLen commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewWtpPreambleLen
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWWTPPREAMBLELEN;
       }
    }

    if (save_flags & FLAG_NEWRTSTHRESHOLD) {
       save_flags &= ~FLAG_NEWRTSTHRESHOLD; /* clear NewRtsThreshold */
       /*
        * TODO:482:o: |-> commit column NewRtsThreshold.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11NewWtpWirelessIfConfigTable column NewRtsThreshold commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewRtsThreshold
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWRTSTHRESHOLD;
       }
    }

    if (save_flags & FLAG_NEWFRAGTHRESHLOD) {
       save_flags &= ~FLAG_NEWFRAGTHRESHLOD; /* clear NewFragThreshlod */
       /*
        * TODO:482:o: |-> commit column NewFragThreshlod.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11NewWtpWirelessIfConfigTable column NewFragThreshlod commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewFragThreshlod
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWFRAGTHRESHLOD;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11NewWtpWirelessIfConfigTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11NewWtpWirelessIfConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11NewWtpWirelessIfConfigTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11NewWtpWirelessIfConfigTable_undo_commit( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:dot11NewWtpWirelessIfConfigTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11NewWtpWirelessIfConfigTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11NewWtpWirelessIfConfigTable_undo_commit */

/*
 * TODO:420:r: Implement dot11NewWtpWirelessIfConfigTable index validation.
 */
/*
 * TODO:440:M: Implement dot11NewWtpWirelessIfConfigTable node value checks.
 * TODO:450:M: Implement dot11NewWtpWirelessIfConfigTable undo functions.
 * TODO:460:M: Implement dot11NewWtpWirelessIfConfigTable set functions.
 * TODO:480:M: Implement dot11NewWtpWirelessIfConfigTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewwtpWirelessIfBeaconIntvl
 * NewwtpWirelessIfBeaconIntvl is subid 3 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.3
 * Description:
Beacon frame interval.(Range25 - 1000ms)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  25 - 1000;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewwtpWirelessIfBeaconIntvl_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11NewWtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  25 - 1000
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewwtpWirelessIfBeaconIntvl_check_value( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewwtpWirelessIfBeaconIntvl_val)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfBeaconIntvl_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewwtpWirelessIfBeaconIntvl value.
     */

    return MFD_SUCCESS; /* NewwtpWirelessIfBeaconIntvl value not illegal */
} /* NewwtpWirelessIfBeaconIntvl_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11NewWtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11NewWtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewwtpWirelessIfBeaconIntvl_undo_setup( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfBeaconIntvl_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewwtpWirelessIfBeaconIntvl undo.
     */
    /*
     * copy NewwtpWirelessIfBeaconIntvl data
     * set rowreq_ctx->undo->NewwtpWirelessIfBeaconIntvl from rowreq_ctx->data.NewwtpWirelessIfBeaconIntvl
     */
    rowreq_ctx->undo->NewwtpWirelessIfBeaconIntvl = rowreq_ctx->data.NewwtpWirelessIfBeaconIntvl;


    return MFD_SUCCESS;
} /* NewwtpWirelessIfBeaconIntvl_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewwtpWirelessIfBeaconIntvl_val
 *        A long containing the new value.
 */
int
NewwtpWirelessIfBeaconIntvl_set( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewwtpWirelessIfBeaconIntvl_val )
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfBeaconIntvl_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewwtpWirelessIfBeaconIntvl reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewwtpWirelessIfBeaconIntvl value.
     * set NewwtpWirelessIfBeaconIntvl value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int ret = 0,radio_id;
	int rc = MFD_ERROR;
	
	ret=config_radio_beaconinterval(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpRadCurrID,NewwtpWirelessIfBeaconIntvl_val);

	if(ret == 1)
	{
   		 rowreq_ctx->data.NewwtpWirelessIfBeaconIntvl = NewwtpWirelessIfBeaconIntvl_val;
		 rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* NewwtpWirelessIfBeaconIntvl_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewwtpWirelessIfBeaconIntvl_undo( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfBeaconIntvl_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewwtpWirelessIfBeaconIntvl undo.
     */
    /*
     * copy NewwtpWirelessIfBeaconIntvl data
     * set rowreq_ctx->data.NewwtpWirelessIfBeaconIntvl from rowreq_ctx->undo->NewwtpWirelessIfBeaconIntvl
     */
    rowreq_ctx->data.NewwtpWirelessIfBeaconIntvl = rowreq_ctx->undo->NewwtpWirelessIfBeaconIntvl;

    
    return MFD_SUCCESS;
} /* NewwtpWirelessIfBeaconIntvl_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewwtpWirelessIfDtimIntvl
 * NewwtpWirelessIfDtimIntvl is subid 4 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.4
 * Description:
DTIM interval.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewwtpWirelessIfDtimIntvl_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11NewWtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 15
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewwtpWirelessIfDtimIntvl_check_value( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewwtpWirelessIfDtimIntvl_val)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfDtimIntvl_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewwtpWirelessIfDtimIntvl value.
     */

    return MFD_SUCCESS; /* NewwtpWirelessIfDtimIntvl value not illegal */
} /* NewwtpWirelessIfDtimIntvl_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11NewWtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11NewWtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewwtpWirelessIfDtimIntvl_undo_setup( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfDtimIntvl_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewwtpWirelessIfDtimIntvl undo.
     */
    /*
     * copy NewwtpWirelessIfDtimIntvl data
     * set rowreq_ctx->undo->NewwtpWirelessIfDtimIntvl from rowreq_ctx->data.NewwtpWirelessIfDtimIntvl
     */
    rowreq_ctx->undo->NewwtpWirelessIfDtimIntvl = rowreq_ctx->data.NewwtpWirelessIfDtimIntvl;


    return MFD_SUCCESS;
} /* NewwtpWirelessIfDtimIntvl_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewwtpWirelessIfDtimIntvl_val
 *        A long containing the new value.
 */
int
NewwtpWirelessIfDtimIntvl_set( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewwtpWirelessIfDtimIntvl_val )
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfDtimIntvl_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewwtpWirelessIfDtimIntvl reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewwtpWirelessIfDtimIntvl value.
     * set NewwtpWirelessIfDtimIntvl value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int ret = 0,radio_id;
	int rc = MFD_ERROR;
	ret=config_radio_dtim(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpRadCurrID,NewwtpWirelessIfDtimIntvl_val);

	if(ret == 1)
	{
    	rowreq_ctx->data.NewwtpWirelessIfDtimIntvl = NewwtpWirelessIfDtimIntvl_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* NewwtpWirelessIfDtimIntvl_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewwtpWirelessIfDtimIntvl_undo( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfDtimIntvl_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewwtpWirelessIfDtimIntvl undo.
     */
    /*
     * copy NewwtpWirelessIfDtimIntvl data
     * set rowreq_ctx->data.NewwtpWirelessIfDtimIntvl from rowreq_ctx->undo->NewwtpWirelessIfDtimIntvl
     */
    rowreq_ctx->data.NewwtpWirelessIfDtimIntvl = rowreq_ctx->undo->NewwtpWirelessIfDtimIntvl;

    
    return MFD_SUCCESS;
} /* NewwtpWirelessIfDtimIntvl_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewwtpWirelessIfShtRetryThld
 * NewwtpWirelessIfShtRetryThld is subid 5 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.5
 * Description:
The total times that retry times when frame length less than RTS threshold  .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewwtpWirelessIfShtRetryThld_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11NewWtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - 15
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewwtpWirelessIfShtRetryThld_check_value( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewwtpWirelessIfShtRetryThld_val)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfShtRetryThld_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewwtpWirelessIfShtRetryThld value.
     */

    return MFD_SUCCESS; /* NewwtpWirelessIfShtRetryThld value not illegal */
} /* NewwtpWirelessIfShtRetryThld_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11NewWtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11NewWtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewwtpWirelessIfShtRetryThld_undo_setup( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfShtRetryThld_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewwtpWirelessIfShtRetryThld undo.
     */
    /*
     * copy NewwtpWirelessIfShtRetryThld data
     * set rowreq_ctx->undo->NewwtpWirelessIfShtRetryThld from rowreq_ctx->data.NewwtpWirelessIfShtRetryThld
     */
    rowreq_ctx->undo->NewwtpWirelessIfShtRetryThld = rowreq_ctx->data.NewwtpWirelessIfShtRetryThld;


    return MFD_SUCCESS;
} /* NewwtpWirelessIfShtRetryThld_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewwtpWirelessIfShtRetryThld_val
 *        A long containing the new value.
 */
int
NewwtpWirelessIfShtRetryThld_set( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewwtpWirelessIfShtRetryThld_val )
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfShtRetryThld_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewwtpWirelessIfShtRetryThld reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewwtpWirelessIfShtRetryThld value.
     * set NewwtpWirelessIfShtRetryThld value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int ret = 0;
	int rc = MFD_ERROR;
	ret=config_radio_shortretry(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpRadCurrID,NewwtpWirelessIfShtRetryThld_val);

	if(ret == 1)
	{
   		 rowreq_ctx->data.NewwtpWirelessIfShtRetryThld = NewwtpWirelessIfShtRetryThld_val;
		 rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* NewwtpWirelessIfShtRetryThld_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewwtpWirelessIfShtRetryThld_undo( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfShtRetryThld_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewwtpWirelessIfShtRetryThld undo.
     */
    /*
     * copy NewwtpWirelessIfShtRetryThld data
     * set rowreq_ctx->data.NewwtpWirelessIfShtRetryThld from rowreq_ctx->undo->NewwtpWirelessIfShtRetryThld
     */
    rowreq_ctx->data.NewwtpWirelessIfShtRetryThld = rowreq_ctx->undo->NewwtpWirelessIfShtRetryThld;

    
    return MFD_SUCCESS;
} /* NewwtpWirelessIfShtRetryThld_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewwtpWirelessIfLongRetryThld
 * NewwtpWirelessIfLongRetryThld is subid 6 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.6
 * Description:
The total times that retry times when frame length over RTS threshold  .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewwtpWirelessIfLongRetryThld_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11NewWtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 15
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewwtpWirelessIfLongRetryThld_check_value( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewwtpWirelessIfLongRetryThld_val)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfLongRetryThld_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewwtpWirelessIfLongRetryThld value.
     */

    return MFD_SUCCESS; /* NewwtpWirelessIfLongRetryThld value not illegal */
} /* NewwtpWirelessIfLongRetryThld_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11NewWtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11NewWtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewwtpWirelessIfLongRetryThld_undo_setup( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfLongRetryThld_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewwtpWirelessIfLongRetryThld undo.
     */
    /*
     * copy NewwtpWirelessIfLongRetryThld data
     * set rowreq_ctx->undo->NewwtpWirelessIfLongRetryThld from rowreq_ctx->data.NewwtpWirelessIfLongRetryThld
     */
    rowreq_ctx->undo->NewwtpWirelessIfLongRetryThld = rowreq_ctx->data.NewwtpWirelessIfLongRetryThld;


    return MFD_SUCCESS;
} /* NewwtpWirelessIfLongRetryThld_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewwtpWirelessIfLongRetryThld_val
 *        A long containing the new value.
 */
int
NewwtpWirelessIfLongRetryThld_set( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewwtpWirelessIfLongRetryThld_val )
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfLongRetryThld_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewwtpWirelessIfLongRetryThld reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewwtpWirelessIfLongRetryThld value.
     * set NewwtpWirelessIfLongRetryThld value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int ret = 0;
	int rc = MFD_ERROR;
	ret=config_radio_longretry(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpRadCurrID,NewwtpWirelessIfLongRetryThld_val);

	if(ret == 1)
	{
    	rowreq_ctx->data.NewwtpWirelessIfLongRetryThld = NewwtpWirelessIfLongRetryThld_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* NewwtpWirelessIfLongRetryThld_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewwtpWirelessIfLongRetryThld_undo( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfLongRetryThld_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewwtpWirelessIfLongRetryThld undo.
     */
    /*
     * copy NewwtpWirelessIfLongRetryThld data
     * set rowreq_ctx->data.NewwtpWirelessIfLongRetryThld from rowreq_ctx->undo->NewwtpWirelessIfLongRetryThld
     */
    rowreq_ctx->data.NewwtpWirelessIfLongRetryThld = rowreq_ctx->undo->NewwtpWirelessIfLongRetryThld;

    
    return MFD_SUCCESS;
} /* NewwtpWirelessIfLongRetryThld_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewwtpWirelessIfMaxRxLifetime
 * NewwtpWirelessIfMaxRxLifetime is subid 7 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.7
 * Description:
Received data packets lifetime(units ms).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewwtpWirelessIfMaxRxLifetime_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11NewWtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewwtpWirelessIfMaxRxLifetime_check_value( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, u_long NewwtpWirelessIfMaxRxLifetime_val)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfMaxRxLifetime_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewwtpWirelessIfMaxRxLifetime value.
     */

    return MFD_SUCCESS; /* NewwtpWirelessIfMaxRxLifetime value not illegal */
} /* NewwtpWirelessIfMaxRxLifetime_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11NewWtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11NewWtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewwtpWirelessIfMaxRxLifetime_undo_setup( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfMaxRxLifetime_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewwtpWirelessIfMaxRxLifetime undo.
     */
    /*
     * copy NewwtpWirelessIfMaxRxLifetime data
     * set rowreq_ctx->undo->NewwtpWirelessIfMaxRxLifetime from rowreq_ctx->data.NewwtpWirelessIfMaxRxLifetime
     */
    rowreq_ctx->undo->NewwtpWirelessIfMaxRxLifetime = rowreq_ctx->data.NewwtpWirelessIfMaxRxLifetime;


    return MFD_SUCCESS;
} /* NewwtpWirelessIfMaxRxLifetime_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewwtpWirelessIfMaxRxLifetime_val
 *        A u_long containing the new value.
 */
int
NewwtpWirelessIfMaxRxLifetime_set( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, u_long NewwtpWirelessIfMaxRxLifetime_val )
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfMaxRxLifetime_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewwtpWirelessIfMaxRxLifetime reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewwtpWirelessIfMaxRxLifetime value.
     * set NewwtpWirelessIfMaxRxLifetime value in rowreq_ctx->data
     */
    rowreq_ctx->data.NewwtpWirelessIfMaxRxLifetime = NewwtpWirelessIfMaxRxLifetime_val;

    return MFD_SUCCESS;
} /* NewwtpWirelessIfMaxRxLifetime_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewwtpWirelessIfMaxRxLifetime_undo( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfMaxRxLifetime_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewwtpWirelessIfMaxRxLifetime undo.
     */
    /*
     * copy NewwtpWirelessIfMaxRxLifetime data
     * set rowreq_ctx->data.NewwtpWirelessIfMaxRxLifetime from rowreq_ctx->undo->NewwtpWirelessIfMaxRxLifetime
     */
    rowreq_ctx->data.NewwtpWirelessIfMaxRxLifetime = rowreq_ctx->undo->NewwtpWirelessIfMaxRxLifetime;

    
    return MFD_SUCCESS;
} /* NewwtpWirelessIfMaxRxLifetime_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewWtpPreambleLen
 * NewWtpPreambleLen is subid 8 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.8
 * Description:
AP PreambleLen.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  long(1), short(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewWtpPreambleLen_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11NewWtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  long(1), short(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewWtpPreambleLen_check_value( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, u_long NewWtpPreambleLen_val)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewWtpPreambleLen_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewWtpPreambleLen value.
     */

    return MFD_SUCCESS; /* NewWtpPreambleLen value not illegal */
} /* NewWtpPreambleLen_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11NewWtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11NewWtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewWtpPreambleLen_undo_setup( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewWtpPreambleLen_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewWtpPreambleLen undo.
     */
    /*
     * copy NewWtpPreambleLen data
     * set rowreq_ctx->undo->NewWtpPreambleLen from rowreq_ctx->data.NewWtpPreambleLen
     */
    rowreq_ctx->undo->NewWtpPreambleLen = rowreq_ctx->data.NewWtpPreambleLen;


    return MFD_SUCCESS;
} /* NewWtpPreambleLen_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewWtpPreambleLen_val
 *        A long containing the new value.
 */
int
NewWtpPreambleLen_set( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, u_long NewWtpPreambleLen_val )
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewWtpPreambleLen_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewWtpPreambleLen reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(NewWtpPreambleLen_val) {
        case NEWWTPPREAMBLELEN_LONG:
             NewWtpPreambleLen_val = INTERNAL_NEWWTPPREAMBLELEN_LONG;
             break;

        case NEWWTPPREAMBLELEN_SHORT:
             NewWtpPreambleLen_val = INTERNAL_NEWWTPPREAMBLELEN_SHORT;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for NewWtpPreambleLen\n", NewWtpPreambleLen_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set NewWtpPreambleLen value.
     * set NewWtpPreambleLen value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int ret = 0;
	int rc = MFD_ERROR;
	int count = 0;
	count = NewWtpPreambleLen_val;
	char input[20] = { 0 };
	memset(input,0,20);
	if( count == 1)
	{
		strncpy(input,"short",sizeof(input)-1);
	}
	else if( 2 == count)
	{
		strncpy(input,"long",sizeof(input)-1);
	}
	ret = config_radio_preamble(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpRadCurrID,input); 
	if(ret ==1)
	{						
	    rowreq_ctx->data.NewWtpPreambleLen = NewWtpPreambleLen_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* NewWtpPreambleLen_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewWtpPreambleLen_undo( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewWtpPreambleLen_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewWtpPreambleLen undo.
     */
    /*
     * copy NewWtpPreambleLen data
     * set rowreq_ctx->data.NewWtpPreambleLen from rowreq_ctx->undo->NewWtpPreambleLen
     */
    rowreq_ctx->data.NewWtpPreambleLen = rowreq_ctx->undo->NewWtpPreambleLen;

    
    return MFD_SUCCESS;
} /* NewWtpPreambleLen_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewRtsThreshold
 * NewRtsThreshold is subid 9 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.9
 * Description:
RtsThreshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewRtsThreshold_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11NewWtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewRtsThreshold_check_value( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewRtsThreshold_val)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewRtsThreshold_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewRtsThreshold value.
     */

    return MFD_SUCCESS; /* NewRtsThreshold value not illegal */
} /* NewRtsThreshold_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11NewWtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11NewWtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewRtsThreshold_undo_setup( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewRtsThreshold_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewRtsThreshold undo.
     */
    /*
     * copy NewRtsThreshold data
     * set rowreq_ctx->undo->NewRtsThreshold from rowreq_ctx->data.NewRtsThreshold
     */
    rowreq_ctx->undo->NewRtsThreshold = rowreq_ctx->data.NewRtsThreshold;


    return MFD_SUCCESS;
} /* NewRtsThreshold_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewRtsThreshold_val
 *        A long containing the new value.
 */
int
NewRtsThreshold_set( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewRtsThreshold_val )
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewRtsThreshold_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewRtsThreshold reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewRtsThreshold value.
     * set NewRtsThreshold value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int ret = 0;
	int rc = MFD_ERROR;

	if((NewRtsThreshold_val<256)||(NewRtsThreshold_val>2346))
	{
		rc = MFD_ERROR;
	}
	else
	{
		//add function here
		ret = config_radio_rtsthreshold(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpRadCurrID,NewRtsThreshold_val);
		if(ret ==1)
		{
    		rowreq_ctx->data.NewRtsThreshold = NewRtsThreshold_val;
			rc = MFD_SUCCESS;
		}
		else
		{
		    if(SNMPD_CONNECTION_ERROR == ret) {
                close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	        }
			rc = MFD_ERROR;
		}
	}

    return rc;
} /* NewRtsThreshold_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewRtsThreshold_undo( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewRtsThreshold_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewRtsThreshold undo.
     */
    /*
     * copy NewRtsThreshold data
     * set rowreq_ctx->data.NewRtsThreshold from rowreq_ctx->undo->NewRtsThreshold
     */
    rowreq_ctx->data.NewRtsThreshold = rowreq_ctx->undo->NewRtsThreshold;

    
    return MFD_SUCCESS;
} /* NewRtsThreshold_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewFragThreshlod
 * NewFragThreshlod is subid 10 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.10
 * Description:
NewFragThreshlod.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewFragThreshlod_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11NewWtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewFragThreshlod_check_value( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewFragThreshlod_val)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewFragThreshlod_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewFragThreshlod value.
     */

    return MFD_SUCCESS; /* NewFragThreshlod value not illegal */
} /* NewFragThreshlod_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11NewWtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11NewWtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewFragThreshlod_undo_setup( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewFragThreshlod_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewFragThreshlod undo.
     */
    /*
     * copy NewFragThreshlod data
     * set rowreq_ctx->undo->NewFragThreshlod from rowreq_ctx->data.NewFragThreshlod
     */
    rowreq_ctx->undo->NewFragThreshlod = rowreq_ctx->data.NewFragThreshlod;


    return MFD_SUCCESS;
} /* NewFragThreshlod_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewFragThreshlod_val
 *        A long containing the new value.
 */
int
NewFragThreshlod_set( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewFragThreshlod_val )
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewFragThreshlod_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewFragThreshlod reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewFragThreshlod value.
     * set NewFragThreshlod value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int ret = 0;
	int rc = MFD_ERROR;
	if((NewFragThreshlod_val < 256)||(NewFragThreshlod_val > 2346))
	{
		rc = MFD_ERROR;
	}
	else
	{
		//add function here
		ret = config_radio_fragmentation(rowreq_ctx->data.parameter,connection,rowreq_ctx->data.wtpRadCurrID,NewFragThreshlod_val);
		if (ret ==1)
		{
    		rowreq_ctx->data.NewFragThreshlod = NewFragThreshlod_val;
			rc = MFD_SUCCESS;
		}
		else
		{
		    if(SNMPD_CONNECTION_ERROR == ret) {
                close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	        }
			rc = MFD_ERROR;		
		}
	}

    return rc;
} /* NewFragThreshlod_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewFragThreshlod_undo( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewFragThreshlod_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewFragThreshlod undo.
     */
    /*
     * copy NewFragThreshlod data
     * set rowreq_ctx->data.NewFragThreshlod from rowreq_ctx->undo->NewFragThreshlod
     */
    rowreq_ctx->data.NewFragThreshlod = rowreq_ctx->undo->NewFragThreshlod;

    
    return MFD_SUCCESS;
} /* NewFragThreshlod_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11NewWtpWirelessIfConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11NewWtpWirelessIfConfigTable if you don't have dependencies)
 */
int
dot11NewWtpWirelessIfConfigTable_check_dependencies(dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11NewWtpWirelessIfConfigTable:dot11NewWtpWirelessIfConfigTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11NewWtpWirelessIfConfigTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11NewWtpWirelessIfConfigTable_check_dependencies */

/** @} */
