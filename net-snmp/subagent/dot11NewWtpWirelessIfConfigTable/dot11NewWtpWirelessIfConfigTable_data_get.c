/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11NewWtpWirelessIfConfigTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11NewWtpWirelessIfConfigTable get routines.
 * TODO:240:M: Implement dot11NewWtpWirelessIfConfigTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11NewWtpWirelessIfConfigTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11NewWtpWirelessIfConfigTable is subid 9 of wtpInterface.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11NewWtpWirelessIfConfigTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewwtpID
 * NewwtpID is subid 1 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.1
 * Description:
wtpID
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewwtpID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewwtpID_map_wtpwire(long *mib_NewwtpID_val_ptr, long raw_NewwtpID_val)
{
    netsnmp_assert(NULL != mib_NewwtpID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewwtpID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewwtpID_val_ptr) = raw_NewwtpID_val;

    return MFD_SUCCESS;
} /* NewwtpID_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewapWirelessIfIndex
 * NewapWirelessIfIndex is subid 2 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.2
 * Description:
wtpWirelessIfIndex	
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewapWirelessIfIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewapWirelessIfIndex_map_wtpwire(long *mib_NewapWirelessIfIndex_val_ptr, long raw_NewapWirelessIfIndex_val)
{
    netsnmp_assert(NULL != mib_NewapWirelessIfIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewapWirelessIfIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewapWirelessIfIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewapWirelessIfIndex_val_ptr) = raw_NewapWirelessIfIndex_val;

    return MFD_SUCCESS;
} /* NewapWirelessIfIndex_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11NewWtpWirelessIfConfigTable_indexes_set_tbl_idx(dot11NewWtpWirelessIfConfigTable_mib_index *tbl_idx, long NewwtpID_val, long NewapWirelessIfIndex_val)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:dot11NewWtpWirelessIfConfigTable_indexes_set_tbl_idx","called\n"));

    /* NewwtpID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->NewwtpID = NewwtpID_val;
    
    /* NewapWirelessIfIndex(2)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->NewapWirelessIfIndex = NewapWirelessIfIndex_val;
    

    return MFD_SUCCESS;
} /* dot11NewWtpWirelessIfConfigTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11NewWtpWirelessIfConfigTable_indexes_set(dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long NewwtpID_val, long NewapWirelessIfIndex_val)
{
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:dot11NewWtpWirelessIfConfigTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11NewWtpWirelessIfConfigTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , NewwtpID_val
                                   , NewapWirelessIfIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11NewWtpWirelessIfConfigTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11NewWtpWirelessIfConfigTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewwtpWirelessIfBeaconIntvl
 * NewwtpWirelessIfBeaconIntvl is subid 3 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.3
 * Description:
Beacon frame interval.(Range25 - 1000ms)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  25 - 1000;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewwtpWirelessIfBeaconIntvl.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewwtpWirelessIfBeaconIntvl_map(long *mib_NewwtpWirelessIfBeaconIntvl_val_ptr, long raw_NewwtpWirelessIfBeaconIntvl_val)
{
    netsnmp_assert(NULL != mib_NewwtpWirelessIfBeaconIntvl_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfBeaconIntvl_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewwtpWirelessIfBeaconIntvl mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewwtpWirelessIfBeaconIntvl_val_ptr) = raw_NewwtpWirelessIfBeaconIntvl_val;

    return MFD_SUCCESS;
} /* NewwtpWirelessIfBeaconIntvl_map */

/**
 * Extract the current value of the NewwtpWirelessIfBeaconIntvl data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewwtpWirelessIfBeaconIntvl_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewwtpWirelessIfBeaconIntvl_get( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long * NewwtpWirelessIfBeaconIntvl_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewwtpWirelessIfBeaconIntvl_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfBeaconIntvl_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewwtpWirelessIfBeaconIntvl data.
 * set (* NewwtpWirelessIfBeaconIntvl_val_ptr ) from rowreq_ctx->data
 */
    (* NewwtpWirelessIfBeaconIntvl_val_ptr ) = rowreq_ctx->data.NewwtpWirelessIfBeaconIntvl;

    return MFD_SUCCESS;
} /* NewwtpWirelessIfBeaconIntvl_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewwtpWirelessIfDtimIntvl
 * NewwtpWirelessIfDtimIntvl is subid 4 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.4
 * Description:
DTIM interval.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewwtpWirelessIfDtimIntvl.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewwtpWirelessIfDtimIntvl_map(long *mib_NewwtpWirelessIfDtimIntvl_val_ptr, long raw_NewwtpWirelessIfDtimIntvl_val)
{
    netsnmp_assert(NULL != mib_NewwtpWirelessIfDtimIntvl_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfDtimIntvl_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewwtpWirelessIfDtimIntvl mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewwtpWirelessIfDtimIntvl_val_ptr) = raw_NewwtpWirelessIfDtimIntvl_val;

    return MFD_SUCCESS;
} /* NewwtpWirelessIfDtimIntvl_map */

/**
 * Extract the current value of the NewwtpWirelessIfDtimIntvl data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewwtpWirelessIfDtimIntvl_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewwtpWirelessIfDtimIntvl_get( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long * NewwtpWirelessIfDtimIntvl_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewwtpWirelessIfDtimIntvl_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfDtimIntvl_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewwtpWirelessIfDtimIntvl data.
 * set (* NewwtpWirelessIfDtimIntvl_val_ptr ) from rowreq_ctx->data
 */
    (* NewwtpWirelessIfDtimIntvl_val_ptr ) = rowreq_ctx->data.NewwtpWirelessIfDtimIntvl;

    return MFD_SUCCESS;
} /* NewwtpWirelessIfDtimIntvl_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewwtpWirelessIfShtRetryThld
 * NewwtpWirelessIfShtRetryThld is subid 5 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.5
 * Description:
The total times that retry times when frame length less than RTS threshold  .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewwtpWirelessIfShtRetryThld.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewwtpWirelessIfShtRetryThld_map(long *mib_NewwtpWirelessIfShtRetryThld_val_ptr, long raw_NewwtpWirelessIfShtRetryThld_val)
{
    netsnmp_assert(NULL != mib_NewwtpWirelessIfShtRetryThld_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfShtRetryThld_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewwtpWirelessIfShtRetryThld mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewwtpWirelessIfShtRetryThld_val_ptr) = raw_NewwtpWirelessIfShtRetryThld_val;

    return MFD_SUCCESS;
} /* NewwtpWirelessIfShtRetryThld_map */

/**
 * Extract the current value of the NewwtpWirelessIfShtRetryThld data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewwtpWirelessIfShtRetryThld_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewwtpWirelessIfShtRetryThld_get( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long * NewwtpWirelessIfShtRetryThld_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewwtpWirelessIfShtRetryThld_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfShtRetryThld_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewwtpWirelessIfShtRetryThld data.
 * set (* NewwtpWirelessIfShtRetryThld_val_ptr ) from rowreq_ctx->data
 */
    (* NewwtpWirelessIfShtRetryThld_val_ptr ) = rowreq_ctx->data.NewwtpWirelessIfShtRetryThld;

    return MFD_SUCCESS;
} /* NewwtpWirelessIfShtRetryThld_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewwtpWirelessIfLongRetryThld
 * NewwtpWirelessIfLongRetryThld is subid 6 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.6
 * Description:
The total times that retry times when frame length over RTS threshold  .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewwtpWirelessIfLongRetryThld.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewwtpWirelessIfLongRetryThld_map(long *mib_NewwtpWirelessIfLongRetryThld_val_ptr, long raw_NewwtpWirelessIfLongRetryThld_val)
{
    netsnmp_assert(NULL != mib_NewwtpWirelessIfLongRetryThld_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfLongRetryThld_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewwtpWirelessIfLongRetryThld mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewwtpWirelessIfLongRetryThld_val_ptr) = raw_NewwtpWirelessIfLongRetryThld_val;

    return MFD_SUCCESS;
} /* NewwtpWirelessIfLongRetryThld_map */

/**
 * Extract the current value of the NewwtpWirelessIfLongRetryThld data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewwtpWirelessIfLongRetryThld_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewwtpWirelessIfLongRetryThld_get( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long * NewwtpWirelessIfLongRetryThld_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewwtpWirelessIfLongRetryThld_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfLongRetryThld_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewwtpWirelessIfLongRetryThld data.
 * set (* NewwtpWirelessIfLongRetryThld_val_ptr ) from rowreq_ctx->data
 */
    (* NewwtpWirelessIfLongRetryThld_val_ptr ) = rowreq_ctx->data.NewwtpWirelessIfLongRetryThld;

    return MFD_SUCCESS;
} /* NewwtpWirelessIfLongRetryThld_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewwtpWirelessIfMaxRxLifetime
 * NewwtpWirelessIfMaxRxLifetime is subid 7 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.7
 * Description:
Received data packets lifetime(units ms).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewwtpWirelessIfMaxRxLifetime.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewwtpWirelessIfMaxRxLifetime_map(u_long *mib_NewwtpWirelessIfMaxRxLifetime_val_ptr, u_long raw_NewwtpWirelessIfMaxRxLifetime_val)
{
    netsnmp_assert(NULL != mib_NewwtpWirelessIfMaxRxLifetime_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfMaxRxLifetime_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewwtpWirelessIfMaxRxLifetime mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewwtpWirelessIfMaxRxLifetime_val_ptr) = raw_NewwtpWirelessIfMaxRxLifetime_val;

    return MFD_SUCCESS;
} /* NewwtpWirelessIfMaxRxLifetime_map */

/**
 * Extract the current value of the NewwtpWirelessIfMaxRxLifetime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewwtpWirelessIfMaxRxLifetime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewwtpWirelessIfMaxRxLifetime_get( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewwtpWirelessIfMaxRxLifetime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewwtpWirelessIfMaxRxLifetime_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewwtpWirelessIfMaxRxLifetime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewwtpWirelessIfMaxRxLifetime data.
 * set (* NewwtpWirelessIfMaxRxLifetime_val_ptr ) from rowreq_ctx->data
 */
    (* NewwtpWirelessIfMaxRxLifetime_val_ptr ) = rowreq_ctx->data.NewwtpWirelessIfMaxRxLifetime;

    return MFD_SUCCESS;
} /* NewwtpWirelessIfMaxRxLifetime_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewWtpPreambleLen
 * NewWtpPreambleLen is subid 8 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.8
 * Description:
AP PreambleLen.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  long(1), short(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewWtpPreambleLen.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewWtpPreambleLen_map(u_long *mib_NewWtpPreambleLen_val_ptr, u_long raw_NewWtpPreambleLen_val)
{
    netsnmp_assert(NULL != mib_NewWtpPreambleLen_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewWtpPreambleLen_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewWtpPreambleLen enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_NewWtpPreambleLen_val) {
        case INTERNAL_NEWWTPPREAMBLELEN_LONG:
             *mib_NewWtpPreambleLen_val_ptr = NEWWTPPREAMBLELEN_LONG;
             break;

        case INTERNAL_NEWWTPPREAMBLELEN_SHORT:
             *mib_NewWtpPreambleLen_val_ptr = NEWWTPPREAMBLELEN_SHORT;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for NewWtpPreambleLen\n", raw_NewWtpPreambleLen_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* NewWtpPreambleLen_map */

/**
 * Extract the current value of the NewWtpPreambleLen data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewWtpPreambleLen_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewWtpPreambleLen_get( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewWtpPreambleLen_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewWtpPreambleLen_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewWtpPreambleLen_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewWtpPreambleLen data.
 * set (* NewWtpPreambleLen_val_ptr ) from rowreq_ctx->data
 */
    (* NewWtpPreambleLen_val_ptr ) = rowreq_ctx->data.NewWtpPreambleLen;

    return MFD_SUCCESS;
} /* NewWtpPreambleLen_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewRtsThreshold
 * NewRtsThreshold is subid 9 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.9
 * Description:
RtsThreshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewRtsThreshold.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewRtsThreshold_map(long *mib_NewRtsThreshold_val_ptr, long raw_NewRtsThreshold_val)
{
    netsnmp_assert(NULL != mib_NewRtsThreshold_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewRtsThreshold_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewRtsThreshold mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewRtsThreshold_val_ptr) = raw_NewRtsThreshold_val;

    return MFD_SUCCESS;
} /* NewRtsThreshold_map */

/**
 * Extract the current value of the NewRtsThreshold data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewRtsThreshold_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewRtsThreshold_get( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long * NewRtsThreshold_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewRtsThreshold_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewRtsThreshold_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewRtsThreshold data.
 * set (* NewRtsThreshold_val_ptr ) from rowreq_ctx->data
 */
    (* NewRtsThreshold_val_ptr ) = rowreq_ctx->data.NewRtsThreshold;

    return MFD_SUCCESS;
} /* NewRtsThreshold_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewWtpWirelessIfConfigEntry.NewFragThreshlod
 * NewFragThreshlod is subid 10 of dot11NewWtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.9.1.10
 * Description:
NewFragThreshlod.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewFragThreshlod.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewFragThreshlod_map(long *mib_NewFragThreshlod_val_ptr, long raw_NewFragThreshlod_val)
{
    netsnmp_assert(NULL != mib_NewFragThreshlod_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewFragThreshlod_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewFragThreshlod mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewFragThreshlod_val_ptr) = raw_NewFragThreshlod_val;

    return MFD_SUCCESS;
} /* NewFragThreshlod_map */

/**
 * Extract the current value of the NewFragThreshlod data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewFragThreshlod_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewFragThreshlod_get( dot11NewWtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long * NewFragThreshlod_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewFragThreshlod_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewWtpWirelessIfConfigTable:NewFragThreshlod_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewFragThreshlod data.
 * set (* NewFragThreshlod_val_ptr ) from rowreq_ctx->data
 */
    (* NewFragThreshlod_val_ptr ) = rowreq_ctx->data.NewFragThreshlod;

    return MFD_SUCCESS;
} /* NewFragThreshlod_get */



/** @} */
