/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpEndStaInfoTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpEndStaInfoTable get routines.
 * TODO:240:M: Implement dot11WtpEndStaInfoTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpEndStaInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpEndStaInfoTable is subid 2 of wtpStation.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpEndStaInfoTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */






int
wtpMacAddr_dot11WtpEndStaInfoTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */













/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpStaMacAddr
 * wtpStaMacAddr is subid 1 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.1
 * Description:
The terminal MAC address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaMacAddr_map(char **mib_wtpStaMacAddr_val_ptr_ptr, size_t *mib_wtpStaMacAddr_val_ptr_len_ptr, char *raw_wtpStaMacAddr_val_ptr, size_t raw_wtpStaMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpStaMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpStaMacAddr_val_ptr_ptr) && (NULL != mib_wtpStaMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpStaMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpStaMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpStaMacAddr_val_ptr_ptr) || (*mib_wtpStaMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpStaMacAddr_val_ptr_ptr = realloc( *mib_wtpStaMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpStaMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpStaMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpStaMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpStaMacAddr_val_ptr_ptr, raw_wtpStaMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpStaMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpEndStaInfoTable_indexes_set_tbl_idx(dot11WtpEndStaInfoTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, char *wtpStaMacAddr_val_ptr,  size_t wtpStaMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:dot11WtpEndStaInfoTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wtpStaMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpStaMacAddr_len = sizeof(tbl_idx->wtpStaMacAddr);
    /*
     * make sure there is enough space for wtpStaMacAddr data
     */
    if ((NULL == tbl_idx->wtpStaMacAddr) ||
        (tbl_idx->wtpStaMacAddr_len < (wtpStaMacAddr_val_ptr_len * sizeof(tbl_idx->wtpStaMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpStaMacAddr_len = wtpStaMacAddr_val_ptr_len * sizeof(tbl_idx->wtpStaMacAddr[0]);
    memcpy( tbl_idx->wtpStaMacAddr, wtpStaMacAddr_val_ptr, tbl_idx->wtpStaMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11WtpEndStaInfoTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpEndStaInfoTable_indexes_set(dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, char *wtpStaMacAddr_val_ptr,  size_t wtpStaMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:dot11WtpEndStaInfoTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpEndStaInfoTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpStaMacAddr_val_ptr, wtpStaMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpEndStaInfoTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpEndStaInfoTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpEndWMMSta
 * wtpEndWMMSta is subid 1 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.1
 * Description:
End station WMM property.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpEndWMMSta.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpEndWMMSta_map(long *mib_wtpEndWMMSta_val_ptr, long raw_wtpEndWMMSta_val)
{
    netsnmp_assert(NULL != mib_wtpEndWMMSta_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpEndWMMSta_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpEndWMMSta mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpEndWMMSta_val_ptr) = raw_wtpEndWMMSta_val;

    return MFD_SUCCESS;
} /* wtpEndWMMSta_map */

/**
 * Extract the current value of the wtpEndWMMSta data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpEndWMMSta_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpEndWMMSta_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, long * wtpEndWMMSta_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpEndWMMSta_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpEndWMMSta_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpEndWMMSta data.
 * set (* wtpEndWMMSta_val_ptr ) from rowreq_ctx->data
 */
    (* wtpEndWMMSta_val_ptr ) = rowreq_ctx->data.wtpEndWMMSta;

    return MFD_SUCCESS;
} /* wtpEndWMMSta_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpStaIPAddress
 * wtpStaIPAddress is subid 2 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.2
 * Description:
End station IP address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaIPAddress.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaIPAddress_map(u_long *mib_wtpStaIPAddress_val_ptr, u_long raw_wtpStaIPAddress_val)
{
    netsnmp_assert(NULL != mib_wtpStaIPAddress_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaIPAddress_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaIPAddress mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpStaIPAddress_val_ptr) = raw_wtpStaIPAddress_val;

    return MFD_SUCCESS;
} /* wtpStaIPAddress_map */

/**
 * Extract the current value of the wtpStaIPAddress data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaIPAddress_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaIPAddress_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpStaIPAddress_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaIPAddress_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaIPAddress_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaIPAddress data.
 * set (* wtpStaIPAddress_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaIPAddress_val_ptr ) = rowreq_ctx->data.wtpStaIPAddress;

    return MFD_SUCCESS;
} /* wtpStaIPAddress_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpStaRadioMode
 * wtpStaRadioMode is subid 3 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.3
 * Description:
End station wireless mode(All the above base on the 802.11 standard).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 4/8. Values:  a(1), b(2), g(4), bg(6), bgn(18)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaRadioMode.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaRadioMode_map(u_long *mib_wtpStaRadioMode_val_ptr, u_long raw_wtpStaRadioMode_val)
{
    netsnmp_assert(NULL != mib_wtpStaRadioMode_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaRadioMode_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaRadioMode enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpStaRadioMode_val) {
        case INTERNAL_WTPSTARADIOMODE_A:
             *mib_wtpStaRadioMode_val_ptr = WTPSTARADIOMODE_A;
             break;

        case INTERNAL_WTPSTARADIOMODE_B:
             *mib_wtpStaRadioMode_val_ptr = WTPSTARADIOMODE_B;
             break;

        case INTERNAL_WTPSTARADIOMODE_G:
             *mib_wtpStaRadioMode_val_ptr = WTPSTARADIOMODE_G;
             break;

        case INTERNAL_WTPSTARADIOMODE_BG:
             *mib_wtpStaRadioMode_val_ptr = WTPSTARADIOMODE_BG;
             break;

        case INTERNAL_WTPSTARADIOMODE_AN:
             *mib_wtpStaRadioMode_val_ptr = WTPSTARADIOMODE_AN;
             break;
			 
        case INTERNAL_WTPSTARADIOMODE_AAN:
             *mib_wtpStaRadioMode_val_ptr = WTPSTARADIOMODE_AAN;
             break;

        case INTERNAL_WTPSTARADIOMODE_GN:
             *mib_wtpStaRadioMode_val_ptr = WTPSTARADIOMODE_GN;
             break;

        case INTERNAL_WTPSTARADIOMODE_GGN:
             *mib_wtpStaRadioMode_val_ptr = WTPSTARADIOMODE_GGN;
             break;

        case INTERNAL_WTPSTARADIOMODE_BGGN:
             *mib_wtpStaRadioMode_val_ptr = WTPSTARADIOMODE_BGGN;
             break;
             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpStaRadioMode\n", raw_wtpStaRadioMode_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpStaRadioMode_map */

/**
 * Extract the current value of the wtpStaRadioMode data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaRadioMode_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaRadioMode_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpStaRadioMode_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaRadioMode_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaRadioMode_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaRadioMode data.
 * set (* wtpStaRadioMode_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaRadioMode_val_ptr ) = rowreq_ctx->data.wtpStaRadioMode;

    return MFD_SUCCESS;
} /* wtpStaRadioMode_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpStaRadioChannel
 * wtpStaRadioChannel is subid 4 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.4
 * Description:
Channel that end station used.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaRadioChannel.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaRadioChannel_map(long *mib_wtpStaRadioChannel_val_ptr, long raw_wtpStaRadioChannel_val)
{
    netsnmp_assert(NULL != mib_wtpStaRadioChannel_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaRadioChannel_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaRadioChannel mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpStaRadioChannel_val_ptr) = raw_wtpStaRadioChannel_val;

    return MFD_SUCCESS;
} /* wtpStaRadioChannel_map */

/**
 * Extract the current value of the wtpStaRadioChannel data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaRadioChannel_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaRadioChannel_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, long * wtpStaRadioChannel_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaRadioChannel_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaRadioChannel_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaRadioChannel data.
 * set (* wtpStaRadioChannel_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaRadioChannel_val_ptr ) = rowreq_ctx->data.wtpStaRadioChannel;

    return MFD_SUCCESS;
} /* wtpStaRadioChannel_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpAPTxRates
 * wtpAPTxRates is subid 5 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.5
 * Description:
End station current login rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpAPTxRates.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpAPTxRates_map(long *mib_wtpAPTxRates_val_ptr, long raw_wtpAPTxRates_val)
{
    netsnmp_assert(NULL != mib_wtpAPTxRates_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpAPTxRates_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpAPTxRates mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpAPTxRates_val_ptr) = raw_wtpAPTxRates_val;

    return MFD_SUCCESS;
} /* wtpAPTxRates_map */

/**
 * Extract the current value of the wtpAPTxRates data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAPTxRates_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpAPTxRates_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, long * wtpAPTxRates_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAPTxRates_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpAPTxRates_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpAPTxRates data.
 * set (* wtpAPTxRates_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAPTxRates_val_ptr ) = rowreq_ctx->data.wtpAPTxRates;

    return MFD_SUCCESS;
} /* wtpAPTxRates_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpStaPowerSaveMode
 * wtpStaPowerSaveMode is subid 6 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.6
 * Description:
End station power save mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  active(1), powersave(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaPowerSaveMode.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaPowerSaveMode_map(u_long *mib_wtpStaPowerSaveMode_val_ptr, u_long raw_wtpStaPowerSaveMode_val)
{
    netsnmp_assert(NULL != mib_wtpStaPowerSaveMode_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaPowerSaveMode_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaPowerSaveMode enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpStaPowerSaveMode_val) {
        case INTERNAL_WTPSTAPOWERSAVEMODE_ACTIVE:
             *mib_wtpStaPowerSaveMode_val_ptr = WTPSTAPOWERSAVEMODE_ACTIVE;
             break;

        case INTERNAL_WTPSTAPOWERSAVEMODE_POWERSAVE:
             *mib_wtpStaPowerSaveMode_val_ptr = WTPSTAPOWERSAVEMODE_POWERSAVE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpStaPowerSaveMode\n", raw_wtpStaPowerSaveMode_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpStaPowerSaveMode_map */

/**
 * Extract the current value of the wtpStaPowerSaveMode data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaPowerSaveMode_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaPowerSaveMode_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpStaPowerSaveMode_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaPowerSaveMode_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaPowerSaveMode_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaPowerSaveMode data.
 * set (* wtpStaPowerSaveMode_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaPowerSaveMode_val_ptr ) = rowreq_ctx->data.wtpStaPowerSaveMode;

    return MFD_SUCCESS;
} /* wtpStaPowerSaveMode_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpStaVlanId
 * wtpStaVlanId is subid 7 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.7
 * Description:
Vlan ID that end station connect in.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaVlanId.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaVlanId_map(long *mib_wtpStaVlanId_val_ptr, long raw_wtpStaVlanId_val)
{
    netsnmp_assert(NULL != mib_wtpStaVlanId_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaVlanId_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaVlanId mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpStaVlanId_val_ptr) = raw_wtpStaVlanId_val;

    return MFD_SUCCESS;
} /* wtpStaVlanId_map */

/**
 * Extract the current value of the wtpStaVlanId data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaVlanId_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaVlanId_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, long * wtpStaVlanId_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaVlanId_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaVlanId_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaVlanId data.
 * set (* wtpStaVlanId_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaVlanId_val_ptr ) = rowreq_ctx->data.wtpStaVlanId;

    return MFD_SUCCESS;
} /* wtpStaVlanId_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpStaSSIDName
 * wtpStaSSIDName is subid 8 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.8
 * Description:
SSID name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaSSIDName.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaSSIDName_map(char **mib_wtpStaSSIDName_val_ptr_ptr, size_t *mib_wtpStaSSIDName_val_ptr_len_ptr, char *raw_wtpStaSSIDName_val_ptr, size_t raw_wtpStaSSIDName_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpStaSSIDName_val_ptr);
    netsnmp_assert((NULL != mib_wtpStaSSIDName_val_ptr_ptr) && (NULL != mib_wtpStaSSIDName_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaSSIDName_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpStaSSIDName non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpStaSSIDName_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpStaSSIDName_val_ptr_ptr) || (*mib_wtpStaSSIDName_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpStaSSIDName_val_ptr_ptr = realloc( *mib_wtpStaSSIDName_val_ptr_ptr, converted_len * sizeof(**mib_wtpStaSSIDName_val_ptr_ptr));
        if(NULL == *mib_wtpStaSSIDName_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpStaSSIDName_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpStaSSIDName_val_ptr_ptr, raw_wtpStaSSIDName_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpStaSSIDName_map */

/**
 * Extract the current value of the wtpStaSSIDName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaSSIDName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpStaSSIDName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpStaSSIDName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpStaSSIDName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpStaSSIDName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpStaSSIDName_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, char **wtpStaSSIDName_val_ptr_ptr, size_t *wtpStaSSIDName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpStaSSIDName_val_ptr_ptr) && (NULL != *wtpStaSSIDName_val_ptr_ptr));
   netsnmp_assert( NULL != wtpStaSSIDName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaSSIDName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaSSIDName data.
 * set (* wtpStaSSIDName_val_ptr_ptr ) and (* wtpStaSSIDName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpStaSSIDName data
     */
    if ((NULL == (* wtpStaSSIDName_val_ptr_ptr )) ||
        ((* wtpStaSSIDName_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpStaSSIDName_len * sizeof((* wtpStaSSIDName_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpStaSSIDName data
         */
        (* wtpStaSSIDName_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpStaSSIDName_len * sizeof((* wtpStaSSIDName_val_ptr_ptr )[0]));
        if(NULL == (* wtpStaSSIDName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpStaSSIDName_val_ptr_len_ptr ) = rowreq_ctx->data.wtpStaSSIDName_len * sizeof((* wtpStaSSIDName_val_ptr_ptr )[0]);
    memcpy( (* wtpStaSSIDName_val_ptr_ptr ), rowreq_ctx->data.wtpStaSSIDName, (* wtpStaSSIDName_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpStaSSIDName_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpStaAuthenMode
 * wtpStaAuthenMode is subid 9 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.9
 * Description:
Authentication mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 1/8. Values:  opensystem(1), sharedkey(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaAuthenMode.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaAuthenMode_map(u_long *mib_wtpStaAuthenMode_val_ptr, u_long raw_wtpStaAuthenMode_val)
{
    netsnmp_assert(NULL != mib_wtpStaAuthenMode_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaAuthenMode_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaAuthenMode enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpStaAuthenMode_val) {
        case INTERNAL_WTPSTAAUTHENMODE_OPENSYSTEM:
             *mib_wtpStaAuthenMode_val_ptr = WTPSTAAUTHENMODE_OPENSYSTEM;
             break;

        case INTERNAL_WTPSTAAUTHENMODE_SHAREDKEY:
             *mib_wtpStaAuthenMode_val_ptr = WTPSTAAUTHENMODE_SHAREDKEY;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpStaAuthenMode\n", raw_wtpStaAuthenMode_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpStaAuthenMode_map */

/**
 * Extract the current value of the wtpStaAuthenMode data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaAuthenMode_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaAuthenMode_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpStaAuthenMode_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaAuthenMode_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaAuthenMode_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaAuthenMode data.
 * set (* wtpStaAuthenMode_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaAuthenMode_val_ptr ) = rowreq_ctx->data.wtpStaAuthenMode;

    return MFD_SUCCESS;
} /* wtpStaAuthenMode_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpStaSecurityCiphers
 * wtpStaSecurityCiphers is subid 10 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.10
 * Description:
Encryption mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 3/8. Values:  none(1), Wpa(2), Wpa2(3), Wapi(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaSecurityCiphers.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaSecurityCiphers_map(u_long *mib_wtpStaSecurityCiphers_val_ptr, u_long raw_wtpStaSecurityCiphers_val)
{
    netsnmp_assert(NULL != mib_wtpStaSecurityCiphers_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaSecurityCiphers_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaSecurityCiphers enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpStaSecurityCiphers_val) {
        case INTERNAL_WTPSTASECURITYCIPHERS_NONE:
             *mib_wtpStaSecurityCiphers_val_ptr = WTPSTASECURITYCIPHERS_NONE;
             break;

        case INTERNAL_WTPSTASECURITYCIPHERS_WPA:
             *mib_wtpStaSecurityCiphers_val_ptr = WTPSTASECURITYCIPHERS_WPA;
             break;

        case INTERNAL_WTPSTASECURITYCIPHERS_WPA2:
             *mib_wtpStaSecurityCiphers_val_ptr = WTPSTASECURITYCIPHERS_WPA2;
             break;

        case INTERNAL_WTPSTASECURITYCIPHERS_WAPI:
             *mib_wtpStaSecurityCiphers_val_ptr = WTPSTASECURITYCIPHERS_WAPI;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpStaSecurityCiphers\n", raw_wtpStaSecurityCiphers_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpStaSecurityCiphers_map */

/**
 * Extract the current value of the wtpStaSecurityCiphers data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaSecurityCiphers_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaSecurityCiphers_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpStaSecurityCiphers_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaSecurityCiphers_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaSecurityCiphers_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaSecurityCiphers data.
 * set (* wtpStaSecurityCiphers_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaSecurityCiphers_val_ptr ) = rowreq_ctx->data.wtpStaSecurityCiphers;

    return MFD_SUCCESS;
} /* wtpStaSecurityCiphers_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpAuthenModel
 * wtpAuthenModel is subid 11 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.11
 * Description:
wtpAuthenModel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 4/8. Values:  none(1), PSK(2), Radius(3), WAPI_CER(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpAuthenModel.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpAuthenModel_map(u_long *mib_wtpAuthenModel_val_ptr, u_long raw_wtpAuthenModel_val)
{
    netsnmp_assert(NULL != mib_wtpAuthenModel_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpAuthenModel_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpAuthenModel enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpAuthenModel_val) {
        case INTERNAL_WTPAUTHENMODEL_NONE:
             *mib_wtpAuthenModel_val_ptr = WTPAUTHENMODEL_NONE;
             break;

        case INTERNAL_WTPAUTHENMODEL_PSK:
             *mib_wtpAuthenModel_val_ptr = WTPAUTHENMODEL_PSK;
             break;

        case INTERNAL_WTPAUTHENMODEL_RADIUS:
             *mib_wtpAuthenModel_val_ptr = WTPAUTHENMODEL_RADIUS;
             break;

        case INTERNAL_WTPAUTHENMODEL_WAPI_CER:
             *mib_wtpAuthenModel_val_ptr = WTPAUTHENMODEL_WAPI_CER;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpAuthenModel\n", raw_wtpAuthenModel_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpAuthenModel_map */

/**
 * Extract the current value of the wtpAuthenModel data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAuthenModel_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpAuthenModel_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpAuthenModel_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAuthenModel_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpAuthenModel_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpAuthenModel data.
 * set (* wtpAuthenModel_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAuthenModel_val_ptr ) = rowreq_ctx->data.wtpAuthenModel;

    return MFD_SUCCESS;
} /* wtpAuthenModel_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpSecurityCiphers
 * wtpSecurityCiphers is subid 12 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.12
 * Description:
wtpSecurityCiphers.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 4/8. Values:  none(1), wep40(2), wep104(3), tkip(4), aesccmp(5), wpi_SMS4(6)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSecurityCiphers.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSecurityCiphers_map(u_long *mib_wtpSecurityCiphers_val_ptr, u_long raw_wtpSecurityCiphers_val)
{
    netsnmp_assert(NULL != mib_wtpSecurityCiphers_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpSecurityCiphers_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSecurityCiphers enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpSecurityCiphers_val) {
        case INTERNAL_WTPSECURITYCIPHERS_NONE:
             *mib_wtpSecurityCiphers_val_ptr = WTPSECURITYCIPHERS_NONE;
             break;

        case INTERNAL_WTPSECURITYCIPHERS_WEP40:
             *mib_wtpSecurityCiphers_val_ptr = WTPSECURITYCIPHERS_WEP40;
             break;

        case INTERNAL_WTPSECURITYCIPHERS_WEP104:
             *mib_wtpSecurityCiphers_val_ptr = WTPSECURITYCIPHERS_WEP104;
             break;

        case INTERNAL_WTPSECURITYCIPHERS_TKIP:
             *mib_wtpSecurityCiphers_val_ptr = WTPSECURITYCIPHERS_TKIP;
             break;

        case INTERNAL_WTPSECURITYCIPHERS_AESCCMP:
             *mib_wtpSecurityCiphers_val_ptr = WTPSECURITYCIPHERS_AESCCMP;
             break;

        case INTERNAL_WTPSECURITYCIPHERS_WPI_SMS4:
             *mib_wtpSecurityCiphers_val_ptr = WTPSECURITYCIPHERS_WPI_SMS4;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpSecurityCiphers\n", raw_wtpSecurityCiphers_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpSecurityCiphers_map */

/**
 * Extract the current value of the wtpSecurityCiphers data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSecurityCiphers_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSecurityCiphers_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpSecurityCiphers_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSecurityCiphers_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpSecurityCiphers_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSecurityCiphers data.
 * set (* wtpSecurityCiphers_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSecurityCiphers_val_ptr ) = rowreq_ctx->data.wtpSecurityCiphers;

    return MFD_SUCCESS;
} /* wtpSecurityCiphers_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.apEndStaID
 * apEndStaID is subid 13 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.13
 * Description:
apID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-apEndStaID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
apEndStaID_map(long *mib_apEndStaID_val_ptr, long raw_apEndStaID_val)
{
    netsnmp_assert(NULL != mib_apEndStaID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:apEndStaID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement apEndStaID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_apEndStaID_val_ptr) = raw_apEndStaID_val;

    return MFD_SUCCESS;
} /* apEndStaID_map */

/**
 * Extract the current value of the apEndStaID data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param apEndStaID_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
apEndStaID_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, long * apEndStaID_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != apEndStaID_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:apEndStaID_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the apEndStaID data.
 * set (* apEndStaID_val_ptr ) from rowreq_ctx->data
 */
    (* apEndStaID_val_ptr ) = rowreq_ctx->data.apEndStaID;

    return MFD_SUCCESS;
} /* apEndStaID_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEndStaInfoEntry.wtpStaIPV6Address
 * wtpStaIPV6Address is subid 17 of dot11WtpEndStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.2.1.17
 * Description:
The Station IPV6Address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 2x:
 *
 * Ranges:  16;
 *
 * Its syntax is InetAddressIPv6 (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 16)
 */
/**
 * Extract the current value of the wtpStaIPV6Address data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaIPV6Address_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpStaIPV6Address_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpStaIPV6Address.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpStaIPV6Address_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpStaIPV6Address_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpStaIPV6Address_get( dot11WtpEndStaInfoTable_rowreq_ctx *rowreq_ctx, char **wtpStaIPV6Address_val_ptr_ptr, size_t *wtpStaIPV6Address_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpStaIPV6Address_val_ptr_ptr) && (NULL != *wtpStaIPV6Address_val_ptr_ptr));
   netsnmp_assert( NULL != wtpStaIPV6Address_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEndStaInfoTable:wtpStaIPV6Address_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaIPV6Address data.
 * copy (* wtpStaIPV6Address_val_ptr_ptr ) data and (* wtpStaIPV6Address_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpStaIPV6Address data
     */
    if ((NULL == (* wtpStaIPV6Address_val_ptr_ptr )) ||
        ((* wtpStaIPV6Address_val_ptr_len_ptr ) <
         (rowreq_ctx->data.wtpStaIPV6Address_len* sizeof(rowreq_ctx->data.wtpStaIPV6Address[0])))) {
        /*
         * allocate space for wtpStaIPV6Address data
         */
        (* wtpStaIPV6Address_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpStaIPV6Address_len* sizeof(rowreq_ctx->data.wtpStaIPV6Address[0]));
        if(NULL == (* wtpStaIPV6Address_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpStaIPV6Address_val_ptr_len_ptr ) = rowreq_ctx->data.wtpStaIPV6Address_len* sizeof(rowreq_ctx->data.wtpStaIPV6Address[0]);
    memcpy( (* wtpStaIPV6Address_val_ptr_ptr ), rowreq_ctx->data.wtpStaIPV6Address, rowreq_ctx->data.wtpStaIPV6Address_len* sizeof(rowreq_ctx->data.wtpStaIPV6Address[0]) );

    return MFD_SUCCESS;
} /* wtpStaIPV6Address_get */



/** @} */
