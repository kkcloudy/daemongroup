/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpIfTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpIfTable get routines.
 * TODO:240:M: Implement dot11WtpIfTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpIfTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpIfTable is subid 1 of wtpInterface.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpIfTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_map_wtpif(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpIfIndex
 * wtpIfIndex is subid 1 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.1
 * Description:
The interface number
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfIndex_map_wtpif(long *mib_wtpIfIndex_val_ptr, long raw_wtpIfIndex_val)
{
    netsnmp_assert(NULL != mib_wtpIfIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpIfIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpIfIndex_val_ptr) = raw_wtpIfIndex_val;

    return MFD_SUCCESS;
} /* wtpIfIndex_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpIfTable_indexes_set_tbl_idx(dot11WtpIfTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpIfIndex_val)
{
    DEBUGMSGTL(("verbose:dot11WtpIfTable:dot11WtpIfTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wtpIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wtpIfIndex = wtpIfIndex_val;
    

    return MFD_SUCCESS;
} /* dot11WtpIfTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpIfTable_indexes_set(dot11WtpIfTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpIfIndex_val)
{
    DEBUGMSGTL(("verbose:dot11WtpIfTable:dot11WtpIfTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpIfTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpIfIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpIfTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpIfTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpMTU
 * wtpMTU is subid 2 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.2
 * Description:
The max transport unit which sent/received on an port.(byte)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMTU.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMTU_map(long *mib_wtpMTU_val_ptr, long raw_wtpMTU_val)
{
    netsnmp_assert(NULL != mib_wtpMTU_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpMTU_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMTU mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMTU_val_ptr) = raw_wtpMTU_val;

    return MFD_SUCCESS;
} /* wtpMTU_map */

/**
 * Extract the current value of the wtpMTU data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMTU_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMTU_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, long * wtpMTU_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMTU_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpMTU_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMTU data.
 * set (* wtpMTU_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMTU_val_ptr ) = rowreq_ctx->data.wtpMTU;

    return MFD_SUCCESS;
} /* wtpMTU_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpIfSpeed
 * wtpIfSpeed is subid 3 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.3
 * Description:
AP physical interface speed,include 10M and 100M. The input must be 10 or 100,otherwise you can't set it successfully.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is GAUGE (based on perltype GAUGE)
 * The net-snmp type is ASN_GAUGE. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfSpeed.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfSpeed_map(u_long *mib_wtpIfSpeed_val_ptr, u_long raw_wtpIfSpeed_val)
{
    netsnmp_assert(NULL != mib_wtpIfSpeed_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfSpeed_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpIfSpeed mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpIfSpeed_val_ptr) = raw_wtpIfSpeed_val;

    return MFD_SUCCESS;
} /* wtpIfSpeed_map */

/**
 * Extract the current value of the wtpIfSpeed data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIfSpeed_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpIfSpeed_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, u_long * wtpIfSpeed_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIfSpeed_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfSpeed_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfSpeed data.
 * set (* wtpIfSpeed_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIfSpeed_val_ptr ) = rowreq_ctx->data.wtpIfSpeed;

    return MFD_SUCCESS;
} /* wtpIfSpeed_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpIfPhyAddr
 * wtpIfPhyAddr is subid 4 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.4
 * Description:
AP interface physical address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is PhysAddress (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfPhyAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfPhyAddr_map(char **mib_wtpIfPhyAddr_val_ptr_ptr, size_t *mib_wtpIfPhyAddr_val_ptr_len_ptr, char *raw_wtpIfPhyAddr_val_ptr, size_t raw_wtpIfPhyAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpIfPhyAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpIfPhyAddr_val_ptr_ptr) && (NULL != mib_wtpIfPhyAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfPhyAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpIfPhyAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpIfPhyAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpIfPhyAddr_val_ptr_ptr) || (*mib_wtpIfPhyAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpIfPhyAddr_val_ptr_ptr = realloc( *mib_wtpIfPhyAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpIfPhyAddr_val_ptr_ptr));
        if(NULL == *mib_wtpIfPhyAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpIfPhyAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpIfPhyAddr_val_ptr_ptr, raw_wtpIfPhyAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpIfPhyAddr_map */

/**
 * Extract the current value of the wtpIfPhyAddr data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIfPhyAddr_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpIfPhyAddr_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpIfPhyAddr.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpIfPhyAddr_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpIfPhyAddr_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpIfPhyAddr_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, char **wtpIfPhyAddr_val_ptr_ptr, size_t *wtpIfPhyAddr_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpIfPhyAddr_val_ptr_ptr) && (NULL != *wtpIfPhyAddr_val_ptr_ptr));
   netsnmp_assert( NULL != wtpIfPhyAddr_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfPhyAddr_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfPhyAddr data.
 * set (* wtpIfPhyAddr_val_ptr_ptr ) and (* wtpIfPhyAddr_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpIfPhyAddr data
     */
    if ((NULL == (* wtpIfPhyAddr_val_ptr_ptr )) ||
        ((* wtpIfPhyAddr_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpIfPhyAddr_len * sizeof((* wtpIfPhyAddr_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpIfPhyAddr data
         */
        (* wtpIfPhyAddr_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpIfPhyAddr_len * sizeof((* wtpIfPhyAddr_val_ptr_ptr )[0]));
        if(NULL == (* wtpIfPhyAddr_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpIfPhyAddr_val_ptr_len_ptr ) = rowreq_ctx->data.wtpIfPhyAddr_len * sizeof((* wtpIfPhyAddr_val_ptr_ptr )[0]);
    memcpy( (* wtpIfPhyAddr_val_ptr_ptr ), rowreq_ctx->data.wtpIfPhyAddr, (* wtpIfPhyAddr_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpIfPhyAddr_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpIfIntro
 * wtpIfIntro is subid 5 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.5
 * Description:
The discription of AP interface information.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfIntro.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfIntro_map(char **mib_wtpIfIntro_val_ptr_ptr, size_t *mib_wtpIfIntro_val_ptr_len_ptr, char *raw_wtpIfIntro_val_ptr, size_t raw_wtpIfIntro_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpIfIntro_val_ptr);
    netsnmp_assert((NULL != mib_wtpIfIntro_val_ptr_ptr) && (NULL != mib_wtpIfIntro_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfIntro_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpIfIntro non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpIfIntro_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpIfIntro_val_ptr_ptr) || (*mib_wtpIfIntro_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpIfIntro_val_ptr_ptr = realloc( *mib_wtpIfIntro_val_ptr_ptr, converted_len * sizeof(**mib_wtpIfIntro_val_ptr_ptr));
        if(NULL == *mib_wtpIfIntro_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpIfIntro_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpIfIntro_val_ptr_ptr, raw_wtpIfIntro_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpIfIntro_map */

/**
 * Extract the current value of the wtpIfIntro data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIfIntro_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpIfIntro_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpIfIntro.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpIfIntro_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpIfIntro_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpIfIntro_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, char **wtpIfIntro_val_ptr_ptr, size_t *wtpIfIntro_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpIfIntro_val_ptr_ptr) && (NULL != *wtpIfIntro_val_ptr_ptr));
   netsnmp_assert( NULL != wtpIfIntro_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfIntro_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfIntro data.
 * set (* wtpIfIntro_val_ptr_ptr ) and (* wtpIfIntro_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpIfIntro data
     */
    if ((NULL == (* wtpIfIntro_val_ptr_ptr )) ||
        ((* wtpIfIntro_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpIfIntro_len * sizeof((* wtpIfIntro_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpIfIntro data
         */
        (* wtpIfIntro_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpIfIntro_len * sizeof((* wtpIfIntro_val_ptr_ptr )[0]));
        if(NULL == (* wtpIfIntro_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpIfIntro_val_ptr_len_ptr ) = rowreq_ctx->data.wtpIfIntro_len * sizeof((* wtpIfIntro_val_ptr_ptr )[0]);
    memcpy( (* wtpIfIntro_val_ptr_ptr ), rowreq_ctx->data.wtpIfIntro, (* wtpIfIntro_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpIfIntro_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpIfName
 * wtpIfName is subid 6 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.6
 * Description:
AP interface name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfName.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfName_map(char **mib_wtpIfName_val_ptr_ptr, size_t *mib_wtpIfName_val_ptr_len_ptr, char *raw_wtpIfName_val_ptr, size_t raw_wtpIfName_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpIfName_val_ptr);
    netsnmp_assert((NULL != mib_wtpIfName_val_ptr_ptr) && (NULL != mib_wtpIfName_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfName_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpIfName non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpIfName_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpIfName_val_ptr_ptr) || (*mib_wtpIfName_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpIfName_val_ptr_ptr = realloc( *mib_wtpIfName_val_ptr_ptr, converted_len * sizeof(**mib_wtpIfName_val_ptr_ptr));
        if(NULL == *mib_wtpIfName_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpIfName_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpIfName_val_ptr_ptr, raw_wtpIfName_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpIfName_map */

/**
 * Extract the current value of the wtpIfName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIfName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpIfName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpIfName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpIfName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpIfName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpIfName_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, char **wtpIfName_val_ptr_ptr, size_t *wtpIfName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpIfName_val_ptr_ptr) && (NULL != *wtpIfName_val_ptr_ptr));
   netsnmp_assert( NULL != wtpIfName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfName data.
 * set (* wtpIfName_val_ptr_ptr ) and (* wtpIfName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpIfName data
     */
    if ((NULL == (* wtpIfName_val_ptr_ptr )) ||
        ((* wtpIfName_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpIfName_len * sizeof((* wtpIfName_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpIfName data
         */
        (* wtpIfName_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpIfName_len * sizeof((* wtpIfName_val_ptr_ptr )[0]));
        if(NULL == (* wtpIfName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpIfName_val_ptr_len_ptr ) = rowreq_ctx->data.wtpIfName_len * sizeof((* wtpIfName_val_ptr_ptr )[0]);
    memcpy( (* wtpIfName_val_ptr_ptr ), rowreq_ctx->data.wtpIfName, (* wtpIfName_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpIfName_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpIfType
 * wtpIfType is subid 7 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.7
 * Description:
AP physical interface type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 1/8. Values:  Ethernet(6)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfType.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfType_map(u_long *mib_wtpIfType_val_ptr, u_long raw_wtpIfType_val)
{
    netsnmp_assert(NULL != mib_wtpIfType_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfType_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpIfType enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpIfType_val) {
        case INTERNAL_WTPIFTYPE_ETHERNET:
             *mib_wtpIfType_val_ptr = WTPIFTYPE_ETHERNET;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpIfType\n", raw_wtpIfType_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpIfType_map */

/**
 * Extract the current value of the wtpIfType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIfType_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpIfType_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, u_long * wtpIfType_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIfType_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfType data.
 * set (* wtpIfType_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIfType_val_ptr ) = rowreq_ctx->data.wtpIfType;

    return MFD_SUCCESS;
} /* wtpIfType_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpIfAdminStatus
 * wtpIfAdminStatus is subid 8 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.8
 * Description:
AP physical  interface type status(up or down).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 1/8. Values:  down(0), up(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfAdminStatus.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfAdminStatus_map(u_long *mib_wtpIfAdminStatus_val_ptr, u_long raw_wtpIfAdminStatus_val)
{
    netsnmp_assert(NULL != mib_wtpIfAdminStatus_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfAdminStatus_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpIfAdminStatus enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpIfAdminStatus_val) {
        case INTERNAL_WTPIFADMINSTATUS_DOWN:
             *mib_wtpIfAdminStatus_val_ptr = WTPIFADMINSTATUS_DOWN;
             break;

        case INTERNAL_WTPIFADMINSTATUS_UP:
             *mib_wtpIfAdminStatus_val_ptr = WTPIFADMINSTATUS_UP;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpIfAdminStatus\n", raw_wtpIfAdminStatus_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpIfAdminStatus_map */

/**
 * Extract the current value of the wtpIfAdminStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIfAdminStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpIfAdminStatus_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, u_long * wtpIfAdminStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIfAdminStatus_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfAdminStatus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfAdminStatus data.
 * set (* wtpIfAdminStatus_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIfAdminStatus_val_ptr ) = rowreq_ctx->data.wtpIfAdminStatus;

    return MFD_SUCCESS;
} /* wtpIfAdminStatus_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpIfOperStatus
 * wtpIfOperStatus is subid 9 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.9
 * Description:
AP physical  interface operate status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 1/8. Values:  down(0), up(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfOperStatus.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfOperStatus_map(u_long *mib_wtpIfOperStatus_val_ptr, u_long raw_wtpIfOperStatus_val)
{
    netsnmp_assert(NULL != mib_wtpIfOperStatus_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfOperStatus_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpIfOperStatus enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpIfOperStatus_val) {
        case INTERNAL_WTPIFOPERSTATUS_DOWN:
             *mib_wtpIfOperStatus_val_ptr = WTPIFOPERSTATUS_DOWN;
             break;

        case INTERNAL_WTPIFOPERSTATUS_UP:
             *mib_wtpIfOperStatus_val_ptr = WTPIFOPERSTATUS_UP;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpIfOperStatus\n", raw_wtpIfOperStatus_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpIfOperStatus_map */

/**
 * Extract the current value of the wtpIfOperStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIfOperStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpIfOperStatus_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, u_long * wtpIfOperStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIfOperStatus_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfOperStatus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfOperStatus data.
 * set (* wtpIfOperStatus_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIfOperStatus_val_ptr ) = rowreq_ctx->data.wtpIfOperStatus;

    return MFD_SUCCESS;
} /* wtpIfOperStatus_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpIfLastChange
 * wtpIfLastChange is subid 10 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.10
 * Description:
The last time that AP changed current work mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfLastChange.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfLastChange_map(u_long *mib_wtpIfLastChange_val_ptr, u_long raw_wtpIfLastChange_val)
{
    netsnmp_assert(NULL != mib_wtpIfLastChange_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfLastChange_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpIfLastChange mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpIfLastChange_val_ptr) = raw_wtpIfLastChange_val;

    return MFD_SUCCESS;
} /* wtpIfLastChange_map */

/**
 * Extract the current value of the wtpIfLastChange data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIfLastChange_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpIfLastChange_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, u_long * wtpIfLastChange_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIfLastChange_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfLastChange_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfLastChange data.
 * set (* wtpIfLastChange_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIfLastChange_val_ptr ) = rowreq_ctx->data.wtpIfLastChange;

    return MFD_SUCCESS;
} /* wtpIfLastChange_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpIfUplinkRealtimeRate
 * wtpIfUplinkRealtimeRate is subid 11 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.11
 * Description:
The uplink real time rate of AP eth interface.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfUplinkRealtimeRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfUplinkRealtimeRate_map(u_long *mib_wtpIfUplinkRealtimeRate_val_ptr, u_long raw_wtpIfUplinkRealtimeRate_val)
{
    netsnmp_assert(NULL != mib_wtpIfUplinkRealtimeRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfUplinkRealtimeRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpIfUplinkRealtimeRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpIfUplinkRealtimeRate_val_ptr) = raw_wtpIfUplinkRealtimeRate_val;

    return MFD_SUCCESS;
} /* wtpIfUplinkRealtimeRate_map */

/**
 * Extract the current value of the wtpIfUplinkRealtimeRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIfUplinkRealtimeRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpIfUplinkRealtimeRate_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, u_long * wtpIfUplinkRealtimeRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIfUplinkRealtimeRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfUplinkRealtimeRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfUplinkRealtimeRate data.
 * set (* wtpIfUplinkRealtimeRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIfUplinkRealtimeRate_val_ptr ) = rowreq_ctx->data.wtpIfUplinkRealtimeRate;

    return MFD_SUCCESS;
} /* wtpIfUplinkRealtimeRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfEntry.wtpIfDownlinkRealtimeRate
 * wtpIfDownlinkRealtimeRate is subid 12 of dot11WtpIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1.1.12
 * Description:
The downlink real time rate of AP eth interface.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfDownlinkRealtimeRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfDownlinkRealtimeRate_map(u_long *mib_wtpIfDownlinkRealtimeRate_val_ptr, u_long raw_wtpIfDownlinkRealtimeRate_val)
{
    netsnmp_assert(NULL != mib_wtpIfDownlinkRealtimeRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfDownlinkRealtimeRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpIfDownlinkRealtimeRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpIfDownlinkRealtimeRate_val_ptr) = raw_wtpIfDownlinkRealtimeRate_val;

    return MFD_SUCCESS;
} /* wtpIfDownlinkRealtimeRate_map */

/**
 * Extract the current value of the wtpIfDownlinkRealtimeRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIfDownlinkRealtimeRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpIfDownlinkRealtimeRate_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, u_long * wtpIfDownlinkRealtimeRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIfDownlinkRealtimeRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfDownlinkRealtimeRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfDownlinkRealtimeRate data.
 * set (* wtpIfDownlinkRealtimeRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIfDownlinkRealtimeRate_val_ptr ) = rowreq_ctx->data.wtpIfDownlinkRealtimeRate;

    return MFD_SUCCESS;
} /* wtpIfDownlinkRealtimeRate_get */

int
wtpIfUplinkRealtimeKbyteRate_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, u_long * wtpIfUplinkRealtimeKbyteRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIfUplinkRealtimeKbyteRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfUplinkRealtimeKbyteRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfUplinkRealtimeKbyteRate data.
 * set (* wtpIfUplinkRealtimeKbyteRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIfUplinkRealtimeKbyteRate_val_ptr ) = rowreq_ctx->data.wtpIfUplinkRealtimeKbyteRate;

    return MFD_SUCCESS;
} /* wtpIfUplinkRealtimeKbyteRate_get */

int
wtpIfDownlinkRealtimeKbyteRate_get( dot11WtpIfTable_rowreq_ctx *rowreq_ctx, u_long * wtpIfDownlinkRealtimeKbyteRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIfDownlinkRealtimeKbyteRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTable:wtpIfDownlinkRealtimeKbyteRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfUplinkRealtimeKbyteRate data.
 * set (* wtpIfUplinkRealtimeKbyteRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIfDownlinkRealtimeKbyteRate_val_ptr ) = rowreq_ctx->data.wtpIfDownlinkRealtimeKbyteRate;

    return MFD_SUCCESS;
} /* wtpIfDownlinkRealtimeKbyteRate_get */


/** @} */
