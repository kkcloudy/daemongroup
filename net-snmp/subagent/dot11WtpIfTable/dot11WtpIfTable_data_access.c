/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "ws_sysinfo.h"
#include "ws_init_dbus.h"
#include "autelanWtpGroup.h"
/* include our parent header */
#include "dot11WtpIfTable.h"


#include "dot11WtpIfTable_data_access.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpIfTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpIfTable is subid 1 of wtpInterface.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.1, length: 12
*/

/**
 * initialization for dot11WtpIfTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11WtpIfTable_reg
 *        Pointer to dot11WtpIfTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11WtpIfTable_init_data(dot11WtpIfTable_registration_ptr dot11WtpIfTable_reg)
{
    DEBUGMSGTL(("verbose:dot11WtpIfTable:dot11WtpIfTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11WtpIfTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11WtpIfTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11WtpIfTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11WtpIfTable:dot11WtpIfTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11WtpIfTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11WtpIfTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11WTPIFTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11WtpIfTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11WtpIfTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11WtpIfTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11WtpIfTable_cache_load(netsnmp_container *container)
{
	
	snmp_log(LOG_DEBUG, "enter dot11WtpIfTable_cache_load\n");
	
    dot11WtpIfTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    /*
     * temporary storage for index values
     */
        /*
         * wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
         */
        /** 128 - 1(entry) - 1(col) - 1(other indexes) = 111 */
	char   wtpMacAddr[20];
	size_t      wtpMacAddr_len;
	/*
	* wtpIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
	*/
	long   wtpIfIndex;

	char temp_mac[20] = { 0 };
	memset(temp_mac,0,20);
	char wtpIfIntro[255] = { 0 };
	size_t wtpIfIntro_len = 0;
	char wtpIfName[255] = { 0 };
	size_t wtpIfName_len = 0;

	
    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_all_wtp_ifname_information_cmd\n");
    messageHead = list_connection_call_dbus_method(show_all_wtp_ifname_information_cmd, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_all_wtp_ifname_information_cmd,messageHead=%p\n", messageHead);

	
	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    struct WtpIfnameInfo *WtpHead = messageNode->message;
		    if(WtpHead)
		    {
		        struct WtpIfnameInfo *WtpShowNode = NULL;
		        for(WtpShowNode = WtpHead->WtpIfnameInfo_list; NULL != WtpShowNode; WtpShowNode = WtpShowNode->next)
		        {



    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11WtpIfTable container.
     * loop over your dot11WtpIfTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * get a line (skip blank lines)
     */

    /*
     * parse line into variables
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/


		memset(temp_mac,0,20);
		if(WtpShowNode->wtpMacAddr)
		{
			snprintf(temp_mac,sizeof(temp_mac)-1,"%02X:%02X:%02X:%02X:%02X:%02X",WtpShowNode->wtpMacAddr[0],WtpShowNode->wtpMacAddr[1],WtpShowNode->wtpMacAddr[2],
				WtpShowNode->wtpMacAddr[3],WtpShowNode->wtpMacAddr[4],WtpShowNode->wtpMacAddr[5]);
		}


		struct WtpIfInfo_sub_info *SubShowNode = NULL;
		int j=0;
		for(j=0;j<WtpShowNode->wtpIfIndexNum;j++)
		{
			if(SubShowNode == NULL)
			{
				SubShowNode = WtpShowNode->WtpIfInfo_sub_info_head;
			}
			else 
			{
				SubShowNode = SubShowNode->next;
			}
			if(SubShowNode == NULL)
			{
				break;
			}
	        /*
	         * TODO:352:M: |   |-> set indexes in new dot11WtpIfTable rowreq context.
	         */
	        wtpMacAddr_len = strlen(temp_mac);
	        rowreq_ctx = dot11WtpIfTable_allocate_rowreq_ctx();
	        if (NULL == rowreq_ctx) {
	            snmp_log(LOG_ERR, "memory allocation failed\n");
                free_dbus_message_list(&messageHead, Free_show_all_wtp_ifname_information_cmd);
	            return MFD_RESOURCE_UNAVAILABLE;
	        }
	        if(MFD_SUCCESS != dot11WtpIfTable_indexes_set(rowreq_ctx
	                               , temp_mac, wtpMacAddr_len
	                               , SubShowNode->wtpIfIndex+1
	               )) {
	            snmp_log(LOG_ERR,"error setting index while loading "
	                     "dot11WtpIfTable cache.\n");
	            dot11WtpIfTable_release_rowreq_ctx(rowreq_ctx);
	            continue;
	        }

			memcpy(&(rowreq_ctx->data.parameter), &(messageNode->parameter), sizeof(dbus_parameter));
			rowreq_ctx->data.wtpCurrID = WtpShowNode->wtpCurrID;
			rowreq_ctx->data.wtpIfIndex = SubShowNode->wtpIfIndex+1;
	        /*
	         * TODO:352:r: |   |-> populate dot11WtpIfTable data context.
	         * Populate data context here. (optionally, delay until row prep)
	         */
	    /*
	     * TRANSIENT or semi-TRANSIENT data:
	     * copy data or save any info needed to do it in row_prep.
	     */
	    /*
	     * setup/save data for wtpMTU
	     * wtpMTU(2)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpMTU mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.wtpMTU = SubShowNode->wtpMTU;
	    
	    /*
	     * setup/save data for wtpIfSpeed
	     * wtpIfSpeed(3)/GAUGE/ASN_GAUGE/u_long(u_long)//l/A/W/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpIfSpeed mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.wtpIfSpeed = SubShowNode->wtpIfSpeed/1000;

		
	    
	    /*
	     * setup/save data for wtpIfPhyAddr
	     * wtpIfPhyAddr(4)/PhysAddress/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpIfPhyAddr mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * if(MFD_SUCCESS !=
	     *    wtpIfPhyAddr_map(&rowreq_ctx->data.wtpIfPhyAddr, &rowreq_ctx->data.wtpIfPhyAddr_len,
	     *                wtpIfPhyAddr, wtpIfPhyAddr_len, 0)) {
	     *    return MFD_ERROR;
	     * }
	     */
	    /*
	     * make sure there is enough space for wtpIfPhyAddr data
	     */
	    int wtpIfPhyAddr_len = 0;
	    /*if ((NULL == rowreq_ctx->data.wtpIfPhyAddr) ||
	        (rowreq_ctx->data.wtpIfPhyAddr_len < (wtpIfPhyAddr_len * sizeof(rowreq_ctx->data.wtpIfPhyAddr[0])))) {
	        snmp_log(LOG_ERR,"not enough space for value\n");
	        return MFD_ERROR;
	    }*/
		wtpIfPhyAddr_len  = MIN(strlen(temp_mac),sizeof(rowreq_ctx->data.wtpIfPhyAddr));
	    rowreq_ctx->data.wtpIfPhyAddr_len = wtpIfPhyAddr_len * sizeof(rowreq_ctx->data.wtpIfPhyAddr[0]);
	    memcpy( rowreq_ctx->data.wtpIfPhyAddr, temp_mac, rowreq_ctx->data.wtpIfPhyAddr_len );
	    /*
	     * setup/save data for wtpIfIntro
	     * wtpIfIntro(5)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
	     */
	    /*
	     * TODO:246:r: |-> Define wtpIfIntro mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * if(MFD_SUCCESS !=
	     *    wtpIfIntro_map(&rowreq_ctx->data.wtpIfIntro, &rowreq_ctx->data.wtpIfIntro_len,
	     *                wtpIfIntro, wtpIfIntro_len, 0)) {
	     *    return MFD_ERROR;
	     * }
	     */
	    /*
	     * make sure there is enough space for wtpIfIntro data
	     */
	    /*if ((NULL == rowreq_ctx->data.wtpIfIntro) ||
	        (rowreq_ctx->data.wtpIfIntro_len < (wtpIfIntro_len * sizeof(rowreq_ctx->data.wtpIfIntro[0])))) {
	        snmp_log(LOG_ERR,"not enough space for value\n");
	        return MFD_ERROR;
	    }*/
	    #if 0
		char logo[50];
		memset(logo,0,50);
		get_logo(logo);
		delete_enter(logo);
		#endif
		
	    char eth[4][5] = {"eth0","eth1","eth2","eth3"};
		wtpIfIntro_len = MIN(strlen(eth[SubShowNode->wtpIfIndex]),sizeof(wtpIfIntro)-1);
		memset(wtpIfIntro,0,sizeof(wtpIfIntro));
		memcpy(wtpIfIntro,eth[SubShowNode->wtpIfIndex],wtpIfIntro_len);

	    rowreq_ctx->data.wtpIfIntro_len = wtpIfIntro_len * sizeof(rowreq_ctx->data.wtpIfIntro[0]);
	    memcpy( rowreq_ctx->data.wtpIfIntro, wtpIfIntro, rowreq_ctx->data.wtpIfIntro_len );
	    
	    /*
	     * setup/save data for wtpIfName
	     * wtpIfName(6)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
	     */
	    /*
	     * TODO:246:r: |-> Define wtpIfName mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * if(MFD_SUCCESS !=
	     *    wtpIfName_map(&rowreq_ctx->data.wtpIfName, &rowreq_ctx->data.wtpIfName_len,
	     *                wtpIfName, wtpIfName_len, 0)) {
	     *    return MFD_ERROR;
	     * }
	     */
	    /*
	     * make sure there is enough space for wtpIfName data
	     */
		wtpIfName_len = MIN(strlen(eth[SubShowNode->wtpIfIndex]),sizeof(wtpIfName)-1);
		memset(wtpIfName,0,sizeof(wtpIfName));
		memcpy(wtpIfName,eth[SubShowNode->wtpIfIndex],wtpIfName_len);
		
	    rowreq_ctx->data.wtpIfName_len = wtpIfName_len * sizeof(rowreq_ctx->data.wtpIfName[0]);
	    memcpy( rowreq_ctx->data.wtpIfName, wtpIfName, rowreq_ctx->data.wtpIfName_len );
	    /*
	     * setup/save data for wtpIfType
	     * wtpIfType(7)/INTEGER/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpIfType mapping.
	     * Map values between raw/native values and MIB values
	     *
	    * enums usually need mapping.
	    */
	    rowreq_ctx->data.wtpIfType = 6;
	    
	    /*
	     * setup/save data for wtpIfAdminStatus
	     * wtpIfAdminStatus(8)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpIfAdminStatus mapping.
	     * Map values between raw/native values and MIB values
	     *
	    * enums usually need mapping.
	    */

        unsigned long wtpIfAdminStatus = 0;
        if(1 ==  SubShowNode->wtpIfAdminStatus)
        {
          wtpIfAdminStatus = 1;
		}
		else 
		{
		  wtpIfAdminStatus = 0;
		}
		
	    rowreq_ctx->data.wtpIfAdminStatus = wtpIfAdminStatus;
	    
	    /*
	     * setup/save data for wtpIfOperStatus
	     * wtpIfOperStatus(9)/INTEGER/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpIfOperStatus mapping.
	     * Map values between raw/native values and MIB values
	     *
	    * enums usually need mapping.
	    */

        unsigned long wtpIfOperStatus = 0;
        if(1 ==  SubShowNode->wtpIfOperStatus)
        {
          wtpIfOperStatus = 1;
		}
		else 
		{
		  wtpIfOperStatus = 0;
		}


		rowreq_ctx->data.wtpIfOperStatus = wtpIfOperStatus;
	    
	    /*
	     * setup/save data for wtpIfLastChange
	     * wtpIfLastChange(10)/TICKS/ASN_TIMETICKS/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpIfLastChange mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.wtpIfLastChange = SubShowNode->wtpIfLastChange;
    
    /*
     * setup/save data for wtpIfUplinkRealtimeRate
     * wtpIfUplinkRealtimeRate(11)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpIfUplinkRealtimeRate mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpIfUplinkRealtimeRate = SubShowNode->wtpIfUplinkRealtimeRate;
    
    /*
     * setup/save data for wtpIfDownlinkRealtimeRate
     * wtpIfDownlinkRealtimeRate(12)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpIfDownlinkRealtimeRate mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpIfDownlinkRealtimeRate = SubShowNode->wtpIfDownlinkRealtimeRate;
    rowreq_ctx->data.wtpIfUplinkRealtimeKbyteRate = SubShowNode->wtpIfUplinkRealtimeRate / 1024;
    rowreq_ctx->data.wtpIfDownlinkRealtimeKbyteRate = SubShowNode->wtpIfDownlinkRealtimeRate / 1024;
	        /*
	         * insert into table container
	         */
        	        	if(CONTAINER_INSERT(container, rowreq_ctx))
        	        	{
        					dot11WtpIfTable_release_rowreq_ctx(rowreq_ctx);
        	        	}
        	        	++count;
        			}
            	}
            }
    	}
    	free_dbus_message_list(&messageHead, Free_show_all_wtp_ifname_information_cmd);
	}
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11WtpIfTable:dot11WtpIfTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11WtpIfTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11WtpIfTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11WtpIfTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11WtpIfTable:dot11WtpIfTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11WtpIfTable cache.
     */
} /* dot11WtpIfTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11WtpIfTable_row_prep( dot11WtpIfTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpIfTable:dot11WtpIfTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11WtpIfTable_row_prep */

/** @} */
