/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "ws_dbus_list_interface.h"
#include "nm/app/eag/eag_errcode.h"
#include "nm/app/eag/eag_conf.h"
#include "nm/app/eag/eag_interface.h"

/* include our parent header */
#include "dot11ConfigEagServerGroupTable.h"


#include "dot11ConfigEagServerGroupTable_data_access.h"
#include "ws_eag_conf.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigEagServerGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigEagServerGroupTable is subid 4 of dot11ConfigPortalServerGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4, length: 12
*/

/**
 * initialization for dot11ConfigEagServerGroupTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11ConfigEagServerGroupTable_reg
 *        Pointer to dot11ConfigEagServerGroupTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11ConfigEagServerGroupTable_init_data(dot11ConfigEagServerGroupTable_registration_ptr dot11ConfigEagServerGroupTable_reg)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11ConfigEagServerGroupTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11ConfigEagServerGroupTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11ConfigEagServerGroupTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11ConfigEagServerGroupTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11ConfigEagServerGroupTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11CONFIGEAGSERVERGROUPTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11ConfigEagServerGroupTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11ConfigEagServerGroupTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11ConfigEagServerGroupTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11ConfigEagServerGroupTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG,"dot11ConfigEagServerGroupTable_cache_load\n");
	
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * InstanceId(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   InstanceId = 0;

    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_container_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
       // return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11ConfigEagServerGroupTable container.
     * loop over your dot11ConfigEagServerGroupTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    instance_parameter *paraHead = NULL;
	instance_parameter *pq = NULL;
	int ret;
	struct eag_base_conf baseconf;	
	u_long RedirListen = 0;
	u_long EagEnable = 2;
	u_long JustAllowWirelessUserLogin = 2;
	long RadiusAcctInterval = 0;
	long MaxIdleTime = 0;
	long MaxIdleFlow = 0;
	long PortalInteractivePort = 0;
	long MaxHttpResponse = 0;
	u_long AutoCreateUserSession = 2;
	
	snmp_log(LOG_DEBUG, "enter list_instance_parameter\n");
	list_instance_parameter(&paraHead, SNMPD_INSTANCE_MASTER); 
	snmp_log(LOG_DEBUG, "exit list_instance_parameter,paraHead=%p\n", paraHead);
	for(pq = paraHead; (NULL != pq); pq = pq->next)
	{
		InstanceId = local_to_global_ID(pq->parameter, pq->parameter.instance_id, WIRELESS_MAX_NUM);

	    /*
	    ***************************************************
	    ***             START EXAMPLE CODE              ***
	    ***---------------------------------------------***/
	    /*
	     * get a line (skip blank lines)
	     */
	    //do {
	        //if (!fgets(line, sizeof(line), filep)) {
	            /* we're done */
	            //fclose(filep);
	            //filep = NULL;
	        //}
	    //} while (filep && (line[0] == '\n'));

	    /*
	     * check for end of data
	     */
	    //if(NULL == filep)
	        //break;

	    /*
	     * parse line into variables
	     */
	    /*
	    ***---------------------------------------------***
	    ***              END  EXAMPLE CODE              ***
	    ***************************************************/

	        /*
	         * TODO:352:M: |   |-> set indexes in new dot11ConfigEagServerGroupTable rowreq context.
	         * data context will be set from the param (unless NULL,
	         *      in which case a new data context will be allocated)
	         */
	        rowreq_ctx = dot11ConfigEagServerGroupTable_allocate_rowreq_ctx();
	        if (NULL == rowreq_ctx) {
	            snmp_log(LOG_ERR, "memory allocation failed\n");
				free_instance_parameter_list(&paraHead);
	            return MFD_RESOURCE_UNAVAILABLE;
	        }
	        if(MFD_SUCCESS != dot11ConfigEagServerGroupTable_indexes_set(rowreq_ctx
	                               , InstanceId
	               )) {
	            snmp_log(LOG_ERR,"error setting index while loading "
	                     "dot11ConfigEagServerGroupTable cache.\n");
	            dot11ConfigEagServerGroupTable_release_rowreq_ctx(rowreq_ctx);
	            continue;
	        }

	        /*
	         * TODO:352:r: |   |-> populate dot11ConfigEagServerGroupTable data context.
	         * Populate data context here. (optionally, delay until row prep)
	         */
	    /*
	     * TRANSIENT or semi-TRANSIENT data:
	     * copy data or save any info needed to do it in row_prep.
	     */
	    /*
	     * setup/save data for RedirListen
	     * RedirListen(2)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h
	     */
	    /** no mapping */
		memcpy(&(rowreq_ctx->data.parameter), &(pq->parameter), sizeof(dbus_parameter));

		memset( &baseconf, 0, sizeof(baseconf));
		RedirListen = 0;
		EagEnable = 2;
	    JustAllowWirelessUserLogin = 2;
	    RadiusAcctInterval = 0;
		MaxIdleTime = 0;
		MaxIdleFlow = 0;
		PortalInteractivePort = 0;
		MaxHttpResponse = 0;
		AutoCreateUserSession = 2;
		snmp_log(LOG_DEBUG, "enter eag_get_base_conf\n");
		ret = eag_get_base_conf( pq->connection,pq->parameter.local_id,pq->parameter.instance_id, &baseconf );	
		snmp_log(LOG_DEBUG, "exit eag_get_base_conf,ret=%d\n",ret);
		if( EAG_RETURN_OK == ret )
		{
			RedirListen = baseconf.nasip;
			
			if(1 == baseconf.status)	
				EagEnable = 1;
			else
				EagEnable = 2;
			
			if(1 == baseconf.force_wireless)
		    	JustAllowWirelessUserLogin = 1;
			else
				JustAllowWirelessUserLogin = 2;

			RadiusAcctInterval = baseconf.radius_acct_interval;

			MaxIdleTime = baseconf.idle_timeout;

			MaxIdleFlow = baseconf.idle_flow;

			PortalInteractivePort = baseconf.portal_port;

			MaxHttpResponse = baseconf.max_redir_times;

			if(1 == baseconf.auto_session)
		    	AutoCreateUserSession = 1;
			else
				AutoCreateUserSession = 2;
		}
			
	    rowreq_ctx->data.RedirListen = RedirListen;
	    
	    /*
	     * setup/save data for RedirListenPort
	     * RedirListenPort(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.RedirListenPort = 0;
	    
	    /*
	     * setup/save data for EagEnable
	     * EagEnable(4)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
	     */
	    /** no mapping */
		rowreq_ctx->data.EagEnable = EagEnable;
	    
	    /*
	     * setup/save data for IdleTimeFunction
	     * IdleTimeFunction(5)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.IdleTimeFunction = 2;
	    
	    /*
	     * setup/save data for DebugLog
	     * DebugLog(6)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.DebugLog = 2;
	    
	    /*
	     * setup/save data for JustAllowWirelessUserLogin
	     * JustAllowWirelessUserLogin(7)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
	     */
	    /** no mapping */
		rowreq_ctx->data.JustAllowWirelessUserLogin = JustAllowWirelessUserLogin;
	    
	    /*
	     * setup/save data for RadiusAcctInterval
	     * RadiusAcctInterval(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.RadiusAcctInterval = RadiusAcctInterval;
	    
	    /*
	     * setup/save data for MaxIdleTime
	     * MaxIdleTime(9)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.MaxIdleTime = MaxIdleTime;
	    
	    /*
	     * setup/save data for MaxIdleFlow
	     * MaxIdleFlow(10)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.MaxIdleFlow = MaxIdleFlow;
	    
	    /*
	     * setup/save data for VrrpId
	     * VrrpId(11)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.VrrpId = 0;
	    
	    /*
	     * setup/save data for PortalInteractivePort
	     * PortalInteractivePort(12)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.PortalInteractivePort = PortalInteractivePort;
	    
	    /*
	     * setup/save data for MaxHttpResponse
	     * MaxHttpResponse(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.MaxHttpResponse = MaxHttpResponse;
	    
	    /*
	     * setup/save data for AutoCreateUserSession
	     * AutoCreateUserSession(14)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
	     */
	    /** no mapping */
		rowreq_ctx->data.AutoCreateUserSession = AutoCreateUserSession;
	    
	        
	        /*
	         * insert into table container
	         */
	        if(CONTAINER_INSERT(container, rowreq_ctx))
			{
				dot11ConfigEagServerGroupTable_release_rowreq_ctx(rowreq_ctx);
			}
	        ++count;
	}
	free_instance_parameter_list(&paraHead);	


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
        //fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG,"dot11ConfigEagServerGroupTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11ConfigEagServerGroupTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11ConfigEagServerGroupTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11ConfigEagServerGroupTable cache.
     */
} /* dot11ConfigEagServerGroupTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11ConfigEagServerGroupTable_row_prep( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11ConfigEagServerGroupTable_row_prep */

/** @} */
