/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.43.2.3 $ of : mfd-interface.m2c,v $ 
 *
 * $Id:$
 */
/*
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 * ***                                                               ***
 * ***  NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE  ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THIS FILE DOES NOT CONTAIN ANY USER EDITABLE CODE.      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THE GENERATED CODE IS INTERNAL IMPLEMENTATION, AND      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***    IS SUBJECT TO CHANGE WITHOUT WARNING IN FUTURE RELEASES.   ***
 * ***                                                               ***
 * ***                                                               ***
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 */

/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigEagServerGroupTable.h"


#include <net-snmp/library/container.h>

#include "dot11ConfigEagServerGroupTable_interface.h"

/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigEagServerGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigEagServerGroupTable is subid 4 of dot11ConfigPortalServerGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4, length: 12
*/
typedef struct dot11ConfigEagServerGroupTable_interface_ctx_s {

   netsnmp_container              *container;
   netsnmp_cache                  *cache; /* optional cache */

   dot11ConfigEagServerGroupTable_registration_ptr      user_ctx;
   
   netsnmp_table_registration_info  tbl_info;

   netsnmp_baby_steps_access_methods access_multiplexer;

} dot11ConfigEagServerGroupTable_interface_ctx;

static dot11ConfigEagServerGroupTable_interface_ctx dot11ConfigEagServerGroupTable_if_ctx;

static void _dot11ConfigEagServerGroupTable_container_init(
    dot11ConfigEagServerGroupTable_interface_ctx *if_ctx);


static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_pre_request;
static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_post_request;
static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_object_lookup;
static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_get_values;
static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_check_objects;
static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_undo_setup;
static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_set_values;
static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_undo_cleanup;
static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_undo_values;
static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_commit;
static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_undo_commit;
static Netsnmp_Node_Handler _mfd_dot11ConfigEagServerGroupTable_check_dependencies;

NETSNMP_STATIC_INLINE int _dot11ConfigEagServerGroupTable_undo_column( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx,
                                                   netsnmp_variable_list *var, int column );

dot11ConfigEagServerGroupTable_data *dot11ConfigEagServerGroupTable_allocate_data(void);

/**
 * @internal
 * Initialize the table dot11ConfigEagServerGroupTable 
 *    (Define its contents and how it's structured)
 */
void
_dot11ConfigEagServerGroupTable_initialize_interface(dot11ConfigEagServerGroupTable_registration_ptr reg_ptr,  u_long flags)
{
    netsnmp_baby_steps_access_methods *access_multiplexer =
        &dot11ConfigEagServerGroupTable_if_ctx.access_multiplexer;
    netsnmp_table_registration_info *tbl_info = &dot11ConfigEagServerGroupTable_if_ctx.tbl_info;
    netsnmp_handler_registration *reginfo;
    netsnmp_mib_handler *handler;
    int    mfd_modes = 0;

    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_dot11ConfigEagServerGroupTable_initialize_interface","called\n"));


    /*************************************************
     *
     * save interface context for dot11ConfigEagServerGroupTable
     */
    /*
     * Setting up the table's definition
     */
    netsnmp_table_helper_add_indexes(tbl_info,
                                  ASN_INTEGER, /** index: InstanceId */
                             0);

    /*  Define the minimum and maximum accessible columns.  This
        optimizes retrival. */
    tbl_info->min_column = DOT11CONFIGEAGSERVERGROUPTABLE_MIN_COL;
    tbl_info->max_column = DOT11CONFIGEAGSERVERGROUPTABLE_MAX_COL;

    /*
     * save users context
     */
    dot11ConfigEagServerGroupTable_if_ctx.user_ctx = reg_ptr;

    /*
     * call data access initialization code
     */
    dot11ConfigEagServerGroupTable_init_data(reg_ptr);

    /*
     * set up the container
     */
    _dot11ConfigEagServerGroupTable_container_init(&dot11ConfigEagServerGroupTable_if_ctx);
    if (NULL == dot11ConfigEagServerGroupTable_if_ctx.container) {
        snmp_log(LOG_ERR,"could not initialize container for dot11ConfigEagServerGroupTable\n");
        return;
    }
    
    /*
     * access_multiplexer: REQUIRED wrapper for get request handling
     */
    access_multiplexer->object_lookup = _mfd_dot11ConfigEagServerGroupTable_object_lookup;
    access_multiplexer->get_values = _mfd_dot11ConfigEagServerGroupTable_get_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->pre_request = _mfd_dot11ConfigEagServerGroupTable_pre_request;
    access_multiplexer->post_request = _mfd_dot11ConfigEagServerGroupTable_post_request;


    /*
     * REQUIRED wrappers for set request handling
     */
    access_multiplexer->object_syntax_checks = _mfd_dot11ConfigEagServerGroupTable_check_objects;
    access_multiplexer->undo_setup = _mfd_dot11ConfigEagServerGroupTable_undo_setup;
    access_multiplexer->undo_cleanup = _mfd_dot11ConfigEagServerGroupTable_undo_cleanup;
    access_multiplexer->set_values = _mfd_dot11ConfigEagServerGroupTable_set_values;
    access_multiplexer->undo_sets = _mfd_dot11ConfigEagServerGroupTable_undo_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->commit = _mfd_dot11ConfigEagServerGroupTable_commit;
    access_multiplexer->undo_commit = _mfd_dot11ConfigEagServerGroupTable_undo_commit;
    
    /*
     * REQUIRED for tables with dependencies
     */
    access_multiplexer->consistency_checks = _mfd_dot11ConfigEagServerGroupTable_check_dependencies;

    /*************************************************
     *
     * Create a registration, save our reg data, register table.
     */
    DEBUGMSGTL(("dot11ConfigEagServerGroupTable:init_dot11ConfigEagServerGroupTable",
                "Registering dot11ConfigEagServerGroupTable as a mibs-for-dummies table.\n"));		 
    handler = netsnmp_baby_steps_access_multiplexer_get(access_multiplexer);
    reginfo = netsnmp_handler_registration_create("dot11ConfigEagServerGroupTable", handler,
                                                  dot11ConfigEagServerGroupTable_oid,
                                                  dot11ConfigEagServerGroupTable_oid_size,
                                                  HANDLER_CAN_BABY_STEP |
                                                  HANDLER_CAN_RWRITE
                                                  );
    if(NULL == reginfo) {
        snmp_log(LOG_ERR,"error registering table dot11ConfigEagServerGroupTable\n");
        return;
    }
    reginfo->my_reg_void = &dot11ConfigEagServerGroupTable_if_ctx;

    /*************************************************
     *
     * set up baby steps handler, create it and inject it
     */
    if( access_multiplexer->object_lookup )
        mfd_modes |= BABY_STEP_OBJECT_LOOKUP;
    if( access_multiplexer->set_values )
        mfd_modes |= BABY_STEP_SET_VALUES;
    if( access_multiplexer->irreversible_commit )
        mfd_modes |= BABY_STEP_IRREVERSIBLE_COMMIT;
    if( access_multiplexer->object_syntax_checks )
        mfd_modes |= BABY_STEP_CHECK_OBJECT;

    if( access_multiplexer->pre_request )
        mfd_modes |= BABY_STEP_PRE_REQUEST;
    if( access_multiplexer->post_request )
        mfd_modes |= BABY_STEP_POST_REQUEST;
    
    if( access_multiplexer->undo_setup )
        mfd_modes |= BABY_STEP_UNDO_SETUP;
    if( access_multiplexer->undo_cleanup )
        mfd_modes |= BABY_STEP_UNDO_CLEANUP;
    if( access_multiplexer->undo_sets )
        mfd_modes |= BABY_STEP_UNDO_SETS;
    
    if( access_multiplexer->row_creation )
        mfd_modes |= BABY_STEP_ROW_CREATE;
    if( access_multiplexer->consistency_checks )
        mfd_modes |= BABY_STEP_CHECK_CONSISTENCY;
    if( access_multiplexer->commit )
        mfd_modes |= BABY_STEP_COMMIT;
    if( access_multiplexer->undo_commit )
        mfd_modes |= BABY_STEP_UNDO_COMMIT;
    
    handler = netsnmp_baby_steps_handler_get(mfd_modes);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject row_merge helper with prefix rootoid_len + 2 (entry.col)
     */
    handler = netsnmp_get_row_merge_handler(reginfo->rootoid_len + 2);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject container_table helper
     */
    handler =
        netsnmp_container_table_handler_get(tbl_info,
                                            dot11ConfigEagServerGroupTable_if_ctx.container,
                                            TABLE_CONTAINER_KEY_NETSNMP_INDEX);
    netsnmp_inject_handler( reginfo, handler );

    /*************************************************
     *
     * inject cache helper
     */
    if(NULL != dot11ConfigEagServerGroupTable_if_ctx.cache) {
        handler = netsnmp_cache_handler_get(dot11ConfigEagServerGroupTable_if_ctx.cache);
        netsnmp_inject_handler( reginfo, handler );
    }

    /*
     * register table
     */
    netsnmp_register_table(reginfo, tbl_info);
} /* _dot11ConfigEagServerGroupTable_initialize_interface */

void
dot11ConfigEagServerGroupTable_valid_columns_set(netsnmp_column_info *vc)
{
    dot11ConfigEagServerGroupTable_if_ctx.tbl_info.valid_columns = vc;
} /* dot11ConfigEagServerGroupTable_valid_columns_set */

/**
 * @internal
 * convert the index component stored in the context to an oid
 */
int
dot11ConfigEagServerGroupTable_index_to_oid(netsnmp_index *oid_idx,
                         dot11ConfigEagServerGroupTable_mib_index *mib_idx)
{
    int err = SNMP_ERR_NOERROR;
    
    /*
     * temp storage for parsing indexes
     */
    /*
     * InstanceId(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    netsnmp_variable_list var_InstanceId;

    /*
     * set up varbinds
     */
    memset( &var_InstanceId, 0x00, sizeof(var_InstanceId) );
    var_InstanceId.type = ASN_INTEGER;

    /*
     * chain temp index varbinds together
     */
    var_InstanceId.next_variable =  NULL;


    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_index_to_oid","called\n"));

        /* InstanceId(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    snmp_set_var_value(&var_InstanceId, (u_char*)&mib_idx->InstanceId,
                       sizeof(mib_idx->InstanceId));


    err = build_oid_noalloc(oid_idx->oids, oid_idx->len, &oid_idx->len,
                           NULL, 0, &var_InstanceId);
    if(err)
        snmp_log(LOG_ERR,"error %d converting index to oid\n", err);

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers( &var_InstanceId );

    return err;
} /* dot11ConfigEagServerGroupTable_index_to_oid */

/**
 * extract dot11ConfigEagServerGroupTable indexes from a netsnmp_index
 *
 * @retval SNMP_ERR_NOERROR  : no error
 * @retval SNMP_ERR_GENERR   : error
 */
int
dot11ConfigEagServerGroupTable_index_from_oid(netsnmp_index *oid_idx,
                         dot11ConfigEagServerGroupTable_mib_index *mib_idx)
{
    int err = SNMP_ERR_NOERROR;
    
    /*
     * temp storage for parsing indexes
     */
    /*
     * InstanceId(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    netsnmp_variable_list var_InstanceId;

    /*
     * set up varbinds
     */
    memset( &var_InstanceId, 0x00, sizeof(var_InstanceId) );
    var_InstanceId.type = ASN_INTEGER;

    /*
     * chain temp index varbinds together
     */
    var_InstanceId.next_variable =  NULL;


    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_index_from_oid","called\n"));

    /*
     * parse the oid into the individual index components
     */
    err = parse_oid_indexes( oid_idx->oids, oid_idx->len,
                             &var_InstanceId );
    if (err == SNMP_ERR_NOERROR) {
        /*
         * copy out values
         */
    mib_idx->InstanceId = *((long *)var_InstanceId.val.string);


    }

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers( &var_InstanceId );

    return err;
} /* dot11ConfigEagServerGroupTable_index_from_oid */


/*
 * dot11ConfigEagServerGroupTable_allocate_data
 *
 * Purpose: create new dot11ConfigEagServerGroupTable_data.
 */
dot11ConfigEagServerGroupTable_data *
dot11ConfigEagServerGroupTable_allocate_data(void)
{
    dot11ConfigEagServerGroupTable_data *rtn = SNMP_MALLOC_TYPEDEF(dot11ConfigEagServerGroupTable_data);

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_allocate_data","called\n"));

    if(NULL == rtn) {
        snmp_log(LOG_ERR, "unable to malloc memory for new "
                 "dot11ConfigEagServerGroupTable_data.\n");
    }

    return rtn;
} /* dot11ConfigEagServerGroupTable_allocate_data */

/*
 * dot11ConfigEagServerGroupTable_release_data
 *
 * Purpose: release dot11ConfigEagServerGroupTable data.
 */
void
dot11ConfigEagServerGroupTable_release_data(dot11ConfigEagServerGroupTable_data *data)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_release_data","called\n"));

    free(data);
} /* dot11ConfigEagServerGroupTable_release_data */

/* *********************************************************************
 * @internal
 * allocate resources for a dot11ConfigEagServerGroupTable_rowreq_ctx
 */
dot11ConfigEagServerGroupTable_rowreq_ctx *
dot11ConfigEagServerGroupTable_allocate_rowreq_ctx(void)
{
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx =
                  SNMP_MALLOC_TYPEDEF(dot11ConfigEagServerGroupTable_rowreq_ctx);

    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_allocate_rowreq_ctx","called\n"));

    if(NULL == rowreq_ctx) {
        snmp_log(LOG_ERR,"Couldn't allocate memory for a "
                 "dot11ConfigEagServerGroupTable_rowreq_ctx.\n");
    }

    rowreq_ctx->oid_idx.oids = rowreq_ctx->oid_tmp;

    rowreq_ctx->dot11ConfigEagServerGroupTable_data_list = NULL;
    rowreq_ctx->dot11ConfigEagServerGroupTable_reg = dot11ConfigEagServerGroupTable_if_ctx.user_ctx;


    return rowreq_ctx;
} /* dot11ConfigEagServerGroupTable_allocate_rowreq_ctx */

/*
 * @internal
 * release resources for a dot11ConfigEagServerGroupTable_rowreq_ctx
 */
void
dot11ConfigEagServerGroupTable_release_rowreq_ctx(dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_release_rowreq_ctx","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    

    if(rowreq_ctx->undo)
        dot11ConfigEagServerGroupTable_release_data(rowreq_ctx->undo);
 
    /*
     * free index oid pointer
     */
    if(rowreq_ctx->oid_idx.oids != rowreq_ctx->oid_tmp)
        free(rowreq_ctx->oid_idx.oids);

    SNMP_FREE(rowreq_ctx);
} /* dot11ConfigEagServerGroupTable_release_rowreq_ctx */

/**
 * @internal
 * wrapper
 */
static int
_mfd_dot11ConfigEagServerGroupTable_pre_request(netsnmp_mib_handler *handler,
                            netsnmp_handler_registration *reginfo,
                            netsnmp_agent_request_info *agtreq_info,
                            netsnmp_request_info *requests)
{
    int rc = dot11ConfigEagServerGroupTable_pre_request(dot11ConfigEagServerGroupTable_if_ctx.user_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable","error %d from "
                    "dot11ConfigEagServerGroupTable_pre_request\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }
    
    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigEagServerGroupTable_pre_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_dot11ConfigEagServerGroupTable_post_request(netsnmp_mib_handler *handler,
                             netsnmp_handler_registration *reginfo,
                             netsnmp_agent_request_info *agtreq_info,
                             netsnmp_request_info *requests)
{
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx;
    int rc = dot11ConfigEagServerGroupTable_post_request(dot11ConfigEagServerGroupTable_if_ctx.user_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable","error %d from "
                    "dot11ConfigEagServerGroupTable_post_request\n", rc));
    }
    
    /*
     * if there are no errors, check for and handle row creation/deletion
     */
    rc = netsnmp_check_requests_error(requests);
    if ((SNMP_ERR_NOERROR == rc) &&
        (NULL !=
         (rowreq_ctx = netsnmp_container_table_row_extract(requests)))) {
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            rowreq_ctx->rowreq_flags &= ~MFD_ROW_CREATED;
            CONTAINER_INSERT(dot11ConfigEagServerGroupTable_if_ctx.container, rowreq_ctx);
        }
        else if (rowreq_ctx->rowreq_flags & MFD_ROW_DELETED) {
            CONTAINER_REMOVE(dot11ConfigEagServerGroupTable_if_ctx.container, rowreq_ctx);
            dot11ConfigEagServerGroupTable_release_rowreq_ctx(rowreq_ctx);
        }
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigEagServerGroupTable_post_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_dot11ConfigEagServerGroupTable_object_lookup(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_mfd_dot11ConfigEagServerGroupTable_object_lookup","called\n"));

    /*
     * get our context from mfd
     * dot11ConfigEagServerGroupTable_interface_ctx *if_ctx =
     *             (dot11ConfigEagServerGroupTable_interface_ctx *)reginfo->my_reg_void;
     */

    if(NULL == rowreq_ctx) {
        netsnmp_request_set_error_all(requests, SNMP_ERR_NOCREATION);
    }
    else {
        dot11ConfigEagServerGroupTable_row_prep(rowreq_ctx);
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigEagServerGroupTable_object_lookup */

/***********************************************************************
 *
 * GET processing
 *
 ***********************************************************************/
/*
 * @internal
 * Retrieve the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigEagServerGroupTable_get_column( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_mfd_dot11ConfigEagServerGroupTable_get_column","called\n"));


    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* (INDEX) InstanceId(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    case COLUMN_INSTANCEID:
    var->type = ASN_INTEGER;
        var->val_len = sizeof(long);
        (*var->val.integer) = rowreq_ctx->tbl_idx.InstanceId;
        break;

    /* RedirListen(2)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h */
    case COLUMN_REDIRLISTEN:
    var->val_len = sizeof(u_long);
    var->type = ASN_IPADDRESS;
rc = RedirListen_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* RedirListenPort(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_REDIRLISTENPORT:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = RedirListenPort_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* EagEnable(4)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_EAGENABLE:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = EagEnable_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* IdleTimeFunction(5)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_IDLETIMEFUNCTION:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = IdleTimeFunction_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* DebugLog(6)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_DEBUGLOG:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = DebugLog_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* JustAllowWirelessUserLogin(7)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_JUSTALLOWWIRELESSUSERLOGIN:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = JustAllowWirelessUserLogin_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* RadiusAcctInterval(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIUSACCTINTERVAL:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = RadiusAcctInterval_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* MaxIdleTime(9)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXIDLETIME:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = MaxIdleTime_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* MaxIdleFlow(10)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXIDLEFLOW:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = MaxIdleFlow_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* VrrpId(11)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_VRRPID:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = VrrpId_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* PortalInteractivePort(12)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_PORTALINTERACTIVEPORT:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = PortalInteractivePort_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* MaxHttpResponse(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXHTTPRESPONSE:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = MaxHttpResponse_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* AutoCreateUserSession(14)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_AUTOCREATEUSERSESSION:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = AutoCreateUserSession_get(rowreq_ctx, (u_long *)var->val.string );
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigEagServerGroupTable_get_column\n", column);
         break;
    }

    return rc;
} /* _dot11ConfigEagServerGroupTable_get_column */

int
_mfd_dot11ConfigEagServerGroupTable_get_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    u_char                     * old_string;
    void                      (*dataFreeHook)(void *);
    int                        rc;

    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_mfd_dot11ConfigEagServerGroupTable_get_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    for(;requests; requests = requests->next) {
        /*
         * save old pointer, so we can free it if replaced
         */
        old_string = requests->requestvb->val.string;
        dataFreeHook = requests->requestvb->dataFreeHook;
        if(NULL == requests->requestvb->val.string) {
            requests->requestvb->val.string = requests->requestvb->buf;
            requests->requestvb->val_len = sizeof(requests->requestvb->buf);
        }
        else if(requests->requestvb->buf == requests->requestvb->val.string) {
            if(requests->requestvb->val_len != sizeof(requests->requestvb->buf))
                requests->requestvb->val_len = sizeof(requests->requestvb->buf);
        }

        /*
         * get column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _dot11ConfigEagServerGroupTable_get_column(rowreq_ctx, requests->requestvb, tri->colnum);
        if(rc) {
            if(MFD_SKIP == rc) {
                requests->requestvb->type = ASN_PRIV_RETRY;
                rc = SNMP_ERR_NOERROR;
            }
        }
        else if (NULL == requests->requestvb->val.string) {
            snmp_log(LOG_ERR,"NULL varbind data pointer!\n");
            rc = SNMP_ERR_GENERR;
        }
        if(rc)
            netsnmp_request_set_error(requests, SNMP_VALIDATE_ERR(rc));

        /*
         * if the buffer wasn't used previously for the old data (i.e. it
         * was allcoated memory)  and the get routine replaced the pointer,
         * we need to free the previous pointer.
         */
        if(old_string && (old_string != requests->requestvb->buf) &&
           (requests->requestvb->val.string != old_string)) {
            if(dataFreeHook)
                (*dataFreeHook)(old_string);
            else
                free(old_string);
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigEagServerGroupTable_get_values */

/***********************************************************************
 *
 * SET processing
 *
 ***********************************************************************/

/*----------------------------------------------------------------------
 *
 * SET: Syntax checks
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Check the syntax for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigEagServerGroupTable_check_column( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx,
                         netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_dot11ConfigEagServerGroupTable_check_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* RedirListen(2)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h */
    case COLUMN_REDIRLISTEN:
    rc = netsnmp_check_vb_type_and_size( var, ASN_IPADDRESS,
        sizeof( rowreq_ctx->data.RedirListen ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = RedirListen_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from RedirListen_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* RedirListenPort(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_REDIRLISTENPORT:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.RedirListenPort ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = RedirListenPort_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from RedirListenPort_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* EagEnable(4)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_EAGENABLE:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.EagEnable ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != EAGENABLE_TRUE )
 && ( *var->val.integer != EAGENABLE_FALSE )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = EagEnable_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from EagEnable_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* IdleTimeFunction(5)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_IDLETIMEFUNCTION:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.IdleTimeFunction ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != IDLETIMEFUNCTION_TRUE )
 && ( *var->val.integer != IDLETIMEFUNCTION_FALSE )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = IdleTimeFunction_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from IdleTimeFunction_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* DebugLog(6)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_DEBUGLOG:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.DebugLog ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != DEBUGLOG_TRUE )
 && ( *var->val.integer != DEBUGLOG_FALSE )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = DebugLog_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from DebugLog_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* JustAllowWirelessUserLogin(7)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_JUSTALLOWWIRELESSUSERLOGIN:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.JustAllowWirelessUserLogin ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != JUSTALLOWWIRELESSUSERLOGIN_TRUE )
 && ( *var->val.integer != JUSTALLOWWIRELESSUSERLOGIN_FALSE )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = JustAllowWirelessUserLogin_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from JustAllowWirelessUserLogin_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* RadiusAcctInterval(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIUSACCTINTERVAL:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.RadiusAcctInterval ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = RadiusAcctInterval_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from RadiusAcctInterval_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* MaxIdleTime(9)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXIDLETIME:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.MaxIdleTime ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = MaxIdleTime_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from MaxIdleTime_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* MaxIdleFlow(10)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXIDLEFLOW:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.MaxIdleFlow ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = MaxIdleFlow_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from MaxIdleFlow_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* VrrpId(11)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_VRRPID:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.VrrpId ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = VrrpId_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from VrrpId_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* PortalInteractivePort(12)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_PORTALINTERACTIVEPORT:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.PortalInteractivePort ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = PortalInteractivePort_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from PortalInteractivePort_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* MaxHttpResponse(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXHTTPRESPONSE:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.MaxHttpResponse ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = MaxHttpResponse_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from MaxHttpResponse_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* AutoCreateUserSession(14)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_AUTOCREATEUSERSESSION:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.AutoCreateUserSession ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != AUTOCREATEUSERSESSION_TRUE )
 && ( *var->val.integer != AUTOCREATEUSERSESSION_FALSE )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = AutoCreateUserSession_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from AutoCreateUserSession_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

        default: /** We shouldn't get here */
            rc = SNMP_ERR_GENERR;
            snmp_log(LOG_ERR, "unknown column %d in _dot11ConfigEagServerGroupTable_check_column\n", column);
    }

    return rc;
} /* _dot11ConfigEagServerGroupTable_check_column */

int
_mfd_dot11ConfigEagServerGroupTable_check_objects(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    int                          rc;

    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_mfd_dot11ConfigEagServerGroupTable_check_objects","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    for(;requests; requests = requests->next) {

        /*
         * get column number from table request info, and check that column
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;

        rc = _dot11ConfigEagServerGroupTable_check_column(rowreq_ctx, requests->requestvb, tri->colnum);
        if(rc) {
            netsnmp_request_set_error(requests, SNMP_VALIDATE_ERR(rc));
            break;
        }

    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigEagServerGroupTable_check_objects */



/*----------------------------------------------------------------------
 *
 * SET: check dependencies
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Check dependencies wrapper
 */
static int
_mfd_dot11ConfigEagServerGroupTable_check_dependencies(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_mfd_dot11ConfigEagServerGroupTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    rc = dot11ConfigEagServerGroupTable_check_dependencies(rowreq_ctx);
    if(rc){
        DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:mfd","error %d from "
                    "dot11ConfigEagServerGroupTable_check_dependencies\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigEagServerGroupTable_check_dependencies */

/*----------------------------------------------------------------------
 *
 * SET: Undo setup
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Set the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigEagServerGroupTable_undo_setup_column( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, int column )
{
    int rc = SNMPERR_SUCCESS;
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_dot11ConfigEagServerGroupTable_undo_setup_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* RedirListen(2)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h */
    case COLUMN_REDIRLISTEN:
        rowreq_ctx->column_set_flags |= FLAG_REDIRLISTEN;
        rc = RedirListen_undo_setup(rowreq_ctx );
        break;

    /* RedirListenPort(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_REDIRLISTENPORT:
        rowreq_ctx->column_set_flags |= FLAG_REDIRLISTENPORT;
        rc = RedirListenPort_undo_setup(rowreq_ctx );
        break;

    /* EagEnable(4)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_EAGENABLE:
        rowreq_ctx->column_set_flags |= FLAG_EAGENABLE;
        rc = EagEnable_undo_setup(rowreq_ctx );
        break;

    /* IdleTimeFunction(5)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_IDLETIMEFUNCTION:
        rowreq_ctx->column_set_flags |= FLAG_IDLETIMEFUNCTION;
        rc = IdleTimeFunction_undo_setup(rowreq_ctx );
        break;

    /* DebugLog(6)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_DEBUGLOG:
        rowreq_ctx->column_set_flags |= FLAG_DEBUGLOG;
        rc = DebugLog_undo_setup(rowreq_ctx );
        break;

    /* JustAllowWirelessUserLogin(7)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_JUSTALLOWWIRELESSUSERLOGIN:
        rowreq_ctx->column_set_flags |= FLAG_JUSTALLOWWIRELESSUSERLOGIN;
        rc = JustAllowWirelessUserLogin_undo_setup(rowreq_ctx );
        break;

    /* RadiusAcctInterval(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIUSACCTINTERVAL:
        rowreq_ctx->column_set_flags |= FLAG_RADIUSACCTINTERVAL;
        rc = RadiusAcctInterval_undo_setup(rowreq_ctx );
        break;

    /* MaxIdleTime(9)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXIDLETIME:
        rowreq_ctx->column_set_flags |= FLAG_MAXIDLETIME;
        rc = MaxIdleTime_undo_setup(rowreq_ctx );
        break;

    /* MaxIdleFlow(10)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXIDLEFLOW:
        rowreq_ctx->column_set_flags |= FLAG_MAXIDLEFLOW;
        rc = MaxIdleFlow_undo_setup(rowreq_ctx );
        break;

    /* VrrpId(11)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_VRRPID:
        rowreq_ctx->column_set_flags |= FLAG_VRRPID;
        rc = VrrpId_undo_setup(rowreq_ctx );
        break;

    /* PortalInteractivePort(12)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_PORTALINTERACTIVEPORT:
        rowreq_ctx->column_set_flags |= FLAG_PORTALINTERACTIVEPORT;
        rc = PortalInteractivePort_undo_setup(rowreq_ctx );
        break;

    /* MaxHttpResponse(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXHTTPRESPONSE:
        rowreq_ctx->column_set_flags |= FLAG_MAXHTTPRESPONSE;
        rc = MaxHttpResponse_undo_setup(rowreq_ctx );
        break;

    /* AutoCreateUserSession(14)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_AUTOCREATEUSERSESSION:
        rowreq_ctx->column_set_flags |= FLAG_AUTOCREATEUSERSESSION;
        rc = AutoCreateUserSession_undo_setup(rowreq_ctx );
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigEagServerGroupTable_undo_setup_column\n", column);
         break;
    }

    return rc;
} /* _dot11ConfigEagServerGroupTable_undo_setup_column */


/**
 * @internal
 * undo setup
 */
int
_mfd_dot11ConfigEagServerGroupTable_undo_setup(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_mfd_dot11ConfigEagServerGroupTable_undo_setup","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * allocate undo context
     */
    rowreq_ctx->undo = dot11ConfigEagServerGroupTable_allocate_data();
    if(NULL == rowreq_ctx->undo) {
        /** msg already logged */
        netsnmp_request_set_error_all(requests, SNMP_ERR_RESOURCEUNAVAILABLE);
        return SNMP_ERR_NOERROR;
    }

    /*
     * row undo setup
     */
    rowreq_ctx->column_set_flags = 0;
    rc = dot11ConfigEagServerGroupTable_undo_setup(rowreq_ctx);
    if (MFD_SUCCESS != rc)  {
        DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:mfd","error %d from "
                    "dot11ConfigEagServerGroupTable_undo_setup\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }
    else {
        /*
         * column undo setup
         */
        netsnmp_table_request_info * tri;
        for(;requests; requests = requests->next) {
            /*
             * set column data
             */
            tri = netsnmp_extract_table_info(requests);
            if(NULL == tri)
                continue;
            
            rc = _dot11ConfigEagServerGroupTable_undo_setup_column(rowreq_ctx, tri->colnum);
            if(MFD_SUCCESS != rc)  {
                DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:mfd","error %d from "
                            "dot11ConfigEagServerGroupTable_undo_setup_column\n", rc));
                netsnmp_set_request_error(agtreq_info, requests, SNMP_VALIDATE_ERR(rc));
            }
        } /* for results */
    }
    
    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigEagServerGroupTable_undo_setup */

/**
 * @internal
 * undo setup
 */
int
_mfd_dot11ConfigEagServerGroupTable_undo_cleanup(netsnmp_mib_handler *handler,
                             netsnmp_handler_registration *reginfo,
                             netsnmp_agent_request_info *agtreq_info,
                             netsnmp_request_info *requests)
{
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    int rc;

    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_mfd_dot11ConfigEagServerGroupTable_undo_cleanup","called\n"));

    /*
     * failed row create in early stages has no rowreq_ctx
     */
    if (NULL == rowreq_ctx)
        return MFD_SUCCESS;

    /*
     * call user cleanup
     */
    rc = dot11ConfigEagServerGroupTable_undo_cleanup(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:mfd","error %d from "
                    "dot11ConfigEagServerGroupTable_undo_cleanup\n", rc));
    }

    /*
     * release undo context, if needed
     */
    if(rowreq_ctx->undo) {
         dot11ConfigEagServerGroupTable_release_data(rowreq_ctx->undo);
         rowreq_ctx->undo = NULL;
    }

    /*
     * clear set flags
     */
    rowreq_ctx->column_set_flags = 0;

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigEagServerGroupTable_undo_cleanup */

/*----------------------------------------------------------------------
 *
 * SET: Set values
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Set the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigEagServerGroupTable_set_column( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_dot11ConfigEagServerGroupTable_set_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* RedirListen(2)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h */
    case COLUMN_REDIRLISTEN:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_REDIRLISTEN;
        rc = RedirListen_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* RedirListenPort(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_REDIRLISTENPORT:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_REDIRLISTENPORT;
        rc = RedirListenPort_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* EagEnable(4)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_EAGENABLE:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_EAGENABLE;
        rc = EagEnable_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* IdleTimeFunction(5)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_IDLETIMEFUNCTION:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_IDLETIMEFUNCTION;
        rc = IdleTimeFunction_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* DebugLog(6)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_DEBUGLOG:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_DEBUGLOG;
        rc = DebugLog_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* JustAllowWirelessUserLogin(7)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_JUSTALLOWWIRELESSUSERLOGIN:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_JUSTALLOWWIRELESSUSERLOGIN;
        rc = JustAllowWirelessUserLogin_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* RadiusAcctInterval(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIUSACCTINTERVAL:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIUSACCTINTERVAL;
        rc = RadiusAcctInterval_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* MaxIdleTime(9)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXIDLETIME:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_MAXIDLETIME;
        rc = MaxIdleTime_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* MaxIdleFlow(10)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXIDLEFLOW:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_MAXIDLEFLOW;
        rc = MaxIdleFlow_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* VrrpId(11)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_VRRPID:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_VRRPID;
        rc = VrrpId_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* PortalInteractivePort(12)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_PORTALINTERACTIVEPORT:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_PORTALINTERACTIVEPORT;
        rc = PortalInteractivePort_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* MaxHttpResponse(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXHTTPRESPONSE:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_MAXHTTPRESPONSE;
        rc = MaxHttpResponse_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* AutoCreateUserSession(14)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_AUTOCREATEUSERSESSION:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_AUTOCREATEUSERSESSION;
        rc = AutoCreateUserSession_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigEagServerGroupTable_set_column\n", column);
         break;
    }
    
    return rc;
} /* _dot11ConfigEagServerGroupTable_set_column */

int
_mfd_dot11ConfigEagServerGroupTable_set_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    int                          rc = SNMP_ERR_NOERROR;

    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_mfd_dot11ConfigEagServerGroupTable_set_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    rowreq_ctx->column_set_flags = 0;
    for(;requests; requests = requests->next) {
        /*
         * set column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _dot11ConfigEagServerGroupTable_set_column(rowreq_ctx,
                                    requests->requestvb, tri->colnum);
        if(MFD_SUCCESS != rc)  {
            DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:mfd","error %d from "
                        "dot11ConfigEagServerGroupTable_set_column\n", rc));
            netsnmp_set_request_error(agtreq_info, requests, SNMP_VALIDATE_ERR(rc));
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigEagServerGroupTable_set_values */

/*----------------------------------------------------------------------
 *
 * SET: commit
 *
 *---------------------------------------------------------------------*/
/**
 * @internal
 * commit the values
 */
int
_mfd_dot11ConfigEagServerGroupTable_commit(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_mfd_dot11ConfigEagServerGroupTable_commit","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    rc = dot11ConfigEagServerGroupTable_commit(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:mfd","error %d from "
                    "dot11ConfigEagServerGroupTable_commit\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }

    return SNMP_ERR_NOERROR;
}

int
_mfd_dot11ConfigEagServerGroupTable_undo_commit(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_mfd_dot11ConfigEagServerGroupTable_undo_commit","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    rc = dot11ConfigEagServerGroupTable_undo_commit(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:mfd","error %d from "
                    "dot11ConfigEagServerGroupTable_undo_commit\n", rc));
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigEagServerGroupTable_commit */

/*----------------------------------------------------------------------
 *
 * SET: Undo
 *
 *---------------------------------------------------------------------*/
/**
 * @internal
 * undo the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigEagServerGroupTable_undo_column( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_dot11ConfigEagServerGroupTable_undo_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* RedirListen(2)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h */
    case COLUMN_REDIRLISTEN:
        rc = RedirListen_undo(rowreq_ctx);
        break;

    /* RedirListenPort(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_REDIRLISTENPORT:
        rc = RedirListenPort_undo(rowreq_ctx);
        break;

    /* EagEnable(4)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_EAGENABLE:
        rc = EagEnable_undo(rowreq_ctx);
        break;

    /* IdleTimeFunction(5)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_IDLETIMEFUNCTION:
        rc = IdleTimeFunction_undo(rowreq_ctx);
        break;

    /* DebugLog(6)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_DEBUGLOG:
        rc = DebugLog_undo(rowreq_ctx);
        break;

    /* JustAllowWirelessUserLogin(7)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_JUSTALLOWWIRELESSUSERLOGIN:
        rc = JustAllowWirelessUserLogin_undo(rowreq_ctx);
        break;

    /* RadiusAcctInterval(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIUSACCTINTERVAL:
        rc = RadiusAcctInterval_undo(rowreq_ctx);
        break;

    /* MaxIdleTime(9)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXIDLETIME:
        rc = MaxIdleTime_undo(rowreq_ctx);
        break;

    /* MaxIdleFlow(10)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXIDLEFLOW:
        rc = MaxIdleFlow_undo(rowreq_ctx);
        break;

    /* VrrpId(11)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_VRRPID:
        rc = VrrpId_undo(rowreq_ctx);
        break;

    /* PortalInteractivePort(12)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_PORTALINTERACTIVEPORT:
        rc = PortalInteractivePort_undo(rowreq_ctx);
        break;

    /* MaxHttpResponse(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_MAXHTTPRESPONSE:
        rc = MaxHttpResponse_undo(rowreq_ctx);
        break;

    /* AutoCreateUserSession(14)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_AUTOCREATEUSERSESSION:
        rc = AutoCreateUserSession_undo(rowreq_ctx);
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigEagServerGroupTable_undo_column\n", column);
         break;
    }

    return rc;
} /* _dot11ConfigEagServerGroupTable_undo_column */

int
_mfd_dot11ConfigEagServerGroupTable_undo_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;

    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_mfd_dot11ConfigEagServerGroupTable_undo_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    for(;requests; requests = requests->next) {
        /*
         * set column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _dot11ConfigEagServerGroupTable_undo_column(rowreq_ctx, requests->requestvb,
                                     tri->colnum);
        if (MFD_SUCCESS != rc) {
            /*
             * nothing we can do about it but log it
             */
            DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:mfd","error %d from "
                        "dot11ConfigEagServerGroupTable_undo_column\n", rc));
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigEagServerGroupTable_undo_values */

/***********************************************************************
 *
 * DATA ACCESS
 *
 ***********************************************************************/
/**
 * @internal
 */
static int
_cache_load(netsnmp_cache *cache, void *vmagic)
{
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_cache_load","called\n"));

    if((NULL == cache) || (NULL == cache->magic)) {
        snmp_log(LOG_ERR, "invalid cache for dot11ConfigEagServerGroupTable_cache_load\n");
        return -1;
    }

    /** should only be called for an invalid or expired cache */
    netsnmp_assert((0 == cache->valid) || (1 == cache->expired));
    
    /*
     * call user code
     */
    return dot11ConfigEagServerGroupTable_cache_load((netsnmp_container*)cache->magic);
} /* _cache_load */

/**
 * @internal
 */
static void
_cache_item_free(dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, void *context)
{
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_cache_item_free","called\n"));

    if(NULL == rowreq_ctx)
        return;

    dot11ConfigEagServerGroupTable_release_rowreq_ctx(rowreq_ctx);
} /* _cache_item_free */

/**
 * @internal
 */
static void
_cache_free(netsnmp_cache *cache, void *magic)
{
    netsnmp_container *container;

    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_cache_free","called\n"));

    if((NULL == cache) || (NULL == cache->magic)) {
        snmp_log(LOG_ERR, "invalid cache in dot11ConfigEagServerGroupTable_cache_free\n");
        return;
    }

    container = (netsnmp_container*)cache->magic;

    /*
     * call user code
     */
    dot11ConfigEagServerGroupTable_cache_free(container);
    
    /*
     * free all items. inefficient, but easy.
     */
    CONTAINER_CLEAR(container,
                    (netsnmp_container_obj_func *)_cache_item_free,
                    NULL);
} /* _cache_free */

/**
 * @internal
 * initialize the iterator container with functions or wrappers
 */
void
_dot11ConfigEagServerGroupTable_container_init(dot11ConfigEagServerGroupTable_interface_ctx *if_ctx)
{
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:_dot11ConfigEagServerGroupTable_container_init","called\n"));
    
    /*
     * set up the cache
     */
    if_ctx->cache = netsnmp_cache_create(30, /* timeout in seconds */
                                         _cache_load, _cache_free,
                                         dot11ConfigEagServerGroupTable_oid,
                                         dot11ConfigEagServerGroupTable_oid_size);

    if(NULL == if_ctx->cache) {
        snmp_log(LOG_ERR, "error creating cache for dot11ConfigEagServerGroupTable\n");
        return;
    }

    if_ctx->cache->flags = NETSNMP_CACHE_DONT_INVALIDATE_ON_SET;

    dot11ConfigEagServerGroupTable_container_init(&if_ctx->container, if_ctx->cache);
    if(NULL == if_ctx->container)
        if_ctx->container = netsnmp_container_find("dot11ConfigEagServerGroupTable:table_container");
    if(NULL == if_ctx->container) {
        snmp_log(LOG_ERR,"error creating container in "
                 "dot11ConfigEagServerGroupTable_container_init\n");
        return;
    }
    if_ctx->cache->magic = (void*)if_ctx->container;
} /* _dot11ConfigEagServerGroupTable_container_init */

