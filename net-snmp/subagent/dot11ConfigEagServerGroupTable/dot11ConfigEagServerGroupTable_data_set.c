/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "nm/app/eag/eag_errcode.h"
#include "nm/app/eag/eag_conf.h"
#include "nm/app/eag/eag_interface.h"

/* include our parent header */
#include "dot11ConfigEagServerGroupTable.h"


/*if eag is running, return 1*/
static int 
eag_ins_running_state(DBusConnection *connection, int hansitype, int insid)
{
	int ret = 0;
	struct eag_base_conf baseconf;		
	memset( &baseconf, 0, sizeof(baseconf));

	ret = eag_get_base_conf( connection, hansitype, insid, &baseconf );

	if( (EAG_RETURN_OK == ret) && (1 == baseconf.status) )
		ret = 1;
	else
		ret = 0;
	return ret;
}


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigEagServerGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigEagServerGroupTable is subid 4 of dot11ConfigPortalServerGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigEagServerGroupTable_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigEagServerGroupTable undo.
     * set up dot11ConfigEagServerGroupTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11ConfigEagServerGroupTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigEagServerGroupTable_undo_cleanup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigEagServerGroupTable undo.
     */

    return rc;
} /* dot11ConfigEagServerGroupTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigEagServerGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigEagServerGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigEagServerGroupTable_commit( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11ConfigEagServerGroupTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_REDIRLISTEN) {
       save_flags &= ~FLAG_REDIRLISTEN; /* clear RedirListen */
       /*
        * TODO:482:o: |-> commit column RedirListen.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column RedirListen commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo RedirListen
             */
            rowreq_ctx->column_set_flags |= FLAG_REDIRLISTEN;
       }
    }

    if (save_flags & FLAG_REDIRLISTENPORT) {
       save_flags &= ~FLAG_REDIRLISTENPORT; /* clear RedirListenPort */
       /*
        * TODO:482:o: |-> commit column RedirListenPort.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column RedirListenPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo RedirListenPort
             */
            rowreq_ctx->column_set_flags |= FLAG_REDIRLISTENPORT;
       }
    }

    if (save_flags & FLAG_EAGENABLE) {
       save_flags &= ~FLAG_EAGENABLE; /* clear EagEnable */
       /*
        * TODO:482:o: |-> commit column EagEnable.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column EagEnable commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo EagEnable
             */
            rowreq_ctx->column_set_flags |= FLAG_EAGENABLE;
       }
    }

    if (save_flags & FLAG_IDLETIMEFUNCTION) {
       save_flags &= ~FLAG_IDLETIMEFUNCTION; /* clear IdleTimeFunction */
       /*
        * TODO:482:o: |-> commit column IdleTimeFunction.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column IdleTimeFunction commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo IdleTimeFunction
             */
            rowreq_ctx->column_set_flags |= FLAG_IDLETIMEFUNCTION;
       }
    }

    if (save_flags & FLAG_DEBUGLOG) {
       save_flags &= ~FLAG_DEBUGLOG; /* clear DebugLog */
       /*
        * TODO:482:o: |-> commit column DebugLog.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column DebugLog commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo DebugLog
             */
            rowreq_ctx->column_set_flags |= FLAG_DEBUGLOG;
       }
    }

    if (save_flags & FLAG_JUSTALLOWWIRELESSUSERLOGIN) {
       save_flags &= ~FLAG_JUSTALLOWWIRELESSUSERLOGIN; /* clear JustAllowWirelessUserLogin */
       /*
        * TODO:482:o: |-> commit column JustAllowWirelessUserLogin.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column JustAllowWirelessUserLogin commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo JustAllowWirelessUserLogin
             */
            rowreq_ctx->column_set_flags |= FLAG_JUSTALLOWWIRELESSUSERLOGIN;
       }
    }

    if (save_flags & FLAG_RADIUSACCTINTERVAL) {
       save_flags &= ~FLAG_RADIUSACCTINTERVAL; /* clear RadiusAcctInterval */
       /*
        * TODO:482:o: |-> commit column RadiusAcctInterval.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column RadiusAcctInterval commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo RadiusAcctInterval
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIUSACCTINTERVAL;
       }
    }

    if (save_flags & FLAG_MAXIDLETIME) {
       save_flags &= ~FLAG_MAXIDLETIME; /* clear MaxIdleTime */
       /*
        * TODO:482:o: |-> commit column MaxIdleTime.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column MaxIdleTime commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo MaxIdleTime
             */
            rowreq_ctx->column_set_flags |= FLAG_MAXIDLETIME;
       }
    }

    if (save_flags & FLAG_MAXIDLEFLOW) {
       save_flags &= ~FLAG_MAXIDLEFLOW; /* clear MaxIdleFlow */
       /*
        * TODO:482:o: |-> commit column MaxIdleFlow.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column MaxIdleFlow commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo MaxIdleFlow
             */
            rowreq_ctx->column_set_flags |= FLAG_MAXIDLEFLOW;
       }
    }

    if (save_flags & FLAG_VRRPID) {
       save_flags &= ~FLAG_VRRPID; /* clear VrrpId */
       /*
        * TODO:482:o: |-> commit column VrrpId.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column VrrpId commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo VrrpId
             */
            rowreq_ctx->column_set_flags |= FLAG_VRRPID;
       }
    }

    if (save_flags & FLAG_PORTALINTERACTIVEPORT) {
       save_flags &= ~FLAG_PORTALINTERACTIVEPORT; /* clear PortalInteractivePort */
       /*
        * TODO:482:o: |-> commit column PortalInteractivePort.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column PortalInteractivePort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo PortalInteractivePort
             */
            rowreq_ctx->column_set_flags |= FLAG_PORTALINTERACTIVEPORT;
       }
    }

    if (save_flags & FLAG_MAXHTTPRESPONSE) {
       save_flags &= ~FLAG_MAXHTTPRESPONSE; /* clear MaxHttpResponse */
       /*
        * TODO:482:o: |-> commit column MaxHttpResponse.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column MaxHttpResponse commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo MaxHttpResponse
             */
            rowreq_ctx->column_set_flags |= FLAG_MAXHTTPRESPONSE;
       }
    }

    if (save_flags & FLAG_AUTOCREATEUSERSESSION) {
       save_flags &= ~FLAG_AUTOCREATEUSERSESSION; /* clear AutoCreateUserSession */
       /*
        * TODO:482:o: |-> commit column AutoCreateUserSession.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigEagServerGroupTable column AutoCreateUserSession commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo AutoCreateUserSession
             */
            rowreq_ctx->column_set_flags |= FLAG_AUTOCREATEUSERSESSION;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigEagServerGroupTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigEagServerGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigEagServerGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigEagServerGroupTable_undo_commit( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigEagServerGroupTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11ConfigEagServerGroupTable_undo_commit */

/*
 * TODO:420:r: Implement dot11ConfigEagServerGroupTable index validation.
 */
/*
 * TODO:440:M: Implement dot11ConfigEagServerGroupTable node value checks.
 * TODO:450:M: Implement dot11ConfigEagServerGroupTable undo functions.
 * TODO:460:M: Implement dot11ConfigEagServerGroupTable set functions.
 * TODO:480:M: Implement dot11ConfigEagServerGroupTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.RedirListen
 * RedirListen is subid 2 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.2
 * Description:
eag redirListen ip.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RedirListen_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
RedirListen_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long RedirListen_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RedirListen_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid RedirListen value.
     */

    return MFD_SUCCESS; /* RedirListen value not illegal */
} /* RedirListen_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
RedirListen_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RedirListen_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup RedirListen undo.
     */
    /*
     * copy RedirListen data
     * set rowreq_ctx->undo->RedirListen from rowreq_ctx->data.RedirListen
     */
    rowreq_ctx->undo->RedirListen = rowreq_ctx->data.RedirListen;


    return MFD_SUCCESS;
} /* RedirListen_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param RedirListen_val
 *        A u_long containing the new value.
 */
int
RedirListen_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long RedirListen_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RedirListen_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
/*
 * TODO:245:o: |-> Implement RedirListen reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set RedirListen value.
     * set RedirListen value in rowreq_ctx->data
     */
	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;

	int rc = MFD_ERROR;
	int ret = EAG_ERR_UNKNOWN;
	if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 0)
	{
		ret = eag_set_nasip( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id, RedirListen_val);
	}	
	
	if(EAG_RETURN_OK == ret)
	{
		rowreq_ctx->data.RedirListen = RedirListen_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* RedirListen_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
RedirListen_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RedirListen_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up RedirListen undo.
     */
    /*
     * copy RedirListen data
     * set rowreq_ctx->data.RedirListen from rowreq_ctx->undo->RedirListen
     */
    rowreq_ctx->data.RedirListen = rowreq_ctx->undo->RedirListen;

    
    return MFD_SUCCESS;
} /* RedirListen_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.RedirListenPort
 * RedirListenPort is subid 3 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.3
 * Description:
eag redirListen Port.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RedirListenPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
RedirListenPort_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long RedirListenPort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RedirListenPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid RedirListenPort value.
     */

    return MFD_SUCCESS; /* RedirListenPort value not illegal */
} /* RedirListenPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
RedirListenPort_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RedirListenPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup RedirListenPort undo.
     */
    /*
     * copy RedirListenPort data
     * set rowreq_ctx->undo->RedirListenPort from rowreq_ctx->data.RedirListenPort
     */
    rowreq_ctx->undo->RedirListenPort = rowreq_ctx->data.RedirListenPort;


    return MFD_SUCCESS;
} /* RedirListenPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param RedirListenPort_val
 *        A long containing the new value.
 */
int
RedirListenPort_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long RedirListenPort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RedirListenPort_set","called\n"));
	snmp_log(LOG_DEBUG,"RedirListenPort_val=%d\n",RedirListenPort_val);
	
    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement RedirListenPort reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set RedirListenPort value.
     * set RedirListenPort value in rowreq_ctx->data
     */
    rowreq_ctx->data.RedirListenPort = RedirListenPort_val;

    return MFD_SUCCESS;
} /* RedirListenPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
RedirListenPort_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RedirListenPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up RedirListenPort undo.
     */
    /*
     * copy RedirListenPort data
     * set rowreq_ctx->data.RedirListenPort from rowreq_ctx->undo->RedirListenPort
     */
    rowreq_ctx->data.RedirListenPort = rowreq_ctx->undo->RedirListenPort;

    
    return MFD_SUCCESS;
} /* RedirListenPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.EagEnable
 * EagEnable is subid 4 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.4
 * Description:
Whether to open the eag service.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param EagEnable_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
EagEnable_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long EagEnable_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:EagEnable_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid EagEnable value.
     */

    return MFD_SUCCESS; /* EagEnable value not illegal */
} /* EagEnable_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
EagEnable_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:EagEnable_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup EagEnable undo.
     */
    /*
     * copy EagEnable data
     * set rowreq_ctx->undo->EagEnable from rowreq_ctx->data.EagEnable
     */
    rowreq_ctx->undo->EagEnable = rowreq_ctx->data.EagEnable;


    return MFD_SUCCESS;
} /* EagEnable_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param EagEnable_val
 *        A long containing the new value.
 */
int
EagEnable_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long EagEnable_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:EagEnable_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set EagEnable value.
     * set EagEnable value in rowreq_ctx->data
     */

	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;

	int rc = MFD_ERROR;
	int ret = EAG_ERR_UNKNOWN;
	int status = 0;
	int flag = 1;

	if(1 == EagEnable_val)
		status = 1;
	else if(2 == EagEnable_val)
		status = 0;
	else
		flag = 0;

	if(1 == flag)
	{
		ret = eag_set_services_status( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id, status);
	}

	if(EAG_RETURN_OK == ret)
	{
		rowreq_ctx->data.EagEnable = EagEnable_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}
	
    return MFD_SUCCESS;
} /* EagEnable_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
EagEnable_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:EagEnable_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up EagEnable undo.
     */
    /*
     * copy EagEnable data
     * set rowreq_ctx->data.EagEnable from rowreq_ctx->undo->EagEnable
     */
    rowreq_ctx->data.EagEnable = rowreq_ctx->undo->EagEnable;

    
    return MFD_SUCCESS;
} /* EagEnable_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.IdleTimeFunction
 * IdleTimeFunction is subid 5 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.5
 * Description:
Whether to open the idle time function.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param IdleTimeFunction_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
IdleTimeFunction_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long IdleTimeFunction_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:IdleTimeFunction_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid IdleTimeFunction value.
     */

    return MFD_SUCCESS; /* IdleTimeFunction value not illegal */
} /* IdleTimeFunction_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
IdleTimeFunction_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:IdleTimeFunction_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup IdleTimeFunction undo.
     */
    /*
     * copy IdleTimeFunction data
     * set rowreq_ctx->undo->IdleTimeFunction from rowreq_ctx->data.IdleTimeFunction
     */
    rowreq_ctx->undo->IdleTimeFunction = rowreq_ctx->data.IdleTimeFunction;


    return MFD_SUCCESS;
} /* IdleTimeFunction_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param IdleTimeFunction_val
 *        A long containing the new value.
 */
int
IdleTimeFunction_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long IdleTimeFunction_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:IdleTimeFunction_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set IdleTimeFunction value.
     * set IdleTimeFunction value in rowreq_ctx->data
     */
    rowreq_ctx->data.IdleTimeFunction = IdleTimeFunction_val;

    return MFD_SUCCESS;
} /* IdleTimeFunction_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
IdleTimeFunction_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:IdleTimeFunction_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up IdleTimeFunction undo.
     */
    /*
     * copy IdleTimeFunction data
     * set rowreq_ctx->data.IdleTimeFunction from rowreq_ctx->undo->IdleTimeFunction
     */
    rowreq_ctx->data.IdleTimeFunction = rowreq_ctx->undo->IdleTimeFunction;

    
    return MFD_SUCCESS;
} /* IdleTimeFunction_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.DebugLog
 * DebugLog is subid 6 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.6
 * Description:
Whether to open the debug log.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param DebugLog_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
DebugLog_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long DebugLog_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:DebugLog_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid DebugLog value.
     */

    return MFD_SUCCESS; /* DebugLog value not illegal */
} /* DebugLog_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
DebugLog_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:DebugLog_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup DebugLog undo.
     */
    /*
     * copy DebugLog data
     * set rowreq_ctx->undo->DebugLog from rowreq_ctx->data.DebugLog
     */
    rowreq_ctx->undo->DebugLog = rowreq_ctx->data.DebugLog;


    return MFD_SUCCESS;
} /* DebugLog_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param DebugLog_val
 *        A long containing the new value.
 */
int
DebugLog_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long DebugLog_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:DebugLog_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement DebugLog reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    rowreq_ctx->data.DebugLog = DebugLog_val;

    return MFD_SUCCESS;
} /* DebugLog_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
DebugLog_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:DebugLog_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up DebugLog undo.
     */
    /*
     * copy DebugLog data
     * set rowreq_ctx->data.DebugLog from rowreq_ctx->undo->DebugLog
     */
    rowreq_ctx->data.DebugLog = rowreq_ctx->undo->DebugLog;

    
    return MFD_SUCCESS;
} /* DebugLog_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.JustAllowWirelessUserLogin
 * JustAllowWirelessUserLogin is subid 7 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.7
 * Description:
Just Allow Wireless User Login.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param JustAllowWirelessUserLogin_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
JustAllowWirelessUserLogin_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long JustAllowWirelessUserLogin_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:JustAllowWirelessUserLogin_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid JustAllowWirelessUserLogin value.
     */

    return MFD_SUCCESS; /* JustAllowWirelessUserLogin value not illegal */
} /* JustAllowWirelessUserLogin_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
JustAllowWirelessUserLogin_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:JustAllowWirelessUserLogin_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup JustAllowWirelessUserLogin undo.
     */
    /*
     * copy JustAllowWirelessUserLogin data
     * set rowreq_ctx->undo->JustAllowWirelessUserLogin from rowreq_ctx->data.JustAllowWirelessUserLogin
     */
    rowreq_ctx->undo->JustAllowWirelessUserLogin = rowreq_ctx->data.JustAllowWirelessUserLogin;


    return MFD_SUCCESS;
} /* JustAllowWirelessUserLogin_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param JustAllowWirelessUserLogin_val
 *        A long containing the new value.
 */
int
JustAllowWirelessUserLogin_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long JustAllowWirelessUserLogin_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:JustAllowWirelessUserLogin_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set JustAllowWirelessUserLogin value.
     * set JustAllowWirelessUserLogin value in rowreq_ctx->data
     */
	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;

	int rc = MFD_ERROR;
	int ret = EAG_ERR_UNKNOWN;
	if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 0)
	{
		int force_wireless = 0;

		if(1 == JustAllowWirelessUserLogin_val)
			force_wireless = 1;	
		ret = eag_set_force_wireless( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id, force_wireless);
	}	
	
	if(EAG_RETURN_OK == ret)
	{
		rowreq_ctx->data.JustAllowWirelessUserLogin = JustAllowWirelessUserLogin_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}
	
    return MFD_SUCCESS;
} /* JustAllowWirelessUserLogin_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
JustAllowWirelessUserLogin_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:JustAllowWirelessUserLogin_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up JustAllowWirelessUserLogin undo.
     */
    /*
     * copy JustAllowWirelessUserLogin data
     * set rowreq_ctx->data.JustAllowWirelessUserLogin from rowreq_ctx->undo->JustAllowWirelessUserLogin
     */
    rowreq_ctx->data.JustAllowWirelessUserLogin = rowreq_ctx->undo->JustAllowWirelessUserLogin;

    
    return MFD_SUCCESS;
} /* JustAllowWirelessUserLogin_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.RadiusAcctInterval
 * RadiusAcctInterval is subid 8 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.8
 * Description:
Radius acct interval.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RadiusAcctInterval_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
RadiusAcctInterval_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long RadiusAcctInterval_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RadiusAcctInterval_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid RadiusAcctInterval value.
     */

    return MFD_SUCCESS; /* RadiusAcctInterval value not illegal */
} /* RadiusAcctInterval_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
RadiusAcctInterval_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RadiusAcctInterval_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup RadiusAcctInterval undo.
     */
    /*
     * copy RadiusAcctInterval data
     * set rowreq_ctx->undo->RadiusAcctInterval from rowreq_ctx->data.RadiusAcctInterval
     */
    rowreq_ctx->undo->RadiusAcctInterval = rowreq_ctx->data.RadiusAcctInterval;


    return MFD_SUCCESS;
} /* RadiusAcctInterval_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param RadiusAcctInterval_val
 *        A long containing the new value.
 */
int
RadiusAcctInterval_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long RadiusAcctInterval_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RadiusAcctInterval_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set RadiusAcctInterval value.
     * set RadiusAcctInterval value in rowreq_ctx->data
     */

	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;

	int rc = MFD_ERROR;
	int ret = EAG_ERR_UNKNOWN;

	if((RadiusAcctInterval_val>=60)&&(RadiusAcctInterval_val<=3600))
	{
		if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 0)
		{		
			int interval;
			//int idletimeout;
		
			interval = RadiusAcctInterval_val;
			//idletimeout = rowreq_ctx->data.MaxIdleTime;

			ret = eag_set_acct_interval( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id, interval);

		}	
	}
	
	if(EAG_RETURN_OK == ret)
	{
		rowreq_ctx->data.RadiusAcctInterval = RadiusAcctInterval_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}
	
    return MFD_SUCCESS;
} /* RadiusAcctInterval_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
RadiusAcctInterval_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:RadiusAcctInterval_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up RadiusAcctInterval undo.
     */
    /*
     * copy RadiusAcctInterval data
     * set rowreq_ctx->data.RadiusAcctInterval from rowreq_ctx->undo->RadiusAcctInterval
     */
    rowreq_ctx->data.RadiusAcctInterval = rowreq_ctx->undo->RadiusAcctInterval;

    
    return MFD_SUCCESS;
} /* RadiusAcctInterval_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.MaxIdleTime
 * MaxIdleTime is subid 9 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.9
 * Description:
Max Idle Time.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param MaxIdleTime_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
MaxIdleTime_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long MaxIdleTime_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxIdleTime_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid MaxIdleTime value.
     */

    return MFD_SUCCESS; /* MaxIdleTime value not illegal */
} /* MaxIdleTime_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
MaxIdleTime_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxIdleTime_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup MaxIdleTime undo.
     */
    /*
     * copy MaxIdleTime data
     * set rowreq_ctx->undo->MaxIdleTime from rowreq_ctx->data.MaxIdleTime
     */
    rowreq_ctx->undo->MaxIdleTime = rowreq_ctx->data.MaxIdleTime;


    return MFD_SUCCESS;
} /* MaxIdleTime_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param MaxIdleTime_val
 *        A long containing the new value.
 */
int
MaxIdleTime_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long MaxIdleTime_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxIdleTime_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set MaxIdleTime value.
     * set MaxIdleTime value in rowreq_ctx->data
     */

	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;

	int rc = MFD_ERROR;
	int ret = EAG_ERR_UNKNOWN;

	if((MaxIdleTime_val>=60)&&(MaxIdleTime_val<=86400))
	{
		if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 0)
		{		
			unsigned long long  idleflow;
			int idletimeout;
		
			idleflow = rowreq_ctx->data.MaxIdleFlow;
			idletimeout = MaxIdleTime_val;
		#if 0
			ret = eag_set_idleflow( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id, idleflow, idletimeout);
		#endif
		}	
		else {
			return MFD_ERROR;
		}
	}
	
	if(EAG_RETURN_OK == ret)
	{
		rowreq_ctx->data.MaxIdleTime = MaxIdleTime_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}
	
    return MFD_SUCCESS;
} /* MaxIdleTime_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
MaxIdleTime_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxIdleTime_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up MaxIdleTime undo.
     */
    /*
     * copy MaxIdleTime data
     * set rowreq_ctx->data.MaxIdleTime from rowreq_ctx->undo->MaxIdleTime
     */
    rowreq_ctx->data.MaxIdleTime = rowreq_ctx->undo->MaxIdleTime;

    
    return MFD_SUCCESS;
} /* MaxIdleTime_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.MaxIdleFlow
 * MaxIdleFlow is subid 10 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.10
 * Description:
Max Idle Flow.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param MaxIdleFlow_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
MaxIdleFlow_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long MaxIdleFlow_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxIdleFlow_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid MaxIdleFlow value.
     */

    return MFD_SUCCESS; /* MaxIdleFlow value not illegal */
} /* MaxIdleFlow_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
MaxIdleFlow_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxIdleFlow_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup MaxIdleFlow undo.
     */
    /*
     * copy MaxIdleFlow data
     * set rowreq_ctx->undo->MaxIdleFlow from rowreq_ctx->data.MaxIdleFlow
     */
    rowreq_ctx->undo->MaxIdleFlow = rowreq_ctx->data.MaxIdleFlow;


    return MFD_SUCCESS;
} /* MaxIdleFlow_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param MaxIdleFlow_val
 *        A long containing the new value.
 */
int
MaxIdleFlow_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long MaxIdleFlow_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxIdleFlow_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set MaxIdleFlow value.
     * set MaxIdleFlow value in rowreq_ctx->data
     */

	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;

	int rc = MFD_ERROR;
	int ret = EAG_ERR_UNKNOWN;
	if((MaxIdleFlow_val>=0)&&(MaxIdleFlow_val<=10485760))
	{
		if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 0)
		{		
			unsigned long long value = 0;
			int idletimeout;
		
			idletimeout = rowreq_ctx->data.MaxIdleTime;
		
			value = MaxIdleFlow_val;	
		#if 0	
			ret = eag_set_idleflow( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id, value,idletimeout);
		#endif
		}	
		else
			return MFD_ERROR;
	}
	
	if(EAG_RETURN_OK == ret)
	{
		rowreq_ctx->data.MaxIdleFlow = MaxIdleFlow_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}
	
    return MFD_SUCCESS;
} /* MaxIdleFlow_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
MaxIdleFlow_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxIdleFlow_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up MaxIdleFlow undo.
     */
    /*
     * copy MaxIdleFlow data
     * set rowreq_ctx->data.MaxIdleFlow from rowreq_ctx->undo->MaxIdleFlow
     */
    rowreq_ctx->data.MaxIdleFlow = rowreq_ctx->undo->MaxIdleFlow;

    
    return MFD_SUCCESS;
} /* MaxIdleFlow_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.VrrpId
 * VrrpId is subid 11 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.11
 * Description:
Vrrp id.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param VrrpId_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
VrrpId_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long VrrpId_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:VrrpId_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid VrrpId value.
     */

    return MFD_SUCCESS; /* VrrpId value not illegal */
} /* VrrpId_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
VrrpId_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:VrrpId_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup VrrpId undo.
     */
    /*
     * copy VrrpId data
     * set rowreq_ctx->undo->VrrpId from rowreq_ctx->data.VrrpId
     */
    rowreq_ctx->undo->VrrpId = rowreq_ctx->data.VrrpId;


    return MFD_SUCCESS;
} /* VrrpId_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param VrrpId_val
 *        A long containing the new value.
 */
int
VrrpId_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long VrrpId_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:VrrpId_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set VrrpId value.
     * set VrrpId value in rowreq_ctx->data
     */
    rowreq_ctx->data.VrrpId = VrrpId_val;

    return MFD_SUCCESS;
} /* VrrpId_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
VrrpId_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:VrrpId_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up VrrpId undo.
     */
    /*
     * copy VrrpId data
     * set rowreq_ctx->data.VrrpId from rowreq_ctx->undo->VrrpId
     */
    rowreq_ctx->data.VrrpId = rowreq_ctx->undo->VrrpId;

    
    return MFD_SUCCESS;
} /* VrrpId_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.PortalInteractivePort
 * PortalInteractivePort is subid 12 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.12
 * Description:
Portal interactive port.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalInteractivePort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
PortalInteractivePort_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long PortalInteractivePort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:PortalInteractivePort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid PortalInteractivePort value.
     */

    return MFD_SUCCESS; /* PortalInteractivePort value not illegal */
} /* PortalInteractivePort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
PortalInteractivePort_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:PortalInteractivePort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup PortalInteractivePort undo.
     */
    /*
     * copy PortalInteractivePort data
     * set rowreq_ctx->undo->PortalInteractivePort from rowreq_ctx->data.PortalInteractivePort
     */
    rowreq_ctx->undo->PortalInteractivePort = rowreq_ctx->data.PortalInteractivePort;


    return MFD_SUCCESS;
} /* PortalInteractivePort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param PortalInteractivePort_val
 *        A long containing the new value.
 */
int
PortalInteractivePort_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long PortalInteractivePort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:PortalInteractivePort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set PortalInteractivePort value.
     * set PortalInteractivePort value in rowreq_ctx->data
     */

	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;

	int rc = MFD_ERROR;
	int ret = EAG_ERR_UNKNOWN;
	if((PortalInteractivePort_val>=1)&&(PortalInteractivePort_val<=65535))
	{
		if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 0)
		{		
			unsigned short port;
		
			port = PortalInteractivePort_val;		
			ret = eag_set_portal_port( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id, port);
		}	
	}
	
	if(EAG_RETURN_OK == ret)
	{
		rowreq_ctx->data.PortalInteractivePort = PortalInteractivePort_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}
	
    return MFD_SUCCESS;
} /* PortalInteractivePort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
PortalInteractivePort_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:PortalInteractivePort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up PortalInteractivePort undo.
     */
    /*
     * copy PortalInteractivePort data
     * set rowreq_ctx->data.PortalInteractivePort from rowreq_ctx->undo->PortalInteractivePort
     */
    rowreq_ctx->data.PortalInteractivePort = rowreq_ctx->undo->PortalInteractivePort;

    
    return MFD_SUCCESS;
} /* PortalInteractivePort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.MaxHttpResponse
 * MaxHttpResponse is subid 13 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.13
 * Description:
Max http response per 5 seconds.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param MaxHttpResponse_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
MaxHttpResponse_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long MaxHttpResponse_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxHttpResponse_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid MaxHttpResponse value.
     */

    return MFD_SUCCESS; /* MaxHttpResponse value not illegal */
} /* MaxHttpResponse_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
MaxHttpResponse_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxHttpResponse_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup MaxHttpResponse undo.
     */
    /*
     * copy MaxHttpResponse data
     * set rowreq_ctx->undo->MaxHttpResponse from rowreq_ctx->data.MaxHttpResponse
     */
    rowreq_ctx->undo->MaxHttpResponse = rowreq_ctx->data.MaxHttpResponse;


    return MFD_SUCCESS;
} /* MaxHttpResponse_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param MaxHttpResponse_val
 *        A long containing the new value.
 */
int
MaxHttpResponse_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, long MaxHttpResponse_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxHttpResponse_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set MaxHttpResponse value.
     * set MaxHttpResponse value in rowreq_ctx->data
     */
	if((MaxHttpResponse_val<10)||(MaxHttpResponse_val>100))
		return MFD_ERROR;

	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;

	int rc = MFD_ERROR;
	int ret = EAG_ERR_UNKNOWN;
	if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 0)
	{		
		unsigned long request_times = 0;
	
		request_times = MaxHttpResponse_val;	
	#if 0	
		ret = eag_set_max_http_request( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id, request_times);
	#endif
	}	
	
	if(EAG_RETURN_OK == ret)
	{
		rowreq_ctx->data.MaxHttpResponse = MaxHttpResponse_val;	
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}
    return MFD_SUCCESS;
} /* MaxHttpResponse_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
MaxHttpResponse_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:MaxHttpResponse_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up MaxHttpResponse undo.
     */
    /*
     * copy MaxHttpResponse data
     * set rowreq_ctx->data.MaxHttpResponse from rowreq_ctx->undo->MaxHttpResponse
     */
    rowreq_ctx->data.MaxHttpResponse = rowreq_ctx->undo->MaxHttpResponse;

    
    return MFD_SUCCESS;
} /* MaxHttpResponse_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.AutoCreateUserSession
 * AutoCreateUserSession is subid 14 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.14
 * Description:
Auto create user session.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param AutoCreateUserSession_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigEagServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
AutoCreateUserSession_check_value( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long AutoCreateUserSession_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:AutoCreateUserSession_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid AutoCreateUserSession value.
     */

    return MFD_SUCCESS; /* AutoCreateUserSession value not illegal */
} /* AutoCreateUserSession_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigEagServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigEagServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
AutoCreateUserSession_undo_setup( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:AutoCreateUserSession_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup AutoCreateUserSession undo.
     */
    /*
     * copy AutoCreateUserSession data
     * set rowreq_ctx->undo->AutoCreateUserSession from rowreq_ctx->data.AutoCreateUserSession
     */
    rowreq_ctx->undo->AutoCreateUserSession = rowreq_ctx->data.AutoCreateUserSession;


    return MFD_SUCCESS;
} /* AutoCreateUserSession_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param AutoCreateUserSession_val
 *        A long containing the new value.
 */
int
AutoCreateUserSession_set( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx, u_long AutoCreateUserSession_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:AutoCreateUserSession_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set AutoCreateUserSession value.
     * set AutoCreateUserSession value in rowreq_ctx->data
     */

	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;

	int rc = MFD_ERROR;
	int ret = EAG_ERR_UNKNOWN;
	if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 0)
	{
		int auto_session = 0;

		if(1 == AutoCreateUserSession_val)
			auto_session = 1;
		ret = eag_set_auto_session( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id, auto_session);
	}	
	
	if(EAG_RETURN_OK == ret)
	{
		rowreq_ctx->data.AutoCreateUserSession = AutoCreateUserSession_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}
	
    return MFD_SUCCESS;
} /* AutoCreateUserSession_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
AutoCreateUserSession_undo( dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigEagServerGroupTable:AutoCreateUserSession_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up AutoCreateUserSession undo.
     */
    /*
     * copy AutoCreateUserSession data
     * set rowreq_ctx->data.AutoCreateUserSession from rowreq_ctx->undo->AutoCreateUserSession
     */
    rowreq_ctx->data.AutoCreateUserSession = rowreq_ctx->undo->AutoCreateUserSession;

    
    return MFD_SUCCESS;
} /* AutoCreateUserSession_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigEagServerGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigEagServerGroupTable if you don't have dependencies)
 */
int
dot11ConfigEagServerGroupTable_check_dependencies(dot11ConfigEagServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigEagServerGroupTable:dot11ConfigEagServerGroupTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigEagServerGroupTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11ConfigEagServerGroupTable_check_dependencies */
/** @} */
