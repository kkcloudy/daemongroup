/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11AttackMACTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11AttackMACTable get routines.
 * TODO:240:M: Implement dot11AttackMACTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11AttackMACTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11AttackMACTable is subid 1 of dot11Attack.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.11.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11AttackMACTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param attackMACID_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11AttackMACTable_indexes_set_tbl_idx(dot11AttackMACTable_mib_index *tbl_idx, long attackMACID_val)
{
    DEBUGMSGTL(("verbose:dot11AttackMACTable:dot11AttackMACTable_indexes_set_tbl_idx","called\n"));

    /* attackMACID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->attackMACID = attackMACID_val;
    

    return MFD_SUCCESS;
} /* dot11AttackMACTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11AttackMACTable_indexes_set(dot11AttackMACTable_rowreq_ctx *rowreq_ctx, long attackMACID_val)
{
    DEBUGMSGTL(("verbose:dot11AttackMACTable:dot11AttackMACTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11AttackMACTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , attackMACID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11AttackMACTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11AttackMACTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AttackMACEntry.attackMAC
 * attackMAC is subid 2 of dot11AttackMACEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.11.1.1.2
 * Description:
MAC attack.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the attackMAC data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param attackMAC_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param attackMAC_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by attackMAC.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*attackMAC_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update attackMAC_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
attackMAC_get( dot11AttackMACTable_rowreq_ctx *rowreq_ctx, char **attackMAC_val_ptr_ptr, size_t *attackMAC_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != attackMAC_val_ptr_ptr) && (NULL != *attackMAC_val_ptr_ptr));
   netsnmp_assert( NULL != attackMAC_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11AttackMACTable:attackMAC_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the attackMAC data.
 * copy (* attackMAC_val_ptr_ptr ) data and (* attackMAC_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for attackMAC data
     */
    if ((NULL == (* attackMAC_val_ptr_ptr )) ||
        ((* attackMAC_val_ptr_len_ptr ) <
         (rowreq_ctx->data.attackMAC_len* sizeof(rowreq_ctx->data.attackMAC[0])))) {
        /*
         * allocate space for attackMAC data
         */
        (* attackMAC_val_ptr_ptr ) = malloc(rowreq_ctx->data.attackMAC_len* sizeof(rowreq_ctx->data.attackMAC[0]));
        if(NULL == (* attackMAC_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* attackMAC_val_ptr_len_ptr ) = rowreq_ctx->data.attackMAC_len* sizeof(rowreq_ctx->data.attackMAC[0]);
    memcpy( (* attackMAC_val_ptr_ptr ), rowreq_ctx->data.attackMAC, rowreq_ctx->data.attackMAC_len* sizeof(rowreq_ctx->data.attackMAC[0]) );

    return MFD_SUCCESS;
} /* attackMAC_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AttackMACEntry.attackMACRowStatus
 * attackMACRowStatus is subid 3 of dot11AttackMACEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.11.1.1.3
 * Description:
This variable is used to create, modify, and/or delete a row
		        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the attackMACRowStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param attackMACRowStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
attackMACRowStatus_get( dot11AttackMACTable_rowreq_ctx *rowreq_ctx, u_long * attackMACRowStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != attackMACRowStatus_val_ptr );

    (* attackMACRowStatus_val_ptr ) = rowreq_ctx->data.attackMACRowStatus;

    return MFD_SUCCESS;
} /* attackMACRowStatus_get */



/** @} */
