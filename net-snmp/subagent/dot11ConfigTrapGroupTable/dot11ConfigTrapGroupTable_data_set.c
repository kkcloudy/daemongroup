/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigTrapGroupTable.h"
#include "autelanWtpGroup.h"
#include "ws_snmpd_engine.h"
#include "ac_manage_interface.h"
#include "ws_dcli_wlans.h"
#include "ws_usrinfo.h"
#include "ws_err.h"
#include "ws_ec.h"
#include "ws_init_dbus.h"
#include "ws_dbus_list_interface.h"
#include "ac_manage_def.h"

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigTrapGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11ConfigTrapGroupTable is subid 1 of dot11ConfigTrapGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.19.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11ConfigTrapGroupTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11ConfigTrapGroupTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigTrapGroupTable_undo_setup( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:dot11ConfigTrapGroupTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigTrapGroupTable undo.
     * set up dot11ConfigTrapGroupTable undo information, in preparation for a set.
     * Undo storage is in (* TrapRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigTrapGroupTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigTrapGroupTable_undo( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:dot11ConfigTrapGroupTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11ConfigTrapGroupTable undo.
     * dot11ConfigTrapGroupTable undo information, in response to a failed set.
     * Undo storage is in (* TrapRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigTrapGroupTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigTrapGroupTable_undo_cleanup( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:dot11ConfigTrapGroupTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigTrapGroupTable undo.
     * Undo storage is in (* TrapRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigTrapGroupTable_undo_cleanup */

static int
check_create_ready(dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx) {
    if(strlen(rowreq_ctx->data.TrapName) && (0 != rowreq_ctx->data.TrapVersion)) {
        return MFD_SUCCESS;
    }

    return MFD_ERROR;
}

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigTrapGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigTrapGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigTrapGroupTable_commit( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:dot11ConfigTrapGroupTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;
	
    if(RS_NOTREADY == rowreq_ctx->data.TrapRowStatus && MFD_SUCCESS == check_create_ready(rowreq_ctx)) {
        rowreq_ctx->data.TrapRowStatus = RS_NOTINSERVICE;
    }
	
    /*
     * commit dot11ConfigTrapGroupTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_TRAPNAME_FLAG) {
       save_flags &= ~COLUMN_TRAPNAME_FLAG; /* clear TrapName */
       /*
        * TODO:482:o: |-> commit column TrapName.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapGroupTable column TrapName commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo TrapName
             */
            rowreq_ctx->column_set_flags |= COLUMN_TRAPNAME_FLAG;
       }
    }

    if (save_flags & COLUMN_TRAPVERSION_FLAG) {
       save_flags &= ~COLUMN_TRAPVERSION_FLAG; /* clear TrapVersion */
       /*
        * TODO:482:o: |-> commit column TrapVersion.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapGroupTable column TrapVersion commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo TrapVersion
             */
            rowreq_ctx->column_set_flags |= COLUMN_TRAPVERSION_FLAG;
       }
    }

    if (save_flags & COLUMN_TRAPSOURIPADDRESS_FLAG) {
       save_flags &= ~COLUMN_TRAPSOURIPADDRESS_FLAG; /* clear TrapSourIPAddress */
       /*
        * TODO:482:o: |-> commit column TrapSourIPAddress.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapGroupTable column TrapSourIPAddress commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo TrapSourIPAddress
             */
            rowreq_ctx->column_set_flags |= COLUMN_TRAPSOURIPADDRESS_FLAG;
       }
    }

    if (save_flags & COLUMN_TRAPDESTIPADDRESS_FLAG) {
       save_flags &= ~COLUMN_TRAPDESTIPADDRESS_FLAG; /* clear TrapDestIPAddress */
       /*
        * TODO:482:o: |-> commit column TrapDestIPAddress.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapGroupTable column TrapDestIPAddress commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo TrapDestIPAddress
             */
            rowreq_ctx->column_set_flags |= COLUMN_TRAPDESTIPADDRESS_FLAG;
       }
    }

    if (save_flags & COLUMN_TRAPCOMMUNITY_FLAG) {
       save_flags &= ~COLUMN_TRAPCOMMUNITY_FLAG; /* clear TrapCommunity */
       /*
        * TODO:482:o: |-> commit column TrapCommunity.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapGroupTable column TrapCommunity commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo TrapCommunity
             */
            rowreq_ctx->column_set_flags |= COLUMN_TRAPCOMMUNITY_FLAG;
       }
    }

    if (save_flags & COLUMN_TRAPSTATE_FLAG) {
       save_flags &= ~COLUMN_TRAPSTATE_FLAG; /* clear TrapState */
       /*
        * TODO:482:o: |-> commit column TrapState.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapGroupTable column TrapState commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo TrapState
             */
            rowreq_ctx->column_set_flags |= COLUMN_TRAPSTATE_FLAG;
       }
    }

    if (save_flags & COLUMN_TRAPDESTPORT_FLAG) {
       save_flags &= ~COLUMN_TRAPDESTPORT_FLAG; /* clear TrapDestPort */
       /*
        * TODO:482:o: |-> commit column TrapDestPort.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapGroupTable column TrapDestPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo TrapDestPort
             */
            rowreq_ctx->column_set_flags |= COLUMN_TRAPDESTPORT_FLAG;
       }
    }

    if (save_flags & COLUMN_TRAPROWSTATUS_FLAG) {
        save_flags &= ~COLUMN_TRAPROWSTATUS_FLAG; /* clear TrapRowStatus */
        switch(rowreq_ctx->data.TrapRowStatus) {
            case RS_ACTIVE:
                {
					int ret = AC_MANAGE_DBUS_ERROR;
                    void *connection = NULL;
                    if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3)) 
					{
       					STSNMPTrapReceiver receiver;
						memset(&receiver, 0, sizeof(STSNMPTrapReceiver));
						unsigned int type = 1;	/*add trap*/
						int ret_c = 0;
						if(1 == trap_name_is_legal_input(rowreq_ctx->data.TrapName))
						{
							strncpy(receiver.name, rowreq_ctx->data.TrapName, sizeof(receiver.name) - 1);
							receiver.local_id = rowreq_ctx->data.parameter.local_id;
							receiver.instance_id = rowreq_ctx->data.parameter.instance_id;
							receiver.version = rowreq_ctx->data.TrapVersion;
							INET_NTOA(rowreq_ctx->data.TrapSourIPAddress, receiver.sour_ipAddr);
							INET_NTOA(rowreq_ctx->data.TrapDestIPAddress, receiver.dest_ipAddr);
							if(rowreq_ctx->data.TrapCommunity)/*·ÇV3°æ±¾*/
							{
								ret_c = check_snmp_name(rowreq_ctx->data.TrapCommunity, 1);
								if(1 == ret_c)
								{
									strncpy(receiver.trapcom, rowreq_ctx->data.TrapCommunity, sizeof(receiver.trapcom) - 1);
								}
							}
							receiver.status = rowreq_ctx->data.TrapState;
							if(0 == rowreq_ctx->data.TrapDestPort)
							{
								rowreq_ctx->data.TrapDestPort = 162;
							}
							receiver.dest_port = rowreq_ctx->data.TrapDestPort;
							ret = ac_manage_config_trap_config_receiver(connection, &receiver, type);
							snmp_log(LOG_DEBUG, "dot11ConfigTrapGroupTable_commit: ac_manage_config_trap_config_receiver: ret = %d\n", ret);
							if(AC_MANAGE_SUCCESS == ret)
							{
								rc = MFD_SUCCESS;
								break;
							}
							else if(AC_MANAGE_DBUS_ERROR == ret) 
							{
								close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
							}	   
						}             
					}
                }
                rc = -1;
                break;

            case RS_CREATEANDWAIT:
                rowreq_ctx->data.TrapRowStatus = RS_NOTREADY;
                rc = MFD_SUCCESS;
                break;
                
            case RS_DESTROY:  
                {
					int ret = AC_MANAGE_DBUS_ERROR;
					void *connection = NULL;
                    if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3)) 
					{
						if(1 == trap_name_is_legal_input(rowreq_ctx->data.TrapName))
						{
            				ret = ac_manage_config_trap_del_receiver(connection, rowreq_ctx->data.TrapName);
							snmp_log(LOG_DEBUG, "dot11ConfigTrapGroupTable_commit: ac_manage_config_trap_del_receiver: ret = %d\n", ret);
							if(AC_MANAGE_SUCCESS == ret)
							{
	                            rc = MFD_SUCCESS;
	                            break;
	                        }
	                        else if(AC_MANAGE_DBUS_ERROR == ret) 
							{
	                            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	                        }  
						}
                    }
                }
                rowreq_ctx->rowreq_flags &= ~MFD_ROW_DELETED;
                rc = -1;
                break;
                
            default:
                break;
        }
       /*
        * TODO:482:o: |-> commit column TrapRowStatus.
        */
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapGroupTable column TrapRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo TrapRowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_TRAPROWSTATUS_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigTrapGroupTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigTrapGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigTrapGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigTrapGroupTable_undo_commit( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:dot11ConfigTrapGroupTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigTrapGroupTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11ConfigTrapGroupTable_undo_commit */

/*
 * TODO:440:M: Implement dot11ConfigTrapGroupTable node value checks.
 * TODO:450:M: Implement dot11ConfigTrapGroupTable undo functions.
 * TODO:460:M: Implement dot11ConfigTrapGroupTable set functions.
 * TODO:480:M: Implement dot11ConfigTrapGroupTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapGroupEntry.TrapName
 * TrapName is subid 2 of dot11ConfigTrapGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.19.1.1.2
 * Description:
Trap Name,the lenth of trap name should be 1 to 30,
					trap name should be ASCII value in the Non-control characters (Spaces except).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  1 - 30;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 30)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param TrapName_val_ptr
 *        A char containing the new value.
 * @param TrapName_val_ptr_len
 *        The size (in bytes) of the data pointed to by TrapName_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.TrapName).
 *    The length is in (one of) the range set(s):  1 - 30
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
TrapName_check_value( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, char *TrapName_val_ptr,  size_t TrapName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapName_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != TrapName_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid TrapName value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.TrapRowStatus || RS_NOTREADY == rowreq_ctx->data.TrapRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* TrapName value not illegal */
} /* TrapName_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
TrapName_undo_setup( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapName_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup TrapName undo.
     */
    /*
     * copy TrapName and TrapName_len data
     * set rowreq_ctx->undo->TrapName from rowreq_ctx->data.TrapName
     */
    memcpy( rowreq_ctx->undo->TrapName, rowreq_ctx->data.TrapName,
            (rowreq_ctx->data.TrapName_len * sizeof(rowreq_ctx->undo->TrapName[0])));
    rowreq_ctx->undo->TrapName_len = rowreq_ctx->data.TrapName_len;


    return MFD_SUCCESS;
} /* TrapName_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param TrapName_val_ptr
 *        A char containing the new value.
 * @param TrapName_val_ptr_len
 *        The size (in bytes) of the data pointed to by TrapName_val_ptr
 */
int
TrapName_set( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, char *TrapName_val_ptr,  size_t TrapName_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapName_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != TrapName_val_ptr);

    /*
     * TODO:461:M: |-> Set TrapName value.
     * set TrapName value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.TrapName, TrapName_val_ptr, TrapName_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.TrapName_len = TrapName_val_ptr_len / sizeof(TrapName_val_ptr[0]);

    return MFD_SUCCESS;
} /* TrapName_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
TrapName_undo( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapName_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up TrapName undo.
     */
    /*
     * copy TrapName and TrapName_len data
     * set rowreq_ctx->data.TrapName from rowreq_ctx->undo->TrapName
     */
    memcpy( rowreq_ctx->data.TrapName, rowreq_ctx->undo->TrapName,
            (rowreq_ctx->undo->TrapName_len * sizeof(rowreq_ctx->data.TrapName[0])));
    rowreq_ctx->data.TrapName_len = rowreq_ctx->undo->TrapName_len;

    
    return MFD_SUCCESS;
} /* TrapName_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapGroupEntry.TrapVersion
 * TrapVersion is subid 3 of dot11ConfigTrapGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.19.1.1.3
 * Description:
Trap Version.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  v1(1), v2c(2), v3(3)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param TrapVersion_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  v1(1), v2c(2), v3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
TrapVersion_check_value( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long TrapVersion_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapVersion_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid TrapVersion value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.TrapRowStatus || RS_NOTREADY == rowreq_ctx->data.TrapRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* TrapVersion value not illegal */
} /* TrapVersion_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
TrapVersion_undo_setup( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapVersion_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup TrapVersion undo.
     */
    /*
     * copy TrapVersion data
     * set rowreq_ctx->undo->TrapVersion from rowreq_ctx->data.TrapVersion
     */
    rowreq_ctx->undo->TrapVersion = rowreq_ctx->data.TrapVersion;


    return MFD_SUCCESS;
} /* TrapVersion_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param TrapVersion_val
 *        A long containing the new value.
 */
int
TrapVersion_set( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long TrapVersion_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapVersion_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set TrapVersion value.
     * set TrapVersion value in rowreq_ctx->data
     */
    rowreq_ctx->data.TrapVersion = TrapVersion_val;

    return MFD_SUCCESS;
} /* TrapVersion_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
TrapVersion_undo( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapVersion_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up TrapVersion undo.
     */
    /*
     * copy TrapVersion data
     * set rowreq_ctx->data.TrapVersion from rowreq_ctx->undo->TrapVersion
     */
    rowreq_ctx->data.TrapVersion = rowreq_ctx->undo->TrapVersion;

    
    return MFD_SUCCESS;
} /* TrapVersion_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapGroupEntry.TrapSourIPAddress
 * TrapSourIPAddress is subid 4 of dot11ConfigTrapGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.19.1.1.4
 * Description:
Trap Source IP Address,optional.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param TrapSourIPAddress_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
TrapSourIPAddress_check_value( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long TrapSourIPAddress_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapSourIPAddress_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid TrapSourIPAddress value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.TrapRowStatus || RS_NOTREADY == rowreq_ctx->data.TrapRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* TrapSourIPAddress value not illegal */
} /* TrapSourIPAddress_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
TrapSourIPAddress_undo_setup( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapSourIPAddress_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup TrapSourIPAddress undo.
     */
    /*
     * copy TrapSourIPAddress data
     * set rowreq_ctx->undo->TrapSourIPAddress from rowreq_ctx->data.TrapSourIPAddress
     */
    rowreq_ctx->undo->TrapSourIPAddress = rowreq_ctx->data.TrapSourIPAddress;


    return MFD_SUCCESS;
} /* TrapSourIPAddress_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param TrapSourIPAddress_val
 *        A u_long containing the new value.
 */
int
TrapSourIPAddress_set( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long TrapSourIPAddress_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapSourIPAddress_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set TrapSourIPAddress value.
     * set TrapSourIPAddress value in rowreq_ctx->data
     */
    rowreq_ctx->data.TrapSourIPAddress = TrapSourIPAddress_val;

    return MFD_SUCCESS;
} /* TrapSourIPAddress_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
TrapSourIPAddress_undo( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapSourIPAddress_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up TrapSourIPAddress undo.
     */
    /*
     * copy TrapSourIPAddress data
     * set rowreq_ctx->data.TrapSourIPAddress from rowreq_ctx->undo->TrapSourIPAddress
     */
    rowreq_ctx->data.TrapSourIPAddress = rowreq_ctx->undo->TrapSourIPAddress;

    
    return MFD_SUCCESS;
} /* TrapSourIPAddress_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapGroupEntry.TrapDestIPAddress
 * TrapDestIPAddress is subid 5 of dot11ConfigTrapGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.19.1.1.5
 * Description:
Trap Destination IP Address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param TrapDestIPAddress_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
TrapDestIPAddress_check_value( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long TrapDestIPAddress_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapDestIPAddress_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid TrapDestIPAddress value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.TrapRowStatus || RS_NOTREADY == rowreq_ctx->data.TrapRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* TrapDestIPAddress value not illegal */
} /* TrapDestIPAddress_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
TrapDestIPAddress_undo_setup( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapDestIPAddress_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup TrapDestIPAddress undo.
     */
    /*
     * copy TrapDestIPAddress data
     * set rowreq_ctx->undo->TrapDestIPAddress from rowreq_ctx->data.TrapDestIPAddress
     */
    rowreq_ctx->undo->TrapDestIPAddress = rowreq_ctx->data.TrapDestIPAddress;


    return MFD_SUCCESS;
} /* TrapDestIPAddress_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param TrapDestIPAddress_val
 *        A u_long containing the new value.
 */
int
TrapDestIPAddress_set( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long TrapDestIPAddress_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapDestIPAddress_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set TrapDestIPAddress value.
     * set TrapDestIPAddress value in rowreq_ctx->data
     */
    rowreq_ctx->data.TrapDestIPAddress = TrapDestIPAddress_val;

    return MFD_SUCCESS;
} /* TrapDestIPAddress_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
TrapDestIPAddress_undo( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapDestIPAddress_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up TrapDestIPAddress undo.
     */
    /*
     * copy TrapDestIPAddress data
     * set rowreq_ctx->data.TrapDestIPAddress from rowreq_ctx->undo->TrapDestIPAddress
     */
    rowreq_ctx->data.TrapDestIPAddress = rowreq_ctx->undo->TrapDestIPAddress;

    
    return MFD_SUCCESS;
} /* TrapDestIPAddress_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapGroupEntry.TrapCommunity
 * TrapCommunity is subid 6 of dot11ConfigTrapGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.19.1.1.6
 * Description:
Trap Name,The lenth of community name should be 1 to 20,
					community name should be ASCII value in the Non-control characters (Spaces except),
					and it's can't begin with #,double quotes,single quotes,
					\ only appeared in end
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  1 - 20;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 20)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param TrapCommunity_val_ptr
 *        A char containing the new value.
 * @param TrapCommunity_val_ptr_len
 *        The size (in bytes) of the data pointed to by TrapCommunity_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.TrapCommunity).
 *    The length is in (one of) the range set(s):  1 - 20
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
TrapCommunity_check_value( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, char *TrapCommunity_val_ptr,  size_t TrapCommunity_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapCommunity_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != TrapCommunity_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid TrapCommunity value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.TrapRowStatus || RS_NOTREADY == rowreq_ctx->data.TrapRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* TrapCommunity value not illegal */
} /* TrapCommunity_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
TrapCommunity_undo_setup( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapCommunity_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup TrapCommunity undo.
     */
    /*
     * copy TrapCommunity and TrapCommunity_len data
     * set rowreq_ctx->undo->TrapCommunity from rowreq_ctx->data.TrapCommunity
     */
    memcpy( rowreq_ctx->undo->TrapCommunity, rowreq_ctx->data.TrapCommunity,
            (rowreq_ctx->data.TrapCommunity_len * sizeof(rowreq_ctx->undo->TrapCommunity[0])));
    rowreq_ctx->undo->TrapCommunity_len = rowreq_ctx->data.TrapCommunity_len;


    return MFD_SUCCESS;
} /* TrapCommunity_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param TrapCommunity_val_ptr
 *        A char containing the new value.
 * @param TrapCommunity_val_ptr_len
 *        The size (in bytes) of the data pointed to by TrapCommunity_val_ptr
 */
int
TrapCommunity_set( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, char *TrapCommunity_val_ptr,  size_t TrapCommunity_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapCommunity_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != TrapCommunity_val_ptr);

    /*
     * TODO:461:M: |-> Set TrapCommunity value.
     * set TrapCommunity value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.TrapCommunity, TrapCommunity_val_ptr, TrapCommunity_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.TrapCommunity_len = TrapCommunity_val_ptr_len / sizeof(TrapCommunity_val_ptr[0]);

    return MFD_SUCCESS;
} /* TrapCommunity_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
TrapCommunity_undo( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapCommunity_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up TrapCommunity undo.
     */
    /*
     * copy TrapCommunity and TrapCommunity_len data
     * set rowreq_ctx->data.TrapCommunity from rowreq_ctx->undo->TrapCommunity
     */
    memcpy( rowreq_ctx->data.TrapCommunity, rowreq_ctx->undo->TrapCommunity,
            (rowreq_ctx->undo->TrapCommunity_len * sizeof(rowreq_ctx->data.TrapCommunity[0])));
    rowreq_ctx->data.TrapCommunity_len = rowreq_ctx->undo->TrapCommunity_len;

    
    return MFD_SUCCESS;
} /* TrapCommunity_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapGroupEntry.TrapState
 * TrapState is subid 7 of dot11ConfigTrapGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.19.1.1.7
 * Description:
Trap State.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  disable(0), enable(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param TrapState_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  disable(0), enable(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
TrapState_check_value( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long TrapState_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapState_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid TrapState value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.TrapRowStatus || RS_NOTREADY == rowreq_ctx->data.TrapRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* TrapState value not illegal */
} /* TrapState_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
TrapState_undo_setup( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapState_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup TrapState undo.
     */
    /*
     * copy TrapState data
     * set rowreq_ctx->undo->TrapState from rowreq_ctx->data.TrapState
     */
    rowreq_ctx->undo->TrapState = rowreq_ctx->data.TrapState;


    return MFD_SUCCESS;
} /* TrapState_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param TrapState_val
 *        A long containing the new value.
 */
int
TrapState_set( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long TrapState_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapState_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set TrapState value.
     * set TrapState value in rowreq_ctx->data
     */
    rowreq_ctx->data.TrapState = TrapState_val;

    return MFD_SUCCESS;
} /* TrapState_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
TrapState_undo( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapState_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up TrapState undo.
     */
    /*
     * copy TrapState data
     * set rowreq_ctx->data.TrapState from rowreq_ctx->undo->TrapState
     */
    rowreq_ctx->data.TrapState = rowreq_ctx->undo->TrapState;

    
    return MFD_SUCCESS;
} /* TrapState_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapGroupEntry.TrapDestPort
 * TrapDestPort is subid 8 of dot11ConfigTrapGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.19.1.1.8
 * Description:
Trap Destination Port,optional,default is 162.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param TrapDestPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
TrapDestPort_check_value( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, long TrapDestPort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapDestPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid TrapDestPort value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.TrapRowStatus || RS_NOTREADY == rowreq_ctx->data.TrapRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* TrapDestPort value not illegal */
} /* TrapDestPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
TrapDestPort_undo_setup( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapDestPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup TrapDestPort undo.
     */
    /*
     * copy TrapDestPort data
     * set rowreq_ctx->undo->TrapDestPort from rowreq_ctx->data.TrapDestPort
     */
    rowreq_ctx->undo->TrapDestPort = rowreq_ctx->data.TrapDestPort;


    return MFD_SUCCESS;
} /* TrapDestPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param TrapDestPort_val
 *        A long containing the new value.
 */
int
TrapDestPort_set( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, long TrapDestPort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapDestPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set TrapDestPort value.
     * set TrapDestPort value in rowreq_ctx->data
     */
    rowreq_ctx->data.TrapDestPort = TrapDestPort_val;

    return MFD_SUCCESS;
} /* TrapDestPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
TrapDestPort_undo( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapDestPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up TrapDestPort undo.
     */
    /*
     * copy TrapDestPort data
     * set rowreq_ctx->data.TrapDestPort from rowreq_ctx->undo->TrapDestPort
     */
    rowreq_ctx->data.TrapDestPort = rowreq_ctx->undo->TrapDestPort;

    
    return MFD_SUCCESS;
} /* TrapDestPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapGroupEntry.TrapRowStatus
 * TrapRowStatus is subid 9 of dot11ConfigTrapGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.19.1.1.9
 * Description:
This variable is used to create, modify, and/or delete a row
			        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param TrapRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
TrapRowStatus_check_value( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long TrapRowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid TrapRowStatus value.
     */

    return MFD_SUCCESS; /* TrapRowStatus value not illegal */
} /* TrapRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
TrapRowStatus_undo_setup( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup TrapRowStatus undo.
     */
    /*
     * copy TrapRowStatus data
     * set rowreq_ctx->undo->TrapRowStatus from rowreq_ctx->data.TrapRowStatus
     */
    rowreq_ctx->undo->TrapRowStatus = rowreq_ctx->data.TrapRowStatus;


    return MFD_SUCCESS;
} /* TrapRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param TrapRowStatus_val
 *        A long containing the new value.
 */
int
TrapRowStatus_set( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long TrapRowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set TrapRowStatus value.
     * set TrapRowStatus value in rowreq_ctx->data
     */
    
    int ret = MFD_SUCCESS;
    
    switch(TrapRowStatus_val) {
    
        case RS_ACTIVE:         
        case RS_DESTROY:
            rowreq_ctx->data.TrapRowStatus = TrapRowStatus_val;
            ret = MFD_SUCCESS;
            break;
    
        case RS_NOTINSERVICE:
        case RS_NOTREADY:
        case RS_CREATEANDGO:
            ret = MFD_ERROR;
            break;
    
        case RS_CREATEANDWAIT:
            if(RS_NONEXISTENT == rowreq_ctx->data.TrapRowStatus) {
                global_to_local_ID(&(rowreq_ctx->data.parameter), 
                                    rowreq_ctx->tbl_idx.TrapID, 
                                    WIRELESS_MAX_NUM);
                rowreq_ctx->data.TrapRowStatus = TrapRowStatus_val;
                ret = MFD_SUCCESS;
            }
            else {
                ret = MFD_ERROR;
            }
            break;
    
        default:
            snmp_log(LOG_WARNING, "unknow %s in TrapRowStatus_set", TrapRowStatus_val);
            ret = MFD_ERROR;
            break;
    }

    return ret;
    
} /* TrapRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
TrapRowStatus_undo( dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapGroupTable:TrapRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up TrapRowStatus undo.
     */
    /*
     * copy TrapRowStatus data
     * set rowreq_ctx->data.TrapRowStatus from rowreq_ctx->undo->TrapRowStatus
     */
    rowreq_ctx->data.TrapRowStatus = rowreq_ctx->undo->TrapRowStatus;

    
    return MFD_SUCCESS;
} /* TrapRowStatus_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigTrapGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigTrapGroupTable if you don't have dependencies)
 */
int
dot11ConfigTrapGroupTable_check_dependencies(dot11ConfigTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigTrapGroupTable:dot11ConfigTrapGroupTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigTrapGroupTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & COLUMN_TRAPROWSTATUS_FLAG) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->TrapRowStatus,
                                         rowreq_ctx->data.TrapRowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_DESTROY ==  rowreq_ctx->data.TrapRowStatus) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;
            }
            else if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.TrapRowStatus) {
                if ((rowreq_ctx->column_set_flags & DOT11CONFIGTRAPGROUPTABLE_REQUIRED_COLS)
                    != DOT11CONFIGTRAPGROUPTABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11ConfigTrapGroupTable",
                                "required columns missing (0x%0x != 0x%0x)\n",
                                rowreq_ctx->column_set_flags, DOT11CONFIGTRAPGROUPTABLE_REQUIRED_COLS));
                    return MFD_CANNOT_CREATE_NOW;
                }
                rowreq_ctx->data.TrapRowStatus = ROWSTATUS_ACTIVE;
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.TrapRowStatus) {
                if (rowreq_ctx->column_set_flags & ~COLUMN_TRAPROWSTATUS_FLAG) {
                    DEBUGMSGTL(("dot11ConfigTrapGroupTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;

            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11ConfigTrapGroupTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11ConfigTrapGroupTable_check_dependencies */

/** @} */
