/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfTotalTrapGroupTable.h"

#include "ac_manage_def.h"
#include "ws_snmpd_engine.h"
#include "ws_snmpd_manual.h"
#include "ac_manage_interface.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfTotalTrapGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfTotalTrapGroupTable is subid 1 of dot11ConfTotalTrapGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.24.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfTotalTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfTotalTrapGroupTable_undo_setup( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:dot11ConfTotalTrapGroupTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfTotalTrapGroupTable undo.
     * set up dot11ConfTotalTrapGroupTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11ConfTotalTrapGroupTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfTotalTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfTotalTrapGroupTable_undo_cleanup( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:dot11ConfTotalTrapGroupTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfTotalTrapGroupTable undo.
     */

    return rc;
} /* dot11ConfTotalTrapGroupTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfTotalTrapGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfTotalTrapGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfTotalTrapGroupTable_commit( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:dot11ConfTotalTrapGroupTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11ConfTotalTrapGroupTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_NEWTRAPDESCR) {
       save_flags &= ~FLAG_NEWTRAPDESCR; /* clear NewTrapDescr */
       /*
        * TODO:482:o: |-> commit column NewTrapDescr.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfTotalTrapGroupTable column NewTrapDescr commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewTrapDescr
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWTRAPDESCR;
       }
    }

    if (save_flags & FLAG_NEWTRAPONOFF) {
       save_flags &= ~FLAG_NEWTRAPONOFF; /* clear NewTrapOnOff */
       /*
        * TODO:482:o: |-> commit column NewTrapOnOff.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfTotalTrapGroupTable column NewTrapOnOff commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewTrapOnOff
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWTRAPONOFF;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfTotalTrapGroupTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfTotalTrapGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfTotalTrapGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfTotalTrapGroupTable_undo_commit( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:dot11ConfTotalTrapGroupTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfTotalTrapGroupTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11ConfTotalTrapGroupTable_undo_commit */

/*
 * TODO:420:r: Implement dot11ConfTotalTrapGroupTable index validation.
 */
/*
 * TODO:440:M: Implement dot11ConfTotalTrapGroupTable node value checks.
 * TODO:450:M: Implement dot11ConfTotalTrapGroupTable undo functions.
 * TODO:460:M: Implement dot11ConfTotalTrapGroupTable set functions.
 * TODO:480:M: Implement dot11ConfTotalTrapGroupTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfTotalTrapGroupEntry.NewTrapDescr
 * NewTrapDescr is subid 3 of dot11ConfTotalTrapGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.24.1.1.3
 * Description:
NewTrapDescr.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewTrapDescr_val_ptr
 *        A char containing the new value.
 * @param NewTrapDescr_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewTrapDescr_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfTotalTrapGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewTrapDescr_check_value( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx, char *NewTrapDescr_val_ptr,  size_t NewTrapDescr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapDescr_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewTrapDescr_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid NewTrapDescr value.
     */

    return MFD_SUCCESS; /* NewTrapDescr value not illegal */
} /* NewTrapDescr_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfTotalTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfTotalTrapGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewTrapDescr_undo_setup( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapDescr_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewTrapDescr undo.
     */
    /*
     * copy NewTrapDescr and NewTrapDescr_len data
     * set rowreq_ctx->undo->NewTrapDescr from rowreq_ctx->data.NewTrapDescr
     */
    memcpy( rowreq_ctx->undo->NewTrapDescr, rowreq_ctx->data.NewTrapDescr,
            (rowreq_ctx->data.NewTrapDescr_len * sizeof(rowreq_ctx->undo->NewTrapDescr[0])));
    rowreq_ctx->undo->NewTrapDescr_len = rowreq_ctx->data.NewTrapDescr_len;


    return MFD_SUCCESS;
} /* NewTrapDescr_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewTrapDescr_val_ptr
 *        A char containing the new value.
 * @param NewTrapDescr_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewTrapDescr_val_ptr
 */
int
NewTrapDescr_set( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx, char *NewTrapDescr_val_ptr,  size_t NewTrapDescr_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapDescr_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewTrapDescr_val_ptr);

/*
 * TODO:245:o: |-> Implement NewTrapDescr reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewTrapDescr value.
     * set NewTrapDescr value in rowreq_ctx->data
     */    
    int rc = MFD_ERROR;
    
	TRAP_DETAIL_CONFIG trapConf = { 0 };
    strncpy(trapConf.trapEDes, NewTrapDescr_val_ptr, sizeof(trapConf.trapEDes) - 1);
    if(2 == rowreq_ctx->data.NewTrapOnOff) {
        trapConf.trapSwitch = 0;
    }
    else {
        trapConf.trapSwitch = 1;
    }

    int ret = ac_manage_config_trap_switch(ccgi_dbus_connection, rowreq_ctx->tbl_idx.NewTrapIndex - 1, &trapConf);
    if(AC_MANAGE_SUCCESS == ret) {
        memcpy( rowreq_ctx->data.NewTrapDescr, NewTrapDescr_val_ptr, NewTrapDescr_val_ptr_len );
        rowreq_ctx->data.NewTrapDescr_len = NewTrapDescr_val_ptr_len / sizeof(NewTrapDescr_val_ptr[0]);
        rc = MFD_SUCCESS;
    }
    else {
        rc = MFD_ERROR;
    }

    return rc;
} /* NewTrapDescr_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewTrapDescr_undo( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapDescr_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewTrapDescr undo.
     */
    /*
     * copy NewTrapDescr and NewTrapDescr_len data
     * set rowreq_ctx->data.NewTrapDescr from rowreq_ctx->undo->NewTrapDescr
     */
    memcpy( rowreq_ctx->data.NewTrapDescr, rowreq_ctx->undo->NewTrapDescr,
            (rowreq_ctx->undo->NewTrapDescr_len * sizeof(rowreq_ctx->data.NewTrapDescr[0])));
    rowreq_ctx->data.NewTrapDescr_len = rowreq_ctx->undo->NewTrapDescr_len;

    
    return MFD_SUCCESS;
} /* NewTrapDescr_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfTotalTrapGroupEntry.NewTrapOnOff
 * NewTrapOnOff is subid 4 of dot11ConfTotalTrapGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.24.1.1.4
 * Description:
New Trap Switch.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  on(1), off(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewTrapOnOff_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfTotalTrapGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  on(1), off(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewTrapOnOff_check_value( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long NewTrapOnOff_val)
{
    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapOnOff_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewTrapOnOff value.
     */

    return MFD_SUCCESS; /* NewTrapOnOff value not illegal */
} /* NewTrapOnOff_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfTotalTrapGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfTotalTrapGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewTrapOnOff_undo_setup( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapOnOff_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewTrapOnOff undo.
     */
    /*
     * copy NewTrapOnOff data
     * set rowreq_ctx->undo->NewTrapOnOff from rowreq_ctx->data.NewTrapOnOff
     */
    rowreq_ctx->undo->NewTrapOnOff = rowreq_ctx->data.NewTrapOnOff;


    return MFD_SUCCESS;
} /* NewTrapOnOff_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewTrapOnOff_val
 *        A long containing the new value.
 */
int
NewTrapOnOff_set( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long NewTrapOnOff_val )
{

    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapOnOff_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewTrapOnOff reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(NewTrapOnOff_val) {
        case NEWTRAPONOFF_ON:
             NewTrapOnOff_val = INTERNAL_NEWTRAPONOFF_ON;
             break;

        case NEWTRAPONOFF_OFF:
             NewTrapOnOff_val = INTERNAL_NEWTRAPONOFF_OFF;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for NewTrapOnOff\n", NewTrapOnOff_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set NewTrapOnOff value.
     * set NewTrapOnOff value in rowreq_ctx->data
     */    
    int rc = MFD_ERROR;
	TRAP_DETAIL_CONFIG trapConf = { 0 };
    if(2 == NewTrapOnOff_val) {
        trapConf.trapSwitch = 0;
    }
    else {
        trapConf.trapSwitch = 1;
    }

    int ret = ac_manage_config_trap_switch(ccgi_dbus_connection, rowreq_ctx->tbl_idx.NewTrapIndex - 1, &trapConf);
    if(AC_MANAGE_SUCCESS == ret) {
        rowreq_ctx->data.NewTrapOnOff = NewTrapOnOff_val;
        rc = MFD_SUCCESS;
    }
    else {
        rc = MFD_ERROR;
    }
    
    return rc;
} /* NewTrapOnOff_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewTrapOnOff_undo( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapOnOff_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewTrapOnOff undo.
     */
    /*
     * copy NewTrapOnOff data
     * set rowreq_ctx->data.NewTrapOnOff from rowreq_ctx->undo->NewTrapOnOff
     */
    rowreq_ctx->data.NewTrapOnOff = rowreq_ctx->undo->NewTrapOnOff;

    
    return MFD_SUCCESS;
} /* NewTrapOnOff_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfTotalTrapGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfTotalTrapGroupTable if you don't have dependencies)
 */
int
dot11ConfTotalTrapGroupTable_check_dependencies(dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfTotalTrapGroupTable:dot11ConfTotalTrapGroupTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfTotalTrapGroupTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11ConfTotalTrapGroupTable_check_dependencies */

/** @} */
