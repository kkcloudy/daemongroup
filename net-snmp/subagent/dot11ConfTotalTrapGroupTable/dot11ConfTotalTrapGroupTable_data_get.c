/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfTotalTrapGroupTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11ConfTotalTrapGroupTable get routines.
 * TODO:240:M: Implement dot11ConfTotalTrapGroupTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfTotalTrapGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfTotalTrapGroupTable is subid 1 of dot11ConfTotalTrapGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.24.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11ConfTotalTrapGroupTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfTotalTrapGroupEntry.NewTrapIndex
 * NewTrapIndex is subid 1 of dot11ConfTotalTrapGroupEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.24.1.1.1
 * Description:
NewTrapIndex.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewTrapIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewTrapIndex_map(long *mib_NewTrapIndex_val_ptr, long raw_NewTrapIndex_val)
{
    netsnmp_assert(NULL != mib_NewTrapIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewTrapIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewTrapIndex_val_ptr) = raw_NewTrapIndex_val;

    return MFD_SUCCESS;
} /* NewTrapIndex_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11ConfTotalTrapGroupTable_indexes_set_tbl_idx(dot11ConfTotalTrapGroupTable_mib_index *tbl_idx, long NewTrapIndex_val)
{
    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:dot11ConfTotalTrapGroupTable_indexes_set_tbl_idx","called\n"));

    /* NewTrapIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->NewTrapIndex = NewTrapIndex_val;
    

    return MFD_SUCCESS;
} /* dot11ConfTotalTrapGroupTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11ConfTotalTrapGroupTable_indexes_set(dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx, long NewTrapIndex_val)
{
    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:dot11ConfTotalTrapGroupTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11ConfTotalTrapGroupTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , NewTrapIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11ConfTotalTrapGroupTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11ConfTotalTrapGroupTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfTotalTrapGroupEntry.NewTrapName
 * NewTrapName is subid 2 of dot11ConfTotalTrapGroupEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.24.1.1.2
 * Description:
NewTrapName.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewTrapName.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewTrapName_map(char **mib_NewTrapName_val_ptr_ptr, size_t *mib_NewTrapName_val_ptr_len_ptr, char *raw_NewTrapName_val_ptr, size_t raw_NewTrapName_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_NewTrapName_val_ptr);
    netsnmp_assert((NULL != mib_NewTrapName_val_ptr_ptr) && (NULL != mib_NewTrapName_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapName_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement NewTrapName non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_NewTrapName_val_ptr_len; /* assume equal */
    if((NULL == *mib_NewTrapName_val_ptr_ptr) || (*mib_NewTrapName_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_NewTrapName_val_ptr_ptr = realloc( *mib_NewTrapName_val_ptr_ptr, converted_len * sizeof(**mib_NewTrapName_val_ptr_ptr));
        if(NULL == *mib_NewTrapName_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_NewTrapName_val_ptr_len_ptr = converted_len;
    memcpy( *mib_NewTrapName_val_ptr_ptr, raw_NewTrapName_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* NewTrapName_map */

/**
 * Extract the current value of the NewTrapName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewTrapName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewTrapName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewTrapName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewTrapName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewTrapName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewTrapName_get( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx, char **NewTrapName_val_ptr_ptr, size_t *NewTrapName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewTrapName_val_ptr_ptr) && (NULL != *NewTrapName_val_ptr_ptr));
   netsnmp_assert( NULL != NewTrapName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewTrapName data.
 * set (* NewTrapName_val_ptr_ptr ) and (* NewTrapName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewTrapName data
     */
    if ((NULL == (* NewTrapName_val_ptr_ptr )) ||
        ((* NewTrapName_val_ptr_len_ptr ) < (rowreq_ctx->data.NewTrapName_len * sizeof((* NewTrapName_val_ptr_ptr )[0])))) {
        /*
         * allocate space for NewTrapName data
         */
        (* NewTrapName_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewTrapName_len * sizeof((* NewTrapName_val_ptr_ptr )[0]));
        if(NULL == (* NewTrapName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewTrapName_val_ptr_len_ptr ) = rowreq_ctx->data.NewTrapName_len * sizeof((* NewTrapName_val_ptr_ptr )[0]);
    memcpy( (* NewTrapName_val_ptr_ptr ), rowreq_ctx->data.NewTrapName, (* NewTrapName_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* NewTrapName_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfTotalTrapGroupEntry.NewTrapDescr
 * NewTrapDescr is subid 3 of dot11ConfTotalTrapGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.24.1.1.3
 * Description:
NewTrapDescr.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewTrapDescr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewTrapDescr_map(char **mib_NewTrapDescr_val_ptr_ptr, size_t *mib_NewTrapDescr_val_ptr_len_ptr, char *raw_NewTrapDescr_val_ptr, size_t raw_NewTrapDescr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_NewTrapDescr_val_ptr);
    netsnmp_assert((NULL != mib_NewTrapDescr_val_ptr_ptr) && (NULL != mib_NewTrapDescr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapDescr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement NewTrapDescr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_NewTrapDescr_val_ptr_len; /* assume equal */
    if((NULL == *mib_NewTrapDescr_val_ptr_ptr) || (*mib_NewTrapDescr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_NewTrapDescr_val_ptr_ptr = realloc( *mib_NewTrapDescr_val_ptr_ptr, converted_len * sizeof(**mib_NewTrapDescr_val_ptr_ptr));
        if(NULL == *mib_NewTrapDescr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_NewTrapDescr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_NewTrapDescr_val_ptr_ptr, raw_NewTrapDescr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* NewTrapDescr_map */

/**
 * Extract the current value of the NewTrapDescr data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewTrapDescr_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewTrapDescr_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewTrapDescr.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewTrapDescr_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewTrapDescr_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewTrapDescr_get( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx, char **NewTrapDescr_val_ptr_ptr, size_t *NewTrapDescr_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewTrapDescr_val_ptr_ptr) && (NULL != *NewTrapDescr_val_ptr_ptr));
   netsnmp_assert( NULL != NewTrapDescr_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapDescr_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewTrapDescr data.
 * set (* NewTrapDescr_val_ptr_ptr ) and (* NewTrapDescr_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewTrapDescr data
     */
    if ((NULL == (* NewTrapDescr_val_ptr_ptr )) ||
        ((* NewTrapDescr_val_ptr_len_ptr ) < (rowreq_ctx->data.NewTrapDescr_len * sizeof((* NewTrapDescr_val_ptr_ptr )[0])))) {
        /*
         * allocate space for NewTrapDescr data
         */
        (* NewTrapDescr_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewTrapDescr_len * sizeof((* NewTrapDescr_val_ptr_ptr )[0]));
        if(NULL == (* NewTrapDescr_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewTrapDescr_val_ptr_len_ptr ) = rowreq_ctx->data.NewTrapDescr_len * sizeof((* NewTrapDescr_val_ptr_ptr )[0]);
    memcpy( (* NewTrapDescr_val_ptr_ptr ), rowreq_ctx->data.NewTrapDescr, (* NewTrapDescr_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* NewTrapDescr_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfTotalTrapGroupEntry.NewTrapOnOff
 * NewTrapOnOff is subid 4 of dot11ConfTotalTrapGroupEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.24.1.1.4
 * Description:
New Trap Switch.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  on(1), off(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewTrapOnOff.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewTrapOnOff_map(u_long *mib_NewTrapOnOff_val_ptr, u_long raw_NewTrapOnOff_val)
{
    netsnmp_assert(NULL != mib_NewTrapOnOff_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapOnOff_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewTrapOnOff enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_NewTrapOnOff_val) {
        case INTERNAL_NEWTRAPONOFF_ON:
             *mib_NewTrapOnOff_val_ptr = NEWTRAPONOFF_ON;
             break;

        case INTERNAL_NEWTRAPONOFF_OFF:
             *mib_NewTrapOnOff_val_ptr = NEWTRAPONOFF_OFF;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for NewTrapOnOff\n", raw_NewTrapOnOff_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* NewTrapOnOff_map */

/**
 * Extract the current value of the NewTrapOnOff data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewTrapOnOff_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewTrapOnOff_get( dot11ConfTotalTrapGroupTable_rowreq_ctx *rowreq_ctx, u_long * NewTrapOnOff_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewTrapOnOff_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfTotalTrapGroupTable:NewTrapOnOff_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewTrapOnOff data.
 * set (* NewTrapOnOff_val_ptr ) from rowreq_ctx->data
 */
    (* NewTrapOnOff_val_ptr ) = rowreq_ctx->data.NewTrapOnOff;

    return MFD_SUCCESS;
} /* NewTrapOnOff_get */



/** @} */
