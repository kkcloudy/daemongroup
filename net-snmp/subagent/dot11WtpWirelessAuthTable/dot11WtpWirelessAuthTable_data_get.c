/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpWirelessAuthTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpWirelessAuthTable get routines.
 * TODO:240:M: Implement dot11WtpWirelessAuthTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpWirelessAuthTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-WTP-MIB::dot11WtpWirelessAuthTable is subid 5 of wlanPortal.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpWirelessAuthTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param wtpMacAddr_ptr
 * @param wtpMacAddr_ptr_len
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpWirelessAuthTable_indexes_set_tbl_idx(dot11WtpWirelessAuthTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:dot11WtpWirelessAuthTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
    tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr)/sizeof(tbl_idx->wtpMacAddr[0]); /* max length */
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len <
         (wtpMacAddr_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len;
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len* sizeof(wtpMacAddr_val_ptr[0]) );
    

    return MFD_SUCCESS;
} /* dot11WtpWirelessAuthTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpWirelessAuthTable_indexes_set(dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:dot11WtpWirelessAuthTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpWirelessAuthTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpWirelessAuthTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpWirelessAuthTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.authFreeCurusernum
 * authFreeCurusernum is subid 1 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.1
 * Description:
The ap auth free current user total number).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the authFreeCurusernum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param authFreeCurusernum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
authFreeCurusernum_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * authFreeCurusernum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != authFreeCurusernum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:authFreeCurusernum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the authFreeCurusernum data.
 * copy (* authFreeCurusernum_val_ptr ) from rowreq_ctx->data
 */
    (* authFreeCurusernum_val_ptr ) = rowreq_ctx->data.authFreeCurusernum;

    return MFD_SUCCESS;
} /* authFreeCurusernum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.authFreeErrLogoffnum
 * authFreeErrLogoffnum is subid 2 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.2
 * Description:
The ap auth free fail number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the authFreeErrLogoffnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param authFreeErrLogoffnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
authFreeErrLogoffnum_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * authFreeErrLogoffnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != authFreeErrLogoffnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:authFreeErrLogoffnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the authFreeErrLogoffnum data.
 * copy (* authFreeErrLogoffnum_val_ptr ) from rowreq_ctx->data
 */
    (* authFreeErrLogoffnum_val_ptr ) = rowreq_ctx->data.authFreeErrLogoffnum;

    return MFD_SUCCESS;
} /* authFreeErrLogoffnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.authFreeAllUserOnlineTime
 * authFreeAllUserOnlineTime is subid 3 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.3
 * Description:
The auth free all users online time.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the authFreeAllUserOnlineTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param authFreeAllUserOnlineTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
authFreeAllUserOnlineTime_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * authFreeAllUserOnlineTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != authFreeAllUserOnlineTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:authFreeAllUserOnlineTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the authFreeAllUserOnlineTime data.
 * copy (* authFreeAllUserOnlineTime_val_ptr ) from rowreq_ctx->data
 */
    (* authFreeAllUserOnlineTime_val_ptr ) = rowreq_ctx->data.authFreeAllUserOnlineTime;

    return MFD_SUCCESS;
} /* authFreeAllUserOnlineTime_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.assocAuthCurusernum
 * assocAuthCurusernum is subid 4 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.4
 * Description:
The ap assoc auth current user total number).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the assocAuthCurusernum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param assocAuthAuthCurusernum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
assocAuthCurusernum_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * assocAuthCurusernum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != assocAuthCurusernum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:assocAuthCurusernum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the assocAuthCurusernum data.
 * copy (* assocAuthCurusernum_val_ptr ) from rowreq_ctx->data
 */
    (* assocAuthCurusernum_val_ptr ) = rowreq_ctx->data.assocAuthCurusernum;

    return MFD_SUCCESS;
} /* assocAuthCurusernum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.assocAuthErrLogoffnum
 * assocAuthErrLogoffnum is subid 5 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.5
 * Description:
The ap assoc auth fail number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the assocAuthErrLogoffnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param assocAuthErrLogoffnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
assocAuthErrLogoffnum_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * assocAuthErrLogoffnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != assocAuthErrLogoffnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:assocAuthErrLogoffnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the assocAuthErrLogoffnum data.
 * copy (* assocAuthErrLogoffnum_val_ptr ) from rowreq_ctx->data
 */
    (* assocAuthErrLogoffnum_val_ptr ) = rowreq_ctx->data.assocAuthErrLogoffnum;

    return MFD_SUCCESS;
} /* assocAuthErrLogoffnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.assocAuthReqnum
 * assocAuthReqnum is subid 6 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.6
 * Description:
The ap assoc auth request number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the assocAuthReqnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param assocAuthReqnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
assocAuthReqnum_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * assocAuthReqnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != assocAuthReqnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:assocAuthReqnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the assocAuthReqnum data.
 * copy (* assocAuthReqnum_val_ptr ) from rowreq_ctx->data
 */
    (* assocAuthReqnum_val_ptr ) = rowreq_ctx->data.assocAuthReqnum;

    return MFD_SUCCESS;
} /* assocAuthReqnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.assocAuthReqsuccnum
 * assocAuthReqsuccnum is subid 7 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.7
 * Description:
The ap assoc auth request success number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the assocAuthReqsuccnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param assocAuthReqsuccnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
assocAuthReqsuccnum_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * assocAuthReqsuccnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != assocAuthReqsuccnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:assocAuthReqsuccnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the assocAuthReqsuccnum data.
 * copy (* assocAuthReqsuccnum_val_ptr ) from rowreq_ctx->data
 */
    (* assocAuthReqsuccnum_val_ptr ) = rowreq_ctx->data.assocAuthReqsuccnum;

    return MFD_SUCCESS;
} /* assocAuthReqsuccnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.assocAuthReqfailnum
 * assocAuthReqfailnum is subid 8 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.8
 * Description:
The ap assoc auth request fail number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the assocAuthReqfailnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param assocAuthReqfailnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
assocAuthReqfailnum_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * assocAuthReqfailnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != assocAuthReqfailnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:assocAuthReqfailnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the assocAuthReqfailnum data.
 * copy (* assocAuthReqfailnum_val_ptr ) from rowreq_ctx->data
 */
    (* assocAuthReqfailnum_val_ptr ) = rowreq_ctx->data.assocAuthReqfailnum;

    return MFD_SUCCESS;
} /* assocAuthReqfailnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.assocAuthAllUserOnlineTime
 * assocAuthAllUserOnlineTime is subid 9 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.9
 * Description:
The assoc auth all users online time.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the assocAuthAllUserOnlineTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param assocAuthAllUserOnlineTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
assocAuthAllUserOnlineTime_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * assocAuthAllUserOnlineTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != assocAuthAllUserOnlineTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:assocAuthAllUserOnlineTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the assocAuthAllUserOnlineTime data.
 * copy (* assocAuthAllUserOnlineTime_val_ptr ) from rowreq_ctx->data
 */
    (* assocAuthAllUserOnlineTime_val_ptr ) = rowreq_ctx->data.assocAuthAllUserOnlineTime;

    return MFD_SUCCESS;
} /* assocAuthAllUserOnlineTime_get */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.assocAuthOnlineUsernum
 * assocAuthOnlineUsernum is subid 10 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.10
 * Description:
The WEP/PSK current user total number of AP
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the assocAuthOnlineUsernum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param assocAuthOnlineUsernum_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
assocAuthOnlineUsernum_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, long * assocAuthOnlineUsernum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != assocAuthOnlineUsernum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:assocAuthOnlineUsernum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the assocAuthOnlineUsernum data.
 * copy (* assocAuthOnlineUsernum_val_ptr ) from rowreq_ctx->data
 */
    (* assocAuthOnlineUsernum_val_ptr ) = rowreq_ctx->data.assocAuthOnlineUsernum;

    return MFD_SUCCESS;
} /* assocAuthOnlineUsernum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.allAssociateAuthUserOnlineTime
 * allAssociateAuthUserOnlineTime is subid 11 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.11
 * Description:
The WEP/PSK all users online time of AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the allAssociateAuthUserOnlineTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param allAssociateAuthUserOnlineTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
allAssociateAuthUserOnlineTime_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * allAssociateAuthUserOnlineTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != allAssociateAuthUserOnlineTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:allAssociateAuthUserOnlineTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the allAssociateAuthUserOnlineTime data.
 * copy (* allAssociateAuthUserOnlineTime_val_ptr ) from rowreq_ctx->data
 */
    (* allAssociateAuthUserOnlineTime_val_ptr ) = rowreq_ctx->data.allAssociateAuthUserOnlineTime;

    return MFD_SUCCESS;
} /* allAssociateAuthUserOnlineTime_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcStats.assocAuthUserLostConnectionCnt
 * assocAuthUserLostConnectionCnt is subid 63 of dot11AcStats.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.3.1.63
 * Description:
 The WEP/PSK fail number of AC.
 *
 * Attributes:
 *   accessible 1     isscalar 1     enums  0      hasdefval 0
 *   readable   1     iscolumn 0     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the assocAuthUserLostConnectionCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param assocAuthUserLostConnectionCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
assocAuthUserLostConnectionCnt_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * assocAuthUserLostConnectionCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != assocAuthUserLostConnectionCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:assocAuthUserLostConnectionCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the assocAuthUserLostConnectionCnt data.
 * copy (* assocAuthUserLostConnectionCnt_val_ptr ) from rowreq_ctx->data
 */
    (* assocAuthUserLostConnectionCnt_val_ptr ) = rowreq_ctx->data.assocAuthUserLostConnectionCnt;

    return MFD_SUCCESS;
} /* assocAuthUserLostConnectionCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.autoAuthOnlineUsernum
 * autoAuthOnlineUsernum is subid 13 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.13
 * Description:
The SIM/PEAP current user total number of AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the autoAuthOnlineUsernum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param autoAuthOnlineUsernum_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
autoAuthOnlineUsernum_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, long * autoAuthOnlineUsernum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != autoAuthOnlineUsernum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:autoAuthOnlineUsernum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the autoAuthOnlineUsernum data.
 * copy (* autoAuthOnlineUsernum_val_ptr ) from rowreq_ctx->data
 */
    (* autoAuthOnlineUsernum_val_ptr ) = rowreq_ctx->data.autoAuthOnlineUsernum;

    return MFD_SUCCESS;
} /* autoAuthOnlineUsernum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.allAutoAuthUserOnlineTime
 * allAutoAuthUserOnlineTime is subid 14 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.14
 * Description:
The SIM/PEAP all users online time of AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the allAutoAuthUserOnlineTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param allAutoAuthUserOnlineTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
allAutoAuthUserOnlineTime_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * allAutoAuthUserOnlineTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != allAutoAuthUserOnlineTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:allAutoAuthUserOnlineTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the allAutoAuthUserOnlineTime data.
 * copy (* allAutoAuthUserOnlineTime_val_ptr ) from rowreq_ctx->data
 */
    (* allAutoAuthUserOnlineTime_val_ptr ) = rowreq_ctx->data.allAutoAuthUserOnlineTime;

    return MFD_SUCCESS;
} /* allAutoAuthUserOnlineTime_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcStats.autoAuthUserLostConnectionCnt
 * autoAuthUserLostConnectionCnt is subid 68 of dot11AcStats.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.3.1.68
 * Description:
 The SIM/PEAP fail number of AC.
 *
 * Attributes:
 *   accessible 1     isscalar 1     enums  0      hasdefval 0
 *   readable   1     iscolumn 0     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the autoAuthUserLostConnectionCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param autoAuthUserLostConnectionCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
autoAuthUserLostConnectionCnt_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * autoAuthUserLostConnectionCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != autoAuthUserLostConnectionCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:autoAuthUserLostConnectionCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the autoAuthUserLostConnectionCnt data.
 * copy (* autoAuthUserLostConnectionCnt_val_ptr ) from rowreq_ctx->data
 */
    (* autoAuthUserLostConnectionCnt_val_ptr ) = rowreq_ctx->data.autoAuthUserLostConnectionCnt;

    return MFD_SUCCESS;
} /* autoAuthUserLostConnectionCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.autoAuthReqCnt
 * autoAuthReqCnt is subid 16 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.16
 * Description:
 The SIM/PEAP request number of AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the autoAuthReqCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param autoAuthReqCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
autoAuthReqCnt_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * autoAuthReqCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != autoAuthReqCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:autoAuthReqCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the autoAuthReqCnt data.
 * copy (* autoAuthReqCnt_val_ptr ) from rowreq_ctx->data
 */
    (* autoAuthReqCnt_val_ptr ) = rowreq_ctx->data.autoAuthReqCnt;

    return MFD_SUCCESS;
} /* autoAuthReqCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.autoAuthSucCnt
 * autoAuthSucCnt is subid 17 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.17
 * Description:
 The SIM/PEAP request success number of AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the autoAuthSucCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param autoAuthSucCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
autoAuthSucCnt_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * autoAuthSucCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != autoAuthSucCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:autoAuthSucCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the autoAuthSucCnt data.
 * copy (* autoAuthSucCnt_val_ptr ) from rowreq_ctx->data
 */
    (* autoAuthSucCnt_val_ptr ) = rowreq_ctx->data.autoAuthSucCnt;

    return MFD_SUCCESS;
} /* autoAuthSucCnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessAuthEntry.autoAuthReqFailCnt
 * autoAuthReqFailCnt is subid 18 of dot11WtpWirelessAuthEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.5.1.18
 * Description:
 The SIM/PEAP request fail number of AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the autoAuthReqFailCnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param autoAuthReqFailCnt_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
autoAuthReqFailCnt_get( dot11WtpWirelessAuthTable_rowreq_ctx *rowreq_ctx, u_long * autoAuthReqFailCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != autoAuthReqFailCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWirelessAuthTable:autoAuthReqFailCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the autoAuthReqFailCnt data.
 * copy (* autoAuthReqFailCnt_val_ptr ) from rowreq_ctx->data
 */
    (* autoAuthReqFailCnt_val_ptr ) = rowreq_ctx->data.autoAuthReqFailCnt;

    return MFD_SUCCESS;
} /* autoAuthReqFailCnt_get */



/** @} */
