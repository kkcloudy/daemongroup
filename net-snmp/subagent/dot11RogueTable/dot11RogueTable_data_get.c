/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11RogueTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11RogueTable get routines.
 * TODO:240:M: Implement dot11RogueTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11RogueTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11RogueTable is subid 1 of dot11Rogue.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11RogueTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPIndex
 * RogueAPIndex is subid 1 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.1
 * Description:
The rogue AP Index.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPIndex_map(long *mib_RogueAPIndex_val_ptr, long raw_RogueAPIndex_val)
{
    netsnmp_assert(NULL != mib_RogueAPIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RogueAPIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_RogueAPIndex_val_ptr) = raw_RogueAPIndex_val;

    return MFD_SUCCESS;
} /* RogueAPIndex_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11RogueTable_indexes_set_tbl_idx(dot11RogueTable_mib_index *tbl_idx, long RogueAPIndex_val)
{
    DEBUGMSGTL(("verbose:dot11RogueTable:dot11RogueTable_indexes_set_tbl_idx","called\n"));

    /* RogueAPIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->RogueAPIndex = RogueAPIndex_val;
    

    return MFD_SUCCESS;
} /* dot11RogueTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11RogueTable_indexes_set(dot11RogueTable_rowreq_ctx *rowreq_ctx, long RogueAPIndex_val)
{
    DEBUGMSGTL(("verbose:dot11RogueTable:dot11RogueTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11RogueTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , RogueAPIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11RogueTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11RogueTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPMac
 * RogueAPMac is subid 2 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.2
 * Description:
The rogue AP MAC address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPMac.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPMac_map(char **mib_RogueAPMac_val_ptr_ptr, size_t *mib_RogueAPMac_val_ptr_len_ptr, char *raw_RogueAPMac_val_ptr, size_t raw_RogueAPMac_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_RogueAPMac_val_ptr);
    netsnmp_assert((NULL != mib_RogueAPMac_val_ptr_ptr) && (NULL != mib_RogueAPMac_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPMac_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement RogueAPMac non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_RogueAPMac_val_ptr_len; /* assume equal */
    if((NULL == *mib_RogueAPMac_val_ptr_ptr) || (*mib_RogueAPMac_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_RogueAPMac_val_ptr_ptr = realloc( *mib_RogueAPMac_val_ptr_ptr, converted_len * sizeof(**mib_RogueAPMac_val_ptr_ptr));
        if(NULL == *mib_RogueAPMac_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_RogueAPMac_val_ptr_len_ptr = converted_len;
    memcpy( *mib_RogueAPMac_val_ptr_ptr, raw_RogueAPMac_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* RogueAPMac_map */

/**
 * Extract the current value of the RogueAPMac data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPMac_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param RogueAPMac_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by RogueAPMac.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*RogueAPMac_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update RogueAPMac_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
RogueAPMac_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, char **RogueAPMac_val_ptr_ptr, size_t *RogueAPMac_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != RogueAPMac_val_ptr_ptr) && (NULL != *RogueAPMac_val_ptr_ptr));
   netsnmp_assert( NULL != RogueAPMac_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPMac_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPMac data.
 * set (* RogueAPMac_val_ptr_ptr ) and (* RogueAPMac_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for RogueAPMac data
     */
    if ((NULL == (* RogueAPMac_val_ptr_ptr )) ||
        ((* RogueAPMac_val_ptr_len_ptr ) < (rowreq_ctx->data.RogueAPMac_len * sizeof((* RogueAPMac_val_ptr_ptr )[0])))) {
        /*
         * allocate space for RogueAPMac data
         */
        (* RogueAPMac_val_ptr_ptr ) = malloc(rowreq_ctx->data.RogueAPMac_len * sizeof((* RogueAPMac_val_ptr_ptr )[0]));
        if(NULL == (* RogueAPMac_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* RogueAPMac_val_ptr_len_ptr ) = rowreq_ctx->data.RogueAPMac_len * sizeof((* RogueAPMac_val_ptr_ptr )[0]);
    memcpy( (* RogueAPMac_val_ptr_ptr ), rowreq_ctx->data.RogueAPMac, (* RogueAPMac_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* RogueAPMac_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPRssi
 * RogueAPRssi is subid 3 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.3
 * Description:
The rogue AP RSSI.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPRssi.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPRssi_map(long *mib_RogueAPRssi_val_ptr, long raw_RogueAPRssi_val)
{
    netsnmp_assert(NULL != mib_RogueAPRssi_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPRssi_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RogueAPRssi mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_RogueAPRssi_val_ptr) = raw_RogueAPRssi_val;

    return MFD_SUCCESS;
} /* RogueAPRssi_map */

/**
 * Extract the current value of the RogueAPRssi data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPRssi_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
RogueAPRssi_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, long * RogueAPRssi_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != RogueAPRssi_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPRssi_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPRssi data.
 * set (* RogueAPRssi_val_ptr ) from rowreq_ctx->data
 */
    (* RogueAPRssi_val_ptr ) = rowreq_ctx->data.RogueAPRssi;

    return MFD_SUCCESS;
} /* RogueAPRssi_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPMonitorNum
 * RogueAPMonitorNum is subid 4 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.4
 * Description:
The monitor AP number which detect rogue AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPMonitorNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPMonitorNum_map(long *mib_RogueAPMonitorNum_val_ptr, long raw_RogueAPMonitorNum_val)
{
    netsnmp_assert(NULL != mib_RogueAPMonitorNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPMonitorNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RogueAPMonitorNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_RogueAPMonitorNum_val_ptr) = raw_RogueAPMonitorNum_val;

    return MFD_SUCCESS;
} /* RogueAPMonitorNum_map */

/**
 * Extract the current value of the RogueAPMonitorNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPMonitorNum_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
RogueAPMonitorNum_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, long * RogueAPMonitorNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != RogueAPMonitorNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPMonitorNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPMonitorNum data.
 * set (* RogueAPMonitorNum_val_ptr ) from rowreq_ctx->data
 */
    (* RogueAPMonitorNum_val_ptr ) = rowreq_ctx->data.RogueAPMonitorNum;

    return MFD_SUCCESS;
} /* RogueAPMonitorNum_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPRxSignalIntensity
 * RogueAPRxSignalIntensity is subid 5 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.5
 * Description:
The rogue AP receive signal intensity.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPRxSignalIntensity.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPRxSignalIntensity_map(long *mib_RogueAPRxSignalIntensity_val_ptr, long raw_RogueAPRxSignalIntensity_val)
{
    netsnmp_assert(NULL != mib_RogueAPRxSignalIntensity_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPRxSignalIntensity_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RogueAPRxSignalIntensity mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_RogueAPRxSignalIntensity_val_ptr) = raw_RogueAPRxSignalIntensity_val;

    return MFD_SUCCESS;
} /* RogueAPRxSignalIntensity_map */

/**
 * Extract the current value of the RogueAPRxSignalIntensity data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPRxSignalIntensity_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
RogueAPRxSignalIntensity_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, long * RogueAPRxSignalIntensity_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != RogueAPRxSignalIntensity_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPRxSignalIntensity_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPRxSignalIntensity data.
 * set (* RogueAPRxSignalIntensity_val_ptr ) from rowreq_ctx->data
 */
    (* RogueAPRxSignalIntensity_val_ptr ) = rowreq_ctx->data.RogueAPRxSignalIntensity;

    return MFD_SUCCESS;
} /* RogueAPRxSignalIntensity_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPFirstDetectTm
 * RogueAPFirstDetectTm is subid 6 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.6
 * Description:
The first time that detect the rogue AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPFirstDetectTm.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPFirstDetectTm_map(char **mib_RogueAPFirstDetectTm_val_ptr_ptr, size_t *mib_RogueAPFirstDetectTm_val_ptr_len_ptr, char *raw_RogueAPFirstDetectTm_val_ptr, size_t raw_RogueAPFirstDetectTm_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_RogueAPFirstDetectTm_val_ptr);
    netsnmp_assert((NULL != mib_RogueAPFirstDetectTm_val_ptr_ptr) && (NULL != mib_RogueAPFirstDetectTm_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPFirstDetectTm_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement RogueAPFirstDetectTm non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_RogueAPFirstDetectTm_val_ptr_len; /* assume equal */
    if((NULL == *mib_RogueAPFirstDetectTm_val_ptr_ptr) || (*mib_RogueAPFirstDetectTm_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_RogueAPFirstDetectTm_val_ptr_ptr = realloc( *mib_RogueAPFirstDetectTm_val_ptr_ptr, converted_len * sizeof(**mib_RogueAPFirstDetectTm_val_ptr_ptr));
        if(NULL == *mib_RogueAPFirstDetectTm_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_RogueAPFirstDetectTm_val_ptr_len_ptr = converted_len;
    memcpy( *mib_RogueAPFirstDetectTm_val_ptr_ptr, raw_RogueAPFirstDetectTm_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* RogueAPFirstDetectTm_map */

/**
 * Extract the current value of the RogueAPFirstDetectTm data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPFirstDetectTm_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param RogueAPFirstDetectTm_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by RogueAPFirstDetectTm.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*RogueAPFirstDetectTm_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update RogueAPFirstDetectTm_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
RogueAPFirstDetectTm_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, char **RogueAPFirstDetectTm_val_ptr_ptr, size_t *RogueAPFirstDetectTm_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != RogueAPFirstDetectTm_val_ptr_ptr) && (NULL != *RogueAPFirstDetectTm_val_ptr_ptr));
   netsnmp_assert( NULL != RogueAPFirstDetectTm_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPFirstDetectTm_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPFirstDetectTm data.
 * set (* RogueAPFirstDetectTm_val_ptr_ptr ) and (* RogueAPFirstDetectTm_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for RogueAPFirstDetectTm data
     */
    if ((NULL == (* RogueAPFirstDetectTm_val_ptr_ptr )) ||
        ((* RogueAPFirstDetectTm_val_ptr_len_ptr ) < (rowreq_ctx->data.RogueAPFirstDetectTm_len * sizeof((* RogueAPFirstDetectTm_val_ptr_ptr )[0])))) {
        /*
         * allocate space for RogueAPFirstDetectTm data
         */
        (* RogueAPFirstDetectTm_val_ptr_ptr ) = malloc(rowreq_ctx->data.RogueAPFirstDetectTm_len * sizeof((* RogueAPFirstDetectTm_val_ptr_ptr )[0]));
        if(NULL == (* RogueAPFirstDetectTm_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* RogueAPFirstDetectTm_val_ptr_len_ptr ) = rowreq_ctx->data.RogueAPFirstDetectTm_len * sizeof((* RogueAPFirstDetectTm_val_ptr_ptr )[0]);
    memcpy( (* RogueAPFirstDetectTm_val_ptr_ptr ), rowreq_ctx->data.RogueAPFirstDetectTm, (* RogueAPFirstDetectTm_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* RogueAPFirstDetectTm_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPLastDetectTm
 * RogueAPLastDetectTm is subid 7 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.7
 * Description:
The last time that detect the rogue AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPLastDetectTm.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPLastDetectTm_map(char **mib_RogueAPLastDetectTm_val_ptr_ptr, size_t *mib_RogueAPLastDetectTm_val_ptr_len_ptr, char *raw_RogueAPLastDetectTm_val_ptr, size_t raw_RogueAPLastDetectTm_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_RogueAPLastDetectTm_val_ptr);
    netsnmp_assert((NULL != mib_RogueAPLastDetectTm_val_ptr_ptr) && (NULL != mib_RogueAPLastDetectTm_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPLastDetectTm_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement RogueAPLastDetectTm non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_RogueAPLastDetectTm_val_ptr_len; /* assume equal */
    if((NULL == *mib_RogueAPLastDetectTm_val_ptr_ptr) || (*mib_RogueAPLastDetectTm_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_RogueAPLastDetectTm_val_ptr_ptr = realloc( *mib_RogueAPLastDetectTm_val_ptr_ptr, converted_len * sizeof(**mib_RogueAPLastDetectTm_val_ptr_ptr));
        if(NULL == *mib_RogueAPLastDetectTm_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_RogueAPLastDetectTm_val_ptr_len_ptr = converted_len;
    memcpy( *mib_RogueAPLastDetectTm_val_ptr_ptr, raw_RogueAPLastDetectTm_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* RogueAPLastDetectTm_map */

/**
 * Extract the current value of the RogueAPLastDetectTm data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPLastDetectTm_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param RogueAPLastDetectTm_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by RogueAPLastDetectTm.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*RogueAPLastDetectTm_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update RogueAPLastDetectTm_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
RogueAPLastDetectTm_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, char **RogueAPLastDetectTm_val_ptr_ptr, size_t *RogueAPLastDetectTm_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != RogueAPLastDetectTm_val_ptr_ptr) && (NULL != *RogueAPLastDetectTm_val_ptr_ptr));
   netsnmp_assert( NULL != RogueAPLastDetectTm_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPLastDetectTm_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPLastDetectTm data.
 * set (* RogueAPLastDetectTm_val_ptr_ptr ) and (* RogueAPLastDetectTm_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for RogueAPLastDetectTm data
     */
    if ((NULL == (* RogueAPLastDetectTm_val_ptr_ptr )) ||
        ((* RogueAPLastDetectTm_val_ptr_len_ptr ) < (rowreq_ctx->data.RogueAPLastDetectTm_len * sizeof((* RogueAPLastDetectTm_val_ptr_ptr )[0])))) {
        /*
         * allocate space for RogueAPLastDetectTm data
         */
        (* RogueAPLastDetectTm_val_ptr_ptr ) = malloc(rowreq_ctx->data.RogueAPLastDetectTm_len * sizeof((* RogueAPLastDetectTm_val_ptr_ptr )[0]));
        if(NULL == (* RogueAPLastDetectTm_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* RogueAPLastDetectTm_val_ptr_len_ptr ) = rowreq_ctx->data.RogueAPLastDetectTm_len * sizeof((* RogueAPLastDetectTm_val_ptr_ptr )[0]);
    memcpy( (* RogueAPLastDetectTm_val_ptr_ptr ), rowreq_ctx->data.RogueAPLastDetectTm, (* RogueAPLastDetectTm_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* RogueAPLastDetectTm_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPSSID
 * RogueAPSSID is subid 8 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.8
 * Description:
Rogue AP SSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPSSID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPSSID_map(char **mib_RogueAPSSID_val_ptr_ptr, size_t *mib_RogueAPSSID_val_ptr_len_ptr, char *raw_RogueAPSSID_val_ptr, size_t raw_RogueAPSSID_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_RogueAPSSID_val_ptr);
    netsnmp_assert((NULL != mib_RogueAPSSID_val_ptr_ptr) && (NULL != mib_RogueAPSSID_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPSSID_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement RogueAPSSID non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_RogueAPSSID_val_ptr_len; /* assume equal */
    if((NULL == *mib_RogueAPSSID_val_ptr_ptr) || (*mib_RogueAPSSID_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_RogueAPSSID_val_ptr_ptr = realloc( *mib_RogueAPSSID_val_ptr_ptr, converted_len * sizeof(**mib_RogueAPSSID_val_ptr_ptr));
        if(NULL == *mib_RogueAPSSID_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_RogueAPSSID_val_ptr_len_ptr = converted_len;
    memcpy( *mib_RogueAPSSID_val_ptr_ptr, raw_RogueAPSSID_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* RogueAPSSID_map */

/**
 * Extract the current value of the RogueAPSSID data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPSSID_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param RogueAPSSID_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by RogueAPSSID.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*RogueAPSSID_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update RogueAPSSID_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
RogueAPSSID_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, char **RogueAPSSID_val_ptr_ptr, size_t *RogueAPSSID_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != RogueAPSSID_val_ptr_ptr) && (NULL != *RogueAPSSID_val_ptr_ptr));
   netsnmp_assert( NULL != RogueAPSSID_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPSSID_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPSSID data.
 * set (* RogueAPSSID_val_ptr_ptr ) and (* RogueAPSSID_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for RogueAPSSID data
     */
    if ((NULL == (* RogueAPSSID_val_ptr_ptr )) ||
        ((* RogueAPSSID_val_ptr_len_ptr ) < (rowreq_ctx->data.RogueAPSSID_len * sizeof((* RogueAPSSID_val_ptr_ptr )[0])))) {
        /*
         * allocate space for RogueAPSSID data
         */
        (* RogueAPSSID_val_ptr_ptr ) = malloc(rowreq_ctx->data.RogueAPSSID_len * sizeof((* RogueAPSSID_val_ptr_ptr )[0]));
        if(NULL == (* RogueAPSSID_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* RogueAPSSID_val_ptr_len_ptr ) = rowreq_ctx->data.RogueAPSSID_len * sizeof((* RogueAPSSID_val_ptr_ptr )[0]);
    memcpy( (* RogueAPSSID_val_ptr_ptr ), rowreq_ctx->data.RogueAPSSID, (* RogueAPSSID_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* RogueAPSSID_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPMaxSigStrength
 * RogueAPMaxSigStrength is subid 9 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.9
 * Description:
WIDS receive from the suspect the maximum AP signal strength.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPMaxSigStrength.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPMaxSigStrength_map(long *mib_RogueAPMaxSigStrength_val_ptr, long raw_RogueAPMaxSigStrength_val)
{
    netsnmp_assert(NULL != mib_RogueAPMaxSigStrength_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPMaxSigStrength_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RogueAPMaxSigStrength mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_RogueAPMaxSigStrength_val_ptr) = raw_RogueAPMaxSigStrength_val;

    return MFD_SUCCESS;
} /* RogueAPMaxSigStrength_map */

/**
 * Extract the current value of the RogueAPMaxSigStrength data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPMaxSigStrength_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
RogueAPMaxSigStrength_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, long * RogueAPMaxSigStrength_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != RogueAPMaxSigStrength_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPMaxSigStrength_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPMaxSigStrength data.
 * set (* RogueAPMaxSigStrength_val_ptr ) from rowreq_ctx->data
 */
    (* RogueAPMaxSigStrength_val_ptr ) = rowreq_ctx->data.RogueAPMaxSigStrength;

    return MFD_SUCCESS;
} /* RogueAPMaxSigStrength_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPChannel
 * RogueAPChannel is subid 10 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.10
 * Description:
Rogue AP channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPChannel.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPChannel_map(long *mib_RogueAPChannel_val_ptr, long raw_RogueAPChannel_val)
{
    netsnmp_assert(NULL != mib_RogueAPChannel_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPChannel_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RogueAPChannel mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_RogueAPChannel_val_ptr) = raw_RogueAPChannel_val;

    return MFD_SUCCESS;
} /* RogueAPChannel_map */

/**
 * Extract the current value of the RogueAPChannel data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPChannel_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
RogueAPChannel_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, long * RogueAPChannel_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != RogueAPChannel_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPChannel_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPChannel data.
 * set (* RogueAPChannel_val_ptr ) from rowreq_ctx->data
 */
    (* RogueAPChannel_val_ptr ) = rowreq_ctx->data.RogueAPChannel;

    return MFD_SUCCESS;
} /* RogueAPChannel_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPEncryptAgrithm
 * RogueAPEncryptAgrithm is subid 11 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.11
 * Description:
Encryption method of the rogue AP frame.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 4/8. Values:  noEncry(0), wep(1), wpa1(2), wpa2(3)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPEncryptAgrithm.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPEncryptAgrithm_map(u_long *mib_RogueAPEncryptAgrithm_val_ptr, u_long raw_RogueAPEncryptAgrithm_val)
{
    netsnmp_assert(NULL != mib_RogueAPEncryptAgrithm_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPEncryptAgrithm_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RogueAPEncryptAgrithm enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_RogueAPEncryptAgrithm_val) {
        case INTERNAL_ROGUEAPENCRYPTAGRITHM_NOENCRY:
             *mib_RogueAPEncryptAgrithm_val_ptr = ROGUEAPENCRYPTAGRITHM_NOENCRY;
             break;

        case INTERNAL_ROGUEAPENCRYPTAGRITHM_WEP:
             *mib_RogueAPEncryptAgrithm_val_ptr = ROGUEAPENCRYPTAGRITHM_WEP;
             break;

        case INTERNAL_ROGUEAPENCRYPTAGRITHM_WPA1:
             *mib_RogueAPEncryptAgrithm_val_ptr = ROGUEAPENCRYPTAGRITHM_WPA1;
             break;

        case INTERNAL_ROGUEAPENCRYPTAGRITHM_WPA2:
             *mib_RogueAPEncryptAgrithm_val_ptr = ROGUEAPENCRYPTAGRITHM_WPA2;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for RogueAPEncryptAgrithm\n", raw_RogueAPEncryptAgrithm_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* RogueAPEncryptAgrithm_map */

/**
 * Extract the current value of the RogueAPEncryptAgrithm data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPEncryptAgrithm_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
RogueAPEncryptAgrithm_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, u_long * RogueAPEncryptAgrithm_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != RogueAPEncryptAgrithm_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPEncryptAgrithm_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPEncryptAgrithm data.
 * set (* RogueAPEncryptAgrithm_val_ptr ) from rowreq_ctx->data
 */
    (* RogueAPEncryptAgrithm_val_ptr ) = rowreq_ctx->data.RogueAPEncryptAgrithm;

    return MFD_SUCCESS;
} /* RogueAPEncryptAgrithm_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPAttackedStatus
 * RogueAPAttackedStatus is subid 12 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.12
 * Description:
Whether strategies to the rogue AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPAttackedStatus.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPAttackedStatus_map(u_long *mib_RogueAPAttackedStatus_val_ptr, u_long raw_RogueAPAttackedStatus_val)
{
    netsnmp_assert(NULL != mib_RogueAPAttackedStatus_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPAttackedStatus_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RogueAPAttackedStatus enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_RogueAPAttackedStatus_val) {
        case INTERNAL_ROGUEAPATTACKEDSTATUS_TRUE:
             *mib_RogueAPAttackedStatus_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_ROGUEAPATTACKEDSTATUS_FALSE:
             *mib_RogueAPAttackedStatus_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for RogueAPAttackedStatus\n", raw_RogueAPAttackedStatus_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* RogueAPAttackedStatus_map */

/**
 * Extract the current value of the RogueAPAttackedStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPAttackedStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
RogueAPAttackedStatus_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, u_long * RogueAPAttackedStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != RogueAPAttackedStatus_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPAttackedStatus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPAttackedStatus data.
 * set (* RogueAPAttackedStatus_val_ptr ) from rowreq_ctx->data
 */
    (* RogueAPAttackedStatus_val_ptr ) = rowreq_ctx->data.RogueAPAttackedStatus;

    return MFD_SUCCESS;
} /* RogueAPAttackedStatus_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPToIgnore
 * RogueAPToIgnore is subid 13 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.13
 * Description:
Whether or not ignore the rogue AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPToIgnore.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPToIgnore_map(u_long *mib_RogueAPToIgnore_val_ptr, u_long raw_RogueAPToIgnore_val)
{
    netsnmp_assert(NULL != mib_RogueAPToIgnore_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPToIgnore_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RogueAPToIgnore enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_RogueAPToIgnore_val) {
        case INTERNAL_ROGUEAPTOIGNORE_TRUE:
             *mib_RogueAPToIgnore_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_ROGUEAPTOIGNORE_FALSE:
             *mib_RogueAPToIgnore_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for RogueAPToIgnore\n", raw_RogueAPToIgnore_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* RogueAPToIgnore_map */

/**
 * Extract the current value of the RogueAPToIgnore data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPToIgnore_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
RogueAPToIgnore_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, u_long * RogueAPToIgnore_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != RogueAPToIgnore_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPToIgnore_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPToIgnore data.
 * set (* RogueAPToIgnore_val_ptr ) from rowreq_ctx->data
 */
    (* RogueAPToIgnore_val_ptr ) = rowreq_ctx->data.RogueAPToIgnore;

    return MFD_SUCCESS;
} /* RogueAPToIgnore_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueEntry.RogueAPManufacturer
 * RogueAPManufacturer is subid 14 of dot11RogueEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.1.1.14
 * Description:
Rogue AP manufacturer.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RogueAPManufacturer.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RogueAPManufacturer_map(char **mib_RogueAPManufacturer_val_ptr_ptr, size_t *mib_RogueAPManufacturer_val_ptr_len_ptr, char *raw_RogueAPManufacturer_val_ptr, size_t raw_RogueAPManufacturer_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_RogueAPManufacturer_val_ptr);
    netsnmp_assert((NULL != mib_RogueAPManufacturer_val_ptr_ptr) && (NULL != mib_RogueAPManufacturer_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPManufacturer_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement RogueAPManufacturer non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_RogueAPManufacturer_val_ptr_len; /* assume equal */
    if((NULL == *mib_RogueAPManufacturer_val_ptr_ptr) || (*mib_RogueAPManufacturer_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_RogueAPManufacturer_val_ptr_ptr = realloc( *mib_RogueAPManufacturer_val_ptr_ptr, converted_len * sizeof(**mib_RogueAPManufacturer_val_ptr_ptr));
        if(NULL == *mib_RogueAPManufacturer_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_RogueAPManufacturer_val_ptr_len_ptr = converted_len;
    memcpy( *mib_RogueAPManufacturer_val_ptr_ptr, raw_RogueAPManufacturer_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* RogueAPManufacturer_map */

/**
 * Extract the current value of the RogueAPManufacturer data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param RogueAPManufacturer_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param RogueAPManufacturer_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by RogueAPManufacturer.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*RogueAPManufacturer_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update RogueAPManufacturer_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
RogueAPManufacturer_get( dot11RogueTable_rowreq_ctx *rowreq_ctx, char **RogueAPManufacturer_val_ptr_ptr, size_t *RogueAPManufacturer_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != RogueAPManufacturer_val_ptr_ptr) && (NULL != *RogueAPManufacturer_val_ptr_ptr));
   netsnmp_assert( NULL != RogueAPManufacturer_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueTable:RogueAPManufacturer_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the RogueAPManufacturer data.
 * set (* RogueAPManufacturer_val_ptr_ptr ) and (* RogueAPManufacturer_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for RogueAPManufacturer data
     */
    if ((NULL == (* RogueAPManufacturer_val_ptr_ptr )) ||
        ((* RogueAPManufacturer_val_ptr_len_ptr ) < (rowreq_ctx->data.RogueAPManufacturer_len * sizeof((* RogueAPManufacturer_val_ptr_ptr )[0])))) {
        /*
         * allocate space for RogueAPManufacturer data
         */
        (* RogueAPManufacturer_val_ptr_ptr ) = malloc(rowreq_ctx->data.RogueAPManufacturer_len * sizeof((* RogueAPManufacturer_val_ptr_ptr )[0]));
        if(NULL == (* RogueAPManufacturer_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* RogueAPManufacturer_val_ptr_len_ptr ) = rowreq_ctx->data.RogueAPManufacturer_len * sizeof((* RogueAPManufacturer_val_ptr_ptr )[0]);
    memcpy( (* RogueAPManufacturer_val_ptr_ptr ), rowreq_ctx->data.RogueAPManufacturer, (* RogueAPManufacturer_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* RogueAPManufacturer_get */



/** @} */
