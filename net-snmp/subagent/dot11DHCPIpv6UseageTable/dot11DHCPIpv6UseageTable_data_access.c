/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 14170 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11DHCPIpv6UseageTable.h"


#include "dot11DHCPIpv6UseageTable_data_access.h"
#include "ws_dhcpv6.h"
#include "ws_dbus_list_interface.h"


/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11DHCPIpv6UseageTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11DHCPIpv6UseageTable is subid 8 of dot11Service.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.8, length: 12
*/

/**
 * initialization for dot11DHCPIpv6UseageTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11DHCPIpv6UseageTable_reg
 *        Pointer to dot11DHCPIpv6UseageTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11DHCPIpv6UseageTable_init_data(dot11DHCPIpv6UseageTable_registration * dot11DHCPIpv6UseageTable_reg)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6UseageTable:dot11DHCPIpv6UseageTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11DHCPIpv6UseageTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11DHCPIpv6UseageTable_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11DHCPIpv6UseageTable_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6UseageTable:dot11DHCPIpv6UseageTable_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to dot11DHCPIpv6UseageTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to dot11DHCPIpv6UseageTable_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up dot11DHCPIpv6UseageTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11DHCPIPV6USEAGETABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11DHCPIpv6UseageTable_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before dot11DHCPIpv6UseageTable_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
dot11DHCPIpv6UseageTable_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6UseageTable:dot11DHCPIpv6UseageTable_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to dot11DHCPIpv6UseageTable_container_shutdown\n");
        return;
    }

} /* dot11DHCPIpv6UseageTable_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement dot11DHCPIpv6UseageTable data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11DHCPIpv6UseageTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11DHCPIpv6UseageTable_container_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11DHCPIpv6UseageTable_container_load\n");
	dot11DHCPIpv6UseageTable_rowreq_ctx *rowreq_ctx;
	size_t                 count = 0;

	/*
	* temporary storage for index values
	*/
	/*
	 * Ipv6DHCPUSEID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
	 */
	long   Ipv6DHCPUSEID;
	u_long  ipPoolIndex = 0;


	DEBUGMSGTL(("verbose:dot11DHCPIpv6UseageTable:dot11DHCPIpv6UseageTable_container_load","called\n"));

	int ret = 0,i=0;
	struct lease_state  total_state;
	struct sub_lease_state sub_state[MAX_SUB_NET];
	memset(&total_state, 0, sizeof(struct lease_state));
	memset(sub_state, 0, sizeof(struct sub_lease_state)*MAX_SUB_NET);
	unsigned int subnet_num = 0;
	instance_parameter *paraHead = NULL, *paraNode = NULL;
	list_instance_parameter(&paraHead, SNMPD_SLOT_CONNECT);
	for(paraNode = paraHead; paraNode; paraNode = paraNode->next) {
		memset(&total_state, 0, sizeof(struct lease_state));
		memset(sub_state, 0, sizeof(struct sub_lease_state)*MAX_SUB_NET);
       	 	ipPoolIndex = (paraNode->parameter.slot_id - 1) * WIRELESS_MAX_NUM;
		ret = ccgi_show_dhcp_lease_ipv6_state(&total_state,sub_state,&subnet_num,paraNode->parameter.slot_id);
		if(ret == 0)
		{
			for(i=0;i<subnet_num;i++)
			{
				ipPoolIndex++;
				/*
				 * TODO:352:M: |   |-> set indexes in new dot11DHCPIpv6UseageTable rowreq context.
				 * data context will be set from the param (unless NULL,
				 *	in which case a new data context will be allocated)
				 */
				rowreq_ctx = dot11DHCPIpv6UseageTable_allocate_rowreq_ctx(NULL);
				if (NULL == rowreq_ctx) {
				    snmp_log(LOG_ERR, "memory allocation failed\n");
				    free_instance_parameter_list(&paraHead);			
				    return MFD_RESOURCE_UNAVAILABLE;
				}
				if(MFD_SUCCESS != dot11DHCPIpv6UseageTable_indexes_set(rowreq_ctx
						       , ipPoolIndex
				       )) {
				    snmp_log(LOG_ERR,"error setting index while loading "
					     "dot11DHCPIpv6UseageTable data.\n");
				    dot11DHCPIpv6UseageTable_release_rowreq_ctx(rowreq_ctx);
				    continue;
				}
				
				/*
				 * setup/save data for DHCPIpv6poolName
				 * DHCPIpv6poolName(2)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
				 */
					snmp_log(LOG_DEBUG,"i=%d\n",i);
				snmp_log(LOG_DEBUG,"POOLNAME=%s\n",sub_state[i].poolname);
			    rowreq_ctx->data.DHCPIpv6poolName_len=MIN(strlen(sub_state[i].poolname),sizeof(rowreq_ctx->data.DHCPIpv6poolName)-1);
			    memcpy( rowreq_ctx->data.DHCPIpv6poolName, sub_state[i].poolname, rowreq_ctx->data.DHCPIpv6poolName_len );
			    /*
			     * setup/save data for DHCPIpv6PoolUseage
			     * DHCPIpv6PoolUseage(3)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
			     */
			    /** no mapping */
			    snmp_log(LOG_DEBUG,"sub_state[i].subnet_lease_state.total_lease_num=%d\n",sub_state[i].subnet_lease_state.total_lease_num);
			    snmp_log(LOG_DEBUG,"sub_state[i].subnet_lease_state.active_lease_num=%d\n",sub_state[i].subnet_lease_state.active_lease_num);
			    if(sub_state[i].subnet_lease_state.total_lease_num == 0)
			    	rowreq_ctx->data.DHCPIpv6PoolUseage =0;
			    else
			    	rowreq_ctx->data.DHCPIpv6PoolUseage = (sub_state[i].subnet_lease_state.active_lease_num/sub_state[i].subnet_lease_state.total_lease_num)*100;

			/*
			 * insert into table container
			 */
			if(CONTAINER_INSERT(container, rowreq_ctx))
			{
				dot11DHCPIpv6UseageTable_release_rowreq_ctx(rowreq_ctx);

			};
			++count;
			}
		}
	}
    
    DEBUGMSGT(("verbose:dot11DHCPIpv6UseageTable:dot11DHCPIpv6UseageTable_container_load",
               "inserted %d records\n", count));
    snmp_log(LOG_DEBUG,"exit dot11DHCPIpv6UseageTable_container_load");

    return MFD_SUCCESS;
} /* dot11DHCPIpv6UseageTable_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11DHCPIpv6UseageTable_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6UseageTable:dot11DHCPIpv6UseageTable_container_free","called\n"));

    /*
     * TODO:380:M: Free dot11DHCPIpv6UseageTable container data.
     */
} /* dot11DHCPIpv6UseageTable_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11DHCPIpv6UseageTable_row_prep( dot11DHCPIpv6UseageTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6UseageTable:dot11DHCPIpv6UseageTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11DHCPIpv6UseageTable_row_prep */

/** @} */
