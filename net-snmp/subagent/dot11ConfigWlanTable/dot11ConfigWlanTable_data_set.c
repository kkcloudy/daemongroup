/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigWlanTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigWlanTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigWlanTable is subid 2 of dot11wlanConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.2, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigWlanTable_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:dot11ConfigWlanTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigWlanTable undo.
     * set up dot11ConfigWlanTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11ConfigWlanTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigWlanTable_undo_cleanup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:dot11ConfigWlanTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigWlanTable undo.
     */

    return rc;
} /* dot11ConfigWlanTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigWlanTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigWlanTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigWlanTable_commit( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:dot11ConfigWlanTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11ConfigWlanTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WLANBINDSECURITY) {
       save_flags &= ~FLAG_WLANBINDSECURITY; /* clear wlanBindSecurity */
       /*
        * TODO:482:o: |-> commit column wlanBindSecurity.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWlanTable column wlanBindSecurity commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wlanBindSecurity
             */
            rowreq_ctx->column_set_flags |= FLAG_WLANBINDSECURITY;
       }
    }

    if (save_flags & FLAG_WLANBINDIF) {
       save_flags &= ~FLAG_WLANBINDIF; /* clear wlanBindIf */
       /*
        * TODO:482:o: |-> commit column wlanBindIf.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWlanTable column wlanBindIf commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wlanBindIf
             */
            rowreq_ctx->column_set_flags |= FLAG_WLANBINDIF;
       }
    }

    if (save_flags & FLAG_WLANHIDEESSID) {
       save_flags &= ~FLAG_WLANHIDEESSID; /* clear wlanHideEssid */
       /*
        * TODO:482:o: |-> commit column wlanHideEssid.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWlanTable column wlanHideEssid commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wlanHideEssid
             */
            rowreq_ctx->column_set_flags |= FLAG_WLANHIDEESSID;
       }
    }

    if (save_flags & FLAG_WLANSERVICEENABLE) {
       save_flags &= ~FLAG_WLANSERVICEENABLE; /* clear wlanServiceEnable */
       /*
        * TODO:482:o: |-> commit column wlanServiceEnable.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWlanTable column wlanServiceEnable commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wlanServiceEnable
             */
            rowreq_ctx->column_set_flags |= FLAG_WLANSERVICEENABLE;
       }
    }

    if (save_flags & FLAG_WLANMAXCONNECTUSR) {
       save_flags &= ~FLAG_WLANMAXCONNECTUSR; /* clear wlanMaxConnectUsr */
       /*
        * TODO:482:o: |-> commit column wlanMaxConnectUsr.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWlanTable column wlanMaxConnectUsr commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wlanMaxConnectUsr
             */
            rowreq_ctx->column_set_flags |= FLAG_WLANMAXCONNECTUSR;
       }
    }

    if (save_flags & FLAG_WLANLOADBALANCESTATUS) {
       save_flags &= ~FLAG_WLANLOADBALANCESTATUS; /* clear wlanLoadBalanceStatus */
       /*
        * TODO:482:o: |-> commit column wlanLoadBalanceStatus.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWlanTable column wlanLoadBalanceStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wlanLoadBalanceStatus
             */
            rowreq_ctx->column_set_flags |= FLAG_WLANLOADBALANCESTATUS;
       }
    }

    if (save_flags & FLAG_WLANLOADBALANCESTATUSBASEONFLOW) {
       save_flags &= ~FLAG_WLANLOADBALANCESTATUSBASEONFLOW; /* clear wlanLoadBalanceStatusBaseOnFlow */
       /*
        * TODO:482:o: |-> commit column wlanLoadBalanceStatusBaseOnFlow.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWlanTable column wlanLoadBalanceStatusBaseOnFlow commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wlanLoadBalanceStatusBaseOnFlow
             */
            rowreq_ctx->column_set_flags |= FLAG_WLANLOADBALANCESTATUSBASEONFLOW;
       }
    }

    if (save_flags & FLAG_WLANLOADBALANCESTATUSBASEONUSR) {
       save_flags &= ~FLAG_WLANLOADBALANCESTATUSBASEONUSR; /* clear wlanLoadBalanceStatusBaseOnUsr */
       /*
        * TODO:482:o: |-> commit column wlanLoadBalanceStatusBaseOnUsr.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWlanTable column wlanLoadBalanceStatusBaseOnUsr commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wlanLoadBalanceStatusBaseOnUsr
             */
            rowreq_ctx->column_set_flags |= FLAG_WLANLOADBALANCESTATUSBASEONUSR;
       }
    }

    if (save_flags & FLAG_WLANLOADBALANCETRAFFICDIFFTHRESHHD) {
       save_flags &= ~FLAG_WLANLOADBALANCETRAFFICDIFFTHRESHHD; /* clear wlanLoadBalanceTrafficDiffThreshhd */
       /*
        * TODO:482:o: |-> commit column wlanLoadBalanceTrafficDiffThreshhd.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWlanTable column wlanLoadBalanceTrafficDiffThreshhd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wlanLoadBalanceTrafficDiffThreshhd
             */
            rowreq_ctx->column_set_flags |= FLAG_WLANLOADBALANCETRAFFICDIFFTHRESHHD;
       }
    }

    if (save_flags & FLAG_WLANLOADBALANCEUSERSDIFFTHRESHHD) {
       save_flags &= ~FLAG_WLANLOADBALANCEUSERSDIFFTHRESHHD; /* clear wlanLoadBalanceUsersDiffThreshhd */
       /*
        * TODO:482:o: |-> commit column wlanLoadBalanceUsersDiffThreshhd.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWlanTable column wlanLoadBalanceUsersDiffThreshhd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wlanLoadBalanceUsersDiffThreshhd
             */
            rowreq_ctx->column_set_flags |= FLAG_WLANLOADBALANCEUSERSDIFFTHRESHHD;
       }
    }

    if (save_flags & FLAG_WLANINTERFACEIPADDR) {
       save_flags &= ~FLAG_WLANINTERFACEIPADDR; /* clear wlanInterfaceIPAddr */
       /*
        * TODO:482:o: |-> commit column wlanInterfaceIPAddr.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWlanTable column wlanInterfaceIPAddr commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wlanInterfaceIPAddr
             */
            rowreq_ctx->column_set_flags |= FLAG_WLANINTERFACEIPADDR;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigWlanTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigWlanTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigWlanTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigWlanTable_undo_commit( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:dot11ConfigWlanTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigWlanTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11ConfigWlanTable_undo_commit */

/*
 * TODO:420:r: Implement dot11ConfigWlanTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11VlanConfigEntry.wlanID
 * wlanID is subid 1 of dot11VlanConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.7.1.1
 * Description:
Vlan ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/*
 * TODO:440:M: Implement dot11ConfigWlanTable node value checks.
 * TODO:450:M: Implement dot11ConfigWlanTable undo functions.
 * TODO:460:M: Implement dot11ConfigWlanTable set functions.
 * TODO:480:M: Implement dot11ConfigWlanTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWlanEntry.wlanBindSecurity
 * wlanBindSecurity is subid 1 of dot11ConfigWlanEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.2.1.1
 * Description:
Bind security.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wlanBindSecurity_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWlanTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wlanBindSecurity_check_value( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, long wlanBindSecurity_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanBindSecurity_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wlanBindSecurity value.
     */

    return MFD_SUCCESS; /* wlanBindSecurity value not illegal */
} /* wlanBindSecurity_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWlanTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wlanBindSecurity_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanBindSecurity_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wlanBindSecurity undo.
     */
    /*
     * copy wlanBindSecurity data
     * set rowreq_ctx->undo->wlanBindSecurity from rowreq_ctx->data.wlanBindSecurity
     */
    rowreq_ctx->undo->wlanBindSecurity = rowreq_ctx->data.wlanBindSecurity;


    return MFD_SUCCESS;
} /* wlanBindSecurity_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wlanBindSecurity_val
 *        A long containing the new value.
 */
int
wlanBindSecurity_set( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, long wlanBindSecurity_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanBindSecurity_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wlanBindSecurity reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wlanBindSecurity value.
     * set wlanBindSecurity value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
	    return MFD_ERROR;
	
    int rc = MFD_ERROR;
	int ret = 0;

	ret=apply_wlanID(rowreq_ctx->data.parameter,connection,wlanBindSecurity_val,rowreq_ctx->tbl_idx.wlanID);
	if(ret == 1)
	{
		rowreq_ctx->data.wlanBindSecurity = wlanBindSecurity_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wlanBindSecurity_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wlanBindSecurity_undo( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanBindSecurity_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wlanBindSecurity undo.
     */
    /*
     * copy wlanBindSecurity data
     * set rowreq_ctx->data.wlanBindSecurity from rowreq_ctx->undo->wlanBindSecurity
     */
    rowreq_ctx->data.wlanBindSecurity = rowreq_ctx->undo->wlanBindSecurity;

    
    return MFD_SUCCESS;
} /* wlanBindSecurity_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWlanEntry.wlanBindIf
 * wlanBindIf is subid 2 of dot11ConfigWlanEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.2.1.2
 * Description:
Bind interface.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wlanBindIf_val_ptr
 *        A char containing the new value.
 * @param wlanBindIf_val_ptr_len
 *        The size (in bytes) of the data pointed to by wlanBindIf_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWlanTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wlanBindIf_check_value( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, char *wlanBindIf_val_ptr,  size_t wlanBindIf_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanBindIf_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wlanBindIf_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wlanBindIf value.
     */

    return MFD_SUCCESS; /* wlanBindIf value not illegal */
} /* wlanBindIf_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWlanTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wlanBindIf_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanBindIf_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wlanBindIf undo.
     */
    /*
     * copy wlanBindIf and wlanBindIf_len data
     * set rowreq_ctx->undo->wlanBindIf from rowreq_ctx->data.wlanBindIf
     */
    memcpy( rowreq_ctx->undo->wlanBindIf, rowreq_ctx->data.wlanBindIf,
            (rowreq_ctx->data.wlanBindIf_len * sizeof(rowreq_ctx->undo->wlanBindIf[0])));
    rowreq_ctx->undo->wlanBindIf_len = rowreq_ctx->data.wlanBindIf_len;


    return MFD_SUCCESS;
} /* wlanBindIf_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wlanBindIf_val_ptr
 *        A char containing the new value.
 * @param wlanBindIf_val_ptr_len
 *        The size (in bytes) of the data pointed to by wlanBindIf_val_ptr
 */
int
wlanBindIf_set( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, char *wlanBindIf_val_ptr,  size_t wlanBindIf_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanBindIf_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wlanBindIf_val_ptr);

/*
 * TODO:245:o: |-> Implement wlanBindIf reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wlanBindIf value.
     * set wlanBindIf value in rowreq_ctx->data
     */    
	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
	
	int rc = MFD_ERROR;
	int ret = 0;

	ret=wlan_apply_interface(rowreq_ctx->data.parameter,connection,rowreq_ctx->tbl_idx.wlanID,wlanBindIf_val_ptr);
	if(ret == 1)
	{
		memcpy( rowreq_ctx->data.wlanBindIf, wlanBindIf_val_ptr, wlanBindIf_val_ptr_len );
		rowreq_ctx->data.wlanBindIf_len = wlanBindIf_val_ptr_len / sizeof(wlanBindIf_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wlanBindIf_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wlanBindIf_undo( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanBindIf_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wlanBindIf undo.
     */
    /*
     * copy wlanBindIf and wlanBindIf_len data
     * set rowreq_ctx->data.wlanBindIf from rowreq_ctx->undo->wlanBindIf
     */
    memcpy( rowreq_ctx->data.wlanBindIf, rowreq_ctx->undo->wlanBindIf,
            (rowreq_ctx->undo->wlanBindIf_len * sizeof(rowreq_ctx->data.wlanBindIf[0])));
    rowreq_ctx->data.wlanBindIf_len = rowreq_ctx->undo->wlanBindIf_len;

    
    return MFD_SUCCESS;
} /* wlanBindIf_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWlanEntry.wlanHideEssid
 * wlanHideEssid is subid 3 of dot11ConfigWlanEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.2.1.3
 * Description:
Hide ESSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  no(0), yes(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wlanHideEssid_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWlanTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  no(0), yes(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wlanHideEssid_check_value( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, u_long wlanHideEssid_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanHideEssid_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wlanHideEssid value.
     */

    return MFD_SUCCESS; /* wlanHideEssid value not illegal */
} /* wlanHideEssid_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWlanTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wlanHideEssid_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanHideEssid_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wlanHideEssid undo.
     */
    /*
     * copy wlanHideEssid data
     * set rowreq_ctx->undo->wlanHideEssid from rowreq_ctx->data.wlanHideEssid
     */
    rowreq_ctx->undo->wlanHideEssid = rowreq_ctx->data.wlanHideEssid;


    return MFD_SUCCESS;
} /* wlanHideEssid_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wlanHideEssid_val
 *        A long containing the new value.
 */
int
wlanHideEssid_set( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, u_long wlanHideEssid_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanHideEssid_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wlanHideEssid reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wlanHideEssid_val) {
        case WLANHIDEESSID_NO:
             wlanHideEssid_val = INTERNAL_WLANHIDEESSID_NO;
             break;

        case WLANHIDEESSID_YES:
             wlanHideEssid_val = INTERNAL_WLANHIDEESSID_YES;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wlanHideEssid\n", wlanHideEssid_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wlanHideEssid value.
     * set wlanHideEssid value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
	
	int rc = MFD_ERROR;
	int ret = 0;

	ret=set_hideessid(rowreq_ctx->data.parameter,connection,rowreq_ctx->tbl_idx.wlanID,(wlanHideEssid_val==WLANHIDEESSID_YES)?"yes":"no");
	if(ret == 1)
	{
		rowreq_ctx->data.wlanHideEssid = wlanHideEssid_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wlanHideEssid_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wlanHideEssid_undo( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanHideEssid_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wlanHideEssid undo.
     */
    /*
     * copy wlanHideEssid data
     * set rowreq_ctx->data.wlanHideEssid from rowreq_ctx->undo->wlanHideEssid
     */
    rowreq_ctx->data.wlanHideEssid = rowreq_ctx->undo->wlanHideEssid;

    
    return MFD_SUCCESS;
} /* wlanHideEssid_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWlanEntry.wlanServiceEnable
 * wlanServiceEnable is subid 4 of dot11ConfigWlanEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.2.1.4
 * Description:
Service enable.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  disable(0), enable(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wlanServiceEnable_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWlanTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  disable(0), enable(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wlanServiceEnable_check_value( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, u_long wlanServiceEnable_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanServiceEnable_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wlanServiceEnable value.
     */

    return MFD_SUCCESS; /* wlanServiceEnable value not illegal */
} /* wlanServiceEnable_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWlanTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wlanServiceEnable_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanServiceEnable_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wlanServiceEnable undo.
     */
    /*
     * copy wlanServiceEnable data
     * set rowreq_ctx->undo->wlanServiceEnable from rowreq_ctx->data.wlanServiceEnable
     */
    rowreq_ctx->undo->wlanServiceEnable = rowreq_ctx->data.wlanServiceEnable;


    return MFD_SUCCESS;
} /* wlanServiceEnable_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wlanServiceEnable_val
 *        A long containing the new value.
 */
int
wlanServiceEnable_set( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, u_long wlanServiceEnable_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanServiceEnable_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wlanServiceEnable reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wlanServiceEnable_val) {
        case WLANSERVICEENABLE_DISABLE:
             wlanServiceEnable_val = INTERNAL_WLANSERVICEENABLE_DISABLE;
             break;

        case WLANSERVICEENABLE_ENABLE:
             wlanServiceEnable_val = INTERNAL_WLANSERVICEENABLE_ENABLE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wlanServiceEnable\n", wlanServiceEnable_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wlanServiceEnable value.
     * set wlanServiceEnable value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
	
	int rc = MFD_ERROR;
	int ret = 0;

	ret=config_wlan_service(rowreq_ctx->data.parameter,connection,rowreq_ctx->tbl_idx.wlanID,(wlanServiceEnable_val==WLANSERVICEENABLE_ENABLE)?"enable":"disable");
	if(ret == 1)
	{
		rowreq_ctx->data.wlanServiceEnable = wlanServiceEnable_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wlanServiceEnable_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wlanServiceEnable_undo( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanServiceEnable_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wlanServiceEnable undo.
     */
    /*
     * copy wlanServiceEnable data
     * set rowreq_ctx->data.wlanServiceEnable from rowreq_ctx->undo->wlanServiceEnable
     */
    rowreq_ctx->data.wlanServiceEnable = rowreq_ctx->undo->wlanServiceEnable;

    
    return MFD_SUCCESS;
} /* wlanServiceEnable_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWlanEntry.wlanMaxConnectUsr
 * wlanMaxConnectUsr is subid 5 of dot11ConfigWlanEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.2.1.5
 * Description:
Max connect user.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wlanMaxConnectUsr_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWlanTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wlanMaxConnectUsr_check_value( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, long wlanMaxConnectUsr_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanMaxConnectUsr_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wlanMaxConnectUsr value.
     */

    return MFD_SUCCESS; /* wlanMaxConnectUsr value not illegal */
} /* wlanMaxConnectUsr_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWlanTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wlanMaxConnectUsr_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanMaxConnectUsr_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wlanMaxConnectUsr undo.
     */
    /*
     * copy wlanMaxConnectUsr data
     * set rowreq_ctx->undo->wlanMaxConnectUsr from rowreq_ctx->data.wlanMaxConnectUsr
     */
    rowreq_ctx->undo->wlanMaxConnectUsr = rowreq_ctx->data.wlanMaxConnectUsr;


    return MFD_SUCCESS;
} /* wlanMaxConnectUsr_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wlanMaxConnectUsr_val
 *        A long containing the new value.
 */
int
wlanMaxConnectUsr_set( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, long wlanMaxConnectUsr_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanMaxConnectUsr_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wlanMaxConnectUsr reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wlanMaxConnectUsr value.
     * set wlanMaxConnectUsr value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
	
	int rc = MFD_ERROR;
	int ret = 0;
	char max_sta[10]= { 0 };
	memset(max_sta,0,sizeof(max_sta));
	snprintf(max_sta,sizeof(max_sta)-1,"%d",wlanMaxConnectUsr_val);

	ret=config_wlan_max_sta_num(rowreq_ctx->data.parameter,connection,rowreq_ctx->tbl_idx.wlanID,max_sta);
	if(ret == 1)
	{
		rowreq_ctx->data.wlanMaxConnectUsr = wlanMaxConnectUsr_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wlanMaxConnectUsr_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wlanMaxConnectUsr_undo( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanMaxConnectUsr_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wlanMaxConnectUsr undo.
     */
    /*
     * copy wlanMaxConnectUsr data
     * set rowreq_ctx->data.wlanMaxConnectUsr from rowreq_ctx->undo->wlanMaxConnectUsr
     */
    rowreq_ctx->data.wlanMaxConnectUsr = rowreq_ctx->undo->wlanMaxConnectUsr;

    
    return MFD_SUCCESS;
} /* wlanMaxConnectUsr_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWlanEntry.wlanLoadBalanceStatus
 * wlanLoadBalanceStatus is subid 6 of dot11ConfigWlanEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.2.1.6
 * Description:
Load balance status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  no(0), number(1), flow(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wlanLoadBalanceStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWlanTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  no(0), number(1), flow(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wlanLoadBalanceStatus_check_value( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, u_long wlanLoadBalanceStatus_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wlanLoadBalanceStatus value.
     */

    return MFD_SUCCESS; /* wlanLoadBalanceStatus value not illegal */
} /* wlanLoadBalanceStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWlanTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wlanLoadBalanceStatus_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wlanLoadBalanceStatus undo.
     */
    /*
     * copy wlanLoadBalanceStatus data
     * set rowreq_ctx->undo->wlanLoadBalanceStatus from rowreq_ctx->data.wlanLoadBalanceStatus
     */
    rowreq_ctx->undo->wlanLoadBalanceStatus = rowreq_ctx->data.wlanLoadBalanceStatus;


    return MFD_SUCCESS;
} /* wlanLoadBalanceStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wlanLoadBalanceStatus_val
 *        A long containing the new value.
 */
int
wlanLoadBalanceStatus_set( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, u_long wlanLoadBalanceStatus_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wlanLoadBalanceStatus reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wlanLoadBalanceStatus_val) {
        case WLANLOADBALANCESTATUS_NO:
             wlanLoadBalanceStatus_val = INTERNAL_WLANLOADBALANCESTATUS_NO;
             break;

        case WLANLOADBALANCESTATUS_NUMBER:
             wlanLoadBalanceStatus_val = INTERNAL_WLANLOADBALANCESTATUS_NUMBER;
             break;

        case WLANLOADBALANCESTATUS_FLOW:
             wlanLoadBalanceStatus_val = INTERNAL_WLANLOADBALANCESTATUS_FLOW;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wlanLoadBalanceStatus\n", wlanLoadBalanceStatus_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wlanLoadBalanceStatus value.
     * set wlanLoadBalanceStatus value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
	
	int rc = MFD_ERROR;
	int ret = 0;
	char state[10]= { 0 };
	memset(state,0,sizeof(state));
	if(wlanLoadBalanceStatus_val == WLANLOADBALANCESTATUS_NO)
		strncpy(state,"disable",sizeof(state)-1);
	else if(wlanLoadBalanceStatus_val == WLANLOADBALANCESTATUS_NUMBER)
		strncpy(state,"number",sizeof(state)-1);
	else if(wlanLoadBalanceStatus_val == WLANLOADBALANCESTATUS_FLOW)
		strncpy(state,"flow",sizeof(state)-1);	

	ret=config_wlan_load_balance(rowreq_ctx->data.parameter,connection,rowreq_ctx->tbl_idx.wlanID,state);
	if(ret == 1)
	{
		rowreq_ctx->data.wlanLoadBalanceStatus = wlanLoadBalanceStatus_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wlanLoadBalanceStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wlanLoadBalanceStatus_undo( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wlanLoadBalanceStatus undo.
     */
    /*
     * copy wlanLoadBalanceStatus data
     * set rowreq_ctx->data.wlanLoadBalanceStatus from rowreq_ctx->undo->wlanLoadBalanceStatus
     */
    rowreq_ctx->data.wlanLoadBalanceStatus = rowreq_ctx->undo->wlanLoadBalanceStatus;

    
    return MFD_SUCCESS;
} /* wlanLoadBalanceStatus_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWlanEntry.wlanLoadBalanceStatusBaseOnFlow
 * wlanLoadBalanceStatusBaseOnFlow is subid 7 of dot11ConfigWlanEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.2.1.7
 * Description:
Load balance status select.(Base on flow)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  disable(0), enable(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wlanLoadBalanceStatusBaseOnFlow_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWlanTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  disable(0), enable(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wlanLoadBalanceStatusBaseOnFlow_check_value( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, u_long wlanLoadBalanceStatusBaseOnFlow_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatusBaseOnFlow_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wlanLoadBalanceStatusBaseOnFlow value.
     */

    return MFD_SUCCESS; /* wlanLoadBalanceStatusBaseOnFlow value not illegal */
} /* wlanLoadBalanceStatusBaseOnFlow_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWlanTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wlanLoadBalanceStatusBaseOnFlow_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatusBaseOnFlow_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wlanLoadBalanceStatusBaseOnFlow undo.
     */
    /*
     * copy wlanLoadBalanceStatusBaseOnFlow data
     * set rowreq_ctx->undo->wlanLoadBalanceStatusBaseOnFlow from rowreq_ctx->data.wlanLoadBalanceStatusBaseOnFlow
     */
    rowreq_ctx->undo->wlanLoadBalanceStatusBaseOnFlow = rowreq_ctx->data.wlanLoadBalanceStatusBaseOnFlow;


    return MFD_SUCCESS;
} /* wlanLoadBalanceStatusBaseOnFlow_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wlanLoadBalanceStatusBaseOnFlow_val
 *        A long containing the new value.
 */
int
wlanLoadBalanceStatusBaseOnFlow_set( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, u_long wlanLoadBalanceStatusBaseOnFlow_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatusBaseOnFlow_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wlanLoadBalanceStatusBaseOnFlow reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wlanLoadBalanceStatusBaseOnFlow_val) {
        case WLANLOADBALANCESTATUSBASEONFLOW_DISABLE:
             wlanLoadBalanceStatusBaseOnFlow_val = INTERNAL_WLANLOADBALANCESTATUSBASEONFLOW_DISABLE;
             break;

        case WLANLOADBALANCESTATUSBASEONFLOW_ENABLE:
             wlanLoadBalanceStatusBaseOnFlow_val = INTERNAL_WLANLOADBALANCESTATUSBASEONFLOW_ENABLE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wlanLoadBalanceStatusBaseOnFlow\n", wlanLoadBalanceStatusBaseOnFlow_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wlanLoadBalanceStatusBaseOnFlow value.
     * set wlanLoadBalanceStatusBaseOnFlow value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
	
	int rc = MFD_ERROR;
	int ret = 0;
	char state[10]= { 0 };
	memset(state,0,sizeof(state));
	if(wlanLoadBalanceStatusBaseOnFlow_val == WLANLOADBALANCESTATUSBASEONFLOW_ENABLE)
		strncpy(state,"flow",sizeof(state)-1);
	else
		strncpy(state,"disable",sizeof(state)-1);

	ret=config_wlan_load_balance(rowreq_ctx->data.parameter,connection,rowreq_ctx->tbl_idx.wlanID,state);
	if(ret == 1)
	{
		rowreq_ctx->data.wlanLoadBalanceStatusBaseOnFlow = wlanLoadBalanceStatusBaseOnFlow_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wlanLoadBalanceStatusBaseOnFlow_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wlanLoadBalanceStatusBaseOnFlow_undo( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatusBaseOnFlow_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wlanLoadBalanceStatusBaseOnFlow undo.
     */
    /*
     * copy wlanLoadBalanceStatusBaseOnFlow data
     * set rowreq_ctx->data.wlanLoadBalanceStatusBaseOnFlow from rowreq_ctx->undo->wlanLoadBalanceStatusBaseOnFlow
     */
    rowreq_ctx->data.wlanLoadBalanceStatusBaseOnFlow = rowreq_ctx->undo->wlanLoadBalanceStatusBaseOnFlow;

    
    return MFD_SUCCESS;
} /* wlanLoadBalanceStatusBaseOnFlow_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWlanEntry.wlanLoadBalanceStatusBaseOnUsr
 * wlanLoadBalanceStatusBaseOnUsr is subid 8 of dot11ConfigWlanEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.2.1.8
 * Description:
Load balance status select.(Base on user)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  disable(0), enable(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wlanLoadBalanceStatusBaseOnUsr_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWlanTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  disable(0), enable(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wlanLoadBalanceStatusBaseOnUsr_check_value( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, u_long wlanLoadBalanceStatusBaseOnUsr_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatusBaseOnUsr_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wlanLoadBalanceStatusBaseOnUsr value.
     */

    return MFD_SUCCESS; /* wlanLoadBalanceStatusBaseOnUsr value not illegal */
} /* wlanLoadBalanceStatusBaseOnUsr_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWlanTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wlanLoadBalanceStatusBaseOnUsr_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatusBaseOnUsr_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wlanLoadBalanceStatusBaseOnUsr undo.
     */
    /*
     * copy wlanLoadBalanceStatusBaseOnUsr data
     * set rowreq_ctx->undo->wlanLoadBalanceStatusBaseOnUsr from rowreq_ctx->data.wlanLoadBalanceStatusBaseOnUsr
     */
    rowreq_ctx->undo->wlanLoadBalanceStatusBaseOnUsr = rowreq_ctx->data.wlanLoadBalanceStatusBaseOnUsr;


    return MFD_SUCCESS;
} /* wlanLoadBalanceStatusBaseOnUsr_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wlanLoadBalanceStatusBaseOnUsr_val
 *        A long containing the new value.
 */
int
wlanLoadBalanceStatusBaseOnUsr_set( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, u_long wlanLoadBalanceStatusBaseOnUsr_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatusBaseOnUsr_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wlanLoadBalanceStatusBaseOnUsr reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wlanLoadBalanceStatusBaseOnUsr_val) {
        case WLANLOADBALANCESTATUSBASEONUSR_DISABLE:
             wlanLoadBalanceStatusBaseOnUsr_val = INTERNAL_WLANLOADBALANCESTATUSBASEONUSR_DISABLE;
             break;

        case WLANLOADBALANCESTATUSBASEONUSR_ENABLE:
             wlanLoadBalanceStatusBaseOnUsr_val = INTERNAL_WLANLOADBALANCESTATUSBASEONUSR_ENABLE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wlanLoadBalanceStatusBaseOnUsr\n", wlanLoadBalanceStatusBaseOnUsr_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wlanLoadBalanceStatusBaseOnUsr value.
     * set wlanLoadBalanceStatusBaseOnUsr value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
	
	int rc = MFD_ERROR;
	int ret = 0;
	char state[10]= { 0 };
	memset(state,0,sizeof(state));
	if(wlanLoadBalanceStatusBaseOnUsr_val == WLANLOADBALANCESTATUSBASEONUSR_ENABLE)
		strncpy(state,"number",sizeof(state)-1);
	else
		strncpy(state,"disable",sizeof(state)-1);

	ret=config_wlan_load_balance(rowreq_ctx->data.parameter,connection,rowreq_ctx->tbl_idx.wlanID,state);
	if(ret == 1)
	{
		rowreq_ctx->data.wlanLoadBalanceStatusBaseOnUsr = wlanLoadBalanceStatusBaseOnUsr_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wlanLoadBalanceStatusBaseOnUsr_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wlanLoadBalanceStatusBaseOnUsr_undo( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceStatusBaseOnUsr_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wlanLoadBalanceStatusBaseOnUsr undo.
     */
    /*
     * copy wlanLoadBalanceStatusBaseOnUsr data
     * set rowreq_ctx->data.wlanLoadBalanceStatusBaseOnUsr from rowreq_ctx->undo->wlanLoadBalanceStatusBaseOnUsr
     */
    rowreq_ctx->data.wlanLoadBalanceStatusBaseOnUsr = rowreq_ctx->undo->wlanLoadBalanceStatusBaseOnUsr;

    
    return MFD_SUCCESS;
} /* wlanLoadBalanceStatusBaseOnUsr_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWlanEntry.wlanLoadBalanceTrafficDiffThreshhd
 * wlanLoadBalanceTrafficDiffThreshhd is subid 9 of dot11ConfigWlanEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.2.1.9
 * Description:
The threshold of flow difference.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wlanLoadBalanceTrafficDiffThreshhd_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWlanTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wlanLoadBalanceTrafficDiffThreshhd_check_value( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, long wlanLoadBalanceTrafficDiffThreshhd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceTrafficDiffThreshhd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wlanLoadBalanceTrafficDiffThreshhd value.
     */

    return MFD_SUCCESS; /* wlanLoadBalanceTrafficDiffThreshhd value not illegal */
} /* wlanLoadBalanceTrafficDiffThreshhd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWlanTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wlanLoadBalanceTrafficDiffThreshhd_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceTrafficDiffThreshhd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wlanLoadBalanceTrafficDiffThreshhd undo.
     */
    /*
     * copy wlanLoadBalanceTrafficDiffThreshhd data
     * set rowreq_ctx->undo->wlanLoadBalanceTrafficDiffThreshhd from rowreq_ctx->data.wlanLoadBalanceTrafficDiffThreshhd
     */
    rowreq_ctx->undo->wlanLoadBalanceTrafficDiffThreshhd = rowreq_ctx->data.wlanLoadBalanceTrafficDiffThreshhd;


    return MFD_SUCCESS;
} /* wlanLoadBalanceTrafficDiffThreshhd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wlanLoadBalanceTrafficDiffThreshhd_val
 *        A long containing the new value.
 */
int
wlanLoadBalanceTrafficDiffThreshhd_set( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, long wlanLoadBalanceTrafficDiffThreshhd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceTrafficDiffThreshhd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wlanLoadBalanceTrafficDiffThreshhd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wlanLoadBalanceTrafficDiffThreshhd value.
     * set wlanLoadBalanceTrafficDiffThreshhd value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
	
	int rc = MFD_ERROR;
	int ret = 0;
	char parameter[10] = { 0 };
	memset(parameter,0,sizeof(parameter));
	snprintf(parameter,sizeof(parameter)-1,"%d",wlanLoadBalanceTrafficDiffThreshhd_val);

	ret=config_wlan_flow_balance_parameter(rowreq_ctx->data.parameter,connection,rowreq_ctx->tbl_idx.wlanID,parameter);
	if(ret == 1)
	{
		rowreq_ctx->data.wlanLoadBalanceTrafficDiffThreshhd = wlanLoadBalanceTrafficDiffThreshhd_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wlanLoadBalanceTrafficDiffThreshhd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wlanLoadBalanceTrafficDiffThreshhd_undo( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceTrafficDiffThreshhd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wlanLoadBalanceTrafficDiffThreshhd undo.
     */
    /*
     * copy wlanLoadBalanceTrafficDiffThreshhd data
     * set rowreq_ctx->data.wlanLoadBalanceTrafficDiffThreshhd from rowreq_ctx->undo->wlanLoadBalanceTrafficDiffThreshhd
     */
    rowreq_ctx->data.wlanLoadBalanceTrafficDiffThreshhd = rowreq_ctx->undo->wlanLoadBalanceTrafficDiffThreshhd;

    
    return MFD_SUCCESS;
} /* wlanLoadBalanceTrafficDiffThreshhd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWlanEntry.wlanLoadBalanceUsersDiffThreshhd
 * wlanLoadBalanceUsersDiffThreshhd is subid 10 of dot11ConfigWlanEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.2.1.10
 * Description:
The threshold of user difference.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wlanLoadBalanceUsersDiffThreshhd_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWlanTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wlanLoadBalanceUsersDiffThreshhd_check_value( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, long wlanLoadBalanceUsersDiffThreshhd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceUsersDiffThreshhd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wlanLoadBalanceUsersDiffThreshhd value.
     */

    return MFD_SUCCESS; /* wlanLoadBalanceUsersDiffThreshhd value not illegal */
} /* wlanLoadBalanceUsersDiffThreshhd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWlanTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWlanTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wlanLoadBalanceUsersDiffThreshhd_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceUsersDiffThreshhd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wlanLoadBalanceUsersDiffThreshhd undo.
     */
    /*
     * copy wlanLoadBalanceUsersDiffThreshhd data
     * set rowreq_ctx->undo->wlanLoadBalanceUsersDiffThreshhd from rowreq_ctx->data.wlanLoadBalanceUsersDiffThreshhd
     */
    rowreq_ctx->undo->wlanLoadBalanceUsersDiffThreshhd = rowreq_ctx->data.wlanLoadBalanceUsersDiffThreshhd;


    return MFD_SUCCESS;
} /* wlanLoadBalanceUsersDiffThreshhd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wlanLoadBalanceUsersDiffThreshhd_val
 *        A long containing the new value.
 */
int
wlanLoadBalanceUsersDiffThreshhd_set( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, long wlanLoadBalanceUsersDiffThreshhd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceUsersDiffThreshhd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wlanLoadBalanceUsersDiffThreshhd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wlanLoadBalanceUsersDiffThreshhd value.
     * set wlanLoadBalanceUsersDiffThreshhd value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
	
	int rc = MFD_ERROR;
	int ret = 0;
	char parameter[10] = { 0 };
	memset(parameter,0,sizeof(parameter));
	snprintf(parameter,sizeof(parameter)-1,"%d",wlanLoadBalanceUsersDiffThreshhd_val);

	ret=config_wlan_number_balance_parameter(rowreq_ctx->data.parameter,connection,rowreq_ctx->tbl_idx.wlanID,parameter);
	if(ret == 1)
	{
		rowreq_ctx->data.wlanLoadBalanceUsersDiffThreshhd = wlanLoadBalanceUsersDiffThreshhd_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wlanLoadBalanceUsersDiffThreshhd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wlanLoadBalanceUsersDiffThreshhd_undo( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanLoadBalanceUsersDiffThreshhd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wlanLoadBalanceUsersDiffThreshhd undo.
     */
    /*
     * copy wlanLoadBalanceUsersDiffThreshhd data
     * set rowreq_ctx->data.wlanLoadBalanceUsersDiffThreshhd from rowreq_ctx->undo->wlanLoadBalanceUsersDiffThreshhd
     */
    rowreq_ctx->data.wlanLoadBalanceUsersDiffThreshhd = rowreq_ctx->undo->wlanLoadBalanceUsersDiffThreshhd;

    
    return MFD_SUCCESS;
} /* wlanLoadBalanceUsersDiffThreshhd_undo */


int
wlanInterfaceIPAddr_check_value( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, char *WlanInterfaceIPAddr_val_ptr,  size_t WlanInterfaceIPAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanInterfaceIPAddr_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != WlanInterfaceIPAddr_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wlanInterfaceIPAddr value.
     */

    return MFD_SUCCESS; /* wlanInterfaceIPAddr value not illegal */
} /* wlanInterfaceIPAddr_check_value */


int
wlanInterfaceIPAddr_undo_setup( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanInterfaceIPAddr_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wlanInterfaceIPAddr undo.
     */
    /*
     * copy wlanBindIf and WlanInterfaceIPAddr_len data
     * set rowreq_ctx->undo->wlanInterfaceIPAddr from rowreq_ctx->data.wlanInterfaceIPAddr
     */
    memcpy( rowreq_ctx->undo->wlanInterfaceIPAddr, rowreq_ctx->data.wlanInterfaceIPAddr,
            (rowreq_ctx->data.wlanInterfaceIPAddr_len * sizeof(rowreq_ctx->undo->wlanInterfaceIPAddr[0])));
    rowreq_ctx->undo->wlanInterfaceIPAddr_len = rowreq_ctx->data.wlanInterfaceIPAddr_len;


    return MFD_SUCCESS;
} /* wlanInterfaceIPAddr_undo_setup */

int
wlanInterfaceIPAddr_set( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx, char *wlanInterfaceIPAddr_val_ptr,  size_t wlanInterfaceIPAddr_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanInterfaceIPAddr_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wlanInterfaceIPAddr_val_ptr);

/*
 * TODO:245:o: |-> Implement wlanBindIf reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wlanInterfaceIPAddr value.
     * set wlanBindIf value in rowreq_ctx->data
     */    

    snmp_log(LOG_DEBUG, "wlanInterfaceIPAddr_set: wlanInterfaceIPAddr_val_ptr = %s, wlanInterfaceIPAddr_val_ptr_len = %d\n", 
                        wlanInterfaceIPAddr_val_ptr, wlanInterfaceIPAddr_val_ptr_len);
    
	int status = 0, ret = 0;

    char command[128] = { 0 };

    /*frist no interface wlan*/
    snprintf(command, sizeof(command) - 1, "no_inter_ins.sh %d %d %d wlan%d >/dev/null 2>&1",
                rowreq_ctx->data.parameter.slot_id, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id,
                rowreq_ctx->data.local_wlanID);
    
    snmp_log(LOG_DEBUG, "wlanInterfaceIPAddr_set: no interface wlan command = %s\n", command);                
                
	status = system(command); 	 
	ret = WEXITSTATUS(status);


    /*interface wlan*/
    if(wlanInterfaceIPAddr_val_ptr_len) {
        memset(command, 0, sizeof(command));
        snprintf(command, sizeof(command) - 1, "set_intf_ins.sh %d %d %d wlan%d %s >/dev/null 2>&1",
                    rowreq_ctx->data.parameter.slot_id, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id,
                    rowreq_ctx->data.local_wlanID, wlanInterfaceIPAddr_val_ptr);

        snmp_log(LOG_DEBUG, "wlanInterfaceIPAddr_set: interface wlan command = %s\n", command);        

    	status = system(command); 	 
    	ret = WEXITSTATUS(status);
    }

    
	if(0 == ret) {
	    memset(rowreq_ctx->data.wlanInterfaceIPAddr, 0, sizeof(rowreq_ctx->data.wlanInterfaceIPAddr));
		memcpy( rowreq_ctx->data.wlanInterfaceIPAddr, wlanInterfaceIPAddr_val_ptr, wlanInterfaceIPAddr_val_ptr_len );
		rowreq_ctx->data.wlanInterfaceIPAddr_len = wlanInterfaceIPAddr_val_ptr_len / sizeof(wlanInterfaceIPAddr_val_ptr[0]);
		return MFD_SUCCESS;
	}

    return MFD_ERROR;
} /* wlanBindIf_set */

int
wlanInterfaceIPAddr_undo( dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWlanTable:wlanInterfaceIPAddr_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wlanInterfaceIPAddr undo.
     */
    /*
     * copy wlanBindIf and wlanInterfaceIPAddr_len data
     * set rowreq_ctx->data.wlanInterfaceIPAddr from rowreq_ctx->undo->wlanInterfaceIPAddr
     */
    memcpy( rowreq_ctx->data.wlanInterfaceIPAddr, rowreq_ctx->undo->wlanInterfaceIPAddr,
            (rowreq_ctx->undo->wlanInterfaceIPAddr_len * sizeof(rowreq_ctx->data.wlanInterfaceIPAddr[0])));
    rowreq_ctx->data.wlanInterfaceIPAddr_len = rowreq_ctx->undo->wlanInterfaceIPAddr_len;

    
    return MFD_SUCCESS;
} /* wlanInterfaceIPAddr_undo */


/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigWlanTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigWlanTable if you don't have dependencies)
 */
int
dot11ConfigWlanTable_check_dependencies(dot11ConfigWlanTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigWlanTable:dot11ConfigWlanTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigWlanTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11ConfigWlanTable_check_dependencies */

/** @} */
