/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpWAPIPerformanceStatsTable.h"
#include "dot11WtpWAPIPerformanceStatsTable_data_access.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "../mibs_public.h"




/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpWAPIPerformanceStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpWAPIPerformanceStatsTable is subid 4 of wtpWAPI.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.4, length: 12
*/

/**
 * initialization for dot11WtpWAPIPerformanceStatsTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11WtpWAPIPerformanceStatsTable_reg
 *        Pointer to dot11WtpWAPIPerformanceStatsTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11WtpWAPIPerformanceStatsTable_init_data(dot11WtpWAPIPerformanceStatsTable_registration_ptr dot11WtpWAPIPerformanceStatsTable_reg)
{
    DEBUGMSGTL(("verbose:dot11WtpWAPIPerformanceStatsTable:dot11WtpWAPIPerformanceStatsTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11WtpWAPIPerformanceStatsTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11WtpWAPIPerformanceStatsTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11WtpWAPIPerformanceStatsTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11WtpWAPIPerformanceStatsTable:dot11WtpWAPIPerformanceStatsTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11WtpWAPIPerformanceStatsTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11WtpWAPIPerformanceStatsTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11WTPWAPIPERFORMANCESTATSTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11WtpWAPIPerformanceStatsTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11WtpWAPIPerformanceStatsTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11WtpWAPIPerformanceStatsTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11WtpWAPIPerformanceStatsTable_cache_load(netsnmp_container *container)
{
	
	snmp_log(LOG_DEBUG, "enter dot11WtpWAPIPerformanceStatsTable_cache_load\n");
	
    dot11WtpWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
//    FILE *filep;
//    char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * staMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
         */
        /** 128 - 1(entry) - 1(col) - 0(other indexes) = 112 */
   char   staMacAddr[20];
   size_t      staMacAddr_len;

    DEBUGMSGTL(("verbose:dot11WtpWAPIPerformanceStatsTable:dot11WtpWAPIPerformanceStatsTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
//    filep = fopen("/etc/dummy.conf", "r");
//    if(NULL ==  filep) {
//        return MFD_RESOURCE_UNAVAILABLE;
//    }

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11WtpWAPIPerformanceStatsTable container.
     * loop over your dot11WtpWAPIPerformanceStatsTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
	char wtpWapiSelectedUnicastCipher[20] = { 0 };
	int wtpWapiSelectedUnicastCipher_len=0;
	int wtpWapiControlledPortStatus = 2;
	
    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_all_wlan_wapi_performance_stats_information_cmd\n");
    messageHead = list_connection_call_dbus_method(show_all_wlan_wapi_performance_stats_information_cmd, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_all_wlan_wapi_performance_stats_information_cmd,messageHead=%p\n", messageHead);

	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    struct WtpWAPIPerformanceStatsInfo *head = messageNode->message;
		    if(head)
		    {
		        struct WtpWAPIPerformanceStatsInfo *tempNode = NULL;
    	        for(tempNode=head->WtpWAPIPerformanceStatsInfo_list; tempNode!=NULL; tempNode=tempNode->next)
            	{
            	    struct Sub_Sta_WtpWAPIPerformance *StaShowNode = NULL;
            		for(StaShowNode=tempNode->Sub_Sta_WtpWAPIPerformance_head; StaShowNode!=NULL; StaShowNode=StaShowNode->next)
            		{
            			
        				memset(staMacAddr, 0, sizeof(staMacAddr)); 
        		        if(StaShowNode->staMacAddr) {
            				snprintf(staMacAddr, sizeof(staMacAddr) - 1,"%02X:%02X:%02X:%02X:%02X:%02X",
            				        StaShowNode->staMacAddr[0],StaShowNode->staMacAddr[1],StaShowNode->staMacAddr[2],
                                    StaShowNode->staMacAddr[3],StaShowNode->staMacAddr[4],StaShowNode->staMacAddr[5]);
            		    }															

        					

        				memset(wtpWapiSelectedUnicastCipher,0,sizeof(wtpWapiSelectedUnicastCipher));
        				snprintf(wtpWapiSelectedUnicastCipher, sizeof(wtpWapiSelectedUnicastCipher) - 1,"%02X:%02X:%02X:%02X", 
        						StaShowNode->wtpWapiSelectedUnicastCipher[0],StaShowNode->wtpWapiSelectedUnicastCipher[1],
        						StaShowNode->wtpWapiSelectedUnicastCipher[2],StaShowNode->wtpWapiSelectedUnicastCipher[3]);
      
		
		
			
				/*
				***************************************************
				*** 			START EXAMPLE CODE				***
				***---------------------------------------------***/
				/*
				 * get a line (skip blank lines)
				 */
			//	  do {
			//		  if (!fgets(line, sizeof(line), filep)) {
			//			  /* we're done */
			//			  fclose(filep);
			//			  filep = NULL;
			//		  }
			//	  } while (filep && (line[0] == '\n'));
			
				/*
				 * check for end of data
				 */
			//	  if(NULL == filep)
			//		  break;
			
				/*
				 * parse line into variables
				 */
				/*
				***---------------------------------------------***
				*** 			 END  EXAMPLE CODE				***
				***************************************************/
			
					/*
					 * TODO:352:M: |   |-> set indexes in new dot11WtpWAPIPerformanceStatsTable rowreq context.
					 */
					rowreq_ctx = dot11WtpWAPIPerformanceStatsTable_allocate_rowreq_ctx();
					if (NULL == rowreq_ctx) {
						snmp_log(LOG_ERR, "memory allocation failed\n");
                        free_dbus_message_list(&messageHead, Free_show_all_wlan_wapi_performance_stats_information_cmd);
						return MFD_RESOURCE_UNAVAILABLE;
					}
		
					staMacAddr_len=strlen(staMacAddr);
		
					
					if(MFD_SUCCESS != dot11WtpWAPIPerformanceStatsTable_indexes_set(rowreq_ctx
										   , staMacAddr, staMacAddr_len
						   )) {
						snmp_log(LOG_ERR,"error setting index while loading "
								 "dot11WtpWAPIPerformanceStatsTable cache.\n");
						dot11WtpWAPIPerformanceStatsTable_release_rowreq_ctx(rowreq_ctx);
						continue;
					}
			
					/*
					 * TODO:352:r: |   |-> populate dot11WtpWAPIPerformanceStatsTable data context.
					 * Populate data context here. (optionally, delay until row prep)
					 */
				/*
				 * TRANSIENT or semi-TRANSIENT data:
				 * copy data or save any info needed to do it in row_prep.
				 */
				/*
				 * setup/save data for wtpWapiVersion
				 * wtpWapiVersion(2)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiVersion mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiVersion = StaShowNode->wtpWapiVersion;
				
				/*
				 * setup/save data for wtpWapiControlledPortStatus
				 * wtpWapiControlledPortStatus(3)/TruthValue/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiControlledPortStatus mapping.
				 * Map values between raw/native values and MIB values
				 *
				* enums usually need mapping.
				*/
//				if(MFD_SUCCESS !=
//				   wtpWapiControlledPortStatus_map(&rowreq_ctx->data.wtpWapiControlledPortStatus, StaShowNode->wtpWapiControlledPortStatus )) {
//					return MFD_ERROR;
//				}

				if(StaShowNode->wtpWapiControlledPortStatus == 0)
					wtpWapiControlledPortStatus = 2;
				else if(StaShowNode->wtpWapiControlledPortStatus == 1)
					wtpWapiControlledPortStatus = 1;
	
				rowreq_ctx->data.wtpWapiControlledPortStatus = wtpWapiControlledPortStatus;






				
				/*
				 * setup/save data for wtpWapiSelectedUnicastCipher
				 * wtpWapiSelectedUnicastCipher(4)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiSelectedUnicastCipher mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * if(MFD_SUCCESS !=
				 *	  wtpWapiSelectedUnicastCipher_map(&rowreq_ctx->data.wtpWapiSelectedUnicastCipher, &rowreq_ctx->data.wtpWapiSelectedUnicastCipher_len,
				 *				  wtpWapiSelectedUnicastCipher, wtpWapiSelectedUnicastCipher_len, 0)) {
				 *	  return MFD_ERROR;
				 * }
				 */
				/*
				 * make sure there is enough space for wtpWapiSelectedUnicastCipher data
				 */
				/*if ((NULL == rowreq_ctx->data.wtpWapiSelectedUnicastCipher) ||
					(rowreq_ctx->data.wtpWapiSelectedUnicastCipher_len < (wtpWapiSelectedUnicastCipher_len * sizeof(rowreq_ctx->data.wtpWapiSelectedUnicastCipher[0])))) {
					snmp_log(LOG_ERR,"not enough space for value\n");
					return MFD_ERROR;
				}*/
			
                wtpWapiSelectedUnicastCipher_len = MIN(strlen(wtpWapiSelectedUnicastCipher), sizeof(rowreq_ctx->data.wtpWapiSelectedUnicastCipher)-1);				
				rowreq_ctx->data.wtpWapiSelectedUnicastCipher_len = wtpWapiSelectedUnicastCipher_len * sizeof(rowreq_ctx->data.wtpWapiSelectedUnicastCipher[0]);
				memcpy( rowreq_ctx->data.wtpWapiSelectedUnicastCipher, wtpWapiSelectedUnicastCipher, rowreq_ctx->data.wtpWapiSelectedUnicastCipher_len );



				
				/*
				 * setup/save data for wtpWapiWPIReplayCounters
				 * wtpWapiWPIReplayCounters(5)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWPIReplayCounters mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWPIReplayCounters = StaShowNode->wtpWapiWPIReplayCounters;
				
				/*
				 * setup/save data for wtpWapiWPIDecryptableErrors
				 * wtpWapiWPIDecryptableErrors(6)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWPIDecryptableErrors mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWPIDecryptableErrors = StaShowNode->wtpWapiWPIDecryptableErrors;
				
				/*
				 * setup/save data for wtpWapiWPIMICErrors
				 * wtpWapiWPIMICErrors(7)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWPIMICErrors mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWPIMICErrors = StaShowNode->wtpWapiWPIMICErrors;
				
				/*
				 * setup/save data for wtpWapiWAISignatureErrors
				 * wtpWapiWAISignatureErrors(8)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWAISignatureErrors mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWAISignatureErrors = StaShowNode->wtpWapiWAISignatureErrors;
				
				/*
				 * setup/save data for wtpWapiWAIHMACErrors
				 * wtpWapiWAIHMACErrors(9)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWAIHMACErrors mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWAIHMACErrors = StaShowNode->wtpWapiWAIHMACErrors;
				
				/*
				 * setup/save data for wtpWapiWAIAuthResultFailures
				 * wtpWapiWAIAuthResultFailures(10)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWAIAuthResultFailures mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWAIAuthResultFailures = StaShowNode->wtpWapiWAIAuthResultFailures;
				
				/*
				 * setup/save data for wtpWapiWAIDiscardCounters
				 * wtpWapiWAIDiscardCounters(11)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWAIDiscardCounters mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWAIDiscardCounters = StaShowNode->wtpWapiWAIDiscardCounters;
				
				/*
				 * setup/save data for wtpWapiWAITimeoutCounters
				 * wtpWapiWAITimeoutCounters(12)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWAITimeoutCounters mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWAITimeoutCounters = StaShowNode->wtpWapiWAITimeoutCounters;
				
				/*
				 * setup/save data for wtpWapiWAIFormatErrors
				 * wtpWapiWAIFormatErrors(13)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWAIFormatErrors mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWAIFormatErrors = StaShowNode->wtpWapiWAIFormatErrors;
				
				/*
				 * setup/save data for wtpWapiWAICertificateHandshakeFailures
				 * wtpWapiWAICertificateHandshakeFailures(14)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWAICertificateHandshakeFailures mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWAICertificateHandshakeFailures = StaShowNode->wtpWapiWAICertificateHandshakeFailures;
				
				/*
				 * setup/save data for wtpWapiWAIUnicastHandshakeFailures
				 * wtpWapiWAIUnicastHandshakeFailures(15)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWAIUnicastHandshakeFailures mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWAIUnicastHandshakeFailures = StaShowNode->wtpWapiWAIUnicastHandshakeFailures;
				
				/*
				 * setup/save data for wtpWapiWAIMulticastHandshakeFailures
				 * wtpWapiWAIMulticastHandshakeFailures(16)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define wtpWapiWAIMulticastHandshakeFailures mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.wtpWapiWAIMulticastHandshakeFailures = StaShowNode->wtpWapiWAIMulticastHandshakeFailures;
				
					
					/*
					 * insert into table container
					 */
        				if(CONTAINER_INSERT(container, rowreq_ctx))
        				{
        					dot11WtpWAPIPerformanceStatsTable_release_rowreq_ctx(rowreq_ctx);
        				}
        				++count;		
        			}
        		}
            }
    	}
    	free_dbus_message_list(&messageHead, Free_show_all_wlan_wapi_performance_stats_information_cmd);
	}



	
     

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
//    if(NULL != filep)
//        fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11WtpWAPIPerformanceStatsTable:dot11WtpWAPIPerformanceStatsTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11WtpWAPIPerformanceStatsTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11WtpWAPIPerformanceStatsTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11WtpWAPIPerformanceStatsTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11WtpWAPIPerformanceStatsTable:dot11WtpWAPIPerformanceStatsTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11WtpWAPIPerformanceStatsTable cache.
     */
} /* dot11WtpWAPIPerformanceStatsTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11WtpWAPIPerformanceStatsTable_row_prep( dot11WtpWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWAPIPerformanceStatsTable:dot11WtpWAPIPerformanceStatsTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11WtpWAPIPerformanceStatsTable_row_prep */

/** @} */
