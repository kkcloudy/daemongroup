/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigPortalServerGroupTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11ConfigPortalServerGroupTable get routines.
 * TODO:240:M: Implement dot11ConfigPortalServerGroupTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigPortalServerGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupTable is subid 1 of dot11ConfigPortalServerGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11ConfigPortalServerGroupTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param PortalIndex_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11ConfigPortalServerGroupTable_indexes_set_tbl_idx(dot11ConfigPortalServerGroupTable_mib_index *tbl_idx, long PortalIndex_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:dot11ConfigPortalServerGroupTable_indexes_set_tbl_idx","called\n"));

    /* PortalIndex(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    tbl_idx->PortalIndex = PortalIndex_val;
    

    return MFD_SUCCESS;
} /* dot11ConfigPortalServerGroupTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11ConfigPortalServerGroupTable_indexes_set(dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, long PortalIndex_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:dot11ConfigPortalServerGroupTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11ConfigPortalServerGroupTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , PortalIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11ConfigPortalServerGroupTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11ConfigPortalServerGroupTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalKeyType
 * PortalKeyType is subid 7 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.7
 * Description:
Portal KeyType.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 5/8. Values:  essid(0), wlanid(1), vlanid(2), wtpid(3), interface(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the PortalKeyType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalKeyType_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
PortalKeyType_get( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, u_long * PortalKeyType_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != PortalKeyType_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalKeyType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PortalKeyType data.
 * copy (* PortalKeyType_val_ptr ) from rowreq_ctx->data
 */
    (* PortalKeyType_val_ptr ) = rowreq_ctx->data.PortalKeyType;

    return MFD_SUCCESS;
} /* PortalKeyType_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalKey
 * PortalKey is subid 1 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.1
 * Description:
Portal Key.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the PortalKey data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalKey_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param PortalKey_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by PortalKey.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*PortalKey_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update PortalKey_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
PortalKey_get( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, char **PortalKey_val_ptr_ptr, size_t *PortalKey_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != PortalKey_val_ptr_ptr) && (NULL != *PortalKey_val_ptr_ptr));
   netsnmp_assert( NULL != PortalKey_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalKey_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PortalKey data.
 * copy (* PortalKey_val_ptr_ptr ) data and (* PortalKey_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for PortalKey data
     */
    if ((NULL == (* PortalKey_val_ptr_ptr )) ||
        ((* PortalKey_val_ptr_len_ptr ) <
         (rowreq_ctx->data.PortalKey_len* sizeof(rowreq_ctx->data.PortalKey[0])))) {
        /*
         * allocate space for PortalKey data
         */
        (* PortalKey_val_ptr_ptr ) = malloc(rowreq_ctx->data.PortalKey_len* sizeof(rowreq_ctx->data.PortalKey[0]));
        if(NULL == (* PortalKey_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* PortalKey_val_ptr_len_ptr ) = rowreq_ctx->data.PortalKey_len* sizeof(rowreq_ctx->data.PortalKey[0]);
    memcpy( (* PortalKey_val_ptr_ptr ), rowreq_ctx->data.PortalKey, rowreq_ctx->data.PortalKey_len* sizeof(rowreq_ctx->data.PortalKey[0]) );

    return MFD_SUCCESS;
} /* PortalKey_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalURL
 * PortalURL is subid 2 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.2
 * Description:
Portal URL.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the PortalURL data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalURL_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param PortalURL_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by PortalURL.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*PortalURL_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update PortalURL_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
PortalURL_get( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, char **PortalURL_val_ptr_ptr, size_t *PortalURL_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != PortalURL_val_ptr_ptr) && (NULL != *PortalURL_val_ptr_ptr));
   netsnmp_assert( NULL != PortalURL_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalURL_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PortalURL data.
 * copy (* PortalURL_val_ptr_ptr ) data and (* PortalURL_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for PortalURL data
     */
    if ((NULL == (* PortalURL_val_ptr_ptr )) ||
        ((* PortalURL_val_ptr_len_ptr ) <
         (rowreq_ctx->data.PortalURL_len* sizeof(rowreq_ctx->data.PortalURL[0])))) {
        /*
         * allocate space for PortalURL data
         */
        (* PortalURL_val_ptr_ptr ) = malloc(rowreq_ctx->data.PortalURL_len* sizeof(rowreq_ctx->data.PortalURL[0]));
        if(NULL == (* PortalURL_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* PortalURL_val_ptr_len_ptr ) = rowreq_ctx->data.PortalURL_len* sizeof(rowreq_ctx->data.PortalURL[0]);
    memcpy( (* PortalURL_val_ptr_ptr ), rowreq_ctx->data.PortalURL, rowreq_ctx->data.PortalURL_len* sizeof(rowreq_ctx->data.PortalURL[0]) );

    return MFD_SUCCESS;
} /* PortalURL_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalPort
 * PortalPort is subid 3 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.3
 * Description:
Portal Port.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the PortalPort data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalPort_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
PortalPort_get( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, long * PortalPort_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != PortalPort_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalPort_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PortalPort data.
 * copy (* PortalPort_val_ptr ) from rowreq_ctx->data
 */
    (* PortalPort_val_ptr ) = rowreq_ctx->data.PortalPort;

    return MFD_SUCCESS;
} /* PortalPort_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalDomain
 * PortalDomain is subid 5 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.5
 * Description:
Portal Domain.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the PortalDomain data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalDomain_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param PortalDomain_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by PortalDomain.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*PortalDomain_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update PortalDomain_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
PortalDomain_get( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, char **PortalDomain_val_ptr_ptr, size_t *PortalDomain_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != PortalDomain_val_ptr_ptr) && (NULL != *PortalDomain_val_ptr_ptr));
   netsnmp_assert( NULL != PortalDomain_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalDomain_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PortalDomain data.
 * copy (* PortalDomain_val_ptr_ptr ) data and (* PortalDomain_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for PortalDomain data
     */
    if ((NULL == (* PortalDomain_val_ptr_ptr )) ||
        ((* PortalDomain_val_ptr_len_ptr ) <
         (rowreq_ctx->data.PortalDomain_len* sizeof(rowreq_ctx->data.PortalDomain[0])))) {
        /*
         * allocate space for PortalDomain data
         */
        (* PortalDomain_val_ptr_ptr ) = malloc(rowreq_ctx->data.PortalDomain_len* sizeof(rowreq_ctx->data.PortalDomain[0]));
        if(NULL == (* PortalDomain_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* PortalDomain_val_ptr_len_ptr ) = rowreq_ctx->data.PortalDomain_len* sizeof(rowreq_ctx->data.PortalDomain[0]);
    memcpy( (* PortalDomain_val_ptr_ptr ), rowreq_ctx->data.PortalDomain, rowreq_ctx->data.PortalDomain_len* sizeof(rowreq_ctx->data.PortalDomain[0]) );

    return MFD_SUCCESS;
} /* PortalDomain_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalRowStatus
 * PortalRowStatus is subid 6 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.6
 * Description:
This variable is used to create, modify, and/or delete a row
			        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the PortalRowStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalRowStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
PortalRowStatus_get( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, u_long * PortalRowStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != PortalRowStatus_val_ptr );

    (* PortalRowStatus_val_ptr ) = rowreq_ctx->data.PortalRowStatus;

    return MFD_SUCCESS;
} /* PortalRowStatus_get */



/** @} */
