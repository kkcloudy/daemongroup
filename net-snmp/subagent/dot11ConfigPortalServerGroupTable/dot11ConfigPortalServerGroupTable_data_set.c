/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigPortalServerGroupTable.h"
#include "ws_sysinfo.h"
#include "ws_init_dbus.h"
#include "mibs_public.h"
#include "ws_stp.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "nm/app/eag/eag_errcode.h"
#include "nm/app/eag/eag_conf.h"
#include "nm/app/eag/eag_interface.h"
#include "ws_dcli_wlans.h"
#include "ws_dcli_ac.h"
#include "autelanWtpGroup.h"
#include "ws_eag_conf.h"
#include "ws_portal_conf.h"

/*if eag is running, return 1*/
/*static int 
eag_ins_running_state(DBusConnection *connection, int hansitype, int insid)
{
	int ret = 0;
	struct eag_base_conf baseconf;		
	memset( &baseconf, 0, sizeof(baseconf));

	ret = eag_get_base_conf( connection, hansitype, insid, &baseconf );

	if( (EAG_RETURN_OK == ret) && (1 == baseconf.status) )
		ret = 1;
	else
		ret = 0;
	return ret;
}*/

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigPortalServerGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupTable is subid 1 of dot11ConfigPortalServerGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11ConfigPortalServerGroupTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11ConfigPortalServerGroupTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigPortalServerGroupTable_undo_setup( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:dot11ConfigPortalServerGroupTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigPortalServerGroupTable undo.
     * set up dot11ConfigPortalServerGroupTable undo information, in preparation for a set.
     * Undo storage is in (* PortalRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigPortalServerGroupTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigPortalServerGroupTable_undo( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:dot11ConfigPortalServerGroupTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11ConfigPortalServerGroupTable undo.
     * dot11ConfigPortalServerGroupTable undo information, in response to a failed set.
     * Undo storage is in (* PortalRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigPortalServerGroupTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigPortalServerGroupTable_undo_cleanup( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:dot11ConfigPortalServerGroupTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigPortalServerGroupTable undo.
     * Undo storage is in (* PortalRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigPortalServerGroupTable_undo_cleanup */

static int
check_create_ready(dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx) {
    if(strlen(rowreq_ctx->data.PortalKey) && strlen(rowreq_ctx->data.PortalURL) && (0 != rowreq_ctx->data.PortalPort) && strlen(rowreq_ctx->data.PortalDomain)) {
        return MFD_SUCCESS;
    }

    return MFD_ERROR;
}

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigPortalServerGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigPortalServerGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigPortalServerGroupTable_commit( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:dot11ConfigPortalServerGroupTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;
	
    if(RS_NOTREADY == rowreq_ctx->data.PortalRowStatus && MFD_SUCCESS == check_create_ready(rowreq_ctx)) {
        rowreq_ctx->data.PortalRowStatus = RS_NOTINSERVICE;
    }

    /*
     * commit dot11ConfigPortalServerGroupTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_PORTALKEYTYPE_FLAG) {
       save_flags &= ~COLUMN_PORTALKEYTYPE_FLAG; /* clear PortalKeyType */
       /*
        * TODO:482:o: |-> commit column PortalKeyType.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalServerGroupTable column PortalKeyType commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo PortalKeyType
             */
            rowreq_ctx->column_set_flags |= COLUMN_PORTALKEYTYPE_FLAG;
       }
    }

    if (save_flags & COLUMN_PORTALKEY_FLAG) {
       save_flags &= ~COLUMN_PORTALKEY_FLAG; /* clear PortalKey */
       /*
        * TODO:482:o: |-> commit column PortalKey.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalServerGroupTable column PortalKey commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo PortalKey
             */
            rowreq_ctx->column_set_flags |= COLUMN_PORTALKEY_FLAG;
       }
    }

    if (save_flags & COLUMN_PORTALURL_FLAG) {
       save_flags &= ~COLUMN_PORTALURL_FLAG; /* clear PortalURL */
       /*
        * TODO:482:o: |-> commit column PortalURL.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalServerGroupTable column PortalURL commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo PortalURL
             */
            rowreq_ctx->column_set_flags |= COLUMN_PORTALURL_FLAG;
       }
    }

    if (save_flags & COLUMN_PORTALPORT_FLAG) {
       save_flags &= ~COLUMN_PORTALPORT_FLAG; /* clear PortalPort */
       /*
        * TODO:482:o: |-> commit column PortalPort.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalServerGroupTable column PortalPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo PortalPort
             */
            rowreq_ctx->column_set_flags |= COLUMN_PORTALPORT_FLAG;
       }
    }

    if (save_flags & COLUMN_PORTALDOMAIN_FLAG) {
       save_flags &= ~COLUMN_PORTALDOMAIN_FLAG; /* clear PortalDomain */
       /*
        * TODO:482:o: |-> commit column PortalDomain.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalServerGroupTable column PortalDomain commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo PortalDomain
             */
            rowreq_ctx->column_set_flags |= COLUMN_PORTALDOMAIN_FLAG;
       }
    }

    if (save_flags & COLUMN_PORTALROWSTATUS_FLAG) {
        save_flags &= ~COLUMN_PORTALROWSTATUS_FLAG; /* clear PortalRowStatus */
        switch(rowreq_ctx->data.PortalRowStatus) {
            case RS_ACTIVE:
                {
					rc = -1;
					void *connection = NULL;
				    if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3)) 
					{
						int ret = EAG_ERR_UNKNOWN;
						PORTAL_KEY_TYPE key_type;
						unsigned long keyid = 0;
						char *keystr = "";
						char *portal_url = NULL; //rowreq_ctx->data.PortalURL;
						unsigned short ntf_port = (unsigned short)(rowreq_ctx->data.PortalPort);
						char *domain = NULL;
						
						portal_url = rowreq_ctx->data.PortalURL;
//						if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 1)
//						{
							domain = rowreq_ctx->data.PortalDomain;
							key_type = rowreq_ctx->data.PortalKeyType;
							switch(rowreq_ctx->data.PortalKeyType)
							{
								case 0:
									{
										keystr = rowreq_ctx->data.PortalKey;
									}
									break;
								case 1:
									{
										keyid = atoi(rowreq_ctx->data.PortalKey);
										if( keyid == 0 || keyid > 128 )
										{
											rc = -1;
											return MFD_ERROR;
											break;
										}									
									}
									break;
								case 2:
									{
										keyid = atoi(rowreq_ctx->data.PortalKey);
										if( keyid == 0 || keyid > 4096 )
										{
											rc = -1;
											return MFD_ERROR;
											break;
										}		
									}
									break;
								case 3:
									{
										keyid = atoi(rowreq_ctx->data.PortalKey);
									}
									break;
								case 4:
									{
										keystr = rowreq_ctx->data.PortalKey;
									}
									break;
								default:
									{
										rc = -1;
										return MFD_ERROR;
									}
									break;
							}
							
							if(rowreq_ctx->data.is_modify == 0)
							{
								ret = eag_add_portal_server( connection,
														  rowreq_ctx->data.parameter.local_id, 
														  rowreq_ctx->data.parameter.instance_id,					
														  key_type,
														  keyid,
														  keystr,
														  portal_url, 
														  ntf_port,
														  domain ,
														  0,0);
							}
							else
							{
								ret = eag_modify_portal_server( connection,
														  rowreq_ctx->data.parameter.local_id, 
														  rowreq_ctx->data.parameter.instance_id,					
														  key_type,
														  keyid,
														  keystr,
														  portal_url, 
														  ntf_port,
														  domain ,
														  0,0);
							}
							snmp_log(LOG_DEBUG, "dot11ConfigPortalServerGroupTable_commit: eag_add_portal_server: ret = %d\n", ret);

							if(ret == EAG_RETURN_OK)
							{
								 rc = MFD_SUCCESS;
								 break;
							}
							else if(EAG_ERR_DBUS_FAILED == ret) 
							{
								rc = -1;
								close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
							}
//						}
						else
							return MFD_ERROR;
				    }    					    
                }
//                rc = -1;
				rowreq_ctx->data.is_modify = 1;
                break;

            case RS_CREATEANDWAIT:
                rowreq_ctx->data.PortalRowStatus = RS_NOTREADY;
                rc = MFD_SUCCESS;
                break;
                
            case RS_DESTROY:  
                {
					void *connection = NULL;
                    if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
                    {
						int ret = -1;
						PORTAL_KEY_TYPE key_type;
						unsigned long keyid = 0;
						char *keystr = "";
						
						key_type = rowreq_ctx->data.PortalKeyType;
						switch(rowreq_ctx->data.PortalKeyType)
						{
							case 0:
								{
									keystr = rowreq_ctx->data.PortalKey;
								}
								break;
							case 1:
								{
									keyid = atoi(rowreq_ctx->data.PortalKey);
									if( keyid == 0 || keyid > 128 )
									{
										rc = -1;
										return MFD_ERROR;
										break;
									}
								}
								break;
							case 2:
								{
									keyid = atoi(rowreq_ctx->data.PortalKey);
									if( keyid == 0 || keyid > 4096 )
									{
										rc = -1;
										return MFD_ERROR;
										break;
									}		
								}
								break;
							case 3:
								{ 
									keyid = atoi(rowreq_ctx->data.PortalKey);
								}
								break;
							case 4:
								{
									keystr = rowreq_ctx->data.PortalKey;
								}
								break;
							default:
								{
									rc = -1;
									return MFD_ERROR;
								}
								break;
						}
						
//						if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 1)
//						{

							ret = eag_del_portal_server( connection,
													  rowreq_ctx->data.parameter.local_id, 
													  rowreq_ctx->data.parameter.instance_id,				
													  key_type,
													  keyid,
													  keystr);

							snmp_log(LOG_DEBUG, "dot11ConfigPortalServerGroupTable_commit: eag_del_portal_server: ret = %d\n", ret);
							if(ret == EAG_RETURN_OK)
							{
								rc = MFD_SUCCESS;
	                            break;
							}
							else if(EAG_ERR_DBUS_FAILED == ret) 
							{
								rc = -1;
	                            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	                        }
//						}	
                    }
                }
                rowreq_ctx->rowreq_flags &= ~MFD_ROW_DELETED;
//                rc = -1;
                break;
                
            default:
                break;
        }
       /*
        * TODO:482:o: |-> commit column PortalRowStatus.
        */
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalServerGroupTable column PortalRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo PortalRowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_PORTALROWSTATUS_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigPortalServerGroupTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigPortalServerGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigPortalServerGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigPortalServerGroupTable_undo_commit( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:dot11ConfigPortalServerGroupTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigPortalServerGroupTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11ConfigPortalServerGroupTable_undo_commit */

/*
 * TODO:440:M: Implement dot11ConfigPortalServerGroupTable node value checks.
 * TODO:450:M: Implement dot11ConfigPortalServerGroupTable undo functions.
 * TODO:460:M: Implement dot11ConfigPortalServerGroupTable set functions.
 * TODO:480:M: Implement dot11ConfigPortalServerGroupTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalKeyType
 * PortalKeyType is subid 7 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.7
 * Description:
Portal KeyType.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 5/8. Values:  essid(0), wlanid(1), vlanid(2), wtpid(3), interface(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalKeyType_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  essid(0), wlanid(1), vlanid(2), wtpid(3), interface(4)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
PortalKeyType_check_value( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, u_long PortalKeyType_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalKeyType_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid PortalKeyType value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.PortalRowStatus || RS_NOTREADY == rowreq_ctx->data.PortalRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* PortalKeyType value not illegal */
} /* PortalKeyType_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
PortalKeyType_undo_setup( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalKeyType_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup PortalKeyType undo.
     */
    /*
     * copy PortalKeyType data
     * set rowreq_ctx->undo->PortalKeyType from rowreq_ctx->data.PortalKeyType
     */
    rowreq_ctx->undo->PortalKeyType = rowreq_ctx->data.PortalKeyType;


    return MFD_SUCCESS;
} /* PortalKeyType_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param PortalKeyType_val
 *        A long containing the new value.
 */
int
PortalKeyType_set( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, u_long PortalKeyType_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalKeyType_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set PortalKeyType value.
     * set PortalKeyType value in rowreq_ctx->data
     */
	if((rowreq_ctx->data.PortalRowStatus  == RS_NOTINSERVICE)&&(rowreq_ctx->data.is_modify==1))
	{
		return MFD_ERROR;
	}
    rowreq_ctx->data.PortalKeyType = PortalKeyType_val;

    return MFD_SUCCESS;
} /* PortalKeyType_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
PortalKeyType_undo( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalKeyType_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up PortalKeyType undo.
     */
    /*
     * copy PortalKeyType data
     * set rowreq_ctx->data.PortalKeyType from rowreq_ctx->undo->PortalKeyType
     */
    rowreq_ctx->data.PortalKeyType = rowreq_ctx->undo->PortalKeyType;

    
    return MFD_SUCCESS;
} /* PortalKeyType_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalKey
 * PortalKey is subid 1 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.1
 * Description:
Portal Key.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalKey_val_ptr
 *        A char containing the new value.
 * @param PortalKey_val_ptr_len
 *        The size (in bytes) of the data pointed to by PortalKey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.PortalKey).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
PortalKey_check_value( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, char *PortalKey_val_ptr,  size_t PortalKey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalKey_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != PortalKey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid PortalKey value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.PortalRowStatus || RS_NOTREADY == rowreq_ctx->data.PortalRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* PortalKey value not illegal */
} /* PortalKey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
PortalKey_undo_setup( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalKey_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup PortalKey undo.
     */
    /*
     * copy PortalKey and PortalKey_len data
     * set rowreq_ctx->undo->PortalKey from rowreq_ctx->data.PortalKey
     */
    memcpy( rowreq_ctx->undo->PortalKey, rowreq_ctx->data.PortalKey,
            (rowreq_ctx->data.PortalKey_len * sizeof(rowreq_ctx->undo->PortalKey[0])));
    rowreq_ctx->undo->PortalKey_len = rowreq_ctx->data.PortalKey_len;


    return MFD_SUCCESS;
} /* PortalKey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param PortalKey_val_ptr
 *        A char containing the new value.
 * @param PortalKey_val_ptr_len
 *        The size (in bytes) of the data pointed to by PortalKey_val_ptr
 */
int
PortalKey_set( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, char *PortalKey_val_ptr,  size_t PortalKey_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalKey_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != PortalKey_val_ptr);

    /*
     * TODO:461:M: |-> Set PortalKey value.
     * set PortalKey value in rowreq_ctx->data
     */
//     memset(rowreq_ctx->data.PortalKey,0,sizeof(rowreq_ctx->data.PortalKey));
	if(rowreq_ctx->data.PortalRowStatus  == RS_NOTINSERVICE)
	{
		return MFD_ERROR;
	}
    memcpy( rowreq_ctx->data.PortalKey, PortalKey_val_ptr, PortalKey_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.PortalKey_len = PortalKey_val_ptr_len / sizeof(PortalKey_val_ptr[0]);

    return MFD_SUCCESS;
} /* PortalKey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
PortalKey_undo( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalKey_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up PortalKey undo.
     */
    /*
     * copy PortalKey and PortalKey_len data
     * set rowreq_ctx->data.PortalKey from rowreq_ctx->undo->PortalKey
     */
    memcpy( rowreq_ctx->data.PortalKey, rowreq_ctx->undo->PortalKey,
            (rowreq_ctx->undo->PortalKey_len * sizeof(rowreq_ctx->data.PortalKey[0])));
    rowreq_ctx->data.PortalKey_len = rowreq_ctx->undo->PortalKey_len;

    
    return MFD_SUCCESS;
} /* PortalKey_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalURL
 * PortalURL is subid 2 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.2
 * Description:
Portal URL.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalURL_val_ptr
 *        A char containing the new value.
 * @param PortalURL_val_ptr_len
 *        The size (in bytes) of the data pointed to by PortalURL_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.PortalURL).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
PortalURL_check_value( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, char *PortalURL_val_ptr,  size_t PortalURL_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalURL_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != PortalURL_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid PortalURL value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.PortalRowStatus || RS_NOTREADY == rowreq_ctx->data.PortalRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* PortalURL value not illegal */
} /* PortalURL_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
PortalURL_undo_setup( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalURL_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup PortalURL undo.
     */
    /*
     * copy PortalURL and PortalURL_len data
     * set rowreq_ctx->undo->PortalURL from rowreq_ctx->data.PortalURL
     */
    memcpy( rowreq_ctx->undo->PortalURL, rowreq_ctx->data.PortalURL,
            (rowreq_ctx->data.PortalURL_len * sizeof(rowreq_ctx->undo->PortalURL[0])));
    rowreq_ctx->undo->PortalURL_len = rowreq_ctx->data.PortalURL_len;


    return MFD_SUCCESS;
} /* PortalURL_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param PortalURL_val_ptr
 *        A char containing the new value.
 * @param PortalURL_val_ptr_len
 *        The size (in bytes) of the data pointed to by PortalURL_val_ptr
 */
int
PortalURL_set( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, char *PortalURL_val_ptr,  size_t PortalURL_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalURL_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != PortalURL_val_ptr);

    /*
     * TODO:461:M: |-> Set PortalURL value.
     * set PortalURL value in rowreq_ctx->data
     */
	memset(rowreq_ctx->data.PortalURL,0,sizeof(rowreq_ctx->data.PortalURL));
    memcpy( rowreq_ctx->data.PortalURL, PortalURL_val_ptr, PortalURL_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.PortalURL_len = PortalURL_val_ptr_len / sizeof(PortalURL_val_ptr[0]);

    return MFD_SUCCESS;
} /* PortalURL_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
PortalURL_undo( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalURL_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up PortalURL undo.
     */
    /*
     * copy PortalURL and PortalURL_len data
     * set rowreq_ctx->data.PortalURL from rowreq_ctx->undo->PortalURL
     */
    memcpy( rowreq_ctx->data.PortalURL, rowreq_ctx->undo->PortalURL,
            (rowreq_ctx->undo->PortalURL_len * sizeof(rowreq_ctx->data.PortalURL[0])));
    rowreq_ctx->data.PortalURL_len = rowreq_ctx->undo->PortalURL_len;

    
    return MFD_SUCCESS;
} /* PortalURL_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalPort
 * PortalPort is subid 3 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.3
 * Description:
Portal Port.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
PortalPort_check_value( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, long PortalPort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid PortalPort value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.PortalRowStatus || RS_NOTREADY == rowreq_ctx->data.PortalRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* PortalPort value not illegal */
} /* PortalPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
PortalPort_undo_setup( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup PortalPort undo.
     */
    /*
     * copy PortalPort data
     * set rowreq_ctx->undo->PortalPort from rowreq_ctx->data.PortalPort
     */
    rowreq_ctx->undo->PortalPort = rowreq_ctx->data.PortalPort;


    return MFD_SUCCESS;
} /* PortalPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param PortalPort_val
 *        A long containing the new value.
 */
int
PortalPort_set( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, long PortalPort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set PortalPort value.
     * set PortalPort value in rowreq_ctx->data
     */
    rowreq_ctx->data.PortalPort = PortalPort_val;

    return MFD_SUCCESS;
} /* PortalPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
PortalPort_undo( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up PortalPort undo.
     */
    /*
     * copy PortalPort data
     * set rowreq_ctx->data.PortalPort from rowreq_ctx->undo->PortalPort
     */
    rowreq_ctx->data.PortalPort = rowreq_ctx->undo->PortalPort;

    
    return MFD_SUCCESS;
} /* PortalPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalDomain
 * PortalDomain is subid 5 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.5
 * Description:
Portal Domain.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalDomain_val_ptr
 *        A char containing the new value.
 * @param PortalDomain_val_ptr_len
 *        The size (in bytes) of the data pointed to by PortalDomain_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.PortalDomain).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
PortalDomain_check_value( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, char *PortalDomain_val_ptr,  size_t PortalDomain_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalDomain_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != PortalDomain_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid PortalDomain value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.PortalRowStatus || RS_NOTREADY == rowreq_ctx->data.PortalRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* PortalDomain value not illegal */
} /* PortalDomain_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
PortalDomain_undo_setup( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalDomain_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup PortalDomain undo.
     */
    /*
     * copy PortalDomain and PortalDomain_len data
     * set rowreq_ctx->undo->PortalDomain from rowreq_ctx->data.PortalDomain
     */
    memcpy( rowreq_ctx->undo->PortalDomain, rowreq_ctx->data.PortalDomain,
            (rowreq_ctx->data.PortalDomain_len * sizeof(rowreq_ctx->undo->PortalDomain[0])));
    rowreq_ctx->undo->PortalDomain_len = rowreq_ctx->data.PortalDomain_len;


    return MFD_SUCCESS;
} /* PortalDomain_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param PortalDomain_val_ptr
 *        A char containing the new value.
 * @param PortalDomain_val_ptr_len
 *        The size (in bytes) of the data pointed to by PortalDomain_val_ptr
 */
int
PortalDomain_set( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, char *PortalDomain_val_ptr,  size_t PortalDomain_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalDomain_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != PortalDomain_val_ptr);

    /*
     * TODO:461:M: |-> Set PortalDomain value.
     * set PortalDomain value in rowreq_ctx->data
     */
	memset(rowreq_ctx->data.PortalDomain,0,sizeof(rowreq_ctx->data.PortalDomain));
    memcpy( rowreq_ctx->data.PortalDomain, PortalDomain_val_ptr, PortalDomain_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.PortalDomain_len = PortalDomain_val_ptr_len / sizeof(PortalDomain_val_ptr[0]);

    return MFD_SUCCESS;
} /* PortalDomain_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
PortalDomain_undo( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalDomain_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up PortalDomain undo.
     */
    /*
     * copy PortalDomain and PortalDomain_len data
     * set rowreq_ctx->data.PortalDomain from rowreq_ctx->undo->PortalDomain
     */
    memcpy( rowreq_ctx->data.PortalDomain, rowreq_ctx->undo->PortalDomain,
            (rowreq_ctx->undo->PortalDomain_len * sizeof(rowreq_ctx->data.PortalDomain[0])));
    rowreq_ctx->data.PortalDomain_len = rowreq_ctx->undo->PortalDomain_len;

    
    return MFD_SUCCESS;
} /* PortalDomain_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalServerGroupEntry.PortalRowStatus
 * PortalRowStatus is subid 6 of dot11ConfigPortalServerGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.1.1.6
 * Description:
This variable is used to create, modify, and/or delete a row
			        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalServerGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
PortalRowStatus_check_value( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, u_long PortalRowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid PortalRowStatus value.
     */

    return MFD_SUCCESS; /* PortalRowStatus value not illegal */
} /* PortalRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalServerGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalServerGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
PortalRowStatus_undo_setup( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup PortalRowStatus undo.
     */
    /*
     * copy PortalRowStatus data
     * set rowreq_ctx->undo->PortalRowStatus from rowreq_ctx->data.PortalRowStatus
     */
    rowreq_ctx->undo->PortalRowStatus = rowreq_ctx->data.PortalRowStatus;


    return MFD_SUCCESS;
} /* PortalRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param PortalRowStatus_val
 *        A long containing the new value.
 */
int
PortalRowStatus_set( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx, u_long PortalRowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set PortalRowStatus value.
     * set PortalRowStatus value in rowreq_ctx->data
     */
    
    int ret = MFD_SUCCESS;
    
    switch(PortalRowStatus_val) {
    
		case RS_NOTINSERVICE:
        case RS_ACTIVE:         
        case RS_DESTROY:
            rowreq_ctx->data.PortalRowStatus = PortalRowStatus_val;
            ret = MFD_SUCCESS;
            break;
    
//        case RS_NOTINSERVICE:
        case RS_NOTREADY:
        case RS_CREATEANDGO:
            ret = MFD_ERROR;
            break;
    
        case RS_CREATEANDWAIT:
            if(RS_NONEXISTENT == rowreq_ctx->data.PortalRowStatus) {
                global_to_local_ID(&(rowreq_ctx->data.parameter), 
                                    rowreq_ctx->tbl_idx.PortalIndex, 
                                    WIRELESS_MAX_NUM);
                rowreq_ctx->data.PortalRowStatus = PortalRowStatus_val;
                ret = MFD_SUCCESS;
            }
            else {
                ret = MFD_ERROR;
            }
            break;
    
        default:
            snmp_log(LOG_WARNING, "unknow %s in PortalRowStatus_set", PortalRowStatus_val);
            ret = MFD_ERROR;
            break;
    }

    return ret;
    
} /* PortalRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
PortalRowStatus_undo( dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalServerGroupTable:PortalRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up PortalRowStatus undo.
     */
    /*
     * copy PortalRowStatus data
     * set rowreq_ctx->data.PortalRowStatus from rowreq_ctx->undo->PortalRowStatus
     */
    rowreq_ctx->data.PortalRowStatus = rowreq_ctx->undo->PortalRowStatus;

    
    return MFD_SUCCESS;
} /* PortalRowStatus_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigPortalServerGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigPortalServerGroupTable if you don't have dependencies)
 */
int
dot11ConfigPortalServerGroupTable_check_dependencies(dot11ConfigPortalServerGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigPortalServerGroupTable:dot11ConfigPortalServerGroupTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigPortalServerGroupTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & COLUMN_PORTALROWSTATUS_FLAG) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->PortalRowStatus,
                                         rowreq_ctx->data.PortalRowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_DESTROY ==  rowreq_ctx->data.PortalRowStatus) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;
            }
            else if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.PortalRowStatus) {
                if ((rowreq_ctx->column_set_flags & DOT11CONFIGPORTALSERVERGROUPTABLE_REQUIRED_COLS)
                    != DOT11CONFIGPORTALSERVERGROUPTABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11ConfigPortalServerGroupTable",
                                "required columns missing (0x%0x != 0x%0x)\n",
                                rowreq_ctx->column_set_flags, DOT11CONFIGPORTALSERVERGROUPTABLE_REQUIRED_COLS));
                    return MFD_CANNOT_CREATE_NOW;
                }
                rowreq_ctx->data.PortalRowStatus = ROWSTATUS_ACTIVE;
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.PortalRowStatus) {
                if (rowreq_ctx->column_set_flags & ~COLUMN_PORTALROWSTATUS_FLAG) {
                    DEBUGMSGTL(("dot11ConfigPortalServerGroupTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;

            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11ConfigPortalServerGroupTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11ConfigPortalServerGroupTable_check_dependencies */

/** @} */
