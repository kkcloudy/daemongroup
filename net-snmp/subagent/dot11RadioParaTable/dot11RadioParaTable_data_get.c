/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11RadioParaTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11RadioParaTable get routines.
 * TODO:240:M: Implement dot11RadioParaTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11RadioParaTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11RadioParaTable is subid 2 of wtpRadio.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11RadioParaTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11RadioParaTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfIndex
 * wtpWirelessIfIndex is subid 1 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.1
 * Description:
Wireless interface index.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessIfIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessIfIndex_dot11RadioParaTable_map(long *mib_wtpWirelessIfIndex_val_ptr, long raw_wtpWirelessIfIndex_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessIfIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpWirelessIfIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessIfIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessIfIndex_val_ptr) = raw_wtpWirelessIfIndex_val;

    return MFD_SUCCESS;
} /* wtpWirelessIfIndex_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11RadioParaTable_indexes_set_tbl_idx(dot11RadioParaTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpWirelessIfIndex_val)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:dot11RadioParaTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wtpWirelessIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wtpWirelessIfIndex = wtpWirelessIfIndex_val;
    

    return MFD_SUCCESS;
} /* dot11RadioParaTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11RadioParaTable_indexes_set(dot11RadioParaTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpWirelessIfIndex_val)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:dot11RadioParaTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11RadioParaTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpWirelessIfIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11RadioParaTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11RadioParaTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpAntAPlus
 * wtpAntAPlus is subid 1 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.1
 * Description:
Antenna plus(802.11a).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpAntAPlus.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpAntAPlus_map(long *mib_wtpAntAPlus_val_ptr, long raw_wtpAntAPlus_val)
{
    netsnmp_assert(NULL != mib_wtpAntAPlus_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpAntAPlus_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpAntAPlus mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpAntAPlus_val_ptr) = raw_wtpAntAPlus_val;

    return MFD_SUCCESS;
} /* wtpAntAPlus_map */

/**
 * Extract the current value of the wtpAntAPlus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAntAPlus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpAntAPlus_get( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long * wtpAntAPlus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAntAPlus_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpAntAPlus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpAntAPlus data.
 * set (* wtpAntAPlus_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAntAPlus_val_ptr ) = rowreq_ctx->data.wtpAntAPlus;

    return MFD_SUCCESS;
} /* wtpAntAPlus_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpAntBGPlus
 * wtpAntBGPlus is subid 2 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.2
 * Description:
Antenna plus(802.11b/g).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpAntBGPlus.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpAntBGPlus_map(long *mib_wtpAntBGPlus_val_ptr, long raw_wtpAntBGPlus_val)
{
    netsnmp_assert(NULL != mib_wtpAntBGPlus_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpAntBGPlus_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpAntBGPlus mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpAntBGPlus_val_ptr) = raw_wtpAntBGPlus_val;

    return MFD_SUCCESS;
} /* wtpAntBGPlus_map */

/**
 * Extract the current value of the wtpAntBGPlus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAntBGPlus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpAntBGPlus_get( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long * wtpAntBGPlus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAntBGPlus_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpAntBGPlus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpAntBGPlus data.
 * set (* wtpAntBGPlus_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAntBGPlus_val_ptr ) = rowreq_ctx->data.wtpAntBGPlus;

    return MFD_SUCCESS;
} /* wtpAntBGPlus_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpSignalPathMode
 * wtpSignalPathMode is subid 3 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.3
 * Description:
Signal path mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSignalPathMode.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSignalPathMode_map(char **mib_wtpSignalPathMode_val_ptr_ptr, size_t *mib_wtpSignalPathMode_val_ptr_len_ptr, char *raw_wtpSignalPathMode_val_ptr, size_t raw_wtpSignalPathMode_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpSignalPathMode_val_ptr);
    netsnmp_assert((NULL != mib_wtpSignalPathMode_val_ptr_ptr) && (NULL != mib_wtpSignalPathMode_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalPathMode_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpSignalPathMode non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpSignalPathMode_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpSignalPathMode_val_ptr_ptr) || (*mib_wtpSignalPathMode_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpSignalPathMode_val_ptr_ptr = realloc( *mib_wtpSignalPathMode_val_ptr_ptr, converted_len * sizeof(**mib_wtpSignalPathMode_val_ptr_ptr));
        if(NULL == *mib_wtpSignalPathMode_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpSignalPathMode_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpSignalPathMode_val_ptr_ptr, raw_wtpSignalPathMode_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpSignalPathMode_map */

/**
 * Extract the current value of the wtpSignalPathMode data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSignalPathMode_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpSignalPathMode_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpSignalPathMode.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpSignalPathMode_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpSignalPathMode_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpSignalPathMode_get( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, char **wtpSignalPathMode_val_ptr_ptr, size_t *wtpSignalPathMode_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpSignalPathMode_val_ptr_ptr) && (NULL != *wtpSignalPathMode_val_ptr_ptr));
   netsnmp_assert( NULL != wtpSignalPathMode_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalPathMode_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSignalPathMode data.
 * set (* wtpSignalPathMode_val_ptr_ptr ) and (* wtpSignalPathMode_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpSignalPathMode data
     */
    if ((NULL == (* wtpSignalPathMode_val_ptr_ptr )) ||
        ((* wtpSignalPathMode_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpSignalPathMode_len * sizeof((* wtpSignalPathMode_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpSignalPathMode data
         */
        (* wtpSignalPathMode_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpSignalPathMode_len * sizeof((* wtpSignalPathMode_val_ptr_ptr )[0]));
        if(NULL == (* wtpSignalPathMode_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpSignalPathMode_val_ptr_len_ptr ) = rowreq_ctx->data.wtpSignalPathMode_len * sizeof((* wtpSignalPathMode_val_ptr_ptr )[0]);
    memcpy( (* wtpSignalPathMode_val_ptr_ptr ), rowreq_ctx->data.wtpSignalPathMode, (* wtpSignalPathMode_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpSignalPathMode_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpSignalSNR
 * wtpSignalSNR is subid 4 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.4
 * Description:
Signal SNR.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSignalSNR.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSignalSNR_map(char **mib_wtpSignalSNR_val_ptr_ptr, size_t *mib_wtpSignalSNR_val_ptr_len_ptr, char *raw_wtpSignalSNR_val_ptr, size_t raw_wtpSignalSNR_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpSignalSNR_val_ptr);
    netsnmp_assert((NULL != mib_wtpSignalSNR_val_ptr_ptr) && (NULL != mib_wtpSignalSNR_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalSNR_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpSignalSNR non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpSignalSNR_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpSignalSNR_val_ptr_ptr) || (*mib_wtpSignalSNR_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpSignalSNR_val_ptr_ptr = realloc( *mib_wtpSignalSNR_val_ptr_ptr, converted_len * sizeof(**mib_wtpSignalSNR_val_ptr_ptr));
        if(NULL == *mib_wtpSignalSNR_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpSignalSNR_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpSignalSNR_val_ptr_ptr, raw_wtpSignalSNR_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpSignalSNR_map */

/**
 * Extract the current value of the wtpSignalSNR data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSignalSNR_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpSignalSNR_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpSignalSNR.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpSignalSNR_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpSignalSNR_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpSignalSNR_get( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, char **wtpSignalSNR_val_ptr_ptr, size_t *wtpSignalSNR_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpSignalSNR_val_ptr_ptr) && (NULL != *wtpSignalSNR_val_ptr_ptr));
   netsnmp_assert( NULL != wtpSignalSNR_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalSNR_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSignalSNR data.
 * set (* wtpSignalSNR_val_ptr_ptr ) and (* wtpSignalSNR_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpSignalSNR data
     */
    if ((NULL == (* wtpSignalSNR_val_ptr_ptr )) ||
        ((* wtpSignalSNR_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpSignalSNR_len * sizeof((* wtpSignalSNR_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpSignalSNR data
         */
        (* wtpSignalSNR_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpSignalSNR_len * sizeof((* wtpSignalSNR_val_ptr_ptr )[0]));
        if(NULL == (* wtpSignalSNR_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpSignalSNR_val_ptr_len_ptr ) = rowreq_ctx->data.wtpSignalSNR_len * sizeof((* wtpSignalSNR_val_ptr_ptr )[0]);
    memcpy( (* wtpSignalSNR_val_ptr_ptr ), rowreq_ctx->data.wtpSignalSNR, (* wtpSignalSNR_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpSignalSNR_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpFrequencyHopTimes
 * wtpFrequencyHopTimes is subid 5 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.5
 * Description:
The times that change signal path because of shuning Frequency interference.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpFrequencyHopTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpFrequencyHopTimes_map(long *mib_wtpFrequencyHopTimes_val_ptr, long raw_wtpFrequencyHopTimes_val)
{
    netsnmp_assert(NULL != mib_wtpFrequencyHopTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpFrequencyHopTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpFrequencyHopTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpFrequencyHopTimes_val_ptr) = raw_wtpFrequencyHopTimes_val;

    return MFD_SUCCESS;
} /* wtpFrequencyHopTimes_map */

/**
 * Extract the current value of the wtpFrequencyHopTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpFrequencyHopTimes_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpFrequencyHopTimes_get( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long * wtpFrequencyHopTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpFrequencyHopTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpFrequencyHopTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpFrequencyHopTimes data.
 * set (* wtpFrequencyHopTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpFrequencyHopTimes_val_ptr ) = rowreq_ctx->data.wtpFrequencyHopTimes;

    return MFD_SUCCESS;
} /* wtpFrequencyHopTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpFreHopDetectTime
 * wtpFreHopDetectTime is subid 6 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.6
 * Description:
The pass-time that change signal path because of shuning Frequency interference.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpFreHopDetectTime.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpFreHopDetectTime_map(long *mib_wtpFreHopDetectTime_val_ptr, long raw_wtpFreHopDetectTime_val)
{
    netsnmp_assert(NULL != mib_wtpFreHopDetectTime_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpFreHopDetectTime_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpFreHopDetectTime mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpFreHopDetectTime_val_ptr) = raw_wtpFreHopDetectTime_val;

    return MFD_SUCCESS;
} /* wtpFreHopDetectTime_map */

/**
 * Extract the current value of the wtpFreHopDetectTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpFreHopDetectTime_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpFreHopDetectTime_get( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long * wtpFreHopDetectTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpFreHopDetectTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpFreHopDetectTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpFreHopDetectTime data.
 * set (* wtpFreHopDetectTime_val_ptr ) from rowreq_ctx->data
 */
    (* wtpFreHopDetectTime_val_ptr ) = rowreq_ctx->data.wtpFreHopDetectTime;

    return MFD_SUCCESS;
} /* wtpFreHopDetectTime_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpConfigLongRetransThreshold
 * wtpConfigLongRetransThreshold is subid 7 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.7
 * Description:
The value of configed long retransmission threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpConfigLongRetransThreshold.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpConfigLongRetransThreshold_map(long *mib_wtpConfigLongRetransThreshold_val_ptr, long raw_wtpConfigLongRetransThreshold_val)
{
    netsnmp_assert(NULL != mib_wtpConfigLongRetransThreshold_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpConfigLongRetransThreshold_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpConfigLongRetransThreshold mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpConfigLongRetransThreshold_val_ptr) = raw_wtpConfigLongRetransThreshold_val;

    return MFD_SUCCESS;
} /* wtpConfigLongRetransThreshold_map */

/**
 * Extract the current value of the wtpConfigLongRetransThreshold data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpConfigLongRetransThreshold_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpConfigLongRetransThreshold_get( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long * wtpConfigLongRetransThreshold_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpConfigLongRetransThreshold_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpConfigLongRetransThreshold_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpConfigLongRetransThreshold data.
 * set (* wtpConfigLongRetransThreshold_val_ptr ) from rowreq_ctx->data
 */
    (* wtpConfigLongRetransThreshold_val_ptr ) = rowreq_ctx->data.wtpConfigLongRetransThreshold;

    return MFD_SUCCESS;
} /* wtpConfigLongRetransThreshold_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpMessageNeafThreshold
 * wtpMessageNeafThreshold is subid 8 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.8
 * Description:
The value of message neaf threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMessageNeafThreshold.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMessageNeafThreshold_map(long *mib_wtpMessageNeafThreshold_val_ptr, long raw_wtpMessageNeafThreshold_val)
{
    netsnmp_assert(NULL != mib_wtpMessageNeafThreshold_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpMessageNeafThreshold_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMessageNeafThreshold mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMessageNeafThreshold_val_ptr) = raw_wtpMessageNeafThreshold_val;

    return MFD_SUCCESS;
} /* wtpMessageNeafThreshold_map */

/**
 * Extract the current value of the wtpMessageNeafThreshold data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMessageNeafThreshold_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMessageNeafThreshold_get( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long * wtpMessageNeafThreshold_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMessageNeafThreshold_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpMessageNeafThreshold_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMessageNeafThreshold data.
 * set (* wtpMessageNeafThreshold_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMessageNeafThreshold_val_ptr ) = rowreq_ctx->data.wtpMessageNeafThreshold;

    return MFD_SUCCESS;
} /* wtpMessageNeafThreshold_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpRTSThreshold
 * wtpRTSThreshold is subid 9 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.9
 * Description:
The value of RTS threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpRTSThreshold.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpRTSThreshold_map(long *mib_wtpRTSThreshold_val_ptr, long raw_wtpRTSThreshold_val)
{
    netsnmp_assert(NULL != mib_wtpRTSThreshold_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpRTSThreshold_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpRTSThreshold mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpRTSThreshold_val_ptr) = raw_wtpRTSThreshold_val;

    return MFD_SUCCESS;
} /* wtpRTSThreshold_map */

/**
 * Extract the current value of the wtpRTSThreshold data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRTSThreshold_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpRTSThreshold_get( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long * wtpRTSThreshold_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpRTSThreshold_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpRTSThreshold_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpRTSThreshold data.
 * set (* wtpRTSThreshold_val_ptr ) from rowreq_ctx->data
 */
    (* wtpRTSThreshold_val_ptr ) = rowreq_ctx->data.wtpRTSThreshold;

    return MFD_SUCCESS;
} /* wtpRTSThreshold_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpSignalAveIntensity
 * wtpSignalAveIntensity is subid 10 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.10
 * Description:
The average intensity of signal.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSignalAveIntensity.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSignalAveIntensity_map(long *mib_wtpSignalAveIntensity_val_ptr, long raw_wtpSignalAveIntensity_val)
{
    netsnmp_assert(NULL != mib_wtpSignalAveIntensity_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalAveIntensity_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSignalAveIntensity mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSignalAveIntensity_val_ptr) = raw_wtpSignalAveIntensity_val;

    return MFD_SUCCESS;
} /* wtpSignalAveIntensity_map */

/**
 * Extract the current value of the wtpSignalAveIntensity data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSignalAveIntensity_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSignalAveIntensity_get( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long * wtpSignalAveIntensity_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSignalAveIntensity_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalAveIntensity_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSignalAveIntensity data.
 * set (* wtpSignalAveIntensity_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSignalAveIntensity_val_ptr ) = rowreq_ctx->data.wtpSignalAveIntensity;

    return MFD_SUCCESS;
} /* wtpSignalAveIntensity_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpPreambleLen
 * wtpPreambleLen is subid 11 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.11
 * Description:
The Preamble length.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  short(1), long(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpPreambleLen.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpPreambleLen_map(u_long *mib_wtpPreambleLen_val_ptr, u_long raw_wtpPreambleLen_val)
{
    netsnmp_assert(NULL != mib_wtpPreambleLen_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpPreambleLen_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpPreambleLen enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpPreambleLen_val) {
        case INTERNAL_WTPPREAMBLELEN_SHORT:
             *mib_wtpPreambleLen_val_ptr = WTPPREAMBLELEN_SHORT;
             break;

        case INTERNAL_WTPPREAMBLELEN_LONG:
             *mib_wtpPreambleLen_val_ptr = WTPPREAMBLELEN_LONG;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpPreambleLen\n", raw_wtpPreambleLen_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpPreambleLen_map */

/**
 * Extract the current value of the wtpPreambleLen data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpPreambleLen_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpPreambleLen_get( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, u_long * wtpPreambleLen_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpPreambleLen_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpPreambleLen_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpPreambleLen data.
 * set (* wtpPreambleLen_val_ptr ) from rowreq_ctx->data
 */
    (* wtpPreambleLen_val_ptr ) = rowreq_ctx->data.wtpPreambleLen;

    return MFD_SUCCESS;
} /* wtpPreambleLen_get */



/** @} */
