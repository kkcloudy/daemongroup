/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11RadioParaTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11RadioParaTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11RadioParaTable is subid 2 of wtpRadio.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11RadioParaTable_undo_setup( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11RadioParaTable:dot11RadioParaTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11RadioParaTable undo.
     * set up dot11RadioParaTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11RadioParaTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11RadioParaTable_undo_cleanup( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11RadioParaTable:dot11RadioParaTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11RadioParaTable undo.
     */

    return rc;
} /* dot11RadioParaTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11RadioParaTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11RadioParaTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11RadioParaTable_commit( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11RadioParaTable:dot11RadioParaTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11RadioParaTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WTPSIGNALPATHMODE) {
       save_flags &= ~FLAG_WTPSIGNALPATHMODE; /* clear wtpSignalPathMode */
       /*
        * TODO:482:o: |-> commit column wtpSignalPathMode.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioParaTable column wtpSignalPathMode commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpSignalPathMode
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPSIGNALPATHMODE;
       }
    }

    if (save_flags & FLAG_WTPFREHOPDETECTTIME) {
       save_flags &= ~FLAG_WTPFREHOPDETECTTIME; /* clear wtpFreHopDetectTime */
       /*
        * TODO:482:o: |-> commit column wtpFreHopDetectTime.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioParaTable column wtpFreHopDetectTime commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpFreHopDetectTime
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPFREHOPDETECTTIME;
       }
    }

    if (save_flags & FLAG_WTPCONFIGLONGRETRANSTHRESHOLD) {
       save_flags &= ~FLAG_WTPCONFIGLONGRETRANSTHRESHOLD; /* clear wtpConfigLongRetransThreshold */
       /*
        * TODO:482:o: |-> commit column wtpConfigLongRetransThreshold.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioParaTable column wtpConfigLongRetransThreshold commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpConfigLongRetransThreshold
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPCONFIGLONGRETRANSTHRESHOLD;
       }
    }

    if (save_flags & FLAG_WTPMESSAGENEAFTHRESHOLD) {
       save_flags &= ~FLAG_WTPMESSAGENEAFTHRESHOLD; /* clear wtpMessageNeafThreshold */
       /*
        * TODO:482:o: |-> commit column wtpMessageNeafThreshold.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioParaTable column wtpMessageNeafThreshold commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpMessageNeafThreshold
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPMESSAGENEAFTHRESHOLD;
       }
    }

    if (save_flags & FLAG_WTPRTSTHRESHOLD) {
       save_flags &= ~FLAG_WTPRTSTHRESHOLD; /* clear wtpRTSThreshold */
       /*
        * TODO:482:o: |-> commit column wtpRTSThreshold.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioParaTable column wtpRTSThreshold commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpRTSThreshold
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPRTSTHRESHOLD;
       }
    }

    if (save_flags & FLAG_WTPSIGNALAVEINTENSITY) {
       save_flags &= ~FLAG_WTPSIGNALAVEINTENSITY; /* clear wtpSignalAveIntensity */
       /*
        * TODO:482:o: |-> commit column wtpSignalAveIntensity.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioParaTable column wtpSignalAveIntensity commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpSignalAveIntensity
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPSIGNALAVEINTENSITY;
       }
    }

    if (save_flags & FLAG_WTPPREAMBLELEN) {
       save_flags &= ~FLAG_WTPPREAMBLELEN; /* clear wtpPreambleLen */
       /*
        * TODO:482:o: |-> commit column wtpPreambleLen.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioParaTable column wtpPreambleLen commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpPreambleLen
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPPREAMBLELEN;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11RadioParaTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11RadioParaTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11RadioParaTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11RadioParaTable_undo_commit( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11RadioParaTable:dot11RadioParaTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11RadioParaTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11RadioParaTable_undo_commit */

/*
 * TODO:420:r: Implement dot11RadioParaTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpRadCurrID
 * wtpRadCurrID is subid 1 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.1
 * Description:
Current radio ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/*
 * TODO:440:M: Implement dot11RadioParaTable node value checks.
 * TODO:450:M: Implement dot11RadioParaTable undo functions.
 * TODO:460:M: Implement dot11RadioParaTable set functions.
 * TODO:480:M: Implement dot11RadioParaTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpSignalPathMode
 * wtpSignalPathMode is subid 3 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.3
 * Description:
Signal path mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSignalPathMode_val_ptr
 *        A char containing the new value.
 * @param wtpSignalPathMode_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpSignalPathMode_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpSignalPathMode_check_value( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, char *wtpSignalPathMode_val_ptr,  size_t wtpSignalPathMode_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalPathMode_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpSignalPathMode_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wtpSignalPathMode value.
     */

    return MFD_SUCCESS; /* wtpSignalPathMode value not illegal */
} /* wtpSignalPathMode_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpSignalPathMode_undo_setup( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalPathMode_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpSignalPathMode undo.
     */
    /*
     * copy wtpSignalPathMode and wtpSignalPathMode_len data
     * set rowreq_ctx->undo->wtpSignalPathMode from rowreq_ctx->data.wtpSignalPathMode
     */
    memcpy( rowreq_ctx->undo->wtpSignalPathMode, rowreq_ctx->data.wtpSignalPathMode,
            (rowreq_ctx->data.wtpSignalPathMode_len * sizeof(rowreq_ctx->undo->wtpSignalPathMode[0])));
    rowreq_ctx->undo->wtpSignalPathMode_len = rowreq_ctx->data.wtpSignalPathMode_len;


    return MFD_SUCCESS;
} /* wtpSignalPathMode_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpSignalPathMode_val_ptr
 *        A char containing the new value.
 * @param wtpSignalPathMode_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpSignalPathMode_val_ptr
 */
int
wtpSignalPathMode_set( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, char *wtpSignalPathMode_val_ptr,  size_t wtpSignalPathMode_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalPathMode_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpSignalPathMode_val_ptr);

/*
 * TODO:245:o: |-> Implement wtpSignalPathMode reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpSignalPathMode value.
     * set wtpSignalPathMode value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.wtpSignalPathMode, wtpSignalPathMode_val_ptr, wtpSignalPathMode_val_ptr_len );
    rowreq_ctx->data.wtpSignalPathMode_len = wtpSignalPathMode_val_ptr_len / sizeof(wtpSignalPathMode_val_ptr[0]);

    return MFD_SUCCESS;
} /* wtpSignalPathMode_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpSignalPathMode_undo( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalPathMode_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpSignalPathMode undo.
     */
    /*
     * copy wtpSignalPathMode and wtpSignalPathMode_len data
     * set rowreq_ctx->data.wtpSignalPathMode from rowreq_ctx->undo->wtpSignalPathMode
     */
    memcpy( rowreq_ctx->data.wtpSignalPathMode, rowreq_ctx->undo->wtpSignalPathMode,
            (rowreq_ctx->undo->wtpSignalPathMode_len * sizeof(rowreq_ctx->data.wtpSignalPathMode[0])));
    rowreq_ctx->data.wtpSignalPathMode_len = rowreq_ctx->undo->wtpSignalPathMode_len;

    
    return MFD_SUCCESS;
} /* wtpSignalPathMode_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpFreHopDetectTime
 * wtpFreHopDetectTime is subid 6 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.6
 * Description:
The pass-time that change signal path because of shuning Frequency interference.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpFreHopDetectTime_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpFreHopDetectTime_check_value( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long wtpFreHopDetectTime_val)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpFreHopDetectTime_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpFreHopDetectTime value.
     */

    return MFD_SUCCESS; /* wtpFreHopDetectTime value not illegal */
} /* wtpFreHopDetectTime_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpFreHopDetectTime_undo_setup( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpFreHopDetectTime_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpFreHopDetectTime undo.
     */
    /*
     * copy wtpFreHopDetectTime data
     * set rowreq_ctx->undo->wtpFreHopDetectTime from rowreq_ctx->data.wtpFreHopDetectTime
     */
    rowreq_ctx->undo->wtpFreHopDetectTime = rowreq_ctx->data.wtpFreHopDetectTime;


    return MFD_SUCCESS;
} /* wtpFreHopDetectTime_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpFreHopDetectTime_val
 *        A long containing the new value.
 */
int
wtpFreHopDetectTime_set( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long wtpFreHopDetectTime_val )
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpFreHopDetectTime_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpFreHopDetectTime reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpFreHopDetectTime value.
     * set wtpFreHopDetectTime value in rowreq_ctx->data
     */
    rowreq_ctx->data.wtpFreHopDetectTime = wtpFreHopDetectTime_val;

    return MFD_SUCCESS;
} /* wtpFreHopDetectTime_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpFreHopDetectTime_undo( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpFreHopDetectTime_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpFreHopDetectTime undo.
     */
    /*
     * copy wtpFreHopDetectTime data
     * set rowreq_ctx->data.wtpFreHopDetectTime from rowreq_ctx->undo->wtpFreHopDetectTime
     */
    rowreq_ctx->data.wtpFreHopDetectTime = rowreq_ctx->undo->wtpFreHopDetectTime;

    
    return MFD_SUCCESS;
} /* wtpFreHopDetectTime_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpConfigLongRetransThreshold
 * wtpConfigLongRetransThreshold is subid 7 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.7
 * Description:
The value of configed long retransmission threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpConfigLongRetransThreshold_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpConfigLongRetransThreshold_check_value( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long wtpConfigLongRetransThreshold_val)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpConfigLongRetransThreshold_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpConfigLongRetransThreshold value.
     */

    return MFD_SUCCESS; /* wtpConfigLongRetransThreshold value not illegal */
} /* wtpConfigLongRetransThreshold_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpConfigLongRetransThreshold_undo_setup( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpConfigLongRetransThreshold_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpConfigLongRetransThreshold undo.
     */
    /*
     * copy wtpConfigLongRetransThreshold data
     * set rowreq_ctx->undo->wtpConfigLongRetransThreshold from rowreq_ctx->data.wtpConfigLongRetransThreshold
     */
    rowreq_ctx->undo->wtpConfigLongRetransThreshold = rowreq_ctx->data.wtpConfigLongRetransThreshold;


    return MFD_SUCCESS;
} /* wtpConfigLongRetransThreshold_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpConfigLongRetransThreshold_val
 *        A long containing the new value.
 */
int
wtpConfigLongRetransThreshold_set( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long wtpConfigLongRetransThreshold_val )
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpConfigLongRetransThreshold_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpConfigLongRetransThreshold reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpConfigLongRetransThreshold value.
     * set wtpConfigLongRetransThreshold value in rowreq_ctx->data
     */    
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret=0;

	ret=config_radio_longretry(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,wtpConfigLongRetransThreshold_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpConfigLongRetransThreshold = wtpConfigLongRetransThreshold_val;		
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpConfigLongRetransThreshold_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpConfigLongRetransThreshold_undo( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpConfigLongRetransThreshold_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpConfigLongRetransThreshold undo.
     */
    /*
     * copy wtpConfigLongRetransThreshold data
     * set rowreq_ctx->data.wtpConfigLongRetransThreshold from rowreq_ctx->undo->wtpConfigLongRetransThreshold
     */
    rowreq_ctx->data.wtpConfigLongRetransThreshold = rowreq_ctx->undo->wtpConfigLongRetransThreshold;

    
    return MFD_SUCCESS;
} /* wtpConfigLongRetransThreshold_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpMessageNeafThreshold
 * wtpMessageNeafThreshold is subid 8 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.8
 * Description:
The value of message neaf threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMessageNeafThreshold_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpMessageNeafThreshold_check_value( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long wtpMessageNeafThreshold_val)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpMessageNeafThreshold_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpMessageNeafThreshold value.
     */

    return MFD_SUCCESS; /* wtpMessageNeafThreshold value not illegal */
} /* wtpMessageNeafThreshold_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpMessageNeafThreshold_undo_setup( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpMessageNeafThreshold_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpMessageNeafThreshold undo.
     */
    /*
     * copy wtpMessageNeafThreshold data
     * set rowreq_ctx->undo->wtpMessageNeafThreshold from rowreq_ctx->data.wtpMessageNeafThreshold
     */
    rowreq_ctx->undo->wtpMessageNeafThreshold = rowreq_ctx->data.wtpMessageNeafThreshold;


    return MFD_SUCCESS;
} /* wtpMessageNeafThreshold_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpMessageNeafThreshold_val
 *        A long containing the new value.
 */
int
wtpMessageNeafThreshold_set( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long wtpMessageNeafThreshold_val )
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpMessageNeafThreshold_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpMessageNeafThreshold reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpMessageNeafThreshold value.
     * set wtpMessageNeafThreshold value in rowreq_ctx->data
     */  
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int rc = MFD_ERROR;
	int ret=0;

	ret = config_radio_fragmentation(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,wtpMessageNeafThreshold_val);
	if(ret == 1)
	{
		rowreq_ctx->data.wtpMessageNeafThreshold = wtpMessageNeafThreshold_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpMessageNeafThreshold_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpMessageNeafThreshold_undo( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpMessageNeafThreshold_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpMessageNeafThreshold undo.
     */
    /*
     * copy wtpMessageNeafThreshold data
     * set rowreq_ctx->data.wtpMessageNeafThreshold from rowreq_ctx->undo->wtpMessageNeafThreshold
     */
    rowreq_ctx->data.wtpMessageNeafThreshold = rowreq_ctx->undo->wtpMessageNeafThreshold;

    
    return MFD_SUCCESS;
} /* wtpMessageNeafThreshold_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpRTSThreshold
 * wtpRTSThreshold is subid 9 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.9
 * Description:
The value of RTS threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRTSThreshold_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpRTSThreshold_check_value( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long wtpRTSThreshold_val)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpRTSThreshold_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpRTSThreshold value.
     */

    return MFD_SUCCESS; /* wtpRTSThreshold value not illegal */
} /* wtpRTSThreshold_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpRTSThreshold_undo_setup( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpRTSThreshold_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpRTSThreshold undo.
     */
    /*
     * copy wtpRTSThreshold data
     * set rowreq_ctx->undo->wtpRTSThreshold from rowreq_ctx->data.wtpRTSThreshold
     */
    rowreq_ctx->undo->wtpRTSThreshold = rowreq_ctx->data.wtpRTSThreshold;


    return MFD_SUCCESS;
} /* wtpRTSThreshold_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpRTSThreshold_val
 *        A long containing the new value.
 */
int
wtpRTSThreshold_set( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long wtpRTSThreshold_val )
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpRTSThreshold_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpRTSThreshold reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpRTSThreshold value.
     * set wtpRTSThreshold value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret=0;

	ret = config_radio_rtsthreshold(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,wtpRTSThreshold_val);
	if(ret == 1)
	{
		rowreq_ctx->data.wtpRTSThreshold = wtpRTSThreshold_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpRTSThreshold_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpRTSThreshold_undo( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpRTSThreshold_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpRTSThreshold undo.
     */
    /*
     * copy wtpRTSThreshold data
     * set rowreq_ctx->data.wtpRTSThreshold from rowreq_ctx->undo->wtpRTSThreshold
     */
    rowreq_ctx->data.wtpRTSThreshold = rowreq_ctx->undo->wtpRTSThreshold;

    
    return MFD_SUCCESS;
} /* wtpRTSThreshold_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpSignalAveIntensity
 * wtpSignalAveIntensity is subid 10 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.10
 * Description:
The average intensity of signal.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSignalAveIntensity_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpSignalAveIntensity_check_value( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long wtpSignalAveIntensity_val)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalAveIntensity_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpSignalAveIntensity value.
     */

    return MFD_SUCCESS; /* wtpSignalAveIntensity value not illegal */
} /* wtpSignalAveIntensity_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpSignalAveIntensity_undo_setup( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalAveIntensity_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpSignalAveIntensity undo.
     */
    /*
     * copy wtpSignalAveIntensity data
     * set rowreq_ctx->undo->wtpSignalAveIntensity from rowreq_ctx->data.wtpSignalAveIntensity
     */
    rowreq_ctx->undo->wtpSignalAveIntensity = rowreq_ctx->data.wtpSignalAveIntensity;


    return MFD_SUCCESS;
} /* wtpSignalAveIntensity_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpSignalAveIntensity_val
 *        A long containing the new value.
 */
int
wtpSignalAveIntensity_set( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, long wtpSignalAveIntensity_val )
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalAveIntensity_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpSignalAveIntensity reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpSignalAveIntensity value.
     * set wtpSignalAveIntensity value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret=0;

	ret = config_radio_txpower(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,wtpSignalAveIntensity_val);
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpSignalAveIntensity = wtpSignalAveIntensity_val;		
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpSignalAveIntensity_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpSignalAveIntensity_undo( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpSignalAveIntensity_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpSignalAveIntensity undo.
     */
    /*
     * copy wtpSignalAveIntensity data
     * set rowreq_ctx->data.wtpSignalAveIntensity from rowreq_ctx->undo->wtpSignalAveIntensity
     */
    rowreq_ctx->data.wtpSignalAveIntensity = rowreq_ctx->undo->wtpSignalAveIntensity;

    
    return MFD_SUCCESS;
} /* wtpSignalAveIntensity_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioParaEntry.wtpPreambleLen
 * wtpPreambleLen is subid 11 of dot11RadioParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.2.1.11
 * Description:
The Preamble length.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  short(1), long(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpPreambleLen_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  short(1), long(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpPreambleLen_check_value( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, u_long wtpPreambleLen_val)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpPreambleLen_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpPreambleLen value.
     */

    return MFD_SUCCESS; /* wtpPreambleLen value not illegal */
} /* wtpPreambleLen_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpPreambleLen_undo_setup( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpPreambleLen_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpPreambleLen undo.
     */
    /*
     * copy wtpPreambleLen data
     * set rowreq_ctx->undo->wtpPreambleLen from rowreq_ctx->data.wtpPreambleLen
     */
    rowreq_ctx->undo->wtpPreambleLen = rowreq_ctx->data.wtpPreambleLen;


    return MFD_SUCCESS;
} /* wtpPreambleLen_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpPreambleLen_val
 *        A long containing the new value.
 */
int
wtpPreambleLen_set( dot11RadioParaTable_rowreq_ctx *rowreq_ctx, u_long wtpPreambleLen_val )
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpPreambleLen_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpPreambleLen reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpPreambleLen_val) {
        case WTPPREAMBLELEN_SHORT:
             wtpPreambleLen_val = INTERNAL_WTPPREAMBLELEN_SHORT;
             break;

        case WTPPREAMBLELEN_LONG:
             wtpPreambleLen_val = INTERNAL_WTPPREAMBLELEN_LONG;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpPreambleLen\n", wtpPreambleLen_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpPreambleLen value.
     * set wtpPreambleLen value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret=0;
	char input[10] = { 0 };
	memset(input,0,10);

	if(wtpPreambleLen_val == INTERNAL_WTPPREAMBLELEN_SHORT)
	{
		strncpy(input,"short",sizeof(input)-1);
	}
	else if(wtpPreambleLen_val == INTERNAL_WTPPREAMBLELEN_LONG)
	{
		strncpy(input,"long",sizeof(input)-1);
	}
	ret = config_radio_preamble(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,input);
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpPreambleLen = wtpPreambleLen_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpPreambleLen_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpPreambleLen_undo( dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioParaTable:wtpPreambleLen_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpPreambleLen undo.
     */
    /*
     * copy wtpPreambleLen data
     * set rowreq_ctx->data.wtpPreambleLen from rowreq_ctx->undo->wtpPreambleLen
     */
    rowreq_ctx->data.wtpPreambleLen = rowreq_ctx->undo->wtpPreambleLen;

    
    return MFD_SUCCESS;
} /* wtpPreambleLen_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11RadioParaTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11RadioParaTable if you don't have dependencies)
 */
int
dot11RadioParaTable_check_dependencies(dot11RadioParaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11RadioParaTable:dot11RadioParaTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11RadioParaTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11RadioParaTable_check_dependencies */

/** @} */
