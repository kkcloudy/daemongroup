/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11TerminalBlackListTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11TerminalBlackListTable get routines.
 * TODO:240:M: Implement dot11TerminalBlackListTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11TerminalBlackListTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11TerminalBlackListTable is subid 4 of dot11WhiteBlackList.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.4, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11TerminalBlackListTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param TerminalBlackListMAC_ptr
 * @param TerminalBlackListMAC_ptr_len
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11TerminalBlackListTable_indexes_set_tbl_idx(dot11TerminalBlackListTable_mib_index *tbl_idx, char *TerminalBlackListMAC_val_ptr,  size_t TerminalBlackListMAC_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11TerminalBlackListTable:dot11TerminalBlackListTable_indexes_set_tbl_idx","called\n"));

    /* TerminalBlackListMAC(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
    tbl_idx->TerminalBlackListMAC_len = sizeof(tbl_idx->TerminalBlackListMAC)/sizeof(tbl_idx->TerminalBlackListMAC[0]); /* max length */
    /*
     * make sure there is enough space for TerminalBlackListMAC data
     */
    if ((NULL == tbl_idx->TerminalBlackListMAC) ||
        (tbl_idx->TerminalBlackListMAC_len <
         (TerminalBlackListMAC_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->TerminalBlackListMAC_len = TerminalBlackListMAC_val_ptr_len;
    memcpy( tbl_idx->TerminalBlackListMAC, TerminalBlackListMAC_val_ptr, TerminalBlackListMAC_val_ptr_len* sizeof(TerminalBlackListMAC_val_ptr[0]) );
    

    return MFD_SUCCESS;
} /* dot11TerminalBlackListTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11TerminalBlackListTable_indexes_set(dot11TerminalBlackListTable_rowreq_ctx *rowreq_ctx, char *TerminalBlackListMAC_val_ptr,  size_t TerminalBlackListMAC_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11TerminalBlackListTable:dot11TerminalBlackListTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11TerminalBlackListTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , TerminalBlackListMAC_val_ptr, TerminalBlackListMAC_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11TerminalBlackListTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11TerminalBlackListTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11TerminalBlackListEntry.TerminalBlackListAddReason
 * TerminalBlackListAddReason is subid 2 of dot11TerminalBlackListEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.4.1.2
 * Description:
 The reason why add to black list.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the TerminalBlackListAddReason data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param TerminalBlackListAddReason_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param TerminalBlackListAddReason_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by TerminalBlackListAddReason.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*TerminalBlackListAddReason_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update TerminalBlackListAddReason_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
TerminalBlackListAddReason_get( dot11TerminalBlackListTable_rowreq_ctx *rowreq_ctx, char **TerminalBlackListAddReason_val_ptr_ptr, size_t *TerminalBlackListAddReason_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != TerminalBlackListAddReason_val_ptr_ptr) && (NULL != *TerminalBlackListAddReason_val_ptr_ptr));
   netsnmp_assert( NULL != TerminalBlackListAddReason_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11TerminalBlackListTable:TerminalBlackListAddReason_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the TerminalBlackListAddReason data.
 * copy (* TerminalBlackListAddReason_val_ptr_ptr ) data and (* TerminalBlackListAddReason_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for TerminalBlackListAddReason data
     */
    if ((NULL == (* TerminalBlackListAddReason_val_ptr_ptr )) ||
        ((* TerminalBlackListAddReason_val_ptr_len_ptr ) <
         (rowreq_ctx->data.TerminalBlackListAddReason_len* sizeof(rowreq_ctx->data.TerminalBlackListAddReason[0])))) {
        /*
         * allocate space for TerminalBlackListAddReason data
         */
        (* TerminalBlackListAddReason_val_ptr_ptr ) = malloc(rowreq_ctx->data.TerminalBlackListAddReason_len* sizeof(rowreq_ctx->data.TerminalBlackListAddReason[0]));
        if(NULL == (* TerminalBlackListAddReason_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* TerminalBlackListAddReason_val_ptr_len_ptr ) = rowreq_ctx->data.TerminalBlackListAddReason_len* sizeof(rowreq_ctx->data.TerminalBlackListAddReason[0]);
    memcpy( (* TerminalBlackListAddReason_val_ptr_ptr ), rowreq_ctx->data.TerminalBlackListAddReason, rowreq_ctx->data.TerminalBlackListAddReason_len* sizeof(rowreq_ctx->data.TerminalBlackListAddReason[0]) );

    return MFD_SUCCESS;
} /* TerminalBlackListAddReason_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11TerminalBlackListEntry.TerminalBlackListDuration
 * TerminalBlackListDuration is subid 3 of dot11TerminalBlackListEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.4.1.3
 * Description:
 How long it keeps in black list.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the TerminalBlackListDuration data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param TerminalBlackListDuration_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
TerminalBlackListDuration_get( dot11TerminalBlackListTable_rowreq_ctx *rowreq_ctx, u_long * TerminalBlackListDuration_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != TerminalBlackListDuration_val_ptr );


    DEBUGMSGTL(("verbose:dot11TerminalBlackListTable:TerminalBlackListDuration_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the TerminalBlackListDuration data.
 * copy (* TerminalBlackListDuration_val_ptr ) from rowreq_ctx->data
 */
    (* TerminalBlackListDuration_val_ptr ) = rowreq_ctx->data.TerminalBlackListDuration;

    return MFD_SUCCESS;
} /* TerminalBlackListDuration_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11TerminalBlackListEntry.BlackListRowStatus
 * BlackListRowStatus is subid 4 of dot11TerminalBlackListEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.4.1.4
 * Description:
This variable is used to create, modify, and/or delete a row
	        	 	in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the BlackListRowStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param BlackListRowStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
BlackListRowStatus_get( dot11TerminalBlackListTable_rowreq_ctx *rowreq_ctx, u_long * BlackListRowStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != BlackListRowStatus_val_ptr );

    (* BlackListRowStatus_val_ptr ) = rowreq_ctx->data.BlackListRowStatus;

    return MFD_SUCCESS;
} /* BlackListRowStatus_get */



/** @} */
