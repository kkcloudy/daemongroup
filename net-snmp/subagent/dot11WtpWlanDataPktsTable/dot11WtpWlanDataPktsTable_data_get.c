/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpWlanDataPktsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpWlanDataPktsTable get routines.
 * TODO:240:M: Implement dot11WtpWlanDataPktsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpWlanDataPktsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpWlanDataPktsTable is subid 2 of wtpWlan.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpWlanDataPktsTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11WtpWlanDataPktsTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wlanCurrID
 * wlanCurrID is subid 1 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.1
 * Description:
The wlan's ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wlanCurrID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wlanCurrID_map(long *mib_wlanCurrID_val_ptr, long raw_wlanCurrID_val)
{
    netsnmp_assert(NULL != mib_wlanCurrID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wlanCurrID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wlanCurrID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wlanCurrID_val_ptr) = raw_wlanCurrID_val;

    return MFD_SUCCESS;
} /* wlanCurrID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpWlanDataPktsTable_indexes_set_tbl_idx(dot11WtpWlanDataPktsTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:dot11WtpWlanDataPktsTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wlanCurrID = wlanCurrID_val;
    

    return MFD_SUCCESS;
} /* dot11WtpWlanDataPktsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpWlanDataPktsTable_indexes_set(dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:dot11WtpWlanDataPktsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpWlanDataPktsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wlanCurrID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpWlanDataPktsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpWlanDataPktsTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpSsidTxTermAllByte
 * wtpSsidTxTermAllByte is subid 1 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.1
 * Description:
The total bytes which sent to terminal(Appointed SSID).(KB)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSsidTxTermAllByte.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSsidTxTermAllByte_map(unsigned long long *mib_wtpSsidTxTermAllByte_val_ptr, unsigned long long raw_wtpSsidTxTermAllByte_val)
{
    netsnmp_assert(NULL != mib_wtpSsidTxTermAllByte_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidTxTermAllByte_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSsidTxTermAllByte mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSsidTxTermAllByte_val_ptr) = raw_wtpSsidTxTermAllByte_val;

    return MFD_SUCCESS;
} /* wtpSsidTxTermAllByte_map */

/**
 * Extract the current value of the wtpSsidTxTermAllByte data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidTxTermAllByte_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSsidTxTermAllByte_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wtpSsidTxTermAllByte_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSsidTxTermAllByte_val_ptr );



/*
 * TODO:231:o: |-> Extract the current value of the wtpSsidTxTermAllByte data.
 * set (* wtpSsidTxTermAllByte_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSsidTxTermAllByte_val_ptr ) = rowreq_ctx->data.wtpSsidTxTermAllByte;

    return MFD_SUCCESS;
} /* wtpSsidTxTermAllByte_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpSsidRxTermAllPack
 * wtpSsidRxTermAllPack is subid 2 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.2
 * Description:
The total packages which received from terminal(Appointed SSID).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSsidRxTermAllPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSsidRxTermAllPack_map(unsigned long *mib_wtpSsidRxTermAllPack_val_ptr, unsigned long raw_wtpSsidRxTermAllPack_val)
{
    netsnmp_assert(NULL != mib_wtpSsidRxTermAllPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidRxTermAllPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSsidRxTermAllPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSsidRxTermAllPack_val_ptr) = raw_wtpSsidRxTermAllPack_val;

    return MFD_SUCCESS;
} /* wtpSsidRxTermAllPack_map */

/**
 * Extract the current value of the wtpSsidRxTermAllPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidRxTermAllPack_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSsidRxTermAllPack_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpSsidRxTermAllPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSsidRxTermAllPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidRxTermAllPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSsidRxTermAllPack data.
 * set (* wtpSsidRxTermAllPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSsidRxTermAllPack_val_ptr ) = rowreq_ctx->data.wtpSsidRxTermAllPack;

    return MFD_SUCCESS;
} /* wtpSsidRxTermAllPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpSsidRxTermAllByte
 * wtpSsidRxTermAllByte is subid 3 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.3
 * Description:
The total bytes which received from terminal(Appointed SSID).(KB)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSsidRxTermAllByte.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSsidRxTermAllByte_map(unsigned long long *mib_wtpSsidRxTermAllByte_val_ptr, unsigned long long raw_wtpSsidRxTermAllByte_val)
{
    netsnmp_assert(NULL != mib_wtpSsidRxTermAllByte_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidRxTermAllByte_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSsidRxTermAllByte mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSsidRxTermAllByte_val_ptr) = raw_wtpSsidRxTermAllByte_val;

    return MFD_SUCCESS;
} /* wtpSsidRxTermAllByte_map */

/**
 * Extract the current value of the wtpSsidRxTermAllByte data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidRxTermAllByte_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSsidRxTermAllByte_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wtpSsidRxTermAllByte_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSsidRxTermAllByte_val_ptr );


/*
 * TODO:231:o: |-> Extract the current value of the wtpSsidRxTermAllByte data.
 * set (* wtpSsidRxTermAllByte_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSsidRxTermAllByte_val_ptr ) = rowreq_ctx->data.wtpSsidRxTermAllByte;

    return MFD_SUCCESS;
} /* wtpSsidRxTermAllByte_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpSsidWirelessMacRxDataRightByte
 * wtpSsidWirelessMacRxDataRightByte is subid 4 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.4
 * Description:
The received right bytes at MAC level appointed SSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSsidWirelessMacRxDataRightByte.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSsidWirelessMacRxDataRightByte_map(unsigned long long *mib_wtpSsidWirelessMacRxDataRightByte_val_ptr, unsigned long long raw_wtpSsidWirelessMacRxDataRightByte_val)
{
    netsnmp_assert(NULL != mib_wtpSsidWirelessMacRxDataRightByte_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidWirelessMacRxDataRightByte_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSsidWirelessMacRxDataRightByte mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSsidWirelessMacRxDataRightByte_val_ptr) = raw_wtpSsidWirelessMacRxDataRightByte_val;

    return MFD_SUCCESS;
} /* wtpSsidWirelessMacRxDataRightByte_map */

/**
 * Extract the current value of the wtpSsidWirelessMacRxDataRightByte data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidWirelessMacRxDataRightByte_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSsidWirelessMacRxDataRightByte_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wtpSsidWirelessMacRxDataRightByte_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSsidWirelessMacRxDataRightByte_val_ptr );

    (* wtpSsidWirelessMacRxDataRightByte_val_ptr ) = rowreq_ctx->data.wtpSsidWirelessMacRxDataRightByte;

    return MFD_SUCCESS;
} /* wtpSsidWirelessMacRxDataRightByte_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpSsidWirelessMacTxDataRightByte
 * wtpSsidWirelessMacTxDataRightByte is subid 5 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.5
 * Description:
The sent total bytes at MAC level appointed SSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSsidWirelessMacTxDataRightByte.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSsidWirelessMacTxDataRightByte_map(unsigned long long *mib_wtpSsidWirelessMacTxDataRightByte_val_ptr, unsigned long long raw_wtpSsidWirelessMacTxDataRightByte_val)
{
    netsnmp_assert(NULL != mib_wtpSsidWirelessMacTxDataRightByte_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidWirelessMacTxDataRightByte_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSsidWirelessMacTxDataRightByte mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSsidWirelessMacTxDataRightByte_val_ptr) = raw_wtpSsidWirelessMacTxDataRightByte_val;

    return MFD_SUCCESS;
} /* wtpSsidWirelessMacTxDataRightByte_map */

/**
 * Extract the current value of the wtpSsidWirelessMacTxDataRightByte data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidWirelessMacTxDataRightByte_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSsidWirelessMacTxDataRightByte_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wtpSsidWirelessMacTxDataRightByte_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSsidWirelessMacTxDataRightByte_val_ptr );

    (* wtpSsidWirelessMacTxDataRightByte_val_ptr ) = rowreq_ctx->data.wtpSsidWirelessMacTxDataRightByte;

    return MFD_SUCCESS;
} /* wtpSsidWirelessMacTxDataRightByte_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpSsidRxDataErrPack
 * wtpSsidRxDataErrPack is subid 6 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.6
 * Description:
The received wrong packages at MAC level appointed SSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSsidRxDataErrPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSsidRxDataErrPack_map(unsigned long *mib_wtpSsidRxDataErrPack_val_ptr, unsigned long raw_wtpSsidRxDataErrPack_val)
{
    netsnmp_assert(NULL != mib_wtpSsidRxDataErrPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidRxDataErrPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSsidRxDataErrPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSsidRxDataErrPack_val_ptr) = raw_wtpSsidRxDataErrPack_val;

    return MFD_SUCCESS;
} /* wtpSsidRxDataErrPack_map */

/**
 * Extract the current value of the wtpSsidRxDataErrPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidRxDataErrPack_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSsidRxDataErrPack_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpSsidRxDataErrPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSsidRxDataErrPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidRxDataErrPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSsidRxDataErrPack data.
 * set (* wtpSsidRxDataErrPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSsidRxDataErrPack_val_ptr ) = rowreq_ctx->data.wtpSsidRxDataErrPack;

    return MFD_SUCCESS;
} /* wtpSsidRxDataErrPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpNetWiredRxPack
 * wtpNetWiredRxPack is subid 7 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.7
 * Description:
The received packages at network side(Wired side).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNetWiredRxPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNetWiredRxPack_map(unsigned long *mib_wtpNetWiredRxPack_val_ptr, unsigned long raw_wtpNetWiredRxPack_val)
{
    netsnmp_assert(NULL != mib_wtpNetWiredRxPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredRxPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpNetWiredRxPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpNetWiredRxPack_val_ptr) = raw_wtpNetWiredRxPack_val;

    return MFD_SUCCESS;
} /* wtpNetWiredRxPack_map */

/**
 * Extract the current value of the wtpNetWiredRxPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetWiredRxPack_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpNetWiredRxPack_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpNetWiredRxPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpNetWiredRxPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredRxPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpNetWiredRxPack data.
 * set (* wtpNetWiredRxPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpNetWiredRxPack_val_ptr ) = rowreq_ctx->data.wtpNetWiredRxPack;

    return MFD_SUCCESS;
} /* wtpNetWiredRxPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpSsidTxTermAllPack
 * wtpSsidTxTermAllPack is subid 8 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.8
 * Description:
The sent packages at user side(Wireless side).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSsidTxTermAllPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSsidTxTermAllPack_map(unsigned long *mib_wtpSsidTxTermAllPack_val_ptr, unsigned long raw_wtpSsidTxTermAllPack_val)
{
    netsnmp_assert(NULL != mib_wtpSsidTxTermAllPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidTxTermAllPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSsidTxTermAllPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSsidTxTermAllPack_val_ptr) = raw_wtpSsidTxTermAllPack_val;

    return MFD_SUCCESS;
} /* wtpSsidTxTermAllPack_map */

/**
 * Extract the current value of the wtpSsidTxTermAllPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidTxTermAllPack_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSsidTxTermAllPack_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpSsidTxTermAllPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSsidTxTermAllPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidTxTermAllPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSsidTxTermAllPack data.
 * set (* wtpSsidTxTermAllPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSsidTxTermAllPack_val_ptr ) = rowreq_ctx->data.wtpSsidTxTermAllPack;

    return MFD_SUCCESS;
} /* wtpSsidTxTermAllPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpNetWiredTxPack
 * wtpNetWiredTxPack is subid 9 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.9
 * Description:
The sent packages at network side(Wired side).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNetWiredTxPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNetWiredTxPack_map(unsigned long *mib_wtpNetWiredTxPack_val_ptr, unsigned long raw_wtpNetWiredTxPack_val)
{
    netsnmp_assert(NULL != mib_wtpNetWiredTxPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredTxPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpNetWiredTxPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpNetWiredTxPack_val_ptr) = raw_wtpNetWiredTxPack_val;

    return MFD_SUCCESS;
} /* wtpNetWiredTxPack_map */

/**
 * Extract the current value of the wtpNetWiredTxPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetWiredTxPack_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpNetWiredTxPack_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpNetWiredTxPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpNetWiredTxPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredTxPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpNetWiredTxPack data.
 * set (* wtpNetWiredTxPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpNetWiredTxPack_val_ptr ) = rowreq_ctx->data.wtpNetWiredTxPack;

    return MFD_SUCCESS;
} /* wtpNetWiredTxPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpSsidTxDataErrPkts
 * wtpSsidTxDataErrPkts is subid 10 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.10
 * Description:
The total number of failed sent packets of wireless side.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSsidTxDataErrPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSsidTxDataErrPkts_map(unsigned long *mib_wtpSsidTxDataErrPkts_val_ptr, unsigned long raw_wtpSsidTxDataErrPkts_val)
{
    netsnmp_assert(NULL != mib_wtpSsidTxDataErrPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidTxDataErrPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSsidTxDataErrPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSsidTxDataErrPkts_val_ptr) = raw_wtpSsidTxDataErrPkts_val;

    return MFD_SUCCESS;
} /* wtpSsidTxDataErrPkts_map */

/**
 * Extract the current value of the wtpSsidTxDataErrPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidTxDataErrPkts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSsidTxDataErrPkts_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpSsidTxDataErrPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSsidTxDataErrPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidTxDataErrPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSsidTxDataErrPkts data.
 * set (* wtpSsidTxDataErrPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSsidTxDataErrPkts_val_ptr ) = rowreq_ctx->data.wtpSsidTxDataErrPkts;

    return MFD_SUCCESS;
} /* wtpSsidTxDataErrPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpWirelessReTxPkts
 * wtpWirelessReTxPkts is subid 11 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.11
 * Description:
The total number of resent packets of wireless side.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessReTxPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessReTxPkts_map(unsigned long *mib_wtpWirelessReTxPkts_val_ptr, unsigned long raw_wtpWirelessReTxPkts_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessReTxPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpWirelessReTxPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessReTxPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessReTxPkts_val_ptr) = raw_wtpWirelessReTxPkts_val;

    return MFD_SUCCESS;
} /* wtpWirelessReTxPkts_map */

/**
 * Extract the current value of the wtpWirelessReTxPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessReTxPkts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpWirelessReTxPkts_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpWirelessReTxPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWirelessReTxPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpWirelessReTxPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessReTxPkts data.
 * set (* wtpWirelessReTxPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWirelessReTxPkts_val_ptr ) = rowreq_ctx->data.wtpWirelessReTxPkts;

    return MFD_SUCCESS;
} /* wtpWirelessReTxPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpWirelessErrPktsRate
 * wtpWirelessErrPktsRate is subid 12 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.12
 * Description:
The mistake packets rate on the wireless side.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessErrPktsRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessErrPktsRate_map(char **mib_wtpWirelessErrPktsRate_val_ptr_ptr, size_t *mib_wtpWirelessErrPktsRate_val_ptr_len_ptr, char *raw_wtpWirelessErrPktsRate_val_ptr, size_t raw_wtpWirelessErrPktsRate_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpWirelessErrPktsRate_val_ptr);
    netsnmp_assert((NULL != mib_wtpWirelessErrPktsRate_val_ptr_ptr) && (NULL != mib_wtpWirelessErrPktsRate_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpWirelessErrPktsRate_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpWirelessErrPktsRate non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpWirelessErrPktsRate_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpWirelessErrPktsRate_val_ptr_ptr) || (*mib_wtpWirelessErrPktsRate_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpWirelessErrPktsRate_val_ptr_ptr = realloc( *mib_wtpWirelessErrPktsRate_val_ptr_ptr, converted_len * sizeof(**mib_wtpWirelessErrPktsRate_val_ptr_ptr));
        if(NULL == *mib_wtpWirelessErrPktsRate_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpWirelessErrPktsRate_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpWirelessErrPktsRate_val_ptr_ptr, raw_wtpWirelessErrPktsRate_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpWirelessErrPktsRate_map */

/**
 * Extract the current value of the wtpWirelessErrPktsRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessErrPktsRate_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpWirelessErrPktsRate_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpWirelessErrPktsRate.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpWirelessErrPktsRate_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpWirelessErrPktsRate_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpWirelessErrPktsRate_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, char **wtpWirelessErrPktsRate_val_ptr_ptr, size_t *wtpWirelessErrPktsRate_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpWirelessErrPktsRate_val_ptr_ptr) && (NULL != *wtpWirelessErrPktsRate_val_ptr_ptr));
   netsnmp_assert( NULL != wtpWirelessErrPktsRate_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpWirelessErrPktsRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessErrPktsRate data.
 * set (* wtpWirelessErrPktsRate_val_ptr_ptr ) and (* wtpWirelessErrPktsRate_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpWirelessErrPktsRate data
     */
    if ((NULL == (* wtpWirelessErrPktsRate_val_ptr_ptr )) ||
        ((* wtpWirelessErrPktsRate_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpWirelessErrPktsRate_len * sizeof((* wtpWirelessErrPktsRate_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpWirelessErrPktsRate data
         */
        (* wtpWirelessErrPktsRate_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpWirelessErrPktsRate_len * sizeof((* wtpWirelessErrPktsRate_val_ptr_ptr )[0]));
        if(NULL == (* wtpWirelessErrPktsRate_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpWirelessErrPktsRate_val_ptr_len_ptr ) = rowreq_ctx->data.wtpWirelessErrPktsRate_len * sizeof((* wtpWirelessErrPktsRate_val_ptr_ptr )[0]);
    memcpy( (* wtpWirelessErrPktsRate_val_ptr_ptr ), rowreq_ctx->data.wtpWirelessErrPktsRate, (* wtpWirelessErrPktsRate_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpWirelessErrPktsRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpWirelessTxBroadcastMsgNum
 * wtpWirelessTxBroadcastMsgNum is subid 13 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.13
 * Description:
The total number of broadcast message box on the wireless side.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessTxBroadcastMsgNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessTxBroadcastMsgNum_map(unsigned long *mib_wtpWirelessTxBroadcastMsgNum_val_ptr, unsigned long raw_wtpWirelessTxBroadcastMsgNum_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessTxBroadcastMsgNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpWirelessTxBroadcastMsgNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessTxBroadcastMsgNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessTxBroadcastMsgNum_val_ptr) = raw_wtpWirelessTxBroadcastMsgNum_val;

    return MFD_SUCCESS;
} /* wtpWirelessTxBroadcastMsgNum_map */

/**
 * Extract the current value of the wtpWirelessTxBroadcastMsgNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessTxBroadcastMsgNum_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpWirelessTxBroadcastMsgNum_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpWirelessTxBroadcastMsgNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWirelessTxBroadcastMsgNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpWirelessTxBroadcastMsgNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessTxBroadcastMsgNum data.
 * set (* wtpWirelessTxBroadcastMsgNum_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWirelessTxBroadcastMsgNum_val_ptr ) = rowreq_ctx->data.wtpWirelessTxBroadcastMsgNum;

    return MFD_SUCCESS;
} /* wtpWirelessTxBroadcastMsgNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpStaUplinkMaxRate
 * wtpStaUplinkMaxRate is subid 14 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.14
 * Description:
(Kbps)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaUplinkMaxRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaUplinkMaxRate_map(unsigned long *mib_wtpStaUplinkMaxRate_val_ptr, unsigned long raw_wtpStaUplinkMaxRate_val)
{
    netsnmp_assert(NULL != mib_wtpStaUplinkMaxRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpStaUplinkMaxRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaUplinkMaxRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpStaUplinkMaxRate_val_ptr) = raw_wtpStaUplinkMaxRate_val;

    return MFD_SUCCESS;
} /* wtpStaUplinkMaxRate_map */

/**
 * Extract the current value of the wtpStaUplinkMaxRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaUplinkMaxRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaUplinkMaxRate_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpStaUplinkMaxRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaUplinkMaxRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpStaUplinkMaxRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaUplinkMaxRate data.
 * set (* wtpStaUplinkMaxRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaUplinkMaxRate_val_ptr ) = rowreq_ctx->data.wtpStaUplinkMaxRate;

    return MFD_SUCCESS;
} /* wtpStaUplinkMaxRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpStaDwlinkMaxRate
 * wtpStaDwlinkMaxRate is subid 15 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.15
 * Description:
.(Kbps)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaDwlinkMaxRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaDwlinkMaxRate_map(unsigned long *mib_wtpStaDwlinkMaxRate_val_ptr, unsigned long raw_wtpStaDwlinkMaxRate_val)
{
    netsnmp_assert(NULL != mib_wtpStaDwlinkMaxRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpStaDwlinkMaxRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaDwlinkMaxRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpStaDwlinkMaxRate_val_ptr) = raw_wtpStaDwlinkMaxRate_val;

    return MFD_SUCCESS;
} /* wtpStaDwlinkMaxRate_map */

/**
 * Extract the current value of the wtpStaDwlinkMaxRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaDwlinkMaxRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaDwlinkMaxRate_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpStaDwlinkMaxRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaDwlinkMaxRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpStaDwlinkMaxRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaDwlinkMaxRate data.
 * set (* wtpStaDwlinkMaxRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaDwlinkMaxRate_val_ptr ) = rowreq_ctx->data.wtpStaDwlinkMaxRate;

    return MFD_SUCCESS;
} /* wtpStaDwlinkMaxRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpNetWiredRxErrPack
 * wtpNetWiredRxErrPack is subid 16 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.16
 * Description:
The total number of error recieved packets of wired side
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNetWiredRxErrPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNetWiredRxErrPack_map(unsigned long *mib_wtpNetWiredRxErrPack_val_ptr, unsigned long raw_wtpNetWiredRxErrPack_val)
{
    netsnmp_assert(NULL != mib_wtpNetWiredRxErrPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredRxErrPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpNetWiredRxErrPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpNetWiredRxErrPack_val_ptr) = raw_wtpNetWiredRxErrPack_val;

    return MFD_SUCCESS;
} /* wtpNetWiredRxErrPack_map */

/**
 * Extract the current value of the wtpNetWiredRxErrPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetWiredRxErrPack_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpNetWiredRxErrPack_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpNetWiredRxErrPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpNetWiredRxErrPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredRxErrPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpNetWiredRxErrPack data.
 * set (* wtpNetWiredRxErrPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpNetWiredRxErrPack_val_ptr ) = rowreq_ctx->data.wtpNetWiredRxErrPack;

    return MFD_SUCCESS;
} /* wtpNetWiredRxErrPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpNetWiredRxRightPack
 * wtpNetWiredRxRightPack is subid 17 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.17
 * Description:
The total number of successful recieved packets of wired side
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNetWiredRxRightPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNetWiredRxRightPack_map(unsigned long *mib_wtpNetWiredRxRightPack_val_ptr, unsigned long raw_wtpNetWiredRxRightPack_val)
{
    netsnmp_assert(NULL != mib_wtpNetWiredRxRightPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredRxRightPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpNetWiredRxRightPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpNetWiredRxRightPack_val_ptr) = raw_wtpNetWiredRxRightPack_val;

    return MFD_SUCCESS;
} /* wtpNetWiredRxRightPack_map */

/**
 * Extract the current value of the wtpNetWiredRxRightPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetWiredRxRightPack_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpNetWiredRxRightPack_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpNetWiredRxRightPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpNetWiredRxRightPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredRxRightPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpNetWiredRxRightPack data.
 * set (* wtpNetWiredRxRightPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpNetWiredRxRightPack_val_ptr ) = rowreq_ctx->data.wtpNetWiredRxRightPack;

    return MFD_SUCCESS;
} /* wtpNetWiredRxRightPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpNetWiredRxByte
 * wtpNetWiredRxByte is subid 18 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.18
 * Description:
The total number of  recieved bytes of wired side
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNetWiredRxByte.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNetWiredRxByte_map(unsigned long long *mib_wtpNetWiredRxByte_val_ptr, unsigned long long raw_wtpNetWiredRxByte_val)
{
    netsnmp_assert(NULL != mib_wtpNetWiredRxByte_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredRxByte_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpNetWiredRxByte mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpNetWiredRxByte_val_ptr) = raw_wtpNetWiredRxByte_val;

    return MFD_SUCCESS;
} /* wtpNetWiredRxByte_map */

/**
 * Extract the current value of the wtpNetWiredRxByte data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetWiredRxByte_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpNetWiredRxByte_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wtpNetWiredRxByte_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpNetWiredRxByte_val_ptr );

/*
 * TODO:231:o: |-> Extract the current value of the wtpNetWiredRxByte data.
 * set (* wtpNetWiredRxByte_val_ptr ) from rowreq_ctx->data
 */
    (* wtpNetWiredRxByte_val_ptr ) = rowreq_ctx->data.wtpNetWiredRxByte;

    return MFD_SUCCESS;
} /* wtpNetWiredRxByte_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpNetWiredTxByte
 * wtpNetWiredTxByte is subid 19 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.19
 * Description:
The total number of  sent bytes of wired side
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNetWiredTxByte.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNetWiredTxByte_map(unsigned long long *mib_wtpNetWiredTxByte_val_ptr, unsigned long long raw_wtpNetWiredTxByte_val)
{
    netsnmp_assert(NULL != mib_wtpNetWiredTxByte_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredTxByte_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpNetWiredTxByte mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpNetWiredTxByte_val_ptr) = raw_wtpNetWiredTxByte_val;

    return MFD_SUCCESS;
} /* wtpNetWiredTxByte_map */

/**
 * Extract the current value of the wtpNetWiredTxByte data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetWiredTxByte_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpNetWiredTxByte_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long long * wtpNetWiredTxByte_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpNetWiredTxByte_val_ptr );

/*
 * TODO:231:o: |-> Extract the current value of the wtpNetWiredTxByte data.
 * set (* wtpNetWiredTxByte_val_ptr ) from rowreq_ctx->data
 */
    (* wtpNetWiredTxByte_val_ptr ) = rowreq_ctx->data.wtpNetWiredTxByte;

    return MFD_SUCCESS;
} /* wtpNetWiredTxByte_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpNetWiredTxErrPack
 * wtpNetWiredTxErrPack is subid 20 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.20
 * Description:
The total number of  sent error packages of wired side
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNetWiredTxErrPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNetWiredTxErrPack_map(unsigned long *mib_wtpNetWiredTxErrPack_val_ptr, unsigned long raw_wtpNetWiredTxErrPack_val)
{
    netsnmp_assert(NULL != mib_wtpNetWiredTxErrPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredTxErrPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpNetWiredTxErrPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpNetWiredTxErrPack_val_ptr) = raw_wtpNetWiredTxErrPack_val;

    return MFD_SUCCESS;
} /* wtpNetWiredTxErrPack_map */

/**
 * Extract the current value of the wtpNetWiredTxErrPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetWiredTxErrPack_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpNetWiredTxErrPack_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpNetWiredTxErrPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpNetWiredTxErrPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredTxErrPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpNetWiredTxErrPack data.
 * set (* wtpNetWiredTxErrPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpNetWiredTxErrPack_val_ptr ) = rowreq_ctx->data.wtpNetWiredTxErrPack;

    return MFD_SUCCESS;
} /* wtpNetWiredTxErrPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpNetWiredTxRightPack
 * wtpNetWiredTxRightPack is subid 21 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.21
 * Description:
The total number of  sent right packages of wired side
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNetWiredTxRightPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNetWiredTxRightPack_map(unsigned long *mib_wtpNetWiredTxRightPack_val_ptr, unsigned long raw_wtpNetWiredTxRightPack_val)
{
    netsnmp_assert(NULL != mib_wtpNetWiredTxRightPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredTxRightPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpNetWiredTxRightPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpNetWiredTxRightPack_val_ptr) = raw_wtpNetWiredTxRightPack_val;

    return MFD_SUCCESS;
} /* wtpNetWiredTxRightPack_map */

/**
 * Extract the current value of the wtpNetWiredTxRightPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetWiredTxRightPack_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpNetWiredTxRightPack_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpNetWiredTxRightPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpNetWiredTxRightPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredTxRightPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpNetWiredTxRightPack data.
 * set (* wtpNetWiredTxRightPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpNetWiredTxRightPack_val_ptr ) = rowreq_ctx->data.wtpNetWiredTxRightPack;

    return MFD_SUCCESS;
} /* wtpNetWiredTxRightPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpSsidTxDataAllPack
 * wtpSsidTxDataAllPack is subid 22 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.22
 * Description:
The total number of sent data packets.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSsidTxDataAllPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSsidTxDataAllPack_map(unsigned long *mib_wtpSsidTxDataAllPack_val_ptr, unsigned long raw_wtpSsidTxDataAllPack_val)
{
    netsnmp_assert(NULL != mib_wtpSsidTxDataAllPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidTxDataAllPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSsidTxDataAllPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSsidTxDataAllPack_val_ptr) = raw_wtpSsidTxDataAllPack_val;

    return MFD_SUCCESS;
} /* wtpSsidTxDataAllPack_map */

/**
 * Extract the current value of the wtpSsidTxDataAllPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidTxDataAllPack_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSsidTxDataAllPack_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpSsidTxDataAllPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSsidTxDataAllPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpSsidTxDataAllPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSsidTxDataAllPack data.
 * set (* wtpSsidTxDataAllPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSsidTxDataAllPack_val_ptr ) = rowreq_ctx->data.wtpSsidTxDataAllPack;

    return MFD_SUCCESS;
} /* wtpSsidTxDataAllPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpNetWiredRxErrPktsRate
 * wtpNetWiredRxErrPktsRate is subid 23 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.23
 * Description:
The mistake packets rate of recieved on the wired side.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNetWiredRxErrPktsRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNetWiredRxErrPktsRate_map(char **mib_wtpNetWiredRxErrPktsRate_val_ptr_ptr, size_t *mib_wtpNetWiredRxErrPktsRate_val_ptr_len_ptr, char *raw_wtpNetWiredRxErrPktsRate_val_ptr, size_t raw_wtpNetWiredRxErrPktsRate_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpNetWiredRxErrPktsRate_val_ptr);
    netsnmp_assert((NULL != mib_wtpNetWiredRxErrPktsRate_val_ptr_ptr) && (NULL != mib_wtpNetWiredRxErrPktsRate_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredRxErrPktsRate_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpNetWiredRxErrPktsRate non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpNetWiredRxErrPktsRate_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpNetWiredRxErrPktsRate_val_ptr_ptr) || (*mib_wtpNetWiredRxErrPktsRate_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpNetWiredRxErrPktsRate_val_ptr_ptr = realloc( *mib_wtpNetWiredRxErrPktsRate_val_ptr_ptr, converted_len * sizeof(**mib_wtpNetWiredRxErrPktsRate_val_ptr_ptr));
        if(NULL == *mib_wtpNetWiredRxErrPktsRate_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpNetWiredRxErrPktsRate_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpNetWiredRxErrPktsRate_val_ptr_ptr, raw_wtpNetWiredRxErrPktsRate_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpNetWiredRxErrPktsRate_map */

/**
 * Extract the current value of the wtpNetWiredRxErrPktsRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetWiredRxErrPktsRate_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpNetWiredRxErrPktsRate_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpNetWiredRxErrPktsRate.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpNetWiredRxErrPktsRate_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpNetWiredRxErrPktsRate_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpNetWiredRxErrPktsRate_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, char **wtpNetWiredRxErrPktsRate_val_ptr_ptr, size_t *wtpNetWiredRxErrPktsRate_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpNetWiredRxErrPktsRate_val_ptr_ptr) && (NULL != *wtpNetWiredRxErrPktsRate_val_ptr_ptr));
   netsnmp_assert( NULL != wtpNetWiredRxErrPktsRate_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredRxErrPktsRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpNetWiredRxErrPktsRate data.
 * set (* wtpNetWiredRxErrPktsRate_val_ptr_ptr ) and (* wtpNetWiredRxErrPktsRate_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpNetWiredRxErrPktsRate data
     */
    if ((NULL == (* wtpNetWiredRxErrPktsRate_val_ptr_ptr )) ||
        ((* wtpNetWiredRxErrPktsRate_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpNetWiredRxErrPktsRate_len * sizeof((* wtpNetWiredRxErrPktsRate_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpNetWiredRxErrPktsRate data
         */
        (* wtpNetWiredRxErrPktsRate_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpNetWiredRxErrPktsRate_len * sizeof((* wtpNetWiredRxErrPktsRate_val_ptr_ptr )[0]));
        if(NULL == (* wtpNetWiredRxErrPktsRate_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpNetWiredRxErrPktsRate_val_ptr_len_ptr ) = rowreq_ctx->data.wtpNetWiredRxErrPktsRate_len * sizeof((* wtpNetWiredRxErrPktsRate_val_ptr_ptr )[0]);
    memcpy( (* wtpNetWiredRxErrPktsRate_val_ptr_ptr ), rowreq_ctx->data.wtpNetWiredRxErrPktsRate, (* wtpNetWiredRxErrPktsRate_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpNetWiredRxErrPktsRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpNetWiredTxErrPktsRate
 * wtpNetWiredTxErrPktsRate is subid 24 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2.1.24
 * Description:
The mistake packets rate of sent on the wired side.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNetWiredTxErrPktsRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNetWiredTxErrPktsRate_map(char **mib_wtpNetWiredTxErrPktsRate_val_ptr_ptr, size_t *mib_wtpNetWiredTxErrPktsRate_val_ptr_len_ptr, char *raw_wtpNetWiredTxErrPktsRate_val_ptr, size_t raw_wtpNetWiredTxErrPktsRate_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpNetWiredTxErrPktsRate_val_ptr);
    netsnmp_assert((NULL != mib_wtpNetWiredTxErrPktsRate_val_ptr_ptr) && (NULL != mib_wtpNetWiredTxErrPktsRate_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredTxErrPktsRate_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpNetWiredTxErrPktsRate non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpNetWiredTxErrPktsRate_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpNetWiredTxErrPktsRate_val_ptr_ptr) || (*mib_wtpNetWiredTxErrPktsRate_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpNetWiredTxErrPktsRate_val_ptr_ptr = realloc( *mib_wtpNetWiredTxErrPktsRate_val_ptr_ptr, converted_len * sizeof(**mib_wtpNetWiredTxErrPktsRate_val_ptr_ptr));
        if(NULL == *mib_wtpNetWiredTxErrPktsRate_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpNetWiredTxErrPktsRate_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpNetWiredTxErrPktsRate_val_ptr_ptr, raw_wtpNetWiredTxErrPktsRate_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpNetWiredTxErrPktsRate_map */

/**
 * Extract the current value of the wtpNetWiredTxErrPktsRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetWiredTxErrPktsRate_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpNetWiredTxErrPktsRate_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpNetWiredTxErrPktsRate.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpNetWiredTxErrPktsRate_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpNetWiredTxErrPktsRate_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpNetWiredTxErrPktsRate_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, char **wtpNetWiredTxErrPktsRate_val_ptr_ptr, size_t *wtpNetWiredTxErrPktsRate_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpNetWiredTxErrPktsRate_val_ptr_ptr) && (NULL != *wtpNetWiredTxErrPktsRate_val_ptr_ptr));
   netsnmp_assert( NULL != wtpNetWiredTxErrPktsRate_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpNetWiredTxErrPktsRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpNetWiredTxErrPktsRate data.
 * set (* wtpNetWiredTxErrPktsRate_val_ptr_ptr ) and (* wtpNetWiredTxErrPktsRate_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpNetWiredTxErrPktsRate data
     */
    if ((NULL == (* wtpNetWiredTxErrPktsRate_val_ptr_ptr )) ||
        ((* wtpNetWiredTxErrPktsRate_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpNetWiredTxErrPktsRate_len * sizeof((* wtpNetWiredTxErrPktsRate_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpNetWiredTxErrPktsRate data
         */
        (* wtpNetWiredTxErrPktsRate_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpNetWiredTxErrPktsRate_len * sizeof((* wtpNetWiredTxErrPktsRate_val_ptr_ptr )[0]));
        if(NULL == (* wtpNetWiredTxErrPktsRate_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpNetWiredTxErrPktsRate_val_ptr_len_ptr ) = rowreq_ctx->data.wtpNetWiredTxErrPktsRate_len * sizeof((* wtpNetWiredTxErrPktsRate_val_ptr_ptr )[0]);
    memcpy( (* wtpNetWiredTxErrPktsRate_val_ptr_ptr ), rowreq_ctx->data.wtpNetWiredTxErrPktsRate, (* wtpNetWiredTxErrPktsRate_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpNetWiredTxErrPktsRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanDataPktsEntry.wtpUsrWirelessMacRecvDataPack
 * wtpUsrWirelessMacRecvDataPack is subid 27 of dot11WtpWlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.2.1.27
 * Description:
The receive packages at user side(Wireless side).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpUsrWirelessMacRecvDataPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpUsrWirelessMacRecvDataPack_map(unsigned long *mib_wtpUsrWirelessMacRecvDataPack_val_ptr, unsigned long raw_wtpUsrWirelessMacRecvDataPack_val)
{
    netsnmp_assert(NULL != mib_wtpUsrWirelessMacRecvDataPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpUsrWirelessMacRecvDataPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpUsrWirelessMacRecvDataPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpUsrWirelessMacRecvDataPack_val_ptr) = raw_wtpUsrWirelessMacRecvDataPack_val;

    return MFD_SUCCESS;
} /* wtpUsrWirelessMacRecvDataPack_map */

/**
 * Extract the current value of the wtpUsrWirelessMacRecvDataPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUsrWirelessMacRecvDataPack_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpUsrWirelessMacRecvDataPack_get( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpUsrWirelessMacRecvDataPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpUsrWirelessMacRecvDataPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:wtpUsrWirelessMacRecvDataPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpUsrWirelessMacRecvDataPack data.
 * set (* wtpUsrWirelessMacRecvDataPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpUsrWirelessMacRecvDataPack_val_ptr ) = rowreq_ctx->data.wtpUsrWirelessMacRecvDataPack;

    return MFD_SUCCESS;
} /* wtpUsrWirelessMacRecvDataPack_get */



/** @} */
