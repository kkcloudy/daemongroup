/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpWlanDataPktsTable.h"
#include "dot11WtpWlanDataPktsTable_data_access.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "../mibs_public.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpWlanDataPktsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpWlanDataPktsTable is subid 2 of wtpWlan.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.2, length: 12
*/

/**
 * initialization for dot11WtpWlanDataPktsTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11WtpWlanDataPktsTable_reg
 *        Pointer to dot11WtpWlanDataPktsTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11WtpWlanDataPktsTable_init_data(dot11WtpWlanDataPktsTable_registration_ptr dot11WtpWlanDataPktsTable_reg)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:dot11WtpWlanDataPktsTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11WtpWlanDataPktsTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11WtpWlanDataPktsTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11WtpWlanDataPktsTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:dot11WtpWlanDataPktsTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11WtpWlanDataPktsTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11WtpWlanDataPktsTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11WTPWLANDATAPKTSTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11WtpWlanDataPktsTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11WtpWlanDataPktsTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11WtpWlanDataPktsTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11WtpWlanDataPktsTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11WtpWlanDataPktsTable_cache_load\n");

    dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
         */
        /** 128 - 1(entry) - 1(col) - 1(other indexes) = 111 */
   char   wtpMacAddr[20] = { 0 };
   size_t      wtpMacAddr_len = 0;
        /*
         * wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   wlanCurrID = 0;

    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:dot11WtpWlanDataPktsTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
       // return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11WtpWlanDataPktsTable container.
     * loop over your dot11WtpWlanDataPktsTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */

	char WirelessErrPktsRate[10] = { 0 };
	char wtpWirelessErrPktsRate[255] = { 0 };
	size_t wtpWirelessErrPktsRate_len = 0;
	char NetWiredRxErrPktsRate[10] = { 0 };
	char wtpNetWiredRxErrPktsRate[255] = { 0 };
	size_t wtpNetWiredRxErrPktsRate_len = 0;
	char NetWiredTxErrPktsRate[10] = { 0 };
	char wtpNetWiredTxErrPktsRate[255] = { 0 };
	size_t wtpNetWiredTxErrPktsRate_len = 0;

    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_all_wtp_wlan_data_pkts_information_cmd_v2\n");
    messageHead = list_connection_call_dbus_method(show_all_wtp_wlan_data_pkts_information_cmd_v2, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_all_wtp_wlan_data_pkts_information_cmd_v2,messageHead=%p\n", messageHead);

	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    struct WtpWlanDataPktsInfo *WtpHead = messageNode->message;
		    if(WtpHead)
		    {
		        struct WtpWlanDataPktsInfo *q = NULL;
		        
        		for(q = WtpHead; q != NULL; q = q->next)
        		{
        			memset(wtpMacAddr,0,sizeof(wtpMacAddr));
					if(q->wtpMacAddr)
					{
						wtpMacAddr_len = MIN(strlen(q->wtpMacAddr),sizeof(wtpMacAddr)-1);
						memcpy(wtpMacAddr,q->wtpMacAddr,wtpMacAddr_len);
					}
        			
        			struct WlanDataPktsInfo* WlanNode = NULL;
        			for(WlanNode=q->wlan_list; WlanNode != NULL; WlanNode=WlanNode->next)
        			{
        				wlanCurrID = local_to_global_ID(messageNode->parameter, WlanNode->wlanCurrID, WIRELESS_MAX_NUM);
   
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * get a line (skip blank lines)
     */
    //do {
      //  if (!fgets(line, sizeof(line), filep)) {
            /* we're done */
        //    fclose(filep);
          //  filep = NULL;
        //}
    //} while (filep && (line[0] == '\n'));

    /*
     * check for end of data
     */
    //if(NULL == filep)
      //  break;

    /*
     * parse line into variables
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

        /*
         * TODO:352:M: |   |-> set indexes in new dot11WtpWlanDataPktsTable rowreq context.
         */
        rowreq_ctx = dot11WtpWlanDataPktsTable_allocate_rowreq_ctx();
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
            free_dbus_message_list(&messageHead, Free_show_all_wtp_wlan_data_pkts_information_cmd_v2);
            return MFD_RESOURCE_UNAVAILABLE;
        }
        if(MFD_SUCCESS != dot11WtpWlanDataPktsTable_indexes_set(rowreq_ctx
                               , wtpMacAddr, wtpMacAddr_len
                               , wlanCurrID
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "dot11WtpWlanDataPktsTable cache.\n");
            dot11WtpWlanDataPktsTable_release_rowreq_ctx(rowreq_ctx);
            continue;
        }

        /*
         * TODO:352:r: |   |-> populate dot11WtpWlanDataPktsTable data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for wtpSsidTxTermAllByte
     * wtpSsidTxTermAllByte(1)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpSsidTxTermAllByte mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpSsidTxTermAllByte = WlanNode->wtpSsidSendTermAllByte;
    
    /*
     * setup/save data for wtpSsidRxTermAllPack
     * wtpSsidRxTermAllPack(2)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpSsidRxTermAllPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpSsidRxTermAllPack = WlanNode->wtpSsidRecvTermAllPack;
    
    /*
     * setup/save data for wtpSsidRxTermAllByte
     * wtpSsidRxTermAllByte(3)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpSsidRxTermAllByte mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpSsidRxTermAllByte = WlanNode->wtpSsidRecvTermAllByte;
    
    /*
     * setup/save data for wtpSsidRxDataRightPack
     * wtpSsidRxDataRightPack(4)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpSsidRxDataRightPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpSsidWirelessMacRxDataRightByte = WlanNode->wtpSsidWirelessMacRecvDataRightByte;
    
    /*
     * setup/save data for wtpSsidTxDataRightPack
     * wtpSsidTxDataRightPack(5)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpSsidTxDataRightPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpSsidWirelessMacTxDataRightByte = WlanNode->wtpSsidWirelessMacSendDataRightByte;
    
    /*
     * setup/save data for wtpSsidRxDataErrPack
     * wtpSsidRxDataErrPack(6)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpSsidRxDataErrPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpSsidRxDataErrPack = WlanNode->wtpSsidWiredMacRecvDataWrongPack;
    
    /*
     * setup/save data for wtpNetWiredRxPack
     * wtpNetWiredRxPack(7)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpNetWiredRxPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpNetWiredRxPack = WlanNode->wtpNetWiredRecvPack;
    
    /*
     * setup/save data for wtpSsidTxTermAllPack
     * wtpSsidTxTermAllPack(8)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpSsidTxTermAllPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpSsidTxTermAllPack = WlanNode->wtpUsrWirelessMacSendDataPack;
    
    /*
     * setup/save data for wtpNetWiredTxPack
     * wtpNetWiredTxPack(9)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpNetWiredTxPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpNetWiredTxPack = WlanNode->wtpNetWiredSendPack;
    
    /*
     * setup/save data for wtpSsidTxDataErrPkts
     * wtpSsidTxDataErrPkts(10)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpSsidTxDataErrPkts mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpSsidTxDataErrPkts = WlanNode->WtpWirelessSendFailPkts;
    
    /*
     * setup/save data for wtpWirelessReTxPkts
     * wtpWirelessReTxPkts(11)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessReTxPkts mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessReTxPkts = WlanNode->wtpWirelessResendPkts;
    
    /*
     * setup/save data for wtpWirelessErrPktsRate
     * wtpWirelessErrPktsRate(12)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessErrPktsRate mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpWirelessErrPktsRate_map(&rowreq_ctx->data.wtpWirelessErrPktsRate, &rowreq_ctx->data.wtpWirelessErrPktsRate_len,
     *                wtpWirelessErrPktsRate, wtpWirelessErrPktsRate_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpWirelessErrPktsRate data
     */
    /*if ((NULL == rowreq_ctx->data.wtpWirelessErrPktsRate) ||
        (rowreq_ctx->data.wtpWirelessErrPktsRate_len < (wtpWirelessErrPktsRate_len * sizeof(rowreq_ctx->data.wtpWirelessErrPktsRate[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/

	memset(WirelessErrPktsRate, 0, 10);
	if(WlanNode->wtpWirelessWrongPktsRate)
	{
		snprintf(WirelessErrPktsRate,sizeof(WirelessErrPktsRate)-1,"%s",WlanNode->wtpWirelessWrongPktsRate);
		strncat(WirelessErrPktsRate,"%",sizeof(WirelessErrPktsRate)-strlen(WirelessErrPktsRate)-1);
	}
	
	wtpWirelessErrPktsRate_len = MIN(strlen(WirelessErrPktsRate),sizeof(wtpWirelessErrPktsRate)-1);
	memset(wtpWirelessErrPktsRate,0,sizeof(wtpWirelessErrPktsRate));
	memcpy(wtpWirelessErrPktsRate,WirelessErrPktsRate,wtpWirelessErrPktsRate_len);
	
    rowreq_ctx->data.wtpWirelessErrPktsRate_len = wtpWirelessErrPktsRate_len * sizeof(rowreq_ctx->data.wtpWirelessErrPktsRate[0]);
    memcpy( rowreq_ctx->data.wtpWirelessErrPktsRate, wtpWirelessErrPktsRate, rowreq_ctx->data.wtpWirelessErrPktsRate_len );
    
    /*
     * setup/save data for wtpWirelessTxBroadcastMsgNum
     * wtpWirelessTxBroadcastMsgNum(13)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessTxBroadcastMsgNum mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessTxBroadcastMsgNum = WlanNode->wtpWirelessSendBroadcastMsgNum;
    
    /*
     * setup/save data for wtpStaUplinkMaxRate
     * wtpStaUplinkMaxRate(14)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpStaUplinkMaxRate mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpStaUplinkMaxRate = WlanNode->wtpStaUplinkMaxRate;
    
    /*
     * setup/save data for wtpStaDwlinkMaxRate
     * wtpStaDwlinkMaxRate(15)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpStaDwlinkMaxRate mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpStaDwlinkMaxRate = WlanNode->wtpStaDwlinkMaxRate;
    
    /*
     * setup/save data for wtpNetWiredRxErrPack
     * wtpNetWiredRxErrPack(16)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpNetWiredRxErrPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpNetWiredRxErrPack = WlanNode->wtpNetWiredRecvErrPack;
    
    /*
     * setup/save data for wtpNetWiredRxRightPack
     * wtpNetWiredRxRightPack(17)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpNetWiredRxRightPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpNetWiredRxRightPack = WlanNode->wtpNetWiredRecvRightPack;
    
    /*
     * setup/save data for wtpNetWiredRxByte
     * wtpNetWiredRxByte(18)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpNetWiredRxByte mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpNetWiredRxByte = WlanNode->wtpNetWiredRecvByte;
    
    /*
     * setup/save data for wtpNetWiredTxByte
     * wtpNetWiredTxByte(19)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpNetWiredTxByte mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpNetWiredTxByte = WlanNode->wtpNetWiredSendByte;
    
    /*
     * setup/save data for wtpNetWiredTxErrPack
     * wtpNetWiredTxErrPack(20)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpNetWiredTxErrPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpNetWiredTxErrPack = WlanNode->wtpNetWiredSendErrPack;
    
    /*
     * setup/save data for wtpNetWiredTxRightPack
     * wtpNetWiredTxRightPack(21)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpNetWiredTxRightPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpNetWiredTxRightPack = WlanNode->wtpNetWiredSendRightPack;
    
    /*
     * setup/save data for wtpSsidTxDataAllPack
     * wtpSsidTxDataAllPack(22)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpSsidTxDataAllPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpSsidTxDataAllPack = WlanNode->wtpSsidSendDataAllPack;
    
    /*
     * setup/save data for wtpNetWiredRxErrPktsRate
     * wtpNetWiredRxErrPktsRate(23)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpNetWiredRxErrPktsRate mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpNetWiredRxErrPktsRate_map(&rowreq_ctx->data.wtpNetWiredRxErrPktsRate, &rowreq_ctx->data.wtpNetWiredRxErrPktsRate_len,
     *                wtpNetWiredRxErrPktsRate, wtpNetWiredRxErrPktsRate_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpNetWiredRxErrPktsRate data
     */
    /*if ((NULL == rowreq_ctx->data.wtpNetWiredRxErrPktsRate) ||
        (rowreq_ctx->data.wtpNetWiredRxErrPktsRate_len < (wtpNetWiredRxErrPktsRate_len * sizeof(rowreq_ctx->data.wtpNetWiredRxErrPktsRate[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/

	memset(NetWiredRxErrPktsRate, 0, 10);
	if(WlanNode->wtpNetWiredRxWrongPktsRate)
	{
		snprintf(NetWiredRxErrPktsRate,sizeof(NetWiredRxErrPktsRate)-1,"%s",WlanNode->wtpNetWiredRxWrongPktsRate);
		strncat(NetWiredRxErrPktsRate,"%",sizeof(NetWiredRxErrPktsRate)-strlen(NetWiredRxErrPktsRate)-1);
	}

	wtpNetWiredRxErrPktsRate_len = MIN(strlen(NetWiredRxErrPktsRate),sizeof(wtpNetWiredRxErrPktsRate)-1);
	memset(wtpNetWiredRxErrPktsRate,0,sizeof(wtpNetWiredRxErrPktsRate));
	memcpy(wtpNetWiredRxErrPktsRate,NetWiredRxErrPktsRate,wtpNetWiredRxErrPktsRate_len);
	
    rowreq_ctx->data.wtpNetWiredRxErrPktsRate_len = wtpNetWiredRxErrPktsRate_len * sizeof(rowreq_ctx->data.wtpNetWiredRxErrPktsRate[0]);
    memcpy( rowreq_ctx->data.wtpNetWiredRxErrPktsRate, wtpNetWiredRxErrPktsRate, rowreq_ctx->data.wtpNetWiredRxErrPktsRate_len );
    
    /*
     * setup/save data for wtpNetWiredTxErrPktsRate
     * wtpNetWiredTxErrPktsRate(24)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpNetWiredTxErrPktsRate mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpNetWiredTxErrPktsRate_map(&rowreq_ctx->data.wtpNetWiredTxErrPktsRate, &rowreq_ctx->data.wtpNetWiredTxErrPktsRate_len,
     *                wtpNetWiredTxErrPktsRate, wtpNetWiredTxErrPktsRate_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpNetWiredTxErrPktsRate data
     */
    /*if ((NULL == rowreq_ctx->data.wtpNetWiredTxErrPktsRate) ||
        (rowreq_ctx->data.wtpNetWiredTxErrPktsRate_len < (wtpNetWiredTxErrPktsRate_len * sizeof(rowreq_ctx->data.wtpNetWiredTxErrPktsRate[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/

	memset(NetWiredTxErrPktsRate, 0, 10);
	if(WlanNode->wtpNetWiredTxWrongPktsRate)
	{
		snprintf(NetWiredTxErrPktsRate,sizeof(NetWiredTxErrPktsRate)-1,"%s",WlanNode->wtpNetWiredTxWrongPktsRate);
		strncat(NetWiredTxErrPktsRate,"%",sizeof(NetWiredTxErrPktsRate)-strlen(NetWiredTxErrPktsRate)-1);
	}

	wtpNetWiredTxErrPktsRate_len = MIN(strlen(NetWiredTxErrPktsRate),sizeof(wtpNetWiredTxErrPktsRate)-1);
	memset(wtpNetWiredTxErrPktsRate,0,sizeof(wtpNetWiredTxErrPktsRate));
	memcpy(wtpNetWiredTxErrPktsRate,NetWiredTxErrPktsRate,wtpNetWiredTxErrPktsRate_len);
	
    rowreq_ctx->data.wtpNetWiredTxErrPktsRate_len = wtpNetWiredTxErrPktsRate_len * sizeof(rowreq_ctx->data.wtpNetWiredTxErrPktsRate[0]);
    memcpy( rowreq_ctx->data.wtpNetWiredTxErrPktsRate, wtpNetWiredTxErrPktsRate, rowreq_ctx->data.wtpNetWiredTxErrPktsRate_len );
    
    /*
     * setup/save data for wtpUsrWirelessMacRecvDataPack
     * wtpUsrWirelessMacRecvDataPack(27)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpUsrWirelessMacRecvDataPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpUsrWirelessMacRecvDataPack = WlanNode->wtpUsrWirelessMacRecvDataPack;
    
        
        /*
         * insert into table container
         */
    			        if(CONTAINER_INSERT(container, rowreq_ctx))
    			        {
    						dot11WtpWlanDataPktsTable_release_rowreq_ctx(rowreq_ctx);
    			        }
    			        ++count;
        			}			
    			}
    		}
    	}
        free_dbus_message_list(&messageHead, Free_show_all_wtp_wlan_data_pkts_information_cmd_v2);
	}


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
      //  fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11WtpWlanDataPktsTable:dot11WtpWlanDataPktsTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11WtpWlanDataPktsTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11WtpWlanDataPktsTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11WtpWlanDataPktsTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:dot11WtpWlanDataPktsTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11WtpWlanDataPktsTable cache.
     */
} /* dot11WtpWlanDataPktsTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11WtpWlanDataPktsTable_row_prep( dot11WtpWlanDataPktsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanDataPktsTable:dot11WtpWlanDataPktsTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11WtpWlanDataPktsTable_row_prep */

/** @} */
