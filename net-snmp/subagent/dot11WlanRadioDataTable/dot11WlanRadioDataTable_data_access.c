/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WlanRadioDataTable.h"


#include "dot11WlanRadioDataTable_data_access.h"
#include "../mibs_public.h"
//#include "ws_sysinfo.h"
//#include "ws_security.h"
//#include "ws_sta.h"
#include "autelanWtpGroup.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"


/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WlanRadioDataTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WlanRadioDataTable is subid 2 of wlanData.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.16.2, length: 12
*/

/**
 * initialization for dot11WlanRadioDataTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11WlanRadioDataTable_reg
 *        Pointer to dot11WlanRadioDataTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11WlanRadioDataTable_init_data(dot11WlanRadioDataTable_registration_ptr dot11WlanRadioDataTable_reg)
{
    DEBUGMSGTL(("verbose:dot11WlanRadioDataTable:dot11WlanRadioDataTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11WlanRadioDataTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11WlanRadioDataTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11WlanRadioDataTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11WlanRadioDataTable:dot11WlanRadioDataTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11WlanRadioDataTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11WlanRadioDataTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11WLANRADIODATATABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11WlanRadioDataTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11WlanRadioDataTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11WlanRadioDataTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11WlanRadioDataTable_cache_load(netsnmp_container *container)
{
    dot11WlanRadioDataTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   wlanCurrID;

    DEBUGMSGTL(("verbose:dot11WlanRadioDataTable:dot11WlanRadioDataTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
    //    return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11WlanRadioDataTable container.
     * loop over your dot11WlanRadioDataTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
     
    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_wlan_list\n");
    messageHead = list_connection_call_dbus_method(show_wlan_list, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_wlan_list,messageHead=%p\n", messageHead);

	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    DCLI_WLAN_API_GROUP *wlaninfo = messageNode->message;
		    if(wlaninfo)
		    {
		        void *connection = NULL;
                if(SNMPD_DBUS_ERROR == get_slot_dbus_connection(messageNode->parameter.slot_id, &connection, SNMPD_INSTANCE_MASTER_V3))
                    continue;
                    
                int ret = 0;
                struct SSIDStatsInfo *WtpHead = NULL;
                ret = show_all_wlan_ssid_stats_information_of_all_radio_cmd(messageNode->parameter, connection, &WtpHead);
                if(1 != ret || NULL == WtpHead)
                    continue;
                    
                int i = 0;
        		for(i = 0; i<wlaninfo->wlan_num; i++)
        		{
            		unsigned long long  WlanradioRecvFlow = 0;
                	unsigned long long  WlanradioSendFlow = 0;
                	unsigned long long WlanradioReSendFrames = 0;
                	unsigned long long WlanradioSendDiscardFrames = 0;
                	unsigned long long WlanradioSendErrFrames = 0;
                	unsigned long long WlanradioRecvDiscardFrames = 0;
                	unsigned long long WlanradioReRecvFrames = 0;
                	long WlanradioRecvSpeed = 0;
                	long WlanradioSendSpeed = 0;

                    wlanCurrID = local_to_global_ID(messageNode->parameter, wlaninfo->WLAN[i]->WlanID, WIRELESS_MAX_NUM);
                        
                    struct SSIDStatsInfo *showNode = NULL;
                    for(showNode = WtpHead->SSIDStatsInfo_list; NULL != showNode; showNode = showNode->next)
                    {
                        int j = 0;
                        struct SSIDStatsInfo_Radioid_info *Radioid_info_node = NULL;
                    	for( Radioid_info_node = showNode->SSIDStatsInfo_Radioid_info_head,j=0; (NULL != Radioid_info_node)&&(j<showNode->wtpBwlanRadioNum); Radioid_info_node = Radioid_info_node->next,j++ )
                    	{
                    	    int k = 0;
                    	    struct SSIDStatsInfo_sub_wlan *sub_wlan = NULL;
                    		for(sub_wlan = Radioid_info_node->SSIDStatsInfo_sub_wlan_head,k=0; (NULL != sub_wlan)&&(k<Radioid_info_node->radioBwlanNum); sub_wlan = sub_wlan->next, k++)
                    		{
                              if(wlaninfo->WLAN[i]->WlanID == Radioid_info_node->RadioidSupportSSID[k])
                              {		
                    			  WlanradioRecvFlow += sub_wlan->wl_up_flow;
                    			  WlanradioSendFlow += sub_wlan->wl_dw_flow;
                    			  WlanradioReSendFrames += sub_wlan->ch_dw_resend_frm;
                    			  WlanradioSendDiscardFrames += sub_wlan->ch_dw_los_frm;
                    			  WlanradioSendErrFrames += sub_wlan->ch_dw_err_frm;
                    			  WlanradioRecvDiscardFrames += sub_wlan->ch_up_los_frm;
                    			  WlanradioReRecvFrames += sub_wlan->ch_up_resend_frm;
                    			  WlanradioRecvSpeed += sub_wlan->WlanradioRecvSpeed;
                    			  WlanradioSendSpeed += sub_wlan->WlanradioSendSpeed;
                    		    }
                    		}
                    	}
                    }
                    


		        
			/*
			***************************************************
			*** 			START EXAMPLE CODE				***
			***---------------------------------------------***/
			/*
			 * get a line (skip blank lines)
			 */
			//do {
			//	  if (!fgets(line, sizeof(line), filep)) {
			//		  /* we're done */
			//		  fclose(filep);
			//		  filep = NULL;
			//	  }
			//} while (filep && (line[0] == '\n'));
		
			/*
			 * check for end of data
			 */
			//if(NULL == filep)
			//	  break;
		
			/*
			 * parse line into variables
			 */
			/*
			***---------------------------------------------***
			*** 			 END  EXAMPLE CODE				***
			***************************************************/
		
				/*
				 * TODO:352:M: |   |-> set indexes in new dot11WlanRadioDataTable rowreq context.
				 */

				rowreq_ctx = dot11WlanRadioDataTable_allocate_rowreq_ctx();
				if (NULL == rowreq_ctx) {
					snmp_log(LOG_ERR, "memory allocation failed\n");
					Free_show_all_wlan_ssid_stats_information_of_all_radio_cmd(WtpHead);
                    free_dbus_message_list(&messageHead, Free_wlan_head);
					return MFD_RESOURCE_UNAVAILABLE;
				}
				if(MFD_SUCCESS != dot11WlanRadioDataTable_indexes_set(rowreq_ctx
									   , wlanCurrID
					   )) {
					snmp_log(LOG_ERR,"error setting index while loading "
							 "dot11WlanRadioDataTable cache.\n");
					dot11WlanRadioDataTable_release_rowreq_ctx(rowreq_ctx);
					continue;
				}
		
				/*
				 * TODO:352:r: |   |-> populate dot11WlanRadioDataTable data context.
				 * Populate data context here. (optionally, delay until row prep)
				 */
			/*
			 * TRANSIENT or semi-TRANSIENT data:
			 * copy data or save any info needed to do it in row_prep.
			 */
			/*
			 * setup/save data for WlanradioRecvFlow
			 * WlanradioRecvFlow(1)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
			 */
			/*
			 * TODO:246:r: |-> Define WlanradioRecvFlow mapping.
			 * Map values between raw/native values and MIB values
			 *
			 * Integer based value can usually just do a direct copy.
			 */
			 
			rowreq_ctx->data.WlanradioRecvFlow = WlanradioRecvFlow;
			
			/*
			 * setup/save data for WlanradioSendFlow
			 * WlanradioSendFlow(2)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
			 */
			/*
			 * TODO:246:r: |-> Define WlanradioSendFlow mapping.
			 * Map values between raw/native values and MIB values
			 *
			 * Integer based value can usually just do a direct copy.
			 */
			rowreq_ctx->data.WlanradioSendFlow = WlanradioSendFlow;
			
			/*
			 * setup/save data for WlanradioReSendFrames
			 * WlanradioReSendFrames(3)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
			 */
			/*
			 * TODO:246:r: |-> Define WlanradioReSendFrames mapping.
			 * Map values between raw/native values and MIB values
			 *
			 * Integer based value can usually just do a direct copy.
			 */
			rowreq_ctx->data.WlanradioReSendFrames = WlanradioReSendFrames;
			
			/*
			 * setup/save data for WlanradioSendDiscardFrames
			 * WlanradioSendDiscardFrames(4)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
			 */
			/*
			 * TODO:246:r: |-> Define WlanradioSendDiscardFrames mapping.
			 * Map values between raw/native values and MIB values
			 *
			 * Integer based value can usually just do a direct copy.
			 */
			rowreq_ctx->data.WlanradioSendDiscardFrames = WlanradioSendDiscardFrames;
			
			/*
			 * setup/save data for WlanradioSendErrFrames
			 * WlanradioSendErrFrames(5)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
			 */
			/*
			 * TODO:246:r: |-> Define WlanradioSendErrFrames mapping.
			 * Map values between raw/native values and MIB values
			 *
			 * Integer based value can usually just do a direct copy.
			 */
			rowreq_ctx->data.WlanradioSendErrFrames = WlanradioSendErrFrames;
			
			/*
			 * setup/save data for WlanradioRecvDiscardFrames
			 * WlanradioRecvDiscardFrames(6)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
			 */
			/*
			 * TODO:246:r: |-> Define WlanradioRecvDiscardFrames mapping.
			 * Map values between raw/native values and MIB values
			 *
			 * Integer based value can usually just do a direct copy.
			 */
			rowreq_ctx->data.WlanradioRecvDiscardFrames = WlanradioRecvDiscardFrames;
			
			/*
			 * setup/save data for WlanradioReRecvFrames
			 * WlanradioReRecvFrames(7)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
			 */
			/*
			 * TODO:246:r: |-> Define WlanradioReRecvFrames mapping.
			 * Map values between raw/native values and MIB values
			 *
			 * Integer based value can usually just do a direct copy.
			 */
			rowreq_ctx->data.WlanradioReRecvFrames = WlanradioReRecvFrames;
			
			/*
			 * setup/save data for WlanradioRecvSpeed
			 * WlanradioRecvSpeed(8)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
			 */
			/*
			 * TODO:246:r: |-> Define WlanradioRecvSpeed mapping.
			 * Map values between raw/native values and MIB values
			 *
			 * Integer based value can usually just do a direct copy.
			 */
			rowreq_ctx->data.WlanradioRecvSpeed = WlanradioRecvSpeed;
			
			/*
			 * setup/save data for WlanradioSendSpeed
			 * WlanradioSendSpeed(9)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
			 */
			/*
			 * TODO:246:r: |-> Define WlanradioSendSpeed mapping.
			 * Map values between raw/native values and MIB values
			 *
			 * Integer based value can usually just do a direct copy.
			 */
			rowreq_ctx->data.WlanradioSendSpeed = WlanradioSendSpeed;
			
				/*
				 * insert into table container
				 */

    				if(CONTAINER_INSERT(container, rowreq_ctx))
    				{
    					  dot11WlanRadioDataTable_release_rowreq_ctx(rowreq_ctx);
    				}
    				++count;
        		
        		}
        		Free_show_all_wlan_ssid_stats_information_of_all_radio_cmd(WtpHead);
        	}
        }
        free_dbus_message_list(&messageHead, Free_wlan_head);
    }




    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
    //    fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11WlanRadioDataTable:dot11WlanRadioDataTable_cache_load",
               "inserted %d records\n", count));

    return MFD_SUCCESS;
} /* dot11WlanRadioDataTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11WlanRadioDataTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11WlanRadioDataTable:dot11WlanRadioDataTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11WlanRadioDataTable cache.
     */
} /* dot11WlanRadioDataTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11WlanRadioDataTable_row_prep( dot11WlanRadioDataTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WlanRadioDataTable:dot11WlanRadioDataTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11WlanRadioDataTable_row_prep */

/** @} */
