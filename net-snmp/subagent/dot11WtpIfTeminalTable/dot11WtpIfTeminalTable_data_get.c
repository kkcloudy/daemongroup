/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpIfTeminalTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpIfTeminalTable get routines.
 * TODO:240:M: Implement dot11WtpIfTeminalTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpIfTeminalTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpIfTeminalTable is subid 4 of wlanPortal.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.4, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpIfTeminalTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11WtpIfTeminalTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpRadCurrID
 * wtpRadCurrID is subid 1 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.1
 * Description:
Current radio ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpRadCurrID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpRadCurrID_dot11WtpIfTeminalTable_map(long *mib_wtpRadCurrID_val_ptr, long raw_wtpRadCurrID_val)
{
    netsnmp_assert(NULL != mib_wtpRadCurrID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:wtpRadCurrID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpRadCurrID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpRadCurrID_val_ptr) = raw_wtpRadCurrID_val;

    return MFD_SUCCESS;
} /* wtpRadCurrID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpIfTeminalTable_indexes_set_tbl_idx(dot11WtpIfTeminalTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpRadCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:dot11WtpIfTeminalTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wtpRadCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wtpRadCurrID = wtpRadCurrID_val;
    

    return MFD_SUCCESS;
} /* dot11WtpIfTeminalTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpIfTeminalTable_indexes_set(dot11WtpIfTeminalTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpRadCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:dot11WtpIfTeminalTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpIfTeminalTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpRadCurrID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpIfTeminalTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpIfTeminalTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfTeminalEntry.pIfCurusernum
 * pIfCurusernum is subid 1 of dot11WtpIfTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.4.1.1
 * Description:
The ap depend on portal current user total number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-pIfCurusernum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
pIfCurusernum_map(u_long *mib_pIfCurusernum_val_ptr, u_long raw_pIfCurusernum_val)
{
    netsnmp_assert(NULL != mib_pIfCurusernum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:pIfCurusernum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement pIfCurusernum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_pIfCurusernum_val_ptr) = raw_pIfCurusernum_val;

    return MFD_SUCCESS;
} /* pIfCurusernum_map */

/**
 * Extract the current value of the pIfCurusernum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pIfCurusernum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pIfCurusernum_get( dot11WtpIfTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pIfCurusernum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pIfCurusernum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:pIfCurusernum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pIfCurusernum data.
 * set (* pIfCurusernum_val_ptr ) from rowreq_ctx->data
 */
    (* pIfCurusernum_val_ptr ) = rowreq_ctx->data.pIfCurusernum;

    return MFD_SUCCESS;
} /* pIfCurusernum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfTeminalEntry.pIfLogoffnum
 * pIfLogoffnum is subid 2 of dot11WtpIfTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.4.1.2
 * Description:
The ap depend on portal fail number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-pIfLogoffnum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
pIfLogoffnum_map(u_long *mib_pIfLogoffnum_val_ptr, u_long raw_pIfLogoffnum_val)
{
    netsnmp_assert(NULL != mib_pIfLogoffnum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:pIfLogoffnum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement pIfLogoffnum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_pIfLogoffnum_val_ptr) = raw_pIfLogoffnum_val;

    return MFD_SUCCESS;
} /* pIfLogoffnum_map */

/**
 * Extract the current value of the pIfLogoffnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pIfLogoffnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pIfLogoffnum_get( dot11WtpIfTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pIfLogoffnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pIfLogoffnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:pIfLogoffnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pIfLogoffnum data.
 * set (* pIfLogoffnum_val_ptr ) from rowreq_ctx->data
 */
    (* pIfLogoffnum_val_ptr ) = rowreq_ctx->data.pIfLogoffnum;

    return MFD_SUCCESS;
} /* pIfLogoffnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfTeminalEntry.pIfReqnum
 * pIfReqnum is subid 3 of dot11WtpIfTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.4.1.3
 * Description:
The ap depend on portal request number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-pIfReqnum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
pIfReqnum_map(u_long *mib_pIfReqnum_val_ptr, u_long raw_pIfReqnum_val)
{
    netsnmp_assert(NULL != mib_pIfReqnum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:pIfReqnum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement pIfReqnum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_pIfReqnum_val_ptr) = raw_pIfReqnum_val;

    return MFD_SUCCESS;
} /* pIfReqnum_map */

/**
 * Extract the current value of the pIfReqnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pIfReqnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pIfReqnum_get( dot11WtpIfTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pIfReqnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pIfReqnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:pIfReqnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pIfReqnum data.
 * set (* pIfReqnum_val_ptr ) from rowreq_ctx->data
 */
    (* pIfReqnum_val_ptr ) = rowreq_ctx->data.pIfReqnum;

    return MFD_SUCCESS;
} /* pIfReqnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfTeminalEntry.pIfReqsuccnum
 * pIfReqsuccnum is subid 4 of dot11WtpIfTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.4.1.4
 * Description:
The ap depend on portal request success number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-pIfReqsuccnum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
pIfReqsuccnum_map(u_long *mib_pIfReqsuccnum_val_ptr, u_long raw_pIfReqsuccnum_val)
{
    netsnmp_assert(NULL != mib_pIfReqsuccnum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:pIfReqsuccnum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement pIfReqsuccnum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_pIfReqsuccnum_val_ptr) = raw_pIfReqsuccnum_val;

    return MFD_SUCCESS;
} /* pIfReqsuccnum_map */

/**
 * Extract the current value of the pIfReqsuccnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pIfReqsuccnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pIfReqsuccnum_get( dot11WtpIfTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pIfReqsuccnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pIfReqsuccnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:pIfReqsuccnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pIfReqsuccnum data.
 * set (* pIfReqsuccnum_val_ptr ) from rowreq_ctx->data
 */
    (* pIfReqsuccnum_val_ptr ) = rowreq_ctx->data.pIfReqsuccnum;

    return MFD_SUCCESS;
} /* pIfReqsuccnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpIfTeminalEntry.pIfReqfailnum
 * pIfReqfailnum is subid 5 of dot11WtpIfTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.4.1.5
 * Description:
The ap depend on bssid portal request fail number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-pIfReqfailnum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
pIfReqfailnum_map(u_long *mib_pIfReqfailnum_val_ptr, u_long raw_pIfReqfailnum_val)
{
    netsnmp_assert(NULL != mib_pIfReqfailnum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:pIfReqfailnum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement pIfReqfailnum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_pIfReqfailnum_val_ptr) = raw_pIfReqfailnum_val;

    return MFD_SUCCESS;
} /* pIfReqfailnum_map */

/**
 * Extract the current value of the pIfReqfailnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pIfReqfailnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pIfReqfailnum_get( dot11WtpIfTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pIfReqfailnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pIfReqfailnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpIfTeminalTable:pIfReqfailnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pIfReqfailnum data.
 * set (* pIfReqfailnum_val_ptr ) from rowreq_ctx->data
 */
    (* pIfReqfailnum_val_ptr ) = rowreq_ctx->data.pIfReqfailnum;

    return MFD_SUCCESS;
} /* pIfReqfailnum_get */



/** @} */
