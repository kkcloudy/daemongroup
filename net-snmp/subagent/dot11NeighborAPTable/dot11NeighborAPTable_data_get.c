/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11NeighborAPTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11NeighborAPTable get routines.
 * TODO:240:M: Implement dot11NeighborAPTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11NeighborAPTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11NeighborAPTable is subid 1 of wtpNeighbor.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11NeighborAPTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPIndex
 * neighborAPIndex is subid 1 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.1
 * Description:
Rogue AP index
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPIndex_map(long *mib_neighborAPIndex_val_ptr, long raw_neighborAPIndex_val)
{
    netsnmp_assert(NULL != mib_neighborAPIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement neighborAPIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_neighborAPIndex_val_ptr) = raw_neighborAPIndex_val;

    return MFD_SUCCESS;
} /* neighborAPIndex_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11NeighborAPTable_indexes_set_tbl_idx(dot11NeighborAPTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long neighborAPIndex_val)
{
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:dot11NeighborAPTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* neighborAPIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->neighborAPIndex = neighborAPIndex_val;
    

    return MFD_SUCCESS;
} /* dot11NeighborAPTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11NeighborAPTable_indexes_set(dot11NeighborAPTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long neighborAPIndex_val)
{
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:dot11NeighborAPTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11NeighborAPTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , neighborAPIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11NeighborAPTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11NeighborAPTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPMac
 * neighborAPMac is subid 2 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.2
 * Description:
Rogue AP mac
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPMac.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPMac_map(char **mib_neighborAPMac_val_ptr_ptr, size_t *mib_neighborAPMac_val_ptr_len_ptr, char *raw_neighborAPMac_val_ptr, size_t raw_neighborAPMac_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_neighborAPMac_val_ptr);
    netsnmp_assert((NULL != mib_neighborAPMac_val_ptr_ptr) && (NULL != mib_neighborAPMac_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPMac_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement neighborAPMac non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_neighborAPMac_val_ptr_len; /* assume equal */
    if((NULL == *mib_neighborAPMac_val_ptr_ptr) || (*mib_neighborAPMac_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_neighborAPMac_val_ptr_ptr = realloc( *mib_neighborAPMac_val_ptr_ptr, converted_len * sizeof(**mib_neighborAPMac_val_ptr_ptr));
        if(NULL == *mib_neighborAPMac_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_neighborAPMac_val_ptr_len_ptr = converted_len;
    memcpy( *mib_neighborAPMac_val_ptr_ptr, raw_neighborAPMac_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* neighborAPMac_map */

/**
 * Extract the current value of the neighborAPMac data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param neighborAPMac_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param neighborAPMac_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by neighborAPMac.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*neighborAPMac_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update neighborAPMac_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
neighborAPMac_get( dot11NeighborAPTable_rowreq_ctx *rowreq_ctx, char **neighborAPMac_val_ptr_ptr, size_t *neighborAPMac_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != neighborAPMac_val_ptr_ptr) && (NULL != *neighborAPMac_val_ptr_ptr));
   netsnmp_assert( NULL != neighborAPMac_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPMac_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the neighborAPMac data.
 * set (* neighborAPMac_val_ptr_ptr ) and (* neighborAPMac_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for neighborAPMac data
     */
    if ((NULL == (* neighborAPMac_val_ptr_ptr )) ||
        ((* neighborAPMac_val_ptr_len_ptr ) < (rowreq_ctx->data.neighborAPMac_len * sizeof((* neighborAPMac_val_ptr_ptr )[0])))) {
        /*
         * allocate space for neighborAPMac data
         */
        (* neighborAPMac_val_ptr_ptr ) = malloc(rowreq_ctx->data.neighborAPMac_len * sizeof((* neighborAPMac_val_ptr_ptr )[0]));
        if(NULL == (* neighborAPMac_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* neighborAPMac_val_ptr_len_ptr ) = rowreq_ctx->data.neighborAPMac_len * sizeof((* neighborAPMac_val_ptr_ptr )[0]);
    memcpy( (* neighborAPMac_val_ptr_ptr ), rowreq_ctx->data.neighborAPMac, (* neighborAPMac_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* neighborAPMac_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPRate
 * neighborAPRate is subid 3 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.3
 * Description:
Rogue AP rate
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPRate_map(long *mib_neighborAPRate_val_ptr, long raw_neighborAPRate_val)
{
    netsnmp_assert(NULL != mib_neighborAPRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement neighborAPRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_neighborAPRate_val_ptr) = raw_neighborAPRate_val;

    return MFD_SUCCESS;
} /* neighborAPRate_map */

/**
 * Extract the current value of the neighborAPRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param neighborAPRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
neighborAPRate_get( dot11NeighborAPTable_rowreq_ctx *rowreq_ctx, long * neighborAPRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != neighborAPRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the neighborAPRate data.
 * set (* neighborAPRate_val_ptr ) from rowreq_ctx->data
 */
    (* neighborAPRate_val_ptr ) = rowreq_ctx->data.neighborAPRate;

    return MFD_SUCCESS;
} /* neighborAPRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPChannel
 * neighborAPChannel is subid 4 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.4
 * Description:
Rogue AP channel
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPChannel.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPChannel_map(long *mib_neighborAPChannel_val_ptr, long raw_neighborAPChannel_val)
{
    netsnmp_assert(NULL != mib_neighborAPChannel_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPChannel_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement neighborAPChannel mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_neighborAPChannel_val_ptr) = raw_neighborAPChannel_val;

    return MFD_SUCCESS;
} /* neighborAPChannel_map */

/**
 * Extract the current value of the neighborAPChannel data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param neighborAPChannel_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
neighborAPChannel_get( dot11NeighborAPTable_rowreq_ctx *rowreq_ctx, long * neighborAPChannel_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != neighborAPChannel_val_ptr );


    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPChannel_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the neighborAPChannel data.
 * set (* neighborAPChannel_val_ptr ) from rowreq_ctx->data
 */
    (* neighborAPChannel_val_ptr ) = rowreq_ctx->data.neighborAPChannel;

    return MFD_SUCCESS;
} /* neighborAPChannel_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPRssi
 * neighborAPRssi is subid 5 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.5
 * Description:
Rogue AP rssi
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPRssi.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPRssi_map(long *mib_neighborAPRssi_val_ptr, long raw_neighborAPRssi_val)
{
    netsnmp_assert(NULL != mib_neighborAPRssi_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPRssi_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement neighborAPRssi mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_neighborAPRssi_val_ptr) = raw_neighborAPRssi_val;

    return MFD_SUCCESS;
} /* neighborAPRssi_map */

/**
 * Extract the current value of the neighborAPRssi data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param neighborAPRssi_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
neighborAPRssi_get( dot11NeighborAPTable_rowreq_ctx *rowreq_ctx, long * neighborAPRssi_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != neighborAPRssi_val_ptr );


    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPRssi_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the neighborAPRssi data.
 * set (* neighborAPRssi_val_ptr ) from rowreq_ctx->data
 */
    (* neighborAPRssi_val_ptr ) = rowreq_ctx->data.neighborAPRssi;

    return MFD_SUCCESS;
} /* neighborAPRssi_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPNoise
 * neighborAPNoise is subid 6 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.6
 * Description:
Rogue AP noise
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPNoise.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPNoise_map(long *mib_neighborAPNoise_val_ptr, long raw_neighborAPNoise_val)
{
    netsnmp_assert(NULL != mib_neighborAPNoise_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPNoise_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement neighborAPNoise mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_neighborAPNoise_val_ptr) = raw_neighborAPNoise_val;

    return MFD_SUCCESS;
} /* neighborAPNoise_map */

/**
 * Extract the current value of the neighborAPNoise data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param neighborAPNoise_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
neighborAPNoise_get( dot11NeighborAPTable_rowreq_ctx *rowreq_ctx, long * neighborAPNoise_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != neighborAPNoise_val_ptr );


    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPNoise_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the neighborAPNoise data.
 * set (* neighborAPNoise_val_ptr ) from rowreq_ctx->data
 */
    (* neighborAPNoise_val_ptr ) = rowreq_ctx->data.neighborAPNoise;

    return MFD_SUCCESS;
} /* neighborAPNoise_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPBeaconInterval
 * neighborAPBeaconInterval is subid 7 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.7
 * Description:
Rogue AP beacon interval
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPBeaconInterval.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPBeaconInterval_map(long *mib_neighborAPBeaconInterval_val_ptr, long raw_neighborAPBeaconInterval_val)
{
    netsnmp_assert(NULL != mib_neighborAPBeaconInterval_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPBeaconInterval_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement neighborAPBeaconInterval mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_neighborAPBeaconInterval_val_ptr) = raw_neighborAPBeaconInterval_val;

    return MFD_SUCCESS;
} /* neighborAPBeaconInterval_map */

/**
 * Extract the current value of the neighborAPBeaconInterval data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param neighborAPBeaconInterval_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
neighborAPBeaconInterval_get( dot11NeighborAPTable_rowreq_ctx *rowreq_ctx, long * neighborAPBeaconInterval_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != neighborAPBeaconInterval_val_ptr );


    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPBeaconInterval_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the neighborAPBeaconInterval data.
 * set (* neighborAPBeaconInterval_val_ptr ) from rowreq_ctx->data
 */
    (* neighborAPBeaconInterval_val_ptr ) = rowreq_ctx->data.neighborAPBeaconInterval;

    return MFD_SUCCESS;
} /* neighborAPBeaconInterval_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPCapability
 * neighborAPCapability is subid 8 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.8
 * Description:
Rogue AP capability
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPCapability.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPCapability_map(long *mib_neighborAPCapability_val_ptr, long raw_neighborAPCapability_val)
{
    netsnmp_assert(NULL != mib_neighborAPCapability_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPCapability_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement neighborAPCapability mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_neighborAPCapability_val_ptr) = raw_neighborAPCapability_val;

    return MFD_SUCCESS;
} /* neighborAPCapability_map */

/**
 * Extract the current value of the neighborAPCapability data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param neighborAPCapability_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
neighborAPCapability_get( dot11NeighborAPTable_rowreq_ctx *rowreq_ctx, long * neighborAPCapability_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != neighborAPCapability_val_ptr );


    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPCapability_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the neighborAPCapability data.
 * set (* neighborAPCapability_val_ptr ) from rowreq_ctx->data
 */
    (* neighborAPCapability_val_ptr ) = rowreq_ctx->data.neighborAPCapability;

    return MFD_SUCCESS;
} /* neighborAPCapability_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPEssid
 * neighborAPEssid is subid 9 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.9
 * Description:
Rogue AP essid
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPEssid.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPEssid_map(char **mib_neighborAPEssid_val_ptr_ptr, size_t *mib_neighborAPEssid_val_ptr_len_ptr, char *raw_neighborAPEssid_val_ptr, size_t raw_neighborAPEssid_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_neighborAPEssid_val_ptr);
    netsnmp_assert((NULL != mib_neighborAPEssid_val_ptr_ptr) && (NULL != mib_neighborAPEssid_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPEssid_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement neighborAPEssid non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_neighborAPEssid_val_ptr_len; /* assume equal */
    if((NULL == *mib_neighborAPEssid_val_ptr_ptr) || (*mib_neighborAPEssid_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_neighborAPEssid_val_ptr_ptr = realloc( *mib_neighborAPEssid_val_ptr_ptr, converted_len * sizeof(**mib_neighborAPEssid_val_ptr_ptr));
        if(NULL == *mib_neighborAPEssid_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_neighborAPEssid_val_ptr_len_ptr = converted_len;
    memcpy( *mib_neighborAPEssid_val_ptr_ptr, raw_neighborAPEssid_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* neighborAPEssid_map */

/**
 * Extract the current value of the neighborAPEssid data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param neighborAPEssid_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param neighborAPEssid_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by neighborAPEssid.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*neighborAPEssid_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update neighborAPEssid_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
neighborAPEssid_get( dot11NeighborAPTable_rowreq_ctx *rowreq_ctx, char **neighborAPEssid_val_ptr_ptr, size_t *neighborAPEssid_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != neighborAPEssid_val_ptr_ptr) && (NULL != *neighborAPEssid_val_ptr_ptr));
   netsnmp_assert( NULL != neighborAPEssid_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPEssid_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the neighborAPEssid data.
 * set (* neighborAPEssid_val_ptr_ptr ) and (* neighborAPEssid_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for neighborAPEssid data
     */
    if ((NULL == (* neighborAPEssid_val_ptr_ptr )) ||
        ((* neighborAPEssid_val_ptr_len_ptr ) < (rowreq_ctx->data.neighborAPEssid_len * sizeof((* neighborAPEssid_val_ptr_ptr )[0])))) {
        /*
         * allocate space for neighborAPEssid data
         */
        (* neighborAPEssid_val_ptr_ptr ) = malloc(rowreq_ctx->data.neighborAPEssid_len * sizeof((* neighborAPEssid_val_ptr_ptr )[0]));
        if(NULL == (* neighborAPEssid_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* neighborAPEssid_val_ptr_len_ptr ) = rowreq_ctx->data.neighborAPEssid_len * sizeof((* neighborAPEssid_val_ptr_ptr )[0]);
    memcpy( (* neighborAPEssid_val_ptr_ptr ), rowreq_ctx->data.neighborAPEssid, (* neighborAPEssid_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* neighborAPEssid_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPElemInfo
 * neighborAPElemInfo is subid 10 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.10
 * Description:
Rogue AP element info
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPElemInfo.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPElemInfo_map(char **mib_neighborAPElemInfo_val_ptr_ptr, size_t *mib_neighborAPElemInfo_val_ptr_len_ptr, char *raw_neighborAPElemInfo_val_ptr, size_t raw_neighborAPElemInfo_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_neighborAPElemInfo_val_ptr);
    netsnmp_assert((NULL != mib_neighborAPElemInfo_val_ptr_ptr) && (NULL != mib_neighborAPElemInfo_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPElemInfo_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement neighborAPElemInfo non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_neighborAPElemInfo_val_ptr_len; /* assume equal */
    if((NULL == *mib_neighborAPElemInfo_val_ptr_ptr) || (*mib_neighborAPElemInfo_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_neighborAPElemInfo_val_ptr_ptr = realloc( *mib_neighborAPElemInfo_val_ptr_ptr, converted_len * sizeof(**mib_neighborAPElemInfo_val_ptr_ptr));
        if(NULL == *mib_neighborAPElemInfo_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_neighborAPElemInfo_val_ptr_len_ptr = converted_len;
    memcpy( *mib_neighborAPElemInfo_val_ptr_ptr, raw_neighborAPElemInfo_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* neighborAPElemInfo_map */

/**
 * Extract the current value of the neighborAPElemInfo data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param neighborAPElemInfo_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param neighborAPElemInfo_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by neighborAPElemInfo.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*neighborAPElemInfo_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update neighborAPElemInfo_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
neighborAPElemInfo_get( dot11NeighborAPTable_rowreq_ctx *rowreq_ctx, char **neighborAPElemInfo_val_ptr_ptr, size_t *neighborAPElemInfo_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != neighborAPElemInfo_val_ptr_ptr) && (NULL != *neighborAPElemInfo_val_ptr_ptr));
   netsnmp_assert( NULL != neighborAPElemInfo_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPElemInfo_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the neighborAPElemInfo data.
 * set (* neighborAPElemInfo_val_ptr_ptr ) and (* neighborAPElemInfo_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for neighborAPElemInfo data
     */
    if ((NULL == (* neighborAPElemInfo_val_ptr_ptr )) ||
        ((* neighborAPElemInfo_val_ptr_len_ptr ) < (rowreq_ctx->data.neighborAPElemInfo_len * sizeof((* neighborAPElemInfo_val_ptr_ptr )[0])))) {
        /*
         * allocate space for neighborAPElemInfo data
         */
        (* neighborAPElemInfo_val_ptr_ptr ) = malloc(rowreq_ctx->data.neighborAPElemInfo_len * sizeof((* neighborAPElemInfo_val_ptr_ptr )[0]));
        if(NULL == (* neighborAPElemInfo_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* neighborAPElemInfo_val_ptr_len_ptr ) = rowreq_ctx->data.neighborAPElemInfo_len * sizeof((* neighborAPElemInfo_val_ptr_ptr )[0]);
    memcpy( (* neighborAPElemInfo_val_ptr_ptr ), rowreq_ctx->data.neighborAPElemInfo, (* neighborAPElemInfo_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* neighborAPElemInfo_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPSignalIntensity
 * neighborAPSignalIntensity is subid 11 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.11
 * Description:
Rogue AP SignalIntensity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPSignalIntensity.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPSignalIntensity_map(long *mib_neighborAPSignalIntensity_val_ptr, long raw_neighborAPSignalIntensity_val)
{
    netsnmp_assert(NULL != mib_neighborAPSignalIntensity_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPSignalIntensity_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement neighborAPSignalIntensity mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_neighborAPSignalIntensity_val_ptr) = raw_neighborAPSignalIntensity_val;

    return MFD_SUCCESS;
} /* neighborAPSignalIntensity_map */

/**
 * Extract the current value of the neighborAPSignalIntensity data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param neighborAPSignalIntensity_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
neighborAPSignalIntensity_get( dot11NeighborAPTable_rowreq_ctx *rowreq_ctx, long * neighborAPSignalIntensity_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != neighborAPSignalIntensity_val_ptr );


    DEBUGMSGTL(("verbose:dot11NeighborAPTable:neighborAPSignalIntensity_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the neighborAPSignalIntensity data.
 * set (* neighborAPSignalIntensity_val_ptr ) from rowreq_ctx->data
 */
    (* neighborAPSignalIntensity_val_ptr ) = rowreq_ctx->data.neighborAPSignalIntensity;

    return MFD_SUCCESS;
} /* neighborAPSignalIntensity_get */



/** @} */
