/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpStaTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpStaTable get routines.
 * TODO:240:M: Implement dot11WtpStaTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpStaTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpStaTable is subid 1 of wtpStation.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpStaTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_map_wtpsta(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacAddr_map_wtpsta","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpStaMacAddr
 * wtpStaMacAddr is subid 1 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.1
 * Description:
The terminal MAC address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaMacAddr_map_wtpsta(char **mib_wtpStaMacAddr_val_ptr_ptr, size_t *mib_wtpStaMacAddr_val_ptr_len_ptr, char *raw_wtpStaMacAddr_val_ptr, size_t raw_wtpStaMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpStaMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpStaMacAddr_val_ptr_ptr) && (NULL != mib_wtpStaMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpStaMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpStaMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpStaMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpStaMacAddr_val_ptr_ptr) || (*mib_wtpStaMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpStaMacAddr_val_ptr_ptr = realloc( *mib_wtpStaMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpStaMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpStaMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpStaMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpStaMacAddr_val_ptr_ptr, raw_wtpStaMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpStaMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpStaTable_indexes_set_tbl_idx(dot11WtpStaTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, char *wtpStaMacAddr_val_ptr,  size_t wtpStaMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpStaTable:dot11WtpStaTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wtpStaMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpStaMacAddr_len = sizeof(tbl_idx->wtpStaMacAddr);
    /*
     * make sure there is enough space for wtpStaMacAddr data
     */
    if ((NULL == tbl_idx->wtpStaMacAddr) ||
        (tbl_idx->wtpStaMacAddr_len < (wtpStaMacAddr_val_ptr_len * sizeof(tbl_idx->wtpStaMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpStaMacAddr_len = wtpStaMacAddr_val_ptr_len * sizeof(tbl_idx->wtpStaMacAddr[0]);
    memcpy( tbl_idx->wtpStaMacAddr, wtpStaMacAddr_val_ptr, tbl_idx->wtpStaMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11WtpStaTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpStaTable_indexes_set(dot11WtpStaTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, char *wtpStaMacAddr_val_ptr,  size_t wtpStaMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpStaTable:dot11WtpStaTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpStaTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpStaMacAddr_val_ptr, wtpStaMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpStaTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpStaTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpStaIP
 * wtpStaIP is subid 2 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.2
 * Description:
Station IP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaIP.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaIP_map(u_long *mib_wtpStaIP_val_ptr, u_long raw_wtpStaIP_val)
{
    netsnmp_assert(NULL != mib_wtpStaIP_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpStaIP_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaIP mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpStaIP_val_ptr) = raw_wtpStaIP_val;

    return MFD_SUCCESS;
} /* wtpStaIP_map */

/**
 * Extract the current value of the wtpStaIP data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaIP_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaIP_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpStaIP_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaIP_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpStaIP_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaIP data.
 * set (* wtpStaIP_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaIP_val_ptr ) = rowreq_ctx->data.wtpStaIP;

    return MFD_SUCCESS;
} /* wtpStaIP_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpWirelessClientSNR
 * wtpWirelessClientSNR is subid 3 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.3
 * Description:
Wireless terminal SNR.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessClientSNR.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessClientSNR_map(long *mib_wtpWirelessClientSNR_val_ptr, long raw_wtpWirelessClientSNR_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessClientSNR_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpWirelessClientSNR_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessClientSNR mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessClientSNR_val_ptr) = raw_wtpWirelessClientSNR_val;

    return MFD_SUCCESS;
} /* wtpWirelessClientSNR_map */

/**
 * Extract the current value of the wtpWirelessClientSNR data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessClientSNR_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpWirelessClientSNR_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, long * wtpWirelessClientSNR_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWirelessClientSNR_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpWirelessClientSNR_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessClientSNR data.
 * set (* wtpWirelessClientSNR_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWirelessClientSNR_val_ptr ) = rowreq_ctx->data.wtpWirelessClientSNR;

    return MFD_SUCCESS;
} /* wtpWirelessClientSNR_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpTxTerminalPackMount
 * wtpTxTerminalPackMount is subid 4 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.4
 * Description:
The total number of packages which sending to the terminal.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTxTerminalPackMount.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTxTerminalPackMount_map(u_long *mib_wtpTxTerminalPackMount_val_ptr, u_long raw_wtpTxTerminalPackMount_val)
{
    netsnmp_assert(NULL != mib_wtpTxTerminalPackMount_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTxTerminalPackMount_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpTxTerminalPackMount mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpTxTerminalPackMount_val_ptr) = raw_wtpTxTerminalPackMount_val;

    return MFD_SUCCESS;
} /* wtpTxTerminalPackMount_map */

/**
 * Extract the current value of the wtpTxTerminalPackMount data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTxTerminalPackMount_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpTxTerminalPackMount_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpTxTerminalPackMount_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpTxTerminalPackMount_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTxTerminalPackMount_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTxTerminalPackMount data.
 * set (* wtpTxTerminalPackMount_val_ptr ) from rowreq_ctx->data
 */
    (* wtpTxTerminalPackMount_val_ptr ) = rowreq_ctx->data.wtpTxTerminalPackMount;

    return MFD_SUCCESS;
} /* wtpTxTerminalPackMount_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpTxTerminalByteMount
 * wtpTxTerminalByteMount is subid 5 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.5
 * Description:
The total number of bytes which sent to the terminal.(KB)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTxTerminalByteMount.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTxTerminalByteMount_map(unsigned long long *mib_wtpTxTerminalByteMount_val_ptr, unsigned long long raw_wtpTxTerminalByteMount_val)
{
    netsnmp_assert(NULL != mib_wtpTxTerminalByteMount_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTxTerminalByteMount_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpTxTerminalByteMount mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpTxTerminalByteMount_val_ptr) = raw_wtpTxTerminalByteMount_val;

    return MFD_SUCCESS;
} /* wtpTxTerminalByteMount_map */

/**
 * Extract the current value of the wtpTxTerminalByteMount data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTxTerminalByteMount_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpTxTerminalByteMount_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, unsigned long long * wtpTxTerminalByteMount_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpTxTerminalByteMount_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTxTerminalByteMount_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTxTerminalByteMount data.
 * set (* wtpTxTerminalByteMount_val_ptr ) from rowreq_ctx->data
 */
    (* wtpTxTerminalByteMount_val_ptr ) = rowreq_ctx->data.wtpTxTerminalByteMount;

    return MFD_SUCCESS;
} /* wtpTxTerminalByteMount_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpTerminalRxPackMount
 * wtpTerminalRxPackMount is subid 6 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.6
 * Description:
The total number of packages which received from the terminal.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTerminalRxPackMount.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTerminalRxPackMount_map(u_long *mib_wtpTerminalRxPackMount_val_ptr, u_long raw_wtpTerminalRxPackMount_val)
{
    netsnmp_assert(NULL != mib_wtpTerminalRxPackMount_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalRxPackMount_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpTerminalRxPackMount mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpTerminalRxPackMount_val_ptr) = raw_wtpTerminalRxPackMount_val;

    return MFD_SUCCESS;
} /* wtpTerminalRxPackMount_map */

/**
 * Extract the current value of the wtpTerminalRxPackMount data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTerminalRxPackMount_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpTerminalRxPackMount_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpTerminalRxPackMount_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpTerminalRxPackMount_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalRxPackMount_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTerminalRxPackMount data.
 * set (* wtpTerminalRxPackMount_val_ptr ) from rowreq_ctx->data
 */
    (* wtpTerminalRxPackMount_val_ptr ) = rowreq_ctx->data.wtpTerminalRxPackMount;

    return MFD_SUCCESS;
} /* wtpTerminalRxPackMount_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpTerminalRxByteMount
 * wtpTerminalRxByteMount is subid 7 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.7
 * Description:
The total number of bytes which received from the terminal.(KB)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTerminalRxByteMount.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTerminalRxByteMount_map(unsigned long long *mib_wtpTerminalRxByteMount_val_ptr, unsigned long long raw_wtpTerminalRxByteMount_val)
{
    netsnmp_assert(NULL != mib_wtpTerminalRxByteMount_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalRxByteMount_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpTerminalRxByteMount mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpTerminalRxByteMount_val_ptr) = raw_wtpTerminalRxByteMount_val;

    return MFD_SUCCESS;
} /* wtpTerminalRxByteMount_map */

/**
 * Extract the current value of the wtpTerminalRxByteMount data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTerminalRxByteMount_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpTerminalRxByteMount_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, unsigned long long * wtpTerminalRxByteMount_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpTerminalRxByteMount_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalRxByteMount_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTerminalRxByteMount data.
 * set (* wtpTerminalRxByteMount_val_ptr ) from rowreq_ctx->data
 */
    (* wtpTerminalRxByteMount_val_ptr ) = rowreq_ctx->data.wtpTerminalRxByteMount;

    return MFD_SUCCESS;
} /* wtpTerminalRxByteMount_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpTerminalResPack
 * wtpTerminalResPack is subid 8 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.8
 * Description:
The total number of resent packages of the terminal.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTerminalResPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTerminalResPack_map(u_long *mib_wtpTerminalResPack_val_ptr, u_long raw_wtpTerminalResPack_val)
{
    netsnmp_assert(NULL != mib_wtpTerminalResPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalResPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpTerminalResPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpTerminalResPack_val_ptr) = raw_wtpTerminalResPack_val;

    return MFD_SUCCESS;
} /* wtpTerminalResPack_map */

/**
 * Extract the current value of the wtpTerminalResPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTerminalResPack_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpTerminalResPack_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpTerminalResPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpTerminalResPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalResPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTerminalResPack data.
 * set (* wtpTerminalResPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpTerminalResPack_val_ptr ) = rowreq_ctx->data.wtpTerminalResPack;

    return MFD_SUCCESS;
} /* wtpTerminalResPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpTerminalResByte
 * wtpTerminalResByte is subid 9 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.9
 * Description:
The total number of resent bytes of the terminal.(KB)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTerminalResByte.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTerminalResByte_map(u_long *mib_wtpTerminalResByte_val_ptr, u_long raw_wtpTerminalResByte_val)
{
    netsnmp_assert(NULL != mib_wtpTerminalResByte_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalResByte_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpTerminalResByte mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpTerminalResByte_val_ptr) = raw_wtpTerminalResByte_val;

    return MFD_SUCCESS;
} /* wtpTerminalResByte_map */

/**
 * Extract the current value of the wtpTerminalResByte data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTerminalResByte_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpTerminalResByte_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpTerminalResByte_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpTerminalResByte_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalResByte_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTerminalResByte data.
 * set (* wtpTerminalResByte_val_ptr ) from rowreq_ctx->data
 */
    (* wtpTerminalResByte_val_ptr ) = rowreq_ctx->data.wtpTerminalResByte;

    return MFD_SUCCESS;
} /* wtpTerminalResByte_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpTerminalRxErrPack
 * wtpTerminalRxErrPack is subid 10 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.10
 * Description:
The total number of mistake packages which received from the terminal.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTerminalRxErrPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTerminalRxErrPack_map(u_long *mib_wtpTerminalRxErrPack_val_ptr, u_long raw_wtpTerminalRxErrPack_val)
{
    netsnmp_assert(NULL != mib_wtpTerminalRxErrPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalRxErrPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpTerminalRxErrPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpTerminalRxErrPack_val_ptr) = raw_wtpTerminalRxErrPack_val;

    return MFD_SUCCESS;
} /* wtpTerminalRxErrPack_map */

/**
 * Extract the current value of the wtpTerminalRxErrPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTerminalRxErrPack_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpTerminalRxErrPack_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpTerminalRxErrPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpTerminalRxErrPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalRxErrPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTerminalRxErrPack data.
 * set (* wtpTerminalRxErrPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpTerminalRxErrPack_val_ptr ) = rowreq_ctx->data.wtpTerminalRxErrPack;

    return MFD_SUCCESS;
} /* wtpTerminalRxErrPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpMacTermAddrUsrOnlineTime
 * wtpMacTermAddrUsrOnlineTime is subid 11 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.11
 * Description:
The user online time which based on the mac address marked user.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacTermAddrUsrOnlineTime.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacTermAddrUsrOnlineTime_map(u_long *mib_wtpMacTermAddrUsrOnlineTime_val_ptr, u_long raw_wtpMacTermAddrUsrOnlineTime_val)
{
    netsnmp_assert(NULL != mib_wtpMacTermAddrUsrOnlineTime_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAddrUsrOnlineTime_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMacTermAddrUsrOnlineTime mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMacTermAddrUsrOnlineTime_val_ptr) = raw_wtpMacTermAddrUsrOnlineTime_val;

    return MFD_SUCCESS;
} /* wtpMacTermAddrUsrOnlineTime_map */

/**
 * Extract the current value of the wtpMacTermAddrUsrOnlineTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMacTermAddrUsrOnlineTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMacTermAddrUsrOnlineTime_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpMacTermAddrUsrOnlineTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMacTermAddrUsrOnlineTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAddrUsrOnlineTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMacTermAddrUsrOnlineTime data.
 * set (* wtpMacTermAddrUsrOnlineTime_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMacTermAddrUsrOnlineTime_val_ptr ) = rowreq_ctx->data.wtpMacTermAddrUsrOnlineTime;

    return MFD_SUCCESS;
} /* wtpMacTermAddrUsrOnlineTime_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpMacTermAddrUsrTxSpd
 * wtpMacTermAddrUsrTxSpd is subid 12 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.12
 * Description:
The user send speed which based on the mac address marked user.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacTermAddrUsrTxSpd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacTermAddrUsrTxSpd_map(u_long *mib_wtpMacTermAddrUsrTxSpd_val_ptr, u_long raw_wtpMacTermAddrUsrTxSpd_val)
{
    netsnmp_assert(NULL != mib_wtpMacTermAddrUsrTxSpd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAddrUsrTxSpd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMacTermAddrUsrTxSpd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMacTermAddrUsrTxSpd_val_ptr) = raw_wtpMacTermAddrUsrTxSpd_val;

    return MFD_SUCCESS;
} /* wtpMacTermAddrUsrTxSpd_map */

/**
 * Extract the current value of the wtpMacTermAddrUsrTxSpd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMacTermAddrUsrTxSpd_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMacTermAddrUsrTxSpd_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpMacTermAddrUsrTxSpd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMacTermAddrUsrTxSpd_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAddrUsrTxSpd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMacTermAddrUsrTxSpd data.
 * set (* wtpMacTermAddrUsrTxSpd_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMacTermAddrUsrTxSpd_val_ptr ) = rowreq_ctx->data.wtpMacTermAddrUsrTxSpd;

    return MFD_SUCCESS;
} /* wtpMacTermAddrUsrTxSpd_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpMacTermAddrUsrRxSpd
 * wtpMacTermAddrUsrRxSpd is subid 13 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.13
 * Description:
The user receive speed which based on the mac address marked user.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacTermAddrUsrRxSpd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacTermAddrUsrRxSpd_map(u_long *mib_wtpMacTermAddrUsrRxSpd_val_ptr, u_long raw_wtpMacTermAddrUsrRxSpd_val)
{
    netsnmp_assert(NULL != mib_wtpMacTermAddrUsrRxSpd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAddrUsrRxSpd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMacTermAddrUsrRxSpd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMacTermAddrUsrRxSpd_val_ptr) = raw_wtpMacTermAddrUsrRxSpd_val;

    return MFD_SUCCESS;
} /* wtpMacTermAddrUsrRxSpd_map */

/**
 * Extract the current value of the wtpMacTermAddrUsrRxSpd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMacTermAddrUsrRxSpd_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMacTermAddrUsrRxSpd_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpMacTermAddrUsrRxSpd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMacTermAddrUsrRxSpd_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAddrUsrRxSpd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMacTermAddrUsrRxSpd data.
 * set (* wtpMacTermAddrUsrRxSpd_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMacTermAddrUsrRxSpd_val_ptr ) = rowreq_ctx->data.wtpMacTermAddrUsrRxSpd;

    return MFD_SUCCESS;
} /* wtpMacTermAddrUsrRxSpd_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpMacTermAddrUsrAllThroughput
 * wtpMacTermAddrUsrAllThroughput is subid 14 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.14
 * Description:
The total thuoughput  which based on the mac address marked user.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacTermAddrUsrAllThroughput.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacTermAddrUsrAllThroughput_map(u_long *mib_wtpMacTermAddrUsrAllThroughput_val_ptr, u_long raw_wtpMacTermAddrUsrAllThroughput_val)
{
    netsnmp_assert(NULL != mib_wtpMacTermAddrUsrAllThroughput_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAddrUsrAllThroughput_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMacTermAddrUsrAllThroughput mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMacTermAddrUsrAllThroughput_val_ptr) = raw_wtpMacTermAddrUsrAllThroughput_val;

    return MFD_SUCCESS;
} /* wtpMacTermAddrUsrAllThroughput_map */

/**
 * Extract the current value of the wtpMacTermAddrUsrAllThroughput data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMacTermAddrUsrAllThroughput_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMacTermAddrUsrAllThroughput_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpMacTermAddrUsrAllThroughput_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMacTermAddrUsrAllThroughput_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAddrUsrAllThroughput_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMacTermAddrUsrAllThroughput data.
 * set (* wtpMacTermAddrUsrAllThroughput_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMacTermAddrUsrAllThroughput_val_ptr ) = rowreq_ctx->data.wtpMacTermAddrUsrAllThroughput;

    return MFD_SUCCESS;
} /* wtpMacTermAddrUsrAllThroughput_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpMacTermAPReceivedStaSignalStrength
 * wtpMacTermAPReceivedStaSignalStrength is subid 15 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.15
 * Description:
The current signal intensity that AP receives from terminal.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacTermAPReceivedStaSignalStrength.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacTermAPReceivedStaSignalStrength_map(char **mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr, size_t *mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr, char *raw_wtpMacTermAPReceivedStaSignalStrength_val_ptr, size_t raw_wtpMacTermAPReceivedStaSignalStrength_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacTermAPReceivedStaSignalStrength_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr) && (NULL != mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAPReceivedStaSignalStrength_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacTermAPReceivedStaSignalStrength non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacTermAPReceivedStaSignalStrength_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr) || (*mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr = realloc( *mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr));
        if(NULL == *mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr, raw_wtpMacTermAPReceivedStaSignalStrength_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacTermAPReceivedStaSignalStrength_map */

/**
 * Extract the current value of the wtpMacTermAPReceivedStaSignalStrength data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpMacTermAPReceivedStaSignalStrength.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpMacTermAPReceivedStaSignalStrength_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, char **wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr, size_t *wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr) && (NULL != *wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr));
   netsnmp_assert( NULL != wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAPReceivedStaSignalStrength_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMacTermAPReceivedStaSignalStrength data.
 * set (* wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr ) and (* wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpMacTermAPReceivedStaSignalStrength data
     */
    if ((NULL == (* wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr )) ||
        ((* wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength_len * sizeof((* wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpMacTermAPReceivedStaSignalStrength data
         */
        (* wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength_len * sizeof((* wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr )[0]));
        if(NULL == (* wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr ) = rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength_len * sizeof((* wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr )[0]);
    memcpy( (* wtpMacTermAPReceivedStaSignalStrength_val_ptr_ptr ), rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength, (* wtpMacTermAPReceivedStaSignalStrength_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpMacTermAPReceivedStaSignalStrength_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpMacTermStaTxFragmentedPkts
 * wtpMacTermStaTxFragmentedPkts is subid 16 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.16
 * Description:
The total number of be neafed packets transmitted from terminal.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacTermStaTxFragmentedPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacTermStaTxFragmentedPkts_map(u_long *mib_wtpMacTermStaTxFragmentedPkts_val_ptr, u_long raw_wtpMacTermStaTxFragmentedPkts_val)
{
    netsnmp_assert(NULL != mib_wtpMacTermStaTxFragmentedPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermStaTxFragmentedPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMacTermStaTxFragmentedPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMacTermStaTxFragmentedPkts_val_ptr) = raw_wtpMacTermStaTxFragmentedPkts_val;

    return MFD_SUCCESS;
} /* wtpMacTermStaTxFragmentedPkts_map */

/**
 * Extract the current value of the wtpMacTermStaTxFragmentedPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMacTermStaTxFragmentedPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMacTermStaTxFragmentedPkts_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpMacTermStaTxFragmentedPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMacTermStaTxFragmentedPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermStaTxFragmentedPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMacTermStaTxFragmentedPkts data.
 * set (* wtpMacTermStaTxFragmentedPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMacTermStaTxFragmentedPkts_val_ptr ) = rowreq_ctx->data.wtpMacTermStaTxFragmentedPkts;

    return MFD_SUCCESS;
} /* wtpMacTermStaTxFragmentedPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpMacTermAPTxFragmentedPkts
 * wtpMacTermAPTxFragmentedPkts is subid 17 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.17
 * Description:
The total number of be neafed packets transmitted from AP to terminal.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacTermAPTxFragmentedPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacTermAPTxFragmentedPkts_map(u_long *mib_wtpMacTermAPTxFragmentedPkts_val_ptr, u_long raw_wtpMacTermAPTxFragmentedPkts_val)
{
    netsnmp_assert(NULL != mib_wtpMacTermAPTxFragmentedPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAPTxFragmentedPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMacTermAPTxFragmentedPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMacTermAPTxFragmentedPkts_val_ptr) = raw_wtpMacTermAPTxFragmentedPkts_val;

    return MFD_SUCCESS;
} /* wtpMacTermAPTxFragmentedPkts_map */

/**
 * Extract the current value of the wtpMacTermAPTxFragmentedPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMacTermAPTxFragmentedPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMacTermAPTxFragmentedPkts_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpMacTermAPTxFragmentedPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMacTermAPTxFragmentedPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpMacTermAPTxFragmentedPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMacTermAPTxFragmentedPkts data.
 * set (* wtpMacTermAPTxFragmentedPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMacTermAPTxFragmentedPkts_val_ptr ) = rowreq_ctx->data.wtpMacTermAPTxFragmentedPkts;

    return MFD_SUCCESS;
} /* wtpMacTermAPTxFragmentedPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpBelongAPID
 * wtpBelongAPID is subid 18 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.18
 * Description:
It has the same means as wtpMacAddr.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpBelongAPID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpBelongAPID_map(long *mib_wtpBelongAPID_val_ptr, long raw_wtpBelongAPID_val)
{
    netsnmp_assert(NULL != mib_wtpBelongAPID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpBelongAPID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpBelongAPID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpBelongAPID_val_ptr) = raw_wtpBelongAPID_val;

    return MFD_SUCCESS;
} /* wtpBelongAPID_map */

/**
 * Extract the current value of the wtpBelongAPID data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpBelongAPID_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpBelongAPID_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, long * wtpBelongAPID_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpBelongAPID_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpBelongAPID_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpBelongAPID data.
 * set (* wtpBelongAPID_val_ptr ) from rowreq_ctx->data
 */
    (* wtpBelongAPID_val_ptr ) = rowreq_ctx->data.wtpBelongAPID;

    return MFD_SUCCESS;
} /* wtpBelongAPID_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpTerminalAccessTime
 * wtpTerminalAccessTime is subid 19 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.19
 * Description:
The terminal access time.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTerminalAccessTime.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTerminalAccessTime_map(char **mib_wtpTerminalAccessTime_val_ptr_ptr, size_t *mib_wtpTerminalAccessTime_val_ptr_len_ptr, char *raw_wtpTerminalAccessTime_val_ptr, size_t raw_wtpTerminalAccessTime_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpTerminalAccessTime_val_ptr);
    netsnmp_assert((NULL != mib_wtpTerminalAccessTime_val_ptr_ptr) && (NULL != mib_wtpTerminalAccessTime_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalAccessTime_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpTerminalAccessTime non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpTerminalAccessTime_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpTerminalAccessTime_val_ptr_ptr) || (*mib_wtpTerminalAccessTime_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpTerminalAccessTime_val_ptr_ptr = realloc( *mib_wtpTerminalAccessTime_val_ptr_ptr, converted_len * sizeof(**mib_wtpTerminalAccessTime_val_ptr_ptr));
        if(NULL == *mib_wtpTerminalAccessTime_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpTerminalAccessTime_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpTerminalAccessTime_val_ptr_ptr, raw_wtpTerminalAccessTime_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpTerminalAccessTime_map */

/**
 * Extract the current value of the wtpTerminalAccessTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTerminalAccessTime_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpTerminalAccessTime_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpTerminalAccessTime.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpTerminalAccessTime_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpTerminalAccessTime_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpTerminalAccessTime_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, char **wtpTerminalAccessTime_val_ptr_ptr, size_t *wtpTerminalAccessTime_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpTerminalAccessTime_val_ptr_ptr) && (NULL != *wtpTerminalAccessTime_val_ptr_ptr));
   netsnmp_assert( NULL != wtpTerminalAccessTime_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalAccessTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTerminalAccessTime data.
 * set (* wtpTerminalAccessTime_val_ptr_ptr ) and (* wtpTerminalAccessTime_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpTerminalAccessTime data
     */
    if ((NULL == (* wtpTerminalAccessTime_val_ptr_ptr )) ||
        ((* wtpTerminalAccessTime_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpTerminalAccessTime_len * sizeof((* wtpTerminalAccessTime_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpTerminalAccessTime data
         */
        (* wtpTerminalAccessTime_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpTerminalAccessTime_len * sizeof((* wtpTerminalAccessTime_val_ptr_ptr )[0]));
        if(NULL == (* wtpTerminalAccessTime_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpTerminalAccessTime_val_ptr_len_ptr ) = rowreq_ctx->data.wtpTerminalAccessTime_len * sizeof((* wtpTerminalAccessTime_val_ptr_ptr )[0]);
    memcpy( (* wtpTerminalAccessTime_val_ptr_ptr ), rowreq_ctx->data.wtpTerminalAccessTime, (* wtpTerminalAccessTime_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpTerminalAccessTime_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpBelongApName
 * wtpBelongApName is subid 21 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.8.1.1.21
 * Description:
Ap name that the terminal accessed.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpBelongApName.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpBelongApName_map(char **mib_wtpBelongApName_val_ptr_ptr, size_t *mib_wtpBelongApName_val_ptr_len_ptr, char *raw_wtpBelongApName_val_ptr, size_t raw_wtpBelongApName_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpBelongApName_val_ptr);
    netsnmp_assert((NULL != mib_wtpBelongApName_val_ptr_ptr) && (NULL != mib_wtpBelongApName_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpBelongApName_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpBelongApName non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpBelongApName_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpBelongApName_val_ptr_ptr) || (*mib_wtpBelongApName_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpBelongApName_val_ptr_ptr = realloc( *mib_wtpBelongApName_val_ptr_ptr, converted_len * sizeof(**mib_wtpBelongApName_val_ptr_ptr));
        if(NULL == *mib_wtpBelongApName_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpBelongApName_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpBelongApName_val_ptr_ptr, raw_wtpBelongApName_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpBelongApName_map */

/**
 * Extract the current value of the wtpBelongApName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpBelongApName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpBelongApName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpBelongApName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpBelongApName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpBelongApName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpBelongApName_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, char **wtpBelongApName_val_ptr_ptr, size_t *wtpBelongApName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpBelongApName_val_ptr_ptr) && (NULL != *wtpBelongApName_val_ptr_ptr));
   netsnmp_assert( NULL != wtpBelongApName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpBelongApName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpBelongApName data.
 * set (* wtpBelongApName_val_ptr_ptr ) and (* wtpBelongApName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpBelongApName data
     */
    if ((NULL == (* wtpBelongApName_val_ptr_ptr )) ||
        ((* wtpBelongApName_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpBelongApName_len * sizeof((* wtpBelongApName_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpBelongApName data
         */
        (* wtpBelongApName_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpBelongApName_len * sizeof((* wtpBelongApName_val_ptr_ptr )[0]));
        if(NULL == (* wtpBelongApName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpBelongApName_val_ptr_len_ptr ) = rowreq_ctx->data.wtpBelongApName_len * sizeof((* wtpBelongApName_val_ptr_ptr )[0]);
    memcpy( (* wtpBelongApName_val_ptr_ptr ), rowreq_ctx->data.wtpBelongApName, (* wtpBelongApName_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpBelongApName_get */

int
wtpTerminalRecvDataPackMount_map(u_long *mib_wtpTerminalRecvDataPackMount_val_ptr, u_long raw_wtpTerminalRecvDataPackMount_val)
{
    netsnmp_assert(NULL != mib_wtpTerminalRecvDataPackMount_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalRecvDataPackMount_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpTerminalRecvDataPackMount mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpTerminalRecvDataPackMount_val_ptr) = raw_wtpTerminalRecvDataPackMount_val;

    return MFD_SUCCESS;
} /* wtpTerminalRecvDataPackMount_map */

int
wtpTerminalRecvDataPackMount_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpTerminalRecvDataPackMount_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpTerminalRecvDataPackMount_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalRecvDataPackMount_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTxTerminalPackMount data.
 * set (* wtpTerminalRecvDataPackMount_val_ptr ) from rowreq_ctx->data
 */
    (* wtpTerminalRecvDataPackMount_val_ptr ) = rowreq_ctx->data.wtpTerminalRecvDataPackMount;

    return MFD_SUCCESS;
} /* wtpTerminalRecvDataPackMount_get */

int
wtpSendTerminalDataPackMount_map(u_long *mib_wtpSendTerminalDataPackMount_val_ptr, u_long raw_wtpSendTerminalDataPackMount_val)
{
    netsnmp_assert(NULL != mib_wtpSendTerminalDataPackMount_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpSendTerminalDataPackMountt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSendTerminalDataPackMount mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSendTerminalDataPackMount_val_ptr) = raw_wtpSendTerminalDataPackMount_val;

    return MFD_SUCCESS;
} /* wtpSendTerminalDataPackMount_map */

int
wtpSendTerminalDataPackMount_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, u_long * wtpSendTerminalDataPackMount_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSendTerminalDataPackMount_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpSendTerminalDataPackMount_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTxTerminalPackMount data.
 * set (* wtpSendTerminalDataPackMount_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSendTerminalDataPackMount_val_ptr ) = rowreq_ctx->data.wtpSendTerminalDataPackMount;

    return MFD_SUCCESS;
} /* wtpSendTerminalDataPackMount_get */


int
wtpTerminalTxDataRatePkts_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, unsigned char **wtpTerminalTxDataRatePkts_val_ptr_ptr, size_t *wtpTerminalTxDataRatePkts_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpTerminalTxDataRatePkts_val_ptr_ptr) && (NULL != *wtpTerminalTxDataRatePkts_val_ptr_ptr));
   netsnmp_assert( NULL != wtpTerminalTxDataRatePkts_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalTxDataRatePkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTerminalAccessTime data.
 * set (* wtpTerminalTxDataRatePkts_val_ptr_ptr ) and (* wtpTerminalTxDataRatePkts_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpTerminalTxDataRatePkts data
     */
    if ((NULL == (* wtpTerminalTxDataRatePkts_val_ptr_ptr )) ||
        ((* wtpTerminalTxDataRatePkts_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpTerminalTxDataRatePkts_len * sizeof((* wtpTerminalTxDataRatePkts_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpTerminalTxDataRatePkts data
         */
        (* wtpTerminalTxDataRatePkts_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpTerminalTxDataRatePkts_len * sizeof((* wtpTerminalTxDataRatePkts_val_ptr_ptr )[0]));
        if(NULL == (* wtpTerminalTxDataRatePkts_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpTerminalTxDataRatePkts_val_ptr_len_ptr ) = rowreq_ctx->data.wtpTerminalTxDataRatePkts_len * sizeof((* wtpTerminalTxDataRatePkts_val_ptr_ptr )[0]);
    memcpy( (* wtpTerminalTxDataRatePkts_val_ptr_ptr ), rowreq_ctx->data.wtpTerminalTxDataRatePkts, (* wtpTerminalTxDataRatePkts_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpTerminalTxDataRatePkts_get */

int
wtpTerminalRxDataRatePkts_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, unsigned char **wtpTerminalRxDataRatePkts_val_ptr_ptr, size_t *wtpTerminalRxDataRatePkts_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpTerminalRxDataRatePkts_val_ptr_ptr) && (NULL != *wtpTerminalRxDataRatePkts_val_ptr_ptr));
   netsnmp_assert( NULL != wtpTerminalRxDataRatePkts_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalRxDataRatePkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTerminalAccessTime data.
 * set (* wtpTerminalRxDataRatePkts_val_ptr_ptr ) and (* wtpTerminalRxDataRatePkts_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpTerminalRxDataRatePkts data
     */
    if ((NULL == (* wtpTerminalRxDataRatePkts_val_ptr_ptr )) ||
        ((* wtpTerminalRxDataRatePkts_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpTerminalRxDataRatePkts_len * sizeof((* wtpTerminalRxDataRatePkts_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpTerminalRxDataRatePkts data
         */
        (* wtpTerminalRxDataRatePkts_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpTerminalRxDataRatePkts_len * sizeof((* wtpTerminalRxDataRatePkts_val_ptr_ptr )[0]));
        if(NULL == (* wtpTerminalRxDataRatePkts_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpTerminalRxDataRatePkts_val_ptr_len_ptr ) = rowreq_ctx->data.wtpTerminalRxDataRatePkts_len * sizeof((* wtpTerminalRxDataRatePkts_val_ptr_ptr )[0]);
    memcpy( (* wtpTerminalRxDataRatePkts_val_ptr_ptr ), rowreq_ctx->data.wtpTerminalRxDataRatePkts, (* wtpTerminalRxDataRatePkts_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpTerminalRxDataRatePkts_get */

int
wtpTerminalTxSignalStrengthPkts_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, unsigned char **wtpTerminalTxSignalStrengthPkts_val_ptr_ptr, size_t *wtpTerminalTxSignalStrengthPkts_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpTerminalTxSignalStrengthPkts_val_ptr_ptr) && (NULL != *wtpTerminalTxSignalStrengthPkts_val_ptr_ptr));
   netsnmp_assert( NULL != wtpTerminalTxSignalStrengthPkts_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpTerminalTxSignalStrengthPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTerminalAccessTime data.
 * set (* wtpTerminalTxSignalStrengthPkts_val_ptr_ptr ) and (* wtpTerminalTxSignalStrengthPkts_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpTerminalTxSignalStrengthPkts data
     */
    if ((NULL == (* wtpTerminalTxSignalStrengthPkts_val_ptr_ptr )) ||
        ((* wtpTerminalTxSignalStrengthPkts_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpTerminalTxSignalStrengthPkts_len * sizeof((* wtpTerminalTxSignalStrengthPkts_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpTerminalTxSignalStrengthPkts data
         */
        (* wtpTerminalTxSignalStrengthPkts_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpTerminalTxSignalStrengthPkts_len * sizeof((* wtpTerminalTxSignalStrengthPkts_val_ptr_ptr )[0]));
        if(NULL == (* wtpTerminalTxSignalStrengthPkts_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpTerminalTxSignalStrengthPkts_val_ptr_len_ptr ) = rowreq_ctx->data.wtpTerminalTxSignalStrengthPkts_len * sizeof((* wtpTerminalTxSignalStrengthPkts_val_ptr_ptr )[0]);
    memcpy( (* wtpTerminalTxSignalStrengthPkts_val_ptr_ptr ), rowreq_ctx->data.wtpTerminalTxSignalStrengthPkts, (* wtpTerminalTxSignalStrengthPkts_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpTerminalTxSignalStrengthPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.APRxRates
 * APRxRates is subid 27 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.8.1.1.27
 * Description:
Ap name that the terminal accessed.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 64;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 64)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-APRxRates.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
APRxRates_map(char **mib_APRxRates_val_ptr_ptr, size_t *mib_APRxRates_val_ptr_len_ptr, char *raw_APRxRates_val_ptr, size_t raw_APRxRates_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_APRxRates_val_ptr);
    netsnmp_assert((NULL != mib_APRxRates_val_ptr_ptr) && (NULL != mib_APRxRates_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:APRxRates_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement APRxRates non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_APRxRates_val_ptr_len; /* assume equal */
    if((NULL == *mib_APRxRates_val_ptr_ptr) || (*mib_APRxRates_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_APRxRates_val_ptr_ptr = realloc( *mib_APRxRates_val_ptr_ptr, converted_len * sizeof(**mib_APRxRates_val_ptr_ptr));
        if(NULL == *mib_APRxRates_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_APRxRates_val_ptr_len_ptr = converted_len;
    memcpy( *mib_APRxRates_val_ptr_ptr, raw_APRxRates_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* APRxRates_map */

/**
 * Extract the current value of the APRxRates data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param APRxRates_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param APRxRates_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by APRxRates.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*APRxRates_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update APRxRates_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
APRxRates_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, char **APRxRates_val_ptr_ptr, size_t *APRxRates_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != APRxRates_val_ptr_ptr) && (NULL != *APRxRates_val_ptr_ptr));
   netsnmp_assert( NULL != APRxRates_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:APRxRates_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the APRxRates data.
 * set (* APRxRates_val_ptr_ptr ) and (* APRxRates_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for APRxRates data
     */
    if ((NULL == (* APRxRates_val_ptr_ptr )) ||
        ((* APRxRates_val_ptr_len_ptr ) < (rowreq_ctx->data.APRxRates_len * sizeof((* APRxRates_val_ptr_ptr )[0])))) {
        /*
         * allocate space for APRxRates data
         */
        (* APRxRates_val_ptr_ptr ) = malloc(rowreq_ctx->data.APRxRates_len * sizeof((* APRxRates_val_ptr_ptr )[0]));
        if(NULL == (* APRxRates_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* APRxRates_val_ptr_len_ptr ) = rowreq_ctx->data.APRxRates_len * sizeof((* APRxRates_val_ptr_ptr )[0]);
    memcpy( (* APRxRates_val_ptr_ptr ), rowreq_ctx->data.APRxRates, (* APRxRates_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* APRxRates_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.APTxRates
 * APTxRates is subid 28 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.8.1.1.28
 * Description:
Ap name that the terminal accessed.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 64;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 64)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-APTxRates.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
APTxRates_map(char **mib_APTxRates_val_ptr_ptr, size_t *mib_APTxRates_val_ptr_len_ptr, char *raw_APTxRates_val_ptr, size_t raw_APTxRates_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_APTxRates_val_ptr);
    netsnmp_assert((NULL != mib_APTxRates_val_ptr_ptr) && (NULL != mib_APTxRates_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:APTxRates_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement APTxRates non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_APTxRates_val_ptr_len; /* assume equal */
    if((NULL == *mib_APTxRates_val_ptr_ptr) || (*mib_APTxRates_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_APTxRates_val_ptr_ptr = realloc( *mib_APTxRates_val_ptr_ptr, converted_len * sizeof(**mib_APTxRates_val_ptr_ptr));
        if(NULL == *mib_APTxRates_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_APTxRates_val_ptr_len_ptr = converted_len;
    memcpy( *mib_APTxRates_val_ptr_ptr, raw_APTxRates_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* APTxRates_map */

/**
 * Extract the current value of the APTxRates data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param APTxRates_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param APTxRates_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by APTxRates.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*APTxRates_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update APTxRates_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
APTxRates_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, char **APTxRates_val_ptr_ptr, size_t *APTxRates_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != APTxRates_val_ptr_ptr) && (NULL != *APTxRates_val_ptr_ptr));
   netsnmp_assert( NULL != APTxRates_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:APTxRates_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the APTxRates data.
 * set (* APTxRates_val_ptr_ptr ) and (* APTxRates_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for APTxRates data
     */
    if ((NULL == (* APTxRates_val_ptr_ptr )) ||
        ((* APTxRates_val_ptr_len_ptr ) < (rowreq_ctx->data.APTxRates_len * sizeof((* APTxRates_val_ptr_ptr )[0])))) {
        /*
         * allocate space for APTxRates data
         */
        (* APTxRates_val_ptr_ptr ) = malloc(rowreq_ctx->data.APTxRates_len * sizeof((* APTxRates_val_ptr_ptr )[0]));
        if(NULL == (* APTxRates_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* APTxRates_val_ptr_len_ptr ) = rowreq_ctx->data.APTxRates_len * sizeof((* APTxRates_val_ptr_ptr )[0]);
    memcpy( (* APTxRates_val_ptr_ptr ), rowreq_ctx->data.APTxRates, (* APTxRates_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* APTxRates_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.MAXofRateset
 * MAXofRateset is subid 29 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.8.1.1.29
 * Description:
Ap name that the terminal accessed.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 64;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 64)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-MAXofRateset.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
MAXofRateset_map(char **mib_MAXofRateset_val_ptr_ptr, size_t *mib_MAXofRateset_val_ptr_len_ptr, char *raw_MAXofRateset_val_ptr, size_t raw_MAXofRateset_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_MAXofRateset_val_ptr);
    netsnmp_assert((NULL != mib_MAXofRateset_val_ptr_ptr) && (NULL != mib_MAXofRateset_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStaTable:MAXofRateset_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement MAXofRateset non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_MAXofRateset_val_ptr_len; /* assume equal */
    if((NULL == *mib_MAXofRateset_val_ptr_ptr) || (*mib_MAXofRateset_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_MAXofRateset_val_ptr_ptr = realloc( *mib_MAXofRateset_val_ptr_ptr, converted_len * sizeof(**mib_MAXofRateset_val_ptr_ptr));
        if(NULL == *mib_MAXofRateset_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_MAXofRateset_val_ptr_len_ptr = converted_len;
    memcpy( *mib_MAXofRateset_val_ptr_ptr, raw_MAXofRateset_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* MAXofRateset_map */

/**
 * Extract the current value of the MAXofRateset data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param MAXofRateset_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param MAXofRateset_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by MAXofRateset.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*MAXofRateset_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update MAXofRateset_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
MAXofRateset_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, char **MAXofRateset_val_ptr_ptr, size_t *MAXofRateset_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != MAXofRateset_val_ptr_ptr) && (NULL != *MAXofRateset_val_ptr_ptr));
   netsnmp_assert( NULL != MAXofRateset_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:MAXofRateset_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the MAXofRateset data.
 * set (* MAXofRateset_val_ptr_ptr ) and (* MAXofRateset_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for MAXofRateset data
     */
    if ((NULL == (* MAXofRateset_val_ptr_ptr )) ||
        ((* MAXofRateset_val_ptr_len_ptr ) < (rowreq_ctx->data.MAXofRateset_len * sizeof((* MAXofRateset_val_ptr_ptr )[0])))) {
        /*
         * allocate space for MAXofRateset data
         */
        (* MAXofRateset_val_ptr_ptr ) = malloc(rowreq_ctx->data.MAXofRateset_len * sizeof((* MAXofRateset_val_ptr_ptr )[0]));
        if(NULL == (* MAXofRateset_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* MAXofRateset_val_ptr_len_ptr ) = rowreq_ctx->data.MAXofRateset_len * sizeof((* MAXofRateset_val_ptr_ptr )[0]);
    memcpy( (* MAXofRateset_val_ptr_ptr ), rowreq_ctx->data.MAXofRateset, (* MAXofRateset_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* MAXofRateset_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStaEntry.wtpStaIPV6Addr
 * wtpStaIPV6Addr is subid 30 of dot11WtpStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1.1.30
 * Description:
The station ipv6 address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 2x:
 *
 * Ranges:  16;
 *
 * Its syntax is InetAddressIPv6 (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 16)
 */
/**
 * Extract the current value of the wtpStaIPV6Addr data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaIPV6Addr_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpStaIPV6Addr_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpStaIPV6Addr.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpStaIPV6Addr_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpStaIPV6Addr_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpStaIPV6Addr_get( dot11WtpStaTable_rowreq_ctx *rowreq_ctx, char **wtpStaIPV6Addr_val_ptr_ptr, size_t *wtpStaIPV6Addr_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpStaIPV6Addr_val_ptr_ptr) && (NULL != *wtpStaIPV6Addr_val_ptr_ptr));
   netsnmp_assert( NULL != wtpStaIPV6Addr_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStaTable:wtpStaIPV6Addr_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaIPV6Addr data.
 * copy (* wtpStaIPV6Addr_val_ptr_ptr ) data and (* wtpStaIPV6Addr_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpStaIPV6Addr data
     */
    if ((NULL == (* wtpStaIPV6Addr_val_ptr_ptr )) ||
        ((* wtpStaIPV6Addr_val_ptr_len_ptr ) <
         (rowreq_ctx->data.wtpStaIPV6Addr_len* sizeof(rowreq_ctx->data.wtpStaIPV6Addr[0])))) {
        /*
         * allocate space for wtpStaIPV6Addr data
         */
        (* wtpStaIPV6Addr_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpStaIPV6Addr_len* sizeof(rowreq_ctx->data.wtpStaIPV6Addr[0]));
        if(NULL == (* wtpStaIPV6Addr_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpStaIPV6Addr_val_ptr_len_ptr ) = rowreq_ctx->data.wtpStaIPV6Addr_len* sizeof(rowreq_ctx->data.wtpStaIPV6Addr[0]);
    memcpy( (* wtpStaIPV6Addr_val_ptr_ptr ), rowreq_ctx->data.wtpStaIPV6Addr, rowreq_ctx->data.wtpStaIPV6Addr_len* sizeof(rowreq_ctx->data.wtpStaIPV6Addr[0]) );

    return MFD_SUCCESS;
} /* wtpStaIPV6Addr_get */



/** @} */
