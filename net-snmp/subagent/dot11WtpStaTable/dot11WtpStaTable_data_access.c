/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "ws_sta.h"
#include "ws_sysinfo.h"
#include "mibs_public.h"
#include "autelanWtpGroup.h"
/* include our parent header */
#include "dot11WtpStaTable.h"


#include "dot11WtpStaTable_data_access.h"

#define DEFAULT_WTPTXTERMINALBYTEMOUNT_VALUE	0

static unsigned long long
mib_get_wtpTxTerminalByteMount_value(void) {
	unsigned long long wtpTxTerminalByteMount_value = DEFAULT_WTPTXTERMINALBYTEMOUNT_VALUE;	/*default*/
	FILE *fp = NULL;    	
	
	fp = fopen("/var/run/wtpTxTerminalByteMount_value.txt", "r");
	if(NULL == fp) {
		snmp_log(LOG_DEBUG, "Open wtpTxTerminalByteMount_value.txt error\n");
		goto FAIL_GET;
	}

	if(fscanf(fp, "%llu", &wtpTxTerminalByteMount_value) <= 0) {
		snmp_log(LOG_DEBUG, "Fscanf wtpTxTerminalByteMount_value.txt error\n");
		fclose(fp);
		goto FAIL_GET;
	}

	fclose(fp);

	if(!wtpTxTerminalByteMount_value) {
		goto FAIL_GET;
	}

	return wtpTxTerminalByteMount_value;
	
FAIL_GET:
	return DEFAULT_WTPTXTERMINALBYTEMOUNT_VALUE;	/*default*/
}


/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpStaTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpStaTable is subid 1 of wtpStation.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.1, length: 12
*/

/**
 * initialization for dot11WtpStaTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11WtpStaTable_reg
 *        Pointer to dot11WtpStaTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11WtpStaTable_init_data(dot11WtpStaTable_registration_ptr dot11WtpStaTable_reg)
{
    DEBUGMSGTL(("verbose:dot11WtpStaTable:dot11WtpStaTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11WtpStaTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11WtpStaTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11WtpStaTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11WtpStaTable:dot11WtpStaTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11WtpStaTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11WtpStaTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11WTPSTATABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11WtpStaTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11WtpStaTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11WtpStaTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11WtpStaTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11WtpStaTable_cache_load\n");

    dot11WtpStaTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
        /** 128 - 1(entry) - 1(col) - 1(other indexes) = 111 */
   char   wtpMacAddr[20] = { 0 };
   size_t      wtpMacAddr_len = 0;
        /*
         * wtpStaMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
         */
        /** 128 - 1(entry) - 1(col) - 1(other indexes) = 111 */
	char   wtpStaMacAddr[20] = { 0 };
	size_t      wtpStaMacAddr_len = 0;
	
    DEBUGMSGTL(("verbose:dot11WtpStaTable:dot11WtpStaTable_cache_load","called\n"));
    unsigned long long wtpTxTerminalByteMount_value = mib_get_wtpTxTerminalByteMount_value();
	snmp_log(LOG_DEBUG, "wtpTxTerminalByteMount_value = %llu\n", wtpTxTerminalByteMount_value);
	
    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_all_wtp_station_information_cmd\n");
    messageHead = list_connection_call_dbus_method(show_all_wtp_station_information_cmd, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_all_wtp_station_information_cmd,messageHead=%p\n", messageHead);

	
	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    struct WtpStaInfo *StaHead = messageNode->message;
		    if(StaHead)
		    {
		        struct WtpStaInfo *StaNode = NULL;

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/

    /*
     * TODO:351:M: |-> Load/update data in the dot11WtpStaTable container.
     * loop over your dot11WtpStaTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
		for(StaNode=StaHead; StaNode!= NULL; StaNode=StaNode->next)
		{
        /*
         * TODO:352:M: |   |-> set indexes in new dot11WtpStaTable rowreq context.
         */
        if(StaNode->wtpMacAddr)
        {
        	memset(wtpMacAddr,0,sizeof(wtpMacAddr));
			strncpy(wtpMacAddr,StaNode->wtpMacAddr,sizeof(wtpMacAddr)-1);
        }
		if(StaNode->wtpTerminalMacAddr)
        {
        	memset(wtpStaMacAddr,0,sizeof(wtpStaMacAddr));
			strncpy(wtpStaMacAddr,StaNode->wtpTerminalMacAddr,sizeof(wtpStaMacAddr)-1);
        }
		wtpMacAddr_len = strlen(wtpMacAddr);
		wtpStaMacAddr_len = strlen(wtpStaMacAddr);
        rowreq_ctx = dot11WtpStaTable_allocate_rowreq_ctx();
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
            free_dbus_message_list(&messageHead, Free_show_all_wtp_station_information_cmd);
            return MFD_RESOURCE_UNAVAILABLE;
        }
        if(MFD_SUCCESS != dot11WtpStaTable_indexes_set(rowreq_ctx
                               , wtpMacAddr, wtpMacAddr_len
                               , wtpStaMacAddr, wtpStaMacAddr_len
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "dot11WtpStaTable cache.\n");
            dot11WtpStaTable_release_rowreq_ctx(rowreq_ctx);
            continue;
        }

        /*
         * TODO:352:r: |   |-> populate dot11WtpStaTable data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for wtpStaIP
     * wtpStaIP(2)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpStaIP mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpStaIP = StaNode->wtpStaIp;
    
    /*
     * setup/save data for wtpWirelessClientSNR
     * wtpWirelessClientSNR(3)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessClientSNR mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessClientSNR = StaNode->wtpWirelessClientSNR;
    
    /*
     * setup/save data for wtpTxTerminalPackMount
     * wtpTxTerminalPackMount(4)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpTxTerminalPackMount mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpTxTerminalPackMount = (unsigned long)StaNode->wtpSendTerminalPackMount;
    
    /*
     * setup/save data for wtpTxTerminalByteMount
     * wtpTxTerminalByteMount(5)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpTxTerminalByteMount mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
	if(0 == StaNode->wtpSendTerminalByteMount)
	{
		rowreq_ctx->data.wtpTxTerminalByteMount = wtpTxTerminalByteMount_value;
	}
	else
	{
		rowreq_ctx->data.wtpTxTerminalByteMount = StaNode->wtpSendTerminalByteMount;
	}
    
    /*
     * setup/save data for wtpTerminalRxPackMount
     * wtpTerminalRxPackMount(6)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpTerminalRxPackMount mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpTerminalRxPackMount = (unsigned long)StaNode->wtpTerminalRecvPackMount;
    
    /*
     * setup/save data for wtpTerminalRxByteMount
     * wtpTerminalRxByteMount(7)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpTerminalRxByteMount mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpTerminalRxByteMount = StaNode->wtpTerminalRecvByteMount;
    
    /*
     * setup/save data for wtpTerminalResPack
     * wtpTerminalResPack(8)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpTerminalResPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpTerminalResPack = StaNode->wtpTerminalResPack;
    
    /*
     * setup/save data for wtpTerminalResByte
     * wtpTerminalResByte(9)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpTerminalResByte mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpTerminalResByte = StaNode->wtpTerminalResByte;
    
    /*
     * setup/save data for wtpTerminalRxErrPack
     * wtpTerminalRxErrPack(10)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpTerminalRxErrPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpTerminalRxErrPack = StaNode->wtpTerminalRecvWrongPack;
    
    /*
     * setup/save data for wtpMacTermAddrUsrOnlineTime
     * wtpMacTermAddrUsrOnlineTime(11)/TICKS/ASN_TIMETICKS/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpMacTermAddrUsrOnlineTime mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpMacTermAddrUsrOnlineTime = StaNode->wtpMacTermAddrUsrOnlineTime;
    
    /*
     * setup/save data for wtpMacTermAddrUsrTxSpd
     * wtpMacTermAddrUsrTxSpd(12)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpMacTermAddrUsrTxSpd mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpMacTermAddrUsrTxSpd = (unsigned long)StaNode->wtpMacTermAddrUsrSendSpd;
    
    /*
     * setup/save data for wtpMacTermAddrUsrRxSpd
     * wtpMacTermAddrUsrRxSpd(13)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpMacTermAddrUsrRxSpd mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpMacTermAddrUsrRxSpd = (unsigned long)StaNode->wtpMacTermAddrUsrRecvSpd;
    
    /*
     * setup/save data for wtpMacTermAddrUsrAllThroughput
     * wtpMacTermAddrUsrAllThroughput(14)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpMacTermAddrUsrAllThroughput mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpMacTermAddrUsrAllThroughput = (unsigned long)StaNode->wtpMacTermAddrUsrAllThroughput;
    
    /*
     * setup/save data for wtpMacTermAPReceivedStaSignalStrength
     * wtpMacTermAPReceivedStaSignalStrength(15)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpMacTermAPReceivedStaSignalStrength mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpMacTermAPReceivedStaSignalStrength_map(&rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength, &rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength_len,
     *                wtpMacTermAPReceivedStaSignalStrength, wtpMacTermAPReceivedStaSignalStrength_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpMacTermAPReceivedStaSignalStrength data
     */
    int wtpMacTermAPReceivedStaSignalStrength_len = 0;
    if ((NULL == rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength) ||
        (rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength_len < (wtpMacTermAPReceivedStaSignalStrength_len * sizeof(rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }

	if(StaNode->wtpMacTermAPReceivedStaSignalStrength)
	{
		wtpMacTermAPReceivedStaSignalStrength_len = MIN(strlen(StaNode->wtpMacTermAPReceivedStaSignalStrength),sizeof(rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength));
		rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength_len = wtpMacTermAPReceivedStaSignalStrength_len * sizeof(rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength[0]);
		memcpy( rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength, StaNode->wtpMacTermAPReceivedStaSignalStrength, rowreq_ctx->data.wtpMacTermAPReceivedStaSignalStrength_len );
	}
    
    /*
     * setup/save data for wtpMacTermStaTxFragmentedPkts
     * wtpMacTermStaTxFragmentedPkts(16)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpMacTermStaTxFragmentedPkts mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpMacTermStaTxFragmentedPkts = (unsigned long)StaNode->wtpMacTermStaTxFragmentedPkts;
    
    /*
     * setup/save data for wtpMacTermAPTxFragmentedPkts
     * wtpMacTermAPTxFragmentedPkts(17)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpMacTermAPTxFragmentedPkts mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpMacTermAPTxFragmentedPkts = (unsigned long)StaNode->wtpMacTermAPTxFragmentedPkts;
    
    /*
     * setup/save data for wtpBelongAPID
     * wtpBelongAPID(18)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpBelongAPID mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpBelongAPID = StaNode->wtpBelongAPID;
    
    /*
     * setup/save data for wtpTerminalAccessTime
     * wtpTerminalAccessTime(19)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpTerminalAccessTime mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpTerminalAccessTime_map(&rowreq_ctx->data.wtpTerminalAccessTime, &rowreq_ctx->data.wtpTerminalAccessTime_len,
     *                wtpTerminalAccessTime, wtpTerminalAccessTime_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpTerminalAccessTime data
     */
  //  if ((NULL == rowreq_ctx->data.wtpTerminalAccessTime) ||
  //      (rowreq_ctx->data.wtpTerminalAccessTime_len < (wtpTerminalAccessTime_len * sizeof(rowreq_ctx->data.wtpTerminalAccessTime[0])))) {
  //      snmp_log(LOG_ERR,"not enough space for value\n");
  //      return MFD_ERROR;
  // }

    
	if(StaNode->wtpterminalaccesstime) {
		size_t wtpTerminalAccessTime_len = MIN(strlen(StaNode->wtpterminalaccesstime), sizeof(rowreq_ctx->data.wtpTerminalAccessTime) - 1);
        rowreq_ctx->data.wtpTerminalAccessTime_len = wtpTerminalAccessTime_len * sizeof(rowreq_ctx->data.wtpTerminalAccessTime[0]);
        memcpy( rowreq_ctx->data.wtpTerminalAccessTime, StaNode->wtpterminalaccesstime, rowreq_ctx->data.wtpTerminalAccessTime_len );
    }
    /*
     * setup/save data for wtpBelongApName
     * wtpBelongApName(21)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpBelongApName mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpBelongApName_map(&rowreq_ctx->data.wtpBelongApName, &rowreq_ctx->data.wtpBelongApName_len,
     *                wtpBelongApName, wtpBelongApName_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpBelongApName data
     */
    /*if ((NULL == rowreq_ctx->data.wtpBelongApName) ||
        (rowreq_ctx->data.wtpBelongApName_len < (wtpBelongApName_len * sizeof(rowreq_ctx->data.wtpBelongApName[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/
    if(StaNode->wtpName) {
		size_t wtpBelongApName_len = MIN(strlen(StaNode->wtpName), sizeof(rowreq_ctx->data.wtpBelongApName) - 1);
        rowreq_ctx->data.wtpBelongApName_len = wtpBelongApName_len * sizeof(rowreq_ctx->data.wtpBelongApName[0]);
        memcpy( rowreq_ctx->data.wtpBelongApName, StaNode->wtpName, rowreq_ctx->data.wtpBelongApName_len );
    }
    
    rowreq_ctx->data.wtpTerminalRecvDataPackMount = (unsigned long)StaNode->wtpTerminalRecvDataPackMount;
	rowreq_ctx->data.wtpSendTerminalDataPackMount = (unsigned long)StaNode->wtpSendTerminalDataPackMount;

    rowreq_ctx->data.wtpTerminalTxDataRatePkts_len = sizeof(rowreq_ctx->data.wtpTerminalTxDataRatePkts);
    memcpy( rowreq_ctx->data.wtpTerminalTxDataRatePkts, StaNode->wtp_sta_statistics_info.APStaTxDataRatePkts, rowreq_ctx->data.wtpTerminalTxDataRatePkts_len );

    rowreq_ctx->data.wtpTerminalRxDataRatePkts_len = sizeof(rowreq_ctx->data.wtpTerminalRxDataRatePkts);
    memcpy( rowreq_ctx->data.wtpTerminalRxDataRatePkts, StaNode->wtp_sta_statistics_info.APStaRxDataRatePkts, rowreq_ctx->data.wtpTerminalRxDataRatePkts_len );

    rowreq_ctx->data.wtpTerminalTxSignalStrengthPkts_len = sizeof(rowreq_ctx->data.wtpTerminalTxSignalStrengthPkts);
    memcpy( rowreq_ctx->data.wtpTerminalTxSignalStrengthPkts, StaNode->wtp_sta_statistics_info.APStaTxSignalStrengthPkts, rowreq_ctx->data.wtpTerminalTxSignalStrengthPkts_len );

	/*
     * setup/save data for APRxRates
     * APRxRates(27)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define APRxRates mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    APRxRates_map(&rowreq_ctx->data.APRxRates, &rowreq_ctx->data.APRxRates_len,
     *                APRxRates, APRxRates_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for APRxRates data
     */
    /*if ((NULL == rowreq_ctx->data.APRxRates) ||
        (rowreq_ctx->data.APRxRates_len < (APRxRates_len * sizeof(rowreq_ctx->data.APRxRates[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/
    char temp[64] = { 0 };
	memset(temp, 0, sizeof(temp));
	snprintf(temp, sizeof(temp)-1, "%llu", StaNode->wtpMacTermAddrUsrSendSpd);

	size_t APRxRates_len = MIN(strlen(temp), sizeof(rowreq_ctx->data.APRxRates) - 1);
    rowreq_ctx->data.APRxRates_len = APRxRates_len * sizeof(rowreq_ctx->data.APRxRates[0]);
    memcpy( rowreq_ctx->data.APRxRates, temp, rowreq_ctx->data.APRxRates_len );

	/*
     * setup/save data for APTxRates
     * APTxRates(28)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define APTxRates mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    APTxRates_map(&rowreq_ctx->data.APTxRates, &rowreq_ctx->data.APTxRates_len,
     *                APTxRates, APTxRates_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for APTxRates data
     */
    /*if ((NULL == rowreq_ctx->data.APTxRates) ||
        (rowreq_ctx->data.APTxRates_len < (APTxRates_len * sizeof(rowreq_ctx->data.APTxRates[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/
    memset(temp, 0, sizeof(temp));
	snprintf(temp, sizeof(temp)-1, "%llu", StaNode->wtpMacTermAddrUsrRecvSpd);
	
	size_t APTxRates_len = MIN(strlen(temp), sizeof(rowreq_ctx->data.APTxRates) - 1);
    rowreq_ctx->data.APTxRates_len = APTxRates_len * sizeof(rowreq_ctx->data.APTxRates[0]);
    memcpy( rowreq_ctx->data.APTxRates, temp, rowreq_ctx->data.APTxRates_len );

	/*
     * setup/save data for MAXofRateset
     * MAXofRateset(29)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define MAXofRateset mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    MAXofRateset_map(&rowreq_ctx->data.MAXofRateset, &rowreq_ctx->data.MAXofRateset_len,
     *                MAXofRateset, MAXofRateset_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for MAXofRateset data
     */
    /*if ((NULL == rowreq_ctx->data.MAXofRateset) ||
        (rowreq_ctx->data.MAXofRateset_len < (MAXofRateset_len * sizeof(rowreq_ctx->data.MAXofRateset[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/
    memset(temp, 0, sizeof(temp));
	snprintf(temp, sizeof(temp)-1, "%f", (float)(((float)(StaNode->MAXofRateset) ) / 10));	
	
	size_t MAXofRateset_len = MIN(strlen(temp), sizeof(rowreq_ctx->data.MAXofRateset) - 1);
    rowreq_ctx->data.MAXofRateset_len = MAXofRateset_len * sizeof(rowreq_ctx->data.MAXofRateset[0]);
    memcpy( rowreq_ctx->data.MAXofRateset, temp, rowreq_ctx->data.MAXofRateset_len );
    
    /*
     * setup/save data for wtpStaIPV6Addr
     * wtpStaIPV6Addr(30)/InetAddressIPv6/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /** no mapping */
    /*
     * make sure there is enough space for wtpStaIPV6Addr data
     */
	char wtpStaIPV6Address[128]={0};
	int  wtpStaIPV6Address_len= 0;
	memset(wtpStaIPV6Address, 0, sizeof(wtpStaIPV6Address));
	sprintf(wtpStaIPV6Address,"%x:%x:%x:%x:%x:%x:%x:%x",StaNode->wtpStaIp6.s6_addr16[0],StaNode->wtpStaIp6.s6_addr16[1]
							    ,StaNode->wtpStaIp6.s6_addr16[2],StaNode->wtpStaIp6.s6_addr16[3]
							    ,StaNode->wtpStaIp6.s6_addr16[4],StaNode->wtpStaIp6.s6_addr16[5]
							    ,StaNode->wtpStaIp6.s6_addr16[6],StaNode->wtpStaIp6.s6_addr16[7]);

	wtpStaIPV6Address_len = MIN(strlen(wtpStaIPV6Address),sizeof(rowreq_ctx->data.wtpStaIPV6Addr)-1);
	rowreq_ctx->data.wtpStaIPV6Addr_len = wtpStaIPV6Address_len* sizeof(rowreq_ctx->data.wtpStaIPV6Addr[0]);
	memcpy( rowreq_ctx->data.wtpStaIPV6Addr, wtpStaIPV6Address, wtpStaIPV6Address_len );
    
        /*
         * insert into table container
         */
    	            if(CONTAINER_INSERT(container, rowreq_ctx))
        	        {
        				dot11WtpStaTable_release_rowreq_ctx(rowreq_ctx);
        	        }
        	        ++count;
        		}
            }
    	}
	    free_dbus_message_list(&messageHead, Free_show_all_wtp_station_information_cmd);
	}
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11WtpStaTable:dot11WtpStaTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11WtpStaTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11WtpStaTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11WtpStaTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11WtpStaTable:dot11WtpStaTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11WtpStaTable cache.
     */
} /* dot11WtpStaTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11WtpStaTable_row_prep( dot11WtpStaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpStaTable:dot11WtpStaTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11WtpStaTable_row_prep */

/** @} */
