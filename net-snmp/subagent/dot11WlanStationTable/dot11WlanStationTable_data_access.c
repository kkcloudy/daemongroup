/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WlanStationTable.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "ws_sta.h"
#include "autelanWtpGroup.h"


#include "dot11WlanStationTable_data_access.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WlanStationTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WlanStationTable is subid 3 of dot11wlanConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.3, length: 12
*/

/**
 * initialization for dot11WlanStationTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11WlanStationTable_reg
 *        Pointer to dot11WlanStationTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11WlanStationTable_init_data(dot11WlanStationTable_registration_ptr dot11WlanStationTable_reg)
{
    DEBUGMSGTL(("verbose:dot11WlanStationTable:dot11WlanStationTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11WlanStationTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11WlanStationTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11WlanStationTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11WlanStationTable:dot11WlanStationTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11WlanStationTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11WlanStationTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11WLANSTATIONTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11WlanStationTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11WlanStationTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11WlanStationTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11WlanStationTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11WlanStationTable_cache_load\n");
    dot11WlanStationTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wlanID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   wlanID;
        /*
         * StaMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
         */
        /** 128 - 1(entry) - 1(col) - 1(other indexes) = 111 */
   char   StaMacAddr[20] = { 0 };
   size_t      StaMacAddr_len;

    DEBUGMSGTL(("verbose:dot11WlanStationTable:dot11WlanStationTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
    //    return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11WlanStationTable container.
     * loop over your dot11WlanStationTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_wlan_sta_of_all_cmd\n");
    messageHead = list_connection_call_dbus_method(show_wlan_sta_of_all_cmd, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_wlan_sta_of_all_cmd,messageHead=%p\n", messageHead);

	
	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    struct dcli_wlan_info *wlan = messageNode->message;
		    if(wlan)
		    {
		        struct dcli_wlan_info *ShowNode = NULL;
                for(ShowNode = wlan; NULL != ShowNode; ShowNode = ShowNode->next)
                {
                    wlanID = local_to_global_ID(messageNode->parameter, ShowNode->WlanID, WIRELESS_MAX_NUM);
                    if(NULL != ShowNode->bss_list)
                    {
                        struct dcli_sta_info *stainfo = NULL;
                        for(stainfo = ShowNode->bss_list->sta_list; NULL != stainfo; stainfo = stainfo->next)
                        {
                            memset(StaMacAddr, 0, 20);
                            snprintf(StaMacAddr,sizeof(StaMacAddr)-1, 
									 "%02X:%02X:%02X:%02X:%02X:%02X",
                				     stainfo->addr[0], stainfo->addr[1], 
                				     stainfo->addr[2], stainfo->addr[3], 
                				     stainfo->addr[4], stainfo->addr[5]);
            						  
            			   
				/*
				***************************************************
				*** 			START EXAMPLE CODE				***
				***---------------------------------------------***/
				/*
				 * get a line (skip blank lines)
				 */
				//do {
				//	  if (!fgets(line, sizeof(line), filep)) {
				//		  /* we're done */
				//		  fclose(filep);
				//		  filep = NULL;
				//	  }
				//} while (filep && (line[0] == '\n'));
			
				/*
				 * check for end of data
				 */
				//if(NULL == filep)
				//	  break;
			
				/*
				 * parse line into variables
				 */
				/*
				***---------------------------------------------***
				*** 			 END  EXAMPLE CODE				***
				***************************************************/
			
					/*
					 * TODO:352:M: |   |-> set indexes in new dot11WlanStationTable rowreq context.
					 */
					rowreq_ctx = dot11WlanStationTable_allocate_rowreq_ctx();
					if (NULL == rowreq_ctx) {
						snmp_log(LOG_ERR, "memory allocation failed\n");
                        free_dbus_message_list(&messageHead, Free_show_wlan_sta_of_all_cmd);
						return MFD_RESOURCE_UNAVAILABLE;
					}
					StaMacAddr_len = strlen(StaMacAddr);
					if(MFD_SUCCESS != dot11WlanStationTable_indexes_set(rowreq_ctx
										   , wlanID
										   , StaMacAddr, StaMacAddr_len
						   )) {
						snmp_log(LOG_ERR,"error setting index while loading "
								 "dot11WlanStationTable cache.\n");
						dot11WlanStationTable_release_rowreq_ctx(rowreq_ctx);
						continue;
					}
			        memcpy(&rowreq_ctx->data.parameter, &messageNode->parameter, sizeof(dbus_parameter));
                    rowreq_ctx->data.localWlanID = ShowNode->WlanID;
					/*
					 * TODO:352:r: |   |-> populate dot11WlanStationTable data context.
					 * Populate data context here. (optionally, delay until row prep)
					 */
				/*
				 * TRANSIENT or semi-TRANSIENT data:
				 * copy data or save any info needed to do it in row_prep.
				 */
				/*
				 * setup/save data for StaOnlineTime
				 * StaOnlineTime(2)/TICKS/ASN_TIMETICKS/u_long(u_long)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define StaOnlineTime mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				
				//time_t now,online_time,now_sysrun,statime;
				time_t StaOnlineTime = 0;
				int 	hour,min,sec;

				//time(&now);
				//get_sysruntime(&now_sysrun);
				time_t online_time=stainfo->sta_online_time_new;
				hour=online_time/3600;
				min=(online_time-hour*3600)/60;
				sec=(online_time-hour*3600)%60;
				//statime = now - online_time;
				StaOnlineTime=(hour*3600+min*60+sec)*100;
				rowreq_ctx->data.StaOnlineTime = StaOnlineTime;
				
				/*
				 * setup/save data for StaAccessTime
				 * StaAccessTime(3)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
				 */
				/*
				 * TODO:246:r: |-> Define StaAccessTime mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * if(MFD_SUCCESS !=
				 *	  StaAccessTime_map(&rowreq_ctx->data.StaAccessTime, &rowreq_ctx->data.StaAccessTime_len,
				 *				  StaAccessTime, StaAccessTime_len, 0)) {
				 *	  return MFD_ERROR;
				 * }
				 */
				/*
				 * make sure there is enough space for StaAccessTime data
				 */
				//if ((NULL == rowreq_ctx->data.StaAccessTime) ||
				  //  (rowreq_ctx->data.StaAccessTime_len < (StaAccessTime_len * sizeof(rowreq_ctx->data.StaAccessTime[0])))) {
			  //	snmp_log(LOG_ERR,"not enough space for value\n");
				  //  return MFD_ERROR;
				//}
				int StaAccessTime_len = 0;
				char StaAccessTime[255] = {0};
				memset(StaAccessTime,0,255);
				snprintf(StaAccessTime,sizeof(StaAccessTime)-1,"%s",ctime(&stainfo->sta_access_time));
  
				
				StaAccessTime_len = MIN(strlen(StaAccessTime),sizeof(StaAccessTime)-1);
				
				rowreq_ctx->data.StaAccessTime_len = StaAccessTime_len * sizeof(rowreq_ctx->data.StaAccessTime[0]);
				memcpy( rowreq_ctx->data.StaAccessTime, StaAccessTime, rowreq_ctx->data.StaAccessTime_len );
				
				delete_enter(StaAccessTime);
				
				/*
				 * setup/save data for StaUplinkTrafficLimitThreshold
				 * StaUplinkTrafficLimitThreshold(4)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define StaUplinkTrafficLimitThreshold mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.StaUplinkTrafficLimitThreshold = stainfo->sta_traffic_limit;
				/*
				 * setup/save data for StaDownlinkTrafficLimitThreshold
				 * StaDownlinkTrafficLimitThreshold(5)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define StaDownlinkTrafficLimitThreshold mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				rowreq_ctx->data.StaDownlinkTrafficLimitThreshold = stainfo->sta_send_traffic_limit;
				/*
				 * setup/save data for StaUplinkBitRate
				 * StaUplinkBitRate(6)/COUNTER64/ASN_COUNTER64/U64(U64)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define StaUplinkBitRate mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				//rowreq_ctx->data.StaUplinkBitRate.high = StaUplinkBitRate.high;
				unsigned long long StaUplinkBitRate = 0;
				if(online_time>0)
				{
				  StaUplinkBitRate = ((stainfo->rxbytes)*8)/online_time;
				}
				rowreq_ctx->data.StaUplinkBitRate = StaUplinkBitRate;
				
				/*
				 * setup/save data for StaDownlinkBitRate
				 * StaDownlinkBitRate(7)/COUNTER64/ASN_COUNTER64/U64(U64)//l/A/w/e/r/d/h
				 */
				/*
				 * TODO:246:r: |-> Define StaDownlinkBitRate mapping.
				 * Map values between raw/native values and MIB values
				 *
				 * Integer based value can usually just do a direct copy.
				 */
				//rowreq_ctx->data.StaDownlinkBitRate.high = StaDownlinkBitRate.high;
				unsigned long long StaDownlinkBitRate = 0;
				if(online_time>0)
				{
				  StaDownlinkBitRate = ((stainfo->txbytes)*8)/online_time;
				}
				rowreq_ctx->data.StaDownlinkBitRate = StaDownlinkBitRate;
				
					/*
					 * insert into table container
					 */
    					    if(CONTAINER_INSERT(container, rowreq_ctx))
        					{
        						dot11WlanStationTable_release_rowreq_ctx(rowreq_ctx);
        					}
        					++count;
                        }
                    }
                }
            }
        }
        free_dbus_message_list(&messageHead, Free_show_wlan_sta_of_all_cmd);
    }


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
    //    fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11WlanStationTable:dot11WlanStationTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11WlanStationTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11WlanStationTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11WlanStationTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11WlanStationTable:dot11WlanStationTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11WlanStationTable cache.
     */
} /* dot11WlanStationTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11WlanStationTable_row_prep( dot11WlanStationTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WlanStationTable:dot11WlanStationTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11WlanStationTable_row_prep */

/** @} */
