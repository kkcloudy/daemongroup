/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WlanStationTable.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "ws_sta.h"
#include "autelanWtpGroup.h"

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WlanStationTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WlanStationTable is subid 3 of dot11wlanConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.3, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WlanStationTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11WlanStationTable_undo_setup( dot11WlanStationTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WlanStationTable:dot11WlanStationTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11WlanStationTable undo.
     * set up dot11WlanStationTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11WlanStationTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WlanStationTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WlanStationTable_undo_cleanup( dot11WlanStationTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WlanStationTable:dot11WlanStationTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11WlanStationTable undo.
     */

    return rc;
} /* dot11WlanStationTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WlanStationTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WlanStationTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WlanStationTable_commit( dot11WlanStationTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11WlanStationTable:dot11WlanStationTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11WlanStationTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_STAUPLINKTRAFFICLIMITTHRESHOLD) {
       save_flags &= ~FLAG_STAUPLINKTRAFFICLIMITTHRESHOLD; /* clear StaUplinkTrafficLimitThreshold */
       /*
        * TODO:482:o: |-> commit column StaUplinkTrafficLimitThreshold.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WlanStationTable column StaUplinkTrafficLimitThreshold commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo StaUplinkTrafficLimitThreshold
             */
            rowreq_ctx->column_set_flags |= FLAG_STAUPLINKTRAFFICLIMITTHRESHOLD;
       }
    }

    if (save_flags & FLAG_STADOWNLINKTRAFFICLIMITTHRESHOLD) {
       save_flags &= ~FLAG_STADOWNLINKTRAFFICLIMITTHRESHOLD; /* clear StaDownlinkTrafficLimitThreshold */
       /*
        * TODO:482:o: |-> commit column StaDownlinkTrafficLimitThreshold.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WlanStationTable column StaDownlinkTrafficLimitThreshold commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo StaDownlinkTrafficLimitThreshold
             */
            rowreq_ctx->column_set_flags |= FLAG_STADOWNLINKTRAFFICLIMITTHRESHOLD;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11WlanStationTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WlanStationTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WlanStationTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WlanStationTable_undo_commit( dot11WlanStationTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WlanStationTable:dot11WlanStationTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11WlanStationTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11WlanStationTable_undo_commit */

/*
 * TODO:420:r: Implement dot11WlanStationTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11VlanConfigEntry.wlanID
 * wlanID is subid 1 of dot11VlanConfigEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.7.1.1
 * Description:
Vlan ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/*
 * TODO:440:M: Implement dot11WlanStationTable node value checks.
 * TODO:450:M: Implement dot11WlanStationTable undo functions.
 * TODO:460:M: Implement dot11WlanStationTable set functions.
 * TODO:480:M: Implement dot11WlanStationTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11WlanStaEntry.StaUplinkTrafficLimitThreshold
 * StaUplinkTrafficLimitThreshold is subid 4 of dot11WlanStaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.3.1.4
 * Description:
Station uplink traffic limit threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param StaUplinkTrafficLimitThreshold_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WlanStationTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
StaUplinkTrafficLimitThreshold_check_value( dot11WlanStationTable_rowreq_ctx *rowreq_ctx, long StaUplinkTrafficLimitThreshold_val)
{
    DEBUGMSGTL(("verbose:dot11WlanStationTable:StaUplinkTrafficLimitThreshold_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid StaUplinkTrafficLimitThreshold value.
     */

    return MFD_SUCCESS; /* StaUplinkTrafficLimitThreshold value not illegal */
} /* StaUplinkTrafficLimitThreshold_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WlanStationTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WlanStationTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
StaUplinkTrafficLimitThreshold_undo_setup( dot11WlanStationTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WlanStationTable:StaUplinkTrafficLimitThreshold_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup StaUplinkTrafficLimitThreshold undo.
     */
    /*
     * copy StaUplinkTrafficLimitThreshold data
     * set rowreq_ctx->undo->StaUplinkTrafficLimitThreshold from rowreq_ctx->data.StaUplinkTrafficLimitThreshold
     */
    rowreq_ctx->undo->StaUplinkTrafficLimitThreshold = rowreq_ctx->data.StaUplinkTrafficLimitThreshold;


    return MFD_SUCCESS;
} /* StaUplinkTrafficLimitThreshold_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param StaUplinkTrafficLimitThreshold_val
 *        A long containing the new value.
 */
int
StaUplinkTrafficLimitThreshold_set( dot11WlanStationTable_rowreq_ctx *rowreq_ctx, long StaUplinkTrafficLimitThreshold_val )
{

    DEBUGMSGTL(("verbose:dot11WlanStationTable:StaUplinkTrafficLimitThreshold_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement StaUplinkTrafficLimitThreshold reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set StaUplinkTrafficLimitThreshold value.
     * set StaUplinkTrafficLimitThreshold value in rowreq_ctx->data
     */
    int rc = MFD_ERROR;
    int ret = 0;
	char wid[10] = {0};
	memset(wid,0,10);
	snprintf(wid,sizeof(wid)-1,"%d",rowreq_ctx->tbl_idx.wlanID);
    char value[30] = {0};
	memset(value,0,30);
	snprintf(value,sizeof(value)-1,"%d",StaUplinkTrafficLimitThreshold_val);
	
	int ret1 = 0;
	struct dcli_sta_info *sta = NULL;
	
	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	ret1=show_sta_bymac(rowreq_ctx->data.parameter, connection, rowreq_ctx->tbl_idx.StaMacAddr, &sta);
	if(1 == ret1)
	{
		ret = radio_bss_traffic_limit_sta_value_cmd(rowreq_ctx->data.parameter, connection, sta->radio_g_id, wid, rowreq_ctx->tbl_idx.StaMacAddr, value);
		if(1 == ret)
		{
			rowreq_ctx->data.StaUplinkTrafficLimitThreshold = StaUplinkTrafficLimitThreshold_val;
			rc = MFD_SUCCESS;
		}
		else
		{
			rc = MFD_ERROR;
		}
	}
	else
	{
		rc = MFD_ERROR;
	}

	Free_bss_bymac(sta);
	
    return rc;
} /* StaUplinkTrafficLimitThreshold_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
StaUplinkTrafficLimitThreshold_undo( dot11WlanStationTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WlanStationTable:StaUplinkTrafficLimitThreshold_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up StaUplinkTrafficLimitThreshold undo.
     */
    /*
     * copy StaUplinkTrafficLimitThreshold data
     * set rowreq_ctx->data.StaUplinkTrafficLimitThreshold from rowreq_ctx->undo->StaUplinkTrafficLimitThreshold
     */
    rowreq_ctx->data.StaUplinkTrafficLimitThreshold = rowreq_ctx->undo->StaUplinkTrafficLimitThreshold;

    
    return MFD_SUCCESS;
} /* StaUplinkTrafficLimitThreshold_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11WlanStaEntry.StaDownlinkTrafficLimitThreshold
 * StaDownlinkTrafficLimitThreshold is subid 5 of dot11WlanStaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.3.1.5
 * Description:
Station downlink traffic limit threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param StaDownlinkTrafficLimitThreshold_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WlanStationTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
StaDownlinkTrafficLimitThreshold_check_value( dot11WlanStationTable_rowreq_ctx *rowreq_ctx, long StaDownlinkTrafficLimitThreshold_val)
{
    DEBUGMSGTL(("verbose:dot11WlanStationTable:StaDownlinkTrafficLimitThreshold_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid StaDownlinkTrafficLimitThreshold value.
     */

    return MFD_SUCCESS; /* StaDownlinkTrafficLimitThreshold value not illegal */
} /* StaDownlinkTrafficLimitThreshold_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WlanStationTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WlanStationTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
StaDownlinkTrafficLimitThreshold_undo_setup( dot11WlanStationTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WlanStationTable:StaDownlinkTrafficLimitThreshold_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup StaDownlinkTrafficLimitThreshold undo.
     */
    /*
     * copy StaDownlinkTrafficLimitThreshold data
     * set rowreq_ctx->undo->StaDownlinkTrafficLimitThreshold from rowreq_ctx->data.StaDownlinkTrafficLimitThreshold
     */
    rowreq_ctx->undo->StaDownlinkTrafficLimitThreshold = rowreq_ctx->data.StaDownlinkTrafficLimitThreshold;


    return MFD_SUCCESS;
} /* StaDownlinkTrafficLimitThreshold_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param StaDownlinkTrafficLimitThreshold_val
 *        A long containing the new value.
 */
int
StaDownlinkTrafficLimitThreshold_set( dot11WlanStationTable_rowreq_ctx *rowreq_ctx, long StaDownlinkTrafficLimitThreshold_val )
{

    DEBUGMSGTL(("verbose:dot11WlanStationTable:StaDownlinkTrafficLimitThreshold_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement StaDownlinkTrafficLimitThreshold reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set StaDownlinkTrafficLimitThreshold value.
     * set StaDownlinkTrafficLimitThreshold value in rowreq_ctx->data
     */

    int rc = MFD_ERROR;
    int ret = 0;
	char wid[10] = {0};
	memset(wid,0,10);
	snprintf(wid,sizeof(wid)-1,"%d",rowreq_ctx->tbl_idx.wlanID);
    char value[30] = {0};
	memset(value,0,30);
	snprintf(value,sizeof(value)-1,"%d",StaDownlinkTrafficLimitThreshold_val);
	
	int ret1 = 0;
	struct dcli_sta_info *sta = NULL;					

	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
	
	ret1=show_sta_bymac(rowreq_ctx->data.parameter, connection, rowreq_ctx->tbl_idx.StaMacAddr, &sta);
	if(1 == ret1)
	{
		ret = radio_bss_traffic_limit_sta_send_value_cmd(rowreq_ctx->data.parameter, connection,sta->radio_g_id,wid,rowreq_ctx->tbl_idx.StaMacAddr,value);
		if(1 == ret)
		{
			rowreq_ctx->data.StaDownlinkTrafficLimitThreshold = StaDownlinkTrafficLimitThreshold_val;
			rc = MFD_SUCCESS;
		}
		else
		{
			rc = MFD_ERROR;
		}
	}
	else
	{
		rc = MFD_ERROR;
	}
	
	Free_bss_bymac(sta);

    return rc;
} /* StaDownlinkTrafficLimitThreshold_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
StaDownlinkTrafficLimitThreshold_undo( dot11WlanStationTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WlanStationTable:StaDownlinkTrafficLimitThreshold_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up StaDownlinkTrafficLimitThreshold undo.
     */
    /*
     * copy StaDownlinkTrafficLimitThreshold data
     * set rowreq_ctx->data.StaDownlinkTrafficLimitThreshold from rowreq_ctx->undo->StaDownlinkTrafficLimitThreshold
     */
    rowreq_ctx->data.StaDownlinkTrafficLimitThreshold = rowreq_ctx->undo->StaDownlinkTrafficLimitThreshold;

    
    return MFD_SUCCESS;
} /* StaDownlinkTrafficLimitThreshold_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WlanStationTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11WlanStationTable if you don't have dependencies)
 */
int
dot11WlanStationTable_check_dependencies(dot11WlanStationTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11WlanStationTable:dot11WlanStationTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11WlanStationTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11WlanStationTable_check_dependencies */

/** @} */
