/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11UsrLinkTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11UsrLinkTable get routines.
 * TODO:240:M: Implement dot11UsrLinkTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11UsrLinkTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11UsrLinkTable is subid 1 of wtpLink.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11UsrLinkTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_map_usrlink(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11UsrLinkTable_indexes_set_tbl_idx(dot11UsrLinkTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:dot11UsrLinkTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11UsrLinkTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11UsrLinkTable_indexes_set(dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:dot11UsrLinkTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11UsrLinkTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11UsrLinkTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11UsrLinkTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UsrLinkEntry.wtpReasonInvalidFailLinkTimes
 * wtpReasonInvalidFailLinkTimes is subid 1 of dot11UsrLinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1.1.1
 * Description:
The total user link verification failed times because of invalided.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonInvalidFailLinkTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonInvalidFailLinkTimes_map(u_long *mib_wtpReasonInvalidFailLinkTimes_val_ptr, u_long raw_wtpReasonInvalidFailLinkTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonInvalidFailLinkTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonInvalidFailLinkTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonInvalidFailLinkTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonInvalidFailLinkTimes_val_ptr) = raw_wtpReasonInvalidFailLinkTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonInvalidFailLinkTimes_map */

/**
 * Extract the current value of the wtpReasonInvalidFailLinkTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonInvalidFailLinkTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonInvalidFailLinkTimes_get( dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonInvalidFailLinkTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonInvalidFailLinkTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonInvalidFailLinkTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonInvalidFailLinkTimes data.
 * set (* wtpReasonInvalidFailLinkTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonInvalidFailLinkTimes_val_ptr ) = rowreq_ctx->data.wtpReasonInvalidFailLinkTimes;

    return MFD_SUCCESS;
} /* wtpReasonInvalidFailLinkTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UsrLinkEntry.wtpReasonTimeOutFailLinkTimes
 * wtpReasonTimeOutFailLinkTimes is subid 2 of dot11UsrLinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1.1.2
 * Description:
The total user link verification failed times because of time out.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonTimeOutFailLinkTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonTimeOutFailLinkTimes_map(u_long *mib_wtpReasonTimeOutFailLinkTimes_val_ptr, u_long raw_wtpReasonTimeOutFailLinkTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonTimeOutFailLinkTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonTimeOutFailLinkTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonTimeOutFailLinkTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonTimeOutFailLinkTimes_val_ptr) = raw_wtpReasonTimeOutFailLinkTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonTimeOutFailLinkTimes_map */

/**
 * Extract the current value of the wtpReasonTimeOutFailLinkTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonTimeOutFailLinkTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonTimeOutFailLinkTimes_get( dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonTimeOutFailLinkTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonTimeOutFailLinkTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonTimeOutFailLinkTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonTimeOutFailLinkTimes data.
 * set (* wtpReasonTimeOutFailLinkTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonTimeOutFailLinkTimes_val_ptr ) = rowreq_ctx->data.wtpReasonTimeOutFailLinkTimes;

    return MFD_SUCCESS;
} /* wtpReasonTimeOutFailLinkTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UsrLinkEntry.wtpReasonRefuseFailLinkTimes
 * wtpReasonRefuseFailLinkTimes is subid 3 of dot11UsrLinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1.1.3
 * Description:
The total user link verification failed times because of refusing.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonRefuseFailLinkTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonRefuseFailLinkTimes_map(u_long *mib_wtpReasonRefuseFailLinkTimes_val_ptr, u_long raw_wtpReasonRefuseFailLinkTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonRefuseFailLinkTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonRefuseFailLinkTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonRefuseFailLinkTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonRefuseFailLinkTimes_val_ptr) = raw_wtpReasonRefuseFailLinkTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonRefuseFailLinkTimes_map */

/**
 * Extract the current value of the wtpReasonRefuseFailLinkTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonRefuseFailLinkTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonRefuseFailLinkTimes_get( dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonRefuseFailLinkTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonRefuseFailLinkTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonRefuseFailLinkTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonRefuseFailLinkTimes data.
 * set (* wtpReasonRefuseFailLinkTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonRefuseFailLinkTimes_val_ptr ) = rowreq_ctx->data.wtpReasonRefuseFailLinkTimes;

    return MFD_SUCCESS;
} /* wtpReasonRefuseFailLinkTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UsrLinkEntry.wtpReasonOthersFailLinkTimes
 * wtpReasonOthersFailLinkTimes is subid 4 of dot11UsrLinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1.1.4
 * Description:
The total user link verification failed times because of other reasons.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonOthersFailLinkTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonOthersFailLinkTimes_map(u_long *mib_wtpReasonOthersFailLinkTimes_val_ptr, u_long raw_wtpReasonOthersFailLinkTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonOthersFailLinkTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonOthersFailLinkTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonOthersFailLinkTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonOthersFailLinkTimes_val_ptr) = raw_wtpReasonOthersFailLinkTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonOthersFailLinkTimes_map */

/**
 * Extract the current value of the wtpReasonOthersFailLinkTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonOthersFailLinkTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonOthersFailLinkTimes_get( dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonOthersFailLinkTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonOthersFailLinkTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonOthersFailLinkTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonOthersFailLinkTimes data.
 * set (* wtpReasonOthersFailLinkTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonOthersFailLinkTimes_val_ptr ) = rowreq_ctx->data.wtpReasonOthersFailLinkTimes;

    return MFD_SUCCESS;
} /* wtpReasonOthersFailLinkTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UsrLinkEntry.wtpSolutionLinksVerifyLinkTimes
 * wtpSolutionLinksVerifyLinkTimes is subid 5 of dot11UsrLinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1.1.5
 * Description:
The total times that solution link verfication.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSolutionLinksVerifyLinkTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSolutionLinksVerifyLinkTimes_map(u_long *mib_wtpSolutionLinksVerifyLinkTimes_val_ptr, u_long raw_wtpSolutionLinksVerifyLinkTimes_val)
{
    netsnmp_assert(NULL != mib_wtpSolutionLinksVerifyLinkTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpSolutionLinksVerifyLinkTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSolutionLinksVerifyLinkTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSolutionLinksVerifyLinkTimes_val_ptr) = raw_wtpSolutionLinksVerifyLinkTimes_val;

    return MFD_SUCCESS;
} /* wtpSolutionLinksVerifyLinkTimes_map */

/**
 * Extract the current value of the wtpSolutionLinksVerifyLinkTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSolutionLinksVerifyLinkTimes_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSolutionLinksVerifyLinkTimes_get( dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, u_long * wtpSolutionLinksVerifyLinkTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSolutionLinksVerifyLinkTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpSolutionLinksVerifyLinkTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSolutionLinksVerifyLinkTimes data.
 * set (* wtpSolutionLinksVerifyLinkTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSolutionLinksVerifyLinkTimes_val_ptr ) = rowreq_ctx->data.wtpSolutionLinksVerifyLinkTimes;

    return MFD_SUCCESS;
} /* wtpSolutionLinksVerifyLinkTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UsrLinkEntry.wtpReasonUsrLeaveVerfiyLinkTimes
 * wtpReasonUsrLeaveVerfiyLinkTimes is subid 6 of dot11UsrLinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1.1.6
 * Description:
The total times that solution link verfication because of user leaving.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonUsrLeaveVerfiyLinkTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonUsrLeaveVerfiyLinkTimes_map(u_long *mib_wtpReasonUsrLeaveVerfiyLinkTimes_val_ptr, u_long raw_wtpReasonUsrLeaveVerfiyLinkTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonUsrLeaveVerfiyLinkTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonUsrLeaveVerfiyLinkTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonUsrLeaveVerfiyLinkTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonUsrLeaveVerfiyLinkTimes_val_ptr) = raw_wtpReasonUsrLeaveVerfiyLinkTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonUsrLeaveVerfiyLinkTimes_map */

/**
 * Extract the current value of the wtpReasonUsrLeaveVerfiyLinkTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonUsrLeaveVerfiyLinkTimes_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonUsrLeaveVerfiyLinkTimes_get( dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonUsrLeaveVerfiyLinkTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonUsrLeaveVerfiyLinkTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonUsrLeaveVerfiyLinkTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonUsrLeaveVerfiyLinkTimes data.
 * set (* wtpReasonUsrLeaveVerfiyLinkTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonUsrLeaveVerfiyLinkTimes_val_ptr ) = rowreq_ctx->data.wtpReasonUsrLeaveVerfiyLinkTimes;

    return MFD_SUCCESS;
} /* wtpReasonUsrLeaveVerfiyLinkTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UsrLinkEntry.wtpReasonLackAbilityVerifyLinkTimes
 * wtpReasonLackAbilityVerifyLinkTimes is subid 7 of dot11UsrLinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1.1.7
 * Description:
The total times that solution link verfication because of lack of ability.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonLackAbilityVerifyLinkTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonLackAbilityVerifyLinkTimes_map(u_long *mib_wtpReasonLackAbilityVerifyLinkTimes_val_ptr, u_long raw_wtpReasonLackAbilityVerifyLinkTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonLackAbilityVerifyLinkTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonLackAbilityVerifyLinkTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonLackAbilityVerifyLinkTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonLackAbilityVerifyLinkTimes_val_ptr) = raw_wtpReasonLackAbilityVerifyLinkTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonLackAbilityVerifyLinkTimes_map */

/**
 * Extract the current value of the wtpReasonLackAbilityVerifyLinkTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonLackAbilityVerifyLinkTimes_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonLackAbilityVerifyLinkTimes_get( dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonLackAbilityVerifyLinkTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonLackAbilityVerifyLinkTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonLackAbilityVerifyLinkTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonLackAbilityVerifyLinkTimes data.
 * set (* wtpReasonLackAbilityVerifyLinkTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonLackAbilityVerifyLinkTimes_val_ptr ) = rowreq_ctx->data.wtpReasonLackAbilityVerifyLinkTimes;

    return MFD_SUCCESS;
} /* wtpReasonLackAbilityVerifyLinkTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UsrLinkEntry.wtpReasonExceptionVerifyLinkTimes
 * wtpReasonExceptionVerifyLinkTimes is subid 8 of dot11UsrLinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1.1.8
 * Description:
The total times that solution link verfication because of exception.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonExceptionVerifyLinkTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonExceptionVerifyLinkTimes_map(u_long *mib_wtpReasonExceptionVerifyLinkTimes_val_ptr, u_long raw_wtpReasonExceptionVerifyLinkTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonExceptionVerifyLinkTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonExceptionVerifyLinkTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonExceptionVerifyLinkTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonExceptionVerifyLinkTimes_val_ptr) = raw_wtpReasonExceptionVerifyLinkTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonExceptionVerifyLinkTimes_map */

/**
 * Extract the current value of the wtpReasonExceptionVerifyLinkTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonExceptionVerifyLinkTimes_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonExceptionVerifyLinkTimes_get( dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonExceptionVerifyLinkTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonExceptionVerifyLinkTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonExceptionVerifyLinkTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonExceptionVerifyLinkTimes data.
 * set (* wtpReasonExceptionVerifyLinkTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonExceptionVerifyLinkTimes_val_ptr ) = rowreq_ctx->data.wtpReasonExceptionVerifyLinkTimes;

    return MFD_SUCCESS;
} /* wtpReasonExceptionVerifyLinkTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UsrLinkEntry.wtpReasonOtherVerfyLinkTimes
 * wtpReasonOtherVerfyLinkTimes is subid 9 of dot11UsrLinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1.1.9
 * Description:
The total times that solution link verfication because of other reason.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonOtherVerfyLinkTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonOtherVerfyLinkTimes_map(u_long *mib_wtpReasonOtherVerfyLinkTimes_val_ptr, u_long raw_wtpReasonOtherVerfyLinkTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonOtherVerfyLinkTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonOtherVerfyLinkTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonOtherVerfyLinkTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonOtherVerfyLinkTimes_val_ptr) = raw_wtpReasonOtherVerfyLinkTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonOtherVerfyLinkTimes_map */

/**
 * Extract the current value of the wtpReasonOtherVerfyLinkTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonOtherVerfyLinkTimes_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonOtherVerfyLinkTimes_get( dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonOtherVerfyLinkTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonOtherVerfyLinkTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpReasonOtherVerfyLinkTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonOtherVerfyLinkTimes data.
 * set (* wtpReasonOtherVerfyLinkTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonOtherVerfyLinkTimes_val_ptr ) = rowreq_ctx->data.wtpReasonOtherVerfyLinkTimes;

    return MFD_SUCCESS;
} /* wtpReasonOtherVerfyLinkTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UsrLinkEntry.wtpStaTotalOnlineTime
 * wtpStaTotalOnlineTime is subid 10 of dot11UsrLinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1.1.10
 * Description:
The total station online time on the AP .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaTotalOnlineTime.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaTotalOnlineTime_map(u_long *mib_wtpStaTotalOnlineTime_val_ptr, u_long raw_wtpStaTotalOnlineTime_val)
{
    netsnmp_assert(NULL != mib_wtpStaTotalOnlineTime_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpStaTotalOnlineTime_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaTotalOnlineTime mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpStaTotalOnlineTime_val_ptr) = raw_wtpStaTotalOnlineTime_val;

    return MFD_SUCCESS;
} /* wtpStaTotalOnlineTime_map */

/**
 * Extract the current value of the wtpStaTotalOnlineTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaTotalOnlineTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaTotalOnlineTime_get( dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, u_long * wtpStaTotalOnlineTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaTotalOnlineTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpStaTotalOnlineTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaTotalOnlineTime data.
 * set (* wtpStaTotalOnlineTime_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaTotalOnlineTime_val_ptr ) = rowreq_ctx->data.wtpStaTotalOnlineTime;

    return MFD_SUCCESS;
} /* wtpStaTotalOnlineTime_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UsrLinkEntry.wtpStaNewTotalOnlineTime
 * wtpStaNewTotalOnlineTime is subid 11 of dot11UsrLinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.1.1.11
 * Description:
The total station online time on the AP, the time is not cleared if AP restart.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaNewTotalOnlineTime.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStaNewTotalOnlineTime_map(u_long *mib_wtpStaNewTotalOnlineTime_val_ptr, u_long raw_wtpStaNewTotalOnlineTime_val)
{
    netsnmp_assert(NULL != mib_wtpStaNewTotalOnlineTime_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpStaNewTotalOnlineTime_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStaNewTotalOnlineTime mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpStaNewTotalOnlineTime_val_ptr) = raw_wtpStaNewTotalOnlineTime_val;

    return MFD_SUCCESS;
} /* wtpStaNewTotalOnlineTime_map */

/**
 * Extract the current value of the wtpStaNewTotalOnlineTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStaNewTotalOnlineTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStaNewTotalOnlineTime_get( dot11UsrLinkTable_rowreq_ctx *rowreq_ctx, u_long * wtpStaNewTotalOnlineTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStaNewTotalOnlineTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11UsrLinkTable:wtpStaNewTotalOnlineTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStaNewTotalOnlineTime data.
 * set (* wtpStaNewTotalOnlineTime_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStaNewTotalOnlineTime_val_ptr ) = rowreq_ctx->data.wtpStaNewTotalOnlineTime;

    return MFD_SUCCESS;
} /* wtpStaNewTotalOnlineTime_get */



/** @} */
