/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigRadiusAuthServerTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11ConfigRadiusAuthServerTable get routines.
 * TODO:240:M: Implement dot11ConfigRadiusAuthServerTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigRadiusAuthServerTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigRadiusAuthServerTable is subid 6 of dot11securityConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11ConfigRadiusAuthServerTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.RadiusAuthServerID
 * RadiusAuthServerID is subid 1 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.1
 * Description:
RadiusAuthServerID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-RadiusAuthServerID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
RadiusAuthServerID_map(long *mib_RadiusAuthServerID_val_ptr, long raw_RadiusAuthServerID_val)
{
    netsnmp_assert(NULL != mib_RadiusAuthServerID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:RadiusAuthServerID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement RadiusAuthServerID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_RadiusAuthServerID_val_ptr) = raw_RadiusAuthServerID_val;

    return MFD_SUCCESS;
} /* RadiusAuthServerID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11ConfigRadiusAuthServerTable_indexes_set_tbl_idx(dot11ConfigRadiusAuthServerTable_mib_index *tbl_idx, long RadiusAuthServerID_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_indexes_set_tbl_idx","called\n"));

    /* RadiusAuthServerID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->RadiusAuthServerID = RadiusAuthServerID_val;
    

    return MFD_SUCCESS;
} /* dot11ConfigRadiusAuthServerTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11ConfigRadiusAuthServerTable_indexes_set(dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long RadiusAuthServerID_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11ConfigRadiusAuthServerTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , RadiusAuthServerID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11ConfigRadiusAuthServerTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11ConfigRadiusAuthServerTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewAuthServerIPAdd
 * NewAuthServerIPAdd is subid 2 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.2
 * Description:
NewAuthServerIPAdd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewAuthServerIPAdd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewAuthServerIPAdd_map(u_long *mib_NewAuthServerIPAdd_val_ptr, u_long raw_NewAuthServerIPAdd_val)
{
    netsnmp_assert(NULL != mib_NewAuthServerIPAdd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerIPAdd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewAuthServerIPAdd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewAuthServerIPAdd_val_ptr) = raw_NewAuthServerIPAdd_val;

    return MFD_SUCCESS;
} /* NewAuthServerIPAdd_map */

/**
 * Extract the current value of the NewAuthServerIPAdd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAuthServerIPAdd_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewAuthServerIPAdd_get( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long * NewAuthServerIPAdd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewAuthServerIPAdd_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerIPAdd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewAuthServerIPAdd data.
 * set (* NewAuthServerIPAdd_val_ptr ) from rowreq_ctx->data
 */
    (* NewAuthServerIPAdd_val_ptr ) = rowreq_ctx->data.NewAuthServerIPAdd;

    return MFD_SUCCESS;
} /* NewAuthServerIPAdd_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewAuthServerPort
 * NewAuthServerPort is subid 3 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.3
 * Description:
NewAuthServerPort.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewAuthServerPort.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewAuthServerPort_map(long *mib_NewAuthServerPort_val_ptr, long raw_NewAuthServerPort_val)
{
    netsnmp_assert(NULL != mib_NewAuthServerPort_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerPort_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewAuthServerPort mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewAuthServerPort_val_ptr) = raw_NewAuthServerPort_val;

    return MFD_SUCCESS;
} /* NewAuthServerPort_map */

/**
 * Extract the current value of the NewAuthServerPort data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAuthServerPort_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewAuthServerPort_get( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long * NewAuthServerPort_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewAuthServerPort_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerPort_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewAuthServerPort data.
 * set (* NewAuthServerPort_val_ptr ) from rowreq_ctx->data
 */
    (* NewAuthServerPort_val_ptr ) = rowreq_ctx->data.NewAuthServerPort;

    return MFD_SUCCESS;
} /* NewAuthServerPort_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewAuthServerSharedkey
 * NewAuthServerSharedkey is subid 4 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.4
 * Description:
NewAuthServerSharedkey.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewAuthServerSharedkey.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewAuthServerSharedkey_map(char **mib_NewAuthServerSharedkey_val_ptr_ptr, size_t *mib_NewAuthServerSharedkey_val_ptr_len_ptr, char *raw_NewAuthServerSharedkey_val_ptr, size_t raw_NewAuthServerSharedkey_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_NewAuthServerSharedkey_val_ptr);
    netsnmp_assert((NULL != mib_NewAuthServerSharedkey_val_ptr_ptr) && (NULL != mib_NewAuthServerSharedkey_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerSharedkey_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement NewAuthServerSharedkey non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_NewAuthServerSharedkey_val_ptr_len; /* assume equal */
    if((NULL == *mib_NewAuthServerSharedkey_val_ptr_ptr) || (*mib_NewAuthServerSharedkey_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_NewAuthServerSharedkey_val_ptr_ptr = realloc( *mib_NewAuthServerSharedkey_val_ptr_ptr, converted_len * sizeof(**mib_NewAuthServerSharedkey_val_ptr_ptr));
        if(NULL == *mib_NewAuthServerSharedkey_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_NewAuthServerSharedkey_val_ptr_len_ptr = converted_len;
    memcpy( *mib_NewAuthServerSharedkey_val_ptr_ptr, raw_NewAuthServerSharedkey_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* NewAuthServerSharedkey_map */

/**
 * Extract the current value of the NewAuthServerSharedkey data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAuthServerSharedkey_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewAuthServerSharedkey_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewAuthServerSharedkey.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewAuthServerSharedkey_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewAuthServerSharedkey_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewAuthServerSharedkey_get( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char **NewAuthServerSharedkey_val_ptr_ptr, size_t *NewAuthServerSharedkey_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewAuthServerSharedkey_val_ptr_ptr) && (NULL != *NewAuthServerSharedkey_val_ptr_ptr));
   netsnmp_assert( NULL != NewAuthServerSharedkey_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerSharedkey_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewAuthServerSharedkey data.
 * set (* NewAuthServerSharedkey_val_ptr_ptr ) and (* NewAuthServerSharedkey_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewAuthServerSharedkey data
     */
    if ((NULL == (* NewAuthServerSharedkey_val_ptr_ptr )) ||
        ((* NewAuthServerSharedkey_val_ptr_len_ptr ) < (rowreq_ctx->data.NewAuthServerSharedkey_len * sizeof((* NewAuthServerSharedkey_val_ptr_ptr )[0])))) {
        /*
         * allocate space for NewAuthServerSharedkey data
         */
        (* NewAuthServerSharedkey_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewAuthServerSharedkey_len * sizeof((* NewAuthServerSharedkey_val_ptr_ptr )[0]));
        if(NULL == (* NewAuthServerSharedkey_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewAuthServerSharedkey_val_ptr_len_ptr ) = rowreq_ctx->data.NewAuthServerSharedkey_len * sizeof((* NewAuthServerSharedkey_val_ptr_ptr )[0]);
    memcpy( (* NewAuthServerSharedkey_val_ptr_ptr ), rowreq_ctx->data.NewAuthServerSharedkey, (* NewAuthServerSharedkey_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* NewAuthServerSharedkey_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewRadiusAuthServerIPAdd
 * NewRadiusAuthServerIPAdd is subid 5 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.5
 * Description:
NewRadiusAuthServerIPAdd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewRadiusAuthServerIPAdd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewRadiusAuthServerIPAdd_map(u_long *mib_NewRadiusAuthServerIPAdd_val_ptr, u_long raw_NewRadiusAuthServerIPAdd_val)
{
    netsnmp_assert(NULL != mib_NewRadiusAuthServerIPAdd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerIPAdd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewRadiusAuthServerIPAdd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewRadiusAuthServerIPAdd_val_ptr) = raw_NewRadiusAuthServerIPAdd_val;

    return MFD_SUCCESS;
} /* NewRadiusAuthServerIPAdd_map */

/**
 * Extract the current value of the NewRadiusAuthServerIPAdd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewRadiusAuthServerIPAdd_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewRadiusAuthServerIPAdd_get( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long * NewRadiusAuthServerIPAdd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewRadiusAuthServerIPAdd_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerIPAdd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewRadiusAuthServerIPAdd data.
 * set (* NewRadiusAuthServerIPAdd_val_ptr ) from rowreq_ctx->data
 */
    (* NewRadiusAuthServerIPAdd_val_ptr ) = rowreq_ctx->data.NewRadiusAuthServerIPAdd;

    return MFD_SUCCESS;
} /* NewRadiusAuthServerIPAdd_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewRadiusAuthServerPort
 * NewRadiusAuthServerPort is subid 6 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.6
 * Description:
NewRadiusAuthServerPort.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewRadiusAuthServerPort.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewRadiusAuthServerPort_map(long *mib_NewRadiusAuthServerPort_val_ptr, long raw_NewRadiusAuthServerPort_val)
{
    netsnmp_assert(NULL != mib_NewRadiusAuthServerPort_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerPort_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewRadiusAuthServerPort mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewRadiusAuthServerPort_val_ptr) = raw_NewRadiusAuthServerPort_val;

    return MFD_SUCCESS;
} /* NewRadiusAuthServerPort_map */

/**
 * Extract the current value of the NewRadiusAuthServerPort data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewRadiusAuthServerPort_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewRadiusAuthServerPort_get( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long * NewRadiusAuthServerPort_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewRadiusAuthServerPort_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerPort_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewRadiusAuthServerPort data.
 * set (* NewRadiusAuthServerPort_val_ptr ) from rowreq_ctx->data
 */
    (* NewRadiusAuthServerPort_val_ptr ) = rowreq_ctx->data.NewRadiusAuthServerPort;

    return MFD_SUCCESS;
} /* NewRadiusAuthServerPort_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewRadiusAuthServerSharedkey
 * NewRadiusAuthServerSharedkey is subid 7 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.7
 * Description:
NewRadiusAuthServerSharedkey.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewRadiusAuthServerSharedkey.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewRadiusAuthServerSharedkey_map(char **mib_NewRadiusAuthServerSharedkey_val_ptr_ptr, size_t *mib_NewRadiusAuthServerSharedkey_val_ptr_len_ptr, char *raw_NewRadiusAuthServerSharedkey_val_ptr, size_t raw_NewRadiusAuthServerSharedkey_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_NewRadiusAuthServerSharedkey_val_ptr);
    netsnmp_assert((NULL != mib_NewRadiusAuthServerSharedkey_val_ptr_ptr) && (NULL != mib_NewRadiusAuthServerSharedkey_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerSharedkey_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement NewRadiusAuthServerSharedkey non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_NewRadiusAuthServerSharedkey_val_ptr_len; /* assume equal */
    if((NULL == *mib_NewRadiusAuthServerSharedkey_val_ptr_ptr) || (*mib_NewRadiusAuthServerSharedkey_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_NewRadiusAuthServerSharedkey_val_ptr_ptr = realloc( *mib_NewRadiusAuthServerSharedkey_val_ptr_ptr, converted_len * sizeof(**mib_NewRadiusAuthServerSharedkey_val_ptr_ptr));
        if(NULL == *mib_NewRadiusAuthServerSharedkey_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_NewRadiusAuthServerSharedkey_val_ptr_len_ptr = converted_len;
    memcpy( *mib_NewRadiusAuthServerSharedkey_val_ptr_ptr, raw_NewRadiusAuthServerSharedkey_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* NewRadiusAuthServerSharedkey_map */

/**
 * Extract the current value of the NewRadiusAuthServerSharedkey data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewRadiusAuthServerSharedkey_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewRadiusAuthServerSharedkey_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewRadiusAuthServerSharedkey.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewRadiusAuthServerSharedkey_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewRadiusAuthServerSharedkey_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewRadiusAuthServerSharedkey_get( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char **NewRadiusAuthServerSharedkey_val_ptr_ptr, size_t *NewRadiusAuthServerSharedkey_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewRadiusAuthServerSharedkey_val_ptr_ptr) && (NULL != *NewRadiusAuthServerSharedkey_val_ptr_ptr));
   netsnmp_assert( NULL != NewRadiusAuthServerSharedkey_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerSharedkey_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewRadiusAuthServerSharedkey data.
 * set (* NewRadiusAuthServerSharedkey_val_ptr_ptr ) and (* NewRadiusAuthServerSharedkey_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewRadiusAuthServerSharedkey data
     */
    if ((NULL == (* NewRadiusAuthServerSharedkey_val_ptr_ptr )) ||
        ((* NewRadiusAuthServerSharedkey_val_ptr_len_ptr ) < (rowreq_ctx->data.NewRadiusAuthServerSharedkey_len * sizeof((* NewRadiusAuthServerSharedkey_val_ptr_ptr )[0])))) {
        /*
         * allocate space for NewRadiusAuthServerSharedkey data
         */
        (* NewRadiusAuthServerSharedkey_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewRadiusAuthServerSharedkey_len * sizeof((* NewRadiusAuthServerSharedkey_val_ptr_ptr )[0]));
        if(NULL == (* NewRadiusAuthServerSharedkey_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewRadiusAuthServerSharedkey_val_ptr_len_ptr ) = rowreq_ctx->data.NewRadiusAuthServerSharedkey_len * sizeof((* NewRadiusAuthServerSharedkey_val_ptr_ptr )[0]);
    memcpy( (* NewRadiusAuthServerSharedkey_val_ptr_ptr ), rowreq_ctx->data.NewRadiusAuthServerSharedkey, (* NewRadiusAuthServerSharedkey_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* NewRadiusAuthServerSharedkey_get */



/** @} */
