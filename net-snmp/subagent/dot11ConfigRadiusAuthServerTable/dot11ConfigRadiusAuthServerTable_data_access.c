/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "ws_sysinfo.h"
#include "ws_init_dbus.h"
#include <stdlib.h>
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
#include "ws_init_dbus.h"
#include <dbus/dbus.h>
#include "sysdef/npd_sysdef.h"
#include "dbus/npd/npd_dbus_def.h"
#include "autelanWtpGroup.h"
#include "ws_security.h"
#include "mibs_public.h"
/* include our parent header */
#include "dot11ConfigRadiusAuthServerTable.h"


#include "dot11ConfigRadiusAuthServerTable_data_access.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigRadiusAuthServerTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigRadiusAuthServerTable is subid 6 of dot11securityConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6, length: 12
*/

/**
 * initialization for dot11ConfigRadiusAuthServerTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11ConfigRadiusAuthServerTable_reg
 *        Pointer to dot11ConfigRadiusAuthServerTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11ConfigRadiusAuthServerTable_init_data(dot11ConfigRadiusAuthServerTable_registration_ptr dot11ConfigRadiusAuthServerTable_reg)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11ConfigRadiusAuthServerTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11ConfigRadiusAuthServerTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11ConfigRadiusAuthServerTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11ConfigRadiusAuthServerTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11ConfigRadiusAuthServerTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11CONFIGRADIUSAUTHSERVERTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11ConfigRadiusAuthServerTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11ConfigRadiusAuthServerTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11ConfigRadiusAuthServerTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11ConfigRadiusAuthServerTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11ConfigRadiusAuthServerTable_cache_load\n");

    dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    /*
     * temporary storage for index values
     */
        /*
         * globalRadiusAuthServerID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   globalRadiusAuthServerID;

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_radius_all_cmd\n");
    messageHead = list_connection_call_dbus_method(show_radius_all_cmd, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_radius_all_cmd,messageHead=%p\n", messageHead);

	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    struct dcli_security *head = messageNode->message;
		    if(head)
		    {
		        struct dcli_security *ShowNode = NULL;
		        
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11ConfigRadiusAuthServerTable container.
     * loop over your dot11ConfigRadiusAuthServerTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
	
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * get a line (skip blank lines)
     */

    /*
     * parse line into variables
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

        /*
         * TODO:352:M: |   |-> set indexes in new dot11ConfigRadiusAuthServerTable rowreq context.
         */

		for(ShowNode = head; ShowNode != NULL; ShowNode = ShowNode->next)
		{
		    globalRadiusAuthServerID = local_to_global_ID(messageNode->parameter, ShowNode->RadiusID, WIRELESS_MAX_NUM);
		    
	        rowreq_ctx = dot11ConfigRadiusAuthServerTable_allocate_rowreq_ctx();
	        if (NULL == rowreq_ctx) {
	            snmp_log(LOG_ERR, "memory allocation failed\n");
                free_dbus_message_list(&messageHead, Free_show_radius_all_cmd);
	            return MFD_RESOURCE_UNAVAILABLE;
	        }
	        if(MFD_SUCCESS != dot11ConfigRadiusAuthServerTable_indexes_set(rowreq_ctx
	                               , globalRadiusAuthServerID
	               )) {
	            snmp_log(LOG_ERR,"error setting index while loading "
	                     "dot11ConfigRadiusAuthServerTable cache.\n");
	            dot11ConfigRadiusAuthServerTable_release_rowreq_ctx(rowreq_ctx);
	        }

            memcpy(&rowreq_ctx->data.parameter, &messageNode->parameter, sizeof(dbus_parameter));
            rowreq_ctx->data.RadiusAuthServerID = ShowNode->RadiusID;
            
			int ip = 0;
			char NewRadiusAuthServerSharedkey[DEFAULT_LEN] = { 0 };
			memset(NewRadiusAuthServerSharedkey,0,DEFAULT_LEN);
			int NewRadiusAuthServerIPAdd = 0;
			int NewAuthServerPort = 0;
			int NewRadiusAuthServerPort = 0;
			char NewAuthServerSharedkey[DEFAULT_LEN] = { 0 };
			memset(NewAuthServerSharedkey,0,DEFAULT_LEN);
			
			INET_ATON(ip,ShowNode->auth.auth_ip);
			NewAuthServerPort = ShowNode->auth.auth_port;
			strncpy(NewAuthServerSharedkey,ShowNode->auth.auth_shared_secret,sizeof(NewAuthServerSharedkey)-1);
			INET_ATON(NewRadiusAuthServerIPAdd,ShowNode->auth.secondary_auth_ip);
			NewRadiusAuthServerPort = ShowNode->auth.secondary_auth_port;
			strncpy(NewRadiusAuthServerSharedkey,ShowNode->auth.secondary_auth_shared_secret,sizeof(NewRadiusAuthServerSharedkey)-1);
	        /*
	         * TODO:352:r: |   |-> populate dot11ConfigRadiusAuthServerTable data context.
	         * Populate data context here. (optionally, delay until row prep)
	         */
	    /*
	     * TRANSIENT or semi-TRANSIENT data:
	     * copy data or save any info needed to do it in row_prep.
	     */
	    /*
	     * setup/save data for NewAuthServerIPAdd
	     * NewAuthServerIPAdd(2)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define NewAuthServerIPAdd mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.NewAuthServerIPAdd = ip;
	    /*
	     * setup/save data for NewAuthServerPort
	     * NewAuthServerPort(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define NewAuthServerPort mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.NewAuthServerPort = NewAuthServerPort;
	    /*
	     * setup/save data for NewAuthServerSharedkey
	     * NewAuthServerSharedkey(4)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H
	     */
	    /*
	     * TODO:246:r: |-> Define NewAuthServerSharedkey mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * if(MFD_SUCCESS !=
	     *    NewAuthServerSharedkey_map(&rowreq_ctx->data.NewAuthServerSharedkey, &rowreq_ctx->data.NewAuthServerSharedkey_len,
	     *                NewAuthServerSharedkey, NewAuthServerSharedkey_len, 0)) {
	     *    return MFD_ERROR;
	     * }
	     */
	    /*
	     * make sure there is enough space for NewAuthServerSharedkey data
	     */
	    int NewAuthServerSharedkey_len = 0;
	    if ((NULL == rowreq_ctx->data.NewAuthServerSharedkey) ||
	        (rowreq_ctx->data.NewAuthServerSharedkey_len < (NewAuthServerSharedkey_len * sizeof(rowreq_ctx->data.NewAuthServerSharedkey[0])))) {
	        snmp_log(LOG_ERR,"not enough space for value\n");
	        return MFD_ERROR;
	    }
		if(NULL == NewAuthServerSharedkey)
		{
			strncpy(NewAuthServerSharedkey,"",sizeof(NewAuthServerSharedkey)-1);
		}
		NewAuthServerSharedkey_len = MIN(strlen(NewAuthServerSharedkey),sizeof(rowreq_ctx->data.NewAuthServerSharedkey));
	    rowreq_ctx->data.NewAuthServerSharedkey_len = NewAuthServerSharedkey_len * sizeof(rowreq_ctx->data.NewAuthServerSharedkey[0]);
	    memcpy( rowreq_ctx->data.NewAuthServerSharedkey, NewAuthServerSharedkey, rowreq_ctx->data.NewAuthServerSharedkey_len );

		
	    /*
	     * setup/save data for NewRadiusAuthServerIPAdd
	     * NewRadiusAuthServerIPAdd(5)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define NewRadiusAuthServerIPAdd mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.NewRadiusAuthServerIPAdd = NewRadiusAuthServerIPAdd;
	    /*
	     * setup/save data for NewRadiusAuthServerPort
	     * NewRadiusAuthServerPort(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define NewRadiusAuthServerPort mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.NewRadiusAuthServerPort = NewRadiusAuthServerPort;
	    /*
	     * setup/save data for NewRadiusAuthServerSharedkey
	     * NewRadiusAuthServerSharedkey(7)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H
	     */
	    /*
	     * TODO:246:r: |-> Define NewRadiusAuthServerSharedkey mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * if(MFD_SUCCESS !=
	     *    NewRadiusAuthServerSharedkey_map(&rowreq_ctx->data.NewRadiusAuthServerSharedkey, &rowreq_ctx->data.NewRadiusAuthServerSharedkey_len,
	     *                NewRadiusAuthServerSharedkey, NewRadiusAuthServerSharedkey_len, 0)) {
	     *    return MFD_ERROR;
	     * }
	     */
	    /*
	     * make sure there is enough space for NewRadiusAuthServerSharedkey data
	     */
	    int NewRadiusAuthServerSharedkey_len = 0;
	    if ((NULL == rowreq_ctx->data.NewRadiusAuthServerSharedkey) ||
	        (rowreq_ctx->data.NewRadiusAuthServerSharedkey_len < (NewRadiusAuthServerSharedkey_len * sizeof(rowreq_ctx->data.NewRadiusAuthServerSharedkey[0])))) {
	        snmp_log(LOG_ERR,"not enough space for value\n");
	        return MFD_ERROR;
	    }
		if(NULL == NewRadiusAuthServerSharedkey)
		{
			strncpy(NewRadiusAuthServerSharedkey,"",sizeof(NewRadiusAuthServerSharedkey)-1);
		}
		NewRadiusAuthServerSharedkey_len = MIN(strlen(NewRadiusAuthServerSharedkey),sizeof(rowreq_ctx->data.NewRadiusAuthServerSharedkey));
	    rowreq_ctx->data.NewRadiusAuthServerSharedkey_len = NewRadiusAuthServerSharedkey_len * sizeof(rowreq_ctx->data.NewRadiusAuthServerSharedkey[0]);
	    memcpy( rowreq_ctx->data.NewRadiusAuthServerSharedkey, NewRadiusAuthServerSharedkey, rowreq_ctx->data.NewRadiusAuthServerSharedkey_len );
	        /*
	         * insert into table container
	         */

    	            if(CONTAINER_INSERT(container, rowreq_ctx))
        	        {
        				dot11ConfigRadiusAuthServerTable_release_rowreq_ctx(rowreq_ctx);
        	        }
        	        ++count;
        		}
            }
    	}
	    free_dbus_message_list(&messageHead, Free_show_radius_all_cmd);
	}
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11ConfigRadiusAuthServerTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11ConfigRadiusAuthServerTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11ConfigRadiusAuthServerTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11ConfigRadiusAuthServerTable cache.
     */
} /* dot11ConfigRadiusAuthServerTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11ConfigRadiusAuthServerTable_row_prep( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11ConfigRadiusAuthServerTable_row_prep */

/** @} */
