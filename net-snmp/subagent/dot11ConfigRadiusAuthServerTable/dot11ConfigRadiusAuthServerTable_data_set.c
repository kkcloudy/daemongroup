/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "ws_sysinfo.h"
#include "ws_init_dbus.h"
#include <stdlib.h>
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
#include "ws_init_dbus.h"
#include <dbus/dbus.h>
#include "sysdef/npd_sysdef.h"
#include "dbus/npd/npd_dbus_def.h"
#include "autelanWtpGroup.h"
#include "ws_security.h"
/* include our parent header */
#include "dot11ConfigRadiusAuthServerTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigRadiusAuthServerTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigRadiusAuthServerTable is subid 6 of dot11securityConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigRadiusAuthServerTable_undo_setup( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigRadiusAuthServerTable undo.
     * set up dot11ConfigRadiusAuthServerTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11ConfigRadiusAuthServerTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigRadiusAuthServerTable_undo_cleanup( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigRadiusAuthServerTable undo.
     */

    return rc;
} /* dot11ConfigRadiusAuthServerTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigRadiusAuthServerTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigRadiusAuthServerTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigRadiusAuthServerTable_commit( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11ConfigRadiusAuthServerTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_NEWAUTHSERVERIPADD) {
       save_flags &= ~FLAG_NEWAUTHSERVERIPADD; /* clear NewAuthServerIPAdd */
       /*
        * TODO:482:o: |-> commit column NewAuthServerIPAdd.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAuthServerTable column NewAuthServerIPAdd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewAuthServerIPAdd
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWAUTHSERVERIPADD;
       }
    }

    if (save_flags & FLAG_NEWAUTHSERVERPORT) {
       save_flags &= ~FLAG_NEWAUTHSERVERPORT; /* clear NewAuthServerPort */
       /*
        * TODO:482:o: |-> commit column NewAuthServerPort.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAuthServerTable column NewAuthServerPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewAuthServerPort
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWAUTHSERVERPORT;
       }
    }

    if (save_flags & FLAG_NEWAUTHSERVERSHAREDKEY) {
       save_flags &= ~FLAG_NEWAUTHSERVERSHAREDKEY; /* clear NewAuthServerSharedkey */
       /*
        * TODO:482:o: |-> commit column NewAuthServerSharedkey.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAuthServerTable column NewAuthServerSharedkey commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewAuthServerSharedkey
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWAUTHSERVERSHAREDKEY;
       }
    }

    if (save_flags & FLAG_NEWRADIUSAUTHSERVERIPADD) {
       save_flags &= ~FLAG_NEWRADIUSAUTHSERVERIPADD; /* clear NewRadiusAuthServerIPAdd */
       /*
        * TODO:482:o: |-> commit column NewRadiusAuthServerIPAdd.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAuthServerTable column NewRadiusAuthServerIPAdd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewRadiusAuthServerIPAdd
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWRADIUSAUTHSERVERIPADD;
       }
    }

    if (save_flags & FLAG_NEWRADIUSAUTHSERVERPORT) {
       save_flags &= ~FLAG_NEWRADIUSAUTHSERVERPORT; /* clear NewRadiusAuthServerPort */
       /*
        * TODO:482:o: |-> commit column NewRadiusAuthServerPort.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAuthServerTable column NewRadiusAuthServerPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewRadiusAuthServerPort
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWRADIUSAUTHSERVERPORT;
       }
    }

    if (save_flags & FLAG_NEWRADIUSAUTHSERVERSHAREDKEY) {
       save_flags &= ~FLAG_NEWRADIUSAUTHSERVERSHAREDKEY; /* clear NewRadiusAuthServerSharedkey */
       /*
        * TODO:482:o: |-> commit column NewRadiusAuthServerSharedkey.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAuthServerTable column NewRadiusAuthServerSharedkey commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewRadiusAuthServerSharedkey
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWRADIUSAUTHSERVERSHAREDKEY;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigRadiusAuthServerTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigRadiusAuthServerTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigRadiusAuthServerTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigRadiusAuthServerTable_undo_commit( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigRadiusAuthServerTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11ConfigRadiusAuthServerTable_undo_commit */

/*
 * TODO:420:r: Implement dot11ConfigRadiusAuthServerTable index validation.
 */
/*
 * TODO:440:M: Implement dot11ConfigRadiusAuthServerTable node value checks.
 * TODO:450:M: Implement dot11ConfigRadiusAuthServerTable undo functions.
 * TODO:460:M: Implement dot11ConfigRadiusAuthServerTable set functions.
 * TODO:480:M: Implement dot11ConfigRadiusAuthServerTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewAuthServerIPAdd
 * NewAuthServerIPAdd is subid 2 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.2
 * Description:
NewAuthServerIPAdd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAuthServerIPAdd_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewAuthServerIPAdd_check_value( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewAuthServerIPAdd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerIPAdd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewAuthServerIPAdd value.
     */

    return MFD_SUCCESS; /* NewAuthServerIPAdd value not illegal */
} /* NewAuthServerIPAdd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewAuthServerIPAdd_undo_setup( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerIPAdd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewAuthServerIPAdd undo.
     */
    /*
     * copy NewAuthServerIPAdd data
     * set rowreq_ctx->undo->NewAuthServerIPAdd from rowreq_ctx->data.NewAuthServerIPAdd
     */
    rowreq_ctx->undo->NewAuthServerIPAdd = rowreq_ctx->data.NewAuthServerIPAdd;


    return MFD_SUCCESS;
} /* NewAuthServerIPAdd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewAuthServerIPAdd_val
 *        A u_long containing the new value.
 */
int
NewAuthServerIPAdd_set( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewAuthServerIPAdd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerIPAdd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewAuthServerIPAdd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewAuthServerIPAdd value.
     * set NewAuthServerIPAdd value in rowreq_ctx->data
     */
	int ret = 0;
	int rc = MFD_ERROR;
	char input_string[20] = {0};
	memset(input_string,0,20);
	INET_NTOA(NewAuthServerIPAdd_val,input_string);
	
	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
                            
	ret = security_auth_acct(rowreq_ctx->data.parameter, connection,1,rowreq_ctx->data.RadiusAuthServerID,input_string,rowreq_ctx->data.NewAuthServerPort,rowreq_ctx->data.NewAuthServerSharedkey);
	if(ret == 1)
	{	

	    rowreq_ctx->data.NewAuthServerIPAdd = NewAuthServerIPAdd_val;
		rc = MFD_SUCCESS;

	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* NewAuthServerIPAdd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewAuthServerIPAdd_undo( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerIPAdd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewAuthServerIPAdd undo.
     */
    /*
     * copy NewAuthServerIPAdd data
     * set rowreq_ctx->data.NewAuthServerIPAdd from rowreq_ctx->undo->NewAuthServerIPAdd
     */
    rowreq_ctx->data.NewAuthServerIPAdd = rowreq_ctx->undo->NewAuthServerIPAdd;

    
    return MFD_SUCCESS;
} /* NewAuthServerIPAdd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewAuthServerPort
 * NewAuthServerPort is subid 3 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.3
 * Description:
NewAuthServerPort.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAuthServerPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewAuthServerPort_check_value( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewAuthServerPort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewAuthServerPort value.
     */

    return MFD_SUCCESS; /* NewAuthServerPort value not illegal */
} /* NewAuthServerPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewAuthServerPort_undo_setup( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewAuthServerPort undo.
     */
    /*
     * copy NewAuthServerPort data
     * set rowreq_ctx->undo->NewAuthServerPort from rowreq_ctx->data.NewAuthServerPort
     */
    rowreq_ctx->undo->NewAuthServerPort = rowreq_ctx->data.NewAuthServerPort;


    return MFD_SUCCESS;
} /* NewAuthServerPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewAuthServerPort_val
 *        A long containing the new value.
 */
int
NewAuthServerPort_set( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewAuthServerPort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewAuthServerPort reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewAuthServerPort value.
     * set NewAuthServerPort value in rowreq_ctx->data
     */
	int ret = 0;
	int rc = MFD_ERROR;
	char NewAuthServerIPAdd[20]= {0};
	memset(NewAuthServerIPAdd,0,20);
	INET_NTOA(rowreq_ctx->data.NewAuthServerIPAdd,NewAuthServerIPAdd);

	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
	
	ret = security_auth_acct(rowreq_ctx->data.parameter, connection,1,rowreq_ctx->data.RadiusAuthServerID,NewAuthServerIPAdd,NewAuthServerPort_val,rowreq_ctx->data.NewAuthServerSharedkey);
	if(ret == 1)
	{	
		 rowreq_ctx->data.NewAuthServerPort = NewAuthServerPort_val;
		 rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* NewAuthServerPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewAuthServerPort_undo( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewAuthServerPort undo.
     */
    /*
     * copy NewAuthServerPort data
     * set rowreq_ctx->data.NewAuthServerPort from rowreq_ctx->undo->NewAuthServerPort
     */
    rowreq_ctx->data.NewAuthServerPort = rowreq_ctx->undo->NewAuthServerPort;

    
    return MFD_SUCCESS;
} /* NewAuthServerPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewAuthServerSharedkey
 * NewAuthServerSharedkey is subid 4 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.4
 * Description:
NewAuthServerSharedkey.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAuthServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewAuthServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewAuthServerSharedkey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewAuthServerSharedkey_check_value( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewAuthServerSharedkey_val_ptr,  size_t NewAuthServerSharedkey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerSharedkey_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewAuthServerSharedkey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid NewAuthServerSharedkey value.
     */

    return MFD_SUCCESS; /* NewAuthServerSharedkey value not illegal */
} /* NewAuthServerSharedkey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewAuthServerSharedkey_undo_setup( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerSharedkey_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewAuthServerSharedkey undo.
     */
    /*
     * copy NewAuthServerSharedkey and NewAuthServerSharedkey_len data
     * set rowreq_ctx->undo->NewAuthServerSharedkey from rowreq_ctx->data.NewAuthServerSharedkey
     */
    memcpy( rowreq_ctx->undo->NewAuthServerSharedkey, rowreq_ctx->data.NewAuthServerSharedkey,
            (rowreq_ctx->data.NewAuthServerSharedkey_len * sizeof(rowreq_ctx->undo->NewAuthServerSharedkey[0])));
    rowreq_ctx->undo->NewAuthServerSharedkey_len = rowreq_ctx->data.NewAuthServerSharedkey_len;


    return MFD_SUCCESS;
} /* NewAuthServerSharedkey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewAuthServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewAuthServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewAuthServerSharedkey_val_ptr
 */
int
NewAuthServerSharedkey_set( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewAuthServerSharedkey_val_ptr,  size_t NewAuthServerSharedkey_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerSharedkey_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewAuthServerSharedkey_val_ptr);

/*
 * TODO:245:o: |-> Implement NewAuthServerSharedkey reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewAuthServerSharedkey value.
     * set NewAuthServerSharedkey value in rowreq_ctx->data
     */
	int ret = 0;
	int rc = MFD_ERROR;
	char NewAuthServerIPAdd[20]= {0};
	memset(NewAuthServerIPAdd,0,20);
	INET_NTOA(rowreq_ctx->data.NewAuthServerIPAdd,NewAuthServerIPAdd);

	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	ret = security_auth_acct(rowreq_ctx->data.parameter, connection,1,rowreq_ctx->data.RadiusAuthServerID,NewAuthServerIPAdd,rowreq_ctx->data.NewAuthServerPort,NewAuthServerSharedkey_val_ptr);
	if(ret == 1)
	{	
	    memcpy( rowreq_ctx->data.NewAuthServerSharedkey, NewAuthServerSharedkey_val_ptr, NewAuthServerSharedkey_val_ptr_len );
	    rowreq_ctx->data.NewAuthServerSharedkey_len = NewAuthServerSharedkey_val_ptr_len / sizeof(NewAuthServerSharedkey_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* NewAuthServerSharedkey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewAuthServerSharedkey_undo( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewAuthServerSharedkey_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewAuthServerSharedkey undo.
     */
    /*
     * copy NewAuthServerSharedkey and NewAuthServerSharedkey_len data
     * set rowreq_ctx->data.NewAuthServerSharedkey from rowreq_ctx->undo->NewAuthServerSharedkey
     */
    memcpy( rowreq_ctx->data.NewAuthServerSharedkey, rowreq_ctx->undo->NewAuthServerSharedkey,
            (rowreq_ctx->undo->NewAuthServerSharedkey_len * sizeof(rowreq_ctx->data.NewAuthServerSharedkey[0])));
    rowreq_ctx->data.NewAuthServerSharedkey_len = rowreq_ctx->undo->NewAuthServerSharedkey_len;

    
    return MFD_SUCCESS;
} /* NewAuthServerSharedkey_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewRadiusAuthServerIPAdd
 * NewRadiusAuthServerIPAdd is subid 5 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.5
 * Description:
NewRadiusAuthServerIPAdd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewRadiusAuthServerIPAdd_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewRadiusAuthServerIPAdd_check_value( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewRadiusAuthServerIPAdd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerIPAdd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewRadiusAuthServerIPAdd value.
     */

    return MFD_SUCCESS; /* NewRadiusAuthServerIPAdd value not illegal */
} /* NewRadiusAuthServerIPAdd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewRadiusAuthServerIPAdd_undo_setup( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerIPAdd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewRadiusAuthServerIPAdd undo.
     */
    /*
     * copy NewRadiusAuthServerIPAdd data
     * set rowreq_ctx->undo->NewRadiusAuthServerIPAdd from rowreq_ctx->data.NewRadiusAuthServerIPAdd
     */
    rowreq_ctx->undo->NewRadiusAuthServerIPAdd = rowreq_ctx->data.NewRadiusAuthServerIPAdd;


    return MFD_SUCCESS;
} /* NewRadiusAuthServerIPAdd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewRadiusAuthServerIPAdd_val
 *        A u_long containing the new value.
 */
int
NewRadiusAuthServerIPAdd_set( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewRadiusAuthServerIPAdd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerIPAdd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewRadiusAuthServerIPAdd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewRadiusAuthServerIPAdd value.
     * set NewRadiusAuthServerIPAdd value in rowreq_ctx->data
     */
	int ret = 0;
	char input_string[20] = {0};
	memset(input_string,0,20);
	int rc = MFD_ERROR;
	INET_NTOA(NewRadiusAuthServerIPAdd_val,input_string);	

	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	ret = secondary_radius_auth(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.RadiusAuthServerID,input_string,rowreq_ctx->data.NewRadiusAuthServerPort,rowreq_ctx->data.NewRadiusAuthServerSharedkey);
	if(ret == 1)
	{										 
		rowreq_ctx->data.NewRadiusAuthServerIPAdd = NewRadiusAuthServerIPAdd_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}					

    return rc;
} /* NewRadiusAuthServerIPAdd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewRadiusAuthServerIPAdd_undo( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerIPAdd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewRadiusAuthServerIPAdd undo.
     */
    /*
     * copy NewRadiusAuthServerIPAdd data
     * set rowreq_ctx->data.NewRadiusAuthServerIPAdd from rowreq_ctx->undo->NewRadiusAuthServerIPAdd
     */
    rowreq_ctx->data.NewRadiusAuthServerIPAdd = rowreq_ctx->undo->NewRadiusAuthServerIPAdd;

    
    return MFD_SUCCESS;
} /* NewRadiusAuthServerIPAdd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewRadiusAuthServerPort
 * NewRadiusAuthServerPort is subid 6 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.6
 * Description:
NewRadiusAuthServerPort.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewRadiusAuthServerPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewRadiusAuthServerPort_check_value( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewRadiusAuthServerPort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewRadiusAuthServerPort value.
     */

    return MFD_SUCCESS; /* NewRadiusAuthServerPort value not illegal */
} /* NewRadiusAuthServerPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewRadiusAuthServerPort_undo_setup( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewRadiusAuthServerPort undo.
     */
    /*
     * copy NewRadiusAuthServerPort data
     * set rowreq_ctx->undo->NewRadiusAuthServerPort from rowreq_ctx->data.NewRadiusAuthServerPort
     */
    rowreq_ctx->undo->NewRadiusAuthServerPort = rowreq_ctx->data.NewRadiusAuthServerPort;


    return MFD_SUCCESS;
} /* NewRadiusAuthServerPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewRadiusAuthServerPort_val
 *        A long containing the new value.
 */
int
NewRadiusAuthServerPort_set( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewRadiusAuthServerPort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewRadiusAuthServerPort reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewRadiusAuthServerPort value.
     * set NewRadiusAuthServerPort value in rowreq_ctx->data
     */
	int ret = 0;
	int rc = MFD_ERROR;
	char NewRadiusAuthServerIPAdd[20] = {0};
	memset(NewRadiusAuthServerIPAdd,0,20);
	INET_NTOA(rowreq_ctx->data.NewRadiusAuthServerIPAdd,NewRadiusAuthServerIPAdd);	

	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
	
	ret = secondary_radius_auth(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.RadiusAuthServerID,NewRadiusAuthServerIPAdd,NewRadiusAuthServerPort_val,rowreq_ctx->data.NewRadiusAuthServerSharedkey);
	if(ret == 1)
	{	
		rowreq_ctx->data.NewRadiusAuthServerPort = NewRadiusAuthServerPort_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* NewRadiusAuthServerPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewRadiusAuthServerPort_undo( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewRadiusAuthServerPort undo.
     */
    /*
     * copy NewRadiusAuthServerPort data
     * set rowreq_ctx->data.NewRadiusAuthServerPort from rowreq_ctx->undo->NewRadiusAuthServerPort
     */
    rowreq_ctx->data.NewRadiusAuthServerPort = rowreq_ctx->undo->NewRadiusAuthServerPort;

    
    return MFD_SUCCESS;
} /* NewRadiusAuthServerPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAuthServerEntry.NewRadiusAuthServerSharedkey
 * NewRadiusAuthServerSharedkey is subid 7 of dot11ConfigRadiusAuthServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.6.1.7
 * Description:
NewRadiusAuthServerSharedkey.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewRadiusAuthServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewRadiusAuthServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewRadiusAuthServerSharedkey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewRadiusAuthServerSharedkey_check_value( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewRadiusAuthServerSharedkey_val_ptr,  size_t NewRadiusAuthServerSharedkey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerSharedkey_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewRadiusAuthServerSharedkey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid NewRadiusAuthServerSharedkey value.
     */

    return MFD_SUCCESS; /* NewRadiusAuthServerSharedkey value not illegal */
} /* NewRadiusAuthServerSharedkey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewRadiusAuthServerSharedkey_undo_setup( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerSharedkey_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewRadiusAuthServerSharedkey undo.
     */
    /*
     * copy NewRadiusAuthServerSharedkey and NewRadiusAuthServerSharedkey_len data
     * set rowreq_ctx->undo->NewRadiusAuthServerSharedkey from rowreq_ctx->data.NewRadiusAuthServerSharedkey
     */
    memcpy( rowreq_ctx->undo->NewRadiusAuthServerSharedkey, rowreq_ctx->data.NewRadiusAuthServerSharedkey,
            (rowreq_ctx->data.NewRadiusAuthServerSharedkey_len * sizeof(rowreq_ctx->undo->NewRadiusAuthServerSharedkey[0])));
    rowreq_ctx->undo->NewRadiusAuthServerSharedkey_len = rowreq_ctx->data.NewRadiusAuthServerSharedkey_len;


    return MFD_SUCCESS;
} /* NewRadiusAuthServerSharedkey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewRadiusAuthServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewRadiusAuthServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewRadiusAuthServerSharedkey_val_ptr
 */
int
NewRadiusAuthServerSharedkey_set( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewRadiusAuthServerSharedkey_val_ptr,  size_t NewRadiusAuthServerSharedkey_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerSharedkey_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewRadiusAuthServerSharedkey_val_ptr);

/*
 * TODO:245:o: |-> Implement NewRadiusAuthServerSharedkey reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewRadiusAuthServerSharedkey value.
     * set NewRadiusAuthServerSharedkey value in rowreq_ctx->data
     */

	int ret = 0;
	int rc = MFD_ERROR;
	char NewRadiusAuthServerIPAdd[20] = {0};
	memset(NewRadiusAuthServerIPAdd,0,20);
	INET_NTOA(rowreq_ctx->data.NewRadiusAuthServerIPAdd,NewRadiusAuthServerIPAdd);	
	
	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	ret = secondary_radius_auth(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.RadiusAuthServerID,NewRadiusAuthServerIPAdd,rowreq_ctx->data.NewRadiusAuthServerPort,NewRadiusAuthServerSharedkey_val_ptr);
	if(ret == 1)
	{	
	    memcpy( rowreq_ctx->data.NewRadiusAuthServerSharedkey, NewRadiusAuthServerSharedkey_val_ptr, NewRadiusAuthServerSharedkey_val_ptr_len );
	    rowreq_ctx->data.NewRadiusAuthServerSharedkey_len = NewRadiusAuthServerSharedkey_val_ptr_len / sizeof(NewRadiusAuthServerSharedkey_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* NewRadiusAuthServerSharedkey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewRadiusAuthServerSharedkey_undo( dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAuthServerTable:NewRadiusAuthServerSharedkey_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewRadiusAuthServerSharedkey undo.
     */
    /*
     * copy NewRadiusAuthServerSharedkey and NewRadiusAuthServerSharedkey_len data
     * set rowreq_ctx->data.NewRadiusAuthServerSharedkey from rowreq_ctx->undo->NewRadiusAuthServerSharedkey
     */
    memcpy( rowreq_ctx->data.NewRadiusAuthServerSharedkey, rowreq_ctx->undo->NewRadiusAuthServerSharedkey,
            (rowreq_ctx->undo->NewRadiusAuthServerSharedkey_len * sizeof(rowreq_ctx->data.NewRadiusAuthServerSharedkey[0])));
    rowreq_ctx->data.NewRadiusAuthServerSharedkey_len = rowreq_ctx->undo->NewRadiusAuthServerSharedkey_len;

    
    return MFD_SUCCESS;
} /* NewRadiusAuthServerSharedkey_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigRadiusAuthServerTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigRadiusAuthServerTable if you don't have dependencies)
 */
int
dot11ConfigRadiusAuthServerTable_check_dependencies(dot11ConfigRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigRadiusAuthServerTable:dot11ConfigRadiusAuthServerTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigRadiusAuthServerTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11ConfigRadiusAuthServerTable_check_dependencies */

/** @} */
