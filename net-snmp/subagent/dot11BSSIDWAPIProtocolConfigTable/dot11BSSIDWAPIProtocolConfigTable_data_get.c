/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11BSSIDWAPIProtocolConfigTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11BSSIDWAPIProtocolConfigTable get routines.
 * TODO:240:M: Implement dot11BSSIDWAPIProtocolConfigTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11BSSIDWAPIProtocolConfigTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11BSSIDWAPIProtocolConfigTable is subid 2 of wtpWAPI.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11BSSIDWAPIProtocolConfigTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11BSSIDWAPIProtocolConfigTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssEntry.wtpBssCurrID
 * wtpBssCurrID is subid 1 of dot11WtpBssEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.7.2.1.1
 * Description:
The current AP BSSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpBssCurrID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpBssCurrID_dot11BSSIDWAPIProtocolConfigTable_map(char **mib_wtpBssCurrID_val_ptr_ptr, size_t *mib_wtpBssCurrID_val_ptr_len_ptr, char *raw_wtpBssCurrID_val_ptr, size_t raw_wtpBssCurrID_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpBssCurrID_val_ptr);
    netsnmp_assert((NULL != mib_wtpBssCurrID_val_ptr_ptr) && (NULL != mib_wtpBssCurrID_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wtpBssCurrID_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpBssCurrID non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpBssCurrID_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpBssCurrID_val_ptr_ptr) || (*mib_wtpBssCurrID_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpBssCurrID_val_ptr_ptr = realloc( *mib_wtpBssCurrID_val_ptr_ptr, converted_len * sizeof(**mib_wtpBssCurrID_val_ptr_ptr));
        if(NULL == *mib_wtpBssCurrID_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpBssCurrID_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpBssCurrID_val_ptr_ptr, raw_wtpBssCurrID_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpBssCurrID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11BSSIDWAPIProtocolConfigTable_indexes_set_tbl_idx(dot11BSSIDWAPIProtocolConfigTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, char *wtpBssCurrID_val_ptr,  size_t wtpBssCurrID_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:dot11BSSIDWAPIProtocolConfigTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wtpBssCurrID(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpBssCurrID_len = sizeof(tbl_idx->wtpBssCurrID);
    /*
     * make sure there is enough space for wtpBssCurrID data
     */
    if ((NULL == tbl_idx->wtpBssCurrID) ||
        (tbl_idx->wtpBssCurrID_len < (wtpBssCurrID_val_ptr_len * sizeof(tbl_idx->wtpBssCurrID[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpBssCurrID_len = wtpBssCurrID_val_ptr_len * sizeof(tbl_idx->wtpBssCurrID[0]);
    memcpy( tbl_idx->wtpBssCurrID, wtpBssCurrID_val_ptr, tbl_idx->wtpBssCurrID_len );
    

    return MFD_SUCCESS;
} /* dot11BSSIDWAPIProtocolConfigTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11BSSIDWAPIProtocolConfigTable_indexes_set(dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, char *wtpBssCurrID_val_ptr,  size_t wtpBssCurrID_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:dot11BSSIDWAPIProtocolConfigTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11BSSIDWAPIProtocolConfigTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpBssCurrID_val_ptr, wtpBssCurrID_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11BSSIDWAPIProtocolConfigTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11BSSIDWAPIProtocolConfigTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiControlledAuthControl
 * wapiControlledAuthControl is subid 1 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.1
 * Description:
Whether using uthorization.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wapiControlledAuthControl.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wapiControlledAuthControl_map(u_long *mib_wapiControlledAuthControl_val_ptr, u_long raw_wapiControlledAuthControl_val)
{
    netsnmp_assert(NULL != mib_wapiControlledAuthControl_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiControlledAuthControl_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wapiControlledAuthControl enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wapiControlledAuthControl_val) {
        case INTERNAL_WAPICONTROLLEDAUTHCONTROL_TRUE:
             *mib_wapiControlledAuthControl_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_WAPICONTROLLEDAUTHCONTROL_FALSE:
             *mib_wapiControlledAuthControl_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wapiControlledAuthControl\n", raw_wapiControlledAuthControl_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wapiControlledAuthControl_map */

/**
 * Extract the current value of the wapiControlledAuthControl data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiControlledAuthControl_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wapiControlledAuthControl_get( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long * wapiControlledAuthControl_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wapiControlledAuthControl_val_ptr );


    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiControlledAuthControl_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wapiControlledAuthControl data.
 * set (* wapiControlledAuthControl_val_ptr ) from rowreq_ctx->data
 */
    (* wapiControlledAuthControl_val_ptr ) = rowreq_ctx->data.wapiControlledAuthControl;

    return MFD_SUCCESS;
} /* wapiControlledAuthControl_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiControlledPortControl
 * wapiControlledPortControl is subid 2 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.2
 * Description:
Port control type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  Auto(0), Forceunautherized(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wapiControlledPortControl.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wapiControlledPortControl_map(u_long *mib_wapiControlledPortControl_val_ptr, u_long raw_wapiControlledPortControl_val)
{
    netsnmp_assert(NULL != mib_wapiControlledPortControl_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiControlledPortControl_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wapiControlledPortControl enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wapiControlledPortControl_val) {
        case INTERNAL_WAPICONTROLLEDPORTCONTROL_AUTO:
             *mib_wapiControlledPortControl_val_ptr = WAPICONTROLLEDPORTCONTROL_AUTO;
             break;

        case INTERNAL_WAPICONTROLLEDPORTCONTROL_FORCEUNAUTHERIZED:
             *mib_wapiControlledPortControl_val_ptr = WAPICONTROLLEDPORTCONTROL_FORCEUNAUTHERIZED;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wapiControlledPortControl\n", raw_wapiControlledPortControl_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wapiControlledPortControl_map */

/**
 * Extract the current value of the wapiControlledPortControl data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiControlledPortControl_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wapiControlledPortControl_get( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long * wapiControlledPortControl_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wapiControlledPortControl_val_ptr );


    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiControlledPortControl_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wapiControlledPortControl data.
 * set (* wapiControlledPortControl_val_ptr ) from rowreq_ctx->data
 */
    (* wapiControlledPortControl_val_ptr ) = rowreq_ctx->data.wapiControlledPortControl;

    return MFD_SUCCESS;
} /* wapiControlledPortControl_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiEnabled
 * wapiEnabled is subid 3 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.3
 * Description:
Whether using wapi.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wapiEnabled.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wapiEnabled_map(u_long *mib_wapiEnabled_val_ptr, u_long raw_wapiEnabled_val)
{
    netsnmp_assert(NULL != mib_wapiEnabled_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiEnabled_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wapiEnabled enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wapiEnabled_val) {
        case INTERNAL_WAPIENABLED_TRUE:
             *mib_wapiEnabled_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_WAPIENABLED_FALSE:
             *mib_wapiEnabled_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wapiEnabled\n", raw_wapiEnabled_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wapiEnabled_map */

/**
 * Extract the current value of the wapiEnabled data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiEnabled_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wapiEnabled_get( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long * wapiEnabled_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wapiEnabled_val_ptr );


    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiEnabled_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wapiEnabled data.
 * set (* wapiEnabled_val_ptr ) from rowreq_ctx->data
 */
    (* wapiEnabled_val_ptr ) = rowreq_ctx->data.wapiEnabled;

    return MFD_SUCCESS;
} /* wapiEnabled_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiCertificateUpdateCount
 * wapiCertificateUpdateCount is subid 4 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.4
 * Description:
Authentication handshake retry times.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wapiCertificateUpdateCount.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wapiCertificateUpdateCount_map(u_long *mib_wapiCertificateUpdateCount_val_ptr, u_long raw_wapiCertificateUpdateCount_val)
{
    netsnmp_assert(NULL != mib_wapiCertificateUpdateCount_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiCertificateUpdateCount_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wapiCertificateUpdateCount mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wapiCertificateUpdateCount_val_ptr) = raw_wapiCertificateUpdateCount_val;

    return MFD_SUCCESS;
} /* wapiCertificateUpdateCount_map */

/**
 * Extract the current value of the wapiCertificateUpdateCount data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiCertificateUpdateCount_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wapiCertificateUpdateCount_get( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long * wapiCertificateUpdateCount_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wapiCertificateUpdateCount_val_ptr );


    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiCertificateUpdateCount_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wapiCertificateUpdateCount data.
 * set (* wapiCertificateUpdateCount_val_ptr ) from rowreq_ctx->data
 */
    (* wapiCertificateUpdateCount_val_ptr ) = rowreq_ctx->data.wapiCertificateUpdateCount;

    return MFD_SUCCESS;
} /* wapiCertificateUpdateCount_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiMulticastUpdateCount
 * wapiMulticastUpdateCount is subid 5 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.5
 * Description:
MSK handshake retry times.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wapiMulticastUpdateCount.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wapiMulticastUpdateCount_map(u_long *mib_wapiMulticastUpdateCount_val_ptr, u_long raw_wapiMulticastUpdateCount_val)
{
    netsnmp_assert(NULL != mib_wapiMulticastUpdateCount_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiMulticastUpdateCount_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wapiMulticastUpdateCount mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wapiMulticastUpdateCount_val_ptr) = raw_wapiMulticastUpdateCount_val;

    return MFD_SUCCESS;
} /* wapiMulticastUpdateCount_map */

/**
 * Extract the current value of the wapiMulticastUpdateCount data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiMulticastUpdateCount_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wapiMulticastUpdateCount_get( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long * wapiMulticastUpdateCount_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wapiMulticastUpdateCount_val_ptr );


    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiMulticastUpdateCount_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wapiMulticastUpdateCount data.
 * set (* wapiMulticastUpdateCount_val_ptr ) from rowreq_ctx->data
 */
    (* wapiMulticastUpdateCount_val_ptr ) = rowreq_ctx->data.wapiMulticastUpdateCount;

    return MFD_SUCCESS;
} /* wapiMulticastUpdateCount_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiUnicastUpdateCount
 * wapiUnicastUpdateCount is subid 6 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.6
 * Description:
Unicast handshake retry key times.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wapiUnicastUpdateCount.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wapiUnicastUpdateCount_map(u_long *mib_wapiUnicastUpdateCount_val_ptr, u_long raw_wapiUnicastUpdateCount_val)
{
    netsnmp_assert(NULL != mib_wapiUnicastUpdateCount_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiUnicastUpdateCount_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wapiUnicastUpdateCount mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wapiUnicastUpdateCount_val_ptr) = raw_wapiUnicastUpdateCount_val;

    return MFD_SUCCESS;
} /* wapiUnicastUpdateCount_map */

/**
 * Extract the current value of the wapiUnicastUpdateCount data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiUnicastUpdateCount_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wapiUnicastUpdateCount_get( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long * wapiUnicastUpdateCount_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wapiUnicastUpdateCount_val_ptr );


    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiUnicastUpdateCount_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wapiUnicastUpdateCount data.
 * set (* wapiUnicastUpdateCount_val_ptr ) from rowreq_ctx->data
 */
    (* wapiUnicastUpdateCount_val_ptr ) = rowreq_ctx->data.wapiUnicastUpdateCount;

    return MFD_SUCCESS;
} /* wapiUnicastUpdateCount_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiAuthSuiteSelected
 * wapiAuthSuiteSelected is subid 7 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.7
 * Description:
AKM selected.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wapiAuthSuiteSelected.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wapiAuthSuiteSelected_map(long *mib_wapiAuthSuiteSelected_val_ptr, long raw_wapiAuthSuiteSelected_val)
{
    netsnmp_assert(NULL != mib_wapiAuthSuiteSelected_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiAuthSuiteSelected_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wapiAuthSuiteSelected mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wapiAuthSuiteSelected_val_ptr) = raw_wapiAuthSuiteSelected_val;

    return MFD_SUCCESS;
} /* wapiAuthSuiteSelected_map */

/**
 * Extract the current value of the wapiAuthSuiteSelected data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiAuthSuiteSelected_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wapiAuthSuiteSelected_get( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, long * wapiAuthSuiteSelected_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wapiAuthSuiteSelected_val_ptr );


    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiAuthSuiteSelected_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wapiAuthSuiteSelected data.
 * set (* wapiAuthSuiteSelected_val_ptr ) from rowreq_ctx->data
 */
    (* wapiAuthSuiteSelected_val_ptr ) = rowreq_ctx->data.wapiAuthSuiteSelected;

    return MFD_SUCCESS;
} /* wapiAuthSuiteSelected_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiAuthSuiteRequested
 * wapiAuthSuiteRequested is subid 8 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.8
 * Description:
Request a choice of AKM.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wapiAuthSuiteRequested.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wapiAuthSuiteRequested_map(long *mib_wapiAuthSuiteRequested_val_ptr, long raw_wapiAuthSuiteRequested_val)
{
    netsnmp_assert(NULL != mib_wapiAuthSuiteRequested_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiAuthSuiteRequested_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wapiAuthSuiteRequested mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wapiAuthSuiteRequested_val_ptr) = raw_wapiAuthSuiteRequested_val;

    return MFD_SUCCESS;
} /* wapiAuthSuiteRequested_map */

/**
 * Extract the current value of the wapiAuthSuiteRequested data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiAuthSuiteRequested_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wapiAuthSuiteRequested_get( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, long * wapiAuthSuiteRequested_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wapiAuthSuiteRequested_val_ptr );


    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiAuthSuiteRequested_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wapiAuthSuiteRequested data.
 * set (* wapiAuthSuiteRequested_val_ptr ) from rowreq_ctx->data
 */
    (* wapiAuthSuiteRequested_val_ptr ) = rowreq_ctx->data.wapiAuthSuiteRequested;

    return MFD_SUCCESS;
} /* wapiAuthSuiteRequested_get */



/** @} */
