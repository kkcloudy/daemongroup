/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11BSSIDWAPIProtocolConfigTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11BSSIDWAPIProtocolConfigTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11BSSIDWAPIProtocolConfigTable is subid 2 of wtpWAPI.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11BSSIDWAPIProtocolConfigTable_undo_setup( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:dot11BSSIDWAPIProtocolConfigTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11BSSIDWAPIProtocolConfigTable undo.
     * set up dot11BSSIDWAPIProtocolConfigTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11BSSIDWAPIProtocolConfigTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11BSSIDWAPIProtocolConfigTable_undo_cleanup( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:dot11BSSIDWAPIProtocolConfigTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11BSSIDWAPIProtocolConfigTable undo.
     */

    return rc;
} /* dot11BSSIDWAPIProtocolConfigTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11BSSIDWAPIProtocolConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11BSSIDWAPIProtocolConfigTable_commit( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:dot11BSSIDWAPIProtocolConfigTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11BSSIDWAPIProtocolConfigTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WAPIENABLED) {
       save_flags &= ~FLAG_WAPIENABLED; /* clear wapiEnabled */
       /*
        * TODO:482:o: |-> commit column wapiEnabled.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11BSSIDWAPIProtocolConfigTable column wapiEnabled commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wapiEnabled
             */
            rowreq_ctx->column_set_flags |= FLAG_WAPIENABLED;
       }
    }

    if (save_flags & FLAG_WAPICERTIFICATEUPDATECOUNT) {
       save_flags &= ~FLAG_WAPICERTIFICATEUPDATECOUNT; /* clear wapiCertificateUpdateCount */
       /*
        * TODO:482:o: |-> commit column wapiCertificateUpdateCount.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11BSSIDWAPIProtocolConfigTable column wapiCertificateUpdateCount commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wapiCertificateUpdateCount
             */
            rowreq_ctx->column_set_flags |= FLAG_WAPICERTIFICATEUPDATECOUNT;
       }
    }

    if (save_flags & FLAG_WAPIMULTICASTUPDATECOUNT) {
       save_flags &= ~FLAG_WAPIMULTICASTUPDATECOUNT; /* clear wapiMulticastUpdateCount */
       /*
        * TODO:482:o: |-> commit column wapiMulticastUpdateCount.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11BSSIDWAPIProtocolConfigTable column wapiMulticastUpdateCount commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wapiMulticastUpdateCount
             */
            rowreq_ctx->column_set_flags |= FLAG_WAPIMULTICASTUPDATECOUNT;
       }
    }

    if (save_flags & FLAG_WAPIUNICASTUPDATECOUNT) {
       save_flags &= ~FLAG_WAPIUNICASTUPDATECOUNT; /* clear wapiUnicastUpdateCount */
       /*
        * TODO:482:o: |-> commit column wapiUnicastUpdateCount.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11BSSIDWAPIProtocolConfigTable column wapiUnicastUpdateCount commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wapiUnicastUpdateCount
             */
            rowreq_ctx->column_set_flags |= FLAG_WAPIUNICASTUPDATECOUNT;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11BSSIDWAPIProtocolConfigTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11BSSIDWAPIProtocolConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11BSSIDWAPIProtocolConfigTable_undo_commit( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:dot11BSSIDWAPIProtocolConfigTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11BSSIDWAPIProtocolConfigTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11BSSIDWAPIProtocolConfigTable_undo_commit */

/*
 * TODO:420:r: Implement dot11BSSIDWAPIProtocolConfigTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssEntry.wtpBssCurrID
 * wtpBssCurrID is subid 1 of dot11WtpBssEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.7.2.1.1
 * Description:
The current AP BSSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/*
 * TODO:440:M: Implement dot11BSSIDWAPIProtocolConfigTable node value checks.
 * TODO:450:M: Implement dot11BSSIDWAPIProtocolConfigTable undo functions.
 * TODO:460:M: Implement dot11BSSIDWAPIProtocolConfigTable set functions.
 * TODO:480:M: Implement dot11BSSIDWAPIProtocolConfigTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiEnabled
 * wapiEnabled is subid 3 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.3
 * Description:
Whether using wapi.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiEnabled_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11BSSIDWAPIProtocolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wapiEnabled_check_value( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long wapiEnabled_val)
{
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiEnabled_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wapiEnabled value.
     */

    return MFD_SUCCESS; /* wapiEnabled value not illegal */
} /* wapiEnabled_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11BSSIDWAPIProtocolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wapiEnabled_undo_setup( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiEnabled_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wapiEnabled undo.
     */
    /*
     * copy wapiEnabled data
     * set rowreq_ctx->undo->wapiEnabled from rowreq_ctx->data.wapiEnabled
     */
    rowreq_ctx->undo->wapiEnabled = rowreq_ctx->data.wapiEnabled;


    return MFD_SUCCESS;
} /* wapiEnabled_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wapiEnabled_val
 *        A long containing the new value.
 */
int
wapiEnabled_set( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long wapiEnabled_val )
{

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiEnabled_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wapiEnabled reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wapiEnabled_val) {
        case TRUTHVALUE_TRUE:
             wapiEnabled_val = INTERNAL_WAPIENABLED_TRUE;
             break;

        case TRUTHVALUE_FALSE:
             wapiEnabled_val = INTERNAL_WAPIENABLED_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wapiEnabled\n", wapiEnabled_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wapiEnabled value.
     * set wapiEnabled value in rowreq_ctx->data
     */
    rowreq_ctx->data.wapiEnabled = wapiEnabled_val;

    return MFD_SUCCESS;
} /* wapiEnabled_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wapiEnabled_undo( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiEnabled_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wapiEnabled undo.
     */
    /*
     * copy wapiEnabled data
     * set rowreq_ctx->data.wapiEnabled from rowreq_ctx->undo->wapiEnabled
     */
    rowreq_ctx->data.wapiEnabled = rowreq_ctx->undo->wapiEnabled;

    
    return MFD_SUCCESS;
} /* wapiEnabled_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiCertificateUpdateCount
 * wapiCertificateUpdateCount is subid 4 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.4
 * Description:
Authentication handshake retry times.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiCertificateUpdateCount_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11BSSIDWAPIProtocolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wapiCertificateUpdateCount_check_value( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long wapiCertificateUpdateCount_val)
{
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiCertificateUpdateCount_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wapiCertificateUpdateCount value.
     */

    return MFD_SUCCESS; /* wapiCertificateUpdateCount value not illegal */
} /* wapiCertificateUpdateCount_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11BSSIDWAPIProtocolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wapiCertificateUpdateCount_undo_setup( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiCertificateUpdateCount_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wapiCertificateUpdateCount undo.
     */
    /*
     * copy wapiCertificateUpdateCount data
     * set rowreq_ctx->undo->wapiCertificateUpdateCount from rowreq_ctx->data.wapiCertificateUpdateCount
     */
    rowreq_ctx->undo->wapiCertificateUpdateCount = rowreq_ctx->data.wapiCertificateUpdateCount;


    return MFD_SUCCESS;
} /* wapiCertificateUpdateCount_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wapiCertificateUpdateCount_val
 *        A u_long containing the new value.
 */
int
wapiCertificateUpdateCount_set( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long wapiCertificateUpdateCount_val )
{

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiCertificateUpdateCount_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wapiCertificateUpdateCount reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wapiCertificateUpdateCount value.
     * set wapiCertificateUpdateCount value in rowreq_ctx->data
     */
    rowreq_ctx->data.wapiCertificateUpdateCount = wapiCertificateUpdateCount_val;

    return MFD_SUCCESS;
} /* wapiCertificateUpdateCount_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wapiCertificateUpdateCount_undo( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiCertificateUpdateCount_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wapiCertificateUpdateCount undo.
     */
    /*
     * copy wapiCertificateUpdateCount data
     * set rowreq_ctx->data.wapiCertificateUpdateCount from rowreq_ctx->undo->wapiCertificateUpdateCount
     */
    rowreq_ctx->data.wapiCertificateUpdateCount = rowreq_ctx->undo->wapiCertificateUpdateCount;

    
    return MFD_SUCCESS;
} /* wapiCertificateUpdateCount_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiMulticastUpdateCount
 * wapiMulticastUpdateCount is subid 5 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.5
 * Description:
MSK handshake retry times.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiMulticastUpdateCount_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11BSSIDWAPIProtocolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wapiMulticastUpdateCount_check_value( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long wapiMulticastUpdateCount_val)
{
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiMulticastUpdateCount_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wapiMulticastUpdateCount value.
     */

    return MFD_SUCCESS; /* wapiMulticastUpdateCount value not illegal */
} /* wapiMulticastUpdateCount_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11BSSIDWAPIProtocolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wapiMulticastUpdateCount_undo_setup( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiMulticastUpdateCount_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wapiMulticastUpdateCount undo.
     */
    /*
     * copy wapiMulticastUpdateCount data
     * set rowreq_ctx->undo->wapiMulticastUpdateCount from rowreq_ctx->data.wapiMulticastUpdateCount
     */
    rowreq_ctx->undo->wapiMulticastUpdateCount = rowreq_ctx->data.wapiMulticastUpdateCount;


    return MFD_SUCCESS;
} /* wapiMulticastUpdateCount_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wapiMulticastUpdateCount_val
 *        A u_long containing the new value.
 */
int
wapiMulticastUpdateCount_set( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long wapiMulticastUpdateCount_val )
{

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiMulticastUpdateCount_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wapiMulticastUpdateCount reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wapiMulticastUpdateCount value.
     * set wapiMulticastUpdateCount value in rowreq_ctx->data
     */
    rowreq_ctx->data.wapiMulticastUpdateCount = wapiMulticastUpdateCount_val;

    return MFD_SUCCESS;
} /* wapiMulticastUpdateCount_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wapiMulticastUpdateCount_undo( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiMulticastUpdateCount_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wapiMulticastUpdateCount undo.
     */
    /*
     * copy wapiMulticastUpdateCount data
     * set rowreq_ctx->data.wapiMulticastUpdateCount from rowreq_ctx->undo->wapiMulticastUpdateCount
     */
    rowreq_ctx->data.wapiMulticastUpdateCount = rowreq_ctx->undo->wapiMulticastUpdateCount;

    
    return MFD_SUCCESS;
} /* wapiMulticastUpdateCount_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BSSIDWAPIProtocolConfigEntry.wapiUnicastUpdateCount
 * wapiUnicastUpdateCount is subid 6 of dot11BSSIDWAPIProtocolConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.2.1.6
 * Description:
Unicast handshake retry key times.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wapiUnicastUpdateCount_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11BSSIDWAPIProtocolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wapiUnicastUpdateCount_check_value( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long wapiUnicastUpdateCount_val)
{
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiUnicastUpdateCount_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wapiUnicastUpdateCount value.
     */

    return MFD_SUCCESS; /* wapiUnicastUpdateCount value not illegal */
} /* wapiUnicastUpdateCount_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11BSSIDWAPIProtocolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wapiUnicastUpdateCount_undo_setup( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiUnicastUpdateCount_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wapiUnicastUpdateCount undo.
     */
    /*
     * copy wapiUnicastUpdateCount data
     * set rowreq_ctx->undo->wapiUnicastUpdateCount from rowreq_ctx->data.wapiUnicastUpdateCount
     */
    rowreq_ctx->undo->wapiUnicastUpdateCount = rowreq_ctx->data.wapiUnicastUpdateCount;


    return MFD_SUCCESS;
} /* wapiUnicastUpdateCount_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wapiUnicastUpdateCount_val
 *        A u_long containing the new value.
 */
int
wapiUnicastUpdateCount_set( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx, u_long wapiUnicastUpdateCount_val )
{

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiUnicastUpdateCount_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wapiUnicastUpdateCount reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wapiUnicastUpdateCount value.
     * set wapiUnicastUpdateCount value in rowreq_ctx->data
     */
    rowreq_ctx->data.wapiUnicastUpdateCount = wapiUnicastUpdateCount_val;

    return MFD_SUCCESS;
} /* wapiUnicastUpdateCount_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wapiUnicastUpdateCount_undo( dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11BSSIDWAPIProtocolConfigTable:wapiUnicastUpdateCount_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wapiUnicastUpdateCount undo.
     */
    /*
     * copy wapiUnicastUpdateCount data
     * set rowreq_ctx->data.wapiUnicastUpdateCount from rowreq_ctx->undo->wapiUnicastUpdateCount
     */
    rowreq_ctx->data.wapiUnicastUpdateCount = rowreq_ctx->undo->wapiUnicastUpdateCount;

    
    return MFD_SUCCESS;
} /* wapiUnicastUpdateCount_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11BSSIDWAPIProtocolConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11BSSIDWAPIProtocolConfigTable if you don't have dependencies)
 */
int
dot11BSSIDWAPIProtocolConfigTable_check_dependencies(dot11BSSIDWAPIProtocolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11BSSIDWAPIProtocolConfigTable:dot11BSSIDWAPIProtocolConfigTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11BSSIDWAPIProtocolConfigTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11BSSIDWAPIProtocolConfigTable_check_dependencies */

/** @} */
