/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11SSIDConfigTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11SSIDConfigTable get routines.
 * TODO:240:M: Implement dot11SSIDConfigTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11SSIDConfigTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11SSIDConfigTable is subid 6 of dot11wlanConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11SSIDConfigTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param wlanID_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11SSIDConfigTable_indexes_set_tbl_idx(dot11SSIDConfigTable_mib_index *tbl_idx, long wlanID_val)
{
    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:dot11SSIDConfigTable_indexes_set_tbl_idx","called\n"));

    /* wlanID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    tbl_idx->wlanID = wlanID_val;
    

    return MFD_SUCCESS;
} /* dot11SSIDConfigTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11SSIDConfigTable_indexes_set(dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, long wlanID_val)
{
    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:dot11SSIDConfigTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11SSIDConfigTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wlanID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11SSIDConfigTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11SSIDConfigTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewSSIDName
 * NewSSIDName is subid 1 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.1
 * Description:
Create SSName.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the NewSSIDName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewSSIDName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewSSIDName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewSSIDName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewSSIDName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewSSIDName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewSSIDName_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, char **NewSSIDName_val_ptr_ptr, size_t *NewSSIDName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewSSIDName_val_ptr_ptr) && (NULL != *NewSSIDName_val_ptr_ptr));
   netsnmp_assert( NULL != NewSSIDName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewSSIDName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewSSIDName data.
 * copy (* NewSSIDName_val_ptr_ptr ) data and (* NewSSIDName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewSSIDName data
     */
    if ((NULL == (* NewSSIDName_val_ptr_ptr )) ||
        ((* NewSSIDName_val_ptr_len_ptr ) <
         (rowreq_ctx->data.NewSSIDName_len* sizeof(rowreq_ctx->data.NewSSIDName[0])))) {
        /*
         * allocate space for NewSSIDName data
         */
        (* NewSSIDName_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewSSIDName_len* sizeof(rowreq_ctx->data.NewSSIDName[0]));
        if(NULL == (* NewSSIDName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewSSIDName_val_ptr_len_ptr ) = rowreq_ctx->data.NewSSIDName_len* sizeof(rowreq_ctx->data.NewSSIDName[0]);
    memcpy( (* NewSSIDName_val_ptr_ptr ), rowreq_ctx->data.NewSSIDName, rowreq_ctx->data.NewSSIDName_len* sizeof(rowreq_ctx->data.NewSSIDName[0]) );

    return MFD_SUCCESS;
} /* NewSSIDName_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewSSIDEnabled
 * NewSSIDEnabled is subid 2 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.2
 * Description:
NewSSIDEnabled .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  no(0), yes(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the NewSSIDEnabled data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewSSIDEnabled_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewSSIDEnabled_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewSSIDEnabled_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewSSIDEnabled_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewSSIDEnabled_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewSSIDEnabled data.
 * copy (* NewSSIDEnabled_val_ptr ) from rowreq_ctx->data
 */
    (* NewSSIDEnabled_val_ptr ) = rowreq_ctx->data.NewSSIDEnabled;

    return MFD_SUCCESS;
} /* NewSSIDEnabled_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewSSIDHidden
 * NewSSIDHidden is subid 3 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.3
 * Description:
NewSSIDHidden .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  no(0), yes(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the NewSSIDHidden data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewSSIDHidden_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewSSIDHidden_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewSSIDHidden_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewSSIDHidden_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewSSIDHidden_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewSSIDHidden data.
 * copy (* NewSSIDHidden_val_ptr ) from rowreq_ctx->data
 */
    (* NewSSIDHidden_val_ptr ) = rowreq_ctx->data.NewSSIDHidden;

    return MFD_SUCCESS;
} /* NewSSIDHidden_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewStaIsolate
 * NewStaIsolate is subid 4 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.4
 * Description:
NewStaIsolate .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  no(0), yes(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the NewStaIsolate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewStaIsolate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewStaIsolate_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewStaIsolate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewStaIsolate_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewStaIsolate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewStaIsolate data.
 * copy (* NewStaIsolate_val_ptr ) from rowreq_ctx->data
 */
    (* NewStaIsolate_val_ptr ) = rowreq_ctx->data.NewStaIsolate;

    return MFD_SUCCESS;
} /* NewStaIsolate_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewDot11Auth
 * NewDot11Auth is subid 5 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.5
 * Description:
NewDot11Auth .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 1/8. Values:  open(1), shared(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the NewDot11Auth data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewDot11Auth_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewDot11Auth_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewDot11Auth_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewDot11Auth_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewDot11Auth_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewDot11Auth data.
 * copy (* NewDot11Auth_val_ptr ) from rowreq_ctx->data
 */
    (* NewDot11Auth_val_ptr ) = rowreq_ctx->data.NewDot11Auth;

    return MFD_SUCCESS;
} /* NewDot11Auth_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.Newsecurity
 * Newsecurity is subid 6 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.6
 * Description:
Newsecurity .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 4/8. Values:  none(1), wpa(2), wpa2(3), wapi(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the Newsecurity data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param Newsecurity_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
Newsecurity_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * Newsecurity_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != Newsecurity_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:Newsecurity_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the Newsecurity data.
 * copy (* Newsecurity_val_ptr ) from rowreq_ctx->data
 */
    (* Newsecurity_val_ptr ) = rowreq_ctx->data.Newsecurity;

    return MFD_SUCCESS;
} /* Newsecurity_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewAuthenMode
 * NewAuthenMode is subid 7 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.7
 * Description:
NewAuthenMode .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  none(1), PSK(2), Radius(3), wpai_cer(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the NewAuthenMode data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAuthenMode_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewAuthenMode_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewAuthenMode_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewAuthenMode_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewAuthenMode_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewAuthenMode data.
 * copy (* NewAuthenMode_val_ptr ) from rowreq_ctx->data
 */
    (* NewAuthenMode_val_ptr ) = rowreq_ctx->data.NewAuthenMode;

    return MFD_SUCCESS;
} /* NewAuthenMode_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewSecurityCiphers
 * NewSecurityCiphers is subid 8 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.8
 * Description:
NewSecurityCiphers .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 4/8. Values:  none(1), wep40(2), wep104(3), aes(4), tkip(5), sms4(6)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the NewSecurityCiphers data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewSecurityCiphers_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewSecurityCiphers_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewSecurityCiphers_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewSecurityCiphers_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewSecurityCiphers_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewSecurityCiphers data.
 * copy (* NewSecurityCiphers_val_ptr ) from rowreq_ctx->data
 */
    (* NewSecurityCiphers_val_ptr ) = rowreq_ctx->data.NewSecurityCiphers;

    return MFD_SUCCESS;
} /* NewSecurityCiphers_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewEncrInputType
 * NewEncrInputType is subid 9 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.9
 * Description:
Input type key.While security type is none,this value cannot be set.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  NONE(0), ASCII(1), HEX(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the NewEncrInputType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewEncrInputType_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewEncrInputType_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewEncrInputType_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewEncrInputType_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewEncrInputType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewEncrInputType data.
 * copy (* NewEncrInputType_val_ptr ) from rowreq_ctx->data
 */
    (* NewEncrInputType_val_ptr ) = rowreq_ctx->data.NewEncrInputType;

    return MFD_SUCCESS;
} /* NewEncrInputType_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewSecurityKEY
 * NewSecurityKEY is subid 10 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.10
 * Description:
Key.While security type is none,this value cannot be set.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the NewSecurityKEY data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewSecurityKEY_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewSecurityKEY_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewSecurityKEY.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewSecurityKEY_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewSecurityKEY_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewSecurityKEY_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, char **NewSecurityKEY_val_ptr_ptr, size_t *NewSecurityKEY_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewSecurityKEY_val_ptr_ptr) && (NULL != *NewSecurityKEY_val_ptr_ptr));
   netsnmp_assert( NULL != NewSecurityKEY_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewSecurityKEY_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewSecurityKEY data.
 * copy (* NewSecurityKEY_val_ptr_ptr ) data and (* NewSecurityKEY_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewSecurityKEY data
     */
    if ((NULL == (* NewSecurityKEY_val_ptr_ptr )) ||
        ((* NewSecurityKEY_val_ptr_len_ptr ) <
         (rowreq_ctx->data.NewSecurityKEY_len* sizeof(rowreq_ctx->data.NewSecurityKEY[0])))) {
        /*
         * allocate space for NewSecurityKEY data
         */
        (* NewSecurityKEY_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewSecurityKEY_len* sizeof(rowreq_ctx->data.NewSecurityKEY[0]));
        if(NULL == (* NewSecurityKEY_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewSecurityKEY_val_ptr_len_ptr ) = rowreq_ctx->data.NewSecurityKEY_len* sizeof(rowreq_ctx->data.NewSecurityKEY[0]);
    memcpy( (* NewSecurityKEY_val_ptr_ptr ), rowreq_ctx->data.NewSecurityKEY, rowreq_ctx->data.NewSecurityKEY_len* sizeof(rowreq_ctx->data.NewSecurityKEY[0]) );

    return MFD_SUCCESS;
} /* NewSecurityKEY_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewExtensibleAuth
 * NewExtensibleAuth is subid 11 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.11
 * Description:
Extensible authentication.This value is just for open and shared security type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  disable(0), enable(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the NewExtensibleAuth data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewExtensibleAuth_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewExtensibleAuth_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewExtensibleAuth_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewExtensibleAuth_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewExtensibleAuth_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewExtensibleAuth data.
 * copy (* NewExtensibleAuth_val_ptr ) from rowreq_ctx->data
 */
    (* NewExtensibleAuth_val_ptr ) = rowreq_ctx->data.NewExtensibleAuth;

    return MFD_SUCCESS;
} /* NewExtensibleAuth_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewAuthIP
 * NewAuthIP is subid 12 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.12
 * Description:
Security Auth IP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the NewAuthIP data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAuthIP_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewAuthIP_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewAuthIP_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewAuthIP_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewAuthIP_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewAuthIP data.
 * copy (* NewAuthIP_val_ptr ) from rowreq_ctx->data
 */
    (* NewAuthIP_val_ptr ) = rowreq_ctx->data.NewAuthIP;

    return MFD_SUCCESS;
} /* NewAuthIP_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewAuthPort
 * NewAuthPort is subid 13 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.13
 * Description:
Security Auth Port.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the NewAuthPort data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAuthPort_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewAuthPort_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, long * NewAuthPort_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewAuthPort_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewAuthPort_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewAuthPort data.
 * copy (* NewAuthPort_val_ptr ) from rowreq_ctx->data
 */
    (* NewAuthPort_val_ptr ) = rowreq_ctx->data.NewAuthPort;

    return MFD_SUCCESS;
} /* NewAuthPort_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewAuthSharedSecret
 * NewAuthSharedSecret is subid 14 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.14
 * Description:
Security Auth Shared Secret.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the NewAuthSharedSecret data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAuthSharedSecret_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewAuthSharedSecret_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewAuthSharedSecret.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewAuthSharedSecret_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewAuthSharedSecret_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewAuthSharedSecret_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, char **NewAuthSharedSecret_val_ptr_ptr, size_t *NewAuthSharedSecret_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewAuthSharedSecret_val_ptr_ptr) && (NULL != *NewAuthSharedSecret_val_ptr_ptr));
   netsnmp_assert( NULL != NewAuthSharedSecret_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewAuthSharedSecret_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewAuthSharedSecret data.
 * copy (* NewAuthSharedSecret_val_ptr_ptr ) data and (* NewAuthSharedSecret_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewAuthSharedSecret data
     */
    if ((NULL == (* NewAuthSharedSecret_val_ptr_ptr )) ||
        ((* NewAuthSharedSecret_val_ptr_len_ptr ) <
         (rowreq_ctx->data.NewAuthSharedSecret_len* sizeof(rowreq_ctx->data.NewAuthSharedSecret[0])))) {
        /*
         * allocate space for NewAuthSharedSecret data
         */
        (* NewAuthSharedSecret_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewAuthSharedSecret_len* sizeof(rowreq_ctx->data.NewAuthSharedSecret[0]));
        if(NULL == (* NewAuthSharedSecret_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewAuthSharedSecret_val_ptr_len_ptr ) = rowreq_ctx->data.NewAuthSharedSecret_len* sizeof(rowreq_ctx->data.NewAuthSharedSecret[0]);
    memcpy( (* NewAuthSharedSecret_val_ptr_ptr ), rowreq_ctx->data.NewAuthSharedSecret, rowreq_ctx->data.NewAuthSharedSecret_len* sizeof(rowreq_ctx->data.NewAuthSharedSecret[0]) );

    return MFD_SUCCESS;
} /* NewAuthSharedSecret_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewAcctIP
 * NewAcctIP is subid 15 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.15
 * Description:
Security Acct IP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the NewAcctIP data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAcctIP_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewAcctIP_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewAcctIP_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewAcctIP_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewAcctIP_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewAcctIP data.
 * copy (* NewAcctIP_val_ptr ) from rowreq_ctx->data
 */
    (* NewAcctIP_val_ptr ) = rowreq_ctx->data.NewAcctIP;

    return MFD_SUCCESS;
} /* NewAcctIP_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewAcctPort
 * NewAcctPort is subid 16 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.16
 * Description:
Security Acct Port.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the NewAcctPort data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAcctPort_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewAcctPort_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, long * NewAcctPort_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewAcctPort_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewAcctPort_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewAcctPort data.
 * copy (* NewAcctPort_val_ptr ) from rowreq_ctx->data
 */
    (* NewAcctPort_val_ptr ) = rowreq_ctx->data.NewAcctPort;

    return MFD_SUCCESS;
} /* NewAcctPort_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewAcctSharedSecret
 * NewAcctSharedSecret is subid 17 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.17
 * Description:
Security Acct Shared Secret.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the NewAcctSharedSecret data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAcctSharedSecret_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewAcctSharedSecret_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewAcctSharedSecret.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewAcctSharedSecret_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewAcctSharedSecret_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewAcctSharedSecret_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, char **NewAcctSharedSecret_val_ptr_ptr, size_t *NewAcctSharedSecret_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewAcctSharedSecret_val_ptr_ptr) && (NULL != *NewAcctSharedSecret_val_ptr_ptr));
   netsnmp_assert( NULL != NewAcctSharedSecret_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewAcctSharedSecret_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewAcctSharedSecret data.
 * copy (* NewAcctSharedSecret_val_ptr_ptr ) data and (* NewAcctSharedSecret_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewAcctSharedSecret data
     */
    if ((NULL == (* NewAcctSharedSecret_val_ptr_ptr )) ||
        ((* NewAcctSharedSecret_val_ptr_len_ptr ) <
         (rowreq_ctx->data.NewAcctSharedSecret_len* sizeof(rowreq_ctx->data.NewAcctSharedSecret[0])))) {
        /*
         * allocate space for NewAcctSharedSecret data
         */
        (* NewAcctSharedSecret_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewAcctSharedSecret_len* sizeof(rowreq_ctx->data.NewAcctSharedSecret[0]));
        if(NULL == (* NewAcctSharedSecret_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewAcctSharedSecret_val_ptr_len_ptr ) = rowreq_ctx->data.NewAcctSharedSecret_len* sizeof(rowreq_ctx->data.NewAcctSharedSecret[0]);
    memcpy( (* NewAcctSharedSecret_val_ptr_ptr ), rowreq_ctx->data.NewAcctSharedSecret, rowreq_ctx->data.NewAcctSharedSecret_len* sizeof(rowreq_ctx->data.NewAcctSharedSecret[0]) );

    return MFD_SUCCESS;
} /* NewAcctSharedSecret_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewVlanId
 * NewVlanId is subid 18 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.18
 * Description:
NewVlanId .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the NewVlanId data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewVlanId_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewVlanId_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, long * NewVlanId_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewVlanId_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewVlanId_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewVlanId data.
 * copy (* NewVlanId_val_ptr ) from rowreq_ctx->data
 */
    (* NewVlanId_val_ptr ) = rowreq_ctx->data.NewVlanId;

    return MFD_SUCCESS;
} /* NewVlanId_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewMaxSimultUsers
 * NewMaxSimultUsers is subid 19 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.19
 * Description:
NewMaxSimultUsers .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the NewMaxSimultUsers data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewMaxSimultUsers_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewMaxSimultUsers_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, long * NewMaxSimultUsers_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewMaxSimultUsers_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewMaxSimultUsers_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewMaxSimultUsers data.
 * copy (* NewMaxSimultUsers_val_ptr ) from rowreq_ctx->data
 */
    (* NewMaxSimultUsers_val_ptr ) = rowreq_ctx->data.NewMaxSimultUsers;

    return MFD_SUCCESS;
} /* NewMaxSimultUsers_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewStaUplinkMaxRate
 * NewStaUplinkMaxRate is subid 20 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.20
 * Description:
NewStaUplinkMaxRate. 0 meaning not limited to speed
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the NewStaUplinkMaxRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewStaUplinkMaxRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewStaUplinkMaxRate_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, long * NewStaUplinkMaxRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewStaUplinkMaxRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewStaUplinkMaxRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewStaUplinkMaxRate data.
 * copy (* NewStaUplinkMaxRate_val_ptr ) from rowreq_ctx->data
 */
    (* NewStaUplinkMaxRate_val_ptr ) = rowreq_ctx->data.NewStaUplinkMaxRate;

    return MFD_SUCCESS;
} /* NewStaUplinkMaxRate_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewStaDwlinkMaxRate
 * NewStaDwlinkMaxRate is subid 21 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.21
 * Description:
NewStaDwlinkMaxRate .0 meaning not limited to speed
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the NewStaDwlinkMaxRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewStaDwlinkMaxRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewStaDwlinkMaxRate_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, long * NewStaDwlinkMaxRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewStaDwlinkMaxRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewStaDwlinkMaxRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewStaDwlinkMaxRate data.
 * copy (* NewStaDwlinkMaxRate_val_ptr ) from rowreq_ctx->data
 */
    (* NewStaDwlinkMaxRate_val_ptr ) = rowreq_ctx->data.NewStaDwlinkMaxRate;

    return MFD_SUCCESS;
} /* NewStaDwlinkMaxRate_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.SSIDRowStatus
 * SSIDRowStatus is subid 22 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.22
 * Description:
This variable is used to create, modify, and/or delete a row
		        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the SSIDRowStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDRowStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDRowStatus_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * SSIDRowStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDRowStatus_val_ptr );

    (* SSIDRowStatus_val_ptr ) = rowreq_ctx->data.SSIDRowStatus;

    return MFD_SUCCESS;
} /* SSIDRowStatus_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.StasDeadTime
 * StasDeadTime is subid 25 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.25
 * Description:
The station dead time
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the StasDeadTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param StasDeadTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
StasDeadTime_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * StasDeadTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != StasDeadTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:StasDeadTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the StasDeadTime data.
 * copy (* StasDeadTime_val_ptr ) from rowreq_ctx->data
 */
    (* StasDeadTime_val_ptr ) = rowreq_ctx->data.StasDeadTime;

    return MFD_SUCCESS;
} /* StasDeadTime_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.AuthDeadTime
 * AuthDeadTime is subid 26 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.26
 * Description:
The auth user dead time
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the AuthDeadTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param AuthDeadTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
AuthDeadTime_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * AuthDeadTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != AuthDeadTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:AuthDeadTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the AuthDeadTime data.
 * copy (* AuthDeadTime_val_ptr ) from rowreq_ctx->data
 */
    (* AuthDeadTime_val_ptr ) = rowreq_ctx->data.AuthDeadTime;

    return MFD_SUCCESS;
} /* AuthDeadTime_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewWapiAuthIP
 * NewWapiAuthIP is subid 27 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.27
 * Description:
Wapi Auth IP,this value is just for wapi_auth security mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the NewWapiAuthIP data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewWapiAuthIP_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewWapiAuthIP_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewWapiAuthIP_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewWapiAuthIP_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewWapiAuthIP_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewWapiAuthIP data.
 * copy (* NewWapiAuthIP_val_ptr ) from rowreq_ctx->data
 */
    (* NewWapiAuthIP_val_ptr ) = rowreq_ctx->data.NewWapiAuthIP;

    return MFD_SUCCESS;
} /* NewWapiAuthIP_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewWapiCertificationType
 * NewWapiCertificationType is subid 28 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.28
 * Description:
Wapi certification type,this value is just for wapi_auth security mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  X509(1), GBW(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the NewWapiCertificationType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewWapiCertificationType_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewWapiCertificationType_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, u_long * NewWapiCertificationType_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewWapiCertificationType_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewWapiCertificationType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewWapiCertificationType data.
 * copy (* NewWapiCertificationType_val_ptr ) from rowreq_ctx->data
 */
    (* NewWapiCertificationType_val_ptr ) = rowreq_ctx->data.NewWapiCertificationType;

    return MFD_SUCCESS;
} /* NewWapiCertificationType_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewWapiASCertificationPath
 * NewWapiASCertificationPath is subid 29 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.29
 * Description:
Wapi AS certification path,this value is just for wapi_auth security mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the NewWapiASCertificationPath data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewWapiASCertificationPath_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewWapiASCertificationPath_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewWapiASCertificationPath.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewWapiASCertificationPath_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewWapiASCertificationPath_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewWapiASCertificationPath_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, char **NewWapiASCertificationPath_val_ptr_ptr, size_t *NewWapiASCertificationPath_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewWapiASCertificationPath_val_ptr_ptr) && (NULL != *NewWapiASCertificationPath_val_ptr_ptr));
   netsnmp_assert( NULL != NewWapiASCertificationPath_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewWapiASCertificationPath_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewWapiASCertificationPath data.
 * copy (* NewWapiASCertificationPath_val_ptr_ptr ) data and (* NewWapiASCertificationPath_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewWapiASCertificationPath data
     */
    if ((NULL == (* NewWapiASCertificationPath_val_ptr_ptr )) ||
        ((* NewWapiASCertificationPath_val_ptr_len_ptr ) <
         (rowreq_ctx->data.NewWapiASCertificationPath_len* sizeof(rowreq_ctx->data.NewWapiASCertificationPath[0])))) {
        /*
         * allocate space for NewWapiASCertificationPath data
         */
        (* NewWapiASCertificationPath_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewWapiASCertificationPath_len* sizeof(rowreq_ctx->data.NewWapiASCertificationPath[0]));
        if(NULL == (* NewWapiASCertificationPath_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewWapiASCertificationPath_val_ptr_len_ptr ) = rowreq_ctx->data.NewWapiASCertificationPath_len* sizeof(rowreq_ctx->data.NewWapiASCertificationPath[0]);
    memcpy( (* NewWapiASCertificationPath_val_ptr_ptr ), rowreq_ctx->data.NewWapiASCertificationPath, rowreq_ctx->data.NewWapiASCertificationPath_len* sizeof(rowreq_ctx->data.NewWapiASCertificationPath[0]) );

    return MFD_SUCCESS;
} /* NewWapiASCertificationPath_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDConfigEntry.NewWapiAECertificationPath
 * NewWapiAECertificationPath is subid 30 of dot11SSIDConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.13.6.1.30
 * Description:
Wapi AS certification path,this value is just for wapi_auth security mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the NewWapiAECertificationPath data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewWapiAECertificationPath_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewWapiAECertificationPath_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewWapiAECertificationPath.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewWapiAECertificationPath_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewWapiAECertificationPath_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewWapiAECertificationPath_get( dot11SSIDConfigTable_rowreq_ctx *rowreq_ctx, char **NewWapiAECertificationPath_val_ptr_ptr, size_t *NewWapiAECertificationPath_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewWapiAECertificationPath_val_ptr_ptr) && (NULL != *NewWapiAECertificationPath_val_ptr_ptr));
   netsnmp_assert( NULL != NewWapiAECertificationPath_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDConfigTable:NewWapiAECertificationPath_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewWapiAECertificationPath data.
 * copy (* NewWapiAECertificationPath_val_ptr_ptr ) data and (* NewWapiAECertificationPath_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewWapiAECertificationPath data
     */
    if ((NULL == (* NewWapiAECertificationPath_val_ptr_ptr )) ||
        ((* NewWapiAECertificationPath_val_ptr_len_ptr ) <
         (rowreq_ctx->data.NewWapiAECertificationPath_len* sizeof(rowreq_ctx->data.NewWapiAECertificationPath[0])))) {
        /*
         * allocate space for NewWapiAECertificationPath data
         */
        (* NewWapiAECertificationPath_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewWapiAECertificationPath_len* sizeof(rowreq_ctx->data.NewWapiAECertificationPath[0]));
        if(NULL == (* NewWapiAECertificationPath_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewWapiAECertificationPath_val_ptr_len_ptr ) = rowreq_ctx->data.NewWapiAECertificationPath_len* sizeof(rowreq_ctx->data.NewWapiAECertificationPath[0]);
    memcpy( (* NewWapiAECertificationPath_val_ptr_ptr ), rowreq_ctx->data.NewWapiAECertificationPath, rowreq_ctx->data.NewWapiAECertificationPath_len* sizeof(rowreq_ctx->data.NewWapiAECertificationPath[0]) );

    return MFD_SUCCESS;
} /* NewWapiAECertificationPath_get */



/** @} */
