/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpStatisticsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpStatisticsTable get routines.
 * TODO:240:M: Implement dot11WtpStatisticsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpStatisticsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpStatisticsTable is subid 6 of wtpStats.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.6, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpStatisticsTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11WtpStatisticsTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpStatisticsTable_indexes_set_tbl_idx(dot11WtpStatisticsTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:dot11WtpStatisticsTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11WtpStatisticsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpStatisticsTable_indexes_set(dot11WtpStatisticsTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:dot11WtpStatisticsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpStatisticsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpStatisticsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpStatisticsTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatisticsEntry.wtpRxPacketLossRate
 * wtpRxPacketLossRate is subid 1 of dot11WtpStatisticsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.6.1.1
 * Description:
The receive packet loss rate of AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpRxPacketLossRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpRxPacketLossRate_map(char **mib_wtpRxPacketLossRate_val_ptr_ptr, size_t *mib_wtpRxPacketLossRate_val_ptr_len_ptr, char *raw_wtpRxPacketLossRate_val_ptr, size_t raw_wtpRxPacketLossRate_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpRxPacketLossRate_val_ptr);
    netsnmp_assert((NULL != mib_wtpRxPacketLossRate_val_ptr_ptr) && (NULL != mib_wtpRxPacketLossRate_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpRxPacketLossRate_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpRxPacketLossRate non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpRxPacketLossRate_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpRxPacketLossRate_val_ptr_ptr) || (*mib_wtpRxPacketLossRate_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpRxPacketLossRate_val_ptr_ptr = realloc( *mib_wtpRxPacketLossRate_val_ptr_ptr, converted_len * sizeof(**mib_wtpRxPacketLossRate_val_ptr_ptr));
        if(NULL == *mib_wtpRxPacketLossRate_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpRxPacketLossRate_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpRxPacketLossRate_val_ptr_ptr, raw_wtpRxPacketLossRate_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpRxPacketLossRate_map */

/**
 * Extract the current value of the wtpRxPacketLossRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRxPacketLossRate_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpRxPacketLossRate_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpRxPacketLossRate.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpRxPacketLossRate_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpRxPacketLossRate_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpRxPacketLossRate_get( dot11WtpStatisticsTable_rowreq_ctx *rowreq_ctx, char **wtpRxPacketLossRate_val_ptr_ptr, size_t *wtpRxPacketLossRate_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpRxPacketLossRate_val_ptr_ptr) && (NULL != *wtpRxPacketLossRate_val_ptr_ptr));
   netsnmp_assert( NULL != wtpRxPacketLossRate_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpRxPacketLossRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpRxPacketLossRate data.
 * set (* wtpRxPacketLossRate_val_ptr_ptr ) and (* wtpRxPacketLossRate_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpRxPacketLossRate data
     */
    if ((NULL == (* wtpRxPacketLossRate_val_ptr_ptr )) ||
        ((* wtpRxPacketLossRate_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpRxPacketLossRate_len * sizeof((* wtpRxPacketLossRate_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpRxPacketLossRate data
         */
        (* wtpRxPacketLossRate_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpRxPacketLossRate_len * sizeof((* wtpRxPacketLossRate_val_ptr_ptr )[0]));
        if(NULL == (* wtpRxPacketLossRate_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpRxPacketLossRate_val_ptr_len_ptr ) = rowreq_ctx->data.wtpRxPacketLossRate_len * sizeof((* wtpRxPacketLossRate_val_ptr_ptr )[0]);
    memcpy( (* wtpRxPacketLossRate_val_ptr_ptr ), rowreq_ctx->data.wtpRxPacketLossRate, (* wtpRxPacketLossRate_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpRxPacketLossRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatisticsEntry.wtpTxPacketLossRate
 * wtpTxPacketLossRate is subid 2 of dot11WtpStatisticsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.6.1.2
 * Description:
The send packet loss rate of AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTxPacketLossRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTxPacketLossRate_map(char **mib_wtpTxPacketLossRate_val_ptr_ptr, size_t *mib_wtpTxPacketLossRate_val_ptr_len_ptr, char *raw_wtpTxPacketLossRate_val_ptr, size_t raw_wtpTxPacketLossRate_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpTxPacketLossRate_val_ptr);
    netsnmp_assert((NULL != mib_wtpTxPacketLossRate_val_ptr_ptr) && (NULL != mib_wtpTxPacketLossRate_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpTxPacketLossRate_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpTxPacketLossRate non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpTxPacketLossRate_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpTxPacketLossRate_val_ptr_ptr) || (*mib_wtpTxPacketLossRate_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpTxPacketLossRate_val_ptr_ptr = realloc( *mib_wtpTxPacketLossRate_val_ptr_ptr, converted_len * sizeof(**mib_wtpTxPacketLossRate_val_ptr_ptr));
        if(NULL == *mib_wtpTxPacketLossRate_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpTxPacketLossRate_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpTxPacketLossRate_val_ptr_ptr, raw_wtpTxPacketLossRate_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpTxPacketLossRate_map */

/**
 * Extract the current value of the wtpTxPacketLossRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTxPacketLossRate_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpTxPacketLossRate_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpTxPacketLossRate.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpTxPacketLossRate_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpTxPacketLossRate_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpTxPacketLossRate_get( dot11WtpStatisticsTable_rowreq_ctx *rowreq_ctx, char **wtpTxPacketLossRate_val_ptr_ptr, size_t *wtpTxPacketLossRate_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpTxPacketLossRate_val_ptr_ptr) && (NULL != *wtpTxPacketLossRate_val_ptr_ptr));
   netsnmp_assert( NULL != wtpTxPacketLossRate_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpTxPacketLossRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTxPacketLossRate data.
 * set (* wtpTxPacketLossRate_val_ptr_ptr ) and (* wtpTxPacketLossRate_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpTxPacketLossRate data
     */
    if ((NULL == (* wtpTxPacketLossRate_val_ptr_ptr )) ||
        ((* wtpTxPacketLossRate_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpTxPacketLossRate_len * sizeof((* wtpTxPacketLossRate_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpTxPacketLossRate data
         */
        (* wtpTxPacketLossRate_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpTxPacketLossRate_len * sizeof((* wtpTxPacketLossRate_val_ptr_ptr )[0]));
        if(NULL == (* wtpTxPacketLossRate_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpTxPacketLossRate_val_ptr_len_ptr ) = rowreq_ctx->data.wtpTxPacketLossRate_len * sizeof((* wtpTxPacketLossRate_val_ptr_ptr )[0]);
    memcpy( (* wtpTxPacketLossRate_val_ptr_ptr ), rowreq_ctx->data.wtpTxPacketLossRate, (* wtpTxPacketLossRate_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpTxPacketLossRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatisticsEntry.wtpDownBandwidthUtilization
 * wtpDownBandwidthUtilization is subid 3 of dot11WtpStatisticsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.6.1.3
 * Description:
The bandwidth utilization of downlink.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpDownBandwidthUtilization.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpDownBandwidthUtilization_map(char **mib_wtpDownBandwidthUtilization_val_ptr_ptr, size_t *mib_wtpDownBandwidthUtilization_val_ptr_len_ptr, char *raw_wtpDownBandwidthUtilization_val_ptr, size_t raw_wtpDownBandwidthUtilization_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpDownBandwidthUtilization_val_ptr);
    netsnmp_assert((NULL != mib_wtpDownBandwidthUtilization_val_ptr_ptr) && (NULL != mib_wtpDownBandwidthUtilization_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpDownBandwidthUtilization_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpDownBandwidthUtilization non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpDownBandwidthUtilization_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpDownBandwidthUtilization_val_ptr_ptr) || (*mib_wtpDownBandwidthUtilization_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpDownBandwidthUtilization_val_ptr_ptr = realloc( *mib_wtpDownBandwidthUtilization_val_ptr_ptr, converted_len * sizeof(**mib_wtpDownBandwidthUtilization_val_ptr_ptr));
        if(NULL == *mib_wtpDownBandwidthUtilization_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpDownBandwidthUtilization_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpDownBandwidthUtilization_val_ptr_ptr, raw_wtpDownBandwidthUtilization_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpDownBandwidthUtilization_map */

/**
 * Extract the current value of the wtpDownBandwidthUtilization data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpDownBandwidthUtilization_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpDownBandwidthUtilization_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpDownBandwidthUtilization.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpDownBandwidthUtilization_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpDownBandwidthUtilization_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpDownBandwidthUtilization_get( dot11WtpStatisticsTable_rowreq_ctx *rowreq_ctx, char **wtpDownBandwidthUtilization_val_ptr_ptr, size_t *wtpDownBandwidthUtilization_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpDownBandwidthUtilization_val_ptr_ptr) && (NULL != *wtpDownBandwidthUtilization_val_ptr_ptr));
   netsnmp_assert( NULL != wtpDownBandwidthUtilization_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpDownBandwidthUtilization_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpDownBandwidthUtilization data.
 * set (* wtpDownBandwidthUtilization_val_ptr_ptr ) and (* wtpDownBandwidthUtilization_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpDownBandwidthUtilization data
     */
    if ((NULL == (* wtpDownBandwidthUtilization_val_ptr_ptr )) ||
        ((* wtpDownBandwidthUtilization_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpDownBandwidthUtilization_len * sizeof((* wtpDownBandwidthUtilization_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpDownBandwidthUtilization data
         */
        (* wtpDownBandwidthUtilization_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpDownBandwidthUtilization_len * sizeof((* wtpDownBandwidthUtilization_val_ptr_ptr )[0]));
        if(NULL == (* wtpDownBandwidthUtilization_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpDownBandwidthUtilization_val_ptr_len_ptr ) = rowreq_ctx->data.wtpDownBandwidthUtilization_len * sizeof((* wtpDownBandwidthUtilization_val_ptr_ptr )[0]);
    memcpy( (* wtpDownBandwidthUtilization_val_ptr_ptr ), rowreq_ctx->data.wtpDownBandwidthUtilization, (* wtpDownBandwidthUtilization_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpDownBandwidthUtilization_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatisticsEntry.wtpUpBandwidthUtilization
 * wtpUpBandwidthUtilization is subid 4 of dot11WtpStatisticsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.6.1.4
 * Description:
The bandwidth utilization of uplink.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpUpBandwidthUtilization.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpUpBandwidthUtilization_map(char **mib_wtpUpBandwidthUtilization_val_ptr_ptr, size_t *mib_wtpUpBandwidthUtilization_val_ptr_len_ptr, char *raw_wtpUpBandwidthUtilization_val_ptr, size_t raw_wtpUpBandwidthUtilization_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpUpBandwidthUtilization_val_ptr);
    netsnmp_assert((NULL != mib_wtpUpBandwidthUtilization_val_ptr_ptr) && (NULL != mib_wtpUpBandwidthUtilization_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpUpBandwidthUtilization_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpUpBandwidthUtilization non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpUpBandwidthUtilization_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpUpBandwidthUtilization_val_ptr_ptr) || (*mib_wtpUpBandwidthUtilization_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpUpBandwidthUtilization_val_ptr_ptr = realloc( *mib_wtpUpBandwidthUtilization_val_ptr_ptr, converted_len * sizeof(**mib_wtpUpBandwidthUtilization_val_ptr_ptr));
        if(NULL == *mib_wtpUpBandwidthUtilization_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpUpBandwidthUtilization_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpUpBandwidthUtilization_val_ptr_ptr, raw_wtpUpBandwidthUtilization_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpUpBandwidthUtilization_map */

/**
 * Extract the current value of the wtpUpBandwidthUtilization data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUpBandwidthUtilization_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpUpBandwidthUtilization_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpUpBandwidthUtilization.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpUpBandwidthUtilization_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpUpBandwidthUtilization_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpUpBandwidthUtilization_get( dot11WtpStatisticsTable_rowreq_ctx *rowreq_ctx, char **wtpUpBandwidthUtilization_val_ptr_ptr, size_t *wtpUpBandwidthUtilization_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpUpBandwidthUtilization_val_ptr_ptr) && (NULL != *wtpUpBandwidthUtilization_val_ptr_ptr));
   netsnmp_assert( NULL != wtpUpBandwidthUtilization_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpUpBandwidthUtilization_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpUpBandwidthUtilization data.
 * set (* wtpUpBandwidthUtilization_val_ptr_ptr ) and (* wtpUpBandwidthUtilization_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpUpBandwidthUtilization data
     */
    if ((NULL == (* wtpUpBandwidthUtilization_val_ptr_ptr )) ||
        ((* wtpUpBandwidthUtilization_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpUpBandwidthUtilization_len * sizeof((* wtpUpBandwidthUtilization_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpUpBandwidthUtilization data
         */
        (* wtpUpBandwidthUtilization_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpUpBandwidthUtilization_len * sizeof((* wtpUpBandwidthUtilization_val_ptr_ptr )[0]));
        if(NULL == (* wtpUpBandwidthUtilization_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpUpBandwidthUtilization_val_ptr_len_ptr ) = rowreq_ctx->data.wtpUpBandwidthUtilization_len * sizeof((* wtpUpBandwidthUtilization_val_ptr_ptr )[0]);
    memcpy( (* wtpUpBandwidthUtilization_val_ptr_ptr ), rowreq_ctx->data.wtpUpBandwidthUtilization, (* wtpUpBandwidthUtilization_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpUpBandwidthUtilization_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatisticsEntry.wtpReceiveRate
 * wtpReceiveRate is subid 5 of dot11WtpStatisticsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.6.1.5
 * Description:
The ap receive rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReceiveRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReceiveRate_map(char **mib_wtpReceiveRate_val_ptr_ptr, size_t *mib_wtpReceiveRate_val_ptr_len_ptr, char *raw_wtpReceiveRate_val_ptr, size_t raw_wtpReceiveRate_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpReceiveRate_val_ptr);
    netsnmp_assert((NULL != mib_wtpReceiveRate_val_ptr_ptr) && (NULL != mib_wtpReceiveRate_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpReceiveRate_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpReceiveRate non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpReceiveRate_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpReceiveRate_val_ptr_ptr) || (*mib_wtpReceiveRate_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpReceiveRate_val_ptr_ptr = realloc( *mib_wtpReceiveRate_val_ptr_ptr, converted_len * sizeof(**mib_wtpReceiveRate_val_ptr_ptr));
        if(NULL == *mib_wtpReceiveRate_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpReceiveRate_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpReceiveRate_val_ptr_ptr, raw_wtpReceiveRate_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpReceiveRate_map */

/**
 * Extract the current value of the wtpReceiveRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReceiveRate_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpReceiveRate_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpReceiveRate.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpReceiveRate_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpReceiveRate_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpReceiveRate_get( dot11WtpStatisticsTable_rowreq_ctx *rowreq_ctx, char **wtpReceiveRate_val_ptr_ptr, size_t *wtpReceiveRate_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpReceiveRate_val_ptr_ptr) && (NULL != *wtpReceiveRate_val_ptr_ptr));
   netsnmp_assert( NULL != wtpReceiveRate_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpReceiveRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReceiveRate data.
 * set (* wtpReceiveRate_val_ptr_ptr ) and (* wtpReceiveRate_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpReceiveRate data
     */
    if ((NULL == (* wtpReceiveRate_val_ptr_ptr )) ||
        ((* wtpReceiveRate_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpReceiveRate_len * sizeof((* wtpReceiveRate_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpReceiveRate data
         */
        (* wtpReceiveRate_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpReceiveRate_len * sizeof((* wtpReceiveRate_val_ptr_ptr )[0]));
        if(NULL == (* wtpReceiveRate_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpReceiveRate_val_ptr_len_ptr ) = rowreq_ctx->data.wtpReceiveRate_len * sizeof((* wtpReceiveRate_val_ptr_ptr )[0]);
    memcpy( (* wtpReceiveRate_val_ptr_ptr ), rowreq_ctx->data.wtpReceiveRate, (* wtpReceiveRate_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpReceiveRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatisticsEntry.wtpSendRate
 * wtpSendRate is subid 6 of dot11WtpStatisticsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.6.1.6
 * Description:
The ap send rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSendRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSendRate_map(char **mib_wtpSendRate_val_ptr_ptr, size_t *mib_wtpSendRate_val_ptr_len_ptr, char *raw_wtpSendRate_val_ptr, size_t raw_wtpSendRate_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpSendRate_val_ptr);
    netsnmp_assert((NULL != mib_wtpSendRate_val_ptr_ptr) && (NULL != mib_wtpSendRate_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpSendRate_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpSendRate non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpSendRate_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpSendRate_val_ptr_ptr) || (*mib_wtpSendRate_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpSendRate_val_ptr_ptr = realloc( *mib_wtpSendRate_val_ptr_ptr, converted_len * sizeof(**mib_wtpSendRate_val_ptr_ptr));
        if(NULL == *mib_wtpSendRate_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpSendRate_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpSendRate_val_ptr_ptr, raw_wtpSendRate_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpSendRate_map */

/**
 * Extract the current value of the wtpSendRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSendRate_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpSendRate_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpSendRate.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpSendRate_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpSendRate_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpSendRate_get( dot11WtpStatisticsTable_rowreq_ctx *rowreq_ctx, char **wtpSendRate_val_ptr_ptr, size_t *wtpSendRate_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpSendRate_val_ptr_ptr) && (NULL != *wtpSendRate_val_ptr_ptr));
   netsnmp_assert( NULL != wtpSendRate_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatisticsTable:wtpSendRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSendRate data.
 * set (* wtpSendRate_val_ptr_ptr ) and (* wtpSendRate_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpSendRate data
     */
    if ((NULL == (* wtpSendRate_val_ptr_ptr )) ||
        ((* wtpSendRate_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpSendRate_len * sizeof((* wtpSendRate_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpSendRate data
         */
        (* wtpSendRate_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpSendRate_len * sizeof((* wtpSendRate_val_ptr_ptr )[0]));
        if(NULL == (* wtpSendRate_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpSendRate_val_ptr_len_ptr ) = rowreq_ctx->data.wtpSendRate_len * sizeof((* wtpSendRate_val_ptr_ptr )[0]);
    memcpy( (* wtpSendRate_val_ptr_ptr ), rowreq_ctx->data.wtpSendRate, (* wtpSendRate_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpSendRate_get */



/** @} */
