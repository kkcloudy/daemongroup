/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11SecurityMechTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11SecurityMechTable get routines.
 * TODO:240:M: Implement dot11SecurityMechTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11SecurityMechTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11SecurityMechTable is subid 1 of wtpSecurity.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.9.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11SecurityMechTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11SecurityMechTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wlanCurrID
 * wlanCurrID is subid 1 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.3.1.1
 * Description:
The wlan's ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wlanCurrID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wlanCurrID_dot11SecurityMechTable_map(long *mib_wlanCurrID_val_ptr, long raw_wlanCurrID_val)
{
    netsnmp_assert(NULL != mib_wlanCurrID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wlanCurrID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wlanCurrID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wlanCurrID_val_ptr) = raw_wlanCurrID_val;

    return MFD_SUCCESS;
} /* wlanCurrID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11SecurityMechTable_indexes_set_tbl_idx(dot11SecurityMechTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:dot11SecurityMechTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wlanCurrID = wlanCurrID_val;
    

    return MFD_SUCCESS;
} /* dot11SecurityMechTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11SecurityMechTable_indexes_set(dot11SecurityMechTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:dot11SecurityMechTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11SecurityMechTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wlanCurrID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11SecurityMechTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11SecurityMechTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SecurityMechEntry.wtpWirelessSecurMechName
 * wtpWirelessSecurMechName is subid 1 of dot11SecurityMechEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.9.1.1.1
 * Description:
Security mechanism name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessSecurMechName.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessSecurMechName_map(char **mib_wtpWirelessSecurMechName_val_ptr_ptr, size_t *mib_wtpWirelessSecurMechName_val_ptr_len_ptr, char *raw_wtpWirelessSecurMechName_val_ptr, size_t raw_wtpWirelessSecurMechName_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpWirelessSecurMechName_val_ptr);
    netsnmp_assert((NULL != mib_wtpWirelessSecurMechName_val_ptr_ptr) && (NULL != mib_wtpWirelessSecurMechName_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechName_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpWirelessSecurMechName non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpWirelessSecurMechName_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpWirelessSecurMechName_val_ptr_ptr) || (*mib_wtpWirelessSecurMechName_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpWirelessSecurMechName_val_ptr_ptr = realloc( *mib_wtpWirelessSecurMechName_val_ptr_ptr, converted_len * sizeof(**mib_wtpWirelessSecurMechName_val_ptr_ptr));
        if(NULL == *mib_wtpWirelessSecurMechName_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpWirelessSecurMechName_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpWirelessSecurMechName_val_ptr_ptr, raw_wtpWirelessSecurMechName_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechName_map */

/**
 * Extract the current value of the wtpWirelessSecurMechName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessSecurMechName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpWirelessSecurMechName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpWirelessSecurMechName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpWirelessSecurMechName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpWirelessSecurMechName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpWirelessSecurMechName_get( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx, char **wtpWirelessSecurMechName_val_ptr_ptr, size_t *wtpWirelessSecurMechName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpWirelessSecurMechName_val_ptr_ptr) && (NULL != *wtpWirelessSecurMechName_val_ptr_ptr));
   netsnmp_assert( NULL != wtpWirelessSecurMechName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessSecurMechName data.
 * set (* wtpWirelessSecurMechName_val_ptr_ptr ) and (* wtpWirelessSecurMechName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpWirelessSecurMechName data
     */
    if ((NULL == (* wtpWirelessSecurMechName_val_ptr_ptr )) ||
        ((* wtpWirelessSecurMechName_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpWirelessSecurMechName_len * sizeof((* wtpWirelessSecurMechName_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpWirelessSecurMechName data
         */
        (* wtpWirelessSecurMechName_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpWirelessSecurMechName_len * sizeof((* wtpWirelessSecurMechName_val_ptr_ptr )[0]));
        if(NULL == (* wtpWirelessSecurMechName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpWirelessSecurMechName_val_ptr_len_ptr ) = rowreq_ctx->data.wtpWirelessSecurMechName_len * sizeof((* wtpWirelessSecurMechName_val_ptr_ptr )[0]);
    memcpy( (* wtpWirelessSecurMechName_val_ptr_ptr ), rowreq_ctx->data.wtpWirelessSecurMechName, (* wtpWirelessSecurMechName_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechName_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SecurityMechEntry.wtpWirelessSecurMechID
 * wtpWirelessSecurMechID is subid 2 of dot11SecurityMechEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.9.1.1.2
 * Description:
Security mechanism ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessSecurMechID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessSecurMechID_map(long *mib_wtpWirelessSecurMechID_val_ptr, long raw_wtpWirelessSecurMechID_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessSecurMechID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessSecurMechID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessSecurMechID_val_ptr) = raw_wtpWirelessSecurMechID_val;

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechID_map */

/**
 * Extract the current value of the wtpWirelessSecurMechID data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessSecurMechID_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpWirelessSecurMechID_get( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx, long * wtpWirelessSecurMechID_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWirelessSecurMechID_val_ptr );


    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechID_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessSecurMechID data.
 * set (* wtpWirelessSecurMechID_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWirelessSecurMechID_val_ptr ) = rowreq_ctx->data.wtpWirelessSecurMechID;

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechID_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SecurityMechEntry.wtpWirelessSecurMechType
 * wtpWirelessSecurMechType is subid 3 of dot11SecurityMechEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.9.1.1.3
 * Description:
Security mechanism type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessSecurMechType.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessSecurMechType_map(char **mib_wtpWirelessSecurMechType_val_ptr_ptr, size_t *mib_wtpWirelessSecurMechType_val_ptr_len_ptr, char *raw_wtpWirelessSecurMechType_val_ptr, size_t raw_wtpWirelessSecurMechType_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpWirelessSecurMechType_val_ptr);
    netsnmp_assert((NULL != mib_wtpWirelessSecurMechType_val_ptr_ptr) && (NULL != mib_wtpWirelessSecurMechType_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechType_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpWirelessSecurMechType non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpWirelessSecurMechType_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpWirelessSecurMechType_val_ptr_ptr) || (*mib_wtpWirelessSecurMechType_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpWirelessSecurMechType_val_ptr_ptr = realloc( *mib_wtpWirelessSecurMechType_val_ptr_ptr, converted_len * sizeof(**mib_wtpWirelessSecurMechType_val_ptr_ptr));
        if(NULL == *mib_wtpWirelessSecurMechType_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpWirelessSecurMechType_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpWirelessSecurMechType_val_ptr_ptr, raw_wtpWirelessSecurMechType_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechType_map */

/**
 * Extract the current value of the wtpWirelessSecurMechType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessSecurMechType_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpWirelessSecurMechType_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpWirelessSecurMechType.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpWirelessSecurMechType_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpWirelessSecurMechType_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpWirelessSecurMechType_get( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx, char **wtpWirelessSecurMechType_val_ptr_ptr, size_t *wtpWirelessSecurMechType_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpWirelessSecurMechType_val_ptr_ptr) && (NULL != *wtpWirelessSecurMechType_val_ptr_ptr));
   netsnmp_assert( NULL != wtpWirelessSecurMechType_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessSecurMechType data.
 * set (* wtpWirelessSecurMechType_val_ptr_ptr ) and (* wtpWirelessSecurMechType_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpWirelessSecurMechType data
     */
    if ((NULL == (* wtpWirelessSecurMechType_val_ptr_ptr )) ||
        ((* wtpWirelessSecurMechType_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpWirelessSecurMechType_len * sizeof((* wtpWirelessSecurMechType_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpWirelessSecurMechType data
         */
        (* wtpWirelessSecurMechType_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpWirelessSecurMechType_len * sizeof((* wtpWirelessSecurMechType_val_ptr_ptr )[0]));
        if(NULL == (* wtpWirelessSecurMechType_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpWirelessSecurMechType_val_ptr_len_ptr ) = rowreq_ctx->data.wtpWirelessSecurMechType_len * sizeof((* wtpWirelessSecurMechType_val_ptr_ptr )[0]);
    memcpy( (* wtpWirelessSecurMechType_val_ptr_ptr ), rowreq_ctx->data.wtpWirelessSecurMechType, (* wtpWirelessSecurMechType_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechType_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SecurityMechEntry.wtpWirelessSecurMechEncryType
 * wtpWirelessSecurMechEncryType is subid 4 of dot11SecurityMechEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.9.1.1.4
 * Description:
Security mechanism encryption type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 4/8. Values:  NONE(0), WEP(1), AES(2), TKIP(3), SMS4(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessSecurMechEncryType.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessSecurMechEncryType_map(u_long *mib_wtpWirelessSecurMechEncryType_val_ptr, u_long raw_wtpWirelessSecurMechEncryType_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessSecurMechEncryType_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechEncryType_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessSecurMechEncryType enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpWirelessSecurMechEncryType_val) {
        case INTERNAL_WTPWIRELESSSECURMECHENCRYTYPE_NONE:
             *mib_wtpWirelessSecurMechEncryType_val_ptr = WTPWIRELESSSECURMECHENCRYTYPE_NONE;
             break;

        case INTERNAL_WTPWIRELESSSECURMECHENCRYTYPE_WEP:
             *mib_wtpWirelessSecurMechEncryType_val_ptr = WTPWIRELESSSECURMECHENCRYTYPE_WEP;
             break;

        case INTERNAL_WTPWIRELESSSECURMECHENCRYTYPE_AES:
             *mib_wtpWirelessSecurMechEncryType_val_ptr = WTPWIRELESSSECURMECHENCRYTYPE_AES;
             break;

        case INTERNAL_WTPWIRELESSSECURMECHENCRYTYPE_TKIP:
             *mib_wtpWirelessSecurMechEncryType_val_ptr = WTPWIRELESSSECURMECHENCRYTYPE_TKIP;
             break;

        case INTERNAL_WTPWIRELESSSECURMECHENCRYTYPE_SMS4:
             *mib_wtpWirelessSecurMechEncryType_val_ptr = WTPWIRELESSSECURMECHENCRYTYPE_SMS4;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpWirelessSecurMechEncryType\n", raw_wtpWirelessSecurMechEncryType_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechEncryType_map */

/**
 * Extract the current value of the wtpWirelessSecurMechEncryType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessSecurMechEncryType_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpWirelessSecurMechEncryType_get( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx, u_long * wtpWirelessSecurMechEncryType_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWirelessSecurMechEncryType_val_ptr );


    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechEncryType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessSecurMechEncryType data.
 * set (* wtpWirelessSecurMechEncryType_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWirelessSecurMechEncryType_val_ptr ) = rowreq_ctx->data.wtpWirelessSecurMechEncryType;

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechEncryType_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SecurityMechEntry.wtpWirelessSecurMechSecurPolicyKey
 * wtpWirelessSecurMechSecurPolicyKey is subid 5 of dot11SecurityMechEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.9.1.1.5
 * Description:
Security mechanism security policy key.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessSecurMechSecurPolicyKey.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessSecurMechSecurPolicyKey_map(char **mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr, size_t *mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr, char *raw_wtpWirelessSecurMechSecurPolicyKey_val_ptr, size_t raw_wtpWirelessSecurMechSecurPolicyKey_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpWirelessSecurMechSecurPolicyKey_val_ptr);
    netsnmp_assert((NULL != mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr) && (NULL != mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechSecurPolicyKey_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpWirelessSecurMechSecurPolicyKey non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpWirelessSecurMechSecurPolicyKey_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr) || (*mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr = realloc( *mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr, converted_len * sizeof(**mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr));
        if(NULL == *mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr, raw_wtpWirelessSecurMechSecurPolicyKey_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechSecurPolicyKey_map */

/**
 * Extract the current value of the wtpWirelessSecurMechSecurPolicyKey data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpWirelessSecurMechSecurPolicyKey.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpWirelessSecurMechSecurPolicyKey_get( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx, char **wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr, size_t *wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr) && (NULL != *wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr));
   netsnmp_assert( NULL != wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechSecurPolicyKey_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessSecurMechSecurPolicyKey data.
 * set (* wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr ) and (* wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpWirelessSecurMechSecurPolicyKey data
     */
    if ((NULL == (* wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr )) ||
        ((* wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpWirelessSecurMechSecurPolicyKey_len * sizeof((* wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpWirelessSecurMechSecurPolicyKey data
         */
        (* wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpWirelessSecurMechSecurPolicyKey_len * sizeof((* wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr )[0]));
        if(NULL == (* wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr ) = rowreq_ctx->data.wtpWirelessSecurMechSecurPolicyKey_len * sizeof((* wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr )[0]);
    memcpy( (* wtpWirelessSecurMechSecurPolicyKey_val_ptr_ptr ), rowreq_ctx->data.wtpWirelessSecurMechSecurPolicyKey, (* wtpWirelessSecurMechSecurPolicyKey_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechSecurPolicyKey_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SecurityMechEntry.wtpWirelessSecurMechKeyInputType
 * wtpWirelessSecurMechKeyInputType is subid 6 of dot11SecurityMechEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.9.1.1.6
 * Description:
Security mechanism key input type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessSecurMechKeyInputType.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessSecurMechKeyInputType_map(char **mib_wtpWirelessSecurMechKeyInputType_val_ptr_ptr, size_t *mib_wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr, char *raw_wtpWirelessSecurMechKeyInputType_val_ptr, size_t raw_wtpWirelessSecurMechKeyInputType_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpWirelessSecurMechKeyInputType_val_ptr);
    netsnmp_assert((NULL != mib_wtpWirelessSecurMechKeyInputType_val_ptr_ptr) && (NULL != mib_wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechKeyInputType_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpWirelessSecurMechKeyInputType non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpWirelessSecurMechKeyInputType_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpWirelessSecurMechKeyInputType_val_ptr_ptr) || (*mib_wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpWirelessSecurMechKeyInputType_val_ptr_ptr = realloc( *mib_wtpWirelessSecurMechKeyInputType_val_ptr_ptr, converted_len * sizeof(**mib_wtpWirelessSecurMechKeyInputType_val_ptr_ptr));
        if(NULL == *mib_wtpWirelessSecurMechKeyInputType_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpWirelessSecurMechKeyInputType_val_ptr_ptr, raw_wtpWirelessSecurMechKeyInputType_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechKeyInputType_map */

/**
 * Extract the current value of the wtpWirelessSecurMechKeyInputType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessSecurMechKeyInputType_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpWirelessSecurMechKeyInputType.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpWirelessSecurMechKeyInputType_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpWirelessSecurMechKeyInputType_get( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx, char **wtpWirelessSecurMechKeyInputType_val_ptr_ptr, size_t *wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpWirelessSecurMechKeyInputType_val_ptr_ptr) && (NULL != *wtpWirelessSecurMechKeyInputType_val_ptr_ptr));
   netsnmp_assert( NULL != wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechKeyInputType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessSecurMechKeyInputType data.
 * set (* wtpWirelessSecurMechKeyInputType_val_ptr_ptr ) and (* wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpWirelessSecurMechKeyInputType data
     */
    if ((NULL == (* wtpWirelessSecurMechKeyInputType_val_ptr_ptr )) ||
        ((* wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpWirelessSecurMechKeyInputType_len * sizeof((* wtpWirelessSecurMechKeyInputType_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpWirelessSecurMechKeyInputType data
         */
        (* wtpWirelessSecurMechKeyInputType_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpWirelessSecurMechKeyInputType_len * sizeof((* wtpWirelessSecurMechKeyInputType_val_ptr_ptr )[0]));
        if(NULL == (* wtpWirelessSecurMechKeyInputType_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr ) = rowreq_ctx->data.wtpWirelessSecurMechKeyInputType_len * sizeof((* wtpWirelessSecurMechKeyInputType_val_ptr_ptr )[0]);
    memcpy( (* wtpWirelessSecurMechKeyInputType_val_ptr_ptr ), rowreq_ctx->data.wtpWirelessSecurMechKeyInputType, (* wtpWirelessSecurMechKeyInputType_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpWirelessSecurMechKeyInputType_get */



/** @} */
