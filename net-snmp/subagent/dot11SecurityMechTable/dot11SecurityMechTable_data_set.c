/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11SecurityMechTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11SecurityMechTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11SecurityMechTable is subid 1 of wtpSecurity.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.9.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SecurityMechTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11SecurityMechTable_undo_setup( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11SecurityMechTable:dot11SecurityMechTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11SecurityMechTable undo.
     * set up dot11SecurityMechTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11SecurityMechTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SecurityMechTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11SecurityMechTable_undo_cleanup( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11SecurityMechTable:dot11SecurityMechTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11SecurityMechTable undo.
     */

    return rc;
} /* dot11SecurityMechTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11SecurityMechTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11SecurityMechTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11SecurityMechTable_commit( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11SecurityMechTable:dot11SecurityMechTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11SecurityMechTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WTPWIRELESSSECURMECHID) {
       save_flags &= ~FLAG_WTPWIRELESSSECURMECHID; /* clear wtpWirelessSecurMechID */
       /*
        * TODO:482:o: |-> commit column wtpWirelessSecurMechID.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11SecurityMechTable column wtpWirelessSecurMechID commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessSecurMechID
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSSECURMECHID;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSSECURMECHENCRYTYPE) {
       save_flags &= ~FLAG_WTPWIRELESSSECURMECHENCRYTYPE; /* clear wtpWirelessSecurMechEncryType */
       /*
        * TODO:482:o: |-> commit column wtpWirelessSecurMechEncryType.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11SecurityMechTable column wtpWirelessSecurMechEncryType commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessSecurMechEncryType
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSSECURMECHENCRYTYPE;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11SecurityMechTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11SecurityMechTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11SecurityMechTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11SecurityMechTable_undo_commit( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11SecurityMechTable:dot11SecurityMechTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11SecurityMechTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11SecurityMechTable_undo_commit */

/*
 * TODO:420:r: Implement dot11SecurityMechTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wlanCurrID
 * wlanCurrID is subid 1 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.3.1.1
 * Description:
The wlan's ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/*
 * TODO:440:M: Implement dot11SecurityMechTable node value checks.
 * TODO:450:M: Implement dot11SecurityMechTable undo functions.
 * TODO:460:M: Implement dot11SecurityMechTable set functions.
 * TODO:480:M: Implement dot11SecurityMechTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SecurityMechEntry.wtpWirelessSecurMechID
 * wtpWirelessSecurMechID is subid 2 of dot11SecurityMechEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.9.1.1.2
 * Description:
Security mechanism ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessSecurMechID_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11SecurityMechTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessSecurMechID_check_value( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx, long wtpWirelessSecurMechID_val)
{
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechID_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessSecurMechID value.
     */

    return MFD_SUCCESS; /* wtpWirelessSecurMechID value not illegal */
} /* wtpWirelessSecurMechID_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SecurityMechTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11SecurityMechTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessSecurMechID_undo_setup( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechID_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessSecurMechID undo.
     */
    /*
     * copy wtpWirelessSecurMechID data
     * set rowreq_ctx->undo->wtpWirelessSecurMechID from rowreq_ctx->data.wtpWirelessSecurMechID
     */
    rowreq_ctx->undo->wtpWirelessSecurMechID = rowreq_ctx->data.wtpWirelessSecurMechID;


    return MFD_SUCCESS;
} /* wtpWirelessSecurMechID_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessSecurMechID_val
 *        A long containing the new value.
 */
int
wtpWirelessSecurMechID_set( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx, long wtpWirelessSecurMechID_val )
{

    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechID_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessSecurMechID reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpWirelessSecurMechID value.
     * set wtpWirelessSecurMechID value in rowreq_ctx->data
     */
    
	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
    int rc = MFD_ERROR;
    int instance_id = 0;
	//instance_id = get_main_instance_id();
    int ret = 0;
	int ret_dis = 0;
	int ret_en = 0;
	ret_dis = config_wlan_service(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.localwlanCurrID, "disable");
    if(1 == ret_dis)
    {
		ret = apply_wlanID(rowreq_ctx->data.parameter, connection, wtpWirelessSecurMechID_val, rowreq_ctx->data.localwlanCurrID);
		if(1 == ret)
		{
		  rowreq_ctx->data.wtpWirelessSecurMechID = wtpWirelessSecurMechID_val;
		  rc = MFD_SUCCESS;
		}
		else
		{
		  rc = MFD_ERROR;
		}
    }
    else
    {
	  rc = MFD_ERROR;
	}
	ret_en = config_wlan_service(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.localwlanCurrID, "enable");
	if(1 == ret_en)
	{
	  rc = MFD_SUCCESS;
	}
	else
	{
	  rc = MFD_ERROR;
	}
    return rc;
} /* wtpWirelessSecurMechID_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessSecurMechID_undo( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechID_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessSecurMechID undo.
     */
    /*
     * copy wtpWirelessSecurMechID data
     * set rowreq_ctx->data.wtpWirelessSecurMechID from rowreq_ctx->undo->wtpWirelessSecurMechID
     */
    rowreq_ctx->data.wtpWirelessSecurMechID = rowreq_ctx->undo->wtpWirelessSecurMechID;

    
    return MFD_SUCCESS;
} /* wtpWirelessSecurMechID_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SecurityMechEntry.wtpWirelessSecurMechEncryType
 * wtpWirelessSecurMechEncryType is subid 4 of dot11SecurityMechEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.9.1.1.4
 * Description:
Security mechanism encryption type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 4/8. Values:  NONE(0), WEP(1), AES(2), TKIP(3), SMS4(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessSecurMechEncryType_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11SecurityMechTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  NONE(0), WEP(1), AES(2), TKIP(3), SMS4(4)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessSecurMechEncryType_check_value( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx, u_long wtpWirelessSecurMechEncryType_val)
{
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechEncryType_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessSecurMechEncryType value.
     */

    return MFD_SUCCESS; /* wtpWirelessSecurMechEncryType value not illegal */
} /* wtpWirelessSecurMechEncryType_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SecurityMechTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11SecurityMechTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessSecurMechEncryType_undo_setup( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechEncryType_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessSecurMechEncryType undo.
     */
    /*
     * copy wtpWirelessSecurMechEncryType data
     * set rowreq_ctx->undo->wtpWirelessSecurMechEncryType from rowreq_ctx->data.wtpWirelessSecurMechEncryType
     */
    rowreq_ctx->undo->wtpWirelessSecurMechEncryType = rowreq_ctx->data.wtpWirelessSecurMechEncryType;


    return MFD_SUCCESS;
} /* wtpWirelessSecurMechEncryType_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessSecurMechEncryType_val
 *        A long containing the new value.
 */
int
wtpWirelessSecurMechEncryType_set( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx, u_long wtpWirelessSecurMechEncryType_val )
{

    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechEncryType_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessSecurMechEncryType reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpWirelessSecurMechEncryType_val) {
        case WTPWIRELESSSECURMECHENCRYTYPE_NONE:
             wtpWirelessSecurMechEncryType_val = INTERNAL_WTPWIRELESSSECURMECHENCRYTYPE_NONE;
             break;

        case WTPWIRELESSSECURMECHENCRYTYPE_WEP:
             wtpWirelessSecurMechEncryType_val = INTERNAL_WTPWIRELESSSECURMECHENCRYTYPE_WEP;
             break;

        case WTPWIRELESSSECURMECHENCRYTYPE_AES:
             wtpWirelessSecurMechEncryType_val = INTERNAL_WTPWIRELESSSECURMECHENCRYTYPE_AES;
             break;

        case WTPWIRELESSSECURMECHENCRYTYPE_TKIP:
             wtpWirelessSecurMechEncryType_val = INTERNAL_WTPWIRELESSSECURMECHENCRYTYPE_TKIP;
             break;

        case WTPWIRELESSSECURMECHENCRYTYPE_SMS4:
             wtpWirelessSecurMechEncryType_val = INTERNAL_WTPWIRELESSSECURMECHENCRYTYPE_SMS4;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpWirelessSecurMechEncryType\n", wtpWirelessSecurMechEncryType_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpWirelessSecurMechEncryType value.
     * set wtpWirelessSecurMechEncryType value in rowreq_ctx->data
     */
    
	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
    int ret_dis = 0;
	int ret_en = 0;
    int ret = 0;
	int ret1 = 0;
	int ret2 = 0;
    int rc = MFD_ERROR;
	int rc1 = MFD_ERROR;
    int instance_id = 0;
	//instance_id = get_main_instance_id();
	ret_dis = config_wlan_service(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.localwlanCurrID,"disable");
    char encr_type[30] = {0};
	memset(encr_type,0,30);
	if(1 == ret_dis)
	{
		if (0 == wtpWirelessSecurMechEncryType_val)
		    {
			strncpy(encr_type,"none",sizeof(encr_type)-1);	
			ret = encryption_type(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpWirelessSecurMechID,encr_type);
		    }
		else if (1 == wtpWirelessSecurMechEncryType_val)
			{
			strncpy(encr_type,"WEP",sizeof(encr_type)-1);
			ret = encryption_type(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpWirelessSecurMechID,encr_type);
			if(ret == 1)
				ret1 = security_key(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpWirelessSecurMechID,"12345","ascii");
			}
		else if (2 == wtpWirelessSecurMechEncryType_val)
			{
			strncpy(encr_type,"AES",sizeof(encr_type)-1);
			ret = encryption_type(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpWirelessSecurMechID,encr_type);
			if(ret == 1)
				ret1 = security_key(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpWirelessSecurMechID,"00000000","ascii");
			}
		else if (3 == wtpWirelessSecurMechEncryType_val)
			{
			strncpy(encr_type,"TKIP",sizeof(encr_type)-1);
			ret = encryption_type(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpWirelessSecurMechID,encr_type);
			if(ret == 1)
				ret1 = security_key(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpWirelessSecurMechID,"00000000","ascii");
			}
		else if (4 == wtpWirelessSecurMechEncryType_val)
			{
			strncpy(encr_type,"SMS4",sizeof(encr_type)-1);
			ret = encryption_type(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpWirelessSecurMechID,encr_type);
		    }
		else 
		{		
			rc = MFD_ERROR;
		}

		if((4 == wtpWirelessSecurMechEncryType_val)||(0 == wtpWirelessSecurMechEncryType_val))
		{	
			if(1 == ret)
				{rowreq_ctx->data.wtpWirelessSecurMechEncryType = wtpWirelessSecurMechEncryType_val;
				rc = MFD_SUCCESS;}
			else
				rc = MFD_ERROR;
		}
		else if((1 == wtpWirelessSecurMechEncryType_val)||(2 == wtpWirelessSecurMechEncryType_val)||(3 == wtpWirelessSecurMechEncryType_val))
		{
			if((1 == ret)&&(ret1 ==1))
				{rowreq_ctx->data.wtpWirelessSecurMechEncryType = wtpWirelessSecurMechEncryType_val;
				rc = MFD_SUCCESS;}
			else
				rc = MFD_ERROR;
		}
		else
		{
			rc = MFD_ERROR;
		}

	}
	else
	{
		return	MFD_ERROR;
	}
	ret2 = apply_wlanID(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpWirelessSecurMechID,rowreq_ctx->data.localwlanCurrID);
	ret_en = config_wlan_service(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.localwlanCurrID,"enable");
    if((1 == ret_en)&&(ret2 == 1)&&(rc == MFD_SUCCESS))
    {
		rc1 = MFD_SUCCESS;
	}
	else
	{
	    rc1 = MFD_ERROR;
	}
    return rc1;
} /* wtpWirelessSecurMechEncryType_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessSecurMechEncryType_undo( dot11SecurityMechTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11SecurityMechTable:wtpWirelessSecurMechEncryType_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessSecurMechEncryType undo.
     */
    /*
     * copy wtpWirelessSecurMechEncryType data
     * set rowreq_ctx->data.wtpWirelessSecurMechEncryType from rowreq_ctx->undo->wtpWirelessSecurMechEncryType
     */
    rowreq_ctx->data.wtpWirelessSecurMechEncryType = rowreq_ctx->undo->wtpWirelessSecurMechEncryType;

    
    return MFD_SUCCESS;
} /* wtpWirelessSecurMechEncryType_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11SecurityMechTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11SecurityMechTable if you don't have dependencies)
 */
int
dot11SecurityMechTable_check_dependencies(dot11SecurityMechTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11SecurityMechTable:dot11SecurityMechTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11SecurityMechTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11SecurityMechTable_check_dependencies */

/** @} */
