/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpWlanStatsTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpWlanStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpWlanStatsTable is subid 3 of wtpWlan.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWlanStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11WtpWlanStatsTable_undo_setup( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:dot11WtpWlanStatsTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11WtpWlanStatsTable undo.
     * set up dot11WtpWlanStatsTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11WtpWlanStatsTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWlanStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWlanStatsTable_undo_cleanup( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:dot11WtpWlanStatsTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11WtpWlanStatsTable undo.
     */

    return rc;
} /* dot11WtpWlanStatsTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpWlanStatsTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpWlanStatsTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWlanStatsTable_commit( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:dot11WtpWlanStatsTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11WtpWlanStatsTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WTPSSIDBROADCAST) {
       save_flags &= ~FLAG_WTPSSIDBROADCAST; /* clear wtpSsidBroadcast */
       /*
        * TODO:482:o: |-> commit column wtpSsidBroadcast.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWlanStatsTable column wtpSsidBroadcast commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpSsidBroadcast
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPSSIDBROADCAST;
       }
    }

    if (save_flags & FLAG_WTPCONFSECURMECH) {
       save_flags &= ~FLAG_WTPCONFSECURMECH; /* clear wtpConfSecurMech */
       /*
        * TODO:482:o: |-> commit column wtpConfSecurMech.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWlanStatsTable column wtpConfSecurMech commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpConfSecurMech
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPCONFSECURMECH;
       }
    }

    if (save_flags & FLAG_WTPSSIDPRIORITY) {
       save_flags &= ~FLAG_WTPSSIDPRIORITY; /* clear wtpSSIDPriority */
       /*
        * TODO:482:o: |-> commit column wtpSSIDPriority.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWlanStatsTable column wtpSSIDPriority commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpSSIDPriority
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPSSIDPRIORITY;
       }
    }

    if (save_flags & FLAG_WTPATTACHEDFLOWTHRESHOLD) {
       save_flags &= ~FLAG_WTPATTACHEDFLOWTHRESHOLD; /* clear wtpAttachedFlowThreshold */
       /*
        * TODO:482:o: |-> commit column wtpAttachedFlowThreshold.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWlanStatsTable column wtpAttachedFlowThreshold commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpAttachedFlowThreshold
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPATTACHEDFLOWTHRESHOLD;
       }
    }

    if (save_flags & FLAG_WTPUSRNUMAPPENDTHRESHOLD) {
       save_flags &= ~FLAG_WTPUSRNUMAPPENDTHRESHOLD; /* clear wtpUsrNumAppendThreshold */
       /*
        * TODO:482:o: |-> commit column wtpUsrNumAppendThreshold.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWlanStatsTable column wtpUsrNumAppendThreshold commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpUsrNumAppendThreshold
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPUSRNUMAPPENDTHRESHOLD;
       }
    }

    if (save_flags & FLAG_WTPL2INSULATE) {
       save_flags &= ~FLAG_WTPL2INSULATE; /* clear wtpL2Insulate */
       /*
        * TODO:482:o: |-> commit column wtpL2Insulate.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWlanStatsTable column wtpL2Insulate commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpL2Insulate
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPL2INSULATE;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11WtpWlanStatsTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpWlanStatsTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpWlanStatsTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWlanStatsTable_undo_commit( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:dot11WtpWlanStatsTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11WtpWlanStatsTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11WtpWlanStatsTable_undo_commit */

/*
 * TODO:420:r: Implement dot11WtpWlanStatsTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/*
 * TODO:440:M: Implement dot11WtpWlanStatsTable node value checks.
 * TODO:450:M: Implement dot11WtpWlanStatsTable undo functions.
 * TODO:460:M: Implement dot11WtpWlanStatsTable set functions.
 * TODO:480:M: Implement dot11WtpWlanStatsTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpSsidBroadcast
 * wtpSsidBroadcast is subid 5 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.5
 * Description:
The ssid is whether broadcast.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidBroadcast_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWlanStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpSsidBroadcast_check_value( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long wtpSsidBroadcast_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSsidBroadcast_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpSsidBroadcast value.
     */

    return MFD_SUCCESS; /* wtpSsidBroadcast value not illegal */
} /* wtpSsidBroadcast_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWlanStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWlanStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpSsidBroadcast_undo_setup( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSsidBroadcast_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpSsidBroadcast undo.
     */
    /*
     * copy wtpSsidBroadcast data
     * set rowreq_ctx->undo->wtpSsidBroadcast from rowreq_ctx->data.wtpSsidBroadcast
     */
    rowreq_ctx->undo->wtpSsidBroadcast = rowreq_ctx->data.wtpSsidBroadcast;


    return MFD_SUCCESS;
} /* wtpSsidBroadcast_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpSsidBroadcast_val
 *        A long containing the new value.
 */
int
wtpSsidBroadcast_set( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long wtpSsidBroadcast_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSsidBroadcast_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpSsidBroadcast reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpSsidBroadcast_val) {
        case TRUTHVALUE_TRUE:
             wtpSsidBroadcast_val = INTERNAL_WTPSSIDBROADCAST_TRUE;
             break;

        case TRUTHVALUE_FALSE:
             wtpSsidBroadcast_val = INTERNAL_WTPSSIDBROADCAST_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpSsidBroadcast\n", wtpSsidBroadcast_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpSsidBroadcast value.
     * set wtpSsidBroadcast value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int rc = MFD_ERROR;
	int ret = 0;
	char Hessid[5] = { 0 };
	memset(Hessid, 0, 5);
	if(wtpSsidBroadcast_val == INTERNAL_WTPSSIDBROADCAST_TRUE)
	{
		strncpy(Hessid,"no",sizeof(Hessid)-1);
	}
	else
	{
		strncpy(Hessid,"yes",sizeof(Hessid)-1);
	}	

	ret = set_hideessid(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.localwlanCurrID,Hessid);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpSsidBroadcast = wtpSsidBroadcast_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpSsidBroadcast_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpSsidBroadcast_undo( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSsidBroadcast_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpSsidBroadcast undo.
     */
    /*
     * copy wtpSsidBroadcast data
     * set rowreq_ctx->data.wtpSsidBroadcast from rowreq_ctx->undo->wtpSsidBroadcast
     */
    rowreq_ctx->data.wtpSsidBroadcast = rowreq_ctx->undo->wtpSsidBroadcast;

    
    return MFD_SUCCESS;
} /* wtpSsidBroadcast_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpConfSecurMech
 * wtpConfSecurMech is subid 6 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.6
 * Description:
The configed mechanism such as open system or shared key(o means no secyurity mechanism).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 6/16. Values:  openSystem(0), sharedKey(1), dot11x(2), WPA_P(3), WPA2_P(4), WPA_E(5), WPA2_E(6), MD5(7), WAPI_PSK(8), WAPI_AUTH(9)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpConfSecurMech_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWlanStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  openSystem(0), sharedKey(1), dot11x(2), WPA_P(3), WPA2_P(4), WPA_E(5), WPA2_E(6), MD5(7), WAPI_PSK(8), WAPI_AUTH(9)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpConfSecurMech_check_value( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long wtpConfSecurMech_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpConfSecurMech_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpConfSecurMech value.
     */

    return MFD_SUCCESS; /* wtpConfSecurMech value not illegal */
} /* wtpConfSecurMech_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWlanStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWlanStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpConfSecurMech_undo_setup( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpConfSecurMech_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpConfSecurMech undo.
     */
    /*
     * copy wtpConfSecurMech data
     * set rowreq_ctx->undo->wtpConfSecurMech from rowreq_ctx->data.wtpConfSecurMech
     */
    rowreq_ctx->undo->wtpConfSecurMech = rowreq_ctx->data.wtpConfSecurMech;


    return MFD_SUCCESS;
} /* wtpConfSecurMech_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpConfSecurMech_val
 *        A long containing the new value.
 */
int
wtpConfSecurMech_set( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long wtpConfSecurMech_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpConfSecurMech_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpConfSecurMech reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpConfSecurMech_val) {
        case WTPCONFSECURMECH_OPENSYSTEM:
             wtpConfSecurMech_val = INTERNAL_WTPCONFSECURMECH_OPENSYSTEM;
             break;

        case WTPCONFSECURMECH_SHAREDKEY:
             wtpConfSecurMech_val = INTERNAL_WTPCONFSECURMECH_SHAREDKEY;
             break;

        case WTPCONFSECURMECH_DOT11X:
             wtpConfSecurMech_val = INTERNAL_WTPCONFSECURMECH_DOT11X;
             break;

        case WTPCONFSECURMECH_WPA_P:
             wtpConfSecurMech_val = INTERNAL_WTPCONFSECURMECH_WPA_P;
             break;

        case WTPCONFSECURMECH_WPA2_P:
             wtpConfSecurMech_val = INTERNAL_WTPCONFSECURMECH_WPA2_P;
             break;

        case WTPCONFSECURMECH_WPA_E:
             wtpConfSecurMech_val = INTERNAL_WTPCONFSECURMECH_WPA_E;
             break;

        case WTPCONFSECURMECH_WPA2_E:
             wtpConfSecurMech_val = INTERNAL_WTPCONFSECURMECH_WPA2_E;
             break;

        case WTPCONFSECURMECH_MD5:
             wtpConfSecurMech_val = INTERNAL_WTPCONFSECURMECH_MD5;
             break;

        case WTPCONFSECURMECH_WAPI_PSK:
             wtpConfSecurMech_val = INTERNAL_WTPCONFSECURMECH_WAPI_PSK;
             break;

        case WTPCONFSECURMECH_WAPI_AUTH:
             wtpConfSecurMech_val = INTERNAL_WTPCONFSECURMECH_WAPI_AUTH;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpConfSecurMech\n", wtpConfSecurMech_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpConfSecurMech value.
     * set wtpConfSecurMech value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = apply_wlanID(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.SecurityID,rowreq_ctx->data.localwlanCurrID);	
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpConfSecurMech = wtpConfSecurMech_val;		
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpConfSecurMech_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpConfSecurMech_undo( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpConfSecurMech_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpConfSecurMech undo.
     */
    /*
     * copy wtpConfSecurMech data
     * set rowreq_ctx->data.wtpConfSecurMech from rowreq_ctx->undo->wtpConfSecurMech
     */
    rowreq_ctx->data.wtpConfSecurMech = rowreq_ctx->undo->wtpConfSecurMech;

    
    return MFD_SUCCESS;
} /* wtpConfSecurMech_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpSSIDPriority
 * wtpSSIDPriority is subid 11 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.11
 * Description:
SSID priority
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSSIDPriority_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWlanStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpSSIDPriority_check_value( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long wtpSSIDPriority_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSSIDPriority_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpSSIDPriority value.
     */

    return MFD_SUCCESS; /* wtpSSIDPriority value not illegal */
} /* wtpSSIDPriority_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWlanStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWlanStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpSSIDPriority_undo_setup( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSSIDPriority_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpSSIDPriority undo.
     */
    /*
     * copy wtpSSIDPriority data
     * set rowreq_ctx->undo->wtpSSIDPriority from rowreq_ctx->data.wtpSSIDPriority
     */
    rowreq_ctx->undo->wtpSSIDPriority = rowreq_ctx->data.wtpSSIDPriority;


    return MFD_SUCCESS;
} /* wtpSSIDPriority_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpSSIDPriority_val
 *        A long containing the new value.
 */
int
wtpSSIDPriority_set( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long wtpSSIDPriority_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSSIDPriority_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpSSIDPriority reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpSSIDPriority value.
     * set wtpSSIDPriority value in rowreq_ctx->data
     */
    rowreq_ctx->data.wtpSSIDPriority = wtpSSIDPriority_val;

    return MFD_SUCCESS;
} /* wtpSSIDPriority_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpSSIDPriority_undo( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSSIDPriority_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpSSIDPriority undo.
     */
    /*
     * copy wtpSSIDPriority data
     * set rowreq_ctx->data.wtpSSIDPriority from rowreq_ctx->undo->wtpSSIDPriority
     */
    rowreq_ctx->data.wtpSSIDPriority = rowreq_ctx->undo->wtpSSIDPriority;

    
    return MFD_SUCCESS;
} /* wtpSSIDPriority_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpAttachedFlowThreshold
 * wtpAttachedFlowThreshold is subid 12 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.12
 * Description:
Traffic load balancing threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 30;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAttachedFlowThreshold_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWlanStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 30
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpAttachedFlowThreshold_check_value( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long wtpAttachedFlowThreshold_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpAttachedFlowThreshold_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpAttachedFlowThreshold value.
     */

    return MFD_SUCCESS; /* wtpAttachedFlowThreshold value not illegal */
} /* wtpAttachedFlowThreshold_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWlanStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWlanStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpAttachedFlowThreshold_undo_setup( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpAttachedFlowThreshold_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpAttachedFlowThreshold undo.
     */
    /*
     * copy wtpAttachedFlowThreshold data
     * set rowreq_ctx->undo->wtpAttachedFlowThreshold from rowreq_ctx->data.wtpAttachedFlowThreshold
     */
    rowreq_ctx->undo->wtpAttachedFlowThreshold = rowreq_ctx->data.wtpAttachedFlowThreshold;


    return MFD_SUCCESS;
} /* wtpAttachedFlowThreshold_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpAttachedFlowThreshold_val
 *        A long containing the new value.
 */
int
wtpAttachedFlowThreshold_set( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long wtpAttachedFlowThreshold_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpAttachedFlowThreshold_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpAttachedFlowThreshold reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpAttachedFlowThreshold value.
     * set wtpAttachedFlowThreshold value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char parameter[5] = { 0 };
	memset(parameter, 0, 5);
	snprintf(parameter,sizeof(parameter)-1,"%d",wtpAttachedFlowThreshold_val);

	ret = config_wlan_flow_balance_parameter(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.localwlanCurrID,parameter);	
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpAttachedFlowThreshold = wtpAttachedFlowThreshold_val;
		rc = MFD_SUCCESS;
	}
	else
	{		
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpAttachedFlowThreshold_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpAttachedFlowThreshold_undo( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpAttachedFlowThreshold_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpAttachedFlowThreshold undo.
     */
    /*
     * copy wtpAttachedFlowThreshold data
     * set rowreq_ctx->data.wtpAttachedFlowThreshold from rowreq_ctx->undo->wtpAttachedFlowThreshold
     */
    rowreq_ctx->data.wtpAttachedFlowThreshold = rowreq_ctx->undo->wtpAttachedFlowThreshold;

    
    return MFD_SUCCESS;
} /* wtpAttachedFlowThreshold_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpUsrNumAppendThreshold
 * wtpUsrNumAppendThreshold is subid 13 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.13
 * Description:
Load balancing the number of users threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 10;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUsrNumAppendThreshold_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWlanStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 10
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpUsrNumAppendThreshold_check_value( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long wtpUsrNumAppendThreshold_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpUsrNumAppendThreshold_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpUsrNumAppendThreshold value.
     */

    return MFD_SUCCESS; /* wtpUsrNumAppendThreshold value not illegal */
} /* wtpUsrNumAppendThreshold_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWlanStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWlanStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpUsrNumAppendThreshold_undo_setup( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpUsrNumAppendThreshold_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpUsrNumAppendThreshold undo.
     */
    /*
     * copy wtpUsrNumAppendThreshold data
     * set rowreq_ctx->undo->wtpUsrNumAppendThreshold from rowreq_ctx->data.wtpUsrNumAppendThreshold
     */
    rowreq_ctx->undo->wtpUsrNumAppendThreshold = rowreq_ctx->data.wtpUsrNumAppendThreshold;


    return MFD_SUCCESS;
} /* wtpUsrNumAppendThreshold_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpUsrNumAppendThreshold_val
 *        A long containing the new value.
 */
int
wtpUsrNumAppendThreshold_set( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long wtpUsrNumAppendThreshold_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpUsrNumAppendThreshold_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpUsrNumAppendThreshold reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpUsrNumAppendThreshold value.
     * set wtpUsrNumAppendThreshold value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char parameter[5] = { 0 };
	memset(parameter, 0, 5);
	snprintf(parameter,sizeof(parameter)-1,"%d",wtpUsrNumAppendThreshold_val);

	ret = config_wlan_number_balance_parameter(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.localwlanCurrID,parameter);	
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpUsrNumAppendThreshold = wtpUsrNumAppendThreshold_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpUsrNumAppendThreshold_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpUsrNumAppendThreshold_undo( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpUsrNumAppendThreshold_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpUsrNumAppendThreshold undo.
     */
    /*
     * copy wtpUsrNumAppendThreshold data
     * set rowreq_ctx->data.wtpUsrNumAppendThreshold from rowreq_ctx->undo->wtpUsrNumAppendThreshold
     */
    rowreq_ctx->data.wtpUsrNumAppendThreshold = rowreq_ctx->undo->wtpUsrNumAppendThreshold;

    
    return MFD_SUCCESS;
} /* wtpUsrNumAppendThreshold_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpL2Insulate
 * wtpL2Insulate is subid 14 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.14
 * Description:
L2 insulate whether open.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  close(0), open(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpL2Insulate_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWlanStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  close(0), open(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpL2Insulate_check_value( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long wtpL2Insulate_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpL2Insulate_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpL2Insulate value.
     */

    return MFD_SUCCESS; /* wtpL2Insulate value not illegal */
} /* wtpL2Insulate_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWlanStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWlanStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpL2Insulate_undo_setup( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpL2Insulate_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpL2Insulate undo.
     */
    /*
     * copy wtpL2Insulate data
     * set rowreq_ctx->undo->wtpL2Insulate from rowreq_ctx->data.wtpL2Insulate
     */
    rowreq_ctx->undo->wtpL2Insulate = rowreq_ctx->data.wtpL2Insulate;


    return MFD_SUCCESS;
} /* wtpL2Insulate_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpL2Insulate_val
 *        A long containing the new value.
 */
int
wtpL2Insulate_set( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long wtpL2Insulate_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpL2Insulate_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

	char radioL2Insulate[10] = {0};

/*
 * TODO:245:o: |-> Implement wtpL2Insulate reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpL2Insulate_val) {
        case WTPL2INSULATE_CLOSE:
             wtpL2Insulate_val = INTERNAL_WTPL2INSULATE_CLOSE;
			 strncpy(radioL2Insulate, "disable", strlen("disable"));
             break;

        case WTPL2INSULATE_OPEN:
             wtpL2Insulate_val = INTERNAL_WTPL2INSULATE_OPEN;
			 strncpy(radioL2Insulate, "enable", strlen("enable"));
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpL2Insulate\n", wtpL2Insulate_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpL2Insulate value.
     * set wtpL2Insulate value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	unsigned int radioid = rowreq_ctx->data.wtpCurrID * 4;
	char wlanid[5] = { 0 };
	snprintf(wlanid, sizeof(wlanid)-1, "%d", rowreq_ctx->data.localwlanCurrID);

	ret = set_radio_l2_isolation_func(rowreq_ctx->data.parameter, connection, radioid, wlanid, radioL2Insulate);	
	if(ret == 1)
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rowreq_ctx->data.wtpL2Insulate = wtpL2Insulate_val;
		rc = MFD_SUCCESS;
	}
	else
	{						
		rc = MFD_ERROR;
	}
    

    return MFD_SUCCESS;
} /* wtpL2Insulate_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpL2Insulate_undo( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpL2Insulate_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpL2Insulate undo.
     */
    /*
     * copy wtpL2Insulate data
     * set rowreq_ctx->data.wtpL2Insulate from rowreq_ctx->undo->wtpL2Insulate
     */
    rowreq_ctx->data.wtpL2Insulate = rowreq_ctx->undo->wtpL2Insulate;

    
    return MFD_SUCCESS;
} /* wtpL2Insulate_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpWlanStatsTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11WtpWlanStatsTable if you don't have dependencies)
 */
int
dot11WtpWlanStatsTable_check_dependencies(dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11WtpWlanStatsTable:dot11WtpWlanStatsTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11WtpWlanStatsTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11WtpWlanStatsTable_check_dependencies */

/** @} */
