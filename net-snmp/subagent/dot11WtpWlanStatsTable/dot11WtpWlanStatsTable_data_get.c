/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpWlanStatsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpWlanStatsTable get routines.
 * TODO:240:M: Implement dot11WtpWlanStatsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpWlanStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpWlanStatsTable is subid 3 of wtpWlan.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpWlanStatsTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11WtpWlanStatsTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wlanCurrID
 * wlanCurrID is subid 1 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.1
 * Description:
The wlan's ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wlanCurrID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wlanCurrID_dot11WtpWlanStatsTable_map(long *mib_wlanCurrID_val_ptr, long raw_wlanCurrID_val)
{
    netsnmp_assert(NULL != mib_wlanCurrID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wlanCurrID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wlanCurrID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wlanCurrID_val_ptr) = raw_wlanCurrID_val;

    return MFD_SUCCESS;
} /* wlanCurrID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpWlanStatsTable_indexes_set_tbl_idx(dot11WtpWlanStatsTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:dot11WtpWlanStatsTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wlanCurrID = wlanCurrID_val;
    

    return MFD_SUCCESS;
} /* dot11WtpWlanStatsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpWlanStatsTable_indexes_set(dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:dot11WtpWlanStatsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpWlanStatsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wlanCurrID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpWlanStatsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpWlanStatsTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpEAPAuthenSupport
 * wtpEAPAuthenSupport is subid 2 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.2
 * Description:
The EPA module whether open.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  enable_EAP_Authentication(0), disable_EAP_Authentication(1), not_Supporting_EAP_Authentication(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpEAPAuthenSupport.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpEAPAuthenSupport_map(u_long *mib_wtpEAPAuthenSupport_val_ptr, u_long raw_wtpEAPAuthenSupport_val)
{
    netsnmp_assert(NULL != mib_wtpEAPAuthenSupport_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpEAPAuthenSupport_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpEAPAuthenSupport enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpEAPAuthenSupport_val) {
        case INTERNAL_WTPEAPAUTHENSUPPORT_ENABLE_EAP_AUTHENTICATION:
             *mib_wtpEAPAuthenSupport_val_ptr = WTPEAPAUTHENSUPPORT_ENABLE_EAP_AUTHENTICATION;
             break;

        case INTERNAL_WTPEAPAUTHENSUPPORT_DISABLE_EAP_AUTHENTICATION:
             *mib_wtpEAPAuthenSupport_val_ptr = WTPEAPAUTHENSUPPORT_DISABLE_EAP_AUTHENTICATION;
             break;

        case INTERNAL_WTPEAPAUTHENSUPPORT_NOT_SUPPORTING_EAP_AUTHENTICATION:
             *mib_wtpEAPAuthenSupport_val_ptr = WTPEAPAUTHENSUPPORT_NOT_SUPPORTING_EAP_AUTHENTICATION;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpEAPAuthenSupport\n", raw_wtpEAPAuthenSupport_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpEAPAuthenSupport_map */

/**
 * Extract the current value of the wtpEAPAuthenSupport data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpEAPAuthenSupport_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpEAPAuthenSupport_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpEAPAuthenSupport_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpEAPAuthenSupport_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpEAPAuthenSupport_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpEAPAuthenSupport data.
 * set (* wtpEAPAuthenSupport_val_ptr ) from rowreq_ctx->data
 */
    (* wtpEAPAuthenSupport_val_ptr ) = rowreq_ctx->data.wtpEAPAuthenSupport;

    return MFD_SUCCESS;
} /* wtpEAPAuthenSupport_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpLoadBalance
 * wtpLoadBalance is subid 3 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.3
 * Description:
Based on the total number of users for load balancing.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpLoadBalance.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpLoadBalance_map(u_long *mib_wtpLoadBalance_val_ptr, u_long raw_wtpLoadBalance_val)
{
    netsnmp_assert(NULL != mib_wtpLoadBalance_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpLoadBalance_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpLoadBalance enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpLoadBalance_val) {
        case INTERNAL_WTPLOADBALANCE_TRUE:
             *mib_wtpLoadBalance_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_WTPLOADBALANCE_FALSE:
             *mib_wtpLoadBalance_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpLoadBalance\n", raw_wtpLoadBalance_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpLoadBalance_map */

/**
 * Extract the current value of the wtpLoadBalance data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpLoadBalance_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpLoadBalance_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpLoadBalance_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpLoadBalance_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpLoadBalance_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpLoadBalance data.
 * set (* wtpLoadBalance_val_ptr ) from rowreq_ctx->data
 */
    (* wtpLoadBalance_val_ptr ) = rowreq_ctx->data.wtpLoadBalance;

    return MFD_SUCCESS;
} /* wtpLoadBalance_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wirelessSSID
 * wirelessSSID is subid 4 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.4
 * Description:
SSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wirelessSSID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wirelessSSID_map(char **mib_wirelessSSID_val_ptr_ptr, size_t *mib_wirelessSSID_val_ptr_len_ptr, char *raw_wirelessSSID_val_ptr, size_t raw_wirelessSSID_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wirelessSSID_val_ptr);
    netsnmp_assert((NULL != mib_wirelessSSID_val_ptr_ptr) && (NULL != mib_wirelessSSID_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wirelessSSID_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wirelessSSID non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wirelessSSID_val_ptr_len; /* assume equal */
    if((NULL == *mib_wirelessSSID_val_ptr_ptr) || (*mib_wirelessSSID_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wirelessSSID_val_ptr_ptr = realloc( *mib_wirelessSSID_val_ptr_ptr, converted_len * sizeof(**mib_wirelessSSID_val_ptr_ptr));
        if(NULL == *mib_wirelessSSID_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wirelessSSID_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wirelessSSID_val_ptr_ptr, raw_wirelessSSID_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wirelessSSID_map */

/**
 * Extract the current value of the wirelessSSID data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wirelessSSID_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wirelessSSID_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wirelessSSID.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wirelessSSID_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wirelessSSID_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wirelessSSID_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, char **wirelessSSID_val_ptr_ptr, size_t *wirelessSSID_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wirelessSSID_val_ptr_ptr) && (NULL != *wirelessSSID_val_ptr_ptr));
   netsnmp_assert( NULL != wirelessSSID_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wirelessSSID_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wirelessSSID data.
 * set (* wirelessSSID_val_ptr_ptr ) and (* wirelessSSID_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wirelessSSID data
     */
    if ((NULL == (* wirelessSSID_val_ptr_ptr )) ||
        ((* wirelessSSID_val_ptr_len_ptr ) < (rowreq_ctx->data.wirelessSSID_len * sizeof((* wirelessSSID_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wirelessSSID data
         */
        (* wirelessSSID_val_ptr_ptr ) = malloc(rowreq_ctx->data.wirelessSSID_len * sizeof((* wirelessSSID_val_ptr_ptr )[0]));
        if(NULL == (* wirelessSSID_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wirelessSSID_val_ptr_len_ptr ) = rowreq_ctx->data.wirelessSSID_len * sizeof((* wirelessSSID_val_ptr_ptr )[0]);
    memcpy( (* wirelessSSID_val_ptr_ptr ), rowreq_ctx->data.wirelessSSID, (* wirelessSSID_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wirelessSSID_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpSsidBroadcast
 * wtpSsidBroadcast is subid 5 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.5
 * Description:
The ssid is whether broadcast.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSsidBroadcast.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSsidBroadcast_map(u_long *mib_wtpSsidBroadcast_val_ptr, u_long raw_wtpSsidBroadcast_val)
{
    netsnmp_assert(NULL != mib_wtpSsidBroadcast_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSsidBroadcast_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSsidBroadcast enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpSsidBroadcast_val) {
        case INTERNAL_WTPSSIDBROADCAST_TRUE:
             *mib_wtpSsidBroadcast_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_WTPSSIDBROADCAST_FALSE:
             *mib_wtpSsidBroadcast_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpSsidBroadcast\n", raw_wtpSsidBroadcast_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpSsidBroadcast_map */

/**
 * Extract the current value of the wtpSsidBroadcast data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidBroadcast_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSsidBroadcast_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpSsidBroadcast_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSsidBroadcast_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSsidBroadcast_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSsidBroadcast data.
 * set (* wtpSsidBroadcast_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSsidBroadcast_val_ptr ) = rowreq_ctx->data.wtpSsidBroadcast;

    return MFD_SUCCESS;
} /* wtpSsidBroadcast_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpConfSecurMech
 * wtpConfSecurMech is subid 6 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.6
 * Description:
The configed mechanism such as open system or shared key(o means no secyurity mechanism).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 6/16. Values:  openSystem(0), sharedKey(1), dot11x(2), WPA_P(3), WPA2_P(4), WPA_E(5), WPA2_E(6), MD5(7), WAPI_PSK(8), WAPI_AUTH(9)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpConfSecurMech.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpConfSecurMech_map(u_long *mib_wtpConfSecurMech_val_ptr, u_long raw_wtpConfSecurMech_val)
{
    netsnmp_assert(NULL != mib_wtpConfSecurMech_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpConfSecurMech_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpConfSecurMech enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpConfSecurMech_val) {
        case INTERNAL_WTPCONFSECURMECH_OPENSYSTEM:
             *mib_wtpConfSecurMech_val_ptr = WTPCONFSECURMECH_OPENSYSTEM;
             break;

        case INTERNAL_WTPCONFSECURMECH_SHAREDKEY:
             *mib_wtpConfSecurMech_val_ptr = WTPCONFSECURMECH_SHAREDKEY;
             break;

        case INTERNAL_WTPCONFSECURMECH_DOT11X:
             *mib_wtpConfSecurMech_val_ptr = WTPCONFSECURMECH_DOT11X;
             break;

        case INTERNAL_WTPCONFSECURMECH_WPA_P:
             *mib_wtpConfSecurMech_val_ptr = WTPCONFSECURMECH_WPA_P;
             break;

        case INTERNAL_WTPCONFSECURMECH_WPA2_P:
             *mib_wtpConfSecurMech_val_ptr = WTPCONFSECURMECH_WPA2_P;
             break;

        case INTERNAL_WTPCONFSECURMECH_WPA_E:
             *mib_wtpConfSecurMech_val_ptr = WTPCONFSECURMECH_WPA_E;
             break;

        case INTERNAL_WTPCONFSECURMECH_WPA2_E:
             *mib_wtpConfSecurMech_val_ptr = WTPCONFSECURMECH_WPA2_E;
             break;

        case INTERNAL_WTPCONFSECURMECH_MD5:
             *mib_wtpConfSecurMech_val_ptr = WTPCONFSECURMECH_MD5;
             break;

        case INTERNAL_WTPCONFSECURMECH_WAPI_PSK:
             *mib_wtpConfSecurMech_val_ptr = WTPCONFSECURMECH_WAPI_PSK;
             break;

        case INTERNAL_WTPCONFSECURMECH_WAPI_AUTH:
             *mib_wtpConfSecurMech_val_ptr = WTPCONFSECURMECH_WAPI_AUTH;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpConfSecurMech\n", raw_wtpConfSecurMech_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpConfSecurMech_map */

/**
 * Extract the current value of the wtpConfSecurMech data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpConfSecurMech_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpConfSecurMech_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpConfSecurMech_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpConfSecurMech_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpConfSecurMech_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpConfSecurMech data.
 * set (* wtpConfSecurMech_val_ptr ) from rowreq_ctx->data
 */
    (* wtpConfSecurMech_val_ptr ) = rowreq_ctx->data.wtpConfSecurMech;

    return MFD_SUCCESS;
} /* wtpConfSecurMech_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpSsidCurrOnlineUsrNum
 * wtpSsidCurrOnlineUsrNum is subid 7 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.7
 * Description:
The appointed SSID online user number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSsidCurrOnlineUsrNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSsidCurrOnlineUsrNum_map(long *mib_wtpSsidCurrOnlineUsrNum_val_ptr, long raw_wtpSsidCurrOnlineUsrNum_val)
{
    netsnmp_assert(NULL != mib_wtpSsidCurrOnlineUsrNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSsidCurrOnlineUsrNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSsidCurrOnlineUsrNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSsidCurrOnlineUsrNum_val_ptr) = raw_wtpSsidCurrOnlineUsrNum_val;

    return MFD_SUCCESS;
} /* wtpSsidCurrOnlineUsrNum_map */

/**
 * Extract the current value of the wtpSsidCurrOnlineUsrNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSsidCurrOnlineUsrNum_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSsidCurrOnlineUsrNum_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long * wtpSsidCurrOnlineUsrNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSsidCurrOnlineUsrNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSsidCurrOnlineUsrNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSsidCurrOnlineUsrNum data.
 * set (* wtpSsidCurrOnlineUsrNum_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSsidCurrOnlineUsrNum_val_ptr ) = rowreq_ctx->data.wtpSsidCurrOnlineUsrNum;

    return MFD_SUCCESS;
} /* wtpSsidCurrOnlineUsrNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpAcAppointDistOnlineUsrNum
 * wtpAcAppointDistOnlineUsrNum is subid 8 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.8
 * Description:
The total number of online user at appointed district.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpAcAppointDistOnlineUsrNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpAcAppointDistOnlineUsrNum_map(long *mib_wtpAcAppointDistOnlineUsrNum_val_ptr, long raw_wtpAcAppointDistOnlineUsrNum_val)
{
    netsnmp_assert(NULL != mib_wtpAcAppointDistOnlineUsrNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpAcAppointDistOnlineUsrNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpAcAppointDistOnlineUsrNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpAcAppointDistOnlineUsrNum_val_ptr) = raw_wtpAcAppointDistOnlineUsrNum_val;

    return MFD_SUCCESS;
} /* wtpAcAppointDistOnlineUsrNum_map */

/**
 * Extract the current value of the wtpAcAppointDistOnlineUsrNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAcAppointDistOnlineUsrNum_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpAcAppointDistOnlineUsrNum_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long * wtpAcAppointDistOnlineUsrNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAcAppointDistOnlineUsrNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpAcAppointDistOnlineUsrNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpAcAppointDistOnlineUsrNum data.
 * set (* wtpAcAppointDistOnlineUsrNum_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAcAppointDistOnlineUsrNum_val_ptr ) = rowreq_ctx->data.wtpAcAppointDistOnlineUsrNum;

    return MFD_SUCCESS;
} /* wtpAcAppointDistOnlineUsrNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpConRadiusServerIP
 * wtpConRadiusServerIP is subid 9 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.9
 * Description:
Radius server IP address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpConRadiusServerIP.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpConRadiusServerIP_map(u_long *mib_wtpConRadiusServerIP_val_ptr, u_long raw_wtpConRadiusServerIP_val)
{
    netsnmp_assert(NULL != mib_wtpConRadiusServerIP_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpConRadiusServerIP_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpConRadiusServerIP mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpConRadiusServerIP_val_ptr) = raw_wtpConRadiusServerIP_val;

    return MFD_SUCCESS;
} /* wtpConRadiusServerIP_map */

/**
 * Extract the current value of the wtpConRadiusServerIP data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpConRadiusServerIP_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpConRadiusServerIP_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpConRadiusServerIP_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpConRadiusServerIP_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpConRadiusServerIP_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpConRadiusServerIP data.
 * set (* wtpConRadiusServerIP_val_ptr ) from rowreq_ctx->data
 */
    (* wtpConRadiusServerIP_val_ptr ) = rowreq_ctx->data.wtpConRadiusServerIP;

    return MFD_SUCCESS;
} /* wtpConRadiusServerIP_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpConRadiusServerPort
 * wtpConRadiusServerPort is subid 10 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.10
 * Description:
Radius server port number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpConRadiusServerPort.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpConRadiusServerPort_map(long *mib_wtpConRadiusServerPort_val_ptr, long raw_wtpConRadiusServerPort_val)
{
    netsnmp_assert(NULL != mib_wtpConRadiusServerPort_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpConRadiusServerPort_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpConRadiusServerPort mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpConRadiusServerPort_val_ptr) = raw_wtpConRadiusServerPort_val;

    return MFD_SUCCESS;
} /* wtpConRadiusServerPort_map */

/**
 * Extract the current value of the wtpConRadiusServerPort data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpConRadiusServerPort_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpConRadiusServerPort_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long * wtpConRadiusServerPort_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpConRadiusServerPort_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpConRadiusServerPort_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpConRadiusServerPort data.
 * set (* wtpConRadiusServerPort_val_ptr ) from rowreq_ctx->data
 */
    (* wtpConRadiusServerPort_val_ptr ) = rowreq_ctx->data.wtpConRadiusServerPort;

    return MFD_SUCCESS;
} /* wtpConRadiusServerPort_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpSSIDPriority
 * wtpSSIDPriority is subid 11 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.11
 * Description:
SSID priority
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSSIDPriority.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSSIDPriority_map(long *mib_wtpSSIDPriority_val_ptr, long raw_wtpSSIDPriority_val)
{
    netsnmp_assert(NULL != mib_wtpSSIDPriority_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSSIDPriority_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSSIDPriority mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSSIDPriority_val_ptr) = raw_wtpSSIDPriority_val;

    return MFD_SUCCESS;
} /* wtpSSIDPriority_map */

/**
 * Extract the current value of the wtpSSIDPriority data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSSIDPriority_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSSIDPriority_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long * wtpSSIDPriority_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSSIDPriority_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpSSIDPriority_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSSIDPriority data.
 * set (* wtpSSIDPriority_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSSIDPriority_val_ptr ) = rowreq_ctx->data.wtpSSIDPriority;

    return MFD_SUCCESS;
} /* wtpSSIDPriority_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpAttachedFlowThreshold
 * wtpAttachedFlowThreshold is subid 12 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.12
 * Description:
Traffic load balancing threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 30;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpAttachedFlowThreshold.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpAttachedFlowThreshold_map(long *mib_wtpAttachedFlowThreshold_val_ptr, long raw_wtpAttachedFlowThreshold_val)
{
    netsnmp_assert(NULL != mib_wtpAttachedFlowThreshold_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpAttachedFlowThreshold_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpAttachedFlowThreshold mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpAttachedFlowThreshold_val_ptr) = raw_wtpAttachedFlowThreshold_val;

    return MFD_SUCCESS;
} /* wtpAttachedFlowThreshold_map */

/**
 * Extract the current value of the wtpAttachedFlowThreshold data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAttachedFlowThreshold_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpAttachedFlowThreshold_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long * wtpAttachedFlowThreshold_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAttachedFlowThreshold_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpAttachedFlowThreshold_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpAttachedFlowThreshold data.
 * set (* wtpAttachedFlowThreshold_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAttachedFlowThreshold_val_ptr ) = rowreq_ctx->data.wtpAttachedFlowThreshold;

    return MFD_SUCCESS;
} /* wtpAttachedFlowThreshold_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpUsrNumAppendThreshold
 * wtpUsrNumAppendThreshold is subid 13 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.13
 * Description:
Load balancing the number of users threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 10;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpUsrNumAppendThreshold.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpUsrNumAppendThreshold_map(long *mib_wtpUsrNumAppendThreshold_val_ptr, long raw_wtpUsrNumAppendThreshold_val)
{
    netsnmp_assert(NULL != mib_wtpUsrNumAppendThreshold_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpUsrNumAppendThreshold_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpUsrNumAppendThreshold mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpUsrNumAppendThreshold_val_ptr) = raw_wtpUsrNumAppendThreshold_val;

    return MFD_SUCCESS;
} /* wtpUsrNumAppendThreshold_map */

/**
 * Extract the current value of the wtpUsrNumAppendThreshold data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUsrNumAppendThreshold_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpUsrNumAppendThreshold_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, long * wtpUsrNumAppendThreshold_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpUsrNumAppendThreshold_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpUsrNumAppendThreshold_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpUsrNumAppendThreshold data.
 * set (* wtpUsrNumAppendThreshold_val_ptr ) from rowreq_ctx->data
 */
    (* wtpUsrNumAppendThreshold_val_ptr ) = rowreq_ctx->data.wtpUsrNumAppendThreshold;

    return MFD_SUCCESS;
} /* wtpUsrNumAppendThreshold_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wtpL2Insulate
 * wtpL2Insulate is subid 14 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.5.3.1.14
 * Description:
L2 insulate whether open.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  close(0), open(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpL2Insulate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpL2Insulate_map(u_long *mib_wtpL2Insulate_val_ptr, u_long raw_wtpL2Insulate_val)
{
    netsnmp_assert(NULL != mib_wtpL2Insulate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpL2Insulate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpL2Insulate enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpL2Insulate_val) {
        case INTERNAL_WTPL2INSULATE_CLOSE:
             *mib_wtpL2Insulate_val_ptr = WTPL2INSULATE_CLOSE;
             break;

        case INTERNAL_WTPL2INSULATE_OPEN:
             *mib_wtpL2Insulate_val_ptr = WTPL2INSULATE_OPEN;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpL2Insulate\n", raw_wtpL2Insulate_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpL2Insulate_map */

/**
 * Extract the current value of the wtpL2Insulate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpL2Insulate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpL2Insulate_get( dot11WtpWlanStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpL2Insulate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpL2Insulate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpWlanStatsTable:wtpL2Insulate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpL2Insulate data.
 * set (* wtpL2Insulate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpL2Insulate_val_ptr ) = rowreq_ctx->data.wtpL2Insulate;

    return MFD_SUCCESS;
} /* wtpL2Insulate_get */



/** @} */
