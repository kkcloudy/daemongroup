/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WifiPositionGlobalInfoTable.h"
#include "ws_dcli_scanlocate.h"

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WifiPositionGlobalInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-WTP-MIB::dot11WifiPositionGlobalInfoTable is subid 7 of wtpStation.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.7, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11WifiPositionGlobalInfoTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11WifiPositionGlobalInfoTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WifiPositionGlobalInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11WifiPositionGlobalInfoTable_undo_setup( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:dot11WifiPositionGlobalInfoTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11WifiPositionGlobalInfoTable undo.
     * set up dot11WifiPositionGlobalInfoTable undo information, in preparation for a set.
     * Undo storage is in (* WifiPositionGlobalScanSwitch_val_ptr )*
     */

    return rc;
} /* dot11WifiPositionGlobalInfoTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WifiPositionGlobalInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11WifiPositionGlobalInfoTable_undo( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:dot11WifiPositionGlobalInfoTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11WifiPositionGlobalInfoTable undo.
     * dot11WifiPositionGlobalInfoTable undo information, in response to a failed set.
     * Undo storage is in (* WifiPositionGlobalScanSwitch_val_ptr )*
     */

    return rc;
} /* dot11WifiPositionGlobalInfoTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WifiPositionGlobalInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WifiPositionGlobalInfoTable_undo_cleanup( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:dot11WifiPositionGlobalInfoTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11WifiPositionGlobalInfoTable undo.
     * Undo storage is in (* WifiPositionGlobalScanSwitch_val_ptr )*
     */

    return rc;
} /* dot11WifiPositionGlobalInfoTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11WifiPositionGlobalInfoTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WifiPositionGlobalInfoTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WifiPositionGlobalInfoTable_commit( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:dot11WifiPositionGlobalInfoTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11WifiPositionGlobalInfoTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_WIFIPOSITIONGLOBALPARA_FLAG) {
       save_flags &= ~COLUMN_WIFIPOSITIONGLOBALPARA_FLAG; /* clear WifiPositionGlobalPara */
       /*
        * TODO:482:o: |-> commit column WifiPositionGlobalPara.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WifiPositionGlobalInfoTable column WifiPositionGlobalPara commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo WifiPositionGlobalPara
             */
            rowreq_ctx->column_set_flags |= COLUMN_WIFIPOSITIONGLOBALPARA_FLAG;
       }
    }

    if (save_flags & COLUMN_WIFIPOSITIONGLOBALSCANSWITCH_FLAG) {
       save_flags &= ~COLUMN_WIFIPOSITIONGLOBALSCANSWITCH_FLAG; /* clear WifiPositionGlobalScanSwitch */
       /*
        * TODO:482:o: |-> commit column WifiPositionGlobalScanSwitch.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WifiPositionGlobalInfoTable column WifiPositionGlobalScanSwitch commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo WifiPositionGlobalScanSwitch
             */
            rowreq_ctx->column_set_flags |= COLUMN_WIFIPOSITIONGLOBALSCANSWITCH_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11WifiPositionGlobalInfoTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11WifiPositionGlobalInfoTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WifiPositionGlobalInfoTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WifiPositionGlobalInfoTable_undo_commit( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:dot11WifiPositionGlobalInfoTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11WifiPositionGlobalInfoTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11WifiPositionGlobalInfoTable_undo_commit */

/*
 * TODO:440:M: Implement dot11WifiPositionGlobalInfoTable node value checks.
 * TODO:450:M: Implement dot11WifiPositionGlobalInfoTable undo functions.
 * TODO:460:M: Implement dot11WifiPositionGlobalInfoTable set functions.
 * TODO:480:M: Implement dot11WifiPositionGlobalInfoTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WifiPositionGlobalInfoEntry.WifiPositionGlobalPara
 * WifiPositionGlobalPara is subid 1 of dot11WifiPositionGlobalInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.7.1.1
 * Description:
wifi position global parameter.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param WifiPositionGlobalPara_val_ptr
 *        A char containing the new value.
 * @param WifiPositionGlobalPara_val_ptr_len
 *        The size (in bytes) of the data pointed to by WifiPositionGlobalPara_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WifiPositionGlobalInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.WifiPositionGlobalPara).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
WifiPositionGlobalPara_check_value( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx, char *WifiPositionGlobalPara_val_ptr,  size_t WifiPositionGlobalPara_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:WifiPositionGlobalPara_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != WifiPositionGlobalPara_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid WifiPositionGlobalPara value.
     */

    return MFD_SUCCESS; /* WifiPositionGlobalPara value not illegal */
} /* WifiPositionGlobalPara_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WifiPositionGlobalInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WifiPositionGlobalInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
WifiPositionGlobalPara_undo_setup( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:WifiPositionGlobalPara_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup WifiPositionGlobalPara undo.
     */
    /*
     * copy WifiPositionGlobalPara and WifiPositionGlobalPara_len data
     * set rowreq_ctx->undo->WifiPositionGlobalPara from rowreq_ctx->data.WifiPositionGlobalPara
     */
    memcpy( rowreq_ctx->undo->WifiPositionGlobalPara, rowreq_ctx->data.WifiPositionGlobalPara,
            (rowreq_ctx->data.WifiPositionGlobalPara_len * sizeof(rowreq_ctx->undo->WifiPositionGlobalPara[0])));
    rowreq_ctx->undo->WifiPositionGlobalPara_len = rowreq_ctx->data.WifiPositionGlobalPara_len;


    return MFD_SUCCESS;
} /* WifiPositionGlobalPara_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param WifiPositionGlobalPara_val_ptr
 *        A char containing the new value.
 * @param WifiPositionGlobalPara_val_ptr_len
 *        The size (in bytes) of the data pointed to by WifiPositionGlobalPara_val_ptr
 */
int
WifiPositionGlobalPara_set( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx, char *WifiPositionGlobalPara_val_ptr,  size_t WifiPositionGlobalPara_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:WifiPositionGlobalPara_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != WifiPositionGlobalPara_val_ptr);

    /*
     * TODO:461:M: |-> Set WifiPositionGlobalPara value.
     * set WifiPositionGlobalPara value in rowreq_ctx->data
     */
    int rc = MFD_ERROR;
	int ret = 0;
	char input[255] = { 0 };
	unsigned char wtpMac[6] = { 0 };
	char *temp = NULL;
	u_long  ip_addr = 0;
	char ServerIp[20] = { 0 };
	int ServerPort = 0;
	int ScanInterval = 0;
	int ScanTime = 0;
	unsigned long long ScanChannle = 0;
	int ScanType = 0;
	int ReportInterval = 0;
	int Rssi = 0;
	int ScanSwitch = 0;
	int version_num = 0;
	int result_filter = 0;

	memset(input, 0, sizeof(input));
	strncpy(input, WifiPositionGlobalPara_val_ptr, WifiPositionGlobalPara_val_ptr_len);

	temp = strchr(input, '_');
	if(temp)
	{
		memset(ServerIp, 0, sizeof(ServerIp));
		strncpy(ServerIp, input, temp - input);
		INET_ATON(ip_addr,ServerIp);
		
		sscanf(temp+1, "%d_%d_%d_%llu_%d_%d_%d_%d_%d_%d", &ServerPort, &ScanInterval, &ScanTime, 
			&ScanChannle, &ScanType, &ReportInterval, &Rssi, &ScanSwitch, &version_num, &result_filter);
	}

	memset(wtpMac, 255, 6);

    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	
	ret = set_wifi_locate_public_para_cmd(
	                                      rowreq_ctx->data.parameter.local_id,
	                                      rowreq_ctx->data.instanceID,
										  wtpMac,
										  ip_addr,
										  ServerPort,
										  ScanInterval,
										  ScanTime,
										  ScanChannle,
										  ScanType,
										  ReportInterval,
										  Rssi,
										  version_num,
										  result_filter,
										  ScanSwitch,
										  rowreq_ctx->tbl_idx.WifiPositionFrequency,
										  connection);
	if(ret == 1)
	{
		memcpy( rowreq_ctx->data.WifiPositionGlobalPara, WifiPositionGlobalPara_val_ptr, WifiPositionGlobalPara_val_ptr_len );
		/** convert bytes to number of char */
		rowreq_ctx->data.WifiPositionGlobalPara_len = WifiPositionGlobalPara_val_ptr_len / sizeof(WifiPositionGlobalPara_val_ptr[0]);
		
		rc =  MFD_SUCCESS;
	}
	else
	{						
		rc = MFD_ERROR;
	} 

	return rc;
} /* WifiPositionGlobalPara_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
WifiPositionGlobalPara_undo( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:WifiPositionGlobalPara_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up WifiPositionGlobalPara undo.
     */
    /*
     * copy WifiPositionGlobalPara and WifiPositionGlobalPara_len data
     * set rowreq_ctx->data.WifiPositionGlobalPara from rowreq_ctx->undo->WifiPositionGlobalPara
     */
    memcpy( rowreq_ctx->data.WifiPositionGlobalPara, rowreq_ctx->undo->WifiPositionGlobalPara,
            (rowreq_ctx->undo->WifiPositionGlobalPara_len * sizeof(rowreq_ctx->data.WifiPositionGlobalPara[0])));
    rowreq_ctx->data.WifiPositionGlobalPara_len = rowreq_ctx->undo->WifiPositionGlobalPara_len;

    
    return MFD_SUCCESS;
} /* WifiPositionGlobalPara_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WifiPositionGlobalInfoEntry.WifiPositionGlobalScanSwitch
 * WifiPositionGlobalScanSwitch is subid 2 of dot11WifiPositionGlobalInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.7.1.2
 * Description:
wifi position global scan switch.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  off(0), on(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param WifiPositionGlobalScanSwitch_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WifiPositionGlobalInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  off(0), on(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
WifiPositionGlobalScanSwitch_check_value( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx, u_long WifiPositionGlobalScanSwitch_val)
{
    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:WifiPositionGlobalScanSwitch_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid WifiPositionGlobalScanSwitch value.
     */

    return MFD_SUCCESS; /* WifiPositionGlobalScanSwitch value not illegal */
} /* WifiPositionGlobalScanSwitch_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WifiPositionGlobalInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WifiPositionGlobalInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
WifiPositionGlobalScanSwitch_undo_setup( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:WifiPositionGlobalScanSwitch_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup WifiPositionGlobalScanSwitch undo.
     */
    /*
     * copy WifiPositionGlobalScanSwitch data
     * set rowreq_ctx->undo->WifiPositionGlobalScanSwitch from rowreq_ctx->data.WifiPositionGlobalScanSwitch
     */
    rowreq_ctx->undo->WifiPositionGlobalScanSwitch = rowreq_ctx->data.WifiPositionGlobalScanSwitch;


    return MFD_SUCCESS;
} /* WifiPositionGlobalScanSwitch_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param WifiPositionGlobalScanSwitch_val
 *        A long containing the new value.
 */
int
WifiPositionGlobalScanSwitch_set( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx, u_long WifiPositionGlobalScanSwitch_val )
{

    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:WifiPositionGlobalScanSwitch_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set WifiPositionGlobalScanSwitch value.
     * set WifiPositionGlobalScanSwitch value in rowreq_ctx->data
     */
    int rc = MFD_ERROR;
	int ret = 0;

    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	ret = set_wifi_locate_change_on_off_all_wtp_cmd(rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.instanceID,WifiPositionGlobalScanSwitch_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection);
	if(ret == 1)
	{
		rowreq_ctx->data.WifiPositionGlobalScanSwitch = WifiPositionGlobalScanSwitch_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* WifiPositionGlobalScanSwitch_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
WifiPositionGlobalScanSwitch_undo( dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WifiPositionGlobalInfoTable:WifiPositionGlobalScanSwitch_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up WifiPositionGlobalScanSwitch undo.
     */
    /*
     * copy WifiPositionGlobalScanSwitch data
     * set rowreq_ctx->data.WifiPositionGlobalScanSwitch from rowreq_ctx->undo->WifiPositionGlobalScanSwitch
     */
    rowreq_ctx->data.WifiPositionGlobalScanSwitch = rowreq_ctx->undo->WifiPositionGlobalScanSwitch;

    
    return MFD_SUCCESS;
} /* WifiPositionGlobalScanSwitch_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11WifiPositionGlobalInfoTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11WifiPositionGlobalInfoTable if you don't have dependencies)
 */
int
dot11WifiPositionGlobalInfoTable_check_dependencies(dot11WifiPositionGlobalInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11WifiPositionGlobalInfoTable:dot11WifiPositionGlobalInfoTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11WifiPositionGlobalInfoTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11WifiPositionGlobalInfoTable_check_dependencies */

/** @} */
