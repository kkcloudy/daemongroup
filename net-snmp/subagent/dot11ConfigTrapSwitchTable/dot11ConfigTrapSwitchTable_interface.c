/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.43.2.3 $ of : mfd-interface.m2c,v $ 
 *
 * $Id:$
 */
/*
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 * ***                                                               ***
 * ***  NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE  ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THIS FILE DOES NOT CONTAIN ANY USER EDITABLE CODE.      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THE GENERATED CODE IS INTERNAL IMPLEMENTATION, AND      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***    IS SUBJECT TO CHANGE WITHOUT WARNING IN FUTURE RELEASES.   ***
 * ***                                                               ***
 * ***                                                               ***
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 */

/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigTrapSwitchTable.h"


#include <net-snmp/library/container.h>

#include "dot11ConfigTrapSwitchTable_interface.h"

/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigTrapSwitchTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigTrapSwitchTable is subid 2 of dot11AlarmConfiguration.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2, length: 12
*/
typedef struct dot11ConfigTrapSwitchTable_interface_ctx_s {

   netsnmp_container              *container;
   netsnmp_cache                  *cache; /* optional cache */

   dot11ConfigTrapSwitchTable_registration_ptr      user_ctx;
   
   netsnmp_table_registration_info  tbl_info;

   netsnmp_baby_steps_access_methods access_multiplexer;

} dot11ConfigTrapSwitchTable_interface_ctx;

static dot11ConfigTrapSwitchTable_interface_ctx dot11ConfigTrapSwitchTable_if_ctx;

static void _dot11ConfigTrapSwitchTable_container_init(
    dot11ConfigTrapSwitchTable_interface_ctx *if_ctx);


static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_pre_request;
static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_post_request;
static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_object_lookup;
static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_get_values;
static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_check_objects;
static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_undo_setup;
static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_set_values;
static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_undo_cleanup;
static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_undo_values;
static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_commit;
static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_undo_commit;
static Netsnmp_Node_Handler _mfd_dot11ConfigTrapSwitchTable_check_dependencies;

NETSNMP_STATIC_INLINE int _dot11ConfigTrapSwitchTable_undo_column( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx,
                                                   netsnmp_variable_list *var, int column );

dot11ConfigTrapSwitchTable_data *dot11ConfigTrapSwitchTable_allocate_data(void);

/**
 * @internal
 * Initialize the table dot11ConfigTrapSwitchTable 
 *    (Define its contents and how it's structured)
 */
void
_dot11ConfigTrapSwitchTable_initialize_interface(dot11ConfigTrapSwitchTable_registration_ptr reg_ptr,  u_long flags)
{
    netsnmp_baby_steps_access_methods *access_multiplexer =
        &dot11ConfigTrapSwitchTable_if_ctx.access_multiplexer;
    netsnmp_table_registration_info *tbl_info = &dot11ConfigTrapSwitchTable_if_ctx.tbl_info;
    netsnmp_handler_registration *reginfo;
    netsnmp_mib_handler *handler;
    int    mfd_modes = 0;

    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_dot11ConfigTrapSwitchTable_initialize_interface","called\n"));


    /*************************************************
     *
     * save interface context for dot11ConfigTrapSwitchTable
     */
    /*
     * Setting up the table's definition
     */
    netsnmp_table_helper_add_indexes(tbl_info,
                                  ASN_OCTET_STR, /** index: wtpMacAddr */
                             0);

    /*  Define the minimum and maximum accessible columns.  This
        optimizes retrival. */
    tbl_info->min_column = DOT11CONFIGTRAPSWITCHTABLE_MIN_COL;
    tbl_info->max_column = DOT11CONFIGTRAPSWITCHTABLE_MAX_COL;

    /*
     * save users context
     */
    dot11ConfigTrapSwitchTable_if_ctx.user_ctx = reg_ptr;

    /*
     * call data access initialization code
     */
    dot11ConfigTrapSwitchTable_init_data(reg_ptr);

    /*
     * set up the container
     */
    _dot11ConfigTrapSwitchTable_container_init(&dot11ConfigTrapSwitchTable_if_ctx);
    if (NULL == dot11ConfigTrapSwitchTable_if_ctx.container) {
        snmp_log(LOG_ERR,"could not initialize container for dot11ConfigTrapSwitchTable\n");
        return;
    }
    
    /*
     * access_multiplexer: REQUIRED wrapper for get request handling
     */
    access_multiplexer->object_lookup = _mfd_dot11ConfigTrapSwitchTable_object_lookup;
    access_multiplexer->get_values = _mfd_dot11ConfigTrapSwitchTable_get_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->pre_request = _mfd_dot11ConfigTrapSwitchTable_pre_request;
    access_multiplexer->post_request = _mfd_dot11ConfigTrapSwitchTable_post_request;


    /*
     * REQUIRED wrappers for set request handling
     */
    access_multiplexer->object_syntax_checks = _mfd_dot11ConfigTrapSwitchTable_check_objects;
    access_multiplexer->undo_setup = _mfd_dot11ConfigTrapSwitchTable_undo_setup;
    access_multiplexer->undo_cleanup = _mfd_dot11ConfigTrapSwitchTable_undo_cleanup;
    access_multiplexer->set_values = _mfd_dot11ConfigTrapSwitchTable_set_values;
    access_multiplexer->undo_sets = _mfd_dot11ConfigTrapSwitchTable_undo_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->commit = _mfd_dot11ConfigTrapSwitchTable_commit;
    access_multiplexer->undo_commit = _mfd_dot11ConfigTrapSwitchTable_undo_commit;
    
    /*
     * REQUIRED for tables with dependencies
     */
    access_multiplexer->consistency_checks = _mfd_dot11ConfigTrapSwitchTable_check_dependencies;

    /*************************************************
     *
     * Create a registration, save our reg data, register table.
     */
    DEBUGMSGTL(("dot11ConfigTrapSwitchTable:init_dot11ConfigTrapSwitchTable",
                "Registering dot11ConfigTrapSwitchTable as a mibs-for-dummies table.\n"));		 
    handler = netsnmp_baby_steps_access_multiplexer_get(access_multiplexer);
    reginfo = netsnmp_handler_registration_create("dot11ConfigTrapSwitchTable", handler,
                                                  dot11ConfigTrapSwitchTable_oid,
                                                  dot11ConfigTrapSwitchTable_oid_size,
                                                  HANDLER_CAN_BABY_STEP |
                                                  HANDLER_CAN_RWRITE
                                                  );
    if(NULL == reginfo) {
        snmp_log(LOG_ERR,"error registering table dot11ConfigTrapSwitchTable\n");
        return;
    }
    reginfo->my_reg_void = &dot11ConfigTrapSwitchTable_if_ctx;

    /*************************************************
     *
     * set up baby steps handler, create it and inject it
     */
    if( access_multiplexer->object_lookup )
        mfd_modes |= BABY_STEP_OBJECT_LOOKUP;
    if( access_multiplexer->set_values )
        mfd_modes |= BABY_STEP_SET_VALUES;
    if( access_multiplexer->irreversible_commit )
        mfd_modes |= BABY_STEP_IRREVERSIBLE_COMMIT;
    if( access_multiplexer->object_syntax_checks )
        mfd_modes |= BABY_STEP_CHECK_OBJECT;

    if( access_multiplexer->pre_request )
        mfd_modes |= BABY_STEP_PRE_REQUEST;
    if( access_multiplexer->post_request )
        mfd_modes |= BABY_STEP_POST_REQUEST;
    
    if( access_multiplexer->undo_setup )
        mfd_modes |= BABY_STEP_UNDO_SETUP;
    if( access_multiplexer->undo_cleanup )
        mfd_modes |= BABY_STEP_UNDO_CLEANUP;
    if( access_multiplexer->undo_sets )
        mfd_modes |= BABY_STEP_UNDO_SETS;
    
    if( access_multiplexer->row_creation )
        mfd_modes |= BABY_STEP_ROW_CREATE;
    if( access_multiplexer->consistency_checks )
        mfd_modes |= BABY_STEP_CHECK_CONSISTENCY;
    if( access_multiplexer->commit )
        mfd_modes |= BABY_STEP_COMMIT;
    if( access_multiplexer->undo_commit )
        mfd_modes |= BABY_STEP_UNDO_COMMIT;
    
    handler = netsnmp_baby_steps_handler_get(mfd_modes);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject row_merge helper with prefix rootoid_len + 2 (entry.col)
     */
    handler = netsnmp_get_row_merge_handler(reginfo->rootoid_len + 2);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject container_table helper
     */
    handler =
        netsnmp_container_table_handler_get(tbl_info,
                                            dot11ConfigTrapSwitchTable_if_ctx.container,
                                            TABLE_CONTAINER_KEY_NETSNMP_INDEX);
    netsnmp_inject_handler( reginfo, handler );

    /*************************************************
     *
     * inject cache helper
     */
    if(NULL != dot11ConfigTrapSwitchTable_if_ctx.cache) {
        handler = netsnmp_cache_handler_get(dot11ConfigTrapSwitchTable_if_ctx.cache);
        netsnmp_inject_handler( reginfo, handler );
    }

    /*
     * register table
     */
    netsnmp_register_table(reginfo, tbl_info);
} /* _dot11ConfigTrapSwitchTable_initialize_interface */

void
dot11ConfigTrapSwitchTable_valid_columns_set(netsnmp_column_info *vc)
{
    dot11ConfigTrapSwitchTable_if_ctx.tbl_info.valid_columns = vc;
} /* dot11ConfigTrapSwitchTable_valid_columns_set */

/**
 * @internal
 * convert the index component stored in the context to an oid
 */
int
dot11ConfigTrapSwitchTable_index_to_oid(netsnmp_index *oid_idx,
                         dot11ConfigTrapSwitchTable_mib_index *mib_idx)
{
    int err = SNMP_ERR_NOERROR;
    
    /*
     * temp storage for parsing indexes
     */
    /*
     * wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    netsnmp_variable_list var_wtpMacAddr;

    /*
     * set up varbinds
     */
    memset( &var_wtpMacAddr, 0x00, sizeof(var_wtpMacAddr) );
    var_wtpMacAddr.type = ASN_OCTET_STR;

    /*
     * chain temp index varbinds together
     */
    var_wtpMacAddr.next_variable =  NULL;


    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_index_to_oid","called\n"));

        /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
    snmp_set_var_value(&var_wtpMacAddr, (u_char*)&mib_idx->wtpMacAddr,
                       mib_idx->wtpMacAddr_len * sizeof(mib_idx->wtpMacAddr[0]));


    err = build_oid_noalloc(oid_idx->oids, oid_idx->len, &oid_idx->len,
                           NULL, 0, &var_wtpMacAddr);
    if(err)
        snmp_log(LOG_ERR,"error %d converting index to oid\n", err);

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers( &var_wtpMacAddr );

    return err;
} /* dot11ConfigTrapSwitchTable_index_to_oid */

/**
 * extract dot11ConfigTrapSwitchTable indexes from a netsnmp_index
 *
 * @retval SNMP_ERR_NOERROR  : no error
 * @retval SNMP_ERR_GENERR   : error
 */
int
dot11ConfigTrapSwitchTable_index_from_oid(netsnmp_index *oid_idx,
                         dot11ConfigTrapSwitchTable_mib_index *mib_idx)
{
    int err = SNMP_ERR_NOERROR;
    
    /*
     * temp storage for parsing indexes
     */
    /*
     * wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    netsnmp_variable_list var_wtpMacAddr;

    /*
     * set up varbinds
     */
    memset( &var_wtpMacAddr, 0x00, sizeof(var_wtpMacAddr) );
    var_wtpMacAddr.type = ASN_OCTET_STR;

    /*
     * chain temp index varbinds together
     */
    var_wtpMacAddr.next_variable =  NULL;


    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_index_from_oid","called\n"));

    /*
     * parse the oid into the individual index components
     */
    err = parse_oid_indexes( oid_idx->oids, oid_idx->len,
                             &var_wtpMacAddr );
    if (err == SNMP_ERR_NOERROR) {
        /*
         * copy out values
         */
    /*
     * NOTE: val_len is in bytes, wtpMacAddr_len might not be
     */
         if(var_wtpMacAddr.val_len > sizeof(mib_idx->wtpMacAddr))
             err = SNMP_ERR_GENERR;
         else {
             memcpy(mib_idx->wtpMacAddr, var_wtpMacAddr.val.string, var_wtpMacAddr.val_len);
             mib_idx->wtpMacAddr_len = var_wtpMacAddr.val_len / sizeof(mib_idx->wtpMacAddr[0]);
         }


    }

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers( &var_wtpMacAddr );

    return err;
} /* dot11ConfigTrapSwitchTable_index_from_oid */


/*
 * dot11ConfigTrapSwitchTable_allocate_data
 *
 * Purpose: create new dot11ConfigTrapSwitchTable_data.
 */
dot11ConfigTrapSwitchTable_data *
dot11ConfigTrapSwitchTable_allocate_data(void)
{
    dot11ConfigTrapSwitchTable_data *rtn = SNMP_MALLOC_TYPEDEF(dot11ConfigTrapSwitchTable_data);

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_allocate_data","called\n"));

    if(NULL == rtn) {
        snmp_log(LOG_ERR, "unable to malloc memory for new "
                 "dot11ConfigTrapSwitchTable_data.\n");
    }

    return rtn;
} /* dot11ConfigTrapSwitchTable_allocate_data */

/*
 * dot11ConfigTrapSwitchTable_release_data
 *
 * Purpose: release dot11ConfigTrapSwitchTable data.
 */
void
dot11ConfigTrapSwitchTable_release_data(dot11ConfigTrapSwitchTable_data *data)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_release_data","called\n"));

    free(data);
} /* dot11ConfigTrapSwitchTable_release_data */

/* *********************************************************************
 * @internal
 * allocate resources for a dot11ConfigTrapSwitchTable_rowreq_ctx
 */
dot11ConfigTrapSwitchTable_rowreq_ctx *
dot11ConfigTrapSwitchTable_allocate_rowreq_ctx(void)
{
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx =
                  SNMP_MALLOC_TYPEDEF(dot11ConfigTrapSwitchTable_rowreq_ctx);

    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_allocate_rowreq_ctx","called\n"));

    if(NULL == rowreq_ctx) {
        snmp_log(LOG_ERR,"Couldn't allocate memory for a "
                 "dot11ConfigTrapSwitchTable_rowreq_ctx.\n");
    }

    rowreq_ctx->oid_idx.oids = rowreq_ctx->oid_tmp;

    rowreq_ctx->dot11ConfigTrapSwitchTable_data_list = NULL;
    rowreq_ctx->dot11ConfigTrapSwitchTable_reg = dot11ConfigTrapSwitchTable_if_ctx.user_ctx;


    return rowreq_ctx;
} /* dot11ConfigTrapSwitchTable_allocate_rowreq_ctx */

/*
 * @internal
 * release resources for a dot11ConfigTrapSwitchTable_rowreq_ctx
 */
void
dot11ConfigTrapSwitchTable_release_rowreq_ctx(dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_release_rowreq_ctx","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    

    if(rowreq_ctx->undo)
        dot11ConfigTrapSwitchTable_release_data(rowreq_ctx->undo);
 
    /*
     * free index oid pointer
     */
    if(rowreq_ctx->oid_idx.oids != rowreq_ctx->oid_tmp)
        free(rowreq_ctx->oid_idx.oids);

    SNMP_FREE(rowreq_ctx);
} /* dot11ConfigTrapSwitchTable_release_rowreq_ctx */

/**
 * @internal
 * wrapper
 */
static int
_mfd_dot11ConfigTrapSwitchTable_pre_request(netsnmp_mib_handler *handler,
                            netsnmp_handler_registration *reginfo,
                            netsnmp_agent_request_info *agtreq_info,
                            netsnmp_request_info *requests)
{
    int rc = dot11ConfigTrapSwitchTable_pre_request(dot11ConfigTrapSwitchTable_if_ctx.user_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable","error %d from "
                    "dot11ConfigTrapSwitchTable_pre_request\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }
    
    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigTrapSwitchTable_pre_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_dot11ConfigTrapSwitchTable_post_request(netsnmp_mib_handler *handler,
                             netsnmp_handler_registration *reginfo,
                             netsnmp_agent_request_info *agtreq_info,
                             netsnmp_request_info *requests)
{
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx;
    int rc = dot11ConfigTrapSwitchTable_post_request(dot11ConfigTrapSwitchTable_if_ctx.user_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable","error %d from "
                    "dot11ConfigTrapSwitchTable_post_request\n", rc));
    }
    
    /*
     * if there are no errors, check for and handle row creation/deletion
     */
    rc = netsnmp_check_requests_error(requests);
    if ((SNMP_ERR_NOERROR == rc) &&
        (NULL !=
         (rowreq_ctx = netsnmp_container_table_row_extract(requests)))) {
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            rowreq_ctx->rowreq_flags &= ~MFD_ROW_CREATED;
            CONTAINER_INSERT(dot11ConfigTrapSwitchTable_if_ctx.container, rowreq_ctx);
        }
        else if (rowreq_ctx->rowreq_flags & MFD_ROW_DELETED) {
            CONTAINER_REMOVE(dot11ConfigTrapSwitchTable_if_ctx.container, rowreq_ctx);
            dot11ConfigTrapSwitchTable_release_rowreq_ctx(rowreq_ctx);
        }
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigTrapSwitchTable_post_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_dot11ConfigTrapSwitchTable_object_lookup(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_mfd_dot11ConfigTrapSwitchTable_object_lookup","called\n"));

    /*
     * get our context from mfd
     * dot11ConfigTrapSwitchTable_interface_ctx *if_ctx =
     *             (dot11ConfigTrapSwitchTable_interface_ctx *)reginfo->my_reg_void;
     */

    if(NULL == rowreq_ctx) {
        netsnmp_request_set_error_all(requests, SNMP_ERR_NOCREATION);
    }
    else {
        dot11ConfigTrapSwitchTable_row_prep(rowreq_ctx);
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigTrapSwitchTable_object_lookup */

/***********************************************************************
 *
 * GET processing
 *
 ***********************************************************************/
/*
 * @internal
 * Retrieve the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigTrapSwitchTable_get_column( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_mfd_dot11ConfigTrapSwitchTable_get_column","called\n"));


    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* (INDEX) wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
    case COLUMN_WTPMACADDR:
    var->type = ASN_OCTET_STR;
    /*
     * NOTE: val_len is in bytes, wtpMacAddr_len might not be (e.g. oids)
     */
        if (var->val_len < (rowreq_ctx->tbl_idx.wtpMacAddr_len *
                            sizeof(rowreq_ctx->tbl_idx.wtpMacAddr[0]))) {
           var->val.string = malloc(rowreq_ctx->tbl_idx.wtpMacAddr_len *
                                    sizeof(rowreq_ctx->tbl_idx.wtpMacAddr[0]));
        }
        var->val_len = rowreq_ctx->tbl_idx.wtpMacAddr_len * sizeof(rowreq_ctx->tbl_idx.wtpMacAddr[0]);
        memcpy( var->val.string, rowreq_ctx->tbl_idx.wtpMacAddr, var->val_len );
        break;

    /* APInterfThreshhd(2)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_APINTERFTHRESHHD:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = APInterfThreshhd_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* StaInterfThreshhd(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_STAINTERFTHRESHHD:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = StaInterfThreshhd_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* CPUusageThreshhd(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_CPUUSAGETHRESHHD:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = CPUusageThreshhd_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* MemUsageThreshhd(5)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_MEMUSAGETHRESHHD:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = MemUsageThreshhd_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* CoChAPInterfThreshhd(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_COCHAPINTERFTHRESHHD:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = CoChAPInterfThreshhd_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* AdjacentChAPInterfThreshhd(7)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_ADJACENTCHAPINTERFTHRESHHD:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = AdjacentChAPInterfThreshhd_get(rowreq_ctx, (long *)var->val.string );
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigTrapSwitchTable_get_column\n", column);
         break;
    }

    return rc;
} /* _dot11ConfigTrapSwitchTable_get_column */

int
_mfd_dot11ConfigTrapSwitchTable_get_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    u_char                     * old_string;
    void                      (*dataFreeHook)(void *);
    int                        rc;

    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_mfd_dot11ConfigTrapSwitchTable_get_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    for(;requests; requests = requests->next) {
        /*
         * save old pointer, so we can free it if replaced
         */
        old_string = requests->requestvb->val.string;
        dataFreeHook = requests->requestvb->dataFreeHook;
        if(NULL == requests->requestvb->val.string) {
            requests->requestvb->val.string = requests->requestvb->buf;
            requests->requestvb->val_len = sizeof(requests->requestvb->buf);
        }
        else if(requests->requestvb->buf == requests->requestvb->val.string) {
            if(requests->requestvb->val_len != sizeof(requests->requestvb->buf))
                requests->requestvb->val_len = sizeof(requests->requestvb->buf);
        }

        /*
         * get column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _dot11ConfigTrapSwitchTable_get_column(rowreq_ctx, requests->requestvb, tri->colnum);
        if(rc) {
            if(MFD_SKIP == rc) {
                requests->requestvb->type = ASN_PRIV_RETRY;
                rc = SNMP_ERR_NOERROR;
            }
        }
        else if (NULL == requests->requestvb->val.string) {
            snmp_log(LOG_ERR,"NULL varbind data pointer!\n");
            rc = SNMP_ERR_GENERR;
        }
        if(rc)
            netsnmp_request_set_error(requests, SNMP_VALIDATE_ERR(rc));

        /*
         * if the buffer wasn't used previously for the old data (i.e. it
         * was allcoated memory)  and the get routine replaced the pointer,
         * we need to free the previous pointer.
         */
        if(old_string && (old_string != requests->requestvb->buf) &&
           (requests->requestvb->val.string != old_string)) {
            if(dataFreeHook)
                (*dataFreeHook)(old_string);
            else
                free(old_string);
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigTrapSwitchTable_get_values */

/***********************************************************************
 *
 * SET processing
 *
 ***********************************************************************/

/*----------------------------------------------------------------------
 *
 * SET: Syntax checks
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Check the syntax for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigTrapSwitchTable_check_column( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx,
                         netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_dot11ConfigTrapSwitchTable_check_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* APInterfThreshhd(2)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_APINTERFTHRESHHD:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.APInterfThreshhd ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = APInterfThreshhd_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from APInterfThreshhd_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* StaInterfThreshhd(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_STAINTERFTHRESHHD:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.StaInterfThreshhd ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = StaInterfThreshhd_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from StaInterfThreshhd_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* CPUusageThreshhd(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_CPUUSAGETHRESHHD:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.CPUusageThreshhd ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, 1,9999)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = CPUusageThreshhd_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from CPUusageThreshhd_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* MemUsageThreshhd(5)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_MEMUSAGETHRESHHD:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.MemUsageThreshhd ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, 1,99)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = MemUsageThreshhd_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from MemUsageThreshhd_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* CoChAPInterfThreshhd(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_COCHAPINTERFTHRESHHD:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.CoChAPInterfThreshhd ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, -120,0)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = CoChAPInterfThreshhd_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from CoChAPInterfThreshhd_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* AdjacentChAPInterfThreshhd(7)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_ADJACENTCHAPINTERFTHRESHHD:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.AdjacentChAPInterfThreshhd ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, -120,0)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = AdjacentChAPInterfThreshhd_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from AdjacentChAPInterfThreshhd_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

        default: /** We shouldn't get here */
            rc = SNMP_ERR_GENERR;
            snmp_log(LOG_ERR, "unknown column %d in _dot11ConfigTrapSwitchTable_check_column\n", column);
    }

    return rc;
} /* _dot11ConfigTrapSwitchTable_check_column */

int
_mfd_dot11ConfigTrapSwitchTable_check_objects(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    int                          rc;

    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_mfd_dot11ConfigTrapSwitchTable_check_objects","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    for(;requests; requests = requests->next) {

        /*
         * get column number from table request info, and check that column
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;

        rc = _dot11ConfigTrapSwitchTable_check_column(rowreq_ctx, requests->requestvb, tri->colnum);
        if(rc) {
            netsnmp_request_set_error(requests, SNMP_VALIDATE_ERR(rc));
            break;
        }

    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigTrapSwitchTable_check_objects */



/*----------------------------------------------------------------------
 *
 * SET: check dependencies
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Check dependencies wrapper
 */
static int
_mfd_dot11ConfigTrapSwitchTable_check_dependencies(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_mfd_dot11ConfigTrapSwitchTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    rc = dot11ConfigTrapSwitchTable_check_dependencies(rowreq_ctx);
    if(rc){
        DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:mfd","error %d from "
                    "dot11ConfigTrapSwitchTable_check_dependencies\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigTrapSwitchTable_check_dependencies */

/*----------------------------------------------------------------------
 *
 * SET: Undo setup
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Set the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigTrapSwitchTable_undo_setup_column( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_dot11ConfigTrapSwitchTable_undo_setup_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* APInterfThreshhd(2)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_APINTERFTHRESHHD:
        rowreq_ctx->column_set_flags |= FLAG_APINTERFTHRESHHD;
        rc = APInterfThreshhd_undo_setup(rowreq_ctx );
        break;

    /* StaInterfThreshhd(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_STAINTERFTHRESHHD:
        rowreq_ctx->column_set_flags |= FLAG_STAINTERFTHRESHHD;
        rc = StaInterfThreshhd_undo_setup(rowreq_ctx );
        break;

    /* CPUusageThreshhd(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_CPUUSAGETHRESHHD:
        rowreq_ctx->column_set_flags |= FLAG_CPUUSAGETHRESHHD;
        rc = CPUusageThreshhd_undo_setup(rowreq_ctx );
        break;

    /* MemUsageThreshhd(5)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_MEMUSAGETHRESHHD:
        rowreq_ctx->column_set_flags |= FLAG_MEMUSAGETHRESHHD;
        rc = MemUsageThreshhd_undo_setup(rowreq_ctx );
        break;

    /* CoChAPInterfThreshhd(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_COCHAPINTERFTHRESHHD:
        rowreq_ctx->column_set_flags |= FLAG_COCHAPINTERFTHRESHHD;
        rc = CoChAPInterfThreshhd_undo_setup(rowreq_ctx );
        break;

    /* AdjacentChAPInterfThreshhd(7)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_ADJACENTCHAPINTERFTHRESHHD:
        rowreq_ctx->column_set_flags |= FLAG_ADJACENTCHAPINTERFTHRESHHD;
        rc = AdjacentChAPInterfThreshhd_undo_setup(rowreq_ctx );
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigTrapSwitchTable_undo_setup_column\n", column);
         break;
    }

    return rc;
} /* _dot11ConfigTrapSwitchTable_undo_setup_column */


/**
 * @internal
 * undo setup
 */
int
_mfd_dot11ConfigTrapSwitchTable_undo_setup(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_mfd_dot11ConfigTrapSwitchTable_undo_setup","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * allocate undo context
     */
    rowreq_ctx->undo = dot11ConfigTrapSwitchTable_allocate_data();
    if(NULL == rowreq_ctx->undo) {
        /** msg already logged */
        netsnmp_request_set_error_all(requests, SNMP_ERR_RESOURCEUNAVAILABLE);
        return SNMP_ERR_NOERROR;
    }

    /*
     * row undo setup
     */
    rowreq_ctx->column_set_flags = 0;
    rc = dot11ConfigTrapSwitchTable_undo_setup(rowreq_ctx);
    if (MFD_SUCCESS != rc)  {
        DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:mfd","error %d from "
                    "dot11ConfigTrapSwitchTable_undo_setup\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }
    else {
        /*
         * column undo setup
         */
        netsnmp_table_request_info * tri;
        for(;requests; requests = requests->next) {
            /*
             * set column data
             */
            tri = netsnmp_extract_table_info(requests);
            if(NULL == tri)
                continue;
            
            rc = _dot11ConfigTrapSwitchTable_undo_setup_column(rowreq_ctx, tri->colnum);
            if(MFD_SUCCESS != rc)  {
                DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:mfd","error %d from "
                            "dot11ConfigTrapSwitchTable_undo_setup_column\n", rc));
                netsnmp_set_request_error(agtreq_info, requests, SNMP_VALIDATE_ERR(rc));
            }
        } /* for results */
    }
    
    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigTrapSwitchTable_undo_setup */

/**
 * @internal
 * undo setup
 */
int
_mfd_dot11ConfigTrapSwitchTable_undo_cleanup(netsnmp_mib_handler *handler,
                             netsnmp_handler_registration *reginfo,
                             netsnmp_agent_request_info *agtreq_info,
                             netsnmp_request_info *requests)
{
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    int rc;

    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_mfd_dot11ConfigTrapSwitchTable_undo_cleanup","called\n"));

    /*
     * failed row create in early stages has no rowreq_ctx
     */
    if (NULL == rowreq_ctx)
        return MFD_SUCCESS;

    /*
     * call user cleanup
     */
    rc = dot11ConfigTrapSwitchTable_undo_cleanup(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:mfd","error %d from "
                    "dot11ConfigTrapSwitchTable_undo_cleanup\n", rc));
    }

    /*
     * release undo context, if needed
     */
    if(rowreq_ctx->undo) {
         dot11ConfigTrapSwitchTable_release_data(rowreq_ctx->undo);
         rowreq_ctx->undo = NULL;
    }

    /*
     * clear set flags
     */
    rowreq_ctx->column_set_flags = 0;

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigTrapSwitchTable_undo_cleanup */

/*----------------------------------------------------------------------
 *
 * SET: Set values
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Set the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigTrapSwitchTable_set_column( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_dot11ConfigTrapSwitchTable_set_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* APInterfThreshhd(2)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_APINTERFTHRESHHD:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_APINTERFTHRESHHD;
        rc = APInterfThreshhd_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* StaInterfThreshhd(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_STAINTERFTHRESHHD:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_STAINTERFTHRESHHD;
        rc = StaInterfThreshhd_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* CPUusageThreshhd(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_CPUUSAGETHRESHHD:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_CPUUSAGETHRESHHD;
        rc = CPUusageThreshhd_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* MemUsageThreshhd(5)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_MEMUSAGETHRESHHD:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_MEMUSAGETHRESHHD;
        rc = MemUsageThreshhd_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* CoChAPInterfThreshhd(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_COCHAPINTERFTHRESHHD:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_COCHAPINTERFTHRESHHD;
        rc = CoChAPInterfThreshhd_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* AdjacentChAPInterfThreshhd(7)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_ADJACENTCHAPINTERFTHRESHHD:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_ADJACENTCHAPINTERFTHRESHHD;
        rc = AdjacentChAPInterfThreshhd_set(rowreq_ctx, *((long *)var->val.string) );
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigTrapSwitchTable_set_column\n", column);
         break;
    }
    
    return rc;
} /* _dot11ConfigTrapSwitchTable_set_column */

int
_mfd_dot11ConfigTrapSwitchTable_set_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    int                          rc = SNMP_ERR_NOERROR;

    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_mfd_dot11ConfigTrapSwitchTable_set_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    rowreq_ctx->column_set_flags = 0;
    for(;requests; requests = requests->next) {
        /*
         * set column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _dot11ConfigTrapSwitchTable_set_column(rowreq_ctx,
                                    requests->requestvb, tri->colnum);
        if(MFD_SUCCESS != rc)  {
            DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:mfd","error %d from "
                        "dot11ConfigTrapSwitchTable_set_column\n", rc));
            netsnmp_set_request_error(agtreq_info, requests, SNMP_VALIDATE_ERR(rc));
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigTrapSwitchTable_set_values */

/*----------------------------------------------------------------------
 *
 * SET: commit
 *
 *---------------------------------------------------------------------*/
/**
 * @internal
 * commit the values
 */
int
_mfd_dot11ConfigTrapSwitchTable_commit(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_mfd_dot11ConfigTrapSwitchTable_commit","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    rc = dot11ConfigTrapSwitchTable_commit(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:mfd","error %d from "
                    "dot11ConfigTrapSwitchTable_commit\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }

    return SNMP_ERR_NOERROR;
}

int
_mfd_dot11ConfigTrapSwitchTable_undo_commit(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_mfd_dot11ConfigTrapSwitchTable_undo_commit","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    rc = dot11ConfigTrapSwitchTable_undo_commit(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:mfd","error %d from "
                    "dot11ConfigTrapSwitchTable_undo_commit\n", rc));
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigTrapSwitchTable_commit */

/*----------------------------------------------------------------------
 *
 * SET: Undo
 *
 *---------------------------------------------------------------------*/
/**
 * @internal
 * undo the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigTrapSwitchTable_undo_column( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_dot11ConfigTrapSwitchTable_undo_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* APInterfThreshhd(2)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_APINTERFTHRESHHD:
        rc = APInterfThreshhd_undo(rowreq_ctx);
        break;

    /* StaInterfThreshhd(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_STAINTERFTHRESHHD:
        rc = StaInterfThreshhd_undo(rowreq_ctx);
        break;

    /* CPUusageThreshhd(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_CPUUSAGETHRESHHD:
        rc = CPUusageThreshhd_undo(rowreq_ctx);
        break;

    /* MemUsageThreshhd(5)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_MEMUSAGETHRESHHD:
        rc = MemUsageThreshhd_undo(rowreq_ctx);
        break;

    /* CoChAPInterfThreshhd(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_COCHAPINTERFTHRESHHD:
        rc = CoChAPInterfThreshhd_undo(rowreq_ctx);
        break;

    /* AdjacentChAPInterfThreshhd(7)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_ADJACENTCHAPINTERFTHRESHHD:
        rc = AdjacentChAPInterfThreshhd_undo(rowreq_ctx);
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigTrapSwitchTable_undo_column\n", column);
         break;
    }

    return rc;
} /* _dot11ConfigTrapSwitchTable_undo_column */

int
_mfd_dot11ConfigTrapSwitchTable_undo_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;

    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_mfd_dot11ConfigTrapSwitchTable_undo_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    for(;requests; requests = requests->next) {
        /*
         * set column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _dot11ConfigTrapSwitchTable_undo_column(rowreq_ctx, requests->requestvb,
                                     tri->colnum);
        if (MFD_SUCCESS != rc) {
            /*
             * nothing we can do about it but log it
             */
            DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:mfd","error %d from "
                        "dot11ConfigTrapSwitchTable_undo_column\n", rc));
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigTrapSwitchTable_undo_values */

/***********************************************************************
 *
 * DATA ACCESS
 *
 ***********************************************************************/
/**
 * @internal
 */
static int
_cache_load(netsnmp_cache *cache, void *vmagic)
{
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_cache_load","called\n"));

    if((NULL == cache) || (NULL == cache->magic)) {
        snmp_log(LOG_ERR, "invalid cache for dot11ConfigTrapSwitchTable_cache_load\n");
        return -1;
    }

    /** should only be called for an invalid or expired cache */
    netsnmp_assert((0 == cache->valid) || (1 == cache->expired));
    
    /*
     * call user code
     */
    return dot11ConfigTrapSwitchTable_cache_load((netsnmp_container*)cache->magic);
} /* _cache_load */

/**
 * @internal
 */
static void
_cache_item_free(dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, void *context)
{
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_cache_item_free","called\n"));

    if(NULL == rowreq_ctx)
        return;

    dot11ConfigTrapSwitchTable_release_rowreq_ctx(rowreq_ctx);
} /* _cache_item_free */

/**
 * @internal
 */
static void
_cache_free(netsnmp_cache *cache, void *magic)
{
    netsnmp_container *container;

    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_cache_free","called\n"));

    if((NULL == cache) || (NULL == cache->magic)) {
        snmp_log(LOG_ERR, "invalid cache in dot11ConfigTrapSwitchTable_cache_free\n");
        return;
    }

    container = (netsnmp_container*)cache->magic;

    /*
     * call user code
     */
    dot11ConfigTrapSwitchTable_cache_free(container);
    
    /*
     * free all items. inefficient, but easy.
     */
    CONTAINER_CLEAR(container,
                    (netsnmp_container_obj_func *)_cache_item_free,
                    NULL);
} /* _cache_free */

/**
 * @internal
 * initialize the iterator container with functions or wrappers
 */
void
_dot11ConfigTrapSwitchTable_container_init(dot11ConfigTrapSwitchTable_interface_ctx *if_ctx)
{
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:_dot11ConfigTrapSwitchTable_container_init","called\n"));
    
    /*
     * set up the cache
     */
    if_ctx->cache = netsnmp_cache_create(30, /* timeout in seconds */
                                         _cache_load, _cache_free,
                                         dot11ConfigTrapSwitchTable_oid,
                                         dot11ConfigTrapSwitchTable_oid_size);

    if(NULL == if_ctx->cache) {
        snmp_log(LOG_ERR, "error creating cache for dot11ConfigTrapSwitchTable\n");
        return;
    }

    if_ctx->cache->flags = NETSNMP_CACHE_DONT_INVALIDATE_ON_SET;

    dot11ConfigTrapSwitchTable_container_init(&if_ctx->container, if_ctx->cache);
    if(NULL == if_ctx->container)
        if_ctx->container = netsnmp_container_find("dot11ConfigTrapSwitchTable:table_container");
    if(NULL == if_ctx->container) {
        snmp_log(LOG_ERR,"error creating container in "
                 "dot11ConfigTrapSwitchTable_container_init\n");
        return;
    }
    if_ctx->cache->magic = (void*)if_ctx->container;
} /* _dot11ConfigTrapSwitchTable_container_init */

