/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigTrapSwitchTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11ConfigTrapSwitchTable get routines.
 * TODO:240:M: Implement dot11ConfigTrapSwitchTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigTrapSwitchTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigTrapSwitchTable is subid 2 of dot11AlarmConfiguration.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11ConfigTrapSwitchTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11ConfigTrapSwitchTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11ConfigTrapSwitchTable_indexes_set_tbl_idx(dot11ConfigTrapSwitchTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11ConfigTrapSwitchTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11ConfigTrapSwitchTable_indexes_set(dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11ConfigTrapSwitchTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11ConfigTrapSwitchTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11ConfigTrapSwitchTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.APInterfThreshhd
 * APInterfThreshhd is subid 2 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.2
 * Description:
AP Interference NumThreshhd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-APInterfThreshhd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
APInterfThreshhd_map(long *mib_APInterfThreshhd_val_ptr, long raw_APInterfThreshhd_val)
{
    netsnmp_assert(NULL != mib_APInterfThreshhd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:APInterfThreshhd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement APInterfThreshhd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_APInterfThreshhd_val_ptr) = raw_APInterfThreshhd_val;

    return MFD_SUCCESS;
} /* APInterfThreshhd_map */

/**
 * Extract the current value of the APInterfThreshhd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param APInterfThreshhd_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
APInterfThreshhd_get( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long * APInterfThreshhd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != APInterfThreshhd_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:APInterfThreshhd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the APInterfThreshhd data.
 * set (* APInterfThreshhd_val_ptr ) from rowreq_ctx->data
 */
    (* APInterfThreshhd_val_ptr ) = rowreq_ctx->data.APInterfThreshhd;

    return MFD_SUCCESS;
} /* APInterfThreshhd_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.StaInterfThreshhd
 * StaInterfThreshhd is subid 3 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.3
 * Description:
Station Interference Threshhd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-StaInterfThreshhd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
StaInterfThreshhd_map(long *mib_StaInterfThreshhd_val_ptr, long raw_StaInterfThreshhd_val)
{
    netsnmp_assert(NULL != mib_StaInterfThreshhd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:StaInterfThreshhd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement StaInterfThreshhd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_StaInterfThreshhd_val_ptr) = raw_StaInterfThreshhd_val;

    return MFD_SUCCESS;
} /* StaInterfThreshhd_map */

/**
 * Extract the current value of the StaInterfThreshhd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param StaInterfThreshhd_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
StaInterfThreshhd_get( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long * StaInterfThreshhd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != StaInterfThreshhd_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:StaInterfThreshhd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the StaInterfThreshhd data.
 * set (* StaInterfThreshhd_val_ptr ) from rowreq_ctx->data
 */
    (* StaInterfThreshhd_val_ptr ) = rowreq_ctx->data.StaInterfThreshhd;

    return MFD_SUCCESS;
} /* StaInterfThreshhd_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.CPUusageThreshhd
 * CPUusageThreshhd is subid 4 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.4
 * Description:
CPU Threshhd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 9999;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-CPUusageThreshhd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
CPUusageThreshhd_map(long *mib_CPUusageThreshhd_val_ptr, long raw_CPUusageThreshhd_val)
{
    netsnmp_assert(NULL != mib_CPUusageThreshhd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CPUusageThreshhd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement CPUusageThreshhd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_CPUusageThreshhd_val_ptr) = raw_CPUusageThreshhd_val;

    return MFD_SUCCESS;
} /* CPUusageThreshhd_map */

/**
 * Extract the current value of the CPUusageThreshhd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param CPUusageThreshhd_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
CPUusageThreshhd_get( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long * CPUusageThreshhd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != CPUusageThreshhd_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CPUusageThreshhd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the CPUusageThreshhd data.
 * set (* CPUusageThreshhd_val_ptr ) from rowreq_ctx->data
 */
    (* CPUusageThreshhd_val_ptr ) = rowreq_ctx->data.CPUusageThreshhd;

    return MFD_SUCCESS;
} /* CPUusageThreshhd_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.MemUsageThreshhd
 * MemUsageThreshhd is subid 5 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.5
 * Description:
Memory Threshhd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 99;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-MemUsageThreshhd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
MemUsageThreshhd_map(long *mib_MemUsageThreshhd_val_ptr, long raw_MemUsageThreshhd_val)
{
    netsnmp_assert(NULL != mib_MemUsageThreshhd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:MemUsageThreshhd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement MemUsageThreshhd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_MemUsageThreshhd_val_ptr) = raw_MemUsageThreshhd_val;

    return MFD_SUCCESS;
} /* MemUsageThreshhd_map */

/**
 * Extract the current value of the MemUsageThreshhd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param MemUsageThreshhd_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
MemUsageThreshhd_get( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long * MemUsageThreshhd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != MemUsageThreshhd_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:MemUsageThreshhd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the MemUsageThreshhd data.
 * set (* MemUsageThreshhd_val_ptr ) from rowreq_ctx->data
 */
    (* MemUsageThreshhd_val_ptr ) = rowreq_ctx->data.MemUsageThreshhd;

    return MFD_SUCCESS;
} /* MemUsageThreshhd_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.CoChAPInterfThreshhd
 * CoChAPInterfThreshhd is subid 6 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.6
 * Description:
AP interf of the same channel.cheyilong add  20101013
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - -120;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-CoChAPInterfThreshhd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
CoChAPInterfThreshhd_map(long *mib_CoChAPInterfThreshhd_val_ptr, long raw_CoChAPInterfThreshhd_val)
{
    netsnmp_assert(NULL != mib_CoChAPInterfThreshhd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CoChAPInterfThreshhd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement CoChAPInterfThreshhd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_CoChAPInterfThreshhd_val_ptr) = raw_CoChAPInterfThreshhd_val;

    return MFD_SUCCESS;
} /* CoChAPInterfThreshhd_map */

/**
 * Extract the current value of the CoChAPInterfThreshhd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param CoChAPInterfThreshhd_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
CoChAPInterfThreshhd_get( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long * CoChAPInterfThreshhd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != CoChAPInterfThreshhd_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CoChAPInterfThreshhd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the CoChAPInterfThreshhd data.
 * set (* CoChAPInterfThreshhd_val_ptr ) from rowreq_ctx->data
 */
    (* CoChAPInterfThreshhd_val_ptr ) = rowreq_ctx->data.CoChAPInterfThreshhd;

    return MFD_SUCCESS;
} /* CoChAPInterfThreshhd_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.AdjacentChAPInterfThreshhd
 * AdjacentChAPInterfThreshhd is subid 7 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.7
 * Description:
AP interf of Adjacent Channel.cheyilong add  20101013
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - -120;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-AdjacentChAPInterfThreshhd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
AdjacentChAPInterfThreshhd_map(long *mib_AdjacentChAPInterfThreshhd_val_ptr, long raw_AdjacentChAPInterfThreshhd_val)
{
    netsnmp_assert(NULL != mib_AdjacentChAPInterfThreshhd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:AdjacentChAPInterfThreshhd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement AdjacentChAPInterfThreshhd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_AdjacentChAPInterfThreshhd_val_ptr) = raw_AdjacentChAPInterfThreshhd_val;

    return MFD_SUCCESS;
} /* AdjacentChAPInterfThreshhd_map */

/**
 * Extract the current value of the AdjacentChAPInterfThreshhd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param AdjacentChAPInterfThreshhd_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
AdjacentChAPInterfThreshhd_get( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long * AdjacentChAPInterfThreshhd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != AdjacentChAPInterfThreshhd_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:AdjacentChAPInterfThreshhd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the AdjacentChAPInterfThreshhd data.
 * set (* AdjacentChAPInterfThreshhd_val_ptr ) from rowreq_ctx->data
 */
    (* AdjacentChAPInterfThreshhd_val_ptr ) = rowreq_ctx->data.AdjacentChAPInterfThreshhd;

    return MFD_SUCCESS;
} /* AdjacentChAPInterfThreshhd_get */



/** @} */
