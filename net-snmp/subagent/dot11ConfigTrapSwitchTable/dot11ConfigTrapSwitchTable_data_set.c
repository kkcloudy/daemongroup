/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigTrapSwitchTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigTrapSwitchTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigTrapSwitchTable is subid 2 of dot11AlarmConfiguration.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapSwitchTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigTrapSwitchTable_undo_setup( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigTrapSwitchTable undo.
     * set up dot11ConfigTrapSwitchTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11ConfigTrapSwitchTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapSwitchTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigTrapSwitchTable_undo_cleanup( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigTrapSwitchTable undo.
     */

    return rc;
} /* dot11ConfigTrapSwitchTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigTrapSwitchTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigTrapSwitchTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigTrapSwitchTable_commit( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11ConfigTrapSwitchTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_APINTERFTHRESHHD) {
       save_flags &= ~FLAG_APINTERFTHRESHHD; /* clear APInterfThreshhd */
       /*
        * TODO:482:o: |-> commit column APInterfThreshhd.
        */
//       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapSwitchTable column APInterfThreshhd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo APInterfThreshhd
             */
            rowreq_ctx->column_set_flags |= FLAG_APINTERFTHRESHHD;
       }
    }

    if (save_flags & FLAG_STAINTERFTHRESHHD) {
       save_flags &= ~FLAG_STAINTERFTHRESHHD; /* clear StaInterfThreshhd */
       /*
        * TODO:482:o: |-> commit column StaInterfThreshhd.
        */
//       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapSwitchTable column StaInterfThreshhd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo StaInterfThreshhd
             */
            rowreq_ctx->column_set_flags |= FLAG_STAINTERFTHRESHHD;
       }
    }

    if (save_flags & FLAG_CPUUSAGETHRESHHD) {
       save_flags &= ~FLAG_CPUUSAGETHRESHHD; /* clear CPUusageThreshhd */
       /*
        * TODO:482:o: |-> commit column CPUusageThreshhd.
        */
 //      rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapSwitchTable column CPUusageThreshhd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo CPUusageThreshhd
             */
            rowreq_ctx->column_set_flags |= FLAG_CPUUSAGETHRESHHD;
       }
    }

    if (save_flags & FLAG_MEMUSAGETHRESHHD) {
       save_flags &= ~FLAG_MEMUSAGETHRESHHD; /* clear MemUsageThreshhd */
       /*
        * TODO:482:o: |-> commit column MemUsageThreshhd.
        */
//       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapSwitchTable column MemUsageThreshhd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo MemUsageThreshhd
             */
            rowreq_ctx->column_set_flags |= FLAG_MEMUSAGETHRESHHD;
       }
    }

    if (save_flags & FLAG_COCHAPINTERFTHRESHHD) {
       save_flags &= ~FLAG_COCHAPINTERFTHRESHHD; /* clear CoChAPInterfThreshhd */
       /*
        * TODO:482:o: |-> commit column CoChAPInterfThreshhd.
        */
//       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapSwitchTable column CoChAPInterfThreshhd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo CoChAPInterfThreshhd
             */
            rowreq_ctx->column_set_flags |= FLAG_COCHAPINTERFTHRESHHD;
       }
    }

    if (save_flags & FLAG_ADJACENTCHAPINTERFTHRESHHD) {
       save_flags &= ~FLAG_ADJACENTCHAPINTERFTHRESHHD; /* clear AdjacentChAPInterfThreshhd */
       /*
        * TODO:482:o: |-> commit column AdjacentChAPInterfThreshhd.
        */
//       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigTrapSwitchTable column AdjacentChAPInterfThreshhd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo AdjacentChAPInterfThreshhd
             */
            rowreq_ctx->column_set_flags |= FLAG_ADJACENTCHAPINTERFTHRESHHD;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigTrapSwitchTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigTrapSwitchTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigTrapSwitchTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigTrapSwitchTable_undo_commit( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigTrapSwitchTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11ConfigTrapSwitchTable_undo_commit */

/*
 * TODO:420:r: Implement dot11ConfigTrapSwitchTable index validation.
 */
/*
 * TODO:440:M: Implement dot11ConfigTrapSwitchTable node value checks.
 * TODO:450:M: Implement dot11ConfigTrapSwitchTable undo functions.
 * TODO:460:M: Implement dot11ConfigTrapSwitchTable set functions.
 * TODO:480:M: Implement dot11ConfigTrapSwitchTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.APInterfThreshhd
 * APInterfThreshhd is subid 2 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.2
 * Description:
AP Interference NumThreshhd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param APInterfThreshhd_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapSwitchTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
APInterfThreshhd_check_value( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long APInterfThreshhd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:APInterfThreshhd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid APInterfThreshhd value.
     */

    return MFD_SUCCESS; /* APInterfThreshhd value not illegal */
} /* APInterfThreshhd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapSwitchTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapSwitchTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
APInterfThreshhd_undo_setup( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:APInterfThreshhd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup APInterfThreshhd undo.
     */
    /*
     * copy APInterfThreshhd data
     * set rowreq_ctx->undo->APInterfThreshhd from rowreq_ctx->data.APInterfThreshhd
     */
    rowreq_ctx->undo->APInterfThreshhd = rowreq_ctx->data.APInterfThreshhd;


    return MFD_SUCCESS;
} /* APInterfThreshhd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param APInterfThreshhd_val
 *        A long containing the new value.
 */
int
APInterfThreshhd_set( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long APInterfThreshhd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:APInterfThreshhd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement APInterfThreshhd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set APInterfThreshhd value.
     * set APInterfThreshhd value in rowreq_ctx->data
     */

    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	int ret = 0;
	int rc = MFD_ERROR;
	char value[10]={0};
	memset(value,0,10);
	snprintf(value,sizeof(value)-1,"%d",APInterfThreshhd_val);

	ret = set_wtp_trap_threshold_cmd(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpCurrID,"rogueap",&value);
	if(1 == ret)
	{
		rowreq_ctx->data.APInterfThreshhd = APInterfThreshhd_val;
		rc = MFD_SUCCESS;
	}
	else
	{						
		rc = MFD_ERROR;
	}
	
    return rc;
} /* APInterfThreshhd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
APInterfThreshhd_undo( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:APInterfThreshhd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up APInterfThreshhd undo.
     */
    /*
     * copy APInterfThreshhd data
     * set rowreq_ctx->data.APInterfThreshhd from rowreq_ctx->undo->APInterfThreshhd
     */
    rowreq_ctx->data.APInterfThreshhd = rowreq_ctx->undo->APInterfThreshhd;

    
    return MFD_SUCCESS;
} /* APInterfThreshhd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.StaInterfThreshhd
 * StaInterfThreshhd is subid 3 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.3
 * Description:
Station Interference Threshhd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param StaInterfThreshhd_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapSwitchTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
StaInterfThreshhd_check_value( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long StaInterfThreshhd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:StaInterfThreshhd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid StaInterfThreshhd value.
     */

    return MFD_SUCCESS; /* StaInterfThreshhd value not illegal */
} /* StaInterfThreshhd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapSwitchTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapSwitchTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
StaInterfThreshhd_undo_setup( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:StaInterfThreshhd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup StaInterfThreshhd undo.
     */
    /*
     * copy StaInterfThreshhd data
     * set rowreq_ctx->undo->StaInterfThreshhd from rowreq_ctx->data.StaInterfThreshhd
     */
    rowreq_ctx->undo->StaInterfThreshhd = rowreq_ctx->data.StaInterfThreshhd;


    return MFD_SUCCESS;
} /* StaInterfThreshhd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param StaInterfThreshhd_val
 *        A long containing the new value.
 */
int
StaInterfThreshhd_set( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long StaInterfThreshhd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:StaInterfThreshhd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement StaInterfThreshhd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set StaInterfThreshhd value.
     * set StaInterfThreshhd value in rowreq_ctx->data
     */


    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	int ret = 0;
	int rc = MFD_ERROR;
	char value[10]={0};
	memset(value,0,10);
	snprintf(value,sizeof(value)-1,"%d",StaInterfThreshhd_val);

	ret = set_wtp_trap_threshold_cmd(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpCurrID,"rogueterminal",&value);
	if(1 == ret)
	{
		rowreq_ctx->data.StaInterfThreshhd = StaInterfThreshhd_val;
		rc = MFD_SUCCESS;
	}
	else
	{						
		rc = MFD_ERROR;
	}

    return rc;
} /* StaInterfThreshhd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
StaInterfThreshhd_undo( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:StaInterfThreshhd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up StaInterfThreshhd undo.
     */
    /*
     * copy StaInterfThreshhd data
     * set rowreq_ctx->data.StaInterfThreshhd from rowreq_ctx->undo->StaInterfThreshhd
     */
    rowreq_ctx->data.StaInterfThreshhd = rowreq_ctx->undo->StaInterfThreshhd;

    
    return MFD_SUCCESS;
} /* StaInterfThreshhd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.CPUusageThreshhd
 * CPUusageThreshhd is subid 4 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.4
 * Description:
CPU Threshhd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 9999;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param CPUusageThreshhd_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapSwitchTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 9999
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
CPUusageThreshhd_check_value( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long CPUusageThreshhd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CPUusageThreshhd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid CPUusageThreshhd value.
     */

    return MFD_SUCCESS; /* CPUusageThreshhd value not illegal */
} /* CPUusageThreshhd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapSwitchTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapSwitchTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
CPUusageThreshhd_undo_setup( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CPUusageThreshhd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup CPUusageThreshhd undo.
     */
    /*
     * copy CPUusageThreshhd data
     * set rowreq_ctx->undo->CPUusageThreshhd from rowreq_ctx->data.CPUusageThreshhd
     */
    rowreq_ctx->undo->CPUusageThreshhd = rowreq_ctx->data.CPUusageThreshhd;


    return MFD_SUCCESS;
} /* CPUusageThreshhd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param CPUusageThreshhd_val
 *        A long containing the new value.
 */
int
CPUusageThreshhd_set( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long CPUusageThreshhd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CPUusageThreshhd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement CPUusageThreshhd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set CPUusageThreshhd value.
     * set CPUusageThreshhd value in rowreq_ctx->data
     */

    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	int ret = 0;
	int rc = MFD_ERROR;
	char value[10]={0};
	memset(value,0,10);
	snprintf(value,sizeof(value)-1,"%d",CPUusageThreshhd_val);

	ret = set_wtp_trap_threshold_cmd(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpCurrID,"cpu",&value);
	if(1 == ret)
	{
		rowreq_ctx->data.CPUusageThreshhd = CPUusageThreshhd_val;
		rc = MFD_SUCCESS;
	}
	else
	{						
		rc = MFD_ERROR;
	}


    return rc;
} /* CPUusageThreshhd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
CPUusageThreshhd_undo( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CPUusageThreshhd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up CPUusageThreshhd undo.
     */
    /*
     * copy CPUusageThreshhd data
     * set rowreq_ctx->data.CPUusageThreshhd from rowreq_ctx->undo->CPUusageThreshhd
     */
    rowreq_ctx->data.CPUusageThreshhd = rowreq_ctx->undo->CPUusageThreshhd;

    
    return MFD_SUCCESS;
} /* CPUusageThreshhd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.MemUsageThreshhd
 * MemUsageThreshhd is subid 5 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.5
 * Description:
Memory Threshhd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 99;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param MemUsageThreshhd_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapSwitchTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 99
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
MemUsageThreshhd_check_value( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long MemUsageThreshhd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:MemUsageThreshhd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid MemUsageThreshhd value.
     */

    return MFD_SUCCESS; /* MemUsageThreshhd value not illegal */
} /* MemUsageThreshhd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapSwitchTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapSwitchTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
MemUsageThreshhd_undo_setup( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:MemUsageThreshhd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup MemUsageThreshhd undo.
     */
    /*
     * copy MemUsageThreshhd data
     * set rowreq_ctx->undo->MemUsageThreshhd from rowreq_ctx->data.MemUsageThreshhd
     */
    rowreq_ctx->undo->MemUsageThreshhd = rowreq_ctx->data.MemUsageThreshhd;


    return MFD_SUCCESS;
} /* MemUsageThreshhd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param MemUsageThreshhd_val
 *        A long containing the new value.
 */
int
MemUsageThreshhd_set( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long MemUsageThreshhd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:MemUsageThreshhd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement MemUsageThreshhd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set MemUsageThreshhd value.
     * set MemUsageThreshhd value in rowreq_ctx->data
     */

    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	int ret = 0;
	char value[10]={0};
	memset(value,0,10);
	int rc = MFD_ERROR;
	snprintf(value,sizeof(value)-1,"%d",MemUsageThreshhd_val);

	ret = set_wtp_trap_threshold_cmd(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpCurrID,"memory",&value);
	if(1 == ret)
	{
		rowreq_ctx->data.MemUsageThreshhd = MemUsageThreshhd_val;
		rc = MFD_SUCCESS;
	}
	else
	{						
		rc = MFD_ERROR;
	}

    return rc;
} /* MemUsageThreshhd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
MemUsageThreshhd_undo( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:MemUsageThreshhd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up MemUsageThreshhd undo.
     */
    /*
     * copy MemUsageThreshhd data
     * set rowreq_ctx->data.MemUsageThreshhd from rowreq_ctx->undo->MemUsageThreshhd
     */
    rowreq_ctx->data.MemUsageThreshhd = rowreq_ctx->undo->MemUsageThreshhd;

    
    return MFD_SUCCESS;
} /* MemUsageThreshhd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.CoChAPInterfThreshhd
 * CoChAPInterfThreshhd is subid 6 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.6
 * Description:
AP interf of the same channel.cheyilong add  20101013
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - -120;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param CoChAPInterfThreshhd_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapSwitchTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - -120
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
CoChAPInterfThreshhd_check_value( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long CoChAPInterfThreshhd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CoChAPInterfThreshhd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid CoChAPInterfThreshhd value.
     */

    return MFD_SUCCESS; /* CoChAPInterfThreshhd value not illegal */
} /* CoChAPInterfThreshhd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapSwitchTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapSwitchTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
CoChAPInterfThreshhd_undo_setup( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CoChAPInterfThreshhd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup CoChAPInterfThreshhd undo.
     */
    /*
     * copy CoChAPInterfThreshhd data
     * set rowreq_ctx->undo->CoChAPInterfThreshhd from rowreq_ctx->data.CoChAPInterfThreshhd
     */
    rowreq_ctx->undo->CoChAPInterfThreshhd = rowreq_ctx->data.CoChAPInterfThreshhd;


    return MFD_SUCCESS;
} /* CoChAPInterfThreshhd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param CoChAPInterfThreshhd_val
 *        A long containing the new value.
 */
int
CoChAPInterfThreshhd_set( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long CoChAPInterfThreshhd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CoChAPInterfThreshhd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement CoChAPInterfThreshhd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set CoChAPInterfThreshhd value.
     * set CoChAPInterfThreshhd value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
    
	int ret = 0;
	char value[10] = {0};
	memset(value,0,10);
	int rc = MFD_ERROR;
	snprintf(value,sizeof(value)-1,"%d",CoChAPInterfThreshhd_val);
	if((CoChAPInterfThreshhd_val <= 0) && ( CoChAPInterfThreshhd_val >= -120))
	{
        
		ret = set_wtp_trap_neighbor_same_channelrssithreshold_cmd(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpCurrID, "samechannelrssi", &value);
		if(1 == ret)
		{
			rowreq_ctx->data.CoChAPInterfThreshhd = CoChAPInterfThreshhd_val;
			rc = MFD_SUCCESS;
		}
		else
		{						
			rc = MFD_ERROR;
		}
	}
	else
	{						
		rc = MFD_ERROR;
	}
	
    return rc;
} /* CoChAPInterfThreshhd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
CoChAPInterfThreshhd_undo( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:CoChAPInterfThreshhd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up CoChAPInterfThreshhd undo.
     */
    /*
     * copy CoChAPInterfThreshhd data
     * set rowreq_ctx->data.CoChAPInterfThreshhd from rowreq_ctx->undo->CoChAPInterfThreshhd
     */
    rowreq_ctx->data.CoChAPInterfThreshhd = rowreq_ctx->undo->CoChAPInterfThreshhd;

    
    return MFD_SUCCESS;
} /* CoChAPInterfThreshhd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.AdjacentChAPInterfThreshhd
 * AdjacentChAPInterfThreshhd is subid 7 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.7
 * Description:
AP interf of Adjacent Channel.cheyilong add  20101013
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - -120;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param AdjacentChAPInterfThreshhd_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigTrapSwitchTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - -120
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
AdjacentChAPInterfThreshhd_check_value( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long AdjacentChAPInterfThreshhd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:AdjacentChAPInterfThreshhd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid AdjacentChAPInterfThreshhd value.
     */

    return MFD_SUCCESS; /* AdjacentChAPInterfThreshhd value not illegal */
} /* AdjacentChAPInterfThreshhd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigTrapSwitchTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigTrapSwitchTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
AdjacentChAPInterfThreshhd_undo_setup( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:AdjacentChAPInterfThreshhd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup AdjacentChAPInterfThreshhd undo.
     */
    /*
     * copy AdjacentChAPInterfThreshhd data
     * set rowreq_ctx->undo->AdjacentChAPInterfThreshhd from rowreq_ctx->data.AdjacentChAPInterfThreshhd
     */
    rowreq_ctx->undo->AdjacentChAPInterfThreshhd = rowreq_ctx->data.AdjacentChAPInterfThreshhd;


    return MFD_SUCCESS;
} /* AdjacentChAPInterfThreshhd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param AdjacentChAPInterfThreshhd_val
 *        A long containing the new value.
 */
int
AdjacentChAPInterfThreshhd_set( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx, long AdjacentChAPInterfThreshhd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:AdjacentChAPInterfThreshhd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement AdjacentChAPInterfThreshhd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set AdjacentChAPInterfThreshhd value.
     * set AdjacentChAPInterfThreshhd value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int ret = 0;
	char value[10] = {0};
	memset(value,0,10);
	int rc = MFD_ERROR;
	snprintf(value,sizeof(value)-1,"%d",AdjacentChAPInterfThreshhd_val);
	if((AdjacentChAPInterfThreshhd_val <= 0) && ( AdjacentChAPInterfThreshhd_val >= -120))
	{
        
		ret = set_wtp_trap_neighbor_same_channelrssithreshold_cmd(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpCurrID, "neighborchannelrssi", &value);
		if(1 == ret)
		{
			rowreq_ctx->data.CoChAPInterfThreshhd = AdjacentChAPInterfThreshhd_val;
			rc = MFD_SUCCESS;
		}
		else
		{						
			rc = MFD_ERROR;
		}
	}
	else
	{						
		rc = MFD_ERROR;
	}

    return rc;
} /* AdjacentChAPInterfThreshhd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
AdjacentChAPInterfThreshhd_undo( dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigTrapSwitchTable:AdjacentChAPInterfThreshhd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up AdjacentChAPInterfThreshhd undo.
     */
    /*
     * copy AdjacentChAPInterfThreshhd data
     * set rowreq_ctx->data.AdjacentChAPInterfThreshhd from rowreq_ctx->undo->AdjacentChAPInterfThreshhd
     */
    rowreq_ctx->data.AdjacentChAPInterfThreshhd = rowreq_ctx->undo->AdjacentChAPInterfThreshhd;

    
    return MFD_SUCCESS;
} /* AdjacentChAPInterfThreshhd_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigTrapSwitchTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigTrapSwitchTable if you don't have dependencies)
 */
int
dot11ConfigTrapSwitchTable_check_dependencies(dot11ConfigTrapSwitchTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigTrapSwitchTable:dot11ConfigTrapSwitchTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigTrapSwitchTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11ConfigTrapSwitchTable_check_dependencies */

/** @} */
