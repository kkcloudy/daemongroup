/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpEthPortTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpEthPortTable get routines.
 * TODO:240:M: Implement dot11WtpEthPortTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpEthPortTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpEthPortTable is subid 2 of wtpStats.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpEthPortTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */






int
wtpMacAddr_dot11WtpEthPortTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */



















/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpEthPortTable_indexes_set_tbl_idx(dot11WtpEthPortTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:dot11WtpEthPortTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11WtpEthPortTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpEthPortTable_indexes_set(dot11WtpEthPortTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:dot11WtpEthPortTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpEthPortTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpEthPortTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpEthPortTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEthPortEntry.wtpWirelessUpPortRate
 * wtpWirelessUpPortRate is subid 1 of dot11WtpEthPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.2.1.1
 * Description:
Wireless uplink port traffic during the sample time.(Unit kbps)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessUpPortRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessUpPortRate_map(u_long *mib_wtpWirelessUpPortRate_val_ptr, u_long raw_wtpWirelessUpPortRate_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessUpPortRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessUpPortRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessUpPortRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessUpPortRate_val_ptr) = raw_wtpWirelessUpPortRate_val;

    return MFD_SUCCESS;
} /* wtpWirelessUpPortRate_map */

/**
 * Extract the current value of the wtpWirelessUpPortRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessUpPortRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpWirelessUpPortRate_get( dot11WtpEthPortTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpWirelessUpPortRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWirelessUpPortRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessUpPortRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessUpPortRate data.
 * set (* wtpWirelessUpPortRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWirelessUpPortRate_val_ptr ) = rowreq_ctx->data.wtpWirelessUpPortRate;

    return MFD_SUCCESS;
} /* wtpWirelessUpPortRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEthPortEntry.wtpWirelessDownPortRate
 * wtpWirelessDownPortRate is subid 2 of dot11WtpEthPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.2.1.2
 * Description:
Wireless downlink port traffic during the sample time.(Unit kbps)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessDownPortRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessDownPortRate_map(u_long *mib_wtpWirelessDownPortRate_val_ptr, u_long raw_wtpWirelessDownPortRate_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessDownPortRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessDownPortRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessDownPortRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessDownPortRate_val_ptr) = raw_wtpWirelessDownPortRate_val;

    return MFD_SUCCESS;
} /* wtpWirelessDownPortRate_map */

/**
 * Extract the current value of the wtpWirelessDownPortRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessDownPortRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpWirelessDownPortRate_get( dot11WtpEthPortTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpWirelessDownPortRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWirelessDownPortRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessDownPortRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessDownPortRate data.
 * set (* wtpWirelessDownPortRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWirelessDownPortRate_val_ptr ) = rowreq_ctx->data.wtpWirelessDownPortRate;

    return MFD_SUCCESS;
} /* wtpWirelessDownPortRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEthPortEntry.wtpWirelessUpPortUDTimes
 * wtpWirelessUpPortUDTimes is subid 3 of dot11WtpEthPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.2.1.3
 * Description:
Uplink wireless port updown times.(Times)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessUpPortUDTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessUpPortUDTimes_map(long *mib_wtpWirelessUpPortUDTimes_val_ptr, long raw_wtpWirelessUpPortUDTimes_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessUpPortUDTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessUpPortUDTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessUpPortUDTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessUpPortUDTimes_val_ptr) = raw_wtpWirelessUpPortUDTimes_val;

    return MFD_SUCCESS;
} /* wtpWirelessUpPortUDTimes_map */

/**
 * Extract the current value of the wtpWirelessUpPortUDTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessUpPortUDTimes_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpWirelessUpPortUDTimes_get( dot11WtpEthPortTable_rowreq_ctx *rowreq_ctx, long * wtpWirelessUpPortUDTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWirelessUpPortUDTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessUpPortUDTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessUpPortUDTimes data.
 * set (* wtpWirelessUpPortUDTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWirelessUpPortUDTimes_val_ptr ) = rowreq_ctx->data.wtpWirelessUpPortUDTimes;

    return MFD_SUCCESS;
} /* wtpWirelessUpPortUDTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEthPortEntry.wtpWirelessDownPortUDTimes
 * wtpWirelessDownPortUDTimes is subid 4 of dot11WtpEthPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.2.1.4
 * Description:
Downlink wireless port updown times.(Times)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessDownPortUDTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessDownPortUDTimes_map(long *mib_wtpWirelessDownPortUDTimes_val_ptr, long raw_wtpWirelessDownPortUDTimes_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessDownPortUDTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessDownPortUDTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessDownPortUDTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessDownPortUDTimes_val_ptr) = raw_wtpWirelessDownPortUDTimes_val;

    return MFD_SUCCESS;
} /* wtpWirelessDownPortUDTimes_map */

/**
 * Extract the current value of the wtpWirelessDownPortUDTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessDownPortUDTimes_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpWirelessDownPortUDTimes_get( dot11WtpEthPortTable_rowreq_ctx *rowreq_ctx, long * wtpWirelessDownPortUDTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWirelessDownPortUDTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessDownPortUDTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessDownPortUDTimes data.
 * set (* wtpWirelessDownPortUDTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWirelessDownPortUDTimes_val_ptr ) = rowreq_ctx->data.wtpWirelessDownPortUDTimes;

    return MFD_SUCCESS;
} /* wtpWirelessDownPortUDTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEthPortEntry.wtpUplinkDataThroughput
 * wtpUplinkDataThroughput is subid 5 of dot11WtpEthPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.2.1.5
 * Description:
The uplink port traffic of AP ethernet designated by AC.(bps)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpUplinkDataThroughput.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpUplinkDataThroughput_map(u_long *mib_wtpUplinkDataThroughput_val_ptr, u_long raw_wtpUplinkDataThroughput_val)
{
    netsnmp_assert(NULL != mib_wtpUplinkDataThroughput_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpUplinkDataThroughput_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpUplinkDataThroughput mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpUplinkDataThroughput_val_ptr) = raw_wtpUplinkDataThroughput_val;

    return MFD_SUCCESS;
} /* wtpUplinkDataThroughput_map */

/**
 * Extract the current value of the wtpUplinkDataThroughput data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUplinkDataThroughput_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpUplinkDataThroughput_get( dot11WtpEthPortTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpUplinkDataThroughput_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpUplinkDataThroughput_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpUplinkDataThroughput_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpUplinkDataThroughput data.
 * set (* wtpUplinkDataThroughput_val_ptr ) from rowreq_ctx->data
 */
    (* wtpUplinkDataThroughput_val_ptr ) = rowreq_ctx->data.wtpUplinkDataThroughput;

    return MFD_SUCCESS;
} /* wtpUplinkDataThroughput_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEthPortEntry.wtpDownlinkDataThroughput
 * wtpDownlinkDataThroughput is subid 6 of dot11WtpEthPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.2.1.6
 * Description:
The downlink port traffic of AP ethernet designated by AC.(bps).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpDownlinkDataThroughput.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpDownlinkDataThroughput_map(u_long *mib_wtpDownlinkDataThroughput_val_ptr, u_long raw_wtpDownlinkDataThroughput_val)
{
    netsnmp_assert(NULL != mib_wtpDownlinkDataThroughput_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpDownlinkDataThroughput_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpDownlinkDataThroughput mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpDownlinkDataThroughput_val_ptr) = raw_wtpDownlinkDataThroughput_val;

    return MFD_SUCCESS;
} /* wtpDownlinkDataThroughput_map */

/**
 * Extract the current value of the wtpDownlinkDataThroughput data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpDownlinkDataThroughput_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpDownlinkDataThroughput_get( dot11WtpEthPortTable_rowreq_ctx *rowreq_ctx, unsigned long * wtpDownlinkDataThroughput_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpDownlinkDataThroughput_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpDownlinkDataThroughput_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpDownlinkDataThroughput data.
 * set (* wtpDownlinkDataThroughput_val_ptr ) from rowreq_ctx->data
 */
    (* wtpDownlinkDataThroughput_val_ptr ) = rowreq_ctx->data.wtpDownlinkDataThroughput;

    return MFD_SUCCESS;
} /* wtpDownlinkDataThroughput_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpEthPortEntry.wtpPhyInterMnt
 * wtpPhyInterMnt is subid 7 of dot11WtpEthPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.2.1.7
 * Description:
The total number of physical interface in the system.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpPhyInterMnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpPhyInterMnt_map(long *mib_wtpPhyInterMnt_val_ptr, long raw_wtpPhyInterMnt_val)
{
    netsnmp_assert(NULL != mib_wtpPhyInterMnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpPhyInterMnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpPhyInterMnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpPhyInterMnt_val_ptr) = raw_wtpPhyInterMnt_val;

    return MFD_SUCCESS;
} /* wtpPhyInterMnt_map */

/**
 * Extract the current value of the wtpPhyInterMnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpPhyInterMnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpPhyInterMnt_get( dot11WtpEthPortTable_rowreq_ctx *rowreq_ctx, long * wtpPhyInterMnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpPhyInterMnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpPhyInterMnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpPhyInterMnt data.
 * set (* wtpPhyInterMnt_val_ptr ) from rowreq_ctx->data
 */
    (* wtpPhyInterMnt_val_ptr ) = rowreq_ctx->data.wtpPhyInterMnt;

    return MFD_SUCCESS;
} /* wtpPhyInterMnt_get */




int
wtpWirelessBandwidthUsage_map(char **mib_wtpWirelessBandwidthUsage_val_ptr_ptr, size_t *mib_wtpWirelessBandwidthUsage_val_ptr_len_ptr, char *raw_wtpWirelessBandwidthUsage_val_ptr, size_t raw_wtpWirelessBandwidthUsage_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpWirelessBandwidthUsage_val_ptr);
    netsnmp_assert((NULL != mib_wtpWirelessBandwidthUsage_val_ptr_ptr) && (NULL != mib_wtpWirelessBandwidthUsage_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessBandwidthUsage_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpUsrWirelessResoUseRate non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpWirelessBandwidthUsage_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpWirelessBandwidthUsage_val_ptr_ptr) || (*mib_wtpWirelessBandwidthUsage_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpWirelessBandwidthUsage_val_ptr_ptr = realloc( *mib_wtpWirelessBandwidthUsage_val_ptr_ptr, converted_len * sizeof(**mib_wtpWirelessBandwidthUsage_val_ptr_ptr));
        if(NULL == *mib_wtpWirelessBandwidthUsage_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpWirelessBandwidthUsage_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpWirelessBandwidthUsage_val_ptr_ptr, raw_wtpWirelessBandwidthUsage_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpUsrWirelessResoUseRate_map */

/**
 * Extract the current value of the wtpUsrWirelessResoUseRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUsrWirelessResoUseRate_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpUsrWirelessResoUseRate_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpUsrWirelessResoUseRate.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpUsrWirelessResoUseRate_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpUsrWirelessResoUseRate_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpWirelessBandwidthUsage_get( dot11WtpEthPortTable_rowreq_ctx *rowreq_ctx, char **wtpWirelessBandwidthUsage_val_ptr_ptr, size_t *wtpWirelessBandwidthUsage_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpWirelessBandwidthUsage_val_ptr_ptr) && (NULL != *wtpWirelessBandwidthUsage_val_ptr_ptr));
   netsnmp_assert( NULL != wtpWirelessBandwidthUsage_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessBandwidthUsage_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpUsrWirelessResoUseRate data.
 * set (* wtpUsrWirelessResoUseRate_val_ptr_ptr ) and (* wtpUsrWirelessResoUseRate_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpUsrWirelessResoUseRate data
     */
    if ((NULL == (*wtpWirelessBandwidthUsage_val_ptr_ptr )) ||
        ((* wtpWirelessBandwidthUsage_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpWirelessBandwidthUsage_len * sizeof((* wtpWirelessBandwidthUsage_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpUsrWirelessResoUseRate data
         */
        (* wtpWirelessBandwidthUsage_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpWirelessBandwidthUsage_len * sizeof((* wtpWirelessBandwidthUsage_val_ptr_ptr )[0]));
        if(NULL == (* wtpWirelessBandwidthUsage_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpWirelessBandwidthUsage_val_ptr_len_ptr ) = rowreq_ctx->data.wtpWirelessBandwidthUsage_len * sizeof((* wtpWirelessBandwidthUsage_val_ptr_ptr )[0]);
    memcpy( (* wtpWirelessBandwidthUsage_val_ptr_ptr ), rowreq_ctx->data.wtpWirelessBandwidthUsage, (* wtpWirelessBandwidthUsage_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpUsrWirelessResoUseRate_get */


int
wtpWirelessPortRxRate_get( dot11WtpEthPortTable_rowreq_ctx *rowreq_ctx, u_long * wtpWirelessPortRxRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWirelessPortRxRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessPortRxRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessPortRxRate data.
 * set (* wtpWirelessPortRxRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWirelessPortRxRate_val_ptr ) = rowreq_ctx->data.wtpWirelessPortRxRate;

    return MFD_SUCCESS;
} /* wtpWirelessPortRxRate_get */


int
wtpWirelessPortTxRate_get( dot11WtpEthPortTable_rowreq_ctx *rowreq_ctx, u_long * wtpWirelessPortTxRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWirelessPortTxRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpEthPortTable:wtpWirelessPortTxRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWirelessPortRxRate data.
 * set (* wtpWirelessPortTxRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWirelessPortTxRate_val_ptr ) = rowreq_ctx->data.wtpWirelessPortTxRate;

    return MFD_SUCCESS;
} /* wtpWirelessPortTxRate_get */




/** @} */
