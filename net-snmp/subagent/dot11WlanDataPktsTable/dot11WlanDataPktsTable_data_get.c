/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WlanDataPktsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WlanDataPktsTable get routines.
 * TODO:240:M: Implement dot11WlanDataPktsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WlanDataPktsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WlanDataPktsTable is subid 1 of wlanData.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.16.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WlanDataPktsTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wlanCurrID
 * wlanCurrID is subid 1 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.3.1.1
 * Description:
The wlan's ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wlanCurrID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wlanCurrID_dot11WlanDataPktsTable_map(long *mib_wlanCurrID_val_ptr, long raw_wlanCurrID_val)
{
    netsnmp_assert(NULL != mib_wlanCurrID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WlanDataPktsTable:wlanCurrID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wlanCurrID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wlanCurrID_val_ptr) = raw_wlanCurrID_val;

    return MFD_SUCCESS;
} /* wlanCurrID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WlanDataPktsTable_indexes_set_tbl_idx(dot11WlanDataPktsTable_mib_index *tbl_idx, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11WlanDataPktsTable:dot11WlanDataPktsTable_indexes_set_tbl_idx","called\n"));

    /* wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wlanCurrID = wlanCurrID_val;
    

    return MFD_SUCCESS;
} /* dot11WlanDataPktsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WlanDataPktsTable_indexes_set(dot11WlanDataPktsTable_rowreq_ctx *rowreq_ctx, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11WlanDataPktsTable:dot11WlanDataPktsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WlanDataPktsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wlanCurrID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WlanDataPktsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WlanDataPktsTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WlanDataPktsEntry.ethernetRecvCorrectFrames
 * ethernetRecvCorrectFrames is subid 1 of dot11WlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.16.1.1.1
 * Description:
The total bytes which received from terminal(Appointed SSID).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-ethernetRecvCorrectFrames.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
ethernetRecvCorrectFrames_map(u_long *mib_ethernetRecvCorrectFrames_val_ptr, u_long raw_ethernetRecvCorrectFrames_val)
{
    netsnmp_assert(NULL != mib_ethernetRecvCorrectFrames_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WlanDataPktsTable:ethernetRecvCorrectFrames_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement ethernetRecvCorrectFrames mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_ethernetRecvCorrectFrames_val_ptr) = raw_ethernetRecvCorrectFrames_val;

    return MFD_SUCCESS;
} /* ethernetRecvCorrectFrames_map */

/**
 * Extract the current value of the ethernetRecvCorrectFrames data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ethernetRecvCorrectFrames_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
ethernetRecvCorrectFrames_get( dot11WlanDataPktsTable_rowreq_ctx *rowreq_ctx, u_long * ethernetRecvCorrectFrames_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != ethernetRecvCorrectFrames_val_ptr );


    DEBUGMSGTL(("verbose:dot11WlanDataPktsTable:ethernetRecvCorrectFrames_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the ethernetRecvCorrectFrames data.
 * set (* ethernetRecvCorrectFrames_val_ptr ) from rowreq_ctx->data
 */
    (* ethernetRecvCorrectFrames_val_ptr ) = rowreq_ctx->data.ethernetRecvCorrectFrames;

    return MFD_SUCCESS;
} /* ethernetRecvCorrectFrames_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WlanDataPktsEntry.ethernetRecvCorrectBytes
 * ethernetRecvCorrectBytes is subid 2 of dot11WlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.16.1.1.2
 * Description:
The total packages which received from terminal(Appointed SSID).(bts)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-ethernetRecvCorrectBytes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
ethernetRecvCorrectBytes_map(unsigned long long *mib_ethernetRecvCorrectBytes_val_ptr, unsigned long long raw_ethernetRecvCorrectBytes_val)
{
    netsnmp_assert(NULL != mib_ethernetRecvCorrectBytes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WlanDataPktsTable:ethernetRecvCorrectBytes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement ethernetRecvCorrectBytes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_ethernetRecvCorrectBytes_val_ptr) = raw_ethernetRecvCorrectBytes_val;

    return MFD_SUCCESS;
} /* ethernetRecvCorrectBytes_map */

/**
 * Extract the current value of the ethernetRecvCorrectBytes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ethernetRecvCorrectBytes_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
ethernetRecvCorrectBytes_get( dot11WlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long long * ethernetRecvCorrectBytes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != ethernetRecvCorrectBytes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WlanDataPktsTable:ethernetRecvCorrectBytes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the ethernetRecvCorrectBytes data.
 * set (* ethernetRecvCorrectBytes_val_ptr ) from rowreq_ctx->data
 */
    (* ethernetRecvCorrectBytes_val_ptr ) = rowreq_ctx->data.ethernetRecvCorrectBytes;

    return MFD_SUCCESS;
} /* ethernetRecvCorrectBytes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WlanDataPktsEntry.ethernetSendCorrectBytes
 * ethernetSendCorrectBytes is subid 3 of dot11WlanDataPktsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.16.1.1.3
 * Description:
The total bytes which received from terminal(Appointed SSID).(bts)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-ethernetSendCorrectBytes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
ethernetSendCorrectBytes_map(unsigned long long *mib_ethernetSendCorrectBytes_val_ptr, unsigned long long raw_ethernetSendCorrectBytes_val)
{
    netsnmp_assert(NULL != mib_ethernetSendCorrectBytes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WlanDataPktsTable:ethernetSendCorrectBytes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement ethernetSendCorrectBytes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_ethernetSendCorrectBytes_val_ptr) = raw_ethernetSendCorrectBytes_val;

    return MFD_SUCCESS;
} /* ethernetSendCorrectBytes_map */

/**
 * Extract the current value of the ethernetSendCorrectBytes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ethernetSendCorrectBytes_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
ethernetSendCorrectBytes_get( dot11WlanDataPktsTable_rowreq_ctx *rowreq_ctx, unsigned long long * ethernetSendCorrectBytes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != ethernetSendCorrectBytes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WlanDataPktsTable:ethernetSendCorrectBytes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the ethernetSendCorrectBytes data.
 * set (* ethernetSendCorrectBytes_val_ptr ) from rowreq_ctx->data
 */
    (* ethernetSendCorrectBytes_val_ptr ) = rowreq_ctx->data.ethernetSendCorrectBytes;

    return MFD_SUCCESS;
} /* ethernetSendCorrectBytes_get */



/** @} */
