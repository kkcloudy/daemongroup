/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpTeminalTable.h"


#include "dot11WtpTeminalTable_data_access.h"
#include "mibs_public.h"
#include "autelanWtpGroup.h"
#include "ws_user_manage.h"
#include "ws_conf_engine.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "nm/app/eag/eag_conf.h"
#include "nm/public/nm_list.h"
#include "nm/app/eag/eag_interface.h"
#include "ws_dcli_ac.h"
#include "ws_dcli_wlans.h"
#include "ws_init_dbus.h"
/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpTeminalTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpTeminalTable is subid 1 of wlanPortal.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.1, length: 12
*/

/**
 * initialization for dot11WtpTeminalTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11WtpTeminalTable_reg
 *        Pointer to dot11WtpTeminalTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11WtpTeminalTable_init_data(dot11WtpTeminalTable_registration_ptr dot11WtpTeminalTable_reg)
{
    DEBUGMSGTL(("verbose:dot11WtpTeminalTable:dot11WtpTeminalTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11WtpTeminalTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11WtpTeminalTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11WtpTeminalTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11WtpTeminalTable:dot11WtpTeminalTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11WtpTeminalTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11WtpTeminalTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11WTPTEMINALTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11WtpTeminalTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11WtpTeminalTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11WtpTeminalTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11WtpTeminalTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11WtpTeminalTable_cache_load\n");
	
    dot11WtpTeminalTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
         */
        /** 128 - 1(entry) - 1(col) - 0(other indexes) = 112 */
   char   wtpMacAddr[20];
   size_t      wtpMacAddr_len;

    DEBUGMSGTL(("verbose:dot11WtpTeminalTable:dot11WtpTeminalTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
        //return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11WtpTeminalTable container.
     * loop over your dot11WtpTeminalTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    instance_parameter *paraHead = NULL;
	instance_parameter *pq = NULL;
	int result = 0;
	DCLI_WTP_API_GROUP_ONE *WTPINFO = NULL;
	WID_WTP *q = NULL;
	int i = 0;
	struct list_head ap_stat;
	struct eag_ap_stat *tmp = NULL;
	unsigned long portalCurusernum = 0;
	unsigned long portalReqnum = 0;
	unsigned long portalReqsuccnum = 0;
	unsigned long portalReqfailnum = 0;
	unsigned long portalErrLogoffnum = 0;
	unsigned long portalAllUserOnlineTime = 0;
	
	unsigned long macAuthCurusernum  = 0;
	unsigned long macAuthErrLogoffnum  = 0;
	unsigned long macAuthReqnum = 0;
	unsigned long macAuthReqsuccnum  = 0;
	unsigned long macAuthReqfailnum  = 0;
	unsigned long macAuthAllUserOnlineTime = 0;

    snmp_log(LOG_DEBUG, "enter list_instance_parameter\n");
	list_instance_parameter(&paraHead, SNMPD_INSTANCE_MASTER); 
	snmp_log(LOG_DEBUG, "exit list_instance_parameter,paraHead=%p\n", paraHead);
	for(pq = paraHead; (NULL != pq); pq = pq->next)
	{
		eag_get_ap_statistics(pq->connection,pq->parameter.local_id,pq->parameter.instance_id,&ap_stat);
		
		result=show_wtp_list_by_mac_cmd_func(pq->parameter,pq->connection,&WTPINFO);
		if((result == 1)&&(WTPINFO)&&(WTPINFO->WTP_INFO))
		{
			for (i = 0,q = WTPINFO->WTP_INFO->WTP_LIST; 
				((i < WTPINFO->WTP_INFO->list_len)&&(NULL != q)); 
				i++,q = q->next) 
		    {
		    	portalCurusernum = 0;
				portalReqnum = 0;
				portalReqsuccnum = 0;
				portalReqfailnum = 0;
				portalErrLogoffnum = 0;
				portalAllUserOnlineTime = 0;
				macAuthCurusernum  = 0;
				macAuthErrLogoffnum  = 0;
				macAuthReqnum = 0;
			    macAuthReqsuccnum  = 0;
				macAuthReqfailnum  = 0;
				macAuthAllUserOnlineTime = 0;

				memset(wtpMacAddr, 0, sizeof(wtpMacAddr));
				if(q->WTPMAC)
				{
					snprintf(wtpMacAddr, 
							 sizeof(wtpMacAddr)-1, 
							 "%02X:%02X:%02X:%02X:%02X:%02X",
							 q->WTPMAC[0],q->WTPMAC[1], 
							 q->WTPMAC[2],q->WTPMAC[3], 
							 q->WTPMAC[4],q->WTPMAC[5]);
				}
				
				list_for_each_entry(tmp,&ap_stat,node)
				{
					if((q->WTPMAC)&&(tmp)&&(memcmp(tmp->ap_mac, q->WTPMAC, 6) == 0))
					{
						portalCurusernum = tmp->online_user_num;	
						portalErrLogoffnum = tmp->abnormal_logoff_count;
						portalReqnum = tmp->auth_req_count;
						portalReqsuccnum = tmp->auth_ack_0_count;
						portalReqfailnum = (tmp->auth_ack_1_count + tmp->auth_ack_2_count
										+ tmp->auth_ack_3_count + tmp->auth_ack_4_count);
						portalAllUserOnlineTime = (tmp->user_connect_total_time)*100;
						
						macAuthCurusernum  = tmp->macauth_online_user_num;	
						macAuthErrLogoffnum  = tmp->macauth_abnormal_logoff_count;
						macAuthReqnum = tmp->macauth_req_count;
						macAuthReqsuccnum  = tmp->macauth_ack_0_count;
						macAuthReqfailnum  = (tmp->macauth_ack_1_count + tmp->macauth_ack_2_count
										+ tmp->macauth_ack_3_count + tmp->macauth_ack_4_count);
						macAuthAllUserOnlineTime = (tmp->macauth_user_connect_total_time)*100;
						break;
					}
				}
				//portalReqfailnum = portalReqnum - portalReqsuccnum;
	    /*
	    ***************************************************
	    ***             START EXAMPLE CODE              ***
	    ***---------------------------------------------***/
	    /*
	     * get a line (skip blank lines)
	     */
	    //do {
	        //if (!fgets(line, sizeof(line), filep)) {
	            /* we're done */
	            //fclose(filep);
	            //filep = NULL;
	        //}
	    //} while (filep && (line[0] == '\n'));

	    /*
	     * check for end of data
	     */
	    //if(NULL == filep)
	       // break;

	    /*
	     * parse line into variables
	     */
	    /*
	    ***---------------------------------------------***
	    ***              END  EXAMPLE CODE              ***
	    ***************************************************/

	        /*
	         * TODO:352:M: |   |-> set indexes in new dot11WtpTeminalTable rowreq context.
	         */
	        rowreq_ctx = dot11WtpTeminalTable_allocate_rowreq_ctx();
	        if (NULL == rowreq_ctx) {
	            snmp_log(LOG_ERR, "memory allocation failed\n");
				FREE_OBJECT(q->WTPMAC);
				Free_wtp_list_by_mac_head(WTPINFO);
				eag_free_ap_statistics(&ap_stat);
				free_instance_parameter_list(&paraHead);
	            return MFD_RESOURCE_UNAVAILABLE;
	        }
			wtpMacAddr_len = strlen(wtpMacAddr);
	        if(MFD_SUCCESS != dot11WtpTeminalTable_indexes_set(rowreq_ctx
	                               , wtpMacAddr, wtpMacAddr_len
	               )) {
	            snmp_log(LOG_ERR,"error setting index while loading "
	                     "dot11WtpTeminalTable cache.\n");
	            dot11WtpTeminalTable_release_rowreq_ctx(rowreq_ctx);
	            continue;
	        }

	        /*
	         * TODO:352:r: |   |-> populate dot11WtpTeminalTable data context.
	         * Populate data context here. (optionally, delay until row prep)
	         */
	    /*
	     * TRANSIENT or semi-TRANSIENT data:
	     * copy data or save any info needed to do it in row_prep.
	     */
	    /*
	     * setup/save data for portalCurusernum
	     * portalCurusernum(1)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define portalCurusernum mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.portalCurusernum = portalCurusernum;
	    
	    /*
	     * setup/save data for portalErrLogoffnum
	     * portalErrLogoffnum(2)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define portalErrLogoffnum mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.portalErrLogoffnum = portalErrLogoffnum;
	    
	    /*
	     * setup/save data for portalReqnum
	     * portalReqnum(3)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define portalReqnum mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.portalReqnum = portalReqnum;
	    
	    /*
	     * setup/save data for portalReqsuccnum
	     * portalReqsuccnum(4)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define portalReqsuccnum mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.portalReqsuccnum = portalReqsuccnum;
	    
	    /*
	     * setup/save data for portalReqfailnum
	     * portalReqfailnum(5)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define portalReqfailnum mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.portalReqfailnum = portalReqfailnum;
		rowreq_ctx->data.portalAllUserOnlineTime = portalAllUserOnlineTime;
	    
    /*
     * setup/save data for macAuthCurusernum
     * macAuthCurusernum(7)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.macAuthCurusernum = macAuthCurusernum;
    
    /*
     * setup/save data for macAuthErrLogoffnum
     * macAuthErrLogoffnum(8)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.macAuthErrLogoffnum = macAuthErrLogoffnum;
    
    /*
     * setup/save data for macAuthReqnum
     * macAuthReqnum(9)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.macAuthReqnum = macAuthReqnum;
    
    /*
     * setup/save data for macAuthReqsuccnum
     * macAuthReqsuccnum(10)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.macAuthReqsuccnum = macAuthReqsuccnum;
    
    /*
     * setup/save data for macAuthReqfailnum
     * macAuthReqfailnum(11)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.macAuthReqfailnum = macAuthReqfailnum;
    
    /*
     * setup/save data for macAuthAllUserOnlineTime
     * macAuthAllUserOnlineTime(12)/TICKS/ASN_TIMETICKS/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.macAuthAllUserOnlineTime = macAuthAllUserOnlineTime;
    
        
	        /*
	         * insert into table container
	         */
	        if(CONTAINER_INSERT(container, rowreq_ctx))			
			{
				dot11WtpTeminalTable_release_rowreq_ctx(rowreq_ctx);
			}
	        ++count;
			FREE_OBJECT(q->WTPMAC);
	    }	}
		if(result == 1)
		{
			Free_wtp_list_by_mac_head(WTPINFO);
		}
		eag_free_ap_statistics(&ap_stat);
	}
	free_instance_parameter_list(&paraHead);


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
        //fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11WtpTeminalTable:dot11WtpTeminalTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11WtpTeminalTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11WtpTeminalTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11WtpTeminalTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11WtpTeminalTable:dot11WtpTeminalTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11WtpTeminalTable cache.
     */
} /* dot11WtpTeminalTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11WtpTeminalTable_row_prep( dot11WtpTeminalTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpTeminalTable:dot11WtpTeminalTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11WtpTeminalTable_row_prep */

/** @} */
