/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11SpeedLimitTable.h"
#include "ws_tcrule.h"
#include "autelanWtpGroup.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11SpeedLimitTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11SpeedLimitTable is subid 1 of dot11SpeedLimit.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11SpeedLimitTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11SpeedLimitTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SpeedLimitTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11SpeedLimitTable_undo_setup( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:dot11SpeedLimitTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11SpeedLimitTable undo.
     * set up dot11SpeedLimitTable undo information, in preparation for a set.
     * Undo storage is in (* rowStatus_val_ptr )*
     */

    return rc;
} /* dot11SpeedLimitTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SpeedLimitTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11SpeedLimitTable_undo( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:dot11SpeedLimitTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11SpeedLimitTable undo.
     * dot11SpeedLimitTable undo information, in response to a failed set.
     * Undo storage is in (* rowStatus_val_ptr )*
     */

    return rc;
} /* dot11SpeedLimitTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SpeedLimitTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11SpeedLimitTable_undo_cleanup( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:dot11SpeedLimitTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11SpeedLimitTable undo.
     * Undo storage is in (* rowStatus_val_ptr )*
     */

    return rc;
} /* dot11SpeedLimitTable_undo_cleanup */

static int
check_create_ready(dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx) {
    if(strlen(rowreq_ctx->data.ipAddress) && strlen(rowreq_ctx->data.upInterface) && strlen(rowreq_ctx->data.downInterface)
		&& strlen(rowreq_ctx->data.upBandWidth) && strlen(rowreq_ctx->data.downBandWidth) && (rowreq_ctx->data.actionStatus!=0)) {
        return MFD_SUCCESS;
    }

    return MFD_ERROR;
}

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11SpeedLimitTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11SpeedLimitTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11SpeedLimitTable_commit( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:dot11SpeedLimitTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;
	
    if(RS_NOTREADY == rowreq_ctx->data.rowStatus && MFD_SUCCESS == check_create_ready(rowreq_ctx)) {
        rowreq_ctx->data.rowStatus = RS_NOTINSERVICE;
    }

    /*
     * commit dot11SpeedLimitTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_IPADDRESS_FLAG) {
       save_flags &= ~COLUMN_IPADDRESS_FLAG; /* clear ipAddress */
       /*
        * TODO:482:o: |-> commit column ipAddress.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11SpeedLimitTable column ipAddress commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo ipAddress
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPADDRESS_FLAG;
       }
    }

    if (save_flags & COLUMN_UPINTERFACE_FLAG) {
       save_flags &= ~COLUMN_UPINTERFACE_FLAG; /* clear upInterface */
       /*
        * TODO:482:o: |-> commit column upInterface.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11SpeedLimitTable column upInterface commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo upInterface
             */
            rowreq_ctx->column_set_flags |= COLUMN_UPINTERFACE_FLAG;
       }
    }

    if (save_flags & COLUMN_DOWNINTERFACE_FLAG) {
       save_flags &= ~COLUMN_DOWNINTERFACE_FLAG; /* clear downInterface */
       /*
        * TODO:482:o: |-> commit column downInterface.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11SpeedLimitTable column downInterface commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo downInterface
             */
            rowreq_ctx->column_set_flags |= COLUMN_DOWNINTERFACE_FLAG;
       }
    }

    if (save_flags & COLUMN_UPBANDWIDTH_FLAG) {
       save_flags &= ~COLUMN_UPBANDWIDTH_FLAG; /* clear upBandWidth */
       /*
        * TODO:482:o: |-> commit column upBandWidth.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11SpeedLimitTable column upBandWidth commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo upBandWidth
             */
            rowreq_ctx->column_set_flags |= COLUMN_UPBANDWIDTH_FLAG;
       }
    }

    if (save_flags & COLUMN_DOWNBANDWIDTH_FLAG) {
       save_flags &= ~COLUMN_DOWNBANDWIDTH_FLAG; /* clear downBandWidth */
       /*
        * TODO:482:o: |-> commit column downBandWidth.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11SpeedLimitTable column downBandWidth commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo downBandWidth
             */
            rowreq_ctx->column_set_flags |= COLUMN_DOWNBANDWIDTH_FLAG;
       }
    }

    if (save_flags & COLUMN_ACTIONSTATUS_FLAG) {
       save_flags &= ~COLUMN_ACTIONSTATUS_FLAG; /* clear actionStatus */
       /*
        * TODO:482:o: |-> commit column actionStatus.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11SpeedLimitTable column actionStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo actionStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_ACTIONSTATUS_FLAG;
       }
    }

    if (save_flags & COLUMN_ROWSTATUS_FLAG) {
        save_flags &= ~COLUMN_ROWSTATUS_FLAG; /* clear rowStatus */
        switch(rowreq_ctx->data.rowStatus) {
            case RS_ACTIVE:
                {
					if(0 == rowreq_ctx->data.is_modify)
					{
						PTCRule ptcRuleRoot=NULL;
						PTCRule ptcRuleNew=NULL;
						ptcRuleRoot = tcParseDoc( TCRULES_XML_FILE );
						int ip1=0,ip2= 0,ip3 =0,ip4 = 0;
						int ret = 0;
						int mask = 32;
						int addr_status = 0;
						ptcRuleNew = tcNewRule();
						if( NULL != ptcRuleNew )
						{
							ret  = sscanf(rowreq_ctx->data.ipAddress,"%d.%d.%d.%d",&ip1,&ip2,&ip3,&ip4);
							if(ret == 4)
							{
								if(!(((ip1>255)||(ip1<0))||((ip2>255)||(ip2<0))||((ip3>255)||(ip3<0))||((ip4>255)||(ip4<0))))
								{
									if(ip4==0)
									{
										mask=24;
									}
									FREE_OBJECT(ptcRuleNew->addr_begin);
									ptcRuleNew->addr_begin = strdup( rowreq_ctx->data.ipAddress);
									FREE_OBJECT(ptcRuleNew->addr_end);
									if(mask==24)
									{
										ptcRuleNew->addr_end = strdup("255.255.255.0");
										addr_status = 1;
									}
									else
									{
										ptcRuleNew->addr_end = strdup("255.255.255.255");
										addr_status = 2;
									}
									FREE_OBJECT(ptcRuleNew->uplink_speed);
									ptcRuleNew->uplink_speed = strdup(rowreq_ctx->data.upBandWidth);
									FREE_OBJECT(ptcRuleNew->downlink_speed);
									ptcRuleNew->downlink_speed = strdup(rowreq_ctx->data.downBandWidth);
									FREE_OBJECT(ptcRuleNew->interface);
									ptcRuleNew->interface = strdup(rowreq_ctx->data.downInterface);
									FREE_OBJECT(ptcRuleNew->up_interface);
									ptcRuleNew->up_interface = strdup(rowreq_ctx->data.upInterface);
									ptcRuleNew->enable = (rowreq_ctx->data.actionStatus==2)?0:1;
									FREE_OBJECT(ptcRuleNew->addrtype);
									if(addr_status == 1)
									{
										ptcRuleNew->addrtype = strdup("addrrange");
									}
									else
									{
										ptcRuleNew->addrtype = strdup("address");
									}
								}
								else
								{
									rc = -1;
									tcFreeRule(ptcRuleNew);
									tcFreeList(ptcRuleRoot);
									break;
								}
							}
							else
							{
								rc = -1;
								tcFreeRule(ptcRuleNew);
								tcFreeList(ptcRuleRoot);
								break;
							}
							
							ptcRuleNew->ruleIndex = rowreq_ctx->tbl_idx.index;
							ptcRuleRoot = insertRule(ptcRuleRoot,ptcRuleNew,ptcRuleNew->ruleIndex); 					
							tcSaveDoc( TCRULES_XML_FILE, ptcRuleRoot );
							tc_doAllRules( ptcRuleRoot );
//							tcFreeRule(ptcRuleNew);
							tcFreeList(ptcRuleRoot);
							rowreq_ctx->data.is_modify = 1;
							rc = MFD_SUCCESS;
							break;
						}
					}
					else
				    {
				   	    rc = MFD_SUCCESS;
					    break;
				    }
                }
                rc = -1;
                break;

            case RS_CREATEANDWAIT:
                rowreq_ctx->data.rowStatus = RS_NOTREADY;
                rc = MFD_SUCCESS;
                break;
                
            case RS_DESTROY:  
                {
					PTCRule ptcRuleRoot=NULL;
					ptcRuleRoot = tcParseDoc( TCRULES_XML_FILE );
					ptcRuleRoot = deleteRule( ptcRuleRoot, (rowreq_ctx->tbl_idx.index)-1);
					tcSaveDoc( TCRULES_XML_FILE, ptcRuleRoot );
					tc_doAllRules( ptcRuleRoot );
					tcFreeList( ptcRuleRoot );
					rc = MFD_SUCCESS;
                    break;
                }
                rowreq_ctx->rowreq_flags &= ~MFD_ROW_DELETED;
                rc = -1;
                break;
                
            default:
                break;
        }
       /*
        * TODO:482:o: |-> commit column rowStatus.
        */
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11SpeedLimitTable column rowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo rowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_ROWSTATUS_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11SpeedLimitTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11SpeedLimitTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11SpeedLimitTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11SpeedLimitTable_undo_commit( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:dot11SpeedLimitTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11SpeedLimitTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11SpeedLimitTable_undo_commit */

/*
 * TODO:440:M: Implement dot11SpeedLimitTable node value checks.
 * TODO:450:M: Implement dot11SpeedLimitTable undo functions.
 * TODO:460:M: Implement dot11SpeedLimitTable set functions.
 * TODO:480:M: Implement dot11SpeedLimitTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.ipAddress
 * ipAddress is subid 2 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.2
 * Description:
Ip Address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ipAddress_val_ptr
 *        A char containing the new value.
 * @param ipAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by ipAddress_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11SpeedLimitTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.ipAddress).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
ipAddress_check_value( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char *ipAddress_val_ptr,  size_t ipAddress_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:ipAddress_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != ipAddress_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid ipAddress value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus || RS_NOTREADY == rowreq_ctx->data.rowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* ipAddress value not illegal */
} /* ipAddress_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SpeedLimitTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11SpeedLimitTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
ipAddress_undo_setup( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:ipAddress_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup ipAddress undo.
     */
    /*
     * copy ipAddress and ipAddress_len data
     * set rowreq_ctx->undo->ipAddress from rowreq_ctx->data.ipAddress
     */
    memcpy( rowreq_ctx->undo->ipAddress, rowreq_ctx->data.ipAddress,
            (rowreq_ctx->data.ipAddress_len * sizeof(rowreq_ctx->undo->ipAddress[0])));
    rowreq_ctx->undo->ipAddress_len = rowreq_ctx->data.ipAddress_len;


    return MFD_SUCCESS;
} /* ipAddress_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param ipAddress_val_ptr
 *        A char containing the new value.
 * @param ipAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by ipAddress_val_ptr
 */
int
ipAddress_set( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char *ipAddress_val_ptr,  size_t ipAddress_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:ipAddress_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != ipAddress_val_ptr);

    /*
     * TODO:461:M: |-> Set ipAddress value.
     * set ipAddress value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus && rowreq_ctx->data.is_modify) /*modify*/
	{
		PTCRule ptcRuleRoot=NULL;
		PTCRule ptcRuleNew=NULL;
		ptcRuleRoot = tcParseDoc( TCRULES_XML_FILE );
		int ip1=0,ip2= 0,ip3 =0,ip4 = 0;
		int ret = 0;
		int mask = 32;
		int addr_status = 0;
		ptcRuleNew = getTCRuleByIndex(ptcRuleRoot,(rowreq_ctx->tbl_idx.index)-1);
		if( NULL == ptcRuleNew )
		{
			tcFreeList(ptcRuleRoot);
			return MFD_ERROR;
		}
		ret  = sscanf(rowreq_ctx->data.ipAddress,"%d.%d.%d.%d",&ip1,&ip2,&ip3,&ip4);
		if(ret == 4)
		{
			if(!(((ip1>255)||(ip1<0))||((ip2>255)||(ip2<0))||((ip3>255)||(ip3<0))||((ip4>255)||(ip4<0))))
			{
				if(ip4==0)
				{
					mask=24;
				}
				FREE_OBJECT(ptcRuleNew->addr_begin);
				ptcRuleNew->addr_begin = strdup( ipAddress_val_ptr);
				FREE_OBJECT(ptcRuleNew->addr_end);
				if(mask==24)
				{

					ptcRuleNew->addr_end = strdup("255.255.255.0");
					addr_status = 1;
				}
				else
				{
					ptcRuleNew->addr_end = strdup("255.255.255.255");
					addr_status = 2;
				}
			}
		}

		tcSaveDoc( TCRULES_XML_FILE, ptcRuleRoot );
		tc_doAllRules( ptcRuleRoot );
		tcFreeList(ptcRuleRoot);
		memset(rowreq_ctx->data.ipAddress, 0, sizeof(rowreq_ctx->data.ipAddress));	
		memcpy( rowreq_ctx->data.ipAddress, ipAddress_val_ptr, ipAddress_val_ptr_len );
	    rowreq_ctx->data.ipAddress_len = ipAddress_val_ptr_len / sizeof(ipAddress_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else if(RS_NOTREADY == rowreq_ctx->data.rowStatus)/*creat and wait*/
	{
		memcpy( rowreq_ctx->data.ipAddress, ipAddress_val_ptr, ipAddress_val_ptr_len );
	    rowreq_ctx->data.ipAddress_len = ipAddress_val_ptr_len / sizeof(ipAddress_val_ptr[0]);
		rc = MFD_SUCCESS;
	}

    return rc;
} /* ipAddress_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
ipAddress_undo( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:ipAddress_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up ipAddress undo.
     */
    /*
     * copy ipAddress and ipAddress_len data
     * set rowreq_ctx->data.ipAddress from rowreq_ctx->undo->ipAddress
     */
    memcpy( rowreq_ctx->data.ipAddress, rowreq_ctx->undo->ipAddress,
            (rowreq_ctx->undo->ipAddress_len * sizeof(rowreq_ctx->data.ipAddress[0])));
    rowreq_ctx->data.ipAddress_len = rowreq_ctx->undo->ipAddress_len;

    
    return MFD_SUCCESS;
} /* ipAddress_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.upInterface
 * upInterface is subid 3 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.3
 * Description:
UP interface.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param upInterface_val_ptr
 *        A char containing the new value.
 * @param upInterface_val_ptr_len
 *        The size (in bytes) of the data pointed to by upInterface_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11SpeedLimitTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.upInterface).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
upInterface_check_value( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char *upInterface_val_ptr,  size_t upInterface_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:upInterface_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != upInterface_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid upInterface value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus || RS_NOTREADY == rowreq_ctx->data.rowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* upInterface value not illegal */
} /* upInterface_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SpeedLimitTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11SpeedLimitTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
upInterface_undo_setup( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:upInterface_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup upInterface undo.
     */
    /*
     * copy upInterface and upInterface_len data
     * set rowreq_ctx->undo->upInterface from rowreq_ctx->data.upInterface
     */
    memcpy( rowreq_ctx->undo->upInterface, rowreq_ctx->data.upInterface,
            (rowreq_ctx->data.upInterface_len * sizeof(rowreq_ctx->undo->upInterface[0])));
    rowreq_ctx->undo->upInterface_len = rowreq_ctx->data.upInterface_len;


    return MFD_SUCCESS;
} /* upInterface_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param upInterface_val_ptr
 *        A char containing the new value.
 * @param upInterface_val_ptr_len
 *        The size (in bytes) of the data pointed to by upInterface_val_ptr
 */
int
upInterface_set( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char *upInterface_val_ptr,  size_t upInterface_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:upInterface_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != upInterface_val_ptr);

    /*
     * TODO:461:M: |-> Set upInterface value.
     * set upInterface value in rowreq_ctx->data
     */

    int rc = MFD_ERROR;
	if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus && rowreq_ctx->data.is_modify) /*modify*/
	{
		PTCRule ptcRuleRoot=NULL;
		PTCRule ptcRuleNew=NULL;
		ptcRuleRoot = tcParseDoc( TCRULES_XML_FILE );
		ptcRuleNew = getTCRuleByIndex(ptcRuleRoot,(rowreq_ctx->tbl_idx.index)-1);
		if( NULL == ptcRuleNew )
		{
			tcFreeList(ptcRuleRoot);
			return MFD_ERROR;
		}
		FREE_OBJECT(ptcRuleNew->up_interface);
		ptcRuleNew->up_interface= strdup(upInterface_val_ptr);					
		tcSaveDoc( TCRULES_XML_FILE, ptcRuleRoot );
		tc_doAllRules( ptcRuleRoot );
		tcFreeList(ptcRuleRoot);

		memset(rowreq_ctx->data.upInterface, 0, sizeof(rowreq_ctx->data.upInterface));	
		memcpy( rowreq_ctx->data.upInterface, upInterface_val_ptr, upInterface_val_ptr_len );
	    rowreq_ctx->data.upInterface_len = upInterface_val_ptr_len / sizeof(upInterface_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else if(RS_NOTREADY == rowreq_ctx->data.rowStatus)/*creat and wait*/
	{
		memcpy( rowreq_ctx->data.upInterface, upInterface_val_ptr, upInterface_val_ptr_len );
	    rowreq_ctx->data.upInterface_len = upInterface_val_ptr_len / sizeof(upInterface_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
    return rc;
} /* upInterface_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
upInterface_undo( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:upInterface_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up upInterface undo.
     */
    /*
     * copy upInterface and upInterface_len data
     * set rowreq_ctx->data.upInterface from rowreq_ctx->undo->upInterface
     */
    memcpy( rowreq_ctx->data.upInterface, rowreq_ctx->undo->upInterface,
            (rowreq_ctx->undo->upInterface_len * sizeof(rowreq_ctx->data.upInterface[0])));
    rowreq_ctx->data.upInterface_len = rowreq_ctx->undo->upInterface_len;

    
    return MFD_SUCCESS;
} /* upInterface_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.downInterface
 * downInterface is subid 4 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.4
 * Description:
Down interface.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param downInterface_val_ptr
 *        A char containing the new value.
 * @param downInterface_val_ptr_len
 *        The size (in bytes) of the data pointed to by downInterface_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11SpeedLimitTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.downInterface).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
downInterface_check_value( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char *downInterface_val_ptr,  size_t downInterface_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:downInterface_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != downInterface_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid downInterface value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus || RS_NOTREADY == rowreq_ctx->data.rowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* downInterface value not illegal */
} /* downInterface_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SpeedLimitTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11SpeedLimitTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
downInterface_undo_setup( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:downInterface_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup downInterface undo.
     */
    /*
     * copy downInterface and downInterface_len data
     * set rowreq_ctx->undo->downInterface from rowreq_ctx->data.downInterface
     */
    memcpy( rowreq_ctx->undo->downInterface, rowreq_ctx->data.downInterface,
            (rowreq_ctx->data.downInterface_len * sizeof(rowreq_ctx->undo->downInterface[0])));
    rowreq_ctx->undo->downInterface_len = rowreq_ctx->data.downInterface_len;


    return MFD_SUCCESS;
} /* downInterface_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param downInterface_val_ptr
 *        A char containing the new value.
 * @param downInterface_val_ptr_len
 *        The size (in bytes) of the data pointed to by downInterface_val_ptr
 */
int
downInterface_set( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char *downInterface_val_ptr,  size_t downInterface_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:downInterface_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != downInterface_val_ptr);

    /*
     * TODO:461:M: |-> Set downInterface value.
     * set downInterface value in rowreq_ctx->data
     */

    int rc = MFD_ERROR;
	if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus && rowreq_ctx->data.is_modify) /*modify*/
	{
		PTCRule ptcRuleRoot=NULL;
		PTCRule ptcRuleNew=NULL;
		ptcRuleRoot = tcParseDoc( TCRULES_XML_FILE );
		ptcRuleNew = getTCRuleByIndex(ptcRuleRoot,(rowreq_ctx->tbl_idx.index)-1);
		if( NULL == ptcRuleNew )
		{
			tcFreeList(ptcRuleRoot);
			return MFD_ERROR;
		}
		FREE_OBJECT(ptcRuleNew->interface);
		ptcRuleNew->interface= strdup(downInterface_val_ptr);					
		tcSaveDoc( TCRULES_XML_FILE, ptcRuleRoot );
		tc_doAllRules( ptcRuleRoot );
		tcFreeList(ptcRuleRoot);

		memset(rowreq_ctx->data.downInterface, 0, sizeof(rowreq_ctx->data.downInterface));	
		memcpy( rowreq_ctx->data.downInterface, downInterface_val_ptr, downInterface_val_ptr_len );
	    rowreq_ctx->data.downInterface_len = downInterface_val_ptr_len / sizeof(downInterface_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else if(RS_NOTREADY == rowreq_ctx->data.rowStatus)/*creat and wait*/
	{
		memcpy( rowreq_ctx->data.downInterface, downInterface_val_ptr, downInterface_val_ptr_len );
	    rowreq_ctx->data.downInterface_len = downInterface_val_ptr_len / sizeof(downInterface_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
    return rc;
} /* downInterface_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
downInterface_undo( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:downInterface_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up downInterface undo.
     */
    /*
     * copy downInterface and downInterface_len data
     * set rowreq_ctx->data.downInterface from rowreq_ctx->undo->downInterface
     */
    memcpy( rowreq_ctx->data.downInterface, rowreq_ctx->undo->downInterface,
            (rowreq_ctx->undo->downInterface_len * sizeof(rowreq_ctx->data.downInterface[0])));
    rowreq_ctx->data.downInterface_len = rowreq_ctx->undo->downInterface_len;

    
    return MFD_SUCCESS;
} /* downInterface_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.upBandWidth
 * upBandWidth is subid 5 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.5
 * Description:
Up band width(Units kbyte).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param upBandWidth_val_ptr
 *        A char containing the new value.
 * @param upBandWidth_val_ptr_len
 *        The size (in bytes) of the data pointed to by upBandWidth_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11SpeedLimitTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.upBandWidth).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
upBandWidth_check_value( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char *upBandWidth_val_ptr,  size_t upBandWidth_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:upBandWidth_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != upBandWidth_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid upBandWidth value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus || RS_NOTREADY == rowreq_ctx->data.rowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* upBandWidth value not illegal */
} /* upBandWidth_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SpeedLimitTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11SpeedLimitTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
upBandWidth_undo_setup( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:upBandWidth_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup upBandWidth undo.
     */
    /*
     * copy upBandWidth and upBandWidth_len data
     * set rowreq_ctx->undo->upBandWidth from rowreq_ctx->data.upBandWidth
     */
    memcpy( rowreq_ctx->undo->upBandWidth, rowreq_ctx->data.upBandWidth,
            (rowreq_ctx->data.upBandWidth_len * sizeof(rowreq_ctx->undo->upBandWidth[0])));
    rowreq_ctx->undo->upBandWidth_len = rowreq_ctx->data.upBandWidth_len;


    return MFD_SUCCESS;
} /* upBandWidth_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param upBandWidth_val_ptr
 *        A char containing the new value.
 * @param upBandWidth_val_ptr_len
 *        The size (in bytes) of the data pointed to by upBandWidth_val_ptr
 */
int
upBandWidth_set( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char *upBandWidth_val_ptr,  size_t upBandWidth_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:upBandWidth_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != upBandWidth_val_ptr);

    /*
     * TODO:461:M: |-> Set upBandWidth value.
     * set upBandWidth value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus && rowreq_ctx->data.is_modify) /*modify*/
	{		
		PTCRule ptcRuleRoot=NULL;
		PTCRule ptcRuleNew=NULL;
		ptcRuleRoot = tcParseDoc( TCRULES_XML_FILE );
		ptcRuleNew = getTCRuleByIndex(ptcRuleRoot,(rowreq_ctx->tbl_idx.index)-1);
		if( NULL == ptcRuleNew )
		{
			tcFreeList(ptcRuleRoot);
			return MFD_ERROR;
		}
		FREE_OBJECT(ptcRuleNew->uplink_speed);
		ptcRuleNew->uplink_speed= strdup(upBandWidth_val_ptr);					
		tcSaveDoc( TCRULES_XML_FILE, ptcRuleRoot );
		tc_doAllRules( ptcRuleRoot );
		tcFreeList(ptcRuleRoot);
					
		memset(rowreq_ctx->data.upBandWidth, 0, sizeof(rowreq_ctx->data.upBandWidth));	
		memcpy( rowreq_ctx->data.upBandWidth, upBandWidth_val_ptr, upBandWidth_val_ptr_len );
	    rowreq_ctx->data.upBandWidth_len = upBandWidth_val_ptr_len / sizeof(upBandWidth_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else if(RS_NOTREADY == rowreq_ctx->data.rowStatus)/*creat and wait*/
	{
		memcpy( rowreq_ctx->data.upBandWidth, upBandWidth_val_ptr, upBandWidth_val_ptr_len );
	    rowreq_ctx->data.upBandWidth_len = upBandWidth_val_ptr_len / sizeof(upBandWidth_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
    return rc;
} /* upBandWidth_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
upBandWidth_undo( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:upBandWidth_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up upBandWidth undo.
     */
    /*
     * copy upBandWidth and upBandWidth_len data
     * set rowreq_ctx->data.upBandWidth from rowreq_ctx->undo->upBandWidth
     */
    memcpy( rowreq_ctx->data.upBandWidth, rowreq_ctx->undo->upBandWidth,
            (rowreq_ctx->undo->upBandWidth_len * sizeof(rowreq_ctx->data.upBandWidth[0])));
    rowreq_ctx->data.upBandWidth_len = rowreq_ctx->undo->upBandWidth_len;

    
    return MFD_SUCCESS;
} /* upBandWidth_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.downBandWidth
 * downBandWidth is subid 6 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.6
 * Description:
Down band width(Units kbyte).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param downBandWidth_val_ptr
 *        A char containing the new value.
 * @param downBandWidth_val_ptr_len
 *        The size (in bytes) of the data pointed to by downBandWidth_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11SpeedLimitTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.downBandWidth).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
downBandWidth_check_value( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char *downBandWidth_val_ptr,  size_t downBandWidth_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:downBandWidth_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != downBandWidth_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid downBandWidth value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus || RS_NOTREADY == rowreq_ctx->data.rowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* downBandWidth value not illegal */
} /* downBandWidth_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SpeedLimitTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11SpeedLimitTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
downBandWidth_undo_setup( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:downBandWidth_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup downBandWidth undo.
     */
    /*
     * copy downBandWidth and downBandWidth_len data
     * set rowreq_ctx->undo->downBandWidth from rowreq_ctx->data.downBandWidth
     */
    memcpy( rowreq_ctx->undo->downBandWidth, rowreq_ctx->data.downBandWidth,
            (rowreq_ctx->data.downBandWidth_len * sizeof(rowreq_ctx->undo->downBandWidth[0])));
    rowreq_ctx->undo->downBandWidth_len = rowreq_ctx->data.downBandWidth_len;


    return MFD_SUCCESS;
} /* downBandWidth_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param downBandWidth_val_ptr
 *        A char containing the new value.
 * @param downBandWidth_val_ptr_len
 *        The size (in bytes) of the data pointed to by downBandWidth_val_ptr
 */
int
downBandWidth_set( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char *downBandWidth_val_ptr,  size_t downBandWidth_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:downBandWidth_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != downBandWidth_val_ptr);

    /*
     * TODO:461:M: |-> Set downBandWidth value.
     * set downBandWidth value in rowreq_ctx->data
     */
    int rc = MFD_ERROR;
	if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus && rowreq_ctx->data.is_modify) /*modify*/
	{		
		PTCRule ptcRuleRoot=NULL;
		PTCRule ptcRuleNew=NULL;
		ptcRuleRoot = tcParseDoc( TCRULES_XML_FILE );
		ptcRuleNew = getTCRuleByIndex(ptcRuleRoot,(rowreq_ctx->tbl_idx.index)-1);
		if( NULL == ptcRuleNew )
		{
			tcFreeList(ptcRuleRoot);
			return MFD_ERROR;
		}
		FREE_OBJECT(ptcRuleNew->downlink_speed);
		ptcRuleNew->downlink_speed = strdup(downBandWidth_val_ptr);					
		tcSaveDoc( TCRULES_XML_FILE, ptcRuleRoot );
		tc_doAllRules( ptcRuleRoot );
		tcFreeList(ptcRuleRoot);
					
		memset(rowreq_ctx->data.downBandWidth, 0, sizeof(rowreq_ctx->data.downBandWidth));	
		memcpy( rowreq_ctx->data.downBandWidth, downBandWidth_val_ptr, downBandWidth_val_ptr_len );
	    rowreq_ctx->data.downBandWidth_len = downBandWidth_val_ptr_len / sizeof(downBandWidth_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else if(RS_NOTREADY == rowreq_ctx->data.rowStatus)/*creat and wait*/
	{
		memcpy( rowreq_ctx->data.downBandWidth, downBandWidth_val_ptr, downBandWidth_val_ptr_len );
	    rowreq_ctx->data.downBandWidth_len = downBandWidth_val_ptr_len / sizeof(downBandWidth_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
    return rc;
} /* downBandWidth_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
downBandWidth_undo( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:downBandWidth_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up downBandWidth undo.
     */
    /*
     * copy downBandWidth and downBandWidth_len data
     * set rowreq_ctx->data.downBandWidth from rowreq_ctx->undo->downBandWidth
     */
    memcpy( rowreq_ctx->data.downBandWidth, rowreq_ctx->undo->downBandWidth,
            (rowreq_ctx->undo->downBandWidth_len * sizeof(rowreq_ctx->data.downBandWidth[0])));
    rowreq_ctx->data.downBandWidth_len = rowreq_ctx->undo->downBandWidth_len;

    
    return MFD_SUCCESS;
} /* downBandWidth_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.actionStatus
 * actionStatus is subid 7 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.7
 * Description:
Whether use this action.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  use(1), unuse(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param actionStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11SpeedLimitTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  use(1), unuse(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
actionStatus_check_value( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, u_long actionStatus_val)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:actionStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid actionStatus value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus || RS_NOTREADY == rowreq_ctx->data.rowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* actionStatus value not illegal */
} /* actionStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SpeedLimitTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11SpeedLimitTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
actionStatus_undo_setup( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:actionStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup actionStatus undo.
     */
    /*
     * copy actionStatus data
     * set rowreq_ctx->undo->actionStatus from rowreq_ctx->data.actionStatus
     */
    rowreq_ctx->undo->actionStatus = rowreq_ctx->data.actionStatus;


    return MFD_SUCCESS;
} /* actionStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param actionStatus_val
 *        A long containing the new value.
 */
int
actionStatus_set( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, u_long actionStatus_val )
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:actionStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set actionStatus value.
     * set actionStatus value in rowreq_ctx->data
     */
    int rc = MFD_ERROR;
	if(RS_NOTINSERVICE == rowreq_ctx->data.rowStatus && rowreq_ctx->data.is_modify) /*modify*/
	{		
		PTCRule ptcRuleRoot=NULL;
		PTCRule ptcRuleNew=NULL;
		ptcRuleRoot = tcParseDoc( TCRULES_XML_FILE );
		ptcRuleNew = getTCRuleByIndex(ptcRuleRoot,(rowreq_ctx->tbl_idx.index)-1);
		if( NULL == ptcRuleNew )
		{
			tcFreeList(ptcRuleRoot);
			return MFD_ERROR;
		}
		ptcRuleNew->enable = (actionStatus_val == ACTIONSTATUS_UNUSE)?0:1;					
		tcSaveDoc( TCRULES_XML_FILE, ptcRuleRoot );
		tc_doAllRules( ptcRuleRoot );
		tcFreeList(ptcRuleRoot);
					
		rowreq_ctx->data.actionStatus = actionStatus_val;
		rc = MFD_SUCCESS;
	}
	else if(RS_NOTREADY == rowreq_ctx->data.rowStatus)/*creat and wait*/
	{
		rowreq_ctx->data.actionStatus = actionStatus_val;
		rc = MFD_SUCCESS;
	}
    return rc;
} /* actionStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
actionStatus_undo( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:actionStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up actionStatus undo.
     */
    /*
     * copy actionStatus data
     * set rowreq_ctx->data.actionStatus from rowreq_ctx->undo->actionStatus
     */
    rowreq_ctx->data.actionStatus = rowreq_ctx->undo->actionStatus;

    
    return MFD_SUCCESS;
} /* actionStatus_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.rowStatus
 * rowStatus is subid 8 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.8
 * Description:
This variable is used to create, modify, and/or delete a row
		        	 	in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11SpeedLimitTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rowStatus_check_value( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, u_long rowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:rowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rowStatus value.
     */

    return MFD_SUCCESS; /* rowStatus value not illegal */
} /* rowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SpeedLimitTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11SpeedLimitTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rowStatus_undo_setup( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:rowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rowStatus undo.
     */
    /*
     * copy rowStatus data
     * set rowreq_ctx->undo->rowStatus from rowreq_ctx->data.rowStatus
     */
    rowreq_ctx->undo->rowStatus = rowreq_ctx->data.rowStatus;


    return MFD_SUCCESS;
} /* rowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rowStatus_val
 *        A long containing the new value.
 */
int
rowStatus_set( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, u_long rowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:rowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rowStatus value.
     * set rowStatus value in rowreq_ctx->data
     */
    int ret = MFD_SUCCESS;
    
    switch(rowStatus_val) {
    
		case RS_NOTINSERVICE:
        case RS_ACTIVE:         
        case RS_DESTROY:
            rowreq_ctx->data.rowStatus = rowStatus_val;
            ret = MFD_SUCCESS;
            break;
    
        case RS_NOTREADY:
        case RS_CREATEANDGO:
            ret = MFD_ERROR;
            break;
    
        case RS_CREATEANDWAIT:
            if(RS_NONEXISTENT == rowreq_ctx->data.rowStatus) {
                rowreq_ctx->data.rowStatus = rowStatus_val;
                ret = MFD_SUCCESS;
            }
            else {
                ret = MFD_ERROR;
            }
            break;
    
        default:
            snmp_log(LOG_WARNING, "unknow %s in rowStatus_set", rowStatus_val);
            ret = MFD_ERROR;
            break;
    }

    return ret;
} /* rowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rowStatus_undo( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:rowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rowStatus undo.
     */
    /*
     * copy rowStatus data
     * set rowreq_ctx->data.rowStatus from rowreq_ctx->undo->rowStatus
     */
    rowreq_ctx->data.rowStatus = rowreq_ctx->undo->rowStatus;

    
    return MFD_SUCCESS;
} /* rowStatus_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11SpeedLimitTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11SpeedLimitTable if you don't have dependencies)
 */
int
dot11SpeedLimitTable_check_dependencies(dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11SpeedLimitTable:dot11SpeedLimitTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11SpeedLimitTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & COLUMN_ROWSTATUS_FLAG) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->rowStatus,
                                         rowreq_ctx->data.rowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_DESTROY ==  rowreq_ctx->data.rowStatus) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;
            }
            else if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.rowStatus) {
                if ((rowreq_ctx->column_set_flags & DOT11SPEEDLIMITTABLE_REQUIRED_COLS)
                    != DOT11SPEEDLIMITTABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11SpeedLimitTable",
                                "required columns missing (0x%0x != 0x%0x)\n",
                                rowreq_ctx->column_set_flags, DOT11SPEEDLIMITTABLE_REQUIRED_COLS));
                    return MFD_CANNOT_CREATE_NOW;
                }
                rowreq_ctx->data.rowStatus = ROWSTATUS_ACTIVE;
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.rowStatus) {
                if (rowreq_ctx->column_set_flags & ~COLUMN_ROWSTATUS_FLAG) {
                    DEBUGMSGTL(("dot11SpeedLimitTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;

            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11SpeedLimitTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11SpeedLimitTable_check_dependencies */

/** @} */
