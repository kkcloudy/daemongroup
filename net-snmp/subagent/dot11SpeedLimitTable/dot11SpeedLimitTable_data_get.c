/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11SpeedLimitTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11SpeedLimitTable get routines.
 * TODO:240:M: Implement dot11SpeedLimitTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11SpeedLimitTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11SpeedLimitTable is subid 1 of dot11SpeedLimit.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11SpeedLimitTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param index_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11SpeedLimitTable_indexes_set_tbl_idx(dot11SpeedLimitTable_mib_index *tbl_idx, long index_val)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:dot11SpeedLimitTable_indexes_set_tbl_idx","called\n"));

    /* index(1)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    tbl_idx->index = index_val;
    

    return MFD_SUCCESS;
} /* dot11SpeedLimitTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11SpeedLimitTable_indexes_set(dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, long index_val)
{
    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:dot11SpeedLimitTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11SpeedLimitTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , index_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11SpeedLimitTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11SpeedLimitTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.ipAddress
 * ipAddress is subid 2 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.2
 * Description:
Ip Address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the ipAddress data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ipAddress_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param ipAddress_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by ipAddress.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*ipAddress_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update ipAddress_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
ipAddress_get( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char **ipAddress_val_ptr_ptr, size_t *ipAddress_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != ipAddress_val_ptr_ptr) && (NULL != *ipAddress_val_ptr_ptr));
   netsnmp_assert( NULL != ipAddress_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:ipAddress_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the ipAddress data.
 * copy (* ipAddress_val_ptr_ptr ) data and (* ipAddress_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for ipAddress data
     */
    if ((NULL == (* ipAddress_val_ptr_ptr )) ||
        ((* ipAddress_val_ptr_len_ptr ) <
         (rowreq_ctx->data.ipAddress_len* sizeof(rowreq_ctx->data.ipAddress[0])))) {
        /*
         * allocate space for ipAddress data
         */
        (* ipAddress_val_ptr_ptr ) = malloc(rowreq_ctx->data.ipAddress_len* sizeof(rowreq_ctx->data.ipAddress[0]));
        if(NULL == (* ipAddress_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* ipAddress_val_ptr_len_ptr ) = rowreq_ctx->data.ipAddress_len* sizeof(rowreq_ctx->data.ipAddress[0]);
    memcpy( (* ipAddress_val_ptr_ptr ), rowreq_ctx->data.ipAddress, rowreq_ctx->data.ipAddress_len* sizeof(rowreq_ctx->data.ipAddress[0]) );

    return MFD_SUCCESS;
} /* ipAddress_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.upInterface
 * upInterface is subid 3 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.3
 * Description:
UP interface.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the upInterface data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param upInterface_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param upInterface_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by upInterface.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*upInterface_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update upInterface_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
upInterface_get( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char **upInterface_val_ptr_ptr, size_t *upInterface_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != upInterface_val_ptr_ptr) && (NULL != *upInterface_val_ptr_ptr));
   netsnmp_assert( NULL != upInterface_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:upInterface_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the upInterface data.
 * copy (* upInterface_val_ptr_ptr ) data and (* upInterface_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for upInterface data
     */
    if ((NULL == (* upInterface_val_ptr_ptr )) ||
        ((* upInterface_val_ptr_len_ptr ) <
         (rowreq_ctx->data.upInterface_len* sizeof(rowreq_ctx->data.upInterface[0])))) {
        /*
         * allocate space for upInterface data
         */
        (* upInterface_val_ptr_ptr ) = malloc(rowreq_ctx->data.upInterface_len* sizeof(rowreq_ctx->data.upInterface[0]));
        if(NULL == (* upInterface_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* upInterface_val_ptr_len_ptr ) = rowreq_ctx->data.upInterface_len* sizeof(rowreq_ctx->data.upInterface[0]);
    memcpy( (* upInterface_val_ptr_ptr ), rowreq_ctx->data.upInterface, rowreq_ctx->data.upInterface_len* sizeof(rowreq_ctx->data.upInterface[0]) );

    return MFD_SUCCESS;
} /* upInterface_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.downInterface
 * downInterface is subid 4 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.4
 * Description:
Down interface.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the downInterface data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param downInterface_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param downInterface_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by downInterface.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*downInterface_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update downInterface_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
downInterface_get( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char **downInterface_val_ptr_ptr, size_t *downInterface_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != downInterface_val_ptr_ptr) && (NULL != *downInterface_val_ptr_ptr));
   netsnmp_assert( NULL != downInterface_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:downInterface_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the downInterface data.
 * copy (* downInterface_val_ptr_ptr ) data and (* downInterface_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for downInterface data
     */
    if ((NULL == (* downInterface_val_ptr_ptr )) ||
        ((* downInterface_val_ptr_len_ptr ) <
         (rowreq_ctx->data.downInterface_len* sizeof(rowreq_ctx->data.downInterface[0])))) {
        /*
         * allocate space for downInterface data
         */
        (* downInterface_val_ptr_ptr ) = malloc(rowreq_ctx->data.downInterface_len* sizeof(rowreq_ctx->data.downInterface[0]));
        if(NULL == (* downInterface_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* downInterface_val_ptr_len_ptr ) = rowreq_ctx->data.downInterface_len* sizeof(rowreq_ctx->data.downInterface[0]);
    memcpy( (* downInterface_val_ptr_ptr ), rowreq_ctx->data.downInterface, rowreq_ctx->data.downInterface_len* sizeof(rowreq_ctx->data.downInterface[0]) );

    return MFD_SUCCESS;
} /* downInterface_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.upBandWidth
 * upBandWidth is subid 5 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.5
 * Description:
Up band width(Units kbyte).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the upBandWidth data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param upBandWidth_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param upBandWidth_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by upBandWidth.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*upBandWidth_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update upBandWidth_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
upBandWidth_get( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char **upBandWidth_val_ptr_ptr, size_t *upBandWidth_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != upBandWidth_val_ptr_ptr) && (NULL != *upBandWidth_val_ptr_ptr));
   netsnmp_assert( NULL != upBandWidth_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:upBandWidth_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the upBandWidth data.
 * copy (* upBandWidth_val_ptr_ptr ) data and (* upBandWidth_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for upBandWidth data
     */
    if ((NULL == (* upBandWidth_val_ptr_ptr )) ||
        ((* upBandWidth_val_ptr_len_ptr ) <
         (rowreq_ctx->data.upBandWidth_len* sizeof(rowreq_ctx->data.upBandWidth[0])))) {
        /*
         * allocate space for upBandWidth data
         */
        (* upBandWidth_val_ptr_ptr ) = malloc(rowreq_ctx->data.upBandWidth_len* sizeof(rowreq_ctx->data.upBandWidth[0]));
        if(NULL == (* upBandWidth_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* upBandWidth_val_ptr_len_ptr ) = rowreq_ctx->data.upBandWidth_len* sizeof(rowreq_ctx->data.upBandWidth[0]);
    memcpy( (* upBandWidth_val_ptr_ptr ), rowreq_ctx->data.upBandWidth, rowreq_ctx->data.upBandWidth_len* sizeof(rowreq_ctx->data.upBandWidth[0]) );

    return MFD_SUCCESS;
} /* upBandWidth_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.downBandWidth
 * downBandWidth is subid 6 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.6
 * Description:
Down band width(Units kbyte).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the downBandWidth data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param downBandWidth_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param downBandWidth_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by downBandWidth.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*downBandWidth_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update downBandWidth_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
downBandWidth_get( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, char **downBandWidth_val_ptr_ptr, size_t *downBandWidth_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != downBandWidth_val_ptr_ptr) && (NULL != *downBandWidth_val_ptr_ptr));
   netsnmp_assert( NULL != downBandWidth_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:downBandWidth_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the downBandWidth data.
 * copy (* downBandWidth_val_ptr_ptr ) data and (* downBandWidth_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for downBandWidth data
     */
    if ((NULL == (* downBandWidth_val_ptr_ptr )) ||
        ((* downBandWidth_val_ptr_len_ptr ) <
         (rowreq_ctx->data.downBandWidth_len* sizeof(rowreq_ctx->data.downBandWidth[0])))) {
        /*
         * allocate space for downBandWidth data
         */
        (* downBandWidth_val_ptr_ptr ) = malloc(rowreq_ctx->data.downBandWidth_len* sizeof(rowreq_ctx->data.downBandWidth[0]));
        if(NULL == (* downBandWidth_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* downBandWidth_val_ptr_len_ptr ) = rowreq_ctx->data.downBandWidth_len* sizeof(rowreq_ctx->data.downBandWidth[0]);
    memcpy( (* downBandWidth_val_ptr_ptr ), rowreq_ctx->data.downBandWidth, rowreq_ctx->data.downBandWidth_len* sizeof(rowreq_ctx->data.downBandWidth[0]) );

    return MFD_SUCCESS;
} /* downBandWidth_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.actionStatus
 * actionStatus is subid 7 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.7
 * Description:
Whether use this action.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  use(1), unuse(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the actionStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param actionStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
actionStatus_get( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, u_long * actionStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != actionStatus_val_ptr );


    DEBUGMSGTL(("verbose:dot11SpeedLimitTable:actionStatus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the actionStatus data.
 * copy (* actionStatus_val_ptr ) from rowreq_ctx->data
 */
    (* actionStatus_val_ptr ) = rowreq_ctx->data.actionStatus;

    return MFD_SUCCESS;
} /* actionStatus_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SpeedLimitEntry.rowStatus
 * rowStatus is subid 8 of dot11SpeedLimitEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.16.1.1.8
 * Description:
This variable is used to create, modify, and/or delete a row
		        	 	in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the rowStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rowStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rowStatus_get( dot11SpeedLimitTable_rowreq_ctx *rowreq_ctx, u_long * rowStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rowStatus_val_ptr );

    (* rowStatus_val_ptr ) = rowreq_ctx->data.rowStatus;

    return MFD_SUCCESS;
} /* rowStatus_get */



/** @} */
