/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11PortalUserInfoTable.h"


#include "dot11PortalUserInfoTable_data_access.h"
#include "mibs_public.h"
#include "autelanWtpGroup.h"
#include "ws_user_manage.h"
#include "ws_conf_engine.h"
#include <time.h>
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "nm/app/eag/eag_conf.h"
#include "nm/public/nm_list.h"
#include "nm/app/eag/eag_interface.h"
#include "nm/app/eag/eag_errcode.h"
#include "ws_sta.h"
#include "ws_init_dbus.h"


/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11PortalUserInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11PortalUserInfoTable is subid 2 of wlanPortal.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.2, length: 12
*/

/**
 * initialization for dot11PortalUserInfoTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11PortalUserInfoTable_reg
 *        Pointer to dot11PortalUserInfoTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11PortalUserInfoTable_init_data(dot11PortalUserInfoTable_registration_ptr dot11PortalUserInfoTable_reg)
{
    DEBUGMSGTL(("verbose:dot11PortalUserInfoTable:dot11PortalUserInfoTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11PortalUserInfoTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11PortalUserInfoTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11PortalUserInfoTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11PortalUserInfoTable:dot11PortalUserInfoTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11PortalUserInfoTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11PortalUserInfoTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11PORTALUSERINFOTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11PortalUserInfoTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11PortalUserInfoTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11PortalUserInfoTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11PortalUserInfoTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11PortalUserInfoTable_cache_load\n");
	
    dot11PortalUserInfoTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
         */
        /** 128 - 1(entry) - 1(col) - 1(other indexes) = 111 */
   char   wtpMacAddr[20];
   size_t      wtpMacAddr_len;
        /*
         * wtpStaMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
         */
        /** 128 - 1(entry) - 1(col) - 1(other indexes) = 111 */
   char   wtpStaMacAddr[20];
   size_t      wtpStaMacAddr_len;

    DEBUGMSGTL(("verbose:dot11PortalUserInfoTable:dot11PortalUserInfoTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
        //return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11PortalUserInfoTable container.
     * loop over your dot11PortalUserInfoTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
	instance_parameter *paraHead = NULL;
	instance_parameter *pq = NULL;	
	int ret = 0;
	struct eag_userdb userdb = {0};
	struct eag_user *user = NULL;
	char portalUsername[255] = { 0 };
	size_t portalUsername_len = 0;
	int uptimeint = 0;
	time_t timez = time(NULL);
	struct tm *local = NULL;
	char pUserUptime[255] = { 0 };
	char portalUserUptime[255] = { 0 };
	size_t portalUserUptime_len = 0;
	
	snmp_log(LOG_DEBUG, "enter list_instance_parameter\n");
	list_instance_parameter(&paraHead, SNMPD_INSTANCE_MASTER); 
	snmp_log(LOG_DEBUG, "exit list_instance_parameter,paraHead=%p\n", paraHead);
	for(pq = paraHead; (NULL != pq); pq = pq->next)
	{
		/*end load portal user info*/
		eag_userdb_init(&userdb);
		ret = eag_show_user_all(pq->connection,pq->parameter.local_id,pq->parameter.instance_id,&userdb);		
				
		if(EAG_RETURN_OK == ret)
		{		
			list_for_each_entry(user, &(userdb.head), node)
			{
				if (0 != user->sta_state) {
					continue;
				}
				memset(wtpMacAddr, 0, sizeof(wtpMacAddr));
				snprintf(wtpMacAddr, 
						 sizeof(wtpMacAddr)-1, 
						 "%02X:%02X:%02X:%02X:%02X:%02X",
						 user->apmac[0],user->apmac[1], 
						 user->apmac[2],user->apmac[3], 
						 user->apmac[4],user->apmac[5]);
				
				memset(wtpStaMacAddr, 0, sizeof(wtpStaMacAddr));
				snprintf(wtpStaMacAddr,sizeof(wtpStaMacAddr)-1,
						 "%02X:%02X:%02X:%02X:%02X:%02X",
						 user->usermac[0],user->usermac[1],
						 user->usermac[2],user->usermac[3],
						 user->usermac[4],user->usermac[5]);
				
				
	    /*
	    ***************************************************
	    ***             START EXAMPLE CODE              ***
	    ***---------------------------------------------***/
	    /*
	     * get a line (skip blank lines)
	     */
	    //do {
	        //if (!fgets(line, sizeof(line), filep)) {
	            /* we're done */
	            //fclose(filep);
	            //filep = NULL;
	        //}
	    //} while (filep && (line[0] == '\n'));

	    /*
	     * check for end of data
	     */
	    //if(NULL == filep)
	        //break;

	    /*
	     * parse line into variables
	     */
	    /*
	    ***---------------------------------------------***
	    ***              END  EXAMPLE CODE              ***
	    ***************************************************/

	        /*
	         * TODO:352:M: |   |-> set indexes in new dot11PortalUserInfoTable rowreq context.
	         */
	        rowreq_ctx = dot11PortalUserInfoTable_allocate_rowreq_ctx();
	        if (NULL == rowreq_ctx) {
	            snmp_log(LOG_ERR, "memory allocation failed\n");
				eag_userdb_destroy(&userdb);
				free_instance_parameter_list(&paraHead);
	            return MFD_RESOURCE_UNAVAILABLE;
	        }
			wtpMacAddr_len = strlen(wtpMacAddr);
			wtpStaMacAddr_len = strlen(wtpStaMacAddr);
	        if(MFD_SUCCESS != dot11PortalUserInfoTable_indexes_set(rowreq_ctx
	                               , wtpMacAddr, wtpMacAddr_len
	                               , wtpStaMacAddr, wtpStaMacAddr_len
	               )) {
	            snmp_log(LOG_ERR,"error setting index while loading "
	                     "dot11PortalUserInfoTable cache.\n");
	            dot11PortalUserInfoTable_release_rowreq_ctx(rowreq_ctx);
	            continue;
	        }

	        /*
	         * TODO:352:r: |   |-> populate dot11PortalUserInfoTable data context.
	         * Populate data context here. (optionally, delay until row prep)
	         */
	    /*
	     * TRANSIENT or semi-TRANSIENT data:
	     * copy data or save any info needed to do it in row_prep.
	     */
	    /*
	     * setup/save data for portalUserip
	     * portalUserip(1)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define portalUserip mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.portalUserip = user->userip;
	    
	    /*
	     * setup/save data for portalUsername
	     * portalUsername(2)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
	     */
	    /*
	     * TODO:246:r: |-> Define portalUsername mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * if(MFD_SUCCESS !=
	     *    portalUsername_map(&rowreq_ctx->data.portalUsername, &rowreq_ctx->data.portalUsername_len,
	     *                portalUsername, portalUsername_len, 0)) {
	     *    return MFD_ERROR;
	     * }
	     */
	    /*
	     * make sure there is enough space for portalUsername data
	     */
	    /*if ((NULL == rowreq_ctx->data.portalUsername) ||
	        (rowreq_ctx->data.portalUsername_len < (portalUsername_len * sizeof(rowreq_ctx->data.portalUsername[0])))) {
	        snmp_log(LOG_ERR,"not enough space for value\n");
	        return MFD_ERROR;
	    }*/	    
	    portalUsername_len = MIN(strlen(user->username),sizeof(portalUsername)-1);
		memset(portalUsername,0,sizeof(portalUsername));
		memcpy(portalUsername,user->username,portalUsername_len);
			
	    rowreq_ctx->data.portalUsername_len = portalUsername_len * sizeof(rowreq_ctx->data.portalUsername[0]);
	    memcpy( rowreq_ctx->data.portalUsername, portalUsername, rowreq_ctx->data.portalUsername_len );
	    
	    /*
	     * setup/save data for portalUserUptime
	     * portalUserUptime(3)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
	     */
	    /*
	     * TODO:246:r: |-> Define portalUserUptime mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * if(MFD_SUCCESS !=
	     *    portalUserUptime_map(&rowreq_ctx->data.portalUserUptime, &rowreq_ctx->data.portalUserUptime_len,
	     *                portalUserUptime, portalUserUptime_len, 0)) {
	     *    return MFD_ERROR;
	     * }
	     */
	    /*
	     * make sure there is enough space for portalUserUptime data
	     */
	    /*if ((NULL == rowreq_ctx->data.portalUserUptime) ||
	        (rowreq_ctx->data.portalUserUptime_len < (portalUserUptime_len * sizeof(rowreq_ctx->data.portalUserUptime[0])))) {
	        snmp_log(LOG_ERR,"not enough space for value\n");
	        return MFD_ERROR;
	    }*/
	    /*uptimeint = timez - user->session_time;*/
	    uptimeint = user->accurate_start_time;
		local = localtime(&uptimeint);
        if(NULL != local)
        {
			memset(pUserUptime,0,sizeof(pUserUptime));
			snprintf(pUserUptime, sizeof(pUserUptime)-1, 
					 "%d-%02d-%02d %02d:%02d:%02d",
					 local->tm_year+1900, local->tm_mon+1, local->tm_mday,
					 local->tm_hour, local->tm_min, local->tm_sec);
        }
		portalUserUptime_len = MIN(strlen(pUserUptime),sizeof(portalUserUptime)-1);
		memset(portalUserUptime,0,sizeof(portalUserUptime));
		memcpy(portalUserUptime,pUserUptime,portalUserUptime_len);
		
	    rowreq_ctx->data.portalUserUptime_len = portalUserUptime_len * sizeof(rowreq_ctx->data.portalUserUptime[0]);
	    memcpy( rowreq_ctx->data.portalUserUptime, portalUserUptime, rowreq_ctx->data.portalUserUptime_len );
	    
	    /*
	     * setup/save data for portalUseronline
	     * portalUseronline(4)/TICKS/ASN_TIMETICKS/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define portalUseronline mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.portalUseronline = user->session_time * 100;
	    
		rowreq_ctx->data.userAuthType = 1;
	        /*
	         * insert into table container
	         */
	        if(CONTAINER_INSERT(container, rowreq_ctx))
	        {
	        	dot11PortalUserInfoTable_release_rowreq_ctx(rowreq_ctx);
	        }
	        ++count;
			}		
		}
		eag_userdb_destroy(&userdb);
		/*end load portal user info*/


		/*start load non-auth and 1x user info*/

	    int asd_ret = 0;	    
		struct WtpStaInfo * StaHead =NULL;
		struct WtpStaInfo * StaNode =NULL;
		
		char pUsername[255] = { 0 };		
	    snmp_log(LOG_DEBUG, "enter show_all_wtp_station_information_cmd\n");
		asd_ret = show_all_wtp_station_information_cmd(pq->parameter, pq->connection, &StaHead);
		snmp_log(LOG_DEBUG, "exit show_all_wtp_station_information_cmd, asd_ret=%d\n", asd_ret);

	    if(1 == asd_ret && StaHead) {
			for(StaNode = StaHead; NULL != StaNode; StaNode=StaNode->next) {
	            
	            if(NULL == StaNode->wtpMacAddr || NULL == StaNode->wtpTerminalMacAddr) {
	                continue;
	            }

	            if(1 == StaNode->security_type) { /* unknow auth type */
	                continue;
	            }
			    
	            wtpMacAddr_len = strlen(StaNode->wtpMacAddr);
	            wtpStaMacAddr_len = strlen(StaNode->wtpTerminalMacAddr);
	            
				rowreq_ctx = dot11PortalUserInfoTable_allocate_rowreq_ctx();
				if (NULL == rowreq_ctx)
				{
					snmp_log(LOG_ERR, "memory allocation failed\n");
					Free_show_all_wtp_station_information_cmd(StaHead);
					return MFD_RESOURCE_UNAVAILABLE;
				}
				
				if(MFD_SUCCESS != dot11PortalUserInfoTable_indexes_set(rowreq_ctx
												   , StaNode->wtpMacAddr, wtpMacAddr_len
												   , StaNode->wtpTerminalMacAddr, wtpStaMacAddr_len))
				{
					snmp_log(LOG_ERR,"error setting index while loading "
								 "dot11PortalUserInfoTable cache.\n");
					dot11PortalUserInfoTable_release_rowreq_ctx(rowreq_ctx);
					continue;
				}

				rowreq_ctx->data.portalUserip = StaNode->wtpStaIp;

	            /*SIM/PEAP user name*/
				if(StaNode->security_type == asd_auth_type_autoauth)
				{
					memset(pUsername, 0, sizeof(pUsername));
					if(( NULL != StaNode->wtpName) && ( NULL != pUsername))
					{
					   strncpy(pUsername, StaNode->identity, sizeof(pUsername) - 1);
					}
					int portalUsername_len =0;
					portalUsername_len = MIN(strlen(pUsername),sizeof(rowreq_ctx->data.portalUsername)-1);
					rowreq_ctx->data.portalUsername_len = portalUsername_len * sizeof(rowreq_ctx->data.portalUsername[0]);
					memcpy( rowreq_ctx->data.portalUsername, pUsername, rowreq_ctx->data.portalUsername_len );
				}

				
				time_t now,now_sysrun,wtp_access_time;
				time(&now);
				get_sysruntime(&now_sysrun);
				wtp_access_time = now - (now_sysrun - StaNode->wtpterminalaccesstime_int+StaNode->sta_online_time);
				
	            char userUptime[256] = { 0 };
	            struct tm *local = localtime(&wtp_access_time);
	            if(NULL != local) {
	                snprintf(userUptime, sizeof(userUptime) - 1, "%d-%02d-%02d %02d:%02d:%02d",
	                            local->tm_year+1900, local->tm_mon+1, local->tm_mday, local->tm_hour, local->tm_min, local->tm_sec);
	            }
	            					
				int portalUserUptime_len = 0;
				portalUserUptime_len = MIN(strlen(userUptime),sizeof(rowreq_ctx->data.portalUserUptime)-1);
				rowreq_ctx->data.portalUserUptime_len = portalUserUptime_len * sizeof(rowreq_ctx->data.portalUserUptime[0]);
				memcpy( rowreq_ctx->data.portalUserUptime, userUptime, rowreq_ctx->data.portalUserUptime_len );
						
				rowreq_ctx->data.portalUseronline = StaNode->wtpMacTermAddrUsrOnlineTime;
	            if(StaNode->security_type == asd_auth_type_authfree || StaNode->security_type == asd_auth_type_weppsk||StaNode->security_type == asd_auth_type_autoauth)
				{
					rowreq_ctx->data.userAuthType = StaNode->security_type;

				}
				if(CONTAINER_INSERT(container, rowreq_ctx))
				{
					dot11PortalUserInfoTable_release_rowreq_ctx(rowreq_ctx);
				}
				++count;
	            
	        }        
	        Free_show_all_wtp_station_information_cmd(StaHead);
	    }
		/*end load non-auth and 1x user info*/
	}
	free_instance_parameter_list(&paraHead);

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
        //fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11PortalUserInfoTable:dot11PortalUserInfoTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11PortalUserInfoTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11PortalUserInfoTable_cache_load */


/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11PortalUserInfoTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11PortalUserInfoTable:dot11PortalUserInfoTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11PortalUserInfoTable cache.
     */
} /* dot11PortalUserInfoTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11PortalUserInfoTable_row_prep( dot11PortalUserInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11PortalUserInfoTable:dot11PortalUserInfoTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11PortalUserInfoTable_row_prep */

/** @} */
