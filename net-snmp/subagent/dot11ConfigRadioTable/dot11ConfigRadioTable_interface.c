/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.43.2.3 $ of : mfd-interface.m2c,v $ 
 *
 * $Id:$
 */
/*
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 * ***                                                               ***
 * ***  NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE  ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THIS FILE DOES NOT CONTAIN ANY USER EDITABLE CODE.      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THE GENERATED CODE IS INTERNAL IMPLEMENTATION, AND      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***    IS SUBJECT TO CHANGE WITHOUT WARNING IN FUTURE RELEASES.   ***
 * ***                                                               ***
 * ***                                                               ***
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 */

/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigRadioTable.h"


#include <net-snmp/library/container.h>

#include "dot11ConfigRadioTable_interface.h"

/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigRadioTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigRadioTable is subid 3 of wtpRadio.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3, length: 12
*/
typedef struct dot11ConfigRadioTable_interface_ctx_s {

   netsnmp_container              *container;
   netsnmp_cache                  *cache; /* optional cache */

   dot11ConfigRadioTable_registration_ptr      user_ctx;
   
   netsnmp_table_registration_info  tbl_info;

   netsnmp_baby_steps_access_methods access_multiplexer;

} dot11ConfigRadioTable_interface_ctx;

static dot11ConfigRadioTable_interface_ctx dot11ConfigRadioTable_if_ctx;

static void _dot11ConfigRadioTable_container_init(
    dot11ConfigRadioTable_interface_ctx *if_ctx);


static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_pre_request;
static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_post_request;
static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_object_lookup;
static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_get_values;
static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_check_objects;
static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_undo_setup;
static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_set_values;
static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_undo_cleanup;
static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_undo_values;
static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_commit;
static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_undo_commit;
static Netsnmp_Node_Handler _mfd_dot11ConfigRadioTable_check_dependencies;

NETSNMP_STATIC_INLINE int _dot11ConfigRadioTable_undo_column( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx,
                                                   netsnmp_variable_list *var, int column );

dot11ConfigRadioTable_data *dot11ConfigRadioTable_allocate_data(void);

/**
 * @internal
 * Initialize the table dot11ConfigRadioTable 
 *    (Define its contents and how it's structured)
 */
void
_dot11ConfigRadioTable_initialize_interface(dot11ConfigRadioTable_registration_ptr reg_ptr,  u_long flags)
{
    netsnmp_baby_steps_access_methods *access_multiplexer =
        &dot11ConfigRadioTable_if_ctx.access_multiplexer;
    netsnmp_table_registration_info *tbl_info = &dot11ConfigRadioTable_if_ctx.tbl_info;
    netsnmp_handler_registration *reginfo;
    netsnmp_mib_handler *handler;
    int    mfd_modes = 0;

    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_dot11ConfigRadioTable_initialize_interface","called\n"));


    /*************************************************
     *
     * save interface context for dot11ConfigRadioTable
     */
    /*
     * Setting up the table's definition
     */
    netsnmp_table_helper_add_indexes(tbl_info,
                                  ASN_OCTET_STR, /** index: wtpMacAddr */
                                  ASN_INTEGER, /** index: wtpWirelessIfIndex */
                             0);

    /*  Define the minimum and maximum accessible columns.  This
        optimizes retrival. */
    tbl_info->min_column = DOT11CONFIGRADIOTABLE_MIN_COL;
    tbl_info->max_column = DOT11CONFIGRADIOTABLE_MAX_COL;

    /*
     * save users context
     */
    dot11ConfigRadioTable_if_ctx.user_ctx = reg_ptr;

    /*
     * call data access initialization code
     */
    dot11ConfigRadioTable_init_data(reg_ptr);

    /*
     * set up the container
     */
    _dot11ConfigRadioTable_container_init(&dot11ConfigRadioTable_if_ctx);
    if (NULL == dot11ConfigRadioTable_if_ctx.container) {
        snmp_log(LOG_ERR,"could not initialize container for dot11ConfigRadioTable\n");
        return;
    }
    
    /*
     * access_multiplexer: REQUIRED wrapper for get request handling
     */
    access_multiplexer->object_lookup = _mfd_dot11ConfigRadioTable_object_lookup;
    access_multiplexer->get_values = _mfd_dot11ConfigRadioTable_get_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->pre_request = _mfd_dot11ConfigRadioTable_pre_request;
    access_multiplexer->post_request = _mfd_dot11ConfigRadioTable_post_request;


    /*
     * REQUIRED wrappers for set request handling
     */
    access_multiplexer->object_syntax_checks = _mfd_dot11ConfigRadioTable_check_objects;
    access_multiplexer->undo_setup = _mfd_dot11ConfigRadioTable_undo_setup;
    access_multiplexer->undo_cleanup = _mfd_dot11ConfigRadioTable_undo_cleanup;
    access_multiplexer->set_values = _mfd_dot11ConfigRadioTable_set_values;
    access_multiplexer->undo_sets = _mfd_dot11ConfigRadioTable_undo_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->commit = _mfd_dot11ConfigRadioTable_commit;
    access_multiplexer->undo_commit = _mfd_dot11ConfigRadioTable_undo_commit;
    
    /*
     * REQUIRED for tables with dependencies
     */
    access_multiplexer->consistency_checks = _mfd_dot11ConfigRadioTable_check_dependencies;

    /*************************************************
     *
     * Create a registration, save our reg data, register table.
     */
    DEBUGMSGTL(("dot11ConfigRadioTable:init_dot11ConfigRadioTable",
                "Registering dot11ConfigRadioTable as a mibs-for-dummies table.\n"));		 
    handler = netsnmp_baby_steps_access_multiplexer_get(access_multiplexer);
    reginfo = netsnmp_handler_registration_create("dot11ConfigRadioTable", handler,
                                                  dot11ConfigRadioTable_oid,
                                                  dot11ConfigRadioTable_oid_size,
                                                  HANDLER_CAN_BABY_STEP |
                                                  HANDLER_CAN_RWRITE
                                                  );
    if(NULL == reginfo) {
        snmp_log(LOG_ERR,"error registering table dot11ConfigRadioTable\n");
        return;
    }
    reginfo->my_reg_void = &dot11ConfigRadioTable_if_ctx;

    /*************************************************
     *
     * set up baby steps handler, create it and inject it
     */
    if( access_multiplexer->object_lookup )
        mfd_modes |= BABY_STEP_OBJECT_LOOKUP;
    if( access_multiplexer->set_values )
        mfd_modes |= BABY_STEP_SET_VALUES;
    if( access_multiplexer->irreversible_commit )
        mfd_modes |= BABY_STEP_IRREVERSIBLE_COMMIT;
    if( access_multiplexer->object_syntax_checks )
        mfd_modes |= BABY_STEP_CHECK_OBJECT;

    if( access_multiplexer->pre_request )
        mfd_modes |= BABY_STEP_PRE_REQUEST;
    if( access_multiplexer->post_request )
        mfd_modes |= BABY_STEP_POST_REQUEST;
    
    if( access_multiplexer->undo_setup )
        mfd_modes |= BABY_STEP_UNDO_SETUP;
    if( access_multiplexer->undo_cleanup )
        mfd_modes |= BABY_STEP_UNDO_CLEANUP;
    if( access_multiplexer->undo_sets )
        mfd_modes |= BABY_STEP_UNDO_SETS;
    
    if( access_multiplexer->row_creation )
        mfd_modes |= BABY_STEP_ROW_CREATE;
    if( access_multiplexer->consistency_checks )
        mfd_modes |= BABY_STEP_CHECK_CONSISTENCY;
    if( access_multiplexer->commit )
        mfd_modes |= BABY_STEP_COMMIT;
    if( access_multiplexer->undo_commit )
        mfd_modes |= BABY_STEP_UNDO_COMMIT;
    
    handler = netsnmp_baby_steps_handler_get(mfd_modes);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject row_merge helper with prefix rootoid_len + 2 (entry.col)
     */
    handler = netsnmp_get_row_merge_handler(reginfo->rootoid_len + 2);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject container_table helper
     */
    handler =
        netsnmp_container_table_handler_get(tbl_info,
                                            dot11ConfigRadioTable_if_ctx.container,
                                            TABLE_CONTAINER_KEY_NETSNMP_INDEX);
    netsnmp_inject_handler( reginfo, handler );

    /*************************************************
     *
     * inject cache helper
     */
    if(NULL != dot11ConfigRadioTable_if_ctx.cache) {
        handler = netsnmp_cache_handler_get(dot11ConfigRadioTable_if_ctx.cache);
        netsnmp_inject_handler( reginfo, handler );
    }

    /*
     * register table
     */
    netsnmp_register_table(reginfo, tbl_info);
} /* _dot11ConfigRadioTable_initialize_interface */

void
dot11ConfigRadioTable_valid_columns_set(netsnmp_column_info *vc)
{
    dot11ConfigRadioTable_if_ctx.tbl_info.valid_columns = vc;
} /* dot11ConfigRadioTable_valid_columns_set */

/**
 * @internal
 * convert the index component stored in the context to an oid
 */
int
dot11ConfigRadioTable_index_to_oid(netsnmp_index *oid_idx,
                         dot11ConfigRadioTable_mib_index *mib_idx)
{
    int err = SNMP_ERR_NOERROR;
    
    /*
     * temp storage for parsing indexes
     */
    /*
     * wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    netsnmp_variable_list var_wtpMacAddr;
    /*
     * wtpWirelessIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    netsnmp_variable_list var_wtpWirelessIfIndex;

    /*
     * set up varbinds
     */
    memset( &var_wtpMacAddr, 0x00, sizeof(var_wtpMacAddr) );
    var_wtpMacAddr.type = ASN_OCTET_STR;
    memset( &var_wtpWirelessIfIndex, 0x00, sizeof(var_wtpWirelessIfIndex) );
    var_wtpWirelessIfIndex.type = ASN_INTEGER;

    /*
     * chain temp index varbinds together
     */
    var_wtpMacAddr.next_variable =  &var_wtpWirelessIfIndex; var_wtpWirelessIfIndex.next_variable =  NULL;


    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:dot11ConfigRadioTable_index_to_oid","called\n"));

        /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
    snmp_set_var_value(&var_wtpMacAddr, (u_char*)&mib_idx->wtpMacAddr,
                       mib_idx->wtpMacAddr_len * sizeof(mib_idx->wtpMacAddr[0]));

        /* wtpWirelessIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    snmp_set_var_value(&var_wtpWirelessIfIndex, (u_char*)&mib_idx->wtpWirelessIfIndex,
                       sizeof(mib_idx->wtpWirelessIfIndex));


    err = build_oid_noalloc(oid_idx->oids, oid_idx->len, &oid_idx->len,
                           NULL, 0, &var_wtpMacAddr);
    if(err)
        snmp_log(LOG_ERR,"error %d converting index to oid\n", err);

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers( &var_wtpMacAddr );

    return err;
} /* dot11ConfigRadioTable_index_to_oid */

/**
 * extract dot11ConfigRadioTable indexes from a netsnmp_index
 *
 * @retval SNMP_ERR_NOERROR  : no error
 * @retval SNMP_ERR_GENERR   : error
 */
int
dot11ConfigRadioTable_index_from_oid(netsnmp_index *oid_idx,
                         dot11ConfigRadioTable_mib_index *mib_idx)
{
    int err = SNMP_ERR_NOERROR;
    
    /*
     * temp storage for parsing indexes
     */
    /*
     * wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    netsnmp_variable_list var_wtpMacAddr;
    /*
     * wtpWirelessIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    netsnmp_variable_list var_wtpWirelessIfIndex;

    /*
     * set up varbinds
     */
    memset( &var_wtpMacAddr, 0x00, sizeof(var_wtpMacAddr) );
    var_wtpMacAddr.type = ASN_OCTET_STR;
    memset( &var_wtpWirelessIfIndex, 0x00, sizeof(var_wtpWirelessIfIndex) );
    var_wtpWirelessIfIndex.type = ASN_INTEGER;

    /*
     * chain temp index varbinds together
     */
    var_wtpMacAddr.next_variable =  &var_wtpWirelessIfIndex; var_wtpWirelessIfIndex.next_variable =  NULL;


    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:dot11ConfigRadioTable_index_from_oid","called\n"));

    /*
     * parse the oid into the individual index components
     */
    err = parse_oid_indexes( oid_idx->oids, oid_idx->len,
                             &var_wtpMacAddr );
    if (err == SNMP_ERR_NOERROR) {
        /*
         * copy out values
         */
    /*
     * NOTE: val_len is in bytes, wtpMacAddr_len might not be
     */
         if(var_wtpMacAddr.val_len > sizeof(mib_idx->wtpMacAddr))
             err = SNMP_ERR_GENERR;
         else {
             memcpy(mib_idx->wtpMacAddr, var_wtpMacAddr.val.string, var_wtpMacAddr.val_len);
             mib_idx->wtpMacAddr_len = var_wtpMacAddr.val_len / sizeof(mib_idx->wtpMacAddr[0]);
         }
    mib_idx->wtpWirelessIfIndex = *((long *)var_wtpWirelessIfIndex.val.string);


    }

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers( &var_wtpMacAddr );

    return err;
} /* dot11ConfigRadioTable_index_from_oid */


/*
 * dot11ConfigRadioTable_allocate_data
 *
 * Purpose: create new dot11ConfigRadioTable_data.
 */
dot11ConfigRadioTable_data *
dot11ConfigRadioTable_allocate_data(void)
{
    dot11ConfigRadioTable_data *rtn = SNMP_MALLOC_TYPEDEF(dot11ConfigRadioTable_data);

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:dot11ConfigRadioTable_allocate_data","called\n"));

    if(NULL == rtn) {
        snmp_log(LOG_ERR, "unable to malloc memory for new "
                 "dot11ConfigRadioTable_data.\n");
    }

    return rtn;
} /* dot11ConfigRadioTable_allocate_data */

/*
 * dot11ConfigRadioTable_release_data
 *
 * Purpose: release dot11ConfigRadioTable data.
 */
void
dot11ConfigRadioTable_release_data(dot11ConfigRadioTable_data *data)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:dot11ConfigRadioTable_release_data","called\n"));

    free(data);
} /* dot11ConfigRadioTable_release_data */

/* *********************************************************************
 * @internal
 * allocate resources for a dot11ConfigRadioTable_rowreq_ctx
 */
dot11ConfigRadioTable_rowreq_ctx *
dot11ConfigRadioTable_allocate_rowreq_ctx(void)
{
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx =
                  SNMP_MALLOC_TYPEDEF(dot11ConfigRadioTable_rowreq_ctx);

    DEBUGMSGTL(("internal:dot11ConfigRadioTable:dot11ConfigRadioTable_allocate_rowreq_ctx","called\n"));

    if(NULL == rowreq_ctx) {
        snmp_log(LOG_ERR,"Couldn't allocate memory for a "
                 "dot11ConfigRadioTable_rowreq_ctx.\n");
    }

    rowreq_ctx->oid_idx.oids = rowreq_ctx->oid_tmp;

    rowreq_ctx->dot11ConfigRadioTable_data_list = NULL;
    rowreq_ctx->dot11ConfigRadioTable_reg = dot11ConfigRadioTable_if_ctx.user_ctx;


    return rowreq_ctx;
} /* dot11ConfigRadioTable_allocate_rowreq_ctx */

/*
 * @internal
 * release resources for a dot11ConfigRadioTable_rowreq_ctx
 */
void
dot11ConfigRadioTable_release_rowreq_ctx(dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:dot11ConfigRadioTable_release_rowreq_ctx","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    

    if(rowreq_ctx->undo)
        dot11ConfigRadioTable_release_data(rowreq_ctx->undo);
 
    /*
     * free index oid pointer
     */
    if(rowreq_ctx->oid_idx.oids != rowreq_ctx->oid_tmp)
        free(rowreq_ctx->oid_idx.oids);

    SNMP_FREE(rowreq_ctx);
} /* dot11ConfigRadioTable_release_rowreq_ctx */

/**
 * @internal
 * wrapper
 */
static int
_mfd_dot11ConfigRadioTable_pre_request(netsnmp_mib_handler *handler,
                            netsnmp_handler_registration *reginfo,
                            netsnmp_agent_request_info *agtreq_info,
                            netsnmp_request_info *requests)
{
    int rc = dot11ConfigRadioTable_pre_request(dot11ConfigRadioTable_if_ctx.user_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("internal:dot11ConfigRadioTable","error %d from "
                    "dot11ConfigRadioTable_pre_request\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }
    
    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigRadioTable_pre_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_dot11ConfigRadioTable_post_request(netsnmp_mib_handler *handler,
                             netsnmp_handler_registration *reginfo,
                             netsnmp_agent_request_info *agtreq_info,
                             netsnmp_request_info *requests)
{
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx;
    int rc = dot11ConfigRadioTable_post_request(dot11ConfigRadioTable_if_ctx.user_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("internal:dot11ConfigRadioTable","error %d from "
                    "dot11ConfigRadioTable_post_request\n", rc));
    }
    
    /*
     * if there are no errors, check for and handle row creation/deletion
     */
    rc = netsnmp_check_requests_error(requests);
    if ((SNMP_ERR_NOERROR == rc) &&
        (NULL !=
         (rowreq_ctx = netsnmp_container_table_row_extract(requests)))) {
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            rowreq_ctx->rowreq_flags &= ~MFD_ROW_CREATED;
            CONTAINER_INSERT(dot11ConfigRadioTable_if_ctx.container, rowreq_ctx);
        }
        else if (rowreq_ctx->rowreq_flags & MFD_ROW_DELETED) {
            CONTAINER_REMOVE(dot11ConfigRadioTable_if_ctx.container, rowreq_ctx);
            dot11ConfigRadioTable_release_rowreq_ctx(rowreq_ctx);
        }
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigRadioTable_post_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_dot11ConfigRadioTable_object_lookup(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_mfd_dot11ConfigRadioTable_object_lookup","called\n"));

    /*
     * get our context from mfd
     * dot11ConfigRadioTable_interface_ctx *if_ctx =
     *             (dot11ConfigRadioTable_interface_ctx *)reginfo->my_reg_void;
     */

    if(NULL == rowreq_ctx) {
        netsnmp_request_set_error_all(requests, SNMP_ERR_NOCREATION);
    }
    else {
        dot11ConfigRadioTable_row_prep(rowreq_ctx);
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigRadioTable_object_lookup */

/***********************************************************************
 *
 * GET processing
 *
 ***********************************************************************/
/*
 * @internal
 * Retrieve the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigRadioTable_get_column( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_mfd_dot11ConfigRadioTable_get_column","called\n"));


    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* radioChannel(1)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOCHANNEL:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioChannel_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioTxPower(2)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOTXPOWER:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioTxPower_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioType(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOTYPE:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = radioType_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* radioSpeed(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOSPEED:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioSpeed_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioBeacon(5)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOBEACON:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioBeacon_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioFragThreshold(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOFRAGTHRESHOLD:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioFragThreshold_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioDtim(7)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIODTIM:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioDtim_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioRTS(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIORTS:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioRTS_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioLeadCode(9)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOLEADCODE:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = radioLeadCode_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* radioShortRetry(10)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOSHORTRETRY:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioShortRetry_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioLongRetry(11)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOLONGRETRY:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioLongRetry_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioService(12)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOSERVICE:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = radioService_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* radioMaxSpeed(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOMAXSPEED:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioMaxSpeed_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioMaxFlow(14)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOMAXFLOW:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioMaxFlow_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioBindWlan(15)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOBINDWLAN:
    var->type = ASN_OCTET_STR;
rc = radioBindWlan_get(rowreq_ctx, (char **)&var->val.string, &var->val_len );
        break;

    /* radioDelbindWlan(16)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIODELBINDWLAN:
    var->type = ASN_OCTET_STR;
rc = radioDelbindWlan_get(rowreq_ctx, (char **)&var->val.string, &var->val_len );
        break;

    /* radioBindQos(17)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOBINDQOS:
    var->type = ASN_OCTET_STR;
rc = radioBindQos_get(rowreq_ctx, (char **)&var->val.string, &var->val_len );
        break;

    /* radioDelbindQos(18)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIODELBINDQOS:
    var->type = ASN_OCTET_STR;
rc = radioDelbindQos_get(rowreq_ctx, (char **)&var->val.string, &var->val_len );
        break;

    /* radioAmpduSwitch(19)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOAMPDUSWITCH:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = radioAmpduSwitch_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* radioCwmode(20)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOCWMODE:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = radioCwmode_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* radioShortGISwitch(21)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOSHORTGISWITCH:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = radioShortGISwitch_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* radioAmsduSwitch(22)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOAMSDUSWITCH:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = radioAmsduSwitch_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* radioMCS(23)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOMCS:
	var->type = ASN_OCTET_STR;
rc = radioMCS_get(rowreq_ctx, (char **)&var->val.string, &var->val_len );

        break;

    /* radio11nSwitch(24)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIO11NSWITCH:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = radio11nSwitch_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* OnlyAllow11nStaAccess(25)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_ONLYALLOW11NSTAACCESS:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = OnlyAllow11nStaAccess_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* radioRole(28)/INTEGER/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h */
    case COLUMN_RADIOROLE:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = radioRole_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* radioChanelUsage(29)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    case COLUMN_RADIOCHANELUSAGE:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioChanelUsage_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioChanelChangeNum(30)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    case COLUMN_RADIOCHANELCHANGENUM:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioChanelChangeNum_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioChannelWidth(31)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    case COLUMN_RADIOCHANNELWIDTH:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioChannelWidth_get(rowreq_ctx, (long *)var->val.string );
        break;

    /* radioNoiseFloor(32)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    case COLUMN_RADIONOISEFLOOR:
    var->val_len = sizeof(long);
    var->type = ASN_INTEGER;
rc = radioNoiseFloor_get(rowreq_ctx, (long *)var->val.string );
        break;
     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigRadioTable_get_column\n", column);
         break;
    }

    return rc;
} /* _dot11ConfigRadioTable_get_column */

int
_mfd_dot11ConfigRadioTable_get_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    u_char                     * old_string;
    void                      (*dataFreeHook)(void *);
    int                        rc;

    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_mfd_dot11ConfigRadioTable_get_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    for(;requests; requests = requests->next) {
        /*
         * save old pointer, so we can free it if replaced
         */
        old_string = requests->requestvb->val.string;
        dataFreeHook = requests->requestvb->dataFreeHook;
        if(NULL == requests->requestvb->val.string) {
            requests->requestvb->val.string = requests->requestvb->buf;
            requests->requestvb->val_len = sizeof(requests->requestvb->buf);
        }
        else if(requests->requestvb->buf == requests->requestvb->val.string) {
            if(requests->requestvb->val_len != sizeof(requests->requestvb->buf))
                requests->requestvb->val_len = sizeof(requests->requestvb->buf);
        }

        /*
         * get column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _dot11ConfigRadioTable_get_column(rowreq_ctx, requests->requestvb, tri->colnum);
        if(rc) {
            if(MFD_SKIP == rc) {
                requests->requestvb->type = ASN_PRIV_RETRY;
                rc = SNMP_ERR_NOERROR;
            }
        }
        else if (NULL == requests->requestvb->val.string) {
            snmp_log(LOG_ERR,"NULL varbind data pointer!\n");
            rc = SNMP_ERR_GENERR;
        }
        if(rc)
            netsnmp_request_set_error(requests, SNMP_VALIDATE_ERR(rc));

        /*
         * if the buffer wasn't used previously for the old data (i.e. it
         * was allcoated memory)  and the get routine replaced the pointer,
         * we need to free the previous pointer.
         */
        if(old_string && (old_string != requests->requestvb->buf) &&
           (requests->requestvb->val.string != old_string)) {
            if(dataFreeHook)
                (*dataFreeHook)(old_string);
            else
                free(old_string);
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigRadioTable_get_values */

/***********************************************************************
 *
 * SET processing
 *
 ***********************************************************************/

/*----------------------------------------------------------------------
 *
 * SET: Syntax checks
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Check the syntax for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigRadioTable_check_column( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx,
                         netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_dot11ConfigRadioTable_check_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* radioChannel(1)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOCHANNEL:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioChannel ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = radioChannel_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioChannel_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioTxPower(2)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOTXPOWER:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioTxPower ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    #if 0
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, 1,27)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
	#endif	
	if ((*var->val.integer < 1) || (*var->val.integer > 27)) 
	{
	   rc = SNMP_ERR_WRONGVALUE;
	}
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioTxPower_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioTxPower_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioType(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOTYPE:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioType ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != RADIOTYPE_A )
 && ( *var->val.integer != RADIOTYPE_B )
 && ( *var->val.integer != RADIOTYPE_G )
 && ( *var->val.integer != RADIOTYPE_BG )
 && ( *var->val.integer != RADIOTYPE_AN )
 && ( *var->val.integer != RADIOTYPE_AAN )
 && ( *var->val.integer != RADIOTYPE_GN )
 && ( *var->val.integer != RADIOTYPE_GGN )
 && ( *var->val.integer != RADIOTYPE_BGGN )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioType_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioType_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioSpeed(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOSPEED:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioSpeed ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = radioSpeed_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioSpeed_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioBeacon(5)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOBEACON:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioBeacon ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    #if 0
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, 25,1000)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
	#endif
	if ((*var->val.integer < 25) || (*var->val.integer > 1000)) 
	{
	   rc = SNMP_ERR_WRONGVALUE;
	}
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioBeacon_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioBeacon_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioFragThreshold(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOFRAGTHRESHOLD:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioFragThreshold ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    #if 0
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, 256,2346)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
	#endif
	if ((*var->val.integer < 256) || (*var->val.integer > 2346)) 
	{
	   rc = SNMP_ERR_WRONGVALUE;
	}
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioFragThreshold_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioFragThreshold_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioDtim(7)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIODTIM:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioDtim ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    #if 0
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, 1,15)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
	#endif
	if ((*var->val.integer < 1) || (*var->val.integer > 15)) 
	{
	   rc = SNMP_ERR_WRONGVALUE;
	}
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioDtim_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioDtim_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioRTS(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIORTS:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioRTS ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    #if 0
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, 256,2346)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
	#endif
	if ((*var->val.integer < 256) || (*var->val.integer > 2346)) 
	{
	   rc = SNMP_ERR_WRONGVALUE;
	}
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioRTS_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioRTS_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioLeadCode(9)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOLEADCODE:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioLeadCode ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != RADIOLEADCODE_SHORT )
 && ( *var->val.integer != RADIOLEADCODE_LONG )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioLeadCode_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioLeadCode_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioShortRetry(10)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOSHORTRETRY:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioShortRetry ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    #if 0
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, 1,15)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
	#endif
	if ((*var->val.integer < 1) || (*var->val.integer > 15)) 
	{
	   rc = SNMP_ERR_WRONGVALUE;
	}
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioShortRetry_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioShortRetry_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioLongRetry(11)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOLONGRETRY:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioLongRetry ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    #if 0
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, 1,15)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
	#endif
	if ((*var->val.integer < 1) || (*var->val.integer > 15)) 
	{
	   rc = SNMP_ERR_WRONGVALUE;
	}
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioLongRetry_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioLongRetry_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioService(12)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOSERVICE:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioService ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != RADIOSERVICE_DISABLE )
 && ( *var->val.integer != RADIOSERVICE_ENABLE )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioService_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioService_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioMaxSpeed(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOMAXSPEED:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioMaxSpeed ) );
    /* yyy-rks: anything else we can do here? */
    if(SNMPERR_SUCCESS == rc) {
        rc = radioMaxSpeed_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioMaxSpeed_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioMaxFlow(14)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOMAXFLOW:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioMaxFlow ) );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    #if 0
    if( 1
       && ((rc = netsnmp_check_vb_int_range(var, 1,108)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
	#endif
	if ((*var->val.integer < 1) || (*var->val.integer > 108)) 
	{
	   rc = SNMP_ERR_WRONGVALUE;
	}
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioMaxFlow_check_value( rowreq_ctx, *((long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioMaxFlow_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioBindWlan(15)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOBINDWLAN:
    rc = netsnmp_check_vb_type( var, ASN_OCTET_STR );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    if( 1
       && ((rc = netsnmp_check_vb_size_range(var, 0,255)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioBindWlan_check_value( rowreq_ctx, (char *)var->val.string, var->val_len );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioBindWlan_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioDelbindWlan(16)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIODELBINDWLAN:
    rc = netsnmp_check_vb_type( var, ASN_OCTET_STR );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    if( 1
       && ((rc = netsnmp_check_vb_size_range(var, 0,255)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioDelbindWlan_check_value( rowreq_ctx, (char *)var->val.string, var->val_len );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioDelbindWlan_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioBindQos(17)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOBINDQOS:
    rc = netsnmp_check_vb_type( var, ASN_OCTET_STR );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    if( 1
       && ((rc = netsnmp_check_vb_size_range(var, 0,255)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioBindQos_check_value( rowreq_ctx, (char *)var->val.string, var->val_len );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioBindQos_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioDelbindQos(18)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIODELBINDQOS:
    rc = netsnmp_check_vb_type( var, ASN_OCTET_STR );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    if( 1
       && ((rc = netsnmp_check_vb_size_range(var, 0,255)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioDelbindQos_check_value( rowreq_ctx, (char *)var->val.string, var->val_len );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioDelbindQos_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioAmpduSwitch(19)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOAMPDUSWITCH:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioAmpduSwitch ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != RADIOAMPDUSWITCH_OFF )
 && ( *var->val.integer != RADIOAMPDUSWITCH_ON )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioAmpduSwitch_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioAmpduSwitch_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioCwmode(20)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOCWMODE:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioCwmode ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != RADIOCWMODE_HT20 )
 && ( *var->val.integer != RADIOCWMODE_HT20_HT40 )
 && ( *var->val.integer != RADIOCWMODE_HT40 )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioCwmode_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioCwmode_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioShortGISwitch(21)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOSHORTGISWITCH:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioShortGISwitch ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != RADIOSHORTGISWITCH_OFF )
 && ( *var->val.integer != RADIOSHORTGISWITCH_ON )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioShortGISwitch_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioShortGISwitch_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioAmsduSwitch(22)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOAMSDUSWITCH:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radioAmsduSwitch ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != RADIOAMSDUSWITCH_OFF )
 && ( *var->val.integer != RADIOAMSDUSWITCH_ON )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioAmsduSwitch_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioAmsduSwitch_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioMCS(23)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOMCS:
    rc = netsnmp_check_vb_type( var, ASN_OCTET_STR );
    if(SNMPERR_SUCCESS == rc) {
    /* check that the value is in the defined range(s); inefficent
     * but keeps rc value knowledge in libarary where it belongs. */
    if( 1
       && ((rc = netsnmp_check_vb_size_range(var, 0,255)) != SNMP_ERR_NOERROR)
     ) {
        ; /* rc set in condition */
    }
   }
    if(SNMPERR_SUCCESS == rc) {
        rc = radioMCS_check_value( rowreq_ctx, (char *)var->val.string, var->val_len );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radioMCS_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radio11nSwitch(24)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIO11NSWITCH:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.radio11nSwitch ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != RADIO11NSWITCH_OFF )
 && ( *var->val.integer != RADIO11NSWITCH_ON )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = radio11nSwitch_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from radio11nSwitch_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* OnlyAllow11nStaAccess(25)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_ONLYALLOW11NSTAACCESS:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.OnlyAllow11nStaAccess ) );
    if(SNMPERR_SUCCESS == rc) {

    /* check that the value is one of defined enums */
    if( 1
 && ( *var->val.integer != TRUTHVALUE_TRUE )
 && ( *var->val.integer != TRUTHVALUE_FALSE )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    }
    if(SNMPERR_SUCCESS == rc) {
        rc = OnlyAllow11nStaAccess_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from OnlyAllow11nStaAccess_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* radioRole(28)/INTEGER/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h */
    case COLUMN_RADIOROLE:
        rc = SNMP_ERR_NOTWRITABLE;
        break;

    /* radioChanelUsage(29)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    case COLUMN_RADIOCHANELUSAGE:
        rc = SNMP_ERR_NOTWRITABLE;
        break;

    /* radioChanelChangeNum(30)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    case COLUMN_RADIOCHANELCHANGENUM:
        rc = SNMP_ERR_NOTWRITABLE;
        break;

    /* radioChannelWidth(31)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    case COLUMN_RADIOCHANNELWIDTH:
        rc = SNMP_ERR_NOTWRITABLE;
        break;

    /* radioNoiseFloor(32)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    case COLUMN_RADIONOISEFLOOR:
        rc = SNMP_ERR_NOTWRITABLE;
        break;

        default: /** We shouldn't get here */
            rc = SNMP_ERR_GENERR;
            snmp_log(LOG_ERR, "unknown column %d in _dot11ConfigRadioTable_check_column\n", column);
    }

    return rc;
} /* _dot11ConfigRadioTable_check_column */

int
_mfd_dot11ConfigRadioTable_check_objects(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    int                          rc;

    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_mfd_dot11ConfigRadioTable_check_objects","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    for(;requests; requests = requests->next) {

        /*
         * get column number from table request info, and check that column
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;

        rc = _dot11ConfigRadioTable_check_column(rowreq_ctx, requests->requestvb, tri->colnum);
        if(rc) {
            netsnmp_request_set_error(requests, SNMP_VALIDATE_ERR(rc));
            break;
        }

    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigRadioTable_check_objects */



/*----------------------------------------------------------------------
 *
 * SET: check dependencies
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Check dependencies wrapper
 */
static int
_mfd_dot11ConfigRadioTable_check_dependencies(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_mfd_dot11ConfigRadioTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    rc = dot11ConfigRadioTable_check_dependencies(rowreq_ctx);
    if(rc){
        DEBUGMSGTL(("verbose:dot11ConfigRadioTable:mfd","error %d from "
                    "dot11ConfigRadioTable_check_dependencies\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigRadioTable_check_dependencies */

/*----------------------------------------------------------------------
 *
 * SET: Undo setup
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Set the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigRadioTable_undo_setup_column( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_dot11ConfigRadioTable_undo_setup_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* radioChannel(1)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOCHANNEL:
        rowreq_ctx->column_set_flags |= FLAG_RADIOCHANNEL;
        rc = radioChannel_undo_setup(rowreq_ctx );
        break;

    /* radioTxPower(2)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOTXPOWER:
        rowreq_ctx->column_set_flags |= FLAG_RADIOTXPOWER;
        rc = radioTxPower_undo_setup(rowreq_ctx );
        break;

    /* radioType(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOTYPE:
        rowreq_ctx->column_set_flags |= FLAG_RADIOTYPE;
        rc = radioType_undo_setup(rowreq_ctx );
        break;

    /* radioSpeed(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOSPEED:
        rowreq_ctx->column_set_flags |= FLAG_RADIOSPEED;
        rc = radioSpeed_undo_setup(rowreq_ctx );
        break;

    /* radioBeacon(5)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOBEACON:
        rowreq_ctx->column_set_flags |= FLAG_RADIOBEACON;
        rc = radioBeacon_undo_setup(rowreq_ctx );
        break;

    /* radioFragThreshold(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOFRAGTHRESHOLD:
        rowreq_ctx->column_set_flags |= FLAG_RADIOFRAGTHRESHOLD;
        rc = radioFragThreshold_undo_setup(rowreq_ctx );
        break;

    /* radioDtim(7)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIODTIM:
        rowreq_ctx->column_set_flags |= FLAG_RADIODTIM;
        rc = radioDtim_undo_setup(rowreq_ctx );
        break;

    /* radioRTS(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIORTS:
        rowreq_ctx->column_set_flags |= FLAG_RADIORTS;
        rc = radioRTS_undo_setup(rowreq_ctx );
        break;

    /* radioLeadCode(9)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOLEADCODE:
        rowreq_ctx->column_set_flags |= FLAG_RADIOLEADCODE;
        rc = radioLeadCode_undo_setup(rowreq_ctx );
        break;

    /* radioShortRetry(10)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOSHORTRETRY:
        rowreq_ctx->column_set_flags |= FLAG_RADIOSHORTRETRY;
        rc = radioShortRetry_undo_setup(rowreq_ctx );
        break;

    /* radioLongRetry(11)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOLONGRETRY:
        rowreq_ctx->column_set_flags |= FLAG_RADIOLONGRETRY;
        rc = radioLongRetry_undo_setup(rowreq_ctx );
        break;

    /* radioService(12)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOSERVICE:
        rowreq_ctx->column_set_flags |= FLAG_RADIOSERVICE;
        rc = radioService_undo_setup(rowreq_ctx );
        break;

    /* radioMaxSpeed(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOMAXSPEED:
        rowreq_ctx->column_set_flags |= FLAG_RADIOMAXSPEED;
        rc = radioMaxSpeed_undo_setup(rowreq_ctx );
        break;

    /* radioMaxFlow(14)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOMAXFLOW:
        rowreq_ctx->column_set_flags |= FLAG_RADIOMAXFLOW;
        rc = radioMaxFlow_undo_setup(rowreq_ctx );
        break;

    /* radioBindWlan(15)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOBINDWLAN:
        rowreq_ctx->column_set_flags |= FLAG_RADIOBINDWLAN;
        rc = radioBindWlan_undo_setup(rowreq_ctx );
        break;

    /* radioDelbindWlan(16)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIODELBINDWLAN:
        rowreq_ctx->column_set_flags |= FLAG_RADIODELBINDWLAN;
        rc = radioDelbindWlan_undo_setup(rowreq_ctx );
        break;

    /* radioBindQos(17)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOBINDQOS:
        rowreq_ctx->column_set_flags |= FLAG_RADIOBINDQOS;
        rc = radioBindQos_undo_setup(rowreq_ctx );
        break;

    /* radioDelbindQos(18)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIODELBINDQOS:
        rowreq_ctx->column_set_flags |= FLAG_RADIODELBINDQOS;
        rc = radioDelbindQos_undo_setup(rowreq_ctx );
        break;

    /* radioAmpduSwitch(19)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOAMPDUSWITCH:
        rowreq_ctx->column_set_flags |= FLAG_RADIOAMPDUSWITCH;
        rc = radioAmpduSwitch_undo_setup(rowreq_ctx );
        break;

    /* radioCwmode(20)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOCWMODE:
        rowreq_ctx->column_set_flags |= FLAG_RADIOCWMODE;
        rc = radioCwmode_undo_setup(rowreq_ctx );
        break;

    /* radioShortGISwitch(21)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOSHORTGISWITCH:
        rowreq_ctx->column_set_flags |= FLAG_RADIOSHORTGISWITCH;
        rc = radioShortGISwitch_undo_setup(rowreq_ctx );
        break;

    /* radioAmsduSwitch(22)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOAMSDUSWITCH:
        rowreq_ctx->column_set_flags |= FLAG_RADIOAMSDUSWITCH;
        rc = radioAmsduSwitch_undo_setup(rowreq_ctx );
        break;

    /* radioMCS(23)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOMCS:
        rowreq_ctx->column_set_flags |= FLAG_RADIOMCS;
        rc = radioMCS_undo_setup(rowreq_ctx );
        break;

    /* radio11nSwitch(24)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIO11NSWITCH:
        rowreq_ctx->column_set_flags |= FLAG_RADIO11NSWITCH;
        rc = radio11nSwitch_undo_setup(rowreq_ctx );
        break;

    /* OnlyAllow11nStaAccess(25)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_ONLYALLOW11NSTAACCESS:
        rowreq_ctx->column_set_flags |= FLAG_ONLYALLOW11NSTAACCESS;
        rc = OnlyAllow11nStaAccess_undo_setup(rowreq_ctx );
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigRadioTable_undo_setup_column\n", column);
         break;
    }

    return rc;
} /* _dot11ConfigRadioTable_undo_setup_column */


/**
 * @internal
 * undo setup
 */
int
_mfd_dot11ConfigRadioTable_undo_setup(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_mfd_dot11ConfigRadioTable_undo_setup","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * allocate undo context
     */
    rowreq_ctx->undo = dot11ConfigRadioTable_allocate_data();
    if(NULL == rowreq_ctx->undo) {
        /** msg already logged */
        netsnmp_request_set_error_all(requests, SNMP_ERR_RESOURCEUNAVAILABLE);
        return SNMP_ERR_NOERROR;
    }

    /*
     * row undo setup
     */
    rowreq_ctx->column_set_flags = 0;
    rc = dot11ConfigRadioTable_undo_setup(rowreq_ctx);
    if (MFD_SUCCESS != rc)  {
        DEBUGMSGTL(("verbose:dot11ConfigRadioTable:mfd","error %d from "
                    "dot11ConfigRadioTable_undo_setup\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }
    else {
        /*
         * column undo setup
         */
        netsnmp_table_request_info * tri;
        for(;requests; requests = requests->next) {
            /*
             * set column data
             */
            tri = netsnmp_extract_table_info(requests);
            if(NULL == tri)
                continue;
            
            rc = _dot11ConfigRadioTable_undo_setup_column(rowreq_ctx, tri->colnum);
            if(MFD_SUCCESS != rc)  {
                DEBUGMSGTL(("verbose:dot11ConfigRadioTable:mfd","error %d from "
                            "dot11ConfigRadioTable_undo_setup_column\n", rc));
                netsnmp_set_request_error(agtreq_info, requests, SNMP_VALIDATE_ERR(rc));
            }
        } /* for results */
    }
    
    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigRadioTable_undo_setup */

/**
 * @internal
 * undo setup
 */
int
_mfd_dot11ConfigRadioTable_undo_cleanup(netsnmp_mib_handler *handler,
                             netsnmp_handler_registration *reginfo,
                             netsnmp_agent_request_info *agtreq_info,
                             netsnmp_request_info *requests)
{
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    int rc;

    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_mfd_dot11ConfigRadioTable_undo_cleanup","called\n"));

    /*
     * failed row create in early stages has no rowreq_ctx
     */
    if (NULL == rowreq_ctx)
        return MFD_SUCCESS;

    /*
     * call user cleanup
     */
    rc = dot11ConfigRadioTable_undo_cleanup(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("verbose:dot11ConfigRadioTable:mfd","error %d from "
                    "dot11ConfigRadioTable_undo_cleanup\n", rc));
    }

    /*
     * release undo context, if needed
     */
    if(rowreq_ctx->undo) {
         dot11ConfigRadioTable_release_data(rowreq_ctx->undo);
         rowreq_ctx->undo = NULL;
    }

    /*
     * clear set flags
     */
    rowreq_ctx->column_set_flags = 0;

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigRadioTable_undo_cleanup */

/*----------------------------------------------------------------------
 *
 * SET: Set values
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Set the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigRadioTable_set_column( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_dot11ConfigRadioTable_set_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* radioChannel(1)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOCHANNEL:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOCHANNEL;
        rc = radioChannel_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* radioTxPower(2)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOTXPOWER:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOTXPOWER;
        rc = radioTxPower_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* radioType(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOTYPE:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOTYPE;
        rc = radioType_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* radioSpeed(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOSPEED:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOSPEED;
        rc = radioSpeed_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* radioBeacon(5)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOBEACON:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOBEACON;
        rc = radioBeacon_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* radioFragThreshold(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOFRAGTHRESHOLD:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOFRAGTHRESHOLD;
        rc = radioFragThreshold_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* radioDtim(7)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIODTIM:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIODTIM;
        rc = radioDtim_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* radioRTS(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIORTS:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIORTS;
        rc = radioRTS_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* radioLeadCode(9)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOLEADCODE:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOLEADCODE;
        rc = radioLeadCode_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* radioShortRetry(10)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOSHORTRETRY:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOSHORTRETRY;
        rc = radioShortRetry_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* radioLongRetry(11)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOLONGRETRY:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOLONGRETRY;
        rc = radioLongRetry_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* radioService(12)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOSERVICE:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOSERVICE;
        rc = radioService_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* radioMaxSpeed(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOMAXSPEED:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOMAXSPEED;
        rc = radioMaxSpeed_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* radioMaxFlow(14)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOMAXFLOW:
        if(var->val_len != sizeof(long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOMAXFLOW;
        rc = radioMaxFlow_set(rowreq_ctx, *((long *)var->val.string) );
        break;

    /* radioBindWlan(15)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOBINDWLAN:
        rowreq_ctx->column_set_flags |= FLAG_RADIOBINDWLAN;
        rc = radioBindWlan_set(rowreq_ctx, (char *)var->val.string, var->val_len );
        break;

    /* radioDelbindWlan(16)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIODELBINDWLAN:
        rowreq_ctx->column_set_flags |= FLAG_RADIODELBINDWLAN;
        rc = radioDelbindWlan_set(rowreq_ctx, (char *)var->val.string, var->val_len );
        break;

    /* radioBindQos(17)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOBINDQOS:
        rowreq_ctx->column_set_flags |= FLAG_RADIOBINDQOS;
        rc = radioBindQos_set(rowreq_ctx, (char *)var->val.string, var->val_len );
        break;

    /* radioDelbindQos(18)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIODELBINDQOS:
        rowreq_ctx->column_set_flags |= FLAG_RADIODELBINDQOS;
        rc = radioDelbindQos_set(rowreq_ctx, (char *)var->val.string, var->val_len );
        break;

    /* radioAmpduSwitch(19)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOAMPDUSWITCH:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOAMPDUSWITCH;
        rc = radioAmpduSwitch_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* radioCwmode(20)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOCWMODE:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOCWMODE;
        rc = radioCwmode_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* radioShortGISwitch(21)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOSHORTGISWITCH:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOSHORTGISWITCH;
        rc = radioShortGISwitch_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* radioAmsduSwitch(22)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOAMSDUSWITCH:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIOAMSDUSWITCH;
        rc = radioAmsduSwitch_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* radioMCS(23)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOMCS:
		rowreq_ctx->column_set_flags |= FLAG_RADIOMCS;
        rc = radioMCS_set(rowreq_ctx, (char *)var->val.string, var->val_len );
        break;

    /* radio11nSwitch(24)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIO11NSWITCH:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_RADIO11NSWITCH;
        rc = radio11nSwitch_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* OnlyAllow11nStaAccess(25)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_ONLYALLOW11NSTAACCESS:
        if(var->val_len != sizeof(u_long)) {
            rc = SNMP_ERR_WRONGLENGTH;
            snmp_log(LOG_ERR,
                     "varbind size of %d does not match expected size %d\n",
                     var->val_len, sizeof(u_long));
            break;
        }
        rowreq_ctx->column_set_flags |= FLAG_ONLYALLOW11NSTAACCESS;
        rc = OnlyAllow11nStaAccess_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigRadioTable_set_column\n", column);
         break;
    }
    
    return rc;
} /* _dot11ConfigRadioTable_set_column */

int
_mfd_dot11ConfigRadioTable_set_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    int                          rc = SNMP_ERR_NOERROR;

    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_mfd_dot11ConfigRadioTable_set_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    rowreq_ctx->column_set_flags = 0;
    for(;requests; requests = requests->next) {
        /*
         * set column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _dot11ConfigRadioTable_set_column(rowreq_ctx,
                                    requests->requestvb, tri->colnum);
        if(MFD_SUCCESS != rc)  {
            DEBUGMSGTL(("verbose:dot11ConfigRadioTable:mfd","error %d from "
                        "dot11ConfigRadioTable_set_column\n", rc));
            netsnmp_set_request_error(agtreq_info, requests, SNMP_VALIDATE_ERR(rc));
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigRadioTable_set_values */

/*----------------------------------------------------------------------
 *
 * SET: commit
 *
 *---------------------------------------------------------------------*/
/**
 * @internal
 * commit the values
 */
int
_mfd_dot11ConfigRadioTable_commit(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_mfd_dot11ConfigRadioTable_commit","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    rc = dot11ConfigRadioTable_commit(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        DEBUGMSGTL(("verbose:dot11ConfigRadioTable:mfd","error %d from "
                    "dot11ConfigRadioTable_commit\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }

    return SNMP_ERR_NOERROR;
}

int
_mfd_dot11ConfigRadioTable_undo_commit(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_mfd_dot11ConfigRadioTable_undo_commit","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    rc = dot11ConfigRadioTable_undo_commit(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("verbose:dot11ConfigRadioTable:mfd","error %d from "
                    "dot11ConfigRadioTable_undo_commit\n", rc));
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigRadioTable_commit */

/*----------------------------------------------------------------------
 *
 * SET: Undo
 *
 *---------------------------------------------------------------------*/
/**
 * @internal
 * undo the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_dot11ConfigRadioTable_undo_column( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_dot11ConfigRadioTable_undo_column","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* radioChannel(1)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOCHANNEL:
        rc = radioChannel_undo(rowreq_ctx);
        break;

    /* radioTxPower(2)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOTXPOWER:
        rc = radioTxPower_undo(rowreq_ctx);
        break;

    /* radioType(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOTYPE:
        rc = radioType_undo(rowreq_ctx);
        break;

    /* radioSpeed(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOSPEED:
        rc = radioSpeed_undo(rowreq_ctx);
        break;

    /* radioBeacon(5)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOBEACON:
        rc = radioBeacon_undo(rowreq_ctx);
        break;

    /* radioFragThreshold(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOFRAGTHRESHOLD:
        rc = radioFragThreshold_undo(rowreq_ctx);
        break;

    /* radioDtim(7)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIODTIM:
        rc = radioDtim_undo(rowreq_ctx);
        break;

    /* radioRTS(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIORTS:
        rc = radioRTS_undo(rowreq_ctx);
        break;

    /* radioLeadCode(9)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOLEADCODE:
        rc = radioLeadCode_undo(rowreq_ctx);
        break;

    /* radioShortRetry(10)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOSHORTRETRY:
        rc = radioShortRetry_undo(rowreq_ctx);
        break;

    /* radioLongRetry(11)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOLONGRETRY:
        rc = radioLongRetry_undo(rowreq_ctx);
        break;

    /* radioService(12)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOSERVICE:
        rc = radioService_undo(rowreq_ctx);
        break;

    /* radioMaxSpeed(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h */
    case COLUMN_RADIOMAXSPEED:
        rc = radioMaxSpeed_undo(rowreq_ctx);
        break;

    /* radioMaxFlow(14)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOMAXFLOW:
        rc = radioMaxFlow_undo(rowreq_ctx);
        break;

    /* radioBindWlan(15)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOBINDWLAN:
        rc = radioBindWlan_undo(rowreq_ctx);
        break;

    /* radioDelbindWlan(16)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIODELBINDWLAN:
        rc = radioDelbindWlan_undo(rowreq_ctx);
        break;

    /* radioBindQos(17)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIOBINDQOS:
        rc = radioBindQos_undo(rowreq_ctx);
        break;

    /* radioDelbindQos(18)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_RADIODELBINDQOS:
        rc = radioDelbindQos_undo(rowreq_ctx);
        break;

    /* radioAmpduSwitch(19)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOAMPDUSWITCH:
        rc = radioAmpduSwitch_undo(rowreq_ctx);
        break;

    /* radioCwmode(20)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOCWMODE:
        rc = radioCwmode_undo(rowreq_ctx);
        break;

    /* radioShortGISwitch(21)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOSHORTGISWITCH:
        rc = radioShortGISwitch_undo(rowreq_ctx);
        break;

    /* radioAmsduSwitch(22)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIOAMSDUSWITCH:
        rc = radioAmsduSwitch_undo(rowreq_ctx);
        break;

    /* radioMCS(23)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    case COLUMN_RADIOMCS:
        rc = radioMCS_undo(rowreq_ctx);
        break;

    /* radio11nSwitch(24)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_RADIO11NSWITCH:
        rc = radio11nSwitch_undo(rowreq_ctx);
        break;

    /* OnlyAllow11nStaAccess(25)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_ONLYALLOW11NSTAACCESS:
        rc = OnlyAllow11nStaAccess_undo(rowreq_ctx);
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _dot11ConfigRadioTable_undo_column\n", column);
         break;
    }

    return rc;
} /* _dot11ConfigRadioTable_undo_column */

int
_mfd_dot11ConfigRadioTable_undo_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx =
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;

    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_mfd_dot11ConfigRadioTable_undo_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    for(;requests; requests = requests->next) {
        /*
         * set column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _dot11ConfigRadioTable_undo_column(rowreq_ctx, requests->requestvb,
                                     tri->colnum);
        if (MFD_SUCCESS != rc) {
            /*
             * nothing we can do about it but log it
             */
            DEBUGMSGTL(("verbose:dot11ConfigRadioTable:mfd","error %d from "
                        "dot11ConfigRadioTable_undo_column\n", rc));
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_dot11ConfigRadioTable_undo_values */

/***********************************************************************
 *
 * DATA ACCESS
 *
 ***********************************************************************/
/**
 * @internal
 */
static int
_cache_load(netsnmp_cache *cache, void *vmagic)
{
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_cache_load","called\n"));

    if((NULL == cache) || (NULL == cache->magic)) {
        snmp_log(LOG_ERR, "invalid cache for dot11ConfigRadioTable_cache_load\n");
        return -1;
    }

    /** should only be called for an invalid or expired cache */
    netsnmp_assert((0 == cache->valid) || (1 == cache->expired));
    
    /*
     * call user code
     */
    return dot11ConfigRadioTable_cache_load((netsnmp_container*)cache->magic);
} /* _cache_load */

/**
 * @internal
 */
static void
_cache_item_free(dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, void *context)
{
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_cache_item_free","called\n"));

    if(NULL == rowreq_ctx)
        return;

    dot11ConfigRadioTable_release_rowreq_ctx(rowreq_ctx);
} /* _cache_item_free */

/**
 * @internal
 */
static void
_cache_free(netsnmp_cache *cache, void *magic)
{
    netsnmp_container *container;

    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_cache_free","called\n"));

    if((NULL == cache) || (NULL == cache->magic)) {
        snmp_log(LOG_ERR, "invalid cache in dot11ConfigRadioTable_cache_free\n");
        return;
    }

    container = (netsnmp_container*)cache->magic;

    /*
     * call user code
     */
    dot11ConfigRadioTable_cache_free(container);
    
    /*
     * free all items. inefficient, but easy.
     */
    CONTAINER_CLEAR(container,
                    (netsnmp_container_obj_func *)_cache_item_free,
                    NULL);
} /* _cache_free */

/**
 * @internal
 * initialize the iterator container with functions or wrappers
 */
void
_dot11ConfigRadioTable_container_init(dot11ConfigRadioTable_interface_ctx *if_ctx)
{
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:_dot11ConfigRadioTable_container_init","called\n"));
    
    /*
     * set up the cache
     */
    if_ctx->cache = netsnmp_cache_create(30, /* timeout in seconds */
                                         _cache_load, _cache_free,
                                         dot11ConfigRadioTable_oid,
                                         dot11ConfigRadioTable_oid_size);

    if(NULL == if_ctx->cache) {
        snmp_log(LOG_ERR, "error creating cache for dot11ConfigRadioTable\n");
        return;
    }

    if_ctx->cache->flags = NETSNMP_CACHE_DONT_INVALIDATE_ON_SET;

    dot11ConfigRadioTable_container_init(&if_ctx->container, if_ctx->cache);
    if(NULL == if_ctx->container)
        if_ctx->container = netsnmp_container_find("dot11ConfigRadioTable:table_container");
    if(NULL == if_ctx->container) {
        snmp_log(LOG_ERR,"error creating container in "
                 "dot11ConfigRadioTable_container_init\n");
        return;
    }
    if_ctx->cache->magic = (void*)if_ctx->container;
} /* _dot11ConfigRadioTable_container_init */

