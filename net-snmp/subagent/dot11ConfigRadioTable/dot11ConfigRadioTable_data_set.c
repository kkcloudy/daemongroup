/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/ACDbusDef1.h"
#include "dot11ConfigRadioTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigRadioTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigRadioTable is subid 3 of wtpRadio.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigRadioTable_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:dot11ConfigRadioTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigRadioTable undo.
     * set up dot11ConfigRadioTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11ConfigRadioTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigRadioTable_undo_cleanup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:dot11ConfigRadioTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigRadioTable undo.
     */

    return rc;
} /* dot11ConfigRadioTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigRadioTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigRadioTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigRadioTable_commit( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:dot11ConfigRadioTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11ConfigRadioTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_RADIOCHANNEL) {
       save_flags &= ~FLAG_RADIOCHANNEL; /* clear radioChannel */
       /*
        * TODO:482:o: |-> commit column radioChannel.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioChannel commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioChannel
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOCHANNEL;
       }
    }

    if (save_flags & FLAG_RADIOTXPOWER) {
       save_flags &= ~FLAG_RADIOTXPOWER; /* clear radioTxPower */
       /*
        * TODO:482:o: |-> commit column radioTxPower.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioTxPower commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioTxPower
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOTXPOWER;
       }
    }

    if (save_flags & FLAG_RADIOTYPE) {
       save_flags &= ~FLAG_RADIOTYPE; /* clear radioType */
       /*
        * TODO:482:o: |-> commit column radioType.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioType commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioType
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOTYPE;
       }
    }

    if (save_flags & FLAG_RADIOSPEED) {
       save_flags &= ~FLAG_RADIOSPEED; /* clear radioSpeed */
       /*
        * TODO:482:o: |-> commit column radioSpeed.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioSpeed commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioSpeed
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOSPEED;
       }
    }

    if (save_flags & FLAG_RADIOBEACON) {
       save_flags &= ~FLAG_RADIOBEACON; /* clear radioBeacon */
       /*
        * TODO:482:o: |-> commit column radioBeacon.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioBeacon commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioBeacon
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOBEACON;
       }
    }

    if (save_flags & FLAG_RADIOFRAGTHRESHOLD) {
       save_flags &= ~FLAG_RADIOFRAGTHRESHOLD; /* clear radioFragThreshold */
       /*
        * TODO:482:o: |-> commit column radioFragThreshold.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioFragThreshold commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioFragThreshold
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOFRAGTHRESHOLD;
       }
    }

    if (save_flags & FLAG_RADIODTIM) {
       save_flags &= ~FLAG_RADIODTIM; /* clear radioDtim */
       /*
        * TODO:482:o: |-> commit column radioDtim.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioDtim commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioDtim
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIODTIM;
       }
    }

    if (save_flags & FLAG_RADIORTS) {
       save_flags &= ~FLAG_RADIORTS; /* clear radioRTS */
       /*
        * TODO:482:o: |-> commit column radioRTS.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioRTS commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioRTS
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIORTS;
       }
    }

    if (save_flags & FLAG_RADIOLEADCODE) {
       save_flags &= ~FLAG_RADIOLEADCODE; /* clear radioLeadCode */
       /*
        * TODO:482:o: |-> commit column radioLeadCode.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioLeadCode commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioLeadCode
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOLEADCODE;
       }
    }

    if (save_flags & FLAG_RADIOSHORTRETRY) {
       save_flags &= ~FLAG_RADIOSHORTRETRY; /* clear radioShortRetry */
       /*
        * TODO:482:o: |-> commit column radioShortRetry.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioShortRetry commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioShortRetry
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOSHORTRETRY;
       }
    }

    if (save_flags & FLAG_RADIOLONGRETRY) {
       save_flags &= ~FLAG_RADIOLONGRETRY; /* clear radioLongRetry */
       /*
        * TODO:482:o: |-> commit column radioLongRetry.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioLongRetry commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioLongRetry
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOLONGRETRY;
       }
    }

    if (save_flags & FLAG_RADIOSERVICE) {
       save_flags &= ~FLAG_RADIOSERVICE; /* clear radioService */
       /*
        * TODO:482:o: |-> commit column radioService.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioService commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioService
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOSERVICE;
       }
    }

    if (save_flags & FLAG_RADIOMAXSPEED) {
       save_flags &= ~FLAG_RADIOMAXSPEED; /* clear radioMaxSpeed */
       /*
        * TODO:482:o: |-> commit column radioMaxSpeed.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioMaxSpeed commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioMaxSpeed
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOMAXSPEED;
       }
    }

    if (save_flags & FLAG_RADIOMAXFLOW) {
       save_flags &= ~FLAG_RADIOMAXFLOW; /* clear radioMaxFlow */
       /*
        * TODO:482:o: |-> commit column radioMaxFlow.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioMaxFlow commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioMaxFlow
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOMAXFLOW;
       }
    }

    if (save_flags & FLAG_RADIOBINDWLAN) {
       save_flags &= ~FLAG_RADIOBINDWLAN; /* clear radioBindWlan */
       /*
        * TODO:482:o: |-> commit column radioBindWlan.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioBindWlan commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioBindWlan
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOBINDWLAN;
       }
    }

    if (save_flags & FLAG_RADIODELBINDWLAN) {
       save_flags &= ~FLAG_RADIODELBINDWLAN; /* clear radioDelbindWlan */
       /*
        * TODO:482:o: |-> commit column radioDelbindWlan.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioDelbindWlan commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioDelbindWlan
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIODELBINDWLAN;
       }
    }

    if (save_flags & FLAG_RADIOBINDQOS) {
       save_flags &= ~FLAG_RADIOBINDQOS; /* clear radioBindQos */
       /*
        * TODO:482:o: |-> commit column radioBindQos.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioBindQos commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioBindQos
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOBINDQOS;
       }
    }

    if (save_flags & FLAG_RADIODELBINDQOS) {
       save_flags &= ~FLAG_RADIODELBINDQOS; /* clear radioDelbindQos */
       /*
        * TODO:482:o: |-> commit column radioDelbindQos.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioDelbindQos commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioDelbindQos
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIODELBINDQOS;
       }
    }

    if (save_flags & FLAG_RADIOAMPDUSWITCH) {
       save_flags &= ~FLAG_RADIOAMPDUSWITCH; /* clear radioAmpduSwitch */
       /*
        * TODO:482:o: |-> commit column radioAmpduSwitch.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioAmpduSwitch commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioAmpduSwitch
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOAMPDUSWITCH;
       }
    }

    if (save_flags & FLAG_RADIOCWMODE) {
       save_flags &= ~FLAG_RADIOCWMODE; /* clear radioCwmode */
       /*
        * TODO:482:o: |-> commit column radioCwmode.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioCwmode commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioCwmode
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOCWMODE;
       }
    }

    if (save_flags & FLAG_RADIOSHORTGISWITCH) {
       save_flags &= ~FLAG_RADIOSHORTGISWITCH; /* clear radioShortGISwitch */
       /*
        * TODO:482:o: |-> commit column radioShortGISwitch.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioShortGISwitch commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioShortGISwitch
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOSHORTGISWITCH;
       }
    }

    if (save_flags & FLAG_RADIOAMSDUSWITCH) {
       save_flags &= ~FLAG_RADIOAMSDUSWITCH; /* clear radioAmsduSwitch */
       /*
        * TODO:482:o: |-> commit column radioAmsduSwitch.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioAmsduSwitch commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioAmsduSwitch
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOAMSDUSWITCH;
       }
    }

    if (save_flags & FLAG_RADIOMCS) {
       save_flags &= ~FLAG_RADIOMCS; /* clear radioMCS */
       /*
        * TODO:482:o: |-> commit column radioMCS.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radioMCS commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radioMCS
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIOMCS;
       }
    }

    if (save_flags & FLAG_RADIO11NSWITCH) {
       save_flags &= ~FLAG_RADIO11NSWITCH; /* clear radio11nSwitch */
       /*
        * TODO:482:o: |-> commit column radio11nSwitch.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column radio11nSwitch commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo radio11nSwitch
             */
            rowreq_ctx->column_set_flags |= FLAG_RADIO11NSWITCH;
       }
    }

    if (save_flags & FLAG_ONLYALLOW11NSTAACCESS) {
       save_flags &= ~FLAG_ONLYALLOW11NSTAACCESS; /* clear OnlyAllow11nStaAccess */
       /*
        * TODO:482:o: |-> commit column OnlyAllow11nStaAccess.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadioTable column OnlyAllow11nStaAccess commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo OnlyAllow11nStaAccess
             */
            rowreq_ctx->column_set_flags |= FLAG_ONLYALLOW11NSTAACCESS;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigRadioTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigRadioTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigRadioTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigRadioTable_undo_commit( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:dot11ConfigRadioTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigRadioTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11ConfigRadioTable_undo_commit */

/*
 * TODO:420:r: Implement dot11ConfigRadioTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfIndex
 * wtpWirelessIfIndex is subid 1 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.1
 * Description:
Wireless interface index.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/*
 * TODO:440:M: Implement dot11ConfigRadioTable node value checks.
 * TODO:450:M: Implement dot11ConfigRadioTable undo functions.
 * TODO:460:M: Implement dot11ConfigRadioTable set functions.
 * TODO:480:M: Implement dot11ConfigRadioTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioChannel
 * radioChannel is subid 1 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.1
 * Description:
Radio channel,the range is form 149 to 153 when radio type is 11a,the other type is range from 1 to 11.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioChannel_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioChannel_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioChannel_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioChannel_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioChannel value.
     */

    return MFD_SUCCESS; /* radioChannel value not illegal */
} /* radioChannel_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioChannel_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioChannel_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioChannel undo.
     */
    /*
     * copy radioChannel data
     * set rowreq_ctx->undo->radioChannel from rowreq_ctx->data.radioChannel
     */
    rowreq_ctx->undo->radioChannel = rowreq_ctx->data.radioChannel;


    return MFD_SUCCESS;
} /* radioChannel_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioChannel_val
 *        A long containing the new value.
 */
int
radioChannel_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioChannel_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioChannel_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioChannel reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioChannel value.
     * set radioChannel value in rowreq_ctx->data
     */    
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char channel_value[10] = { 0 };
	memset(channel_value, 0, 10);

	if(radioChannel_val != 0)
	{
		snprintf(channel_value,sizeof(channel_value)-1,"%d",radioChannel_val);
	}
	else
	{
		strncpy(channel_value,"auto",sizeof(channel_value)-1);
	}

	ret = config_radio_channel(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,channel_value);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioChannel = radioChannel_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* radioChannel_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioChannel_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioChannel_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioChannel undo.
     */
    /*
     * copy radioChannel data
     * set rowreq_ctx->data.radioChannel from rowreq_ctx->undo->radioChannel
     */
    rowreq_ctx->data.radioChannel = rowreq_ctx->undo->radioChannel;

    
    return MFD_SUCCESS;
} /* radioChannel_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioTxPower
 * radioTxPower is subid 2 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.2
 * Description:
Transmission power(UNITS DB).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 27;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioTxPower_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 27
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioTxPower_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioTxPower_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioTxPower_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioTxPower value.
     */

    return MFD_SUCCESS; /* radioTxPower value not illegal */
} /* radioTxPower_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioTxPower_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioTxPower_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioTxPower undo.
     */
    /*
     * copy radioTxPower data
     * set rowreq_ctx->undo->radioTxPower from rowreq_ctx->data.radioTxPower
     */
    rowreq_ctx->undo->radioTxPower = rowreq_ctx->data.radioTxPower;


    return MFD_SUCCESS;
} /* radioTxPower_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioTxPower_val
 *        A long containing the new value.
 */
int
radioTxPower_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioTxPower_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioTxPower_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioTxPower reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioTxPower value.
     * set radioTxPower value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_txpower(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioTxPower_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioTxPower = radioTxPower_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}    

    return MFD_SUCCESS;
} /* radioTxPower_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioTxPower_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioTxPower_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioTxPower undo.
     */
    /*
     * copy radioTxPower data
     * set rowreq_ctx->data.radioTxPower from rowreq_ctx->undo->radioTxPower
     */
    rowreq_ctx->data.radioTxPower = rowreq_ctx->undo->radioTxPower;

    
    return MFD_SUCCESS;
} /* radioTxPower_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioType
 * radioType is subid 3 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.3
 * Description:
Radio type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 4/8. Values:  a(1), b(2), g(4), bg(6), bgn(18)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioType_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  a(1), b(2), g(4), bg(6), bgn(18)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioType_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioType_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioType_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioType value.
     */

    return MFD_SUCCESS; /* radioType value not illegal */
} /* radioType_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioType_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioType_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioType undo.
     */
    /*
     * copy radioType data
     * set rowreq_ctx->undo->radioType from rowreq_ctx->data.radioType
     */
    rowreq_ctx->undo->radioType = rowreq_ctx->data.radioType;


    return MFD_SUCCESS;
} /* radioType_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioType_val
 *        A long containing the new value.
 */
int
radioType_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioType_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioType_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    snmp_log(LOG_DEBUG, "enter radioType_set\n");
	char radioMode[10] = { 0 };
/*
 * TODO:245:o: |-> Implement radioType reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(radioType_val) {
        case RADIOTYPE_A:
             radioType_val = INTERNAL_RADIOTYPE_A;
             strncpy(radioMode, "11a", sizeof(radioMode) - 1);
             break;

        case RADIOTYPE_B:
             radioType_val = INTERNAL_RADIOTYPE_B;
             strncpy(radioMode, "11b", sizeof(radioMode) - 1);
             break;

        case RADIOTYPE_G:
             radioType_val = INTERNAL_RADIOTYPE_G;
             strncpy(radioMode, "11g", sizeof(radioMode) - 1);
             break;

        case RADIOTYPE_BG:
             strncpy(radioMode, "11b/g", sizeof(radioMode) - 1);
             radioType_val = INTERNAL_RADIOTYPE_BG;
             break;

        case RADIOTYPE_AN:
             strncpy(radioMode, "11an", sizeof(radioMode) - 1);
             radioType_val = INTERNAL_RADIOTYPE_AN;
             break;
             
        case RADIOTYPE_AAN:
            strncpy(radioMode, "11a/an", sizeof(radioMode) - 1);
            radioType_val = INTERNAL_RADIOTYPE_AAN;
            break;

        case RADIOTYPE_GN:
            strncpy(radioMode, "11gn", sizeof(radioMode) - 1);
            radioType_val = INTERNAL_RADIOTYPE_GN;
            break;

        case RADIOTYPE_GGN:
            strncpy(radioMode, "11g/gn", sizeof(radioMode) - 1);
            radioType_val = INTERNAL_RADIOTYPE_GGN;
            break;

        case RADIOTYPE_BGGN:
			strncpy(radioMode, "11b/g/n", sizeof(radioMode) - 1);
            radioType_val = INTERNAL_RADIOTYPE_BGGN;
            break;
                  
             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for radioType\n", radioType_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set radioType value.
     * set radioType value in rowreq_ctx->data
     */
	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
        
	int ret = 0;
	ret = config_radio_mode(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioMode);	
	if(1 == ret) {
		rowreq_ctx->data.radioType = radioType_val;
		return MFD_SUCCESS;
	}

	if(SNMPD_CONNECTION_ERROR == ret) {
		close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	}   

    return MFD_ERROR;
} /* radioType_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioType_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioType_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioType undo.
     */
    /*
     * copy radioType data
     * set rowreq_ctx->data.radioType from rowreq_ctx->undo->radioType
     */
    rowreq_ctx->data.radioType = rowreq_ctx->undo->radioType;

    
    return MFD_SUCCESS;
} /* radioType_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioSpeed
 * radioSpeed is subid 4 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.4
 * Description:
Speed.(1,2,5.5,6,9,11,12,18,24,36,48,54M/bps)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioSpeed_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioSpeed_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioSpeed_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioSpeed_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioSpeed value.
     */

    return MFD_SUCCESS; /* radioSpeed value not illegal */
} /* radioSpeed_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioSpeed_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioSpeed_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioSpeed undo.
     */
    /*
     * copy radioSpeed data
     * set rowreq_ctx->undo->radioSpeed from rowreq_ctx->data.radioSpeed
     */
    rowreq_ctx->undo->radioSpeed = rowreq_ctx->data.radioSpeed;


    return MFD_SUCCESS;
} /* radioSpeed_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioSpeed_val
 *        A long containing the new value.
 */
int
radioSpeed_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioSpeed_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioSpeed_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioSpeed reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioSpeed value.
     * set radioSpeed value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char radioRate[10] = { 0 };
	memset(radioRate, 0, 10);
	snprintf(radioRate,sizeof(radioRate)-1,"%d",radioSpeed_val*10);

	ret = config_radio_rate(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioRate);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioSpeed = radioSpeed_val*10;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}            

    return MFD_SUCCESS;
} /* radioSpeed_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioSpeed_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioSpeed_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioSpeed undo.
     */
    /*
     * copy radioSpeed data
     * set rowreq_ctx->data.radioSpeed from rowreq_ctx->undo->radioSpeed
     */
    rowreq_ctx->data.radioSpeed = rowreq_ctx->undo->radioSpeed;

    
    return MFD_SUCCESS;
} /* radioSpeed_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioBeacon
 * radioBeacon is subid 5 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.5
 * Description:
Beacon.(Units ms)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  25 - 1000;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioBeacon_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  25 - 1000
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioBeacon_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioBeacon_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBeacon_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioBeacon value.
     */

    return MFD_SUCCESS; /* radioBeacon value not illegal */
} /* radioBeacon_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioBeacon_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBeacon_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioBeacon undo.
     */
    /*
     * copy radioBeacon data
     * set rowreq_ctx->undo->radioBeacon from rowreq_ctx->data.radioBeacon
     */
    rowreq_ctx->undo->radioBeacon = rowreq_ctx->data.radioBeacon;


    return MFD_SUCCESS;
} /* radioBeacon_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioBeacon_val
 *        A long containing the new value.
 */
int
radioBeacon_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioBeacon_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBeacon_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioBeacon reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioBeacon value.
     * set radioBeacon value in rowreq_ctx->data
     */  
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_beaconinterval(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioBeacon_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioBeacon = radioBeacon_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}			     

    return MFD_SUCCESS;
} /* radioBeacon_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioBeacon_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBeacon_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioBeacon undo.
     */
    /*
     * copy radioBeacon data
     * set rowreq_ctx->data.radioBeacon from rowreq_ctx->undo->radioBeacon
     */
    rowreq_ctx->data.radioBeacon = rowreq_ctx->undo->radioBeacon;

    
    return MFD_SUCCESS;
} /* radioBeacon_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioFragThreshold
 * radioFragThreshold is subid 6 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.6
 * Description:
Beaf.(Units byte)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  256 - 2346;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioFragThreshold_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  256 - 2346
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioFragThreshold_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioFragThreshold_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioFragThreshold_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioFragThreshold value.
     */

    return MFD_SUCCESS; /* radioFragThreshold value not illegal */
} /* radioFragThreshold_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioFragThreshold_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioFragThreshold_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioFragThreshold undo.
     */
    /*
     * copy radioFragThreshold data
     * set rowreq_ctx->undo->radioFragThreshold from rowreq_ctx->data.radioFragThreshold
     */
    rowreq_ctx->undo->radioFragThreshold = rowreq_ctx->data.radioFragThreshold;


    return MFD_SUCCESS;
} /* radioFragThreshold_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioFragThreshold_val
 *        A long containing the new value.
 */
int
radioFragThreshold_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioFragThreshold_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioFragThreshold_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioFragThreshold reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioFragThreshold value.
     * set radioFragThreshold value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_fragmentation(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioFragThreshold_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioFragThreshold = radioFragThreshold_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}		    

    return MFD_SUCCESS;
} /* radioFragThreshold_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioFragThreshold_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioFragThreshold_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioFragThreshold undo.
     */
    /*
     * copy radioFragThreshold data
     * set rowreq_ctx->data.radioFragThreshold from rowreq_ctx->undo->radioFragThreshold
     */
    rowreq_ctx->data.radioFragThreshold = rowreq_ctx->undo->radioFragThreshold;

    
    return MFD_SUCCESS;
} /* radioFragThreshold_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioDtim
 * radioDtim is subid 7 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.7
 * Description:
Dtim.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioDtim_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 15
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioDtim_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioDtim_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDtim_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioDtim value.
     */

    return MFD_SUCCESS; /* radioDtim value not illegal */
} /* radioDtim_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioDtim_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDtim_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioDtim undo.
     */
    /*
     * copy radioDtim data
     * set rowreq_ctx->undo->radioDtim from rowreq_ctx->data.radioDtim
     */
    rowreq_ctx->undo->radioDtim = rowreq_ctx->data.radioDtim;


    return MFD_SUCCESS;
} /* radioDtim_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioDtim_val
 *        A long containing the new value.
 */
int
radioDtim_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioDtim_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDtim_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioDtim reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioDtim value.
     * set radioDtim value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_dtim(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioDtim_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioDtim = radioDtim_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}	    

    return MFD_SUCCESS;
} /* radioDtim_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioDtim_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDtim_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioDtim undo.
     */
    /*
     * copy radioDtim data
     * set rowreq_ctx->data.radioDtim from rowreq_ctx->undo->radioDtim
     */
    rowreq_ctx->data.radioDtim = rowreq_ctx->undo->radioDtim;

    
    return MFD_SUCCESS;
} /* radioDtim_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioRTS
 * radioRTS is subid 8 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.8
 * Description:
RTS.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  256 - 2346;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioRTS_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  256 - 2346
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioRTS_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioRTS_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioRTS_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioRTS value.
     */

    return MFD_SUCCESS; /* radioRTS value not illegal */
} /* radioRTS_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioRTS_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioRTS_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioRTS undo.
     */
    /*
     * copy radioRTS data
     * set rowreq_ctx->undo->radioRTS from rowreq_ctx->data.radioRTS
     */
    rowreq_ctx->undo->radioRTS = rowreq_ctx->data.radioRTS;


    return MFD_SUCCESS;
} /* radioRTS_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioRTS_val
 *        A long containing the new value.
 */
int
radioRTS_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioRTS_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioRTS_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioRTS reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioRTS value.
     * set radioRTS value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_rtsthreshold(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioRTS_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioRTS = radioRTS_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}	       

    return MFD_SUCCESS;
} /* radioRTS_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioRTS_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioRTS_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioRTS undo.
     */
    /*
     * copy radioRTS data
     * set rowreq_ctx->data.radioRTS from rowreq_ctx->undo->radioRTS
     */
    rowreq_ctx->data.radioRTS = rowreq_ctx->undo->radioRTS;

    
    return MFD_SUCCESS;
} /* radioRTS_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioLeadCode
 * radioLeadCode is subid 9 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.9
 * Description:
Lead code.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  short(1), long(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioLeadCode_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  short(1), long(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioLeadCode_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioLeadCode_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioLeadCode_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioLeadCode value.
     */

    return MFD_SUCCESS; /* radioLeadCode value not illegal */
} /* radioLeadCode_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioLeadCode_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioLeadCode_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioLeadCode undo.
     */
    /*
     * copy radioLeadCode data
     * set rowreq_ctx->undo->radioLeadCode from rowreq_ctx->data.radioLeadCode
     */
    rowreq_ctx->undo->radioLeadCode = rowreq_ctx->data.radioLeadCode;


    return MFD_SUCCESS;
} /* radioLeadCode_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioLeadCode_val
 *        A long containing the new value.
 */
int
radioLeadCode_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioLeadCode_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioLeadCode_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioLeadCode reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(radioLeadCode_val) {
        case RADIOLEADCODE_SHORT:
             radioLeadCode_val = INTERNAL_RADIOLEADCODE_SHORT;
             break;

        case RADIOLEADCODE_LONG:
             radioLeadCode_val = INTERNAL_RADIOLEADCODE_LONG;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for radioLeadCode\n", radioLeadCode_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set radioLeadCode value.
     * set radioLeadCode value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char type[10] = { 0 };
	memset(type, 0, 10);

	switch(radioLeadCode_val)
	{
		case INTERNAL_RADIOLEADCODE_SHORT:
		{
			strncpy(type,"short",sizeof(type)-1);
		}
		break;
		case INTERNAL_RADIOLEADCODE_LONG:
		{
			strncpy(type,"long",sizeof(type)-1);
		}
		break;
	}

	ret = config_radio_preamble(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,type);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioLeadCode = radioLeadCode_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}	         

    return MFD_SUCCESS;
} /* radioLeadCode_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioLeadCode_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioLeadCode_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioLeadCode undo.
     */
    /*
     * copy radioLeadCode data
     * set rowreq_ctx->data.radioLeadCode from rowreq_ctx->undo->radioLeadCode
     */
    rowreq_ctx->data.radioLeadCode = rowreq_ctx->undo->radioLeadCode;

    
    return MFD_SUCCESS;
} /* radioLeadCode_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioShortRetry
 * radioShortRetry is subid 10 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.10
 * Description:
Short retry.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioShortRetry_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 15
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioShortRetry_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioShortRetry_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioShortRetry_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioShortRetry value.
     */

    return MFD_SUCCESS; /* radioShortRetry value not illegal */
} /* radioShortRetry_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioShortRetry_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioShortRetry_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioShortRetry undo.
     */
    /*
     * copy radioShortRetry data
     * set rowreq_ctx->undo->radioShortRetry from rowreq_ctx->data.radioShortRetry
     */
    rowreq_ctx->undo->radioShortRetry = rowreq_ctx->data.radioShortRetry;


    return MFD_SUCCESS;
} /* radioShortRetry_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioShortRetry_val
 *        A long containing the new value.
 */
int
radioShortRetry_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioShortRetry_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioShortRetry_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioShortRetry reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioShortRetry value.
     * set radioShortRetry value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_shortretry(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioShortRetry_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioShortRetry = radioShortRetry_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}	         

    return MFD_SUCCESS;
} /* radioShortRetry_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioShortRetry_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioShortRetry_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioShortRetry undo.
     */
    /*
     * copy radioShortRetry data
     * set rowreq_ctx->data.radioShortRetry from rowreq_ctx->undo->radioShortRetry
     */
    rowreq_ctx->data.radioShortRetry = rowreq_ctx->undo->radioShortRetry;

    
    return MFD_SUCCESS;
} /* radioShortRetry_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioLongRetry
 * radioLongRetry is subid 11 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.11
 * Description:
Long retry.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioLongRetry_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 15
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioLongRetry_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioLongRetry_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioLongRetry_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioLongRetry value.
     */

    return MFD_SUCCESS; /* radioLongRetry value not illegal */
} /* radioLongRetry_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioLongRetry_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioLongRetry_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioLongRetry undo.
     */
    /*
     * copy radioLongRetry data
     * set rowreq_ctx->undo->radioLongRetry from rowreq_ctx->data.radioLongRetry
     */
    rowreq_ctx->undo->radioLongRetry = rowreq_ctx->data.radioLongRetry;


    return MFD_SUCCESS;
} /* radioLongRetry_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioLongRetry_val
 *        A long containing the new value.
 */
int
radioLongRetry_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioLongRetry_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioLongRetry_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioLongRetry reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioLongRetry value.
     * set radioLongRetry value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_longretry(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioLongRetry_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioLongRetry = radioLongRetry_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}	    

    return MFD_SUCCESS;
} /* radioLongRetry_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioLongRetry_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioLongRetry_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioLongRetry undo.
     */
    /*
     * copy radioLongRetry data
     * set rowreq_ctx->data.radioLongRetry from rowreq_ctx->undo->radioLongRetry
     */
    rowreq_ctx->data.radioLongRetry = rowreq_ctx->undo->radioLongRetry;

    
    return MFD_SUCCESS;
} /* radioLongRetry_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioService
 * radioService is subid 12 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.12
 * Description:
Radio service.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  disable(1), enable(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioService_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  disable(1), enable(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioService_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioService_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioService_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioService value.
     */

    return MFD_SUCCESS; /* radioService value not illegal */
} /* radioService_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioService_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioService_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioService undo.
     */
    /*
     * copy radioService data
     * set rowreq_ctx->undo->radioService from rowreq_ctx->data.radioService
     */
    rowreq_ctx->undo->radioService = rowreq_ctx->data.radioService;


    return MFD_SUCCESS;
} /* radioService_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioService_val
 *        A long containing the new value.
 */
int
radioService_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioService_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioService_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioService reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(radioService_val) {
        case RADIOSERVICE_DISABLE:
             radioService_val = INTERNAL_RADIOSERVICE_DISABLE;
             break;

        case RADIOSERVICE_ENABLE:
             radioService_val = INTERNAL_RADIOSERVICE_ENABLE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for radioService\n", radioService_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set radioService value.
     * set radioService value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char state[10] = { 0 };
	memset(state,0,10);
	switch(radioService_val)
	{
		case INTERNAL_RADIOSERVICE_DISABLE:
		{
			strncpy(state,"disable",sizeof(state)-1);
		}
		break;
		case INTERNAL_RADIOSERVICE_ENABLE:
		{
			strncpy(state,"enable",sizeof(state)-1);
		}
		break;
	}

	ret = config_radio_service(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,state);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioService = radioService_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}	        

    return MFD_SUCCESS;
} /* radioService_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioService_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioService_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioService undo.
     */
    /*
     * copy radioService data
     * set rowreq_ctx->data.radioService from rowreq_ctx->undo->radioService
     */
    rowreq_ctx->data.radioService = rowreq_ctx->undo->radioService;

    
    return MFD_SUCCESS;
} /* radioService_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioMaxSpeed
 * radioMaxSpeed is subid 13 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.13
 * Description:
Max speed.(1,2,5.5,6,9,11,12,18,24,36,48,54M/bps)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioMaxSpeed_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioMaxSpeed_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioMaxSpeed_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMaxSpeed_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioMaxSpeed value.
     */

    return MFD_SUCCESS; /* radioMaxSpeed value not illegal */
} /* radioMaxSpeed_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioMaxSpeed_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMaxSpeed_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioMaxSpeed undo.
     */
    /*
     * copy radioMaxSpeed data
     * set rowreq_ctx->undo->radioMaxSpeed from rowreq_ctx->data.radioMaxSpeed
     */
    rowreq_ctx->undo->radioMaxSpeed = rowreq_ctx->data.radioMaxSpeed;


    return MFD_SUCCESS;
} /* radioMaxSpeed_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioMaxSpeed_val
 *        A long containing the new value.
 */
int
radioMaxSpeed_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioMaxSpeed_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMaxSpeed_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioMaxSpeed reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioMaxSpeed value.
     * set radioMaxSpeed value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char rad_rate[10] = { 0 };
	memset(rad_rate, 0, 10);
	snprintf(rad_rate,sizeof(rad_rate)-1,"%d",radioMaxSpeed_val);
	struct RadioList *RadioList_Head = NULL;
	
	ret = config_max_rate(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,rad_rate,&RadioList_Head);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioMaxSpeed = radioMaxSpeed_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}	         

    return rc;
} /* radioMaxSpeed_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioMaxSpeed_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMaxSpeed_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioMaxSpeed undo.
     */
    /*
     * copy radioMaxSpeed data
     * set rowreq_ctx->data.radioMaxSpeed from rowreq_ctx->undo->radioMaxSpeed
     */
    rowreq_ctx->data.radioMaxSpeed = rowreq_ctx->undo->radioMaxSpeed;

    
    return MFD_SUCCESS;
} /* radioMaxSpeed_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioMaxFlow
 * radioMaxFlow is subid 14 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.14
 * Description:
Max flow.(Units M)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 108;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioMaxFlow_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 108
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioMaxFlow_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioMaxFlow_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMaxFlow_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioMaxFlow value.
     */

    return MFD_SUCCESS; /* radioMaxFlow value not illegal */
} /* radioMaxFlow_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioMaxFlow_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMaxFlow_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioMaxFlow undo.
     */
    /*
     * copy radioMaxFlow data
     * set rowreq_ctx->undo->radioMaxFlow from rowreq_ctx->data.radioMaxFlow
     */
    rowreq_ctx->undo->radioMaxFlow = rowreq_ctx->data.radioMaxFlow;


    return MFD_SUCCESS;
} /* radioMaxFlow_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioMaxFlow_val
 *        A long containing the new value.
 */
int
radioMaxFlow_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, long radioMaxFlow_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMaxFlow_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioMaxFlow reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioMaxFlow value.
     * set radioMaxFlow value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char value[10] = { 0 };
	memset(value, 0, 10);
	snprintf(value,sizeof(value)-1,"%d",radioMaxFlow_val);
	
	ret = set_radio_max_throughout_func(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,value);	
	if(ret == 1)
	{
		rowreq_ctx->data.radioMaxFlow = radioMaxFlow_val;
		rc = MFD_SUCCESS;
	}
	else
	{   
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}	      

    return MFD_SUCCESS;
} /* radioMaxFlow_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioMaxFlow_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMaxFlow_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioMaxFlow undo.
     */
    /*
     * copy radioMaxFlow data
     * set rowreq_ctx->data.radioMaxFlow from rowreq_ctx->undo->radioMaxFlow
     */
    rowreq_ctx->data.radioMaxFlow = rowreq_ctx->undo->radioMaxFlow;

    
    return MFD_SUCCESS;
} /* radioMaxFlow_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioBindWlan
 * radioBindWlan is subid 15 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.15
 * Description:
Bind wlan.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioBindWlan_val_ptr
 *        A char containing the new value.
 * @param radioBindWlan_val_ptr_len
 *        The size (in bytes) of the data pointed to by radioBindWlan_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioBindWlan_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, char *radioBindWlan_val_ptr,  size_t radioBindWlan_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBindWlan_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != radioBindWlan_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid radioBindWlan value.
     */

    return MFD_SUCCESS; /* radioBindWlan value not illegal */
} /* radioBindWlan_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioBindWlan_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBindWlan_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioBindWlan undo.
     */
    /*
     * copy radioBindWlan and radioBindWlan_len data
     * set rowreq_ctx->undo->radioBindWlan from rowreq_ctx->data.radioBindWlan
     */
    memcpy( rowreq_ctx->undo->radioBindWlan, rowreq_ctx->data.radioBindWlan,
            (rowreq_ctx->data.radioBindWlan_len * sizeof(rowreq_ctx->undo->radioBindWlan[0])));
    rowreq_ctx->undo->radioBindWlan_len = rowreq_ctx->data.radioBindWlan_len;


    return MFD_SUCCESS;
} /* radioBindWlan_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioBindWlan_val_ptr
 *        A char containing the new value.
 * @param radioBindWlan_val_ptr_len
 *        The size (in bytes) of the data pointed to by radioBindWlan_val_ptr
 */
int
radioBindWlan_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, char *radioBindWlan_val_ptr,  size_t radioBindWlan_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBindWlan_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != radioBindWlan_val_ptr);

/*
 * TODO:245:o: |-> Implement radioBindWlan reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioBindWlan value.
     * set radioBindWlan value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	
	ret = radio_apply_wlan(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioBindWlan_val_ptr);	
	if(ret == 1)
	{
		memcpy( rowreq_ctx->data.radioBindWlan, radioBindWlan_val_ptr, radioBindWlan_val_ptr_len );
	    rowreq_ctx->data.radioBindWlan_len = radioBindWlan_val_ptr_len / sizeof(radioBindWlan_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}	   

    return MFD_SUCCESS;
} /* radioBindWlan_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioBindWlan_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBindWlan_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioBindWlan undo.
     */
    /*
     * copy radioBindWlan and radioBindWlan_len data
     * set rowreq_ctx->data.radioBindWlan from rowreq_ctx->undo->radioBindWlan
     */
    memcpy( rowreq_ctx->data.radioBindWlan, rowreq_ctx->undo->radioBindWlan,
            (rowreq_ctx->undo->radioBindWlan_len * sizeof(rowreq_ctx->data.radioBindWlan[0])));
    rowreq_ctx->data.radioBindWlan_len = rowreq_ctx->undo->radioBindWlan_len;

    
    return MFD_SUCCESS;
} /* radioBindWlan_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioDelbindWlan
 * radioDelbindWlan is subid 16 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.16
 * Description:
Delete Binded wlan.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioDelbindWlan_val_ptr
 *        A char containing the new value.
 * @param radioDelbindWlan_val_ptr_len
 *        The size (in bytes) of the data pointed to by radioDelbindWlan_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioDelbindWlan_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, char *radioDelbindWlan_val_ptr,  size_t radioDelbindWlan_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDelbindWlan_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != radioDelbindWlan_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid radioDelbindWlan value.
     */

    return MFD_SUCCESS; /* radioDelbindWlan value not illegal */
} /* radioDelbindWlan_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioDelbindWlan_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDelbindWlan_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioDelbindWlan undo.
     */
    /*
     * copy radioDelbindWlan and radioDelbindWlan_len data
     * set rowreq_ctx->undo->radioDelbindWlan from rowreq_ctx->data.radioDelbindWlan
     */
    memcpy( rowreq_ctx->undo->radioDelbindWlan, rowreq_ctx->data.radioDelbindWlan,
            (rowreq_ctx->data.radioDelbindWlan_len * sizeof(rowreq_ctx->undo->radioDelbindWlan[0])));
    rowreq_ctx->undo->radioDelbindWlan_len = rowreq_ctx->data.radioDelbindWlan_len;


    return MFD_SUCCESS;
} /* radioDelbindWlan_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioDelbindWlan_val_ptr
 *        A char containing the new value.
 * @param radioDelbindWlan_val_ptr_len
 *        The size (in bytes) of the data pointed to by radioDelbindWlan_val_ptr
 */
int
radioDelbindWlan_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, char *radioDelbindWlan_val_ptr,  size_t radioDelbindWlan_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDelbindWlan_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != radioDelbindWlan_val_ptr);

/*
 * TODO:245:o: |-> Implement radioDelbindWlan reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioDelbindWlan value.
     * set radioDelbindWlan value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	
	ret = set_radio_delete_wlan_cmd(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioDelbindWlan_val_ptr);	
	if(ret == 1)
	{
		memcpy( rowreq_ctx->data.radioDelbindWlan, radioDelbindWlan_val_ptr, radioDelbindWlan_val_ptr_len );
	    rowreq_ctx->data.radioDelbindWlan_len = radioDelbindWlan_val_ptr_len / sizeof(radioDelbindWlan_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}	

    return MFD_SUCCESS;
} /* radioDelbindWlan_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioDelbindWlan_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDelbindWlan_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioDelbindWlan undo.
     */
    /*
     * copy radioDelbindWlan and radioDelbindWlan_len data
     * set rowreq_ctx->data.radioDelbindWlan from rowreq_ctx->undo->radioDelbindWlan
     */
    memcpy( rowreq_ctx->data.radioDelbindWlan, rowreq_ctx->undo->radioDelbindWlan,
            (rowreq_ctx->undo->radioDelbindWlan_len * sizeof(rowreq_ctx->data.radioDelbindWlan[0])));
    rowreq_ctx->data.radioDelbindWlan_len = rowreq_ctx->undo->radioDelbindWlan_len;

    
    return MFD_SUCCESS;
} /* radioDelbindWlan_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioBindQos
 * radioBindQos is subid 17 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.17
 * Description:
Bind Qos.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioBindQos_val_ptr
 *        A char containing the new value.
 * @param radioBindQos_val_ptr_len
 *        The size (in bytes) of the data pointed to by radioBindQos_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioBindQos_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, char *radioBindQos_val_ptr,  size_t radioBindQos_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBindQos_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != radioBindQos_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid radioBindQos value.
     */

    return MFD_SUCCESS; /* radioBindQos value not illegal */
} /* radioBindQos_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioBindQos_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBindQos_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioBindQos undo.
     */
    /*
     * copy radioBindQos and radioBindQos_len data
     * set rowreq_ctx->undo->radioBindQos from rowreq_ctx->data.radioBindQos
     */
    memcpy( rowreq_ctx->undo->radioBindQos, rowreq_ctx->data.radioBindQos,
            (rowreq_ctx->data.radioBindQos_len * sizeof(rowreq_ctx->undo->radioBindQos[0])));
    rowreq_ctx->undo->radioBindQos_len = rowreq_ctx->data.radioBindQos_len;


    return MFD_SUCCESS;
} /* radioBindQos_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioBindQos_val_ptr
 *        A char containing the new value.
 * @param radioBindQos_val_ptr_len
 *        The size (in bytes) of the data pointed to by radioBindQos_val_ptr
 */
int
radioBindQos_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, char *radioBindQos_val_ptr,  size_t radioBindQos_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBindQos_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != radioBindQos_val_ptr);

/*
 * TODO:245:o: |-> Implement radioBindQos reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioBindQos value.
     * set radioBindQos value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	
	ret = radio_apply_qos(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioBindQos_val_ptr);	
	if(ret == 1)
	{
		memcpy( rowreq_ctx->data.radioBindQos, radioBindQos_val_ptr, radioBindQos_val_ptr_len );
	    rowreq_ctx->data.radioBindQos_len = radioBindQos_val_ptr_len / sizeof(radioBindQos_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}	

    return MFD_SUCCESS;
} /* radioBindQos_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioBindQos_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioBindQos_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioBindQos undo.
     */
    /*
     * copy radioBindQos and radioBindQos_len data
     * set rowreq_ctx->data.radioBindQos from rowreq_ctx->undo->radioBindQos
     */
    memcpy( rowreq_ctx->data.radioBindQos, rowreq_ctx->undo->radioBindQos,
            (rowreq_ctx->undo->radioBindQos_len * sizeof(rowreq_ctx->data.radioBindQos[0])));
    rowreq_ctx->data.radioBindQos_len = rowreq_ctx->undo->radioBindQos_len;

    
    return MFD_SUCCESS;
} /* radioBindQos_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioDelbindQos
 * radioDelbindQos is subid 18 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.18
 * Description:
Delete Binded Qos.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioDelbindQos_val_ptr
 *        A char containing the new value.
 * @param radioDelbindQos_val_ptr_len
 *        The size (in bytes) of the data pointed to by radioDelbindQos_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioDelbindQos_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, char *radioDelbindQos_val_ptr,  size_t radioDelbindQos_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDelbindQos_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != radioDelbindQos_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid radioDelbindQos value.
     */

    return MFD_SUCCESS; /* radioDelbindQos value not illegal */
} /* radioDelbindQos_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioDelbindQos_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDelbindQos_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioDelbindQos undo.
     */
    /*
     * copy radioDelbindQos and radioDelbindQos_len data
     * set rowreq_ctx->undo->radioDelbindQos from rowreq_ctx->data.radioDelbindQos
     */
    memcpy( rowreq_ctx->undo->radioDelbindQos, rowreq_ctx->data.radioDelbindQos,
            (rowreq_ctx->data.radioDelbindQos_len * sizeof(rowreq_ctx->undo->radioDelbindQos[0])));
    rowreq_ctx->undo->radioDelbindQos_len = rowreq_ctx->data.radioDelbindQos_len;


    return MFD_SUCCESS;
} /* radioDelbindQos_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioDelbindQos_val_ptr
 *        A char containing the new value.
 * @param radioDelbindQos_val_ptr_len
 *        The size (in bytes) of the data pointed to by radioDelbindQos_val_ptr
 */
int
radioDelbindQos_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, char *radioDelbindQos_val_ptr,  size_t radioDelbindQos_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDelbindQos_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != radioDelbindQos_val_ptr);

/*
 * TODO:245:o: |-> Implement radioDelbindQos reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioDelbindQos value.
     * set radioDelbindQos value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	
	ret = radio_delete_qos(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioDelbindQos_val_ptr);	
	if(ret == 1)
	{
		memcpy( rowreq_ctx->data.radioDelbindQos, radioDelbindQos_val_ptr, radioDelbindQos_val_ptr_len );
	    rowreq_ctx->data.radioDelbindQos_len = radioDelbindQos_val_ptr_len / sizeof(radioDelbindQos_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}	

    return MFD_SUCCESS;
} /* radioDelbindQos_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioDelbindQos_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioDelbindQos_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioDelbindQos undo.
     */
    /*
     * copy radioDelbindQos and radioDelbindQos_len data
     * set rowreq_ctx->data.radioDelbindQos from rowreq_ctx->undo->radioDelbindQos
     */
    memcpy( rowreq_ctx->data.radioDelbindQos, rowreq_ctx->undo->radioDelbindQos,
            (rowreq_ctx->undo->radioDelbindQos_len * sizeof(rowreq_ctx->data.radioDelbindQos[0])));
    rowreq_ctx->data.radioDelbindQos_len = rowreq_ctx->undo->radioDelbindQos_len;

    
    return MFD_SUCCESS;
} /* radioDelbindQos_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioAmpduSwitch
 * radioAmpduSwitch is subid 19 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.4.3.1.19
 * Description:
A-MPDU Switch.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  off(0), on(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioAmpduSwitch_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  off(0), on(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioAmpduSwitch_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioAmpduSwitch_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioAmpduSwitch_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioAmpduSwitch value.
     */

    return MFD_SUCCESS; /* radioAmpduSwitch value not illegal */
} /* radioAmpduSwitch_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioAmpduSwitch_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioAmpduSwitch_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioAmpduSwitch undo.
     */
    /*
     * copy radioAmpduSwitch data
     * set rowreq_ctx->undo->radioAmpduSwitch from rowreq_ctx->data.radioAmpduSwitch
     */
    rowreq_ctx->undo->radioAmpduSwitch = rowreq_ctx->data.radioAmpduSwitch;


    return MFD_SUCCESS;
} /* radioAmpduSwitch_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioAmpduSwitch_val
 *        A long containing the new value.
 */
int
radioAmpduSwitch_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioAmpduSwitch_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioAmpduSwitch_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioAmpduSwitch reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(radioAmpduSwitch_val) {
        case RADIOAMPDUSWITCH_OFF:
             radioAmpduSwitch_val = INTERNAL_RADIOAMPDUSWITCH_OFF;
             break;

        case RADIOAMPDUSWITCH_ON:
             radioAmpduSwitch_val = INTERNAL_RADIOAMPDUSWITCH_ON;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for radioAmpduSwitch\n", radioAmpduSwitch_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set radioAmpduSwitch value.
     * set radioAmpduSwitch value in rowreq_ctx->data
     */	
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char state[10] = { 0 };

	memset(state,0,10);
	if(radioAmpduSwitch_val == RADIOAMPDUSWITCH_OFF)
	{
		strncpy(state,"disable",sizeof(state)-1);	
	}
	else if(radioAmpduSwitch_val == RADIOAMPDUSWITCH_ON)
	{
		strncpy(state,"enable",sizeof(state)-1);	
	}
	
	ret = set_radio_11n_ampdu_able_cmd(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,"ampdu",state);	
	if(ret == 1)
	{		
		rowreq_ctx->data.radioAmpduSwitch = radioAmpduSwitch_val;	
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* radioAmpduSwitch_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioAmpduSwitch_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioAmpduSwitch_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioAmpduSwitch undo.
     */
    /*
     * copy radioAmpduSwitch data
     * set rowreq_ctx->data.radioAmpduSwitch from rowreq_ctx->undo->radioAmpduSwitch
     */
    rowreq_ctx->data.radioAmpduSwitch = rowreq_ctx->undo->radioAmpduSwitch;

    
    return MFD_SUCCESS;
} /* radioAmpduSwitch_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioCwmode
 * radioCwmode is subid 20 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.4.3.1.20
 * Description:
Cwmode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  ht20(0), ht20_ht40(1), ht40(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioCwmode_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  ht20(0), ht20_ht40(1), ht40(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioCwmode_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioCwmode_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioCwmode_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioCwmode value.
     */

    return MFD_SUCCESS; /* radioCwmode value not illegal */
} /* radioCwmode_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioCwmode_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioCwmode_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioCwmode undo.
     */
    /*
     * copy radioCwmode data
     * set rowreq_ctx->undo->radioCwmode from rowreq_ctx->data.radioCwmode
     */
    rowreq_ctx->undo->radioCwmode = rowreq_ctx->data.radioCwmode;


    return MFD_SUCCESS;
} /* radioCwmode_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioCwmode_val
 *        A long containing the new value.
 */
int
radioCwmode_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioCwmode_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioCwmode_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioCwmode reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(radioCwmode_val) {
        case RADIOCWMODE_HT20:
             radioCwmode_val = INTERNAL_RADIOCWMODE_HT20;
             break;

        case RADIOCWMODE_HT20_HT40:
             radioCwmode_val = INTERNAL_RADIOCWMODE_HT20_HT40;
             break;

        case RADIOCWMODE_HT40:
             radioCwmode_val = INTERNAL_RADIOCWMODE_HT40;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for radioCwmode\n", radioCwmode_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set radioCwmode value.
     * set radioCwmode value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char cwmode_value[10] = { 0 };

	memset(cwmode_value,0,10);
	if(radioCwmode_val == RADIOCWMODE_HT20)
	{
		strncpy(cwmode_value,"ht20",sizeof(cwmode_value)-1);	
	}
	else if(radioCwmode_val == RADIOCWMODE_HT20_HT40)
	{
		strncpy(cwmode_value,"ht20/40",sizeof(cwmode_value)-1);	
	}
	else if(radioCwmode_val == RADIOCWMODE_HT40)
	{
		strncpy(cwmode_value,"ht40",sizeof(cwmode_value)-1);	
	}
	
	ret = set_radio_cmmode_cmd(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,cwmode_value);	
	if(ret == 1)
	{		
		rowreq_ctx->data.radioCwmode = radioCwmode_val;
		rc = MFD_SUCCESS;
	}
	else
	{		
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* radioCwmode_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioCwmode_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioCwmode_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioCwmode undo.
     */
    /*
     * copy radioCwmode data
     * set rowreq_ctx->data.radioCwmode from rowreq_ctx->undo->radioCwmode
     */
    rowreq_ctx->data.radioCwmode = rowreq_ctx->undo->radioCwmode;

    
    return MFD_SUCCESS;
} /* radioCwmode_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioShortGISwitch
 * radioShortGISwitch is subid 21 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.4.3.1.21
 * Description:
Short GI Switch.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  off(0), on(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioShortGISwitch_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  off(0), on(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioShortGISwitch_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioShortGISwitch_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioShortGISwitch_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioShortGISwitch value.
     */

    return MFD_SUCCESS; /* radioShortGISwitch value not illegal */
} /* radioShortGISwitch_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioShortGISwitch_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioShortGISwitch_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioShortGISwitch undo.
     */
    /*
     * copy radioShortGISwitch data
     * set rowreq_ctx->undo->radioShortGISwitch from rowreq_ctx->data.radioShortGISwitch
     */
    rowreq_ctx->undo->radioShortGISwitch = rowreq_ctx->data.radioShortGISwitch;


    return MFD_SUCCESS;
} /* radioShortGISwitch_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioShortGISwitch_val
 *        A long containing the new value.
 */
int
radioShortGISwitch_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioShortGISwitch_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioShortGISwitch_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioShortGISwitch reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(radioShortGISwitch_val) {
        case RADIOSHORTGISWITCH_OFF:
             radioShortGISwitch_val = INTERNAL_RADIOSHORTGISWITCH_OFF;
             break;

        case RADIOSHORTGISWITCH_ON:
             radioShortGISwitch_val = INTERNAL_RADIOSHORTGISWITCH_ON;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for radioShortGISwitch\n", radioShortGISwitch_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set radioShortGISwitch value.
     * set radioShortGISwitch value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char Interval[10] = { 0 };

	memset(Interval,0,10);
	if(radioShortGISwitch_val == RADIOSHORTGISWITCH_OFF)
	{
		strncpy(Interval,"800",sizeof(Interval)-1);	
	}
	else if(radioShortGISwitch_val == RADIOSHORTGISWITCH_ON)
	{
		strncpy(Interval,"400",sizeof(Interval)-1);	
	}
	
	ret = set_radio_guard_interval_cmd(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,Interval);	
	if(ret == 1)
	{				
		rowreq_ctx->data.radioShortGISwitch = radioShortGISwitch_val;		
		rc = MFD_SUCCESS;
	}
	else
	{		
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* radioShortGISwitch_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioShortGISwitch_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioShortGISwitch_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioShortGISwitch undo.
     */
    /*
     * copy radioShortGISwitch data
     * set rowreq_ctx->data.radioShortGISwitch from rowreq_ctx->undo->radioShortGISwitch
     */
    rowreq_ctx->data.radioShortGISwitch = rowreq_ctx->undo->radioShortGISwitch;

    
    return MFD_SUCCESS;
} /* radioShortGISwitch_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioAmsduSwitch
 * radioAmsduSwitch is subid 22 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.22
 * Description:
A-MSDU Switch.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  off(0), on(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radioAmsduSwitch_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  off(0), on(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioAmsduSwitch_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioAmsduSwitch_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioAmsduSwitch_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radioAmsduSwitch value.
     */

    return MFD_SUCCESS; /* radioAmsduSwitch value not illegal */
} /* radioAmsduSwitch_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioAmsduSwitch_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioAmsduSwitch_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioAmsduSwitch undo.
     */
    /*
     * copy radioAmsduSwitch data
     * set rowreq_ctx->undo->radioAmsduSwitch from rowreq_ctx->data.radioAmsduSwitch
     */
    rowreq_ctx->undo->radioAmsduSwitch = rowreq_ctx->data.radioAmsduSwitch;


    return MFD_SUCCESS;
} /* radioAmsduSwitch_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioAmsduSwitch_val
 *        A long containing the new value.
 */
int
radioAmsduSwitch_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radioAmsduSwitch_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioAmsduSwitch_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radioAmsduSwitch reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(radioAmsduSwitch_val) {
        case RADIOAMSDUSWITCH_OFF:
             radioAmsduSwitch_val = INTERNAL_RADIOAMSDUSWITCH_OFF;
             break;

        case RADIOAMSDUSWITCH_ON:
             radioAmsduSwitch_val = INTERNAL_RADIOAMSDUSWITCH_ON;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for radioAmsduSwitch\n", radioAmsduSwitch_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set radioAmsduSwitch value.
     * set radioAmsduSwitch value in rowreq_ctx->data
     */ 
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int rc = MFD_ERROR;
	int ret = 0;
	char state[10] = { 0 };

	memset(state,0,10);
	if(radioAmsduSwitch_val == INTERNAL_RADIOAMSDUSWITCH_OFF)
	{
		strncpy(state,"disable",sizeof(state)-1);	
	}
	else if(radioAmsduSwitch_val == INTERNAL_RADIOAMSDUSWITCH_ON)
	{
		strncpy(state,"enable",sizeof(state)-1);	
	}
	
	ret = set_radio_11n_ampdu_able_cmd(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,"amsdu",state);
	if(ret == 1)
	{				
		rowreq_ctx->data.radioAmsduSwitch = radioAmsduSwitch_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* radioAmsduSwitch_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioAmsduSwitch_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioAmsduSwitch_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioAmsduSwitch undo.
     */
    /*
     * copy radioAmsduSwitch data
     * set rowreq_ctx->data.radioAmsduSwitch from rowreq_ctx->undo->radioAmsduSwitch
     */
    rowreq_ctx->data.radioAmsduSwitch = rowreq_ctx->undo->radioAmsduSwitch;

    
    return MFD_SUCCESS;
} /* radioAmsduSwitch_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radioMCS
 * radioMCS is subid 23 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.23
 * Description:
Modulation and Coding Scheme.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *		  Pointer to the row request context.
 * @param radioMCS_val_ptr
 *		  A char containing the new value.
 * @param radioMCS_val_ptr_len
 *		  The size (in bytes) of the data pointed to by radioMCS_val_ptr
 *
 * @retval MFD_SUCCESS		  : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *		
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *	  The syntax is ASN_OCTET_STR
 *	  The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radioMCS_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, char *radioMCS_val_ptr, size_t radioMCS_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMCS_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != radioMCS_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid radioMCS value.
     */

    return MFD_SUCCESS; /* radioMCS value not illegal */
} /* radioMCS_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radioMCS_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMCS_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radioMCS undo.
     */
    /*
     * copy radioMCS data
     * set rowreq_ctx->undo->radioMCS from rowreq_ctx->data.radioMCS
     */
	memcpy( rowreq_ctx->undo->radioMCS, rowreq_ctx->data.radioMCS,
            (rowreq_ctx->data.radioMCS_len * sizeof(rowreq_ctx->undo->radioMCS[0])));
    rowreq_ctx->undo->radioMCS_len = rowreq_ctx->data.radioMCS_len;


    return MFD_SUCCESS;
} /* radioMCS_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radioMCS_val_ptr
 *        A char containing the new value.
 * @param radioMCS_val_ptr_len
 *        The size (in bytes) of the data pointed to by radioMCS_val_ptr
 */
int
radioMCS_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, char *radioMCS_val_ptr,  size_t radioMCS_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMCS_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != radioMCS_val_ptr);

/*
 * TODO:245:o: |-> Implement radioMCS reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set radioMCS value.
     * set radioMCS value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	
	ret = set_radio_mcs_cmd(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.setwtpRadCurrID, radioMCS_val_ptr);
	if(ret == 1)
	{				
		memcpy( rowreq_ctx->data.radioMCS, radioMCS_val_ptr, radioMCS_val_ptr_len );
	    rowreq_ctx->data.radioMCS_len = radioMCS_val_ptr_len / sizeof(radioMCS_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{		
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}

    return rc;
} /* radioMCS_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radioMCS_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radioMCS_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radioMCS undo.
     */
    /*
     * copy radioMCS data
     * set rowreq_ctx->data.radioMCS from rowreq_ctx->undo->radioMCS
     */
	memcpy( rowreq_ctx->data.radioMCS, rowreq_ctx->undo->radioMCS,
			(rowreq_ctx->undo->radioMCS_len * sizeof(rowreq_ctx->data.radioMCS[0])));
	rowreq_ctx->data.radioMCS_len = rowreq_ctx->undo->radioMCS_len;

    
    return MFD_SUCCESS;
} /* radioMCS_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.radio11nSwitch
 * radio11nSwitch is subid 24 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.24
 * Description:
802.11n Switch.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  off(0), on(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param radio11nSwitch_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  off(0), on(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
radio11nSwitch_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radio11nSwitch_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radio11nSwitch_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid radio11nSwitch value.
     */

    return MFD_SUCCESS; /* radio11nSwitch value not illegal */
} /* radio11nSwitch_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
radio11nSwitch_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radio11nSwitch_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup radio11nSwitch undo.
     */
    /*
     * copy radio11nSwitch data
     * set rowreq_ctx->undo->radio11nSwitch from rowreq_ctx->data.radio11nSwitch
     */
    rowreq_ctx->undo->radio11nSwitch = rowreq_ctx->data.radio11nSwitch;


    return MFD_SUCCESS;
} /* radio11nSwitch_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param radio11nSwitch_val
 *        A long containing the new value.
 */
int
radio11nSwitch_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long radio11nSwitch_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radio11nSwitch_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement radio11nSwitch reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(radio11nSwitch_val) {
        case RADIO11NSWITCH_OFF:
             radio11nSwitch_val = INTERNAL_RADIO11NSWITCH_OFF;
             break;

        case RADIO11NSWITCH_ON:
             radio11nSwitch_val = INTERNAL_RADIO11NSWITCH_ON;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for radio11nSwitch\n", radio11nSwitch_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set radio11nSwitch value.
     * set radio11nSwitch value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int ret1 = 0;
    DCLI_RADIO_API_GROUP_ONE *radio = NULL;
	int radio11nSwitch = -1;
	char RType[10] = { 0 };
	
    ret1 = show_radio_one(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,&radio);
	if((ret1 == 1)&&(radio->RADIO[0]))
	{
		if((radio->RADIO[0]->Radio_Type&IEEE80211_11N) > 0)/*radio type include 'n'*/
		{
			radio11nSwitch = RADIO11NSWITCH_ON;
		}
		else
		{
			radio11nSwitch = RADIO11NSWITCH_OFF;
		}
		memset(RType,0,sizeof(RType));
		Radio_Type(radio->RADIO[0]->Radio_Type,RType);
		Free_radio_one_head(radio);
	}
	else if(SNMPD_CONNECTION_ERROR == ret1) {
        close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
        return MFD_ERROR;
    }

	int rc = MFD_ERROR;
	int ret2 = 0;
	char radioMode[10] = { 0 };

	if(radio11nSwitch_val == radio11nSwitch)
	{
		rc = MFD_SUCCESS;
	}
	else
	{
		memset(radioMode,0,10);
		if(radio11nSwitch_val == RADIO11NSWITCH_ON)
		{
			if((strcmp(RType,"11b") == 0) || (strcmp(RType,"11g") == 0) ||(strcmp(RType,"11bg") == 0))
				strncpy(radioMode,"11b/g/n",sizeof(radioMode)-1);	
			else if(strcmp(RType,"11a") == 0)
				strncpy(radioMode,"11a/n",sizeof(radioMode)-1);
		}
		else if(radio11nSwitch_val == RADIO11NSWITCH_OFF)
		{
			if(strcmp(RType,"11n") == 0)
				strncpy(radioMode,"11n",sizeof(radioMode)-1);
			else if(strcmp(RType,"11bgn") == 0)
				strncpy(radioMode,"11b/g",sizeof(radioMode)-1);
			else if(strcmp(RType,"11an") == 0)
				strncpy(radioMode,"11a",sizeof(radioMode)-1);
		}
		
		ret2 = config_radio_mode(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioMode);	
		if(ret2 == 1)
		{				
			rowreq_ctx->data.radio11nSwitch = radio11nSwitch_val;
			rc = MFD_SUCCESS;
		}
		else
		{	
		    if(SNMPD_CONNECTION_ERROR == ret2) {
                close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
    	    }
			rc = MFD_ERROR;
		}
	}	

    return MFD_SUCCESS;
} /* radio11nSwitch_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
radio11nSwitch_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:radio11nSwitch_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up radio11nSwitch undo.
     */
    /*
     * copy radio11nSwitch data
     * set rowreq_ctx->data.radio11nSwitch from rowreq_ctx->undo->radio11nSwitch
     */
    rowreq_ctx->data.radio11nSwitch = rowreq_ctx->undo->radio11nSwitch;

    
    return MFD_SUCCESS;
} /* radio11nSwitch_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11ConfigRadioEntry.OnlyAllow11nStaAccess
 * OnlyAllow11nStaAccess is subid 25 of dot11ConfigRadioEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.3.1.25
 * Description:
Whether only allows 11N terminal access.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param OnlyAllow11nStaAccess_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadioTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
OnlyAllow11nStaAccess_check_value( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long OnlyAllow11nStaAccess_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:OnlyAllow11nStaAccess_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid OnlyAllow11nStaAccess value.
     */

    return MFD_SUCCESS; /* OnlyAllow11nStaAccess value not illegal */
} /* OnlyAllow11nStaAccess_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadioTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadioTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
OnlyAllow11nStaAccess_undo_setup( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:OnlyAllow11nStaAccess_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup OnlyAllow11nStaAccess undo.
     */
    /*
     * copy OnlyAllow11nStaAccess data
     * set rowreq_ctx->undo->OnlyAllow11nStaAccess from rowreq_ctx->data.OnlyAllow11nStaAccess
     */
    rowreq_ctx->undo->OnlyAllow11nStaAccess = rowreq_ctx->data.OnlyAllow11nStaAccess;


    return MFD_SUCCESS;
} /* OnlyAllow11nStaAccess_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param OnlyAllow11nStaAccess_val
 *        A long containing the new value.
 */
int
OnlyAllow11nStaAccess_set( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx, u_long OnlyAllow11nStaAccess_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:OnlyAllow11nStaAccess_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement OnlyAllow11nStaAccess reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(OnlyAllow11nStaAccess_val) {
        case TRUTHVALUE_TRUE:
             OnlyAllow11nStaAccess_val = INTERNAL_ONLYALLOW11NSTAACCESS_TRUE;
             break;

        case TRUTHVALUE_FALSE:
             OnlyAllow11nStaAccess_val = INTERNAL_ONLYALLOW11NSTAACCESS_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for OnlyAllow11nStaAccess\n", OnlyAllow11nStaAccess_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set OnlyAllow11nStaAccess value.
     * set OnlyAllow11nStaAccess value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int ret = 0;
	char workmode[10] = { 0 };
	int rc = MFD_ERROR;
	memset(workmode,0,10);
	if(OnlyAllow11nStaAccess_val == 1)
	{
		strncpy(workmode,"puren",sizeof(workmode)-1);
	}
	else if(OnlyAllow11nStaAccess_val == 2)
	{
		strncpy(workmode,"mixed",sizeof(workmode)-1);
	}
	else
	{
		rc = MFD_ERROR;
	}
	
	ret = set_radio_11n_puren_mixed_cmd(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpRadCurrID,rowreq_ctx->data.radioBindWlan,workmode);

	if(ret == 1)
	{
		rowreq_ctx->data.OnlyAllow11nStaAccess = OnlyAllow11nStaAccess_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}


    return rc;
} /* OnlyAllow11nStaAccess_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
OnlyAllow11nStaAccess_undo( dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadioTable:OnlyAllow11nStaAccess_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up OnlyAllow11nStaAccess undo.
     */
    /*
     * copy OnlyAllow11nStaAccess data
     * set rowreq_ctx->data.OnlyAllow11nStaAccess from rowreq_ctx->undo->OnlyAllow11nStaAccess
     */
    rowreq_ctx->data.OnlyAllow11nStaAccess = rowreq_ctx->undo->OnlyAllow11nStaAccess;

    
    return MFD_SUCCESS;
} /* OnlyAllow11nStaAccess_undo */


/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigRadioTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigRadioTable if you don't have dependencies)
 */
int
dot11ConfigRadioTable_check_dependencies(dot11ConfigRadioTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigRadioTable:dot11ConfigRadioTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigRadioTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11ConfigRadioTable_check_dependencies */

/** @} */
