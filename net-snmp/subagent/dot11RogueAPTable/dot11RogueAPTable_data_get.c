/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11RogueAPTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11RogueAPTable get routines.
 * TODO:240:M: Implement dot11RogueAPTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11RogueAPTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11RogueAPTable is subid 1 of wtpRogue.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.11.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11RogueAPTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11RogueAPTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueAPTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RogueAPEntry.rogueAPIndex
 * rogueAPIndex is subid 1 of dot11RogueAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.11.1.1.1
 * Description:
Rogue AP index
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueAPIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueAPIndex_map(long *mib_rogueAPIndex_val_ptr, long raw_rogueAPIndex_val)
{
    netsnmp_assert(NULL != mib_rogueAPIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueAPIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_rogueAPIndex_val_ptr) = raw_rogueAPIndex_val;

    return MFD_SUCCESS;
} /* rogueAPIndex_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11RogueAPTable_indexes_set_tbl_idx(dot11RogueAPTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long rogueAPIndex_val)
{
    DEBUGMSGTL(("verbose:dot11RogueAPTable:dot11RogueAPTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* rogueAPIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->rogueAPIndex = rogueAPIndex_val;
    

    return MFD_SUCCESS;
} /* dot11RogueAPTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11RogueAPTable_indexes_set(dot11RogueAPTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long rogueAPIndex_val)
{
    DEBUGMSGTL(("verbose:dot11RogueAPTable:dot11RogueAPTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11RogueAPTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , rogueAPIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11RogueAPTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11RogueAPTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RogueAPEntry.rogueAPMac
 * rogueAPMac is subid 2 of dot11RogueAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.11.1.1.2
 * Description:
Rogue AP mac
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueAPMac.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueAPMac_map(char **mib_rogueAPMac_val_ptr_ptr, size_t *mib_rogueAPMac_val_ptr_len_ptr, char *raw_rogueAPMac_val_ptr, size_t raw_rogueAPMac_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_rogueAPMac_val_ptr);
    netsnmp_assert((NULL != mib_rogueAPMac_val_ptr_ptr) && (NULL != mib_rogueAPMac_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPMac_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement rogueAPMac non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_rogueAPMac_val_ptr_len; /* assume equal */
    if((NULL == *mib_rogueAPMac_val_ptr_ptr) || (*mib_rogueAPMac_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_rogueAPMac_val_ptr_ptr = realloc( *mib_rogueAPMac_val_ptr_ptr, converted_len * sizeof(**mib_rogueAPMac_val_ptr_ptr));
        if(NULL == *mib_rogueAPMac_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_rogueAPMac_val_ptr_len_ptr = converted_len;
    memcpy( *mib_rogueAPMac_val_ptr_ptr, raw_rogueAPMac_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* rogueAPMac_map */

/**
 * Extract the current value of the rogueAPMac data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueAPMac_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rogueAPMac_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rogueAPMac.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rogueAPMac_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rogueAPMac_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rogueAPMac_get( dot11RogueAPTable_rowreq_ctx *rowreq_ctx, char **rogueAPMac_val_ptr_ptr, size_t *rogueAPMac_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != rogueAPMac_val_ptr_ptr) && (NULL != *rogueAPMac_val_ptr_ptr));
   netsnmp_assert( NULL != rogueAPMac_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPMac_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueAPMac data.
 * set (* rogueAPMac_val_ptr_ptr ) and (* rogueAPMac_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for rogueAPMac data
     */
    if ((NULL == (* rogueAPMac_val_ptr_ptr )) ||
        ((* rogueAPMac_val_ptr_len_ptr ) < (rowreq_ctx->data.rogueAPMac_len * sizeof((* rogueAPMac_val_ptr_ptr )[0])))) {
        /*
         * allocate space for rogueAPMac data
         */
        (* rogueAPMac_val_ptr_ptr ) = malloc(rowreq_ctx->data.rogueAPMac_len * sizeof((* rogueAPMac_val_ptr_ptr )[0]));
        if(NULL == (* rogueAPMac_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* rogueAPMac_val_ptr_len_ptr ) = rowreq_ctx->data.rogueAPMac_len * sizeof((* rogueAPMac_val_ptr_ptr )[0]);
    memcpy( (* rogueAPMac_val_ptr_ptr ), rowreq_ctx->data.rogueAPMac, (* rogueAPMac_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* rogueAPMac_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RogueAPEntry.rogueAPRate
 * rogueAPRate is subid 3 of dot11RogueAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.11.1.1.3
 * Description:
Rogue AP rate
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueAPRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueAPRate_map(long *mib_rogueAPRate_val_ptr, long raw_rogueAPRate_val)
{
    netsnmp_assert(NULL != mib_rogueAPRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueAPRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_rogueAPRate_val_ptr) = raw_rogueAPRate_val;

    return MFD_SUCCESS;
} /* rogueAPRate_map */

/**
 * Extract the current value of the rogueAPRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueAPRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueAPRate_get( dot11RogueAPTable_rowreq_ctx *rowreq_ctx, long * rogueAPRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueAPRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueAPRate data.
 * set (* rogueAPRate_val_ptr ) from rowreq_ctx->data
 */
    (* rogueAPRate_val_ptr ) = rowreq_ctx->data.rogueAPRate;

    return MFD_SUCCESS;
} /* rogueAPRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RogueAPEntry.rogueAPChannel
 * rogueAPChannel is subid 4 of dot11RogueAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.11.1.1.4
 * Description:
Rogue AP channel
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueAPChannel.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueAPChannel_map(long *mib_rogueAPChannel_val_ptr, long raw_rogueAPChannel_val)
{
    netsnmp_assert(NULL != mib_rogueAPChannel_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPChannel_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueAPChannel mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_rogueAPChannel_val_ptr) = raw_rogueAPChannel_val;

    return MFD_SUCCESS;
} /* rogueAPChannel_map */

/**
 * Extract the current value of the rogueAPChannel data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueAPChannel_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueAPChannel_get( dot11RogueAPTable_rowreq_ctx *rowreq_ctx, long * rogueAPChannel_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueAPChannel_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPChannel_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueAPChannel data.
 * set (* rogueAPChannel_val_ptr ) from rowreq_ctx->data
 */
    (* rogueAPChannel_val_ptr ) = rowreq_ctx->data.rogueAPChannel;

    return MFD_SUCCESS;
} /* rogueAPChannel_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RogueAPEntry.rogueAPRssi
 * rogueAPRssi is subid 5 of dot11RogueAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.11.1.1.5
 * Description:
Rogue AP rssi
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueAPRssi.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueAPRssi_map(long *mib_rogueAPRssi_val_ptr, long raw_rogueAPRssi_val)
{
    netsnmp_assert(NULL != mib_rogueAPRssi_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPRssi_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueAPRssi mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_rogueAPRssi_val_ptr) = raw_rogueAPRssi_val;

    return MFD_SUCCESS;
} /* rogueAPRssi_map */

/**
 * Extract the current value of the rogueAPRssi data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueAPRssi_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueAPRssi_get( dot11RogueAPTable_rowreq_ctx *rowreq_ctx, long * rogueAPRssi_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueAPRssi_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPRssi_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueAPRssi data.
 * set (* rogueAPRssi_val_ptr ) from rowreq_ctx->data
 */
    (* rogueAPRssi_val_ptr ) = rowreq_ctx->data.rogueAPRssi;

    return MFD_SUCCESS;
} /* rogueAPRssi_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RogueAPEntry.rogueAPNoise
 * rogueAPNoise is subid 6 of dot11RogueAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.11.1.1.6
 * Description:
Rogue AP noise
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueAPNoise.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueAPNoise_map(long *mib_rogueAPNoise_val_ptr, long raw_rogueAPNoise_val)
{
    netsnmp_assert(NULL != mib_rogueAPNoise_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPNoise_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueAPNoise mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_rogueAPNoise_val_ptr) = raw_rogueAPNoise_val;

    return MFD_SUCCESS;
} /* rogueAPNoise_map */

/**
 * Extract the current value of the rogueAPNoise data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueAPNoise_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueAPNoise_get( dot11RogueAPTable_rowreq_ctx *rowreq_ctx, long * rogueAPNoise_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueAPNoise_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPNoise_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueAPNoise data.
 * set (* rogueAPNoise_val_ptr ) from rowreq_ctx->data
 */
    (* rogueAPNoise_val_ptr ) = rowreq_ctx->data.rogueAPNoise;

    return MFD_SUCCESS;
} /* rogueAPNoise_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RogueAPEntry.rogueAPBeaconInterval
 * rogueAPBeaconInterval is subid 7 of dot11RogueAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.11.1.1.7
 * Description:
Rogue AP beacon interval
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueAPBeaconInterval.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueAPBeaconInterval_map(long *mib_rogueAPBeaconInterval_val_ptr, long raw_rogueAPBeaconInterval_val)
{
    netsnmp_assert(NULL != mib_rogueAPBeaconInterval_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPBeaconInterval_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueAPBeaconInterval mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_rogueAPBeaconInterval_val_ptr) = raw_rogueAPBeaconInterval_val;

    return MFD_SUCCESS;
} /* rogueAPBeaconInterval_map */

/**
 * Extract the current value of the rogueAPBeaconInterval data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueAPBeaconInterval_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueAPBeaconInterval_get( dot11RogueAPTable_rowreq_ctx *rowreq_ctx, long * rogueAPBeaconInterval_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueAPBeaconInterval_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPBeaconInterval_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueAPBeaconInterval data.
 * set (* rogueAPBeaconInterval_val_ptr ) from rowreq_ctx->data
 */
    (* rogueAPBeaconInterval_val_ptr ) = rowreq_ctx->data.rogueAPBeaconInterval;

    return MFD_SUCCESS;
} /* rogueAPBeaconInterval_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RogueAPEntry.rogueAPCapability
 * rogueAPCapability is subid 8 of dot11RogueAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.11.1.1.8
 * Description:
Rogue AP capability
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueAPCapability.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueAPCapability_map(long *mib_rogueAPCapability_val_ptr, long raw_rogueAPCapability_val)
{
    netsnmp_assert(NULL != mib_rogueAPCapability_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPCapability_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueAPCapability mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_rogueAPCapability_val_ptr) = raw_rogueAPCapability_val;

    return MFD_SUCCESS;
} /* rogueAPCapability_map */

/**
 * Extract the current value of the rogueAPCapability data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueAPCapability_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueAPCapability_get( dot11RogueAPTable_rowreq_ctx *rowreq_ctx, long * rogueAPCapability_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueAPCapability_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPCapability_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueAPCapability data.
 * set (* rogueAPCapability_val_ptr ) from rowreq_ctx->data
 */
    (* rogueAPCapability_val_ptr ) = rowreq_ctx->data.rogueAPCapability;

    return MFD_SUCCESS;
} /* rogueAPCapability_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RogueAPEntry.rogueAPEssid
 * rogueAPEssid is subid 9 of dot11RogueAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.11.1.1.9
 * Description:
Rogue AP essid
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueAPEssid.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueAPEssid_map(char **mib_rogueAPEssid_val_ptr_ptr, size_t *mib_rogueAPEssid_val_ptr_len_ptr, char *raw_rogueAPEssid_val_ptr, size_t raw_rogueAPEssid_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_rogueAPEssid_val_ptr);
    netsnmp_assert((NULL != mib_rogueAPEssid_val_ptr_ptr) && (NULL != mib_rogueAPEssid_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPEssid_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement rogueAPEssid non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_rogueAPEssid_val_ptr_len; /* assume equal */
    if((NULL == *mib_rogueAPEssid_val_ptr_ptr) || (*mib_rogueAPEssid_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_rogueAPEssid_val_ptr_ptr = realloc( *mib_rogueAPEssid_val_ptr_ptr, converted_len * sizeof(**mib_rogueAPEssid_val_ptr_ptr));
        if(NULL == *mib_rogueAPEssid_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_rogueAPEssid_val_ptr_len_ptr = converted_len;
    memcpy( *mib_rogueAPEssid_val_ptr_ptr, raw_rogueAPEssid_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* rogueAPEssid_map */

/**
 * Extract the current value of the rogueAPEssid data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueAPEssid_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rogueAPEssid_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rogueAPEssid.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rogueAPEssid_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rogueAPEssid_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rogueAPEssid_get( dot11RogueAPTable_rowreq_ctx *rowreq_ctx, char **rogueAPEssid_val_ptr_ptr, size_t *rogueAPEssid_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != rogueAPEssid_val_ptr_ptr) && (NULL != *rogueAPEssid_val_ptr_ptr));
   netsnmp_assert( NULL != rogueAPEssid_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPEssid_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueAPEssid data.
 * set (* rogueAPEssid_val_ptr_ptr ) and (* rogueAPEssid_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for rogueAPEssid data
     */
    if ((NULL == (* rogueAPEssid_val_ptr_ptr )) ||
        ((* rogueAPEssid_val_ptr_len_ptr ) < (rowreq_ctx->data.rogueAPEssid_len * sizeof((* rogueAPEssid_val_ptr_ptr )[0])))) {
        /*
         * allocate space for rogueAPEssid data
         */
        (* rogueAPEssid_val_ptr_ptr ) = malloc(rowreq_ctx->data.rogueAPEssid_len * sizeof((* rogueAPEssid_val_ptr_ptr )[0]));
        if(NULL == (* rogueAPEssid_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* rogueAPEssid_val_ptr_len_ptr ) = rowreq_ctx->data.rogueAPEssid_len * sizeof((* rogueAPEssid_val_ptr_ptr )[0]);
    memcpy( (* rogueAPEssid_val_ptr_ptr ), rowreq_ctx->data.rogueAPEssid, (* rogueAPEssid_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* rogueAPEssid_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RogueAPEntry.rogueAPElemInfo
 * rogueAPElemInfo is subid 10 of dot11RogueAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.11.1.1.10
 * Description:
Rogue AP element info
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueAPElemInfo.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueAPElemInfo_map(char **mib_rogueAPElemInfo_val_ptr_ptr, size_t *mib_rogueAPElemInfo_val_ptr_len_ptr, char *raw_rogueAPElemInfo_val_ptr, size_t raw_rogueAPElemInfo_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_rogueAPElemInfo_val_ptr);
    netsnmp_assert((NULL != mib_rogueAPElemInfo_val_ptr_ptr) && (NULL != mib_rogueAPElemInfo_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPElemInfo_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement rogueAPElemInfo non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_rogueAPElemInfo_val_ptr_len; /* assume equal */
    if((NULL == *mib_rogueAPElemInfo_val_ptr_ptr) || (*mib_rogueAPElemInfo_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_rogueAPElemInfo_val_ptr_ptr = realloc( *mib_rogueAPElemInfo_val_ptr_ptr, converted_len * sizeof(**mib_rogueAPElemInfo_val_ptr_ptr));
        if(NULL == *mib_rogueAPElemInfo_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_rogueAPElemInfo_val_ptr_len_ptr = converted_len;
    memcpy( *mib_rogueAPElemInfo_val_ptr_ptr, raw_rogueAPElemInfo_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* rogueAPElemInfo_map */

/**
 * Extract the current value of the rogueAPElemInfo data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueAPElemInfo_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rogueAPElemInfo_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rogueAPElemInfo.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rogueAPElemInfo_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rogueAPElemInfo_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rogueAPElemInfo_get( dot11RogueAPTable_rowreq_ctx *rowreq_ctx, char **rogueAPElemInfo_val_ptr_ptr, size_t *rogueAPElemInfo_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != rogueAPElemInfo_val_ptr_ptr) && (NULL != *rogueAPElemInfo_val_ptr_ptr));
   netsnmp_assert( NULL != rogueAPElemInfo_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueAPTable:rogueAPElemInfo_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueAPElemInfo data.
 * set (* rogueAPElemInfo_val_ptr_ptr ) and (* rogueAPElemInfo_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for rogueAPElemInfo data
     */
    if ((NULL == (* rogueAPElemInfo_val_ptr_ptr )) ||
        ((* rogueAPElemInfo_val_ptr_len_ptr ) < (rowreq_ctx->data.rogueAPElemInfo_len * sizeof((* rogueAPElemInfo_val_ptr_ptr )[0])))) {
        /*
         * allocate space for rogueAPElemInfo data
         */
        (* rogueAPElemInfo_val_ptr_ptr ) = malloc(rowreq_ctx->data.rogueAPElemInfo_len * sizeof((* rogueAPElemInfo_val_ptr_ptr )[0]));
        if(NULL == (* rogueAPElemInfo_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* rogueAPElemInfo_val_ptr_len_ptr ) = rowreq_ctx->data.rogueAPElemInfo_len * sizeof((* rogueAPElemInfo_val_ptr_ptr )[0]);
    memcpy( (* rogueAPElemInfo_val_ptr_ptr ), rowreq_ctx->data.rogueAPElemInfo, (* rogueAPElemInfo_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* rogueAPElemInfo_get */



/** @} */
