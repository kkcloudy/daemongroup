/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11NewNeighborAPTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11NewNeighborAPTable get routines.
 * TODO:240:M: Implement dot11NewNeighborAPTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11NewNeighborAPTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11NewNeighborAPTable is subid 2 of wtpNeighbor.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11NewNeighborAPTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11NewNeighborAPTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfIndex
 * wtpWirelessIfIndex is subid 1 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.1
 * Description:
Wireless interface index.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessIfIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessIfIndex_dot11NewNeighborAPTable_map(long *mib_wtpWirelessIfIndex_val_ptr, long raw_wtpWirelessIfIndex_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessIfIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:wtpWirelessIfIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessIfIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessIfIndex_val_ptr) = raw_wtpWirelessIfIndex_val;

    return MFD_SUCCESS;
} /* wtpWirelessIfIndex_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NeighborAPEntry.neighborAPIndex
 * neighborAPIndex is subid 1 of dot11NeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.1.1.1
 * Description:
Rogue AP index
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-neighborAPIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
neighborAPIndex_dot11NewNeighborAPTable_map(long *mib_neighborAPIndex_val_ptr, long raw_neighborAPIndex_val)
{
    netsnmp_assert(NULL != mib_neighborAPIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:neighborAPIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement neighborAPIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_neighborAPIndex_val_ptr) = raw_neighborAPIndex_val;

    return MFD_SUCCESS;
} /* neighborAPIndex_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11NewNeighborAPTable_indexes_set_tbl_idx(dot11NewNeighborAPTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpWirelessIfIndex_val, long neighborAPIndex_val)
{
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:dot11NewNeighborAPTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wtpWirelessIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wtpWirelessIfIndex = wtpWirelessIfIndex_val;
    
    /* neighborAPIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->neighborAPIndex = neighborAPIndex_val;
    

    return MFD_SUCCESS;
} /* dot11NewNeighborAPTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11NewNeighborAPTable_indexes_set(dot11NewNeighborAPTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpWirelessIfIndex_val, long neighborAPIndex_val)
{
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:dot11NewNeighborAPTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11NewNeighborAPTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpWirelessIfIndex_val
                                   , neighborAPIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11NewNeighborAPTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11NewNeighborAPTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewNeighborAPEntry.NewneighborAPMac
 * NewneighborAPMac is subid 1 of dot11NewNeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.2.1.1
 * Description:
Rogue AP mac
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewneighborAPMac.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewneighborAPMac_map(char **mib_NewneighborAPMac_val_ptr_ptr, size_t *mib_NewneighborAPMac_val_ptr_len_ptr, char *raw_NewneighborAPMac_val_ptr, size_t raw_NewneighborAPMac_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_NewneighborAPMac_val_ptr);
    netsnmp_assert((NULL != mib_NewneighborAPMac_val_ptr_ptr) && (NULL != mib_NewneighborAPMac_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPMac_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement NewneighborAPMac non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_NewneighborAPMac_val_ptr_len; /* assume equal */
    if((NULL == *mib_NewneighborAPMac_val_ptr_ptr) || (*mib_NewneighborAPMac_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_NewneighborAPMac_val_ptr_ptr = realloc( *mib_NewneighborAPMac_val_ptr_ptr, converted_len * sizeof(**mib_NewneighborAPMac_val_ptr_ptr));
        if(NULL == *mib_NewneighborAPMac_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_NewneighborAPMac_val_ptr_len_ptr = converted_len;
    memcpy( *mib_NewneighborAPMac_val_ptr_ptr, raw_NewneighborAPMac_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* NewneighborAPMac_map */

/**
 * Extract the current value of the NewneighborAPMac data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewneighborAPMac_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewneighborAPMac_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewneighborAPMac.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewneighborAPMac_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewneighborAPMac_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewneighborAPMac_get( dot11NewNeighborAPTable_rowreq_ctx *rowreq_ctx, char **NewneighborAPMac_val_ptr_ptr, size_t *NewneighborAPMac_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewneighborAPMac_val_ptr_ptr) && (NULL != *NewneighborAPMac_val_ptr_ptr));
   netsnmp_assert( NULL != NewneighborAPMac_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPMac_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewneighborAPMac data.
 * set (* NewneighborAPMac_val_ptr_ptr ) and (* NewneighborAPMac_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewneighborAPMac data
     */
    if ((NULL == (* NewneighborAPMac_val_ptr_ptr )) ||
        ((* NewneighborAPMac_val_ptr_len_ptr ) < (rowreq_ctx->data.NewneighborAPMac_len * sizeof((* NewneighborAPMac_val_ptr_ptr )[0])))) {
        /*
         * allocate space for NewneighborAPMac data
         */
        (* NewneighborAPMac_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewneighborAPMac_len * sizeof((* NewneighborAPMac_val_ptr_ptr )[0]));
        if(NULL == (* NewneighborAPMac_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewneighborAPMac_val_ptr_len_ptr ) = rowreq_ctx->data.NewneighborAPMac_len * sizeof((* NewneighborAPMac_val_ptr_ptr )[0]);
    memcpy( (* NewneighborAPMac_val_ptr_ptr ), rowreq_ctx->data.NewneighborAPMac, (* NewneighborAPMac_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* NewneighborAPMac_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewNeighborAPEntry.NewneighborAPRate
 * NewneighborAPRate is subid 2 of dot11NewNeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.2.1.2
 * Description:
Rogue AP rate
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewneighborAPRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewneighborAPRate_map(long *mib_NewneighborAPRate_val_ptr, long raw_NewneighborAPRate_val)
{
    netsnmp_assert(NULL != mib_NewneighborAPRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewneighborAPRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewneighborAPRate_val_ptr) = raw_NewneighborAPRate_val;

    return MFD_SUCCESS;
} /* NewneighborAPRate_map */

/**
 * Extract the current value of the NewneighborAPRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewneighborAPRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewneighborAPRate_get( dot11NewNeighborAPTable_rowreq_ctx *rowreq_ctx, long * NewneighborAPRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewneighborAPRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewneighborAPRate data.
 * set (* NewneighborAPRate_val_ptr ) from rowreq_ctx->data
 */
    (* NewneighborAPRate_val_ptr ) = rowreq_ctx->data.NewneighborAPRate;

    return MFD_SUCCESS;
} /* NewneighborAPRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewNeighborAPEntry.NewneighborAPChannel
 * NewneighborAPChannel is subid 3 of dot11NewNeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.2.1.3
 * Description:
Rogue AP channel
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewneighborAPChannel.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewneighborAPChannel_map(long *mib_NewneighborAPChannel_val_ptr, long raw_NewneighborAPChannel_val)
{
    netsnmp_assert(NULL != mib_NewneighborAPChannel_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPChannel_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewneighborAPChannel mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewneighborAPChannel_val_ptr) = raw_NewneighborAPChannel_val;

    return MFD_SUCCESS;
} /* NewneighborAPChannel_map */

/**
 * Extract the current value of the NewneighborAPChannel data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewneighborAPChannel_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewneighborAPChannel_get( dot11NewNeighborAPTable_rowreq_ctx *rowreq_ctx, long * NewneighborAPChannel_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewneighborAPChannel_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPChannel_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewneighborAPChannel data.
 * set (* NewneighborAPChannel_val_ptr ) from rowreq_ctx->data
 */
    (* NewneighborAPChannel_val_ptr ) = rowreq_ctx->data.NewneighborAPChannel;

    return MFD_SUCCESS;
} /* NewneighborAPChannel_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewNeighborAPEntry.NewneighborAPRssi
 * NewneighborAPRssi is subid 4 of dot11NewNeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.2.1.4
 * Description:
Rogue AP rssi
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewneighborAPRssi.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewneighborAPRssi_map(long *mib_NewneighborAPRssi_val_ptr, long raw_NewneighborAPRssi_val)
{
    netsnmp_assert(NULL != mib_NewneighborAPRssi_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPRssi_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewneighborAPRssi mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewneighborAPRssi_val_ptr) = raw_NewneighborAPRssi_val;

    return MFD_SUCCESS;
} /* NewneighborAPRssi_map */

/**
 * Extract the current value of the NewneighborAPRssi data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewneighborAPRssi_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewneighborAPRssi_get( dot11NewNeighborAPTable_rowreq_ctx *rowreq_ctx, long * NewneighborAPRssi_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewneighborAPRssi_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPRssi_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewneighborAPRssi data.
 * set (* NewneighborAPRssi_val_ptr ) from rowreq_ctx->data
 */
    (* NewneighborAPRssi_val_ptr ) = rowreq_ctx->data.NewneighborAPRssi;

    return MFD_SUCCESS;
} /* NewneighborAPRssi_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewNeighborAPEntry.NewneighborAPNoise
 * NewneighborAPNoise is subid 5 of dot11NewNeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.2.1.5
 * Description:
Rogue AP noise
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewneighborAPNoise.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewneighborAPNoise_map(long *mib_NewneighborAPNoise_val_ptr, long raw_NewneighborAPNoise_val)
{
    netsnmp_assert(NULL != mib_NewneighborAPNoise_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPNoise_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewneighborAPNoise mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewneighborAPNoise_val_ptr) = raw_NewneighborAPNoise_val;

    return MFD_SUCCESS;
} /* NewneighborAPNoise_map */

/**
 * Extract the current value of the NewneighborAPNoise data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewneighborAPNoise_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewneighborAPNoise_get( dot11NewNeighborAPTable_rowreq_ctx *rowreq_ctx, long * NewneighborAPNoise_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewneighborAPNoise_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPNoise_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewneighborAPNoise data.
 * set (* NewneighborAPNoise_val_ptr ) from rowreq_ctx->data
 */
    (* NewneighborAPNoise_val_ptr ) = rowreq_ctx->data.NewneighborAPNoise;

    return MFD_SUCCESS;
} /* NewneighborAPNoise_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewNeighborAPEntry.NewneighborAPBeaconInterval
 * NewneighborAPBeaconInterval is subid 6 of dot11NewNeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.2.1.6
 * Description:
Rogue AP beacon interval
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewneighborAPBeaconInterval.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewneighborAPBeaconInterval_map(long *mib_NewneighborAPBeaconInterval_val_ptr, long raw_NewneighborAPBeaconInterval_val)
{
    netsnmp_assert(NULL != mib_NewneighborAPBeaconInterval_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPBeaconInterval_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewneighborAPBeaconInterval mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewneighborAPBeaconInterval_val_ptr) = raw_NewneighborAPBeaconInterval_val;

    return MFD_SUCCESS;
} /* NewneighborAPBeaconInterval_map */

/**
 * Extract the current value of the NewneighborAPBeaconInterval data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewneighborAPBeaconInterval_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewneighborAPBeaconInterval_get( dot11NewNeighborAPTable_rowreq_ctx *rowreq_ctx, long * NewneighborAPBeaconInterval_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewneighborAPBeaconInterval_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPBeaconInterval_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewneighborAPBeaconInterval data.
 * set (* NewneighborAPBeaconInterval_val_ptr ) from rowreq_ctx->data
 */
    (* NewneighborAPBeaconInterval_val_ptr ) = rowreq_ctx->data.NewneighborAPBeaconInterval;

    return MFD_SUCCESS;
} /* NewneighborAPBeaconInterval_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewNeighborAPEntry.NewneighborAPCapability
 * NewneighborAPCapability is subid 7 of dot11NewNeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.2.1.7
 * Description:
Rogue AP capability
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewneighborAPCapability.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewneighborAPCapability_map(long *mib_NewneighborAPCapability_val_ptr, long raw_NewneighborAPCapability_val)
{
    netsnmp_assert(NULL != mib_NewneighborAPCapability_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPCapability_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewneighborAPCapability mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewneighborAPCapability_val_ptr) = raw_NewneighborAPCapability_val;

    return MFD_SUCCESS;
} /* NewneighborAPCapability_map */

/**
 * Extract the current value of the NewneighborAPCapability data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewneighborAPCapability_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewneighborAPCapability_get( dot11NewNeighborAPTable_rowreq_ctx *rowreq_ctx, long * NewneighborAPCapability_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewneighborAPCapability_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPCapability_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewneighborAPCapability data.
 * set (* NewneighborAPCapability_val_ptr ) from rowreq_ctx->data
 */
    (* NewneighborAPCapability_val_ptr ) = rowreq_ctx->data.NewneighborAPCapability;

    return MFD_SUCCESS;
} /* NewneighborAPCapability_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewNeighborAPEntry.NewneighborAPEssid
 * NewneighborAPEssid is subid 8 of dot11NewNeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.2.1.8
 * Description:
Rogue AP essid
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewneighborAPEssid.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewneighborAPEssid_map(char **mib_NewneighborAPEssid_val_ptr_ptr, size_t *mib_NewneighborAPEssid_val_ptr_len_ptr, char *raw_NewneighborAPEssid_val_ptr, size_t raw_NewneighborAPEssid_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_NewneighborAPEssid_val_ptr);
    netsnmp_assert((NULL != mib_NewneighborAPEssid_val_ptr_ptr) && (NULL != mib_NewneighborAPEssid_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPEssid_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement NewneighborAPEssid non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_NewneighborAPEssid_val_ptr_len; /* assume equal */
    if((NULL == *mib_NewneighborAPEssid_val_ptr_ptr) || (*mib_NewneighborAPEssid_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_NewneighborAPEssid_val_ptr_ptr = realloc( *mib_NewneighborAPEssid_val_ptr_ptr, converted_len * sizeof(**mib_NewneighborAPEssid_val_ptr_ptr));
        if(NULL == *mib_NewneighborAPEssid_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_NewneighborAPEssid_val_ptr_len_ptr = converted_len;
    memcpy( *mib_NewneighborAPEssid_val_ptr_ptr, raw_NewneighborAPEssid_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* NewneighborAPEssid_map */

/**
 * Extract the current value of the NewneighborAPEssid data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewneighborAPEssid_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewneighborAPEssid_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewneighborAPEssid.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewneighborAPEssid_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewneighborAPEssid_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewneighborAPEssid_get( dot11NewNeighborAPTable_rowreq_ctx *rowreq_ctx, char **NewneighborAPEssid_val_ptr_ptr, size_t *NewneighborAPEssid_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewneighborAPEssid_val_ptr_ptr) && (NULL != *NewneighborAPEssid_val_ptr_ptr));
   netsnmp_assert( NULL != NewneighborAPEssid_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPEssid_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewneighborAPEssid data.
 * set (* NewneighborAPEssid_val_ptr_ptr ) and (* NewneighborAPEssid_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewneighborAPEssid data
     */
    if ((NULL == (* NewneighborAPEssid_val_ptr_ptr )) ||
        ((* NewneighborAPEssid_val_ptr_len_ptr ) < (rowreq_ctx->data.NewneighborAPEssid_len * sizeof((* NewneighborAPEssid_val_ptr_ptr )[0])))) {
        /*
         * allocate space for NewneighborAPEssid data
         */
        (* NewneighborAPEssid_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewneighborAPEssid_len * sizeof((* NewneighborAPEssid_val_ptr_ptr )[0]));
        if(NULL == (* NewneighborAPEssid_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewneighborAPEssid_val_ptr_len_ptr ) = rowreq_ctx->data.NewneighborAPEssid_len * sizeof((* NewneighborAPEssid_val_ptr_ptr )[0]);
    memcpy( (* NewneighborAPEssid_val_ptr_ptr ), rowreq_ctx->data.NewneighborAPEssid, (* NewneighborAPEssid_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* NewneighborAPEssid_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewNeighborAPEntry.NewneighborAPElemInfo
 * NewneighborAPElemInfo is subid 9 of dot11NewNeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.2.1.9
 * Description:
Rogue AP element info
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewneighborAPElemInfo.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewneighborAPElemInfo_map(char **mib_NewneighborAPElemInfo_val_ptr_ptr, size_t *mib_NewneighborAPElemInfo_val_ptr_len_ptr, char *raw_NewneighborAPElemInfo_val_ptr, size_t raw_NewneighborAPElemInfo_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_NewneighborAPElemInfo_val_ptr);
    netsnmp_assert((NULL != mib_NewneighborAPElemInfo_val_ptr_ptr) && (NULL != mib_NewneighborAPElemInfo_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPElemInfo_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement NewneighborAPElemInfo non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_NewneighborAPElemInfo_val_ptr_len; /* assume equal */
    if((NULL == *mib_NewneighborAPElemInfo_val_ptr_ptr) || (*mib_NewneighborAPElemInfo_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_NewneighborAPElemInfo_val_ptr_ptr = realloc( *mib_NewneighborAPElemInfo_val_ptr_ptr, converted_len * sizeof(**mib_NewneighborAPElemInfo_val_ptr_ptr));
        if(NULL == *mib_NewneighborAPElemInfo_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_NewneighborAPElemInfo_val_ptr_len_ptr = converted_len;
    memcpy( *mib_NewneighborAPElemInfo_val_ptr_ptr, raw_NewneighborAPElemInfo_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* NewneighborAPElemInfo_map */

/**
 * Extract the current value of the NewneighborAPElemInfo data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewneighborAPElemInfo_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewneighborAPElemInfo_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewneighborAPElemInfo.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewneighborAPElemInfo_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewneighborAPElemInfo_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewneighborAPElemInfo_get( dot11NewNeighborAPTable_rowreq_ctx *rowreq_ctx, char **NewneighborAPElemInfo_val_ptr_ptr, size_t *NewneighborAPElemInfo_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewneighborAPElemInfo_val_ptr_ptr) && (NULL != *NewneighborAPElemInfo_val_ptr_ptr));
   netsnmp_assert( NULL != NewneighborAPElemInfo_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPElemInfo_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewneighborAPElemInfo data.
 * set (* NewneighborAPElemInfo_val_ptr_ptr ) and (* NewneighborAPElemInfo_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewneighborAPElemInfo data
     */
    if ((NULL == (* NewneighborAPElemInfo_val_ptr_ptr )) ||
        ((* NewneighborAPElemInfo_val_ptr_len_ptr ) < (rowreq_ctx->data.NewneighborAPElemInfo_len * sizeof((* NewneighborAPElemInfo_val_ptr_ptr )[0])))) {
        /*
         * allocate space for NewneighborAPElemInfo data
         */
        (* NewneighborAPElemInfo_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewneighborAPElemInfo_len * sizeof((* NewneighborAPElemInfo_val_ptr_ptr )[0]));
        if(NULL == (* NewneighborAPElemInfo_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewneighborAPElemInfo_val_ptr_len_ptr ) = rowreq_ctx->data.NewneighborAPElemInfo_len * sizeof((* NewneighborAPElemInfo_val_ptr_ptr )[0]);
    memcpy( (* NewneighborAPElemInfo_val_ptr_ptr ), rowreq_ctx->data.NewneighborAPElemInfo, (* NewneighborAPElemInfo_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* NewneighborAPElemInfo_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11NewNeighborAPEntry.NewneighborAPSignalIntensity
 * NewneighborAPSignalIntensity is subid 10 of dot11NewNeighborAPEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.15.2.1.10
 * Description:
Rogue AP SignalIntensity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewneighborAPSignalIntensity.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewneighborAPSignalIntensity_map(long *mib_NewneighborAPSignalIntensity_val_ptr, long raw_NewneighborAPSignalIntensity_val)
{
    netsnmp_assert(NULL != mib_NewneighborAPSignalIntensity_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPSignalIntensity_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewneighborAPSignalIntensity mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewneighborAPSignalIntensity_val_ptr) = raw_NewneighborAPSignalIntensity_val;

    return MFD_SUCCESS;
} /* NewneighborAPSignalIntensity_map */

/**
 * Extract the current value of the NewneighborAPSignalIntensity data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewneighborAPSignalIntensity_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewneighborAPSignalIntensity_get( dot11NewNeighborAPTable_rowreq_ctx *rowreq_ctx, long * NewneighborAPSignalIntensity_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewneighborAPSignalIntensity_val_ptr );


    DEBUGMSGTL(("verbose:dot11NewNeighborAPTable:NewneighborAPSignalIntensity_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewneighborAPSignalIntensity data.
 * set (* NewneighborAPSignalIntensity_val_ptr ) from rowreq_ctx->data
 */
    (* NewneighborAPSignalIntensity_val_ptr ) = rowreq_ctx->data.NewneighborAPSignalIntensity;

    return MFD_SUCCESS;
} /* NewneighborAPSignalIntensity_get */



/** @} */
