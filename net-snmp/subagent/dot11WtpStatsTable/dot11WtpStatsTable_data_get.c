/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpStatsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpStatsTable get routines.
 * TODO:240:M: Implement dot11WtpStatsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpStatsTable is subid 4 of wtpStats.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpStatsTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11WtpStatsTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpStatsTable_indexes_set_tbl_idx(dot11WtpStatsTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:dot11WtpStatsTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11WtpStatsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpStatsTable_indexes_set(dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:dot11WtpStatsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpStatsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpStatsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpStatsTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpAllowConUsrMnt
 * wtpAllowConUsrMnt is subid 1 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.1
 * Description:
The total number of user allowed to switch-in.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpAllowConUsrMnt.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpAllowConUsrMnt_map(long *mib_wtpAllowConUsrMnt_val_ptr, long raw_wtpAllowConUsrMnt_val)
{
    netsnmp_assert(NULL != mib_wtpAllowConUsrMnt_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpAllowConUsrMnt_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpAllowConUsrMnt mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpAllowConUsrMnt_val_ptr) = raw_wtpAllowConUsrMnt_val;

    return MFD_SUCCESS;
} /* wtpAllowConUsrMnt_map */

/**
 * Extract the current value of the wtpAllowConUsrMnt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAllowConUsrMnt_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpAllowConUsrMnt_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, long * wtpAllowConUsrMnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAllowConUsrMnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpAllowConUsrMnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpAllowConUsrMnt data.
 * set (* wtpAllowConUsrMnt_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAllowConUsrMnt_val_ptr ) = rowreq_ctx->data.wtpAllowConUsrMnt;

    return MFD_SUCCESS;
} /* wtpAllowConUsrMnt_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpMountConUsrTimes
 * wtpMountConUsrTimes is subid 2 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.2
 * Description:
The number of users(802.11b/g) associated.(Times)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMountConUsrTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMountConUsrTimes_map(u_long *mib_wtpMountConUsrTimes_val_ptr, u_long raw_wtpMountConUsrTimes_val)
{
    netsnmp_assert(NULL != mib_wtpMountConUsrTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpMountConUsrTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMountConUsrTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMountConUsrTimes_val_ptr) = raw_wtpMountConUsrTimes_val;

    return MFD_SUCCESS;
} /* wtpMountConUsrTimes_map */

/**
 * Extract the current value of the wtpMountConUsrTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMountConUsrTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMountConUsrTimes_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpMountConUsrTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMountConUsrTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpMountConUsrTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMountConUsrTimes data.
 * set (* wtpMountConUsrTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMountConUsrTimes_val_ptr ) = rowreq_ctx->data.wtpMountConUsrTimes;

    return MFD_SUCCESS;
} /* wtpMountConUsrTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpMountConFailTimes
 * wtpMountConFailTimes is subid 3 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.3
 * Description:
The number of 802.11b/g failed link.(Times)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMountConFailTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMountConFailTimes_map(u_long *mib_wtpMountConFailTimes_val_ptr, u_long raw_wtpMountConFailTimes_val)
{
    netsnmp_assert(NULL != mib_wtpMountConFailTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpMountConFailTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMountConFailTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMountConFailTimes_val_ptr) = raw_wtpMountConFailTimes_val;

    return MFD_SUCCESS;
} /* wtpMountConFailTimes_map */

/**
 * Extract the current value of the wtpMountConFailTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMountConFailTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMountConFailTimes_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpMountConFailTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMountConFailTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpMountConFailTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMountConFailTimes data.
 * set (* wtpMountConFailTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMountConFailTimes_val_ptr ) = rowreq_ctx->data.wtpMountConFailTimes;

    return MFD_SUCCESS;
} /* wtpMountConFailTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpMountConReasTimes
 * wtpMountConReasTimes is subid 4 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.4
 * Description:
The total number of current reassociate(802.11b/g).(Times)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMountConReasTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMountConReasTimes_map(u_long *mib_wtpMountConReasTimes_val_ptr, u_long raw_wtpMountConReasTimes_val)
{
    netsnmp_assert(NULL != mib_wtpMountConReasTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpMountConReasTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMountConReasTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMountConReasTimes_val_ptr) = raw_wtpMountConReasTimes_val;

    return MFD_SUCCESS;
} /* wtpMountConReasTimes_map */

/**
 * Extract the current value of the wtpMountConReasTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMountConReasTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMountConReasTimes_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpMountConReasTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMountConReasTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpMountConReasTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMountConReasTimes data.
 * set (* wtpMountConReasTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMountConReasTimes_val_ptr ) = rowreq_ctx->data.wtpMountConReasTimes;

    return MFD_SUCCESS;
} /* wtpMountConReasTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpSupportSSID
 * wtpSupportSSID is subid 5 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.5
 * Description:
Supported SSID
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSupportSSID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSupportSSID_map(char **mib_wtpSupportSSID_val_ptr_ptr, size_t *mib_wtpSupportSSID_val_ptr_len_ptr, char *raw_wtpSupportSSID_val_ptr, size_t raw_wtpSupportSSID_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpSupportSSID_val_ptr);
    netsnmp_assert((NULL != mib_wtpSupportSSID_val_ptr_ptr) && (NULL != mib_wtpSupportSSID_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpSupportSSID_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpSupportSSID non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpSupportSSID_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpSupportSSID_val_ptr_ptr) || (*mib_wtpSupportSSID_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpSupportSSID_val_ptr_ptr = realloc( *mib_wtpSupportSSID_val_ptr_ptr, converted_len * sizeof(**mib_wtpSupportSSID_val_ptr_ptr));
        if(NULL == *mib_wtpSupportSSID_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpSupportSSID_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpSupportSSID_val_ptr_ptr, raw_wtpSupportSSID_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpSupportSSID_map */

/**
 * Extract the current value of the wtpSupportSSID data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSupportSSID_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpSupportSSID_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpSupportSSID.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpSupportSSID_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpSupportSSID_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpSupportSSID_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, char **wtpSupportSSID_val_ptr_ptr, size_t *wtpSupportSSID_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpSupportSSID_val_ptr_ptr) && (NULL != *wtpSupportSSID_val_ptr_ptr));
   netsnmp_assert( NULL != wtpSupportSSID_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpSupportSSID_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSupportSSID data.
 * set (* wtpSupportSSID_val_ptr_ptr ) and (* wtpSupportSSID_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpSupportSSID data
     */
    if ((NULL == (* wtpSupportSSID_val_ptr_ptr )) ||
        ((* wtpSupportSSID_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpSupportSSID_len * sizeof((* wtpSupportSSID_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpSupportSSID data
         */
        (* wtpSupportSSID_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpSupportSSID_len * sizeof((* wtpSupportSSID_val_ptr_ptr )[0]));
        if(NULL == (* wtpSupportSSID_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpSupportSSID_val_ptr_len_ptr ) = rowreq_ctx->data.wtpSupportSSID_len * sizeof((* wtpSupportSSID_val_ptr_ptr )[0]);
    memcpy( (* wtpSupportSSID_val_ptr_ptr ), rowreq_ctx->data.wtpSupportSSID, (* wtpSupportSSID_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpSupportSSID_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpOnlineUsrNum
 * wtpOnlineUsrNum is subid 6 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.6
 * Description:
The total number of AP online user .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpOnlineUsrNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpOnlineUsrNum_map(long *mib_wtpOnlineUsrNum_val_ptr, long raw_wtpOnlineUsrNum_val)
{
    netsnmp_assert(NULL != mib_wtpOnlineUsrNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpOnlineUsrNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpOnlineUsrNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpOnlineUsrNum_val_ptr) = raw_wtpOnlineUsrNum_val;

    return MFD_SUCCESS;
} /* wtpOnlineUsrNum_map */

/**
 * Extract the current value of the wtpOnlineUsrNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpOnlineUsrNum_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpOnlineUsrNum_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, long * wtpOnlineUsrNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpOnlineUsrNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpOnlineUsrNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpOnlineUsrNum data.
 * set (* wtpOnlineUsrNum_val_ptr ) from rowreq_ctx->data
 */
    (* wtpOnlineUsrNum_val_ptr ) = rowreq_ctx->data.wtpOnlineUsrNum;

    return MFD_SUCCESS;
} /* wtpOnlineUsrNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpUsrRequestConnect
 * wtpUsrRequestConnect is subid 7 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.7
 * Description:
The total times that user request to connnect in.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpUsrRequestConnect.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpUsrRequestConnect_map(u_long *mib_wtpUsrRequestConnect_val_ptr, u_long raw_wtpUsrRequestConnect_val)
{
    netsnmp_assert(NULL != mib_wtpUsrRequestConnect_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpUsrRequestConnect_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpUsrRequestConnect mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpUsrRequestConnect_val_ptr) = raw_wtpUsrRequestConnect_val;

    return MFD_SUCCESS;
} /* wtpUsrRequestConnect_map */

/**
 * Extract the current value of the wtpUsrRequestConnect data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUsrRequestConnect_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpUsrRequestConnect_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpUsrRequestConnect_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpUsrRequestConnect_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpUsrRequestConnect_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpUsrRequestConnect data.
 * set (* wtpUsrRequestConnect_val_ptr ) from rowreq_ctx->data
 */
    (* wtpUsrRequestConnect_val_ptr ) = rowreq_ctx->data.wtpUsrRequestConnect;

    return MFD_SUCCESS;
} /* wtpUsrRequestConnect_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpResponseUsrConnect
 * wtpResponseUsrConnect is subid 8 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.8
 * Description:
The total times that AP response user connection.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpResponseUsrConnect.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpResponseUsrConnect_map(u_long *mib_wtpResponseUsrConnect_val_ptr, u_long raw_wtpResponseUsrConnect_val)
{
    netsnmp_assert(NULL != mib_wtpResponseUsrConnect_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpResponseUsrConnect_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpResponseUsrConnect mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpResponseUsrConnect_val_ptr) = raw_wtpResponseUsrConnect_val;

    return MFD_SUCCESS;
} /* wtpResponseUsrConnect_map */

/**
 * Extract the current value of the wtpResponseUsrConnect data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpResponseUsrConnect_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpResponseUsrConnect_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpResponseUsrConnect_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpResponseUsrConnect_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpResponseUsrConnect_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpResponseUsrConnect data.
 * set (* wtpResponseUsrConnect_val_ptr ) from rowreq_ctx->data
 */
    (* wtpResponseUsrConnect_val_ptr ) = rowreq_ctx->data.wtpResponseUsrConnect;

    return MFD_SUCCESS;
} /* wtpResponseUsrConnect_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpLoginSuccNum
 * wtpLoginSuccNum is subid 9 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.9
 * Description:
The total times that user login successfully.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpLoginSuccNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpLoginSuccNum_map(u_long *mib_wtpLoginSuccNum_val_ptr, u_long raw_wtpLoginSuccNum_val)
{
    netsnmp_assert(NULL != mib_wtpLoginSuccNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpLoginSuccNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpLoginSuccNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpLoginSuccNum_val_ptr) = raw_wtpLoginSuccNum_val;

    return MFD_SUCCESS;
} /* wtpLoginSuccNum_map */

/**
 * Extract the current value of the wtpLoginSuccNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpLoginSuccNum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpLoginSuccNum_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpLoginSuccNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpLoginSuccNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpLoginSuccNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpLoginSuccNum data.
 * set (* wtpLoginSuccNum_val_ptr ) from rowreq_ctx->data
 */
    (* wtpLoginSuccNum_val_ptr ) = rowreq_ctx->data.wtpLoginSuccNum;

    return MFD_SUCCESS;
} /* wtpLoginSuccNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpLessResourceRefuseNewUsrMount
 * wtpLessResourceRefuseNewUsrMount is subid 10 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.10
 * Description:
The total numbers of users who be refused to register because of less resources.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpLessResourceRefuseNewUsrMount.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpLessResourceRefuseNewUsrMount_map(u_long *mib_wtpLessResourceRefuseNewUsrMount_val_ptr, u_long raw_wtpLessResourceRefuseNewUsrMount_val)
{
    netsnmp_assert(NULL != mib_wtpLessResourceRefuseNewUsrMount_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpLessResourceRefuseNewUsrMount_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpLessResourceRefuseNewUsrMount mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpLessResourceRefuseNewUsrMount_val_ptr) = raw_wtpLessResourceRefuseNewUsrMount_val;

    return MFD_SUCCESS;
} /* wtpLessResourceRefuseNewUsrMount_map */

/**
 * Extract the current value of the wtpLessResourceRefuseNewUsrMount data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpLessResourceRefuseNewUsrMount_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpLessResourceRefuseNewUsrMount_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpLessResourceRefuseNewUsrMount_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpLessResourceRefuseNewUsrMount_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpLessResourceRefuseNewUsrMount_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpLessResourceRefuseNewUsrMount data.
 * set (* wtpLessResourceRefuseNewUsrMount_val_ptr ) from rowreq_ctx->data
 */
    (* wtpLessResourceRefuseNewUsrMount_val_ptr ) = rowreq_ctx->data.wtpLessResourceRefuseNewUsrMount;

    return MFD_SUCCESS;
} /* wtpLessResourceRefuseNewUsrMount_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpStatsDisassociated
 * wtpStatsDisassociated is subid 11 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.11
 * Description:
The total times that the system disconnect because of terminal exception.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStatsDisassociated.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpStatsDisassociated_map(u_long *mib_wtpStatsDisassociated_val_ptr, u_long raw_wtpStatsDisassociated_val)
{
    netsnmp_assert(NULL != mib_wtpStatsDisassociated_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpStatsDisassociated_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpStatsDisassociated mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpStatsDisassociated_val_ptr) = raw_wtpStatsDisassociated_val;

    return MFD_SUCCESS;
} /* wtpStatsDisassociated_map */

/**
 * Extract the current value of the wtpStatsDisassociated data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpStatsDisassociated_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpStatsDisassociated_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpStatsDisassociated_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpStatsDisassociated_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpStatsDisassociated_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpStatsDisassociated data.
 * set (* wtpStatsDisassociated_val_ptr ) from rowreq_ctx->data
 */
    (* wtpStatsDisassociated_val_ptr ) = rowreq_ctx->data.wtpStatsDisassociated;

    return MFD_SUCCESS;
} /* wtpStatsDisassociated_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpUsrWirelessResoUseRate
 * wtpUsrWirelessResoUseRate is subid 12 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.12
 * Description:
The available rate of resources in the user side.(Wireless side)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpUsrWirelessResoUseRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpUsrWirelessResoUseRate_map(char **mib_wtpUsrWirelessResoUseRate_val_ptr_ptr, size_t *mib_wtpUsrWirelessResoUseRate_val_ptr_len_ptr, char *raw_wtpUsrWirelessResoUseRate_val_ptr, size_t raw_wtpUsrWirelessResoUseRate_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpUsrWirelessResoUseRate_val_ptr);
    netsnmp_assert((NULL != mib_wtpUsrWirelessResoUseRate_val_ptr_ptr) && (NULL != mib_wtpUsrWirelessResoUseRate_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpUsrWirelessResoUseRate_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpUsrWirelessResoUseRate non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpUsrWirelessResoUseRate_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpUsrWirelessResoUseRate_val_ptr_ptr) || (*mib_wtpUsrWirelessResoUseRate_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpUsrWirelessResoUseRate_val_ptr_ptr = realloc( *mib_wtpUsrWirelessResoUseRate_val_ptr_ptr, converted_len * sizeof(**mib_wtpUsrWirelessResoUseRate_val_ptr_ptr));
        if(NULL == *mib_wtpUsrWirelessResoUseRate_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpUsrWirelessResoUseRate_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpUsrWirelessResoUseRate_val_ptr_ptr, raw_wtpUsrWirelessResoUseRate_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpUsrWirelessResoUseRate_map */

/**
 * Extract the current value of the wtpUsrWirelessResoUseRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUsrWirelessResoUseRate_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpUsrWirelessResoUseRate_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpUsrWirelessResoUseRate.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpUsrWirelessResoUseRate_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpUsrWirelessResoUseRate_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpUsrWirelessResoUseRate_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, char **wtpUsrWirelessResoUseRate_val_ptr_ptr, size_t *wtpUsrWirelessResoUseRate_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpUsrWirelessResoUseRate_val_ptr_ptr) && (NULL != *wtpUsrWirelessResoUseRate_val_ptr_ptr));
   netsnmp_assert( NULL != wtpUsrWirelessResoUseRate_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpUsrWirelessResoUseRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpUsrWirelessResoUseRate data.
 * set (* wtpUsrWirelessResoUseRate_val_ptr_ptr ) and (* wtpUsrWirelessResoUseRate_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpUsrWirelessResoUseRate data
     */
    if ((NULL == (* wtpUsrWirelessResoUseRate_val_ptr_ptr )) ||
        ((* wtpUsrWirelessResoUseRate_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpUsrWirelessResoUseRate_len * sizeof((* wtpUsrWirelessResoUseRate_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpUsrWirelessResoUseRate data
         */
        (* wtpUsrWirelessResoUseRate_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpUsrWirelessResoUseRate_len * sizeof((* wtpUsrWirelessResoUseRate_val_ptr_ptr )[0]));
        if(NULL == (* wtpUsrWirelessResoUseRate_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpUsrWirelessResoUseRate_val_ptr_len_ptr ) = rowreq_ctx->data.wtpUsrWirelessResoUseRate_len * sizeof((* wtpUsrWirelessResoUseRate_val_ptr_ptr )[0]);
    memcpy( (* wtpUsrWirelessResoUseRate_val_ptr_ptr ), rowreq_ctx->data.wtpUsrWirelessResoUseRate, (* wtpUsrWirelessResoUseRate_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpUsrWirelessResoUseRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpSSIDNum
 * wtpSSIDNum is subid 13 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.13
 * Description:
The total numbers of SSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSSIDNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSSIDNum_map(long *mib_wtpSSIDNum_val_ptr, long raw_wtpSSIDNum_val)
{
    netsnmp_assert(NULL != mib_wtpSSIDNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpSSIDNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSSIDNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSSIDNum_val_ptr) = raw_wtpSSIDNum_val;

    return MFD_SUCCESS;
} /* wtpSSIDNum_map */

/**
 * Extract the current value of the wtpSSIDNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSSIDNum_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSSIDNum_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, long * wtpSSIDNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSSIDNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpSSIDNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSSIDNum data.
 * set (* wtpSSIDNum_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSSIDNum_val_ptr ) = rowreq_ctx->data.wtpSSIDNum;

    return MFD_SUCCESS;
} /* wtpSSIDNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpAllStaAssTime
 * wtpAllStaAssTime is subid 14 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.14
 * Description:
The total time that all the AP connectted.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpAllStaAssTime.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpAllStaAssTime_map(u_long *mib_wtpAllStaAssTime_val_ptr, u_long raw_wtpAllStaAssTime_val)
{
    netsnmp_assert(NULL != mib_wtpAllStaAssTime_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpAllStaAssTime_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpAllStaAssTime mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpAllStaAssTime_val_ptr) = raw_wtpAllStaAssTime_val;

    return MFD_SUCCESS;
} /* wtpAllStaAssTime_map */

/**
 * Extract the current value of the wtpAllStaAssTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAllStaAssTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpAllStaAssTime_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpAllStaAssTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAllStaAssTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpAllStaAssTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpAllStaAssTime data.
 * set (* wtpAllStaAssTime_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAllStaAssTime_val_ptr ) = rowreq_ctx->data.wtpAllStaAssTime;

    return MFD_SUCCESS;
} /* wtpAllStaAssTime_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpConfigBSSIDNum
 * wtpConfigBSSIDNum is subid 15 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.4.1.15
 * Description:
The total numbers of configed BSSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpConfigBSSIDNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpConfigBSSIDNum_map(long *mib_wtpConfigBSSIDNum_val_ptr, long raw_wtpConfigBSSIDNum_val)
{
    netsnmp_assert(NULL != mib_wtpConfigBSSIDNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpConfigBSSIDNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpConfigBSSIDNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpConfigBSSIDNum_val_ptr) = raw_wtpConfigBSSIDNum_val;

    return MFD_SUCCESS;
} /* wtpConfigBSSIDNum_map */

/**
 * Extract the current value of the wtpConfigBSSIDNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpConfigBSSIDNum_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpConfigBSSIDNum_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, long * wtpConfigBSSIDNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpConfigBSSIDNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpConfigBSSIDNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpConfigBSSIDNum data.
 * set (* wtpConfigBSSIDNum_val_ptr ) from rowreq_ctx->data
 */
    (* wtpConfigBSSIDNum_val_ptr ) = rowreq_ctx->data.wtpConfigBSSIDNum;

    return MFD_SUCCESS;
} /* wtpConfigBSSIDNum_get */

int
wtpInterfaceNum_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, long * wtpInterfaceNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpInterfaceNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpInterfaceNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpInterfaceNum data.
 * set (* wtpInterfaceNum_val_ptr ) from rowreq_ctx->data
 */
    (* wtpInterfaceNum_val_ptr ) = rowreq_ctx->data.wtpInterfaceNum;

    return MFD_SUCCESS;
} /* wtpConfigBSSIDNum_get */


int
wtpAssocSuccessTimes_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpAssocSuccessTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAssocSuccessTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpAssocSuccessTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMountConUsrTimes data.
 * set (* wtpAssocSuccessTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAssocSuccessTimes_val_ptr ) = rowreq_ctx->data.wtpAssocSuccessTimes;

    return MFD_SUCCESS;
} /* wtpAssocSuccessTimes_get */

int
wtpRSSILowAssocFailTimes_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpRSSILowAssocFailTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpRSSILowAssocFailTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpRSSILowAssocFailTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpInterfaceNum data.
 * set (* wtpRSSILowAssocFailTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpRSSILowAssocFailTimes_val_ptr ) = rowreq_ctx->data.wtpRSSILowAssocFailTimes;

    return MFD_SUCCESS;
} /* wtpRSSILowAssocFailTimes_get */

int
wtpHeartBeatAvgTimeDelay_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpHeartBeatAvgTimeDelay_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpHeartBeatAvgTimeDelay_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpHeartBeatAvgTimeDelay_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpHeartBeatAvgTimeDelay data.
 * set (* wtpHeartBeatAvgTimeDelay_val_ptr ) from rowreq_ctx->data
 */
    (* wtpHeartBeatAvgTimeDelay_val_ptr ) = rowreq_ctx->data.wtpHeartBeatAvgTimeDelay;

    return MFD_SUCCESS;
} /* wtpHeartBeatAvgTimeDelay_get */

int
wtpEchoReqCnt_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpEchoReqCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpEchoReqCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpEchoReqCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpEchoReqCnt data.
 * set (* wtpEchoReqCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wtpEchoReqCnt_val_ptr ) = rowreq_ctx->data.wtpEchoReqCnt;

    return MFD_SUCCESS;
} /* wtpEchoReqCnt_get */

int
wtpEchoRespLossCnt_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpEchoRespLossCnt_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpEchoRespLossCnt_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpEchoRespLossCnt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpEchoRespLossCnt data.
 * set (* wtpEchoRespLossCnt_val_ptr ) from rowreq_ctx->data
 */
    (* wtpEchoRespLossCnt_val_ptr ) = rowreq_ctx->data.wtpEchoRespLossCnt;

    return MFD_SUCCESS;
} /* wtpEchoRespLossCnt_get */

int
wtpResponseUsrConnectSuccess_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpResponseUsrConnectSuccess_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpResponseUsrConnectSuccess_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpResponseUsrConnectSuccess_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpResponseUsrConnectSuccess data.
 * set (* wtpResponseUsrConnectSuccess_val_ptr ) from rowreq_ctx->data
 */
    (* wtpResponseUsrConnectSuccess_val_ptr ) = rowreq_ctx->data.wtpResponseUsrConnectSuccess;

    return MFD_SUCCESS;
} /* wtpResponseUsrConnectSuccess_get */

int
wtpResponseUsrConnectFail_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpResponseUsrConnectFail_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpResponseUsrConnectFail_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpResponseUsrConnectFail_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpResponseUsrConnectFail data.
 * set (* wtpResponseUsrConnectFail_val_ptr ) from rowreq_ctx->data
 */
    (* wtpResponseUsrConnectFail_val_ptr ) = rowreq_ctx->data.wtpResponseUsrConnectFail;

    return MFD_SUCCESS;
} /* wtpResponseUsrConnectFail_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpStatsEntry.wtpMountConReasFailTimes
 * wtpMountConReasFailTimes is subid 26 of dot11WtpStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.4.1.26
 * Description:
The total failed number of current reassociate(802.11bg).(Times)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the wtpMountConReasFailTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMountConReasFailTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */


int
wtpMountConReasFailTimes_map( u_long *mib_wtpMountConReasFailTimes_val_ptr, u_long raw_wtpMountConReasFailTimes_val )
{
    netsnmp_assert(NULL != mib_wtpMountConReasFailTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpMountConReasFailTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMountConFailTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMountConReasFailTimes_val_ptr) = raw_wtpMountConReasFailTimes_val;

    return MFD_SUCCESS;
} /* wtpMountConFailTimes_map */

int
wtpMountConReasFailTimes_get( dot11WtpStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpMountConReasFailTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMountConReasFailTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpStatsTable:wtpMountConReasFailTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMountConReasFailTimes data.
 * copy (* wtpMountConReasFailTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMountConReasFailTimes_val_ptr ) = rowreq_ctx->data.wtpMountConReasFailTimes;

    return MFD_SUCCESS;
} /* wtpMountConReasFailTimes_get */



/** @} */
