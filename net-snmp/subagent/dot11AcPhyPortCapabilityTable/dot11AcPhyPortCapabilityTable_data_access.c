/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 14170 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11AcPhyPortCapabilityTable.h"


#include "dot11AcPhyPortCapabilityTable_data_access.h"
#include "ws_dcli_portconf.h"
#include "ws_dbus_def.h"
#include "../mibs_public.h"

/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11AcPhyPortCapabilityTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11AcPhyPortCapabilityTable is subid 7 of dot11AcInterface.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.7, length: 12
*/

/**
 * initialization for dot11AcPhyPortCapabilityTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11AcPhyPortCapabilityTable_reg
 *        Pointer to dot11AcPhyPortCapabilityTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11AcPhyPortCapabilityTable_init_data(dot11AcPhyPortCapabilityTable_registration * dot11AcPhyPortCapabilityTable_reg)
{
    DEBUGMSGTL(("verbose:dot11AcPhyPortCapabilityTable:dot11AcPhyPortCapabilityTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11AcPhyPortCapabilityTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11AcPhyPortCapabilityTable_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11AcPhyPortCapabilityTable_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11AcPhyPortCapabilityTable:dot11AcPhyPortCapabilityTable_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to dot11AcPhyPortCapabilityTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to dot11AcPhyPortCapabilityTable_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up dot11AcPhyPortCapabilityTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11ACPHYPORTCAPABILITYTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11AcPhyPortCapabilityTable_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before dot11AcPhyPortCapabilityTable_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
dot11AcPhyPortCapabilityTable_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:dot11AcPhyPortCapabilityTable:dot11AcPhyPortCapabilityTable_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to dot11AcPhyPortCapabilityTable_container_shutdown\n");
        return;
    }

} /* dot11AcPhyPortCapabilityTable_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement dot11AcPhyPortCapabilityTable data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11AcPhyPortCapabilityTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11AcPhyPortCapabilityTable_container_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11AcPhyPortCapabilityTable_container_load\n");

    dot11AcPhyPortCapabilityTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * PhyPortIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   PhyPortIndex;

    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];

    DEBUGMSGTL(("verbose:dot11AcPhyPortCapabilityTable:dot11AcPhyPortCapabilityTable_container_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
        //return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11AcPhyPortCapabilityTable container.
     * loop over your dot11AcPhyPortCapabilityTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    int ret = -1;
	ETH_SLOT_LIST  head,*p;
  	ETH_PORT_LIST *pp;
	int slot_num = 0;
	unsigned char type = 0;
	unsigned int value = 0;
	struct eth_port_counter_s EthStat;
	DBusConnection *connection = NULL;
	unsigned long long acPhyPortInUcastPkts = 0;
	unsigned long long acPhyPortInNUcastPkts = 0;
	unsigned long long acPhyPortInOctets = 0;
	unsigned long long acPhyPortInDiscardPkts = 0;
	unsigned long long acPhyPortInErrors = 0;
	unsigned long long acPhyPortOutUcastPkts = 0;
	unsigned long long acPhyPortOutNUcastPkts = 0;
	unsigned long long acPhyPortOutOctets = 0;
	unsigned long long acPhyPortOutDiscardPkts = 0;
	unsigned long long acPhyPortOutErrors = 0;
	unsigned long long acPhyPortInMulticastPkts = 0;
	unsigned long long acPhyPortOutMulticastPkts = 0;
	
	PhyPortIndex = 1;
	snmp_log(LOG_DEBUG, "enter show_ethport_list\n");
	ret = show_ethport_list(&head,&slot_num);
	snmp_log(LOG_DEBUG, "exit show_ethport_list,ret=%d\n", ret);
	if(0 == ret)
	{		
		for(p=head.next; (NULL != p); p=p->next)
		{
			get_slot_dbus_connection(p->slot_no, &connection, SNMPD_INSTANCE_MASTER_V3);
			
			for(pp=p->port.next; (NULL != pp); pp=pp->next)
		    {
				type = 0;
				value = 0;
				value = ((((p->slot_no) & 0x1f) - 1)<<6) + ((pp->port_no) - 1);

				acPhyPortInUcastPkts = 0;
				acPhyPortInNUcastPkts = 0;
				acPhyPortInOctets = 0;
				acPhyPortInDiscardPkts = 0;
				acPhyPortInErrors = 0;
				acPhyPortOutUcastPkts = 0;
				acPhyPortOutNUcastPkts = 0;
				acPhyPortOutOctets = 0;
				acPhyPortOutDiscardPkts = 0;
				acPhyPortOutErrors = 0;
				acPhyPortInMulticastPkts = 0;
				acPhyPortOutMulticastPkts = 0;
				if(show_eth_port_stat(&EthStat,connection,value,type)==CCGI_SUCCESS)
				{
					acPhyPortInUcastPkts = EthStat.rx.uncastframes;
					acPhyPortInNUcastPkts = EthStat.rx.bcastframes + EthStat.rx.mcastframes;

					acPhyPortInOctets = EthStat.rx.goodbytesh;
		    		acPhyPortInOctets = (acPhyPortInOctets<<32) | EthStat.rx.goodbytesl;
					
					acPhyPortInDiscardPkts = EthStat.rx.fifooverruns + EthStat.rx.underSizepkt + EthStat.rx.overSizepkt + EthStat.rx.errorframe + EthStat.otc.badCRC;
					acPhyPortInErrors = EthStat.rx.underSizepkt + EthStat.rx.overSizepkt + EthStat.rx.errorframe + EthStat.otc.badCRC;
					
					acPhyPortOutUcastPkts = EthStat.tx.uncastframe;
					acPhyPortOutNUcastPkts = EthStat.tx.bcastframe + EthStat.tx.mcastframe;

					acPhyPortOutOctets = EthStat.tx.goodbytesh;
		    		acPhyPortOutOctets = (acPhyPortOutOctets<<32) | EthStat.tx.goodbytesl;

					acPhyPortOutDiscardPkts = EthStat.tx.crcerror_fifooverrun;
					acPhyPortOutErrors = EthStat.tx.crcerror_fifooverrun;
					acPhyPortInMulticastPkts = EthStat.rx.mcastframes;
					acPhyPortOutMulticastPkts = EthStat.tx.mcastframe;
				}
		    /*
		    ***************************************************
		    ***             START EXAMPLE CODE              ***
		    ***---------------------------------------------***/
		    /*
		     * get a line (skip blank lines)
		     */
		    //do {
		      //  if (!fgets(line, sizeof(line), filep)) {
		            /* we're done */
		        //    fclose(filep);
		          //  filep = NULL;
		        //}
		    //} while (filep && (line[0] == '\n'));

		    /*
		     * check for end of data
		     */
		    //if(NULL == filep)
		      //  break;

		    /*
		     * parse line into variables
		     */
		    /*
		    ***---------------------------------------------***
		    ***              END  EXAMPLE CODE              ***
		    ***************************************************/

		        /*
		         * TODO:352:M: |   |-> set indexes in new dot11AcPhyPortCapabilityTable rowreq context.
		         * data context will be set from the param (unless NULL,
		         *      in which case a new data context will be allocated)
		         */
		        rowreq_ctx = dot11AcPhyPortCapabilityTable_allocate_rowreq_ctx(NULL);
		        if (NULL == rowreq_ctx) {
		            snmp_log(LOG_ERR, "memory allocation failed\n");
					if(slot_num>0)
					{
						Free_ethslot_head(&head);
					}
		            return MFD_RESOURCE_UNAVAILABLE;
		        }
		        if(MFD_SUCCESS != dot11AcPhyPortCapabilityTable_indexes_set(rowreq_ctx
		                               , PhyPortIndex
		               )) {
		            snmp_log(LOG_ERR,"error setting index while loading "
		                     "dot11AcPhyPortCapabilityTable data.\n");
		            dot11AcPhyPortCapabilityTable_release_rowreq_ctx(rowreq_ctx);
		            continue;
		        }

		        /*
		         * TODO:352:r: |   |-> populate dot11AcPhyPortCapabilityTable data context.
		         * Populate data context here. (optionally, delay until row prep)
		         */
		    /*
		     * TRANSIENT or semi-TRANSIENT data:
		     * copy data or save any info needed to do it in row_prep.
		     */
		    /*
		     * setup/save data for acPhyPortInUcastPkts
		     * acPhyPortInUcastPkts(1)/COUNTER64/ASN_COUNTER64/U64(U64)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortInUcastPkts = acPhyPortInUcastPkts;
		    
		    /*
		     * setup/save data for acPhyPortInNUcastPkts
		     * acPhyPortInNUcastPkts(2)/COUNTER64/ASN_COUNTER64/U64(U64)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortInNUcastPkts = acPhyPortInNUcastPkts;
		    
		    /*
		     * setup/save data for acPhyPortInOctets
		     * acPhyPortInOctets(3)/COUNTER64/ASN_COUNTER64/U64(U64)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortInOctets = acPhyPortInOctets;
		    
		    /*
		     * setup/save data for acPhyPortInDiscardPkts
		     * acPhyPortInDiscardPkts(4)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortInDiscardPkts = acPhyPortInDiscardPkts;
		    
		    /*
		     * setup/save data for acPhyPortInErrors
		     * acPhyPortInErrors(5)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortInErrors = acPhyPortInErrors;
		    
		    /*
		     * setup/save data for acPhyPortOutUcastPkts
		     * acPhyPortOutUcastPkts(6)/COUNTER64/ASN_COUNTER64/U64(U64)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortOutUcastPkts = acPhyPortOutUcastPkts;
		    
		    /*
		     * setup/save data for acPhyPortOutNUcastPkts
		     * acPhyPortOutNUcastPkts(7)/COUNTER64/ASN_COUNTER64/U64(U64)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortOutNUcastPkts = acPhyPortOutNUcastPkts;
		    
		    /*
		     * setup/save data for acPhyPortOutOctets
		     * acPhyPortOutOctets(8)/COUNTER64/ASN_COUNTER64/U64(U64)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortOutOctets = acPhyPortOutOctets;
		    
		    /*
		     * setup/save data for acPhyPortOutDiscardPkts
		     * acPhyPortOutDiscardPkts(9)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortOutDiscardPkts = acPhyPortOutDiscardPkts;
		    
		    /*
		     * setup/save data for acPhyPortOutErrors
		     * acPhyPortOutErrors(10)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortOutErrors = acPhyPortOutErrors;
		    
		    /*
		     * setup/save data for acPhyPortInMulticastPkts
		     * acPhyPortInMulticastPkts(11)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortInMulticastPkts = acPhyPortInMulticastPkts;
		    
		    /*
		     * setup/save data for acPhyPortOutMulticastPkts
		     * acPhyPortOutMulticastPkts(12)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.acPhyPortOutMulticastPkts = acPhyPortOutMulticastPkts;
		    
		        
		        /*
		         * insert into table container
		         */
		        if(CONTAINER_INSERT(container, rowreq_ctx))
		        {
					dot11AcPhyPortCapabilityTable_release_rowreq_ctx(rowreq_ctx);
		        }
		        ++count;
				PhyPortIndex++;
		    }
		}
		if(slot_num>0)
		{
			Free_ethslot_head(&head);
		}
	}
	


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
      //  fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11AcPhyPortCapabilityTable:dot11AcPhyPortCapabilityTable_container_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11AcPhyPortCapabilityTable_container_load\n");
    return MFD_SUCCESS;
} /* dot11AcPhyPortCapabilityTable_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11AcPhyPortCapabilityTable_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11AcPhyPortCapabilityTable:dot11AcPhyPortCapabilityTable_container_free","called\n"));

    /*
     * TODO:380:M: Free dot11AcPhyPortCapabilityTable container data.
     */
} /* dot11AcPhyPortCapabilityTable_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11AcPhyPortCapabilityTable_row_prep( dot11AcPhyPortCapabilityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11AcPhyPortCapabilityTable:dot11AcPhyPortCapabilityTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11AcPhyPortCapabilityTable_row_prep */

/** @} */
