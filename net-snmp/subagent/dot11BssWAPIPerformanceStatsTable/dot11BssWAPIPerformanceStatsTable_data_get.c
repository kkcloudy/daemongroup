/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11BssWAPIPerformanceStatsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11BssWAPIPerformanceStatsTable get routines.
 * TODO:240:M: Implement dot11BssWAPIPerformanceStatsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11BssWAPIPerformanceStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11BssWAPIPerformanceStatsTable is subid 5 of wtpWAPI.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11BssWAPIPerformanceStatsTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11BssWAPIPerformanceStatsTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssEntry.wtpBssCurrID
 * wtpBssCurrID is subid 1 of dot11WtpBssEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.7.2.1.1
 * Description:
The current AP BSSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpBssCurrID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpBssCurrID_map(char **mib_wtpBssCurrID_val_ptr_ptr, size_t *mib_wtpBssCurrID_val_ptr_len_ptr, char *raw_wtpBssCurrID_val_ptr, size_t raw_wtpBssCurrID_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpBssCurrID_val_ptr);
    netsnmp_assert((NULL != mib_wtpBssCurrID_val_ptr_ptr) && (NULL != mib_wtpBssCurrID_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:wtpBssCurrID_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpBssCurrID non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpBssCurrID_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpBssCurrID_val_ptr_ptr) || (*mib_wtpBssCurrID_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpBssCurrID_val_ptr_ptr = realloc( *mib_wtpBssCurrID_val_ptr_ptr, converted_len * sizeof(**mib_wtpBssCurrID_val_ptr_ptr));
        if(NULL == *mib_wtpBssCurrID_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpBssCurrID_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpBssCurrID_val_ptr_ptr, raw_wtpBssCurrID_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpBssCurrID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11BssWAPIPerformanceStatsTable_indexes_set_tbl_idx(dot11BssWAPIPerformanceStatsTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, char *wtpBssCurrID_val_ptr,  size_t wtpBssCurrID_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:dot11BssWAPIPerformanceStatsTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wtpBssCurrID(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpBssCurrID_len = sizeof(tbl_idx->wtpBssCurrID);
    /*
     * make sure there is enough space for wtpBssCurrID data
     */
    if ((NULL == tbl_idx->wtpBssCurrID) ||
        (tbl_idx->wtpBssCurrID_len < (wtpBssCurrID_val_ptr_len * sizeof(tbl_idx->wtpBssCurrID[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpBssCurrID_len = wtpBssCurrID_val_ptr_len * sizeof(tbl_idx->wtpBssCurrID[0]);
    memcpy( tbl_idx->wtpBssCurrID, wtpBssCurrID_val_ptr, tbl_idx->wtpBssCurrID_len );
    

    return MFD_SUCCESS;
} /* dot11BssWAPIPerformanceStatsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11BssWAPIPerformanceStatsTable_indexes_set(dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, char *wtpBssCurrID_val_ptr,  size_t wtpBssCurrID_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:dot11BssWAPIPerformanceStatsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11BssWAPIPerformanceStatsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpBssCurrID_val_ptr, wtpBssCurrID_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11BssWAPIPerformanceStatsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11BssWAPIPerformanceStatsTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiControlledPortStatus
 * bssWapiControlledPortStatus is subid 1 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.1
 * Description:
Authentication controled port status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiControlledPortStatus.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiControlledPortStatus_map(u_long *mib_bssWapiControlledPortStatus_val_ptr, u_long raw_bssWapiControlledPortStatus_val)
{
    netsnmp_assert(NULL != mib_bssWapiControlledPortStatus_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiControlledPortStatus_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiControlledPortStatus enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_bssWapiControlledPortStatus_val) {
        case INTERNAL_BSSWAPICONTROLLEDPORTSTATUS_TRUE:
             *mib_bssWapiControlledPortStatus_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_BSSWAPICONTROLLEDPORTSTATUS_FALSE:
             *mib_bssWapiControlledPortStatus_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for bssWapiControlledPortStatus\n", raw_bssWapiControlledPortStatus_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* bssWapiControlledPortStatus_map */

/**
 * Extract the current value of the bssWapiControlledPortStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiControlledPortStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiControlledPortStatus_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiControlledPortStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiControlledPortStatus_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiControlledPortStatus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiControlledPortStatus data.
 * set (* bssWapiControlledPortStatus_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiControlledPortStatus_val_ptr ) = rowreq_ctx->data.bssWapiControlledPortStatus;

    return MFD_SUCCESS;
} /* bssWapiControlledPortStatus_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiSelectedUnicastCipher
 * bssWapiSelectedUnicastCipher is subid 2 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.2
 * Description:
Selected unicast encryption cipher.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiSelectedUnicastCipher.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiSelectedUnicastCipher_map(char **mib_bssWapiSelectedUnicastCipher_val_ptr_ptr, size_t *mib_bssWapiSelectedUnicastCipher_val_ptr_len_ptr, char *raw_bssWapiSelectedUnicastCipher_val_ptr, size_t raw_bssWapiSelectedUnicastCipher_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_bssWapiSelectedUnicastCipher_val_ptr);
    netsnmp_assert((NULL != mib_bssWapiSelectedUnicastCipher_val_ptr_ptr) && (NULL != mib_bssWapiSelectedUnicastCipher_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiSelectedUnicastCipher_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement bssWapiSelectedUnicastCipher non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_bssWapiSelectedUnicastCipher_val_ptr_len; /* assume equal */
    if((NULL == *mib_bssWapiSelectedUnicastCipher_val_ptr_ptr) || (*mib_bssWapiSelectedUnicastCipher_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_bssWapiSelectedUnicastCipher_val_ptr_ptr = realloc( *mib_bssWapiSelectedUnicastCipher_val_ptr_ptr, converted_len * sizeof(**mib_bssWapiSelectedUnicastCipher_val_ptr_ptr));
        if(NULL == *mib_bssWapiSelectedUnicastCipher_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_bssWapiSelectedUnicastCipher_val_ptr_len_ptr = converted_len;
    memcpy( *mib_bssWapiSelectedUnicastCipher_val_ptr_ptr, raw_bssWapiSelectedUnicastCipher_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* bssWapiSelectedUnicastCipher_map */

/**
 * Extract the current value of the bssWapiSelectedUnicastCipher data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiSelectedUnicastCipher_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param bssWapiSelectedUnicastCipher_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by bssWapiSelectedUnicastCipher.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*bssWapiSelectedUnicastCipher_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update bssWapiSelectedUnicastCipher_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
bssWapiSelectedUnicastCipher_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, char **bssWapiSelectedUnicastCipher_val_ptr_ptr, size_t *bssWapiSelectedUnicastCipher_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != bssWapiSelectedUnicastCipher_val_ptr_ptr) && (NULL != *bssWapiSelectedUnicastCipher_val_ptr_ptr));
   netsnmp_assert( NULL != bssWapiSelectedUnicastCipher_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiSelectedUnicastCipher_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiSelectedUnicastCipher data.
 * set (* bssWapiSelectedUnicastCipher_val_ptr_ptr ) and (* bssWapiSelectedUnicastCipher_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for bssWapiSelectedUnicastCipher data
     */
    if ((NULL == (* bssWapiSelectedUnicastCipher_val_ptr_ptr )) ||
        ((* bssWapiSelectedUnicastCipher_val_ptr_len_ptr ) < (rowreq_ctx->data.bssWapiSelectedUnicastCipher_len * sizeof((* bssWapiSelectedUnicastCipher_val_ptr_ptr )[0])))) {
        /*
         * allocate space for bssWapiSelectedUnicastCipher data
         */
        (* bssWapiSelectedUnicastCipher_val_ptr_ptr ) = malloc(rowreq_ctx->data.bssWapiSelectedUnicastCipher_len * sizeof((* bssWapiSelectedUnicastCipher_val_ptr_ptr )[0]));
        if(NULL == (* bssWapiSelectedUnicastCipher_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* bssWapiSelectedUnicastCipher_val_ptr_len_ptr ) = rowreq_ctx->data.bssWapiSelectedUnicastCipher_len * sizeof((* bssWapiSelectedUnicastCipher_val_ptr_ptr )[0]);
    memcpy( (* bssWapiSelectedUnicastCipher_val_ptr_ptr ), rowreq_ctx->data.bssWapiSelectedUnicastCipher, (* bssWapiSelectedUnicastCipher_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* bssWapiSelectedUnicastCipher_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWPIReplayCounters
 * bssWapiWPIReplayCounters is subid 3 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.3
 * Description:
The amount of WPI MPDU which droped because of response response mechanism.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWPIReplayCounters.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWPIReplayCounters_map(u_long *mib_bssWapiWPIReplayCounters_val_ptr, u_long raw_bssWapiWPIReplayCounters_val)
{
    netsnmp_assert(NULL != mib_bssWapiWPIReplayCounters_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWPIReplayCounters_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWPIReplayCounters mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWPIReplayCounters_val_ptr) = raw_bssWapiWPIReplayCounters_val;

    return MFD_SUCCESS;
} /* bssWapiWPIReplayCounters_map */

/**
 * Extract the current value of the bssWapiWPIReplayCounters data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWPIReplayCounters_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWPIReplayCounters_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWPIReplayCounters_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWPIReplayCounters_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWPIReplayCounters_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWPIReplayCounters data.
 * set (* bssWapiWPIReplayCounters_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWPIReplayCounters_val_ptr ) = rowreq_ctx->data.bssWapiWPIReplayCounters;

    return MFD_SUCCESS;
} /* bssWapiWPIReplayCounters_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWPIDecryptableErrors
 * bssWapiWPIDecryptableErrors is subid 4 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.4
 * Description:
The amount of WPI MPDU which droped because of invalid key.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWPIDecryptableErrors.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWPIDecryptableErrors_map(u_long *mib_bssWapiWPIDecryptableErrors_val_ptr, u_long raw_bssWapiWPIDecryptableErrors_val)
{
    netsnmp_assert(NULL != mib_bssWapiWPIDecryptableErrors_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWPIDecryptableErrors_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWPIDecryptableErrors mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWPIDecryptableErrors_val_ptr) = raw_bssWapiWPIDecryptableErrors_val;

    return MFD_SUCCESS;
} /* bssWapiWPIDecryptableErrors_map */

/**
 * Extract the current value of the bssWapiWPIDecryptableErrors data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWPIDecryptableErrors_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWPIDecryptableErrors_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWPIDecryptableErrors_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWPIDecryptableErrors_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWPIDecryptableErrors_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWPIDecryptableErrors data.
 * set (* bssWapiWPIDecryptableErrors_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWPIDecryptableErrors_val_ptr ) = rowreq_ctx->data.bssWapiWPIDecryptableErrors;

    return MFD_SUCCESS;
} /* bssWapiWPIDecryptableErrors_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWPIMICErrors
 * bssWapiWPIMICErrors is subid 5 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.5
 * Description:
The amount of WPI MPDU which droped because of mic check failure.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWPIMICErrors.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWPIMICErrors_map(u_long *mib_bssWapiWPIMICErrors_val_ptr, u_long raw_bssWapiWPIMICErrors_val)
{
    netsnmp_assert(NULL != mib_bssWapiWPIMICErrors_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWPIMICErrors_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWPIMICErrors mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWPIMICErrors_val_ptr) = raw_bssWapiWPIMICErrors_val;

    return MFD_SUCCESS;
} /* bssWapiWPIMICErrors_map */

/**
 * Extract the current value of the bssWapiWPIMICErrors data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWPIMICErrors_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWPIMICErrors_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWPIMICErrors_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWPIMICErrors_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWPIMICErrors_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWPIMICErrors data.
 * set (* bssWapiWPIMICErrors_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWPIMICErrors_val_ptr ) = rowreq_ctx->data.bssWapiWPIMICErrors;

    return MFD_SUCCESS;
} /* bssWapiWPIMICErrors_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWAISignatureErrors
 * bssWapiWAISignatureErrors is subid 6 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.6
 * Description:
The amount of WAI data packets of signed error.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWAISignatureErrors.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWAISignatureErrors_map(u_long *mib_bssWapiWAISignatureErrors_val_ptr, u_long raw_bssWapiWAISignatureErrors_val)
{
    netsnmp_assert(NULL != mib_bssWapiWAISignatureErrors_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAISignatureErrors_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWAISignatureErrors mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWAISignatureErrors_val_ptr) = raw_bssWapiWAISignatureErrors_val;

    return MFD_SUCCESS;
} /* bssWapiWAISignatureErrors_map */

/**
 * Extract the current value of the bssWapiWAISignatureErrors data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWAISignatureErrors_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWAISignatureErrors_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWAISignatureErrors_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWAISignatureErrors_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAISignatureErrors_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWAISignatureErrors data.
 * set (* bssWapiWAISignatureErrors_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWAISignatureErrors_val_ptr ) = rowreq_ctx->data.bssWapiWAISignatureErrors;

    return MFD_SUCCESS;
} /* bssWapiWAISignatureErrors_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWAIHMACErrors
 * bssWapiWAIHMACErrors is subid 7 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.7
 * Description:
Authentication key WAI validation failure the number of packets.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWAIHMACErrors.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWAIHMACErrors_map(u_long *mib_bssWapiWAIHMACErrors_val_ptr, u_long raw_bssWapiWAIHMACErrors_val)
{
    netsnmp_assert(NULL != mib_bssWapiWAIHMACErrors_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIHMACErrors_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWAIHMACErrors mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWAIHMACErrors_val_ptr) = raw_bssWapiWAIHMACErrors_val;

    return MFD_SUCCESS;
} /* bssWapiWAIHMACErrors_map */

/**
 * Extract the current value of the bssWapiWAIHMACErrors data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWAIHMACErrors_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWAIHMACErrors_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWAIHMACErrors_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWAIHMACErrors_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIHMACErrors_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWAIHMACErrors data.
 * set (* bssWapiWAIHMACErrors_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWAIHMACErrors_val_ptr ) = rowreq_ctx->data.bssWapiWAIHMACErrors;

    return MFD_SUCCESS;
} /* bssWapiWAIHMACErrors_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWAIAuthResultFailures
 * bssWapiWAIAuthResultFailures is subid 8 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.8
 * Description:
The number of WAI failed authentication.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWAIAuthResultFailures.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWAIAuthResultFailures_map(u_long *mib_bssWapiWAIAuthResultFailures_val_ptr, u_long raw_bssWapiWAIAuthResultFailures_val)
{
    netsnmp_assert(NULL != mib_bssWapiWAIAuthResultFailures_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIAuthResultFailures_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWAIAuthResultFailures mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWAIAuthResultFailures_val_ptr) = raw_bssWapiWAIAuthResultFailures_val;

    return MFD_SUCCESS;
} /* bssWapiWAIAuthResultFailures_map */

/**
 * Extract the current value of the bssWapiWAIAuthResultFailures data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWAIAuthResultFailures_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWAIAuthResultFailures_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWAIAuthResultFailures_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWAIAuthResultFailures_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIAuthResultFailures_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWAIAuthResultFailures data.
 * set (* bssWapiWAIAuthResultFailures_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWAIAuthResultFailures_val_ptr ) = rowreq_ctx->data.bssWapiWAIAuthResultFailures;

    return MFD_SUCCESS;
} /* bssWapiWAIAuthResultFailures_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWAIDiscardCounters
 * bssWapiWAIDiscardCounters is subid 9 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.9
 * Description:
The number of WAI packets discarded.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWAIDiscardCounters.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWAIDiscardCounters_map(u_long *mib_bssWapiWAIDiscardCounters_val_ptr, u_long raw_bssWapiWAIDiscardCounters_val)
{
    netsnmp_assert(NULL != mib_bssWapiWAIDiscardCounters_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIDiscardCounters_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWAIDiscardCounters mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWAIDiscardCounters_val_ptr) = raw_bssWapiWAIDiscardCounters_val;

    return MFD_SUCCESS;
} /* bssWapiWAIDiscardCounters_map */

/**
 * Extract the current value of the bssWapiWAIDiscardCounters data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWAIDiscardCounters_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWAIDiscardCounters_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWAIDiscardCounters_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWAIDiscardCounters_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIDiscardCounters_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWAIDiscardCounters data.
 * set (* bssWapiWAIDiscardCounters_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWAIDiscardCounters_val_ptr ) = rowreq_ctx->data.bssWapiWAIDiscardCounters;

    return MFD_SUCCESS;
} /* bssWapiWAIDiscardCounters_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWAITimeoutCounters
 * bssWapiWAITimeoutCounters is subid 10 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.10
 * Description:
The number of WAI overtime packet.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWAITimeoutCounters.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWAITimeoutCounters_map(u_long *mib_bssWapiWAITimeoutCounters_val_ptr, u_long raw_bssWapiWAITimeoutCounters_val)
{
    netsnmp_assert(NULL != mib_bssWapiWAITimeoutCounters_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAITimeoutCounters_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWAITimeoutCounters mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWAITimeoutCounters_val_ptr) = raw_bssWapiWAITimeoutCounters_val;

    return MFD_SUCCESS;
} /* bssWapiWAITimeoutCounters_map */

/**
 * Extract the current value of the bssWapiWAITimeoutCounters data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWAITimeoutCounters_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWAITimeoutCounters_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWAITimeoutCounters_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWAITimeoutCounters_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAITimeoutCounters_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWAITimeoutCounters data.
 * set (* bssWapiWAITimeoutCounters_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWAITimeoutCounters_val_ptr ) = rowreq_ctx->data.bssWapiWAITimeoutCounters;

    return MFD_SUCCESS;
} /* bssWapiWAITimeoutCounters_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWAIFormatErrors
 * bssWapiWAIFormatErrors is subid 11 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.11
 * Description:
WAI malformed packet number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWAIFormatErrors.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWAIFormatErrors_map(u_long *mib_bssWapiWAIFormatErrors_val_ptr, u_long raw_bssWapiWAIFormatErrors_val)
{
    netsnmp_assert(NULL != mib_bssWapiWAIFormatErrors_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIFormatErrors_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWAIFormatErrors mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWAIFormatErrors_val_ptr) = raw_bssWapiWAIFormatErrors_val;

    return MFD_SUCCESS;
} /* bssWapiWAIFormatErrors_map */

/**
 * Extract the current value of the bssWapiWAIFormatErrors data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWAIFormatErrors_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWAIFormatErrors_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWAIFormatErrors_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWAIFormatErrors_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIFormatErrors_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWAIFormatErrors data.
 * set (* bssWapiWAIFormatErrors_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWAIFormatErrors_val_ptr ) = rowreq_ctx->data.bssWapiWAIFormatErrors;

    return MFD_SUCCESS;
} /* bssWapiWAIFormatErrors_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWAICertificateHandshakeFailures
 * bssWapiWAICertificateHandshakeFailures is subid 12 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.12
 * Description:
WAI failed to shake hands with the number of certified.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWAICertificateHandshakeFailures.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWAICertificateHandshakeFailures_map(u_long *mib_bssWapiWAICertificateHandshakeFailures_val_ptr, u_long raw_bssWapiWAICertificateHandshakeFailures_val)
{
    netsnmp_assert(NULL != mib_bssWapiWAICertificateHandshakeFailures_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAICertificateHandshakeFailures_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWAICertificateHandshakeFailures mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWAICertificateHandshakeFailures_val_ptr) = raw_bssWapiWAICertificateHandshakeFailures_val;

    return MFD_SUCCESS;
} /* bssWapiWAICertificateHandshakeFailures_map */

/**
 * Extract the current value of the bssWapiWAICertificateHandshakeFailures data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWAICertificateHandshakeFailures_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWAICertificateHandshakeFailures_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWAICertificateHandshakeFailures_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWAICertificateHandshakeFailures_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAICertificateHandshakeFailures_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWAICertificateHandshakeFailures data.
 * set (* bssWapiWAICertificateHandshakeFailures_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWAICertificateHandshakeFailures_val_ptr ) = rowreq_ctx->data.bssWapiWAICertificateHandshakeFailures;

    return MFD_SUCCESS;
} /* bssWapiWAICertificateHandshakeFailures_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWAIUnicastHandshakeFailures
 * bssWapiWAIUnicastHandshakeFailures is subid 13 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.13
 * Description:
WAI unicast number of the failure of key agreement.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWAIUnicastHandshakeFailures.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWAIUnicastHandshakeFailures_map(u_long *mib_bssWapiWAIUnicastHandshakeFailures_val_ptr, u_long raw_bssWapiWAIUnicastHandshakeFailures_val)
{
    netsnmp_assert(NULL != mib_bssWapiWAIUnicastHandshakeFailures_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIUnicastHandshakeFailures_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWAIUnicastHandshakeFailures mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWAIUnicastHandshakeFailures_val_ptr) = raw_bssWapiWAIUnicastHandshakeFailures_val;

    return MFD_SUCCESS;
} /* bssWapiWAIUnicastHandshakeFailures_map */

/**
 * Extract the current value of the bssWapiWAIUnicastHandshakeFailures data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWAIUnicastHandshakeFailures_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWAIUnicastHandshakeFailures_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWAIUnicastHandshakeFailures_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWAIUnicastHandshakeFailures_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIUnicastHandshakeFailures_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWAIUnicastHandshakeFailures data.
 * set (* bssWapiWAIUnicastHandshakeFailures_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWAIUnicastHandshakeFailures_val_ptr ) = rowreq_ctx->data.bssWapiWAIUnicastHandshakeFailures;

    return MFD_SUCCESS;
} /* bssWapiWAIUnicastHandshakeFailures_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssWAPIPerformanceStatsEntry.bssWapiWAIMulticastHandshakeFailures
 * bssWapiWAIMulticastHandshakeFailures is subid 14 of dot11BssWAPIPerformanceStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.10.5.1.14
 * Description:
WAI multicast number of key failure.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-bssWapiWAIMulticastHandshakeFailures.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
bssWapiWAIMulticastHandshakeFailures_map(u_long *mib_bssWapiWAIMulticastHandshakeFailures_val_ptr, u_long raw_bssWapiWAIMulticastHandshakeFailures_val)
{
    netsnmp_assert(NULL != mib_bssWapiWAIMulticastHandshakeFailures_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIMulticastHandshakeFailures_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement bssWapiWAIMulticastHandshakeFailures mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_bssWapiWAIMulticastHandshakeFailures_val_ptr) = raw_bssWapiWAIMulticastHandshakeFailures_val;

    return MFD_SUCCESS;
} /* bssWapiWAIMulticastHandshakeFailures_map */

/**
 * Extract the current value of the bssWapiWAIMulticastHandshakeFailures data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param bssWapiWAIMulticastHandshakeFailures_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
bssWapiWAIMulticastHandshakeFailures_get( dot11BssWAPIPerformanceStatsTable_rowreq_ctx *rowreq_ctx, u_long * bssWapiWAIMulticastHandshakeFailures_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != bssWapiWAIMulticastHandshakeFailures_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssWAPIPerformanceStatsTable:bssWapiWAIMulticastHandshakeFailures_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the bssWapiWAIMulticastHandshakeFailures data.
 * set (* bssWapiWAIMulticastHandshakeFailures_val_ptr ) from rowreq_ctx->data
 */
    (* bssWapiWAIMulticastHandshakeFailures_val_ptr ) = rowreq_ctx->data.bssWapiWAIMulticastHandshakeFailures;

    return MFD_SUCCESS;
} /* bssWapiWAIMulticastHandshakeFailures_get */



/** @} */
