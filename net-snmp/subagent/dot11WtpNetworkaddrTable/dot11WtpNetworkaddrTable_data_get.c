/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpNetworkaddrTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpNetworkaddrTable get routines.
 * TODO:240:M: Implement dot11WtpNetworkaddrTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpNetworkaddrTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpNetworkaddrTable is subid 5 of wtpGeneralInfo.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpNetworkaddrTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11WtpNetworkaddrTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpNetworkaddrTable_indexes_set_tbl_idx(dot11WtpNetworkaddrTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:dot11WtpNetworkaddrTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11WtpNetworkaddrTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpNetworkaddrTable_indexes_set(dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:dot11WtpNetworkaddrTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpNetworkaddrTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpNetworkaddrTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpNetworkaddrTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpIPAddress
 * wtpIPAddress is subid 1 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.1
 * Description:
AP IP address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIPAddress.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIPAddress_map(u_long *mib_wtpIPAddress_val_ptr, u_long raw_wtpIPAddress_val)
{
    netsnmp_assert(NULL != mib_wtpIPAddress_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpIPAddress_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpIPAddress mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpIPAddress_val_ptr) = raw_wtpIPAddress_val;

    return MFD_SUCCESS;
} /* wtpIPAddress_map */

/**
 * Extract the current value of the wtpIPAddress data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIPAddress_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpIPAddress_get( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long * wtpIPAddress_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIPAddress_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpIPAddress_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIPAddress data.
 * set (* wtpIPAddress_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIPAddress_val_ptr ) = rowreq_ctx->data.wtpIPAddress;

    return MFD_SUCCESS;
} /* wtpIPAddress_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpNetmask
 * wtpNetmask is subid 2 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.2
 * Description:
AP address mask.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpNetmask.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpNetmask_map(u_long *mib_wtpNetmask_val_ptr, u_long raw_wtpNetmask_val)
{
    netsnmp_assert(NULL != mib_wtpNetmask_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpNetmask_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpNetmask mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpNetmask_val_ptr) = raw_wtpNetmask_val;

    return MFD_SUCCESS;
} /* wtpNetmask_map */

/**
 * Extract the current value of the wtpNetmask data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetmask_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpNetmask_get( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long * wtpNetmask_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpNetmask_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpNetmask_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpNetmask data.
 * set (* wtpNetmask_val_ptr ) from rowreq_ctx->data
 */
    (* wtpNetmask_val_ptr ) = rowreq_ctx->data.wtpNetmask;

    return MFD_SUCCESS;
} /* wtpNetmask_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpGateway
 * wtpGateway is subid 3 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.3
 * Description:
The AP gateway address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpGateway.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpGateway_map(u_long *mib_wtpGateway_val_ptr, u_long raw_wtpGateway_val)
{
    netsnmp_assert(NULL != mib_wtpGateway_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpGateway_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpGateway mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpGateway_val_ptr) = raw_wtpGateway_val;

    return MFD_SUCCESS;
} /* wtpGateway_map */

/**
 * Extract the current value of the wtpGateway data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpGateway_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpGateway_get( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long * wtpGateway_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpGateway_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpGateway_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpGateway data.
 * set (* wtpGateway_val_ptr ) from rowreq_ctx->data
 */
    (* wtpGateway_val_ptr ) = rowreq_ctx->data.wtpGateway;

    return MFD_SUCCESS;
} /* wtpGateway_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpFirstDNSServer
 * wtpFirstDNSServer is subid 4 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.4
 * Description:
The First DNS Server IP address.£¨AP£©
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpFirstDNSServer.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpFirstDNSServer_map(u_long *mib_wtpFirstDNSServer_val_ptr, u_long raw_wtpFirstDNSServer_val)
{
    netsnmp_assert(NULL != mib_wtpFirstDNSServer_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpFirstDNSServer_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpFirstDNSServer mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpFirstDNSServer_val_ptr) = raw_wtpFirstDNSServer_val;

    return MFD_SUCCESS;
} /* wtpFirstDNSServer_map */

/**
 * Extract the current value of the wtpFirstDNSServer data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpFirstDNSServer_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpFirstDNSServer_get( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long * wtpFirstDNSServer_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpFirstDNSServer_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpFirstDNSServer_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpFirstDNSServer data.
 * set (* wtpFirstDNSServer_val_ptr ) from rowreq_ctx->data
 */
    (* wtpFirstDNSServer_val_ptr ) = rowreq_ctx->data.wtpFirstDNSServer;

    return MFD_SUCCESS;
} /* wtpFirstDNSServer_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpSeconDNSServer
 * wtpSeconDNSServer is subid 5 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.5
 * Description:
The Second DNS Server IP address.£¨AP£©
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSeconDNSServer.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSeconDNSServer_map(u_long *mib_wtpSeconDNSServer_val_ptr, u_long raw_wtpSeconDNSServer_val)
{
    netsnmp_assert(NULL != mib_wtpSeconDNSServer_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSeconDNSServer_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSeconDNSServer mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSeconDNSServer_val_ptr) = raw_wtpSeconDNSServer_val;

    return MFD_SUCCESS;
} /* wtpSeconDNSServer_map */

/**
 * Extract the current value of the wtpSeconDNSServer data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSeconDNSServer_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSeconDNSServer_get( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long * wtpSeconDNSServer_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSeconDNSServer_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSeconDNSServer_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSeconDNSServer data.
 * set (* wtpSeconDNSServer_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSeconDNSServer_val_ptr ) = rowreq_ctx->data.wtpSeconDNSServer;

    return MFD_SUCCESS;
} /* wtpSeconDNSServer_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpSetAction
 * wtpSetAction is subid 6 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.6
 * Description:
set AP network address action.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 1/8. Values:  set(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSetAction.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSetAction_map(u_long *mib_wtpSetAction_val_ptr, u_long raw_wtpSetAction_val)
{
    netsnmp_assert(NULL != mib_wtpSetAction_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSetAction_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSetAction enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpSetAction_val) {
        case INTERNAL_WTPSETACTION_SET:
             *mib_wtpSetAction_val_ptr = WTPSETACTION_SET;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpSetAction\n", raw_wtpSetAction_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpSetAction_map */

/**
 * Extract the current value of the wtpSetAction data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSetAction_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSetAction_get( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long * wtpSetAction_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSetAction_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSetAction_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSetAction data.
 * set (* wtpSetAction_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSetAction_val_ptr ) = rowreq_ctx->data.wtpSetAction;

    return MFD_SUCCESS;
} /* wtpSetAction_get */



/** @} */
