/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpNetworkaddrTable.h"
#include "../mibs_public.h"

#define		APNETWORKADDR_XMLPATH 	"/var/run/apnetworkaddr.xml"

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpNetworkaddrTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpNetworkaddrTable is subid 5 of wtpGeneralInfo.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpNetworkaddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11WtpNetworkaddrTable_undo_setup( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:dot11WtpNetworkaddrTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11WtpNetworkaddrTable undo.
     * set up dot11WtpNetworkaddrTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11WtpNetworkaddrTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpNetworkaddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpNetworkaddrTable_undo_cleanup( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:dot11WtpNetworkaddrTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11WtpNetworkaddrTable undo.
     */

    return rc;
} /* dot11WtpNetworkaddrTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpNetworkaddrTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpNetworkaddrTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpNetworkaddrTable_commit( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:dot11WtpNetworkaddrTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11WtpNetworkaddrTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WTPIPADDRESS) {
       save_flags &= ~FLAG_WTPIPADDRESS; /* clear wtpIPAddress */
       /*
        * TODO:482:o: |-> commit column wtpIPAddress.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpNetworkaddrTable column wtpIPAddress commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpIPAddress
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPIPADDRESS;
       }
    }

    if (save_flags & FLAG_WTPNETMASK) {
       save_flags &= ~FLAG_WTPNETMASK; /* clear wtpNetmask */
       /*
        * TODO:482:o: |-> commit column wtpNetmask.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpNetworkaddrTable column wtpNetmask commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpNetmask
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPNETMASK;
       }
    }

    if (save_flags & FLAG_WTPGATEWAY) {
       save_flags &= ~FLAG_WTPGATEWAY; /* clear wtpGateway */
       /*
        * TODO:482:o: |-> commit column wtpGateway.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpNetworkaddrTable column wtpGateway commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpGateway
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPGATEWAY;
       }
    }

    if (save_flags & FLAG_WTPFIRSTDNSSERVER) {
       save_flags &= ~FLAG_WTPFIRSTDNSSERVER; /* clear wtpFirstDNSServer */
       /*
        * TODO:482:o: |-> commit column wtpFirstDNSServer.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpNetworkaddrTable column wtpFirstDNSServer commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpFirstDNSServer
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPFIRSTDNSSERVER;
       }
    }

    if (save_flags & FLAG_WTPSECONDNSSERVER) {
       save_flags &= ~FLAG_WTPSECONDNSSERVER; /* clear wtpSeconDNSServer */
       /*
        * TODO:482:o: |-> commit column wtpSeconDNSServer.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpNetworkaddrTable column wtpSeconDNSServer commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpSeconDNSServer
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPSECONDNSSERVER;
       }
    }

    if (save_flags & FLAG_WTPSETACTION) {
       save_flags &= ~FLAG_WTPSETACTION; /* clear wtpSetAction */
       /*
        * TODO:482:o: |-> commit column wtpSetAction.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpNetworkaddrTable column wtpSetAction commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpSetAction
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPSETACTION;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11WtpNetworkaddrTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpNetworkaddrTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpNetworkaddrTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpNetworkaddrTable_undo_commit( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:dot11WtpNetworkaddrTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11WtpNetworkaddrTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11WtpNetworkaddrTable_undo_commit */

/*
 * TODO:420:r: Implement dot11WtpNetworkaddrTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/*
 * TODO:440:M: Implement dot11WtpNetworkaddrTable node value checks.
 * TODO:450:M: Implement dot11WtpNetworkaddrTable undo functions.
 * TODO:460:M: Implement dot11WtpNetworkaddrTable set functions.
 * TODO:480:M: Implement dot11WtpNetworkaddrTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpIPAddress
 * wtpIPAddress is subid 1 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.1
 * Description:
AP IP address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIPAddress_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpNetworkaddrTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpIPAddress_check_value( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpIPAddress_val)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpIPAddress_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpIPAddress value.
     */

    return MFD_SUCCESS; /* wtpIPAddress value not illegal */
} /* wtpIPAddress_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpNetworkaddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpNetworkaddrTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpIPAddress_undo_setup( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpIPAddress_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpIPAddress undo.
     */
    /*
     * copy wtpIPAddress data
     * set rowreq_ctx->undo->wtpIPAddress from rowreq_ctx->data.wtpIPAddress
     */
    rowreq_ctx->undo->wtpIPAddress = rowreq_ctx->data.wtpIPAddress;


    return MFD_SUCCESS;
} /* wtpIPAddress_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpIPAddress_val
 *        A u_long containing the new value.
 */
int
wtpIPAddress_set( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpIPAddress_val )
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpIPAddress_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpIPAddress reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpIPAddress value.
     * set wtpIPAddress value in rowreq_ctx->data
     */
	char wtpIPAddress[32] = { 0 };				
	if(access(APNETWORKADDR_XMLPATH,0)!=0)/*文件不存在*/
	{
		create_dhcp_xml(APNETWORKADDR_XMLPATH);
	}	
	add_dhcp_node_attr(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1");
	
	memset(wtpIPAddress,0,32);
	INET_NTOA( wtpIPAddress_val, wtpIPAddress );
	
	mod_dhcp_node(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1","wtpIPAddress",wtpIPAddress);

    rowreq_ctx->data.wtpIPAddress = wtpIPAddress_val;

    return MFD_SUCCESS;
} /* wtpIPAddress_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpIPAddress_undo( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpIPAddress_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpIPAddress undo.
     */
    /*
     * copy wtpIPAddress data
     * set rowreq_ctx->data.wtpIPAddress from rowreq_ctx->undo->wtpIPAddress
     */
    rowreq_ctx->data.wtpIPAddress = rowreq_ctx->undo->wtpIPAddress;

    
    return MFD_SUCCESS;
} /* wtpIPAddress_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpNetmask
 * wtpNetmask is subid 2 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.2
 * Description:
AP address mask.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetmask_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpNetworkaddrTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpNetmask_check_value( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpNetmask_val)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpNetmask_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpNetmask value.
     */

    return MFD_SUCCESS; /* wtpNetmask value not illegal */
} /* wtpNetmask_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpNetworkaddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpNetworkaddrTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpNetmask_undo_setup( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpNetmask_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpNetmask undo.
     */
    /*
     * copy wtpNetmask data
     * set rowreq_ctx->undo->wtpNetmask from rowreq_ctx->data.wtpNetmask
     */
    rowreq_ctx->undo->wtpNetmask = rowreq_ctx->data.wtpNetmask;


    return MFD_SUCCESS;
} /* wtpNetmask_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpNetmask_val
 *        A u_long containing the new value.
 */
int
wtpNetmask_set( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpNetmask_val )
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpNetmask_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpNetmask reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpNetmask value.
     * set wtpNetmask value in rowreq_ctx->data
     */
    char wtpNetmask[32] = { 0 };				
	if(access(APNETWORKADDR_XMLPATH,0)!=0)/*文件不存在*/
	{
		create_dhcp_xml(APNETWORKADDR_XMLPATH);
	}
	
	add_dhcp_node_attr(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1");
	
	memset(wtpNetmask,0,32);
	INET_NTOA( wtpNetmask_val, wtpNetmask );
	
	mod_dhcp_node(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1","wtpNetmask",wtpNetmask);
	
    rowreq_ctx->data.wtpNetmask = wtpNetmask_val;

    return MFD_SUCCESS;
} /* wtpNetmask_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpNetmask_undo( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpNetmask_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpNetmask undo.
     */
    /*
     * copy wtpNetmask data
     * set rowreq_ctx->data.wtpNetmask from rowreq_ctx->undo->wtpNetmask
     */
    rowreq_ctx->data.wtpNetmask = rowreq_ctx->undo->wtpNetmask;

    
    return MFD_SUCCESS;
} /* wtpNetmask_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpGateway
 * wtpGateway is subid 3 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.3
 * Description:
The AP gateway address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpGateway_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpNetworkaddrTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpGateway_check_value( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpGateway_val)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpGateway_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpGateway value.
     */

    return MFD_SUCCESS; /* wtpGateway value not illegal */
} /* wtpGateway_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpNetworkaddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpNetworkaddrTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpGateway_undo_setup( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpGateway_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpGateway undo.
     */
    /*
     * copy wtpGateway data
     * set rowreq_ctx->undo->wtpGateway from rowreq_ctx->data.wtpGateway
     */
    rowreq_ctx->undo->wtpGateway = rowreq_ctx->data.wtpGateway;


    return MFD_SUCCESS;
} /* wtpGateway_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpGateway_val
 *        A u_long containing the new value.
 */
int
wtpGateway_set( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpGateway_val )
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpGateway_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpGateway reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpGateway value.
     * set wtpGateway value in rowreq_ctx->data
     */
    char wtpGateway[32] = { 0 };

	if(access(APNETWORKADDR_XMLPATH,0)!=0)/*文件不存在*/
	{
		create_dhcp_xml(APNETWORKADDR_XMLPATH);
	}
	
	add_dhcp_node_attr(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1");
	
	memset(wtpGateway,0,32);
	INET_NTOA( wtpGateway_val, wtpGateway );
	
	mod_dhcp_node(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1","wtpGateway",wtpGateway);
				
    rowreq_ctx->data.wtpGateway = wtpGateway_val;

    return MFD_SUCCESS;
} /* wtpGateway_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpGateway_undo( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpGateway_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpGateway undo.
     */
    /*
     * copy wtpGateway data
     * set rowreq_ctx->data.wtpGateway from rowreq_ctx->undo->wtpGateway
     */
    rowreq_ctx->data.wtpGateway = rowreq_ctx->undo->wtpGateway;

    
    return MFD_SUCCESS;
} /* wtpGateway_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpFirstDNSServer
 * wtpFirstDNSServer is subid 4 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.4
 * Description:
The First DNS Server IP address.（AP）
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpFirstDNSServer_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpNetworkaddrTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpFirstDNSServer_check_value( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpFirstDNSServer_val)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpFirstDNSServer_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpFirstDNSServer value.
     */

    return MFD_SUCCESS; /* wtpFirstDNSServer value not illegal */
} /* wtpFirstDNSServer_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpNetworkaddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpNetworkaddrTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpFirstDNSServer_undo_setup( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpFirstDNSServer_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpFirstDNSServer undo.
     */
    /*
     * copy wtpFirstDNSServer data
     * set rowreq_ctx->undo->wtpFirstDNSServer from rowreq_ctx->data.wtpFirstDNSServer
     */
    rowreq_ctx->undo->wtpFirstDNSServer = rowreq_ctx->data.wtpFirstDNSServer;


    return MFD_SUCCESS;
} /* wtpFirstDNSServer_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpFirstDNSServer_val
 *        A u_long containing the new value.
 */
int
wtpFirstDNSServer_set( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpFirstDNSServer_val )
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpFirstDNSServer_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpFirstDNSServer reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpFirstDNSServer value.
     * set wtpFirstDNSServer value in rowreq_ctx->data
     */
    char wtpFirstDNSServer[32] = { 0 };
				
	if(access(APNETWORKADDR_XMLPATH,0)!=0)/*文件不存在*/
	{
		create_dhcp_xml(APNETWORKADDR_XMLPATH);
	}
	
	add_dhcp_node_attr(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1");
	
	memset(wtpFirstDNSServer,0,32);
	INET_NTOA( wtpFirstDNSServer_val, wtpFirstDNSServer );
	
	mod_dhcp_node(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1","wtpFirstDNSServer",wtpFirstDNSServer);
				
    rowreq_ctx->data.wtpFirstDNSServer = wtpFirstDNSServer_val;

    return MFD_SUCCESS;
} /* wtpFirstDNSServer_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpFirstDNSServer_undo( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpFirstDNSServer_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpFirstDNSServer undo.
     */
    /*
     * copy wtpFirstDNSServer data
     * set rowreq_ctx->data.wtpFirstDNSServer from rowreq_ctx->undo->wtpFirstDNSServer
     */
    rowreq_ctx->data.wtpFirstDNSServer = rowreq_ctx->undo->wtpFirstDNSServer;

    
    return MFD_SUCCESS;
} /* wtpFirstDNSServer_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpSeconDNSServer
 * wtpSeconDNSServer is subid 5 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.5
 * Description:
The Second DNS Server IP address.（AP）
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSeconDNSServer_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpNetworkaddrTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpSeconDNSServer_check_value( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpSeconDNSServer_val)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSeconDNSServer_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpSeconDNSServer value.
     */

    return MFD_SUCCESS; /* wtpSeconDNSServer value not illegal */
} /* wtpSeconDNSServer_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpNetworkaddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpNetworkaddrTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpSeconDNSServer_undo_setup( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSeconDNSServer_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpSeconDNSServer undo.
     */
    /*
     * copy wtpSeconDNSServer data
     * set rowreq_ctx->undo->wtpSeconDNSServer from rowreq_ctx->data.wtpSeconDNSServer
     */
    rowreq_ctx->undo->wtpSeconDNSServer = rowreq_ctx->data.wtpSeconDNSServer;


    return MFD_SUCCESS;
} /* wtpSeconDNSServer_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpSeconDNSServer_val
 *        A u_long containing the new value.
 */
int
wtpSeconDNSServer_set( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpSeconDNSServer_val )
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSeconDNSServer_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpSeconDNSServer reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpSeconDNSServer value.
     * set wtpSeconDNSServer value in rowreq_ctx->data
     */
    char wtpSeconDNSServer[32] = { 0 };
				
	if(access(APNETWORKADDR_XMLPATH,0)!=0)/*文件不存在*/
	{
		create_dhcp_xml(APNETWORKADDR_XMLPATH);
	}
	
	add_dhcp_node_attr(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1");
	
	memset(wtpSeconDNSServer,0,32);
	INET_NTOA( wtpSeconDNSServer_val, wtpSeconDNSServer );
	
	mod_dhcp_node(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1","wtpSeconDNSServer",wtpSeconDNSServer);
				
    rowreq_ctx->data.wtpSeconDNSServer = wtpSeconDNSServer_val;

    return MFD_SUCCESS;
} /* wtpSeconDNSServer_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpSeconDNSServer_undo( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSeconDNSServer_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpSeconDNSServer undo.
     */
    /*
     * copy wtpSeconDNSServer data
     * set rowreq_ctx->data.wtpSeconDNSServer from rowreq_ctx->undo->wtpSeconDNSServer
     */
    rowreq_ctx->data.wtpSeconDNSServer = rowreq_ctx->undo->wtpSeconDNSServer;

    
    return MFD_SUCCESS;
} /* wtpSeconDNSServer_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpNetworkaddrEntry.wtpSetAction
 * wtpSetAction is subid 6 of dot11WtpNetworkaddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.5.1.6
 * Description:
set AP network address action.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 1/8. Values:  set(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSetAction_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpNetworkaddrTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  set(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpSetAction_check_value( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpSetAction_val)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSetAction_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpSetAction value.
     */

    return MFD_SUCCESS; /* wtpSetAction value not illegal */
} /* wtpSetAction_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpNetworkaddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpNetworkaddrTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpSetAction_undo_setup( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSetAction_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpSetAction undo.
     */
    /*
     * copy wtpSetAction data
     * set rowreq_ctx->undo->wtpSetAction from rowreq_ctx->data.wtpSetAction
     */
    rowreq_ctx->undo->wtpSetAction = rowreq_ctx->data.wtpSetAction;


    return MFD_SUCCESS;
} /* wtpSetAction_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpSetAction_val
 *        A long containing the new value.
 */
int
wtpSetAction_set( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx, u_long wtpSetAction_val )
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSetAction_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpSetAction reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpSetAction_val) {
        case WTPSETACTION_SET:
             wtpSetAction_val = INTERNAL_WTPSETACTION_SET;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpSetAction\n", wtpSetAction_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpSetAction value.
     * set wtpSetAction value in rowreq_ctx->data
     */  
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
    int ret = 0;
	char ap_ip[20]={0};
	memset(ap_ip,0,20);
	char ap_mask[20]={0};
	memset(ap_mask,0,20);
	char ap_gateway[20]={0};
	memset(ap_gateway,0,20);
	char ap_dns1[20]={0};
	memset(ap_dns1,0,20);
	char ap_dns2[20]={0};
	memset(ap_dns2,0,20);

	get_dhcp_node_attr(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1","wtpIPAddress",ap_ip);
	get_dhcp_node_attr(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1","wtpNetmask",ap_mask);
	get_dhcp_node_attr(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1","wtpGateway",ap_gateway);
	get_dhcp_node_attr(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1","wtpFirstDNSServer",ap_dns1);
	get_dhcp_node_attr(APNETWORKADDR_XMLPATH,"APNetworkaddr","AP","1","wtpSeconDNSServer",ap_dns2);

	if(strlen(ap_ip)&&strlen(ap_mask)&&strlen(ap_gateway)&&strlen(ap_dns1)&&strlen(ap_dns2))
	{
		ret = set_ap_networkaddr_command_cmd(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrID,ap_ip,ap_mask,ap_gateway,ap_dns1,ap_dns2);
		if(ret == 1)
		{		
			rowreq_ctx->data.wtpSetAction = wtpSetAction_val;
			rc = MFD_SUCCESS;
		}	
		else
		{	
		    if(SNMPD_CONNECTION_ERROR == ret) {
		    close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		    }
			rc = MFD_ERROR;
		}
		system("rm /var/run/apnetworkaddr.xml");
	}
	else
	{
		rc = MFD_ERROR;
	}
    return rc;
} /* wtpSetAction_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpSetAction_undo( dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpNetworkaddrTable:wtpSetAction_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpSetAction undo.
     */
    /*
     * copy wtpSetAction data
     * set rowreq_ctx->data.wtpSetAction from rowreq_ctx->undo->wtpSetAction
     */
    rowreq_ctx->data.wtpSetAction = rowreq_ctx->undo->wtpSetAction;

    
    return MFD_SUCCESS;
} /* wtpSetAction_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpNetworkaddrTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11WtpNetworkaddrTable if you don't have dependencies)
 */
int
dot11WtpNetworkaddrTable_check_dependencies(dot11WtpNetworkaddrTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11WtpNetworkaddrTable:dot11WtpNetworkaddrTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11WtpNetworkaddrTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11WtpNetworkaddrTable_check_dependencies */

/** @} */
