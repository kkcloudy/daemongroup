/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigIpGroupTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigIpGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11ConfigIpGroupTable is subid 1 of dot11ConfigIpGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.26.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11ConfigIpGroupTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11ConfigIpGroupTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigIpGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigIpGroupTable_undo_setup( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:dot11ConfigIpGroupTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigIpGroupTable undo.
     * set up dot11ConfigIpGroupTable undo information, in preparation for a set.
     * Undo storage is in (* ConfigIpRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigIpGroupTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigIpGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigIpGroupTable_undo( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:dot11ConfigIpGroupTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11ConfigIpGroupTable undo.
     * dot11ConfigIpGroupTable undo information, in response to a failed set.
     * Undo storage is in (* ConfigIpRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigIpGroupTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigIpGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigIpGroupTable_undo_cleanup( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:dot11ConfigIpGroupTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigIpGroupTable undo.
     * Undo storage is in (* ConfigIpRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigIpGroupTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigIpGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigIpGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigIpGroupTable_commit( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:dot11ConfigIpGroupTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

	if(RS_NOTREADY == rowreq_ctx->data.ConfigIpRowStatus) {
        rowreq_ctx->data.ConfigIpRowStatus = RS_NOTINSERVICE;
    }

    /*
     * commit dot11ConfigIpGroupTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_IFCONFIGIPADDR_FLAG) {
       save_flags &= ~COLUMN_IFCONFIGIPADDR_FLAG; /* clear IfconfigIpAddr */
       /*
        * TODO:482:o: |-> commit column IfconfigIpAddr.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigIpGroupTable column IfconfigIpAddr commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo IfconfigIpAddr
             */
            rowreq_ctx->column_set_flags |= COLUMN_IFCONFIGIPADDR_FLAG;
       }
    }

    if (save_flags & COLUMN_IFCONFIGNETMASK_FLAG) {
       save_flags &= ~COLUMN_IFCONFIGNETMASK_FLAG; /* clear IfconfigNetmask */
       /*
        * TODO:482:o: |-> commit column IfconfigNetmask.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigIpGroupTable column IfconfigNetmask commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo IfconfigNetmask
             */
            rowreq_ctx->column_set_flags |= COLUMN_IFCONFIGNETMASK_FLAG;
       }
    }

    if (save_flags & COLUMN_CONFIGIPROWSTATUS_FLAG) {
       save_flags &= ~COLUMN_CONFIGIPROWSTATUS_FLAG; /* clear ConfigIpRowStatus */
	   
        switch(rowreq_ctx->data.ConfigIpRowStatus) {
            case RS_ACTIVE:
                break;

            case RS_CREATEANDWAIT:
                rowreq_ctx->data.ConfigIpRowStatus = RS_NOTREADY;
                rc = MFD_SUCCESS;
                break;
                
            case RS_DESTROY:  
                rowreq_ctx->rowreq_flags &= ~MFD_ROW_DELETED;
                break;
                
            default:
                break;
        }
          
       /*
        * TODO:482:o: |-> commit column ConfigIpRowStatus.
        */
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigIpGroupTable column ConfigIpRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo ConfigIpRowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_CONFIGIPROWSTATUS_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigIpGroupTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigIpGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigIpGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigIpGroupTable_undo_commit( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:dot11ConfigIpGroupTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigIpGroupTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11ConfigIpGroupTable_undo_commit */

/*
 * TODO:440:M: Implement dot11ConfigIpGroupTable node value checks.
 * TODO:450:M: Implement dot11ConfigIpGroupTable undo functions.
 * TODO:460:M: Implement dot11ConfigIpGroupTable set functions.
 * TODO:480:M: Implement dot11ConfigIpGroupTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigIpGroupEntry.IfconfigIpAddr
 * IfconfigIpAddr is subid 2 of dot11ConfigIpGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.26.1.1.2
 * Description:
Create configIpAddr.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param IfconfigIpAddr_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigIpGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
IfconfigIpAddr_check_value( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx, u_long IfconfigIpAddr_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:IfconfigIpAddr_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid IfconfigIpAddr value.
     */

    return MFD_SUCCESS; /* IfconfigIpAddr value not illegal */
} /* IfconfigIpAddr_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigIpGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigIpGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
IfconfigIpAddr_undo_setup( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:IfconfigIpAddr_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup IfconfigIpAddr undo.
     */
    /*
     * copy IfconfigIpAddr data
     * set rowreq_ctx->undo->IfconfigIpAddr from rowreq_ctx->data.IfconfigIpAddr
     */
    rowreq_ctx->undo->IfconfigIpAddr = rowreq_ctx->data.IfconfigIpAddr;


    return MFD_SUCCESS;
} /* IfconfigIpAddr_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param IfconfigIpAddr_val
 *        A u_long containing the new value.
 */
int
IfconfigIpAddr_set( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx, u_long IfconfigIpAddr_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:IfconfigIpAddr_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
	int ret = -1;
    /*
     * TODO:461:M: |-> Set IfconfigIpAddr value.
     * set IfconfigIpAddr value in rowreq_ctx->data
     */
	if((rowreq_ctx->data.ConfigIpRowStatus == 1) || (rowreq_ctx->data.ConfigIpRowStatus == 2))
		{
			mibif mif;
			memset(&mif,0,sizeof(mif));
		
			memset(mif.ifname,0,MAX_MIBIF_IFNAME_LEN);
			strncpy(mif.ifname,rowreq_ctx->data.Ifname,sizeof(mif.ifname)-1);
			mif.index = rowreq_ctx->data.Ifconfigindex;
			mif.ipaddr = IfconfigIpAddr_val;
			mif.mask = rowreq_ctx->data.IfconfigNetmask;						
				
			ret = set_mibif_ip(&mif,IfconfigIpAddr_val);
			if(ret !=0)
			{
				return MFD_ERROR;
			}
		}
	
    rowreq_ctx->data.IfconfigIpAddr = IfconfigIpAddr_val;

    return MFD_SUCCESS;
} /* IfconfigIpAddr_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
IfconfigIpAddr_undo( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:IfconfigIpAddr_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up IfconfigIpAddr undo.
     */
    /*
     * copy IfconfigIpAddr data
     * set rowreq_ctx->data.IfconfigIpAddr from rowreq_ctx->undo->IfconfigIpAddr
     */
    rowreq_ctx->data.IfconfigIpAddr = rowreq_ctx->undo->IfconfigIpAddr;

    
    return MFD_SUCCESS;
} /* IfconfigIpAddr_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigIpGroupEntry.IfconfigNetmask
 * IfconfigNetmask is subid 3 of dot11ConfigIpGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.26.1.1.3
 * Description:
Create Netmask.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param IfconfigNetmask_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigIpGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
IfconfigNetmask_check_value( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx, u_long IfconfigNetmask_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:IfconfigNetmask_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid IfconfigNetmask value.
     */

    return MFD_SUCCESS; /* IfconfigNetmask value not illegal */
} /* IfconfigNetmask_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigIpGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigIpGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
IfconfigNetmask_undo_setup( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:IfconfigNetmask_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup IfconfigNetmask undo.
     */
    /*
     * copy IfconfigNetmask data
     * set rowreq_ctx->undo->IfconfigNetmask from rowreq_ctx->data.IfconfigNetmask
     */
    rowreq_ctx->undo->IfconfigNetmask = rowreq_ctx->data.IfconfigNetmask;


    return MFD_SUCCESS;
} /* IfconfigNetmask_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param IfconfigNetmask_val
 *        A u_long containing the new value.
 */
int
IfconfigNetmask_set( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx, u_long IfconfigNetmask_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:IfconfigNetmask_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
	int ret = 0;
    /*
     * TODO:461:M: |-> Set IfconfigNetmask value.
     * set IfconfigNetmask value in rowreq_ctx->data
     */
	if((rowreq_ctx->data.ConfigIpRowStatus == 1) || (rowreq_ctx->data.ConfigIpRowStatus == 2))
	{
		mibif mif;
		memset(&mif,0,sizeof(mif));
	
		memset(mif.ifname,0,MAX_MIBIF_IFNAME_LEN);
		strncpy(mif.ifname,rowreq_ctx->data.Ifname,sizeof(mif.ifname)-1);
		mif.index = rowreq_ctx->data.Ifconfigindex;
		mif.ipaddr = rowreq_ctx->data.IfconfigIpAddr;
		mif.mask = IfconfigNetmask_val;						
			
		ret = set_mibif_mask(&mif,IfconfigNetmask_val);
		if(ret != -1)
		{
			return MFD_ERROR;
		}
	}
    rowreq_ctx->data.IfconfigNetmask = IfconfigNetmask_val;

    return MFD_SUCCESS;
} /* IfconfigNetmask_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
IfconfigNetmask_undo( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:IfconfigNetmask_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up IfconfigNetmask undo.
     */
    /*
     * copy IfconfigNetmask data
     * set rowreq_ctx->data.IfconfigNetmask from rowreq_ctx->undo->IfconfigNetmask
     */
    rowreq_ctx->data.IfconfigNetmask = rowreq_ctx->undo->IfconfigNetmask;

    
    return MFD_SUCCESS;
} /* IfconfigNetmask_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigIpGroupEntry.ConfigIpRowStatus
 * ConfigIpRowStatus is subid 4 of dot11ConfigIpGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.26.1.1.4
 * Description:
This variable is used to create, modify, and/or delete a row
			        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ConfigIpRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigIpGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
ConfigIpRowStatus_check_value( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx, u_long ConfigIpRowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:ConfigIpRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid ConfigIpRowStatus value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.ConfigIpRowStatus || RS_NOTREADY == rowreq_ctx->data.ConfigIpRowStatus)
        return MFD_SUCCESS;
    else     
        return MFD_ERROR; /* ConfigIpRowStatus value not illegal */
} /* ConfigIpRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigIpGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigIpGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
ConfigIpRowStatus_undo_setup( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:ConfigIpRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup ConfigIpRowStatus undo.
     */
    /*
     * copy ConfigIpRowStatus data
     * set rowreq_ctx->undo->ConfigIpRowStatus from rowreq_ctx->data.ConfigIpRowStatus
     */
    rowreq_ctx->undo->ConfigIpRowStatus = rowreq_ctx->data.ConfigIpRowStatus;


    return MFD_SUCCESS;
} /* ConfigIpRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param ConfigIpRowStatus_val
 *        A long containing the new value.
 */
int
ConfigIpRowStatus_set( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx, u_long ConfigIpRowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:ConfigIpRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set ConfigIpRowStatus value.
     * set ConfigIpRowStatus value in rowreq_ctx->data
     */
	int ret = MFD_SUCCESS;
    
    switch(ConfigIpRowStatus_val) {
    
        case RS_ACTIVE:         
		case RS_NOTINSERVICE:
        //case RS_DESTROY:
            rowreq_ctx->data.ConfigIpRowStatus = ConfigIpRowStatus_val;
            ret = MFD_SUCCESS;
            break;
    
		case RS_DESTROY:
        case RS_NOTREADY:
        case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
            ret = MFD_ERROR;
            break;
    
       /* case RS_CREATEANDWAIT:
            if(RS_NONEXISTENT == rowreq_ctx->data.ConfigIpRowStatus) {
                rowreq_ctx->data.ConfigIpRowStatus = ConfigIpRowStatus_val;
                ret = MFD_SUCCESS;
            }
            else {
                ret = MFD_ERROR;
            }
            break;*/
    
        default:
            snmp_log(LOG_WARNING, "unknow %lu in ConfigIpRowStatus_set", ConfigIpRowStatus_val);
            ret = MFD_ERROR;
            break;
    }

    return ret;
} /* ConfigIpRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
ConfigIpRowStatus_undo( dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigIpGroupTable:ConfigIpRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up ConfigIpRowStatus undo.
     */
    /*
     * copy ConfigIpRowStatus data
     * set rowreq_ctx->data.ConfigIpRowStatus from rowreq_ctx->undo->ConfigIpRowStatus
     */
    rowreq_ctx->data.ConfigIpRowStatus = rowreq_ctx->undo->ConfigIpRowStatus;

    
    return MFD_SUCCESS;
} /* ConfigIpRowStatus_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigIpGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigIpGroupTable if you don't have dependencies)
 */
int
dot11ConfigIpGroupTable_check_dependencies(dot11ConfigIpGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigIpGroupTable:dot11ConfigIpGroupTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigIpGroupTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & COLUMN_CONFIGIPROWSTATUS_FLAG) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->ConfigIpRowStatus,
                                         rowreq_ctx->data.ConfigIpRowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_DESTROY ==  rowreq_ctx->data.ConfigIpRowStatus) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;
            }
            else if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.ConfigIpRowStatus) {
                if ((rowreq_ctx->column_set_flags & DOT11CONFIGIPGROUPTABLE_REQUIRED_COLS)
                    != DOT11CONFIGIPGROUPTABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11ConfigIpGroupTable",
                                "required columns missing (0x%0x != 0x%0x)\n",
                                rowreq_ctx->column_set_flags, DOT11CONFIGIPGROUPTABLE_REQUIRED_COLS));
                    return MFD_CANNOT_CREATE_NOW;
                }
                rowreq_ctx->data.ConfigIpRowStatus = ROWSTATUS_ACTIVE;
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.ConfigIpRowStatus) {
                if (rowreq_ctx->column_set_flags & ~COLUMN_CONFIGIPROWSTATUS_FLAG) {
                    DEBUGMSGTL(("dot11ConfigIpGroupTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;

            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11ConfigIpGroupTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11ConfigIpGroupTable_check_dependencies */

/** @} */
