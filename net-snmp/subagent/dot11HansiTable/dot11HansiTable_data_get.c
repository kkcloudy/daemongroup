/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11HansiTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11HansiTable get routines.
 * TODO:240:M: Implement dot11HansiTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11HansiTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11HansiTable is subid 1 of dot11Hansi.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.29.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11HansiTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11HansiEntry.VrrpID
 * VrrpID is subid 1 of dot11HansiEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.29.1.1.1
 * Description:
HANSI  ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-VrrpID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
VrrpID_map_hansi(long *mib_VrrpID_val_ptr, long raw_VrrpID_val)
{
    netsnmp_assert(NULL != mib_VrrpID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11HansiTable:VrrpID_map_hansi","called\n"));
    
    /*
     * TODO:241:o: |-> Implement VrrpID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_VrrpID_val_ptr) = raw_VrrpID_val;

    return MFD_SUCCESS;
} /* VrrpID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11HansiTable_indexes_set_tbl_idx(dot11HansiTable_mib_index *tbl_idx, long VrrpID_val)
{
    DEBUGMSGTL(("verbose:dot11HansiTable:dot11HansiTable_indexes_set_tbl_idx","called\n"));

    /* VrrpID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->VrrpID = VrrpID_val;
    

    return MFD_SUCCESS;
} /* dot11HansiTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11HansiTable_indexes_set(dot11HansiTable_rowreq_ctx *rowreq_ctx, long VrrpID_val)
{
    DEBUGMSGTL(("verbose:dot11HansiTable:dot11HansiTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11HansiTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , VrrpID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11HansiTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11HansiTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11HansiEntry.HansiState
 * HansiState is subid 2 of dot11HansiEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.29.1.1.2
 * Description:
HANSI config.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-HansiState.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
HansiState_map(char **mib_HansiState_val_ptr_ptr, size_t *mib_HansiState_val_ptr_len_ptr, char *raw_HansiState_val_ptr, size_t raw_HansiState_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_HansiState_val_ptr);
    netsnmp_assert((NULL != mib_HansiState_val_ptr_ptr) && (NULL != mib_HansiState_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11HansiTable:HansiState_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement HansiState non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_HansiState_val_ptr_len; /* assume equal */
    if((NULL == *mib_HansiState_val_ptr_ptr) || (*mib_HansiState_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_HansiState_val_ptr_ptr = realloc( *mib_HansiState_val_ptr_ptr, converted_len * sizeof(**mib_HansiState_val_ptr_ptr));
        if(NULL == *mib_HansiState_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_HansiState_val_ptr_len_ptr = converted_len;
    memcpy( *mib_HansiState_val_ptr_ptr, raw_HansiState_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* HansiState_map */

/**
 * Extract the current value of the HansiState data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param HansiState_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param HansiState_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by HansiState.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*HansiState_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update HansiState_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
HansiState_get( dot11HansiTable_rowreq_ctx *rowreq_ctx, char **HansiState_val_ptr_ptr, size_t *HansiState_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != HansiState_val_ptr_ptr) && (NULL != *HansiState_val_ptr_ptr));
   netsnmp_assert( NULL != HansiState_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11HansiTable:HansiState_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the HansiState data.
 * set (* HansiState_val_ptr_ptr ) and (* HansiState_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for HansiState data
     */
    if ((NULL == (* HansiState_val_ptr_ptr )) ||
        ((* HansiState_val_ptr_len_ptr ) < (rowreq_ctx->data.HansiState_len * sizeof((* HansiState_val_ptr_ptr )[0])))) {
        /*
         * allocate space for HansiState data
         */
        (* HansiState_val_ptr_ptr ) = malloc(rowreq_ctx->data.HansiState_len * sizeof((* HansiState_val_ptr_ptr )[0]));
        if(NULL == (* HansiState_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* HansiState_val_ptr_len_ptr ) = rowreq_ctx->data.HansiState_len * sizeof((* HansiState_val_ptr_ptr )[0]);
    memcpy( (* HansiState_val_ptr_ptr ), rowreq_ctx->data.HansiState, (* HansiState_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* HansiState_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11HansiEntry.HansiUplinkIP
 * HansiUplinkIP is subid 3 of dot11HansiEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.29.1.1.3
 * Description:
HANSI up-link ip.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-HansiUplinkIP.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
HansiUplinkIP_map(char **mib_HansiUplinkIP_val_ptr_ptr, size_t *mib_HansiUplinkIP_val_ptr_len_ptr, char *raw_HansiUplinkIP_val_ptr, size_t raw_HansiUplinkIP_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_HansiUplinkIP_val_ptr);
    netsnmp_assert((NULL != mib_HansiUplinkIP_val_ptr_ptr) && (NULL != mib_HansiUplinkIP_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11HansiTable:HansiUplinkIP_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement HansiUplinkIP non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_HansiUplinkIP_val_ptr_len; /* assume equal */
    if((NULL == *mib_HansiUplinkIP_val_ptr_ptr) || (*mib_HansiUplinkIP_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_HansiUplinkIP_val_ptr_ptr = realloc( *mib_HansiUplinkIP_val_ptr_ptr, converted_len * sizeof(**mib_HansiUplinkIP_val_ptr_ptr));
        if(NULL == *mib_HansiUplinkIP_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_HansiUplinkIP_val_ptr_len_ptr = converted_len;
    memcpy( *mib_HansiUplinkIP_val_ptr_ptr, raw_HansiUplinkIP_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* HansiUplinkIP_map */

/**
 * Extract the current value of the HansiUplinkIP data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param HansiUplinkIP_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param HansiUplinkIP_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by HansiUplinkIP.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*HansiUplinkIP_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update HansiUplinkIP_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
HansiUplinkIP_get( dot11HansiTable_rowreq_ctx *rowreq_ctx, char **HansiUplinkIP_val_ptr_ptr, size_t *HansiUplinkIP_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != HansiUplinkIP_val_ptr_ptr) && (NULL != *HansiUplinkIP_val_ptr_ptr));
   netsnmp_assert( NULL != HansiUplinkIP_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11HansiTable:HansiUplinkIP_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the HansiUplinkIP data.
 * set (* HansiUplinkIP_val_ptr_ptr ) and (* HansiUplinkIP_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for HansiUplinkIP data
     */
    if ((NULL == (* HansiUplinkIP_val_ptr_ptr )) ||
        ((* HansiUplinkIP_val_ptr_len_ptr ) < (rowreq_ctx->data.HansiUplinkIP_len * sizeof((* HansiUplinkIP_val_ptr_ptr )[0])))) {
        /*
         * allocate space for HansiUplinkIP data
         */
        (* HansiUplinkIP_val_ptr_ptr ) = malloc(rowreq_ctx->data.HansiUplinkIP_len * sizeof((* HansiUplinkIP_val_ptr_ptr )[0]));
        if(NULL == (* HansiUplinkIP_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* HansiUplinkIP_val_ptr_len_ptr ) = rowreq_ctx->data.HansiUplinkIP_len * sizeof((* HansiUplinkIP_val_ptr_ptr )[0]);
    memcpy( (* HansiUplinkIP_val_ptr_ptr ), rowreq_ctx->data.HansiUplinkIP, (* HansiUplinkIP_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* HansiUplinkIP_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11HansiEntry.HansiDownlinkIP
 * HansiDownlinkIP is subid 4 of dot11HansiEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.29.1.1.4
 * Description:
HANSI down-link ip.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-HansiDownlinkIP.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
HansiDownlinkIP_map(char **mib_HansiDownlinkIP_val_ptr_ptr, size_t *mib_HansiDownlinkIP_val_ptr_len_ptr, char *raw_HansiDownlinkIP_val_ptr, size_t raw_HansiDownlinkIP_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_HansiDownlinkIP_val_ptr);
    netsnmp_assert((NULL != mib_HansiDownlinkIP_val_ptr_ptr) && (NULL != mib_HansiDownlinkIP_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11HansiTable:HansiDownlinkIP_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement HansiDownlinkIP non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_HansiDownlinkIP_val_ptr_len; /* assume equal */
    if((NULL == *mib_HansiDownlinkIP_val_ptr_ptr) || (*mib_HansiDownlinkIP_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_HansiDownlinkIP_val_ptr_ptr = realloc( *mib_HansiDownlinkIP_val_ptr_ptr, converted_len * sizeof(**mib_HansiDownlinkIP_val_ptr_ptr));
        if(NULL == *mib_HansiDownlinkIP_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_HansiDownlinkIP_val_ptr_len_ptr = converted_len;
    memcpy( *mib_HansiDownlinkIP_val_ptr_ptr, raw_HansiDownlinkIP_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* HansiDownlinkIP_map */

/**
 * Extract the current value of the HansiDownlinkIP data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param HansiDownlinkIP_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param HansiDownlinkIP_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by HansiDownlinkIP.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*HansiDownlinkIP_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update HansiDownlinkIP_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
HansiDownlinkIP_get( dot11HansiTable_rowreq_ctx *rowreq_ctx, char **HansiDownlinkIP_val_ptr_ptr, size_t *HansiDownlinkIP_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != HansiDownlinkIP_val_ptr_ptr) && (NULL != *HansiDownlinkIP_val_ptr_ptr));
   netsnmp_assert( NULL != HansiDownlinkIP_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11HansiTable:HansiDownlinkIP_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the HansiDownlinkIP data.
 * set (* HansiDownlinkIP_val_ptr_ptr ) and (* HansiDownlinkIP_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for HansiDownlinkIP data
     */
    if ((NULL == (* HansiDownlinkIP_val_ptr_ptr )) ||
        ((* HansiDownlinkIP_val_ptr_len_ptr ) < (rowreq_ctx->data.HansiDownlinkIP_len * sizeof((* HansiDownlinkIP_val_ptr_ptr )[0])))) {
        /*
         * allocate space for HansiDownlinkIP data
         */
        (* HansiDownlinkIP_val_ptr_ptr ) = malloc(rowreq_ctx->data.HansiDownlinkIP_len * sizeof((* HansiDownlinkIP_val_ptr_ptr )[0]));
        if(NULL == (* HansiDownlinkIP_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* HansiDownlinkIP_val_ptr_len_ptr ) = rowreq_ctx->data.HansiDownlinkIP_len * sizeof((* HansiDownlinkIP_val_ptr_ptr )[0]);
    memcpy( (* HansiDownlinkIP_val_ptr_ptr ), rowreq_ctx->data.HansiDownlinkIP, (* HansiDownlinkIP_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* HansiDownlinkIP_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11HansiEntry.HansiHblinkIP
 * HansiHblinkIP is subid 5 of dot11HansiEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.29.1.1.5
 * Description:
HANSI heart-blink ip.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-HansiHblinkIP.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
HansiHblinkIP_map(char **mib_HansiHblinkIP_val_ptr_ptr, size_t *mib_HansiHblinkIP_val_ptr_len_ptr, char *raw_HansiHblinkIP_val_ptr, size_t raw_HansiHblinkIP_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_HansiHblinkIP_val_ptr);
    netsnmp_assert((NULL != mib_HansiHblinkIP_val_ptr_ptr) && (NULL != mib_HansiHblinkIP_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11HansiTable:HansiHblinkIP_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement HansiHblinkIP non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_HansiHblinkIP_val_ptr_len; /* assume equal */
    if((NULL == *mib_HansiHblinkIP_val_ptr_ptr) || (*mib_HansiHblinkIP_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_HansiHblinkIP_val_ptr_ptr = realloc( *mib_HansiHblinkIP_val_ptr_ptr, converted_len * sizeof(**mib_HansiHblinkIP_val_ptr_ptr));
        if(NULL == *mib_HansiHblinkIP_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_HansiHblinkIP_val_ptr_len_ptr = converted_len;
    memcpy( *mib_HansiHblinkIP_val_ptr_ptr, raw_HansiHblinkIP_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* HansiHblinkIP_map */

/**
 * Extract the current value of the HansiHblinkIP data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param HansiHblinkIP_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param HansiHblinkIP_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by HansiHblinkIP.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*HansiHblinkIP_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update HansiHblinkIP_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
HansiHblinkIP_get( dot11HansiTable_rowreq_ctx *rowreq_ctx, char **HansiHblinkIP_val_ptr_ptr, size_t *HansiHblinkIP_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != HansiHblinkIP_val_ptr_ptr) && (NULL != *HansiHblinkIP_val_ptr_ptr));
   netsnmp_assert( NULL != HansiHblinkIP_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11HansiTable:HansiHblinkIP_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the HansiHblinkIP data.
 * set (* HansiHblinkIP_val_ptr_ptr ) and (* HansiHblinkIP_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for HansiHblinkIP data
     */
    if ((NULL == (* HansiHblinkIP_val_ptr_ptr )) ||
        ((* HansiHblinkIP_val_ptr_len_ptr ) < (rowreq_ctx->data.HansiHblinkIP_len * sizeof((* HansiHblinkIP_val_ptr_ptr )[0])))) {
        /*
         * allocate space for HansiHblinkIP data
         */
        (* HansiHblinkIP_val_ptr_ptr ) = malloc(rowreq_ctx->data.HansiHblinkIP_len * sizeof((* HansiHblinkIP_val_ptr_ptr )[0]));
        if(NULL == (* HansiHblinkIP_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* HansiHblinkIP_val_ptr_len_ptr ) = rowreq_ctx->data.HansiHblinkIP_len * sizeof((* HansiHblinkIP_val_ptr_ptr )[0]);
    memcpy( (* HansiHblinkIP_val_ptr_ptr ), rowreq_ctx->data.HansiHblinkIP, (* HansiHblinkIP_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* HansiHblinkIP_get */



/** @} */
