/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11DHCPIpPoolConfigTable.h"
#include "ws_dhcp_conf.h"
#include "ws_log_conf.h"
#include "ws_dcli_dhcp.h"
#include <time.h>

#include "ws_dbus_def.h"
#include "ws_dbus_list_interface.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11DHCPIpPoolConfigTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11DHCPIpPoolConfigTable is subid 5 of dot11Service.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.5, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11DHCPIpPoolConfigTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11DHCPIpPoolConfigTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11DHCPIpPoolConfigTable_undo_setup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:dot11DHCPIpPoolConfigTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11DHCPIpPoolConfigTable undo.
     * set up dot11DHCPIpPoolConfigTable undo information, in preparation for a set.
     * Undo storage is in (* DHCPApOption43_val_ptr_ptr )*
     */

    return rc;
} /* dot11DHCPIpPoolConfigTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11DHCPIpPoolConfigTable_undo( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:dot11DHCPIpPoolConfigTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11DHCPIpPoolConfigTable undo.
     * dot11DHCPIpPoolConfigTable undo information, in response to a failed set.
     * Undo storage is in (* DHCPApOption43_val_ptr_ptr )*
     */

    return rc;
} /* dot11DHCPIpPoolConfigTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11DHCPIpPoolConfigTable_undo_cleanup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:dot11DHCPIpPoolConfigTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11DHCPIpPoolConfigTable undo.
     * Undo storage is in (* DHCPApOption43_val_ptr_ptr )*
     */

    return rc;
} /* dot11DHCPIpPoolConfigTable_undo_cleanup */

static int
check_create_ready(dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx) {
	if((rowreq_ctx->data.IPPollStartAddr!=0) && (rowreq_ctx->data.IPPoolStopAddr!=0) && (rowreq_ctx->data.Netmask!=0) && strlen(rowreq_ctx->data.DHCPPoolName)){
        return MFD_SUCCESS;
    }

    return MFD_ERROR;
}

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11DHCPIpPoolConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11DHCPIpPoolConfigTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11DHCPIpPoolConfigTable_commit( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:dot11DHCPIpPoolConfigTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;
	
    if(RS_NOTREADY == rowreq_ctx->data.DHCPRowStatus && MFD_SUCCESS == check_create_ready(rowreq_ctx)) {
        rowreq_ctx->data.DHCPRowStatus = RS_NOTINSERVICE;
    }
    
    /*
     * commit dot11DHCPIpPoolConfigTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_IPPOLLSTARTADDR_FLAG) {
       save_flags &= ~COLUMN_IPPOLLSTARTADDR_FLAG; /* clear IPPollStartAddr */
       /*
        * TODO:482:o: |-> commit column IPPollStartAddr.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpPoolConfigTable column IPPollStartAddr commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo IPPollStartAddr
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPPOLLSTARTADDR_FLAG;
       }
    }

    if (save_flags & COLUMN_IPPOOLSTOPADDR_FLAG) {
       save_flags &= ~COLUMN_IPPOOLSTOPADDR_FLAG; /* clear IPPoolStopAddr */
       /*
        * TODO:482:o: |-> commit column IPPoolStopAddr.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpPoolConfigTable column IPPoolStopAddr commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo IPPoolStopAddr
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPPOOLSTOPADDR_FLAG;
       }
    }

    if (save_flags & COLUMN_GATEWAYADDR_FLAG) {
       save_flags &= ~COLUMN_GATEWAYADDR_FLAG; /* clear GatewayAddr */
       /*
        * TODO:482:o: |-> commit column GatewayAddr.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpPoolConfigTable column GatewayAddr commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo GatewayAddr
             */
            rowreq_ctx->column_set_flags |= COLUMN_GATEWAYADDR_FLAG;
       }
    }

    if (save_flags & COLUMN_NETMASK_FLAG) {
       save_flags &= ~COLUMN_NETMASK_FLAG; /* clear Netmask */
       /*
        * TODO:482:o: |-> commit column Netmask.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpPoolConfigTable column Netmask commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo Netmask
             */
            rowreq_ctx->column_set_flags |= COLUMN_NETMASK_FLAG;
       }
    }

    if (save_flags & COLUMN_LEASEPERIOD_FLAG) {
       save_flags &= ~COLUMN_LEASEPERIOD_FLAG; /* clear LeasePeriod */
       /*
        * TODO:482:o: |-> commit column LeasePeriod.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpPoolConfigTable column LeasePeriod commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo LeasePeriod
             */
            rowreq_ctx->column_set_flags |= COLUMN_LEASEPERIOD_FLAG;
       }
    }

    if (save_flags & COLUMN_FIRDNSSERVERIPADDRESS_FLAG) {
       save_flags &= ~COLUMN_FIRDNSSERVERIPADDRESS_FLAG; /* clear FirDNSServerIPAddress */
       /*
        * TODO:482:o: |-> commit column FirDNSServerIPAddress.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpPoolConfigTable column FirDNSServerIPAddress commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo FirDNSServerIPAddress
             */
            rowreq_ctx->column_set_flags |= COLUMN_FIRDNSSERVERIPADDRESS_FLAG;
       }
    }

    if (save_flags & COLUMN_SECONDNSSERVERIPADDRESS_FLAG) {
       save_flags &= ~COLUMN_SECONDNSSERVERIPADDRESS_FLAG; /* clear SeconDNSServerIPAddress */
       /*
        * TODO:482:o: |-> commit column SeconDNSServerIPAddress.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpPoolConfigTable column SeconDNSServerIPAddress commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo SeconDNSServerIPAddress
             */
            rowreq_ctx->column_set_flags |= COLUMN_SECONDNSSERVERIPADDRESS_FLAG;
       }
    }

    if (save_flags & COLUMN_DHCPROWSTATUS_FLAG) {
		save_flags &= ~COLUMN_DHCPROWSTATUS_FLAG; /* clear DHCPRowStatus */	   
		switch(rowreq_ctx->data.DHCPRowStatus) 
		{
			case RS_ACTIVE:
				{
					int ret = -1;
					int retconfig = 0;
					int ip_addr = 0;
					unsigned int index = 0,mode = 1;
					unsigned int bkindex = 0;
					char start_ip[32] = { 0 };
					memset(start_ip,0,32);
					char end_ip[32] = { 0 };
					memset(end_ip,0,32);
					char GatewayAddr[32] = { 0 };
					memset(GatewayAddr,0,32);
					char netmask[32] = { 0 };
					memset(netmask,0,32);
					char dns1[32] = { 0 };
					memset(dns1,0,32);
					char dns2[32] = { 0 };
					memset(dns2,0,32);
					int LeasePeriod = 0;
					char lease[32] = { 0 };
					memset(lease,0,32);						
					char dns_ip[128] = { 0 };
					memset(dns_ip,0,128);
					char pool_name[128] = { 0 };
					
					ip_addr = rowreq_ctx->data.IPPollStartAddr;
					INET_NTOA( ip_addr, start_ip );	
					
					ip_addr = rowreq_ctx->data.IPPoolStopAddr;
					INET_NTOA( ip_addr, end_ip );

					ip_addr = rowreq_ctx->data.GatewayAddr;
					INET_NTOA( ip_addr, GatewayAddr );
					
					ip_addr = rowreq_ctx->data.Netmask;
					INET_NTOA( ip_addr, netmask );

					ip_addr = rowreq_ctx->data.FirDNSServerIPAddress;
					INET_NTOA( ip_addr, dns1);

					ip_addr = rowreq_ctx->data.SeconDNSServerIPAddress;
					INET_NTOA( ip_addr, dns2);
					snprintf(dns_ip,sizeof(dns_ip)-1,"%s,%s",dns1,dns2);

					LeasePeriod = rowreq_ctx->data.LeasePeriod;
					
					if(strcmp(rowreq_ctx->data.DHCPPoolName,""))
					{
						strncpy(pool_name,rowreq_ctx->data.DHCPPoolName,sizeof(pool_name)-1);
					}
					
					/*ip pool name*/
					ret=create_ip_pool_name(pool_name, &index, rowreq_ctx->data.slot_id);
					retconfig = config_ip_pool_name(pool_name, &bkindex, rowreq_ctx->data.slot_id);
					if ((1 == ret)||(1 == retconfig))
					{
					    if(1 == retconfig)
						{
							index = bkindex;
						}
						/*add ip pool range*/
						ret=add_dhcp_pool_ip_range("add", start_ip, end_ip, netmask, index , rowreq_ctx->data.slot_id);
						/*add dns*/	
						if(strcmp(dns_ip,"")!=0)
							ip_dhcp_server_dns(mode, index, dns_ip, rowreq_ctx->data.slot_id);
						/*add lease*/
						snprintf(lease,sizeof(lease)-1,"%d",(LeasePeriod/100));
						ip_dhcp_server_lease_default(lease, mode, index, rowreq_ctx->data.slot_id);
						/*add route ip*/
						if(strcmp(GatewayAddr,"")!=0)
						{
							ip_dhcp_server_routers_ip(GatewayAddr, index, mode, rowreq_ctx->data.slot_id);
						}
						ip_dhcp_server_option43(rowreq_ctx->data.DHCPApOption43, mode, index, rowreq_ctx->data.slot_id);
					}

					if ((1 == ret)||(1 == retconfig))
					{   
						rc = MFD_SUCCESS;
						break;				
					}
				}
				rc = -1;
				break;
		   
			case RS_CREATEANDWAIT:
				rowreq_ctx->data.DHCPRowStatus = RS_NOTREADY;
				rc = MFD_SUCCESS;
				break;
				   
			case RS_DESTROY:  
				{
					int ret = 0;
					unsigned int index = 0;
					ret = delete_ip_pool_name(rowreq_ctx->data.DHCPPoolName, &index, rowreq_ctx->data.slot_id);
					if(1 == ret)
					{
						rc = MFD_SUCCESS;							 
						break;
					}
				}
				rowreq_ctx->rowreq_flags &= ~MFD_ROW_DELETED;
				rc = -1;
				break;
			   
			default:
				break;
		}
		  
       /*
        * TODO:482:o: |-> commit column DHCPRowStatus.
        */
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpPoolConfigTable column DHCPRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo DHCPRowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_DHCPROWSTATUS_FLAG;
       }
    }

    if (save_flags & COLUMN_DHCPPOOLNAME_FLAG) {
       save_flags &= ~COLUMN_DHCPPOOLNAME_FLAG; /* clear DHCPPoolName */
       /*
        * TODO:482:o: |-> commit column DHCPPoolName.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpPoolConfigTable column DHCPPoolName commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo DHCPPoolName
             */
            rowreq_ctx->column_set_flags |= COLUMN_DHCPPOOLNAME_FLAG;
       }
    }

    if (save_flags & COLUMN_DHCPAPOPTION43_FLAG) {
       save_flags &= ~COLUMN_DHCPAPOPTION43_FLAG; /* clear DHCPApOption43 */
       /*
        * TODO:482:o: |-> commit column DHCPApOption43.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpPoolConfigTable column DHCPApOption43 commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo DHCPApOption43
             */
            rowreq_ctx->column_set_flags |= COLUMN_DHCPAPOPTION43_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11DHCPIpPoolConfigTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11DHCPIpPoolConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11DHCPIpPoolConfigTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11DHCPIpPoolConfigTable_undo_commit( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:dot11DHCPIpPoolConfigTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11DHCPIpPoolConfigTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11DHCPIpPoolConfigTable_undo_commit */

/*
 * TODO:440:M: Implement dot11DHCPIpPoolConfigTable node value checks.
 * TODO:450:M: Implement dot11DHCPIpPoolConfigTable undo functions.
 * TODO:460:M: Implement dot11DHCPIpPoolConfigTable set functions.
 * TODO:480:M: Implement dot11DHCPIpPoolConfigTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpPoolConfigEntry.IPPollStartAddr
 * IPPollStartAddr is subid 2 of dot11DHCPIpPoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.5.1.2
 * Description:
IP Address pool starting address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param IPPollStartAddr_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpPoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
IPPollStartAddr_check_value( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long IPPollStartAddr_val)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:IPPollStartAddr_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid IPPollStartAddr value.
     */
	if(RS_NOTINSERVICE == rowreq_ctx->data.DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.DHCPRowStatus)
	    return MFD_SUCCESS; 
	else
		return MFD_ERROR; /* IPPollStartAddr value not illegal */
} /* IPPollStartAddr_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpPoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
IPPollStartAddr_undo_setup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:IPPollStartAddr_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup IPPollStartAddr undo.
     */
    /*
     * copy IPPollStartAddr data
     * set rowreq_ctx->undo->IPPollStartAddr from rowreq_ctx->data.IPPollStartAddr
     */
    rowreq_ctx->undo->IPPollStartAddr = rowreq_ctx->data.IPPollStartAddr;


    return MFD_SUCCESS;
} /* IPPollStartAddr_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param IPPollStartAddr_val
 *        A u_long containing the new value.
 */
int
IPPollStartAddr_set( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long IPPollStartAddr_val )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:IPPollStartAddr_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set IPPollStartAddr value.
     * set IPPollStartAddr value in rowreq_ctx->data
     */
    rowreq_ctx->data.IPPollStartAddr = IPPollStartAddr_val;

    return MFD_SUCCESS;
} /* IPPollStartAddr_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
IPPollStartAddr_undo( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:IPPollStartAddr_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up IPPollStartAddr undo.
     */
    /*
     * copy IPPollStartAddr data
     * set rowreq_ctx->data.IPPollStartAddr from rowreq_ctx->undo->IPPollStartAddr
     */
    rowreq_ctx->data.IPPollStartAddr = rowreq_ctx->undo->IPPollStartAddr;

    
    return MFD_SUCCESS;
} /* IPPollStartAddr_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpPoolConfigEntry.IPPoolStopAddr
 * IPPoolStopAddr is subid 3 of dot11DHCPIpPoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.5.1.3
 * Description:
IP Address pool ending address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param IPPoolStopAddr_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpPoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
IPPoolStopAddr_check_value( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long IPPoolStopAddr_val)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:IPPoolStopAddr_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid IPPoolStopAddr value.
     */
    if(RS_NOTINSERVICE == rowreq_ctx->data.DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.DHCPRowStatus)
	    return MFD_SUCCESS; 
	else
		return MFD_ERROR; /* IPPoolStopAddr value not illegal */

} /* IPPoolStopAddr_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpPoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
IPPoolStopAddr_undo_setup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:IPPoolStopAddr_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup IPPoolStopAddr undo.
     */
    /*
     * copy IPPoolStopAddr data
     * set rowreq_ctx->undo->IPPoolStopAddr from rowreq_ctx->data.IPPoolStopAddr
     */
    rowreq_ctx->undo->IPPoolStopAddr = rowreq_ctx->data.IPPoolStopAddr;


    return MFD_SUCCESS;
} /* IPPoolStopAddr_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param IPPoolStopAddr_val
 *        A u_long containing the new value.
 */
int
IPPoolStopAddr_set( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long IPPoolStopAddr_val )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:IPPoolStopAddr_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set IPPoolStopAddr value.
     * set IPPoolStopAddr value in rowreq_ctx->data
     */
    rowreq_ctx->data.IPPoolStopAddr = IPPoolStopAddr_val;

    return MFD_SUCCESS;
} /* IPPoolStopAddr_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
IPPoolStopAddr_undo( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:IPPoolStopAddr_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up IPPoolStopAddr undo.
     */
    /*
     * copy IPPoolStopAddr data
     * set rowreq_ctx->data.IPPoolStopAddr from rowreq_ctx->undo->IPPoolStopAddr
     */
    rowreq_ctx->data.IPPoolStopAddr = rowreq_ctx->undo->IPPoolStopAddr;

    
    return MFD_SUCCESS;
} /* IPPoolStopAddr_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpPoolConfigEntry.GatewayAddr
 * GatewayAddr is subid 4 of dot11DHCPIpPoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.5.1.4
 * Description:
Gateway Address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param GatewayAddr_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpPoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
GatewayAddr_check_value( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long GatewayAddr_val)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:GatewayAddr_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid GatewayAddr value.
     */
	if(RS_NOTINSERVICE == rowreq_ctx->data.DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.DHCPRowStatus)
	    return MFD_SUCCESS; 
	else
		return MFD_ERROR;/* GatewayAddr value not illegal */

} /* GatewayAddr_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpPoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
GatewayAddr_undo_setup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:GatewayAddr_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup GatewayAddr undo.
     */
    /*
     * copy GatewayAddr data
     * set rowreq_ctx->undo->GatewayAddr from rowreq_ctx->data.GatewayAddr
     */
    rowreq_ctx->undo->GatewayAddr = rowreq_ctx->data.GatewayAddr;


    return MFD_SUCCESS;
} /* GatewayAddr_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param GatewayAddr_val
 *        A u_long containing the new value.
 */
int
GatewayAddr_set( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long GatewayAddr_val )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:GatewayAddr_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set GatewayAddr value.
     * set GatewayAddr value in rowreq_ctx->data
     */
    rowreq_ctx->data.GatewayAddr = GatewayAddr_val;

    return MFD_SUCCESS;
} /* GatewayAddr_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
GatewayAddr_undo( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:GatewayAddr_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up GatewayAddr undo.
     */
    /*
     * copy GatewayAddr data
     * set rowreq_ctx->data.GatewayAddr from rowreq_ctx->undo->GatewayAddr
     */
    rowreq_ctx->data.GatewayAddr = rowreq_ctx->undo->GatewayAddr;

    
    return MFD_SUCCESS;
} /* GatewayAddr_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpPoolConfigEntry.Netmask
 * Netmask is subid 5 of dot11DHCPIpPoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.5.1.5
 * Description:
Netmask
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param Netmask_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpPoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
Netmask_check_value( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long Netmask_val)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:Netmask_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid Netmask value.
     */
	if(RS_NOTINSERVICE == rowreq_ctx->data.DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.DHCPRowStatus)
	    return MFD_SUCCESS; 
	else
		return MFD_ERROR;/* Netmask value not illegal */

} /* Netmask_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpPoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
Netmask_undo_setup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:Netmask_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup Netmask undo.
     */
    /*
     * copy Netmask data
     * set rowreq_ctx->undo->Netmask from rowreq_ctx->data.Netmask
     */
    rowreq_ctx->undo->Netmask = rowreq_ctx->data.Netmask;


    return MFD_SUCCESS;
} /* Netmask_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param Netmask_val
 *        A u_long containing the new value.
 */
int
Netmask_set( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long Netmask_val )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:Netmask_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set Netmask value.
     * set Netmask value in rowreq_ctx->data
     */
    rowreq_ctx->data.Netmask = Netmask_val;

    return MFD_SUCCESS;
} /* Netmask_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
Netmask_undo( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:Netmask_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up Netmask undo.
     */
    /*
     * copy Netmask data
     * set rowreq_ctx->data.Netmask from rowreq_ctx->undo->Netmask
     */
    rowreq_ctx->data.Netmask = rowreq_ctx->undo->Netmask;

    
    return MFD_SUCCESS;
} /* Netmask_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpPoolConfigEntry.LeasePeriod
 * LeasePeriod is subid 6 of dot11DHCPIpPoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.5.1.6
 * Description:
IP Address Lease Periods.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param LeasePeriod_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpPoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_TIMETICKS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
LeasePeriod_check_value( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long LeasePeriod_val)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:LeasePeriod_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid LeasePeriod value.
     */
	if(RS_NOTINSERVICE == rowreq_ctx->data.DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.DHCPRowStatus)
	    return MFD_SUCCESS; 
	else
		return MFD_ERROR;/* LeasePeriod value not illegal */
} /* LeasePeriod_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpPoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
LeasePeriod_undo_setup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:LeasePeriod_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup LeasePeriod undo.
     */
    /*
     * copy LeasePeriod data
     * set rowreq_ctx->undo->LeasePeriod from rowreq_ctx->data.LeasePeriod
     */
    rowreq_ctx->undo->LeasePeriod = rowreq_ctx->data.LeasePeriod;


    return MFD_SUCCESS;
} /* LeasePeriod_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param LeasePeriod_val
 *        A u_long containing the new value.
 */
int
LeasePeriod_set( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long LeasePeriod_val )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:LeasePeriod_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set LeasePeriod value.
     * set LeasePeriod value in rowreq_ctx->data
     */
    rowreq_ctx->data.LeasePeriod = LeasePeriod_val;

    return MFD_SUCCESS;
} /* LeasePeriod_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
LeasePeriod_undo( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:LeasePeriod_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up LeasePeriod undo.
     */
    /*
     * copy LeasePeriod data
     * set rowreq_ctx->data.LeasePeriod from rowreq_ctx->undo->LeasePeriod
     */
    rowreq_ctx->data.LeasePeriod = rowreq_ctx->undo->LeasePeriod;

    
    return MFD_SUCCESS;
} /* LeasePeriod_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpPoolConfigEntry.FirDNSServerIPAddress
 * FirDNSServerIPAddress is subid 7 of dot11DHCPIpPoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.5.1.7
 * Description:
IP Address first DNS IP Address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param FirDNSServerIPAddress_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpPoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
FirDNSServerIPAddress_check_value( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long FirDNSServerIPAddress_val)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:FirDNSServerIPAddress_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid FirDNSServerIPAddress value.
     */
	if(RS_NOTINSERVICE == rowreq_ctx->data.DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.DHCPRowStatus)
	    return MFD_SUCCESS; 
	else
		return MFD_ERROR;/* FirDNSServerIPAddress value not illegal */

} /* FirDNSServerIPAddress_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpPoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
FirDNSServerIPAddress_undo_setup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:FirDNSServerIPAddress_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup FirDNSServerIPAddress undo.
     */
    /*
     * copy FirDNSServerIPAddress data
     * set rowreq_ctx->undo->FirDNSServerIPAddress from rowreq_ctx->data.FirDNSServerIPAddress
     */
    rowreq_ctx->undo->FirDNSServerIPAddress = rowreq_ctx->data.FirDNSServerIPAddress;


    return MFD_SUCCESS;
} /* FirDNSServerIPAddress_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param FirDNSServerIPAddress_val
 *        A u_long containing the new value.
 */
int
FirDNSServerIPAddress_set( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long FirDNSServerIPAddress_val )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:FirDNSServerIPAddress_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set FirDNSServerIPAddress value.
     * set FirDNSServerIPAddress value in rowreq_ctx->data
     */
    rowreq_ctx->data.FirDNSServerIPAddress = FirDNSServerIPAddress_val;

    return MFD_SUCCESS;
} /* FirDNSServerIPAddress_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
FirDNSServerIPAddress_undo( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:FirDNSServerIPAddress_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up FirDNSServerIPAddress undo.
     */
    /*
     * copy FirDNSServerIPAddress data
     * set rowreq_ctx->data.FirDNSServerIPAddress from rowreq_ctx->undo->FirDNSServerIPAddress
     */
    rowreq_ctx->data.FirDNSServerIPAddress = rowreq_ctx->undo->FirDNSServerIPAddress;

    
    return MFD_SUCCESS;
} /* FirDNSServerIPAddress_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpPoolConfigEntry.SeconDNSServerIPAddress
 * SeconDNSServerIPAddress is subid 8 of dot11DHCPIpPoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.5.1.8
 * Description:
IP Address second DNS IP Address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SeconDNSServerIPAddress_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpPoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
SeconDNSServerIPAddress_check_value( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long SeconDNSServerIPAddress_val)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:SeconDNSServerIPAddress_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid SeconDNSServerIPAddress value.
     */
	if(RS_NOTINSERVICE == rowreq_ctx->data.DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.DHCPRowStatus)
	    return MFD_SUCCESS; 
	else
		return MFD_ERROR;/* SeconDNSServerIPAddress value not illegal */

} /* SeconDNSServerIPAddress_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpPoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
SeconDNSServerIPAddress_undo_setup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:SeconDNSServerIPAddress_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup SeconDNSServerIPAddress undo.
     */
    /*
     * copy SeconDNSServerIPAddress data
     * set rowreq_ctx->undo->SeconDNSServerIPAddress from rowreq_ctx->data.SeconDNSServerIPAddress
     */
    rowreq_ctx->undo->SeconDNSServerIPAddress = rowreq_ctx->data.SeconDNSServerIPAddress;


    return MFD_SUCCESS;
} /* SeconDNSServerIPAddress_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param SeconDNSServerIPAddress_val
 *        A u_long containing the new value.
 */
int
SeconDNSServerIPAddress_set( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long SeconDNSServerIPAddress_val )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:SeconDNSServerIPAddress_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set SeconDNSServerIPAddress value.
     * set SeconDNSServerIPAddress value in rowreq_ctx->data
     */
    rowreq_ctx->data.SeconDNSServerIPAddress = SeconDNSServerIPAddress_val;

    return MFD_SUCCESS;
} /* SeconDNSServerIPAddress_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
SeconDNSServerIPAddress_undo( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:SeconDNSServerIPAddress_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up SeconDNSServerIPAddress undo.
     */
    /*
     * copy SeconDNSServerIPAddress data
     * set rowreq_ctx->data.SeconDNSServerIPAddress from rowreq_ctx->undo->SeconDNSServerIPAddress
     */
    rowreq_ctx->data.SeconDNSServerIPAddress = rowreq_ctx->undo->SeconDNSServerIPAddress;

    
    return MFD_SUCCESS;
} /* SeconDNSServerIPAddress_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpPoolConfigEntry.DHCPRowStatus
 * DHCPRowStatus is subid 9 of dot11DHCPIpPoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.5.1.9
 * Description:
This variable is used to create, modify, and/or delete a row
			        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param DHCPRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpPoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
DHCPRowStatus_check_value( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long DHCPRowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid DHCPRowStatus value.
     */

    return MFD_SUCCESS; /* DHCPRowStatus value not illegal */
} /* DHCPRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpPoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
DHCPRowStatus_undo_setup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup DHCPRowStatus undo.
     */
    /*
     * copy DHCPRowStatus data
     * set rowreq_ctx->undo->DHCPRowStatus from rowreq_ctx->data.DHCPRowStatus
     */
    rowreq_ctx->undo->DHCPRowStatus = rowreq_ctx->data.DHCPRowStatus;


    return MFD_SUCCESS;
} /* DHCPRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param DHCPRowStatus_val
 *        A long containing the new value.
 */
int
DHCPRowStatus_set( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, u_long DHCPRowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set DHCPRowStatus value.
     * set DHCPRowStatus value in rowreq_ctx->data
     */
    
    int ret = MFD_SUCCESS;
    
    switch(DHCPRowStatus_val) {
    
        case RS_ACTIVE:         
        case RS_DESTROY:
            rowreq_ctx->data.DHCPRowStatus = DHCPRowStatus_val;
            ret = MFD_SUCCESS;
            break;
    
        case RS_NOTINSERVICE:
        case RS_NOTREADY:
        case RS_CREATEANDGO:
            ret = MFD_ERROR;
            break;
    
        case RS_CREATEANDWAIT:
            if(RS_NONEXISTENT == rowreq_ctx->data.DHCPRowStatus) 
			{
                rowreq_ctx->data.DHCPRowStatus = DHCPRowStatus_val;
                rowreq_ctx->data.slot_id = (rowreq_ctx->tbl_idx.DHCPID - 1) / WIRELESS_MAX_NUM + 1;
                ret = MFD_SUCCESS;
            }
            else 
			{
                ret = MFD_ERROR;
            }
            break;
    
        default:
            snmp_log(LOG_WARNING, "unknow %s in DHCPRowStatus_set", DHCPRowStatus_val);
            ret = MFD_ERROR;
            break;
    }

    return ret;    
} /* DHCPRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
DHCPRowStatus_undo( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up DHCPRowStatus undo.
     */
    /*
     * copy DHCPRowStatus data
     * set rowreq_ctx->data.DHCPRowStatus from rowreq_ctx->undo->DHCPRowStatus
     */
    rowreq_ctx->data.DHCPRowStatus = rowreq_ctx->undo->DHCPRowStatus;

    
    return MFD_SUCCESS;
} /* DHCPRowStatus_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpPoolConfigEntry.DHCPPoolName
 * DHCPPoolName is subid 10 of dot11DHCPIpPoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.5.1.10
 * Description:
DHCP pool name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param DHCPPoolName_val_ptr
 *        A char containing the new value.
 * @param DHCPPoolName_val_ptr_len
 *        The size (in bytes) of the data pointed to by DHCPPoolName_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpPoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.DHCPPoolName).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
DHCPPoolName_check_value( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, char *DHCPPoolName_val_ptr,  size_t DHCPPoolName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPPoolName_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != DHCPPoolName_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid DHCPPoolName value.
     */
	if(RS_NOTINSERVICE == rowreq_ctx->data.DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.DHCPRowStatus)
	    return MFD_SUCCESS; 
	else
		return MFD_ERROR; /* DHCPPoolName value not illegal */

} /* DHCPPoolName_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpPoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
DHCPPoolName_undo_setup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPPoolName_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup DHCPPoolName undo.
     */
    /*
     * copy DHCPPoolName and DHCPPoolName_len data
     * set rowreq_ctx->undo->DHCPPoolName from rowreq_ctx->data.DHCPPoolName
     */
    memcpy( rowreq_ctx->undo->DHCPPoolName, rowreq_ctx->data.DHCPPoolName,
            (rowreq_ctx->data.DHCPPoolName_len * sizeof(rowreq_ctx->undo->DHCPPoolName[0])));
    rowreq_ctx->undo->DHCPPoolName_len = rowreq_ctx->data.DHCPPoolName_len;


    return MFD_SUCCESS;
} /* DHCPPoolName_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param DHCPPoolName_val_ptr
 *        A char containing the new value.
 * @param DHCPPoolName_val_ptr_len
 *        The size (in bytes) of the data pointed to by DHCPPoolName_val_ptr
 */
int
DHCPPoolName_set( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, char *DHCPPoolName_val_ptr,  size_t DHCPPoolName_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPPoolName_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != DHCPPoolName_val_ptr);

    /*
     * TODO:461:M: |-> Set DHCPPoolName value.
     * set DHCPPoolName value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.DHCPPoolName, DHCPPoolName_val_ptr, DHCPPoolName_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.DHCPPoolName_len = DHCPPoolName_val_ptr_len / sizeof(DHCPPoolName_val_ptr[0]);

    return MFD_SUCCESS;
} /* DHCPPoolName_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
DHCPPoolName_undo( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPPoolName_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up DHCPPoolName undo.
     */
    /*
     * copy DHCPPoolName and DHCPPoolName_len data
     * set rowreq_ctx->data.DHCPPoolName from rowreq_ctx->undo->DHCPPoolName
     */
    memcpy( rowreq_ctx->data.DHCPPoolName, rowreq_ctx->undo->DHCPPoolName,
            (rowreq_ctx->undo->DHCPPoolName_len * sizeof(rowreq_ctx->data.DHCPPoolName[0])));
    rowreq_ctx->data.DHCPPoolName_len = rowreq_ctx->undo->DHCPPoolName_len;

    
    return MFD_SUCCESS;
} /* DHCPPoolName_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpPoolConfigEntry.DHCPApOption43
 * DHCPApOption43 is subid 11 of dot11DHCPIpPoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.5.1.11
 * Description:
DHCP option43 string.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param DHCPApOption43_val_ptr
 *        A char containing the new value.
 * @param DHCPApOption43_val_ptr_len
 *        The size (in bytes) of the data pointed to by DHCPApOption43_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpPoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.DHCPApOption43).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
DHCPApOption43_check_value( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, char *DHCPApOption43_val_ptr,  size_t DHCPApOption43_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPApOption43_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != DHCPApOption43_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid DHCPApOption43 value.
     */
	if(RS_NOTINSERVICE == rowreq_ctx->data.DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.DHCPRowStatus)
	    return MFD_SUCCESS; 
	else
		return MFD_ERROR; /* DHCPApOption43 value not illegal */

} /* DHCPApOption43_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpPoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpPoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
DHCPApOption43_undo_setup( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPApOption43_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup DHCPApOption43 undo.
     */
    /*
     * copy DHCPApOption43 and DHCPApOption43_len data
     * set rowreq_ctx->undo->DHCPApOption43 from rowreq_ctx->data.DHCPApOption43
     */
    memcpy( rowreq_ctx->undo->DHCPApOption43, rowreq_ctx->data.DHCPApOption43,
            (rowreq_ctx->data.DHCPApOption43_len * sizeof(rowreq_ctx->undo->DHCPApOption43[0])));
    rowreq_ctx->undo->DHCPApOption43_len = rowreq_ctx->data.DHCPApOption43_len;


    return MFD_SUCCESS;
} /* DHCPApOption43_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param DHCPApOption43_val_ptr
 *        A char containing the new value.
 * @param DHCPApOption43_val_ptr_len
 *        The size (in bytes) of the data pointed to by DHCPApOption43_val_ptr
 */
int
DHCPApOption43_set( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx, char *DHCPApOption43_val_ptr,  size_t DHCPApOption43_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPApOption43_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != DHCPApOption43_val_ptr);

    /*
     * TODO:461:M: |-> Set DHCPApOption43 value.
     * set DHCPApOption43 value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.DHCPApOption43, DHCPApOption43_val_ptr, DHCPApOption43_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.DHCPApOption43_len = DHCPApOption43_val_ptr_len / sizeof(DHCPApOption43_val_ptr[0]);

    return MFD_SUCCESS;
} /* DHCPApOption43_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
DHCPApOption43_undo( dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpPoolConfigTable:DHCPApOption43_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up DHCPApOption43 undo.
     */
    /*
     * copy DHCPApOption43 and DHCPApOption43_len data
     * set rowreq_ctx->data.DHCPApOption43 from rowreq_ctx->undo->DHCPApOption43
     */
    memcpy( rowreq_ctx->data.DHCPApOption43, rowreq_ctx->undo->DHCPApOption43,
            (rowreq_ctx->undo->DHCPApOption43_len * sizeof(rowreq_ctx->data.DHCPApOption43[0])));
    rowreq_ctx->data.DHCPApOption43_len = rowreq_ctx->undo->DHCPApOption43_len;

    
    return MFD_SUCCESS;
} /* DHCPApOption43_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11DHCPIpPoolConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11DHCPIpPoolConfigTable if you don't have dependencies)
 */
int
dot11DHCPIpPoolConfigTable_check_dependencies(dot11DHCPIpPoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11DHCPIpPoolConfigTable:dot11DHCPIpPoolConfigTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11DHCPIpPoolConfigTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & COLUMN_DHCPROWSTATUS_FLAG) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->DHCPRowStatus,
                                         rowreq_ctx->data.DHCPRowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_DESTROY ==  rowreq_ctx->data.DHCPRowStatus) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;
            }
            else if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.DHCPRowStatus) {
                if ((rowreq_ctx->column_set_flags & DOT11DHCPIPPOOLCONFIGTABLE_REQUIRED_COLS)
                    != DOT11DHCPIPPOOLCONFIGTABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11DHCPIpPoolConfigTable",
                                "required columns missing (0x%0x != 0x%0x)\n",
                                rowreq_ctx->column_set_flags, DOT11DHCPIPPOOLCONFIGTABLE_REQUIRED_COLS));
                    return MFD_CANNOT_CREATE_NOW;
                }
                rowreq_ctx->data.DHCPRowStatus = ROWSTATUS_ACTIVE;
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.DHCPRowStatus) {
                if (rowreq_ctx->column_set_flags & ~COLUMN_DHCPROWSTATUS_FLAG) {
                    DEBUGMSGTL(("dot11DHCPIpPoolConfigTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;

            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11DHCPIpPoolConfigTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11DHCPIpPoolConfigTable_check_dependencies */

/** @} */
