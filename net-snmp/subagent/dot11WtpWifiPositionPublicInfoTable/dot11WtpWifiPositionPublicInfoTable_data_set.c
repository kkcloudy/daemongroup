/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpWifiPositionPublicInfoTable.h"
#include "ws_dcli_scanlocate.h"

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpWifiPositionPublicInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-WTP-MIB::dot11WtpWifiPositionPublicInfoTable is subid 6 of wtpStation.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.6, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11WtpWifiPositionPublicInfoTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11WtpWifiPositionPublicInfoTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11WtpWifiPositionPublicInfoTable_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:dot11WtpWifiPositionPublicInfoTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11WtpWifiPositionPublicInfoTable undo.
     * set up dot11WtpWifiPositionPublicInfoTable undo information, in preparation for a set.
     * Undo storage is in (* wtpWifiPositionScanSwitch_val_ptr )*
     */

    return rc;
} /* dot11WtpWifiPositionPublicInfoTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11WtpWifiPositionPublicInfoTable_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:dot11WtpWifiPositionPublicInfoTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11WtpWifiPositionPublicInfoTable undo.
     * dot11WtpWifiPositionPublicInfoTable undo information, in response to a failed set.
     * Undo storage is in (* wtpWifiPositionScanSwitch_val_ptr )*
     */

    return rc;
} /* dot11WtpWifiPositionPublicInfoTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWifiPositionPublicInfoTable_undo_cleanup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:dot11WtpWifiPositionPublicInfoTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11WtpWifiPositionPublicInfoTable undo.
     * Undo storage is in (* wtpWifiPositionScanSwitch_val_ptr )*
     */

    return rc;
} /* dot11WtpWifiPositionPublicInfoTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11WtpWifiPositionPublicInfoTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpWifiPositionPublicInfoTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWifiPositionPublicInfoTable_commit( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:dot11WtpWifiPositionPublicInfoTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11WtpWifiPositionPublicInfoTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_WTPWIFIPOSITIONSERVERIP_FLAG) {
       save_flags &= ~COLUMN_WTPWIFIPOSITIONSERVERIP_FLAG; /* clear wtpWifiPositionServerIp */
       /*
        * TODO:482:o: |-> commit column wtpWifiPositionServerIp.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWifiPositionPublicInfoTable column wtpWifiPositionServerIp commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWifiPositionServerIp
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPWIFIPOSITIONSERVERIP_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPWIFIPOSITIONSERVERPORT_FLAG) {
       save_flags &= ~COLUMN_WTPWIFIPOSITIONSERVERPORT_FLAG; /* clear wtpWifiPositionServerPort */
       /*
        * TODO:482:o: |-> commit column wtpWifiPositionServerPort.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWifiPositionPublicInfoTable column wtpWifiPositionServerPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWifiPositionServerPort
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPWIFIPOSITIONSERVERPORT_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPWIFIPOSITIONSCANINTERVAL_FLAG) {
       save_flags &= ~COLUMN_WTPWIFIPOSITIONSCANINTERVAL_FLAG; /* clear wtpWifiPositionScanInterval */
       /*
        * TODO:482:o: |-> commit column wtpWifiPositionScanInterval.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWifiPositionPublicInfoTable column wtpWifiPositionScanInterval commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWifiPositionScanInterval
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPWIFIPOSITIONSCANINTERVAL_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPWIFIPOSITIONSCANTIME_FLAG) {
       save_flags &= ~COLUMN_WTPWIFIPOSITIONSCANTIME_FLAG; /* clear wtpWifiPositionScanTime */
       /*
        * TODO:482:o: |-> commit column wtpWifiPositionScanTime.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWifiPositionPublicInfoTable column wtpWifiPositionScanTime commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWifiPositionScanTime
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPWIFIPOSITIONSCANTIME_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPWIFIPOSITIONSCANCHANNEL_FLAG) {
       save_flags &= ~COLUMN_WTPWIFIPOSITIONSCANCHANNEL_FLAG; /* clear wtpWifiPositionScanChannel */
       /*
        * TODO:482:o: |-> commit column wtpWifiPositionScanChannel.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWifiPositionPublicInfoTable column wtpWifiPositionScanChannel commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWifiPositionScanChannel
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPWIFIPOSITIONSCANCHANNEL_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPWIFIPOSITIONSCANTYPE_FLAG) {
       save_flags &= ~COLUMN_WTPWIFIPOSITIONSCANTYPE_FLAG; /* clear wtpWifiPositionScanType */
       /*
        * TODO:482:o: |-> commit column wtpWifiPositionScanType.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWifiPositionPublicInfoTable column wtpWifiPositionScanType commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWifiPositionScanType
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPWIFIPOSITIONSCANTYPE_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPWIFIPOSITIONDATAREPORTINTERVAL_FLAG) {
       save_flags &= ~COLUMN_WTPWIFIPOSITIONDATAREPORTINTERVAL_FLAG; /* clear wtpWifiPositionDataReportInterval */
       /*
        * TODO:482:o: |-> commit column wtpWifiPositionDataReportInterval.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWifiPositionPublicInfoTable column wtpWifiPositionDataReportInterval commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWifiPositionDataReportInterval
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPWIFIPOSITIONDATAREPORTINTERVAL_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPWIFIPOSITIONRSSITHRESHOLD_FLAG) {
       save_flags &= ~COLUMN_WTPWIFIPOSITIONRSSITHRESHOLD_FLAG; /* clear wtpWifiPositionRssiThreshold */
       /*
        * TODO:482:o: |-> commit column wtpWifiPositionRssiThreshold.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWifiPositionPublicInfoTable column wtpWifiPositionRssiThreshold commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWifiPositionRssiThreshold
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPWIFIPOSITIONRSSITHRESHOLD_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPWIFIPOSITIONSCANSWITCH_FLAG) {
       save_flags &= ~COLUMN_WTPWIFIPOSITIONSCANSWITCH_FLAG; /* clear wtpWifiPositionScanSwitch */
       /*
        * TODO:482:o: |-> commit column wtpWifiPositionScanSwitch.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWifiPositionPublicInfoTable column wtpWifiPositionScanSwitch commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWifiPositionScanSwitch
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPWIFIPOSITIONSCANSWITCH_FLAG;
       }
    }

/*********wangchao add ***********/
    if (save_flags & COLUMN_WTPWIFIPOSITIONVERSION_FLAG) {
       save_flags &= ~COLUMN_WTPWIFIPOSITIONVERSION_FLAG; /* clear wtpWifiPositionVersion */

       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWifiPositionPublicInfoTable column wtpWifiPositionVersion commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWifiPositionScanSwitch
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPWIFIPOSITIONVERSION_FLAG;
       }
    }
	
    if (save_flags & COLUMN_WTPWIFIPOSITIONFILTER_FLAG) {
       save_flags &= ~COLUMN_WTPWIFIPOSITIONFILTER_FLAG; /* clear wtpWifiPositionVersion */

       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWifiPositionPublicInfoTable column wtpWifiPositionfilter commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWifiPositionScanSwitch
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPWIFIPOSITIONFILTER_FLAG;
       }
    }



    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11WtpWifiPositionPublicInfoTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11WtpWifiPositionPublicInfoTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpWifiPositionPublicInfoTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWifiPositionPublicInfoTable_undo_commit( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:dot11WtpWifiPositionPublicInfoTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11WtpWifiPositionPublicInfoTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11WtpWifiPositionPublicInfoTable_undo_commit */

/*
 * TODO:440:M: Implement dot11WtpWifiPositionPublicInfoTable node value checks.
 * TODO:450:M: Implement dot11WtpWifiPositionPublicInfoTable undo functions.
 * TODO:460:M: Implement dot11WtpWifiPositionPublicInfoTable set functions.
 * TODO:480:M: Implement dot11WtpWifiPositionPublicInfoTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWifiPositionPublicInfoEntry.wtpWifiPositionServerIp
 * wtpWifiPositionServerIp is subid 2 of dot11WtpWifiPositionPublicInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.6.1.2
 * Description:
wtp wifi position server ip.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWifiPositionServerIp_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWifiPositionPublicInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWifiPositionServerIp_check_value( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpWifiPositionServerIp_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionServerIp_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWifiPositionServerIp value.
     */

    return MFD_SUCCESS; /* wtpWifiPositionServerIp value not illegal */
} /* wtpWifiPositionServerIp_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWifiPositionPublicInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWifiPositionServerIp_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionServerIp_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWifiPositionServerIp undo.
     */
    /*
     * copy wtpWifiPositionServerIp data
     * set rowreq_ctx->undo->wtpWifiPositionServerIp from rowreq_ctx->data.wtpWifiPositionServerIp
     */
    rowreq_ctx->undo->wtpWifiPositionServerIp = rowreq_ctx->data.wtpWifiPositionServerIp;


    return MFD_SUCCESS;
} /* wtpWifiPositionServerIp_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWifiPositionServerIp_val
 *        A u_long containing the new value.
 */
int
wtpWifiPositionServerIp_set( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpWifiPositionServerIp_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionServerIp_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpWifiPositionServerIp value.
     * set wtpWifiPositionServerIp value in rowreq_ctx->data
     */    
	int rc = MFD_ERROR;
	int ret = 0;
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	ret = set_wifi_locate_change_server_ip_cmd(rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.instanceID,rowreq_ctx->data.wtp_mac,wtpWifiPositionServerIp_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWifiPositionServerIp = wtpWifiPositionServerIp_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWifiPositionServerIp_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWifiPositionServerIp_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionServerIp_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWifiPositionServerIp undo.
     */
    /*
     * copy wtpWifiPositionServerIp data
     * set rowreq_ctx->data.wtpWifiPositionServerIp from rowreq_ctx->undo->wtpWifiPositionServerIp
     */
    rowreq_ctx->data.wtpWifiPositionServerIp = rowreq_ctx->undo->wtpWifiPositionServerIp;

    
    return MFD_SUCCESS;
} /* wtpWifiPositionServerIp_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWifiPositionPublicInfoEntry.wtpWifiPositionServerPort
 * wtpWifiPositionServerPort is subid 3 of dot11WtpWifiPositionPublicInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.6.1.3
 * Description:
wtp wifi position server port.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWifiPositionServerPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWifiPositionPublicInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWifiPositionServerPort_check_value( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, long wtpWifiPositionServerPort_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionServerPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWifiPositionServerPort value.
     */

    return MFD_SUCCESS; /* wtpWifiPositionServerPort value not illegal */
} /* wtpWifiPositionServerPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWifiPositionPublicInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWifiPositionServerPort_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionServerPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWifiPositionServerPort undo.
     */
    /*
     * copy wtpWifiPositionServerPort data
     * set rowreq_ctx->undo->wtpWifiPositionServerPort from rowreq_ctx->data.wtpWifiPositionServerPort
     */
    rowreq_ctx->undo->wtpWifiPositionServerPort = rowreq_ctx->data.wtpWifiPositionServerPort;


    return MFD_SUCCESS;
} /* wtpWifiPositionServerPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWifiPositionServerPort_val
 *        A long containing the new value.
 */
int
wtpWifiPositionServerPort_set( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, long wtpWifiPositionServerPort_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionServerPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpWifiPositionServerPort value.
     * set wtpWifiPositionServerPort value in rowreq_ctx->data
     */    
	int rc = MFD_ERROR;
	int ret = 0;
	void *connection = NULL;

    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;	
	
	ret = set_wifi_locate_change_server_port_cmd(rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.instanceID,rowreq_ctx->data.wtp_mac,wtpWifiPositionServerPort_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection);	

	if(ret == 1)
	{
		rowreq_ctx->data.wtpWifiPositionServerPort = wtpWifiPositionServerPort_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWifiPositionServerPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWifiPositionServerPort_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionServerPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWifiPositionServerPort undo.
     */
    /*
     * copy wtpWifiPositionServerPort data
     * set rowreq_ctx->data.wtpWifiPositionServerPort from rowreq_ctx->undo->wtpWifiPositionServerPort
     */
    rowreq_ctx->data.wtpWifiPositionServerPort = rowreq_ctx->undo->wtpWifiPositionServerPort;

    
    return MFD_SUCCESS;
} /* wtpWifiPositionServerPort_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWifiPositionPublicInfoEntry.wtpWifiPositionScanInterval
 * wtpWifiPositionScanInterval is subid 4 of dot11WtpWifiPositionPublicInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.6.1.4
 * Description:
wtp wifi position scan interval.(unit:ms)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 2000;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWifiPositionScanInterval_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWifiPositionPublicInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - 2000
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWifiPositionScanInterval_check_value( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, long wtpWifiPositionScanInterval_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanInterval_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWifiPositionScanInterval value.
     */

    return MFD_SUCCESS; /* wtpWifiPositionScanInterval value not illegal */
} /* wtpWifiPositionScanInterval_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWifiPositionPublicInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWifiPositionScanInterval_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanInterval_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWifiPositionScanInterval undo.
     */
    /*
     * copy wtpWifiPositionScanInterval data
     * set rowreq_ctx->undo->wtpWifiPositionScanInterval from rowreq_ctx->data.wtpWifiPositionScanInterval
     */
    rowreq_ctx->undo->wtpWifiPositionScanInterval = rowreq_ctx->data.wtpWifiPositionScanInterval;


    return MFD_SUCCESS;
} /* wtpWifiPositionScanInterval_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWifiPositionScanInterval_val
 *        A long containing the new value.
 */
int
wtpWifiPositionScanInterval_set( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, long wtpWifiPositionScanInterval_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanInterval_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpWifiPositionScanInterval value.
     * set wtpWifiPositionScanInterval value in rowreq_ctx->data
     */	
	int rc = MFD_ERROR;
	int ret = 0;
	void *connection = NULL;

    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;	
			
	ret = set_wifi_locate_change_channel_scan_interval_cmd(rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.instanceID,rowreq_ctx->data.wtp_mac,wtpWifiPositionScanInterval_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection); 
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWifiPositionScanInterval = wtpWifiPositionScanInterval_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWifiPositionScanInterval_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWifiPositionScanInterval_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanInterval_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWifiPositionScanInterval undo.
     */
    /*
     * copy wtpWifiPositionScanInterval data
     * set rowreq_ctx->data.wtpWifiPositionScanInterval from rowreq_ctx->undo->wtpWifiPositionScanInterval
     */
    rowreq_ctx->data.wtpWifiPositionScanInterval = rowreq_ctx->undo->wtpWifiPositionScanInterval;

    
    return MFD_SUCCESS;
} /* wtpWifiPositionScanInterval_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWifiPositionPublicInfoEntry.wtpWifiPositionScanTime
 * wtpWifiPositionScanTime is subid 5 of dot11WtpWifiPositionPublicInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.6.1.5
 * Description:
wtp wifi position scan time.(unit:ms)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 2000;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWifiPositionScanTime_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWifiPositionPublicInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 2000
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWifiPositionScanTime_check_value( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, long wtpWifiPositionScanTime_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanTime_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWifiPositionScanTime value.
     */

    return MFD_SUCCESS; /* wtpWifiPositionScanTime value not illegal */
} /* wtpWifiPositionScanTime_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWifiPositionPublicInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWifiPositionScanTime_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanTime_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWifiPositionScanTime undo.
     */
    /*
     * copy wtpWifiPositionScanTime data
     * set rowreq_ctx->undo->wtpWifiPositionScanTime from rowreq_ctx->data.wtpWifiPositionScanTime
     */
    rowreq_ctx->undo->wtpWifiPositionScanTime = rowreq_ctx->data.wtpWifiPositionScanTime;


    return MFD_SUCCESS;
} /* wtpWifiPositionScanTime_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWifiPositionScanTime_val
 *        A long containing the new value.
 */
int
wtpWifiPositionScanTime_set( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, long wtpWifiPositionScanTime_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanTime_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpWifiPositionScanTime value.
     * set wtpWifiPositionScanTime value in rowreq_ctx->data
     */    
	int rc = MFD_ERROR;
	int ret = 0;
	void *connection = NULL;

    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;	
				
	ret = set_wifi_locate_change_channel_scan_time_cmd(rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.instanceID,rowreq_ctx->data.wtp_mac,wtpWifiPositionScanTime_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection); 
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWifiPositionScanTime = wtpWifiPositionScanTime_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWifiPositionScanTime_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWifiPositionScanTime_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanTime_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWifiPositionScanTime undo.
     */
    /*
     * copy wtpWifiPositionScanTime data
     * set rowreq_ctx->data.wtpWifiPositionScanTime from rowreq_ctx->undo->wtpWifiPositionScanTime
     */
    rowreq_ctx->data.wtpWifiPositionScanTime = rowreq_ctx->undo->wtpWifiPositionScanTime;

    
    return MFD_SUCCESS;
} /* wtpWifiPositionScanTime_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWifiPositionPublicInfoEntry.wtpWifiPositionScanChannel
 * wtpWifiPositionScanChannel is subid 6 of dot11WtpWifiPositionPublicInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.6.1.6
 * Description:
wtp wifi position scan channel, default scan all channel.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWifiPositionScanChannel_val
 *        A U64 containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWifiPositionPublicInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_COUNTER64
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWifiPositionScanChannel_check_value( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, unsigned long long wtpWifiPositionScanChannel_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanChannel_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWifiPositionScanChannel value.
     */

    return MFD_SUCCESS; /* wtpWifiPositionScanChannel value not illegal */
} /* wtpWifiPositionScanChannel_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWifiPositionPublicInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWifiPositionScanChannel_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanChannel_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWifiPositionScanChannel undo.
     */
    /*
     * copy wtpWifiPositionScanChannel data
     * set rowreq_ctx->undo->wtpWifiPositionScanChannel from rowreq_ctx->data.wtpWifiPositionScanChannel
     */
    rowreq_ctx->undo->wtpWifiPositionScanChannel = rowreq_ctx->data.wtpWifiPositionScanChannel;


    return MFD_SUCCESS;
} /* wtpWifiPositionScanChannel_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWifiPositionScanChannel_val
 *        A U64 containing the new value.
 */
int
wtpWifiPositionScanChannel_set( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, unsigned long long wtpWifiPositionScanChannel_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanChannel_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpWifiPositionScanChannel value.
     * set wtpWifiPositionScanChannel value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	int ret = 0;
	void *connection = NULL;

    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;	
				
	
	ret = set_wifi_locate_change_channellist_cmd(rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.instanceID,rowreq_ctx->data.wtp_mac,wtpWifiPositionScanChannel_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection); 
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpWifiPositionScanChannel = wtpWifiPositionScanChannel_val;		
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWifiPositionScanChannel_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWifiPositionScanChannel_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanChannel_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWifiPositionScanChannel undo.
     */
    /*
     * copy wtpWifiPositionScanChannel data
     * set rowreq_ctx->data.wtpWifiPositionScanChannel from rowreq_ctx->undo->wtpWifiPositionScanChannel
     */
    rowreq_ctx->data.wtpWifiPositionScanChannel = rowreq_ctx->undo->wtpWifiPositionScanChannel;

    
    return MFD_SUCCESS;
} /* wtpWifiPositionScanChannel_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWifiPositionPublicInfoEntry.wtpWifiPositionScanType
 * wtpWifiPositionScanType is subid 7 of dot11WtpWifiPositionPublicInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.6.1.7
 * Description:
wtp wifi position scan type, default type is all.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  All(0), Associate(1), NonAssociated(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWifiPositionScanType_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWifiPositionPublicInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  All(0), Associate(1), NonAssociated(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWifiPositionScanType_check_value( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpWifiPositionScanType_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanType_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWifiPositionScanType value.
     */

    return MFD_SUCCESS; /* wtpWifiPositionScanType value not illegal */
} /* wtpWifiPositionScanType_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWifiPositionPublicInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWifiPositionScanType_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanType_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWifiPositionScanType undo.
     */
    /*
     * copy wtpWifiPositionScanType data
     * set rowreq_ctx->undo->wtpWifiPositionScanType from rowreq_ctx->data.wtpWifiPositionScanType
     */
    rowreq_ctx->undo->wtpWifiPositionScanType = rowreq_ctx->data.wtpWifiPositionScanType;


    return MFD_SUCCESS;
} /* wtpWifiPositionScanType_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWifiPositionScanType_val
 *        A long containing the new value.
 */
int
wtpWifiPositionScanType_set( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpWifiPositionScanType_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanType_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpWifiPositionScanType value.
     * set wtpWifiPositionScanType value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	int ret = 0;
	void *connection = NULL;

    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;	
					
	ret = set_wifi_locate_change_scan_type_cmd(rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.instanceID,rowreq_ctx->data.wtp_mac,wtpWifiPositionScanType_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection); 
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpWifiPositionScanType = wtpWifiPositionScanType_val;		
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWifiPositionScanType_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWifiPositionScanType_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanType_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWifiPositionScanType undo.
     */
    /*
     * copy wtpWifiPositionScanType data
     * set rowreq_ctx->data.wtpWifiPositionScanType from rowreq_ctx->undo->wtpWifiPositionScanType
     */
    rowreq_ctx->data.wtpWifiPositionScanType = rowreq_ctx->undo->wtpWifiPositionScanType;

    
    return MFD_SUCCESS;
} /* wtpWifiPositionScanType_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWifiPositionPublicInfoEntry.wtpWifiPositionDataReportInterval
 * wtpWifiPositionDataReportInterval is subid 8 of dot11WtpWifiPositionPublicInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.6.1.8
 * Description:
wtp wifi position data report interval.(unit:ms)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1000 - 20000;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWifiPositionDataReportInterval_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWifiPositionPublicInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1000 - 20000
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWifiPositionDataReportInterval_check_value( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, long wtpWifiPositionDataReportInterval_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionDataReportInterval_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWifiPositionDataReportInterval value.
     */

    return MFD_SUCCESS; /* wtpWifiPositionDataReportInterval value not illegal */
} /* wtpWifiPositionDataReportInterval_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWifiPositionPublicInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWifiPositionDataReportInterval_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionDataReportInterval_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWifiPositionDataReportInterval undo.
     */
    /*
     * copy wtpWifiPositionDataReportInterval data
     * set rowreq_ctx->undo->wtpWifiPositionDataReportInterval from rowreq_ctx->data.wtpWifiPositionDataReportInterval
     */
    rowreq_ctx->undo->wtpWifiPositionDataReportInterval = rowreq_ctx->data.wtpWifiPositionDataReportInterval;


    return MFD_SUCCESS;
} /* wtpWifiPositionDataReportInterval_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWifiPositionDataReportInterval_val
 *        A long containing the new value.
 */
int
wtpWifiPositionDataReportInterval_set( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, long wtpWifiPositionDataReportInterval_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionDataReportInterval_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpWifiPositionDataReportInterval value.
     * set wtpWifiPositionDataReportInterval value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	int ret = 0;
	void *connection = NULL;

    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;	
					
	ret = set_wifi_locate_change_report_interval_cmd(rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.instanceID,rowreq_ctx->data.wtp_mac,wtpWifiPositionDataReportInterval_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection); 
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpWifiPositionDataReportInterval = wtpWifiPositionDataReportInterval_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWifiPositionDataReportInterval_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWifiPositionDataReportInterval_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionDataReportInterval_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWifiPositionDataReportInterval undo.
     */
    /*
     * copy wtpWifiPositionDataReportInterval data
     * set rowreq_ctx->data.wtpWifiPositionDataReportInterval from rowreq_ctx->undo->wtpWifiPositionDataReportInterval
     */
    rowreq_ctx->data.wtpWifiPositionDataReportInterval = rowreq_ctx->undo->wtpWifiPositionDataReportInterval;

    
    return MFD_SUCCESS;
} /* wtpWifiPositionDataReportInterval_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWifiPositionPublicInfoEntry.wtpWifiPositionRssiThreshold
 * wtpWifiPositionRssiThreshold is subid 9 of dot11WtpWifiPositionPublicInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.6.1.9
 * Description:
wtp wifi position rssi threshold.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 95;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWifiPositionRssiThreshold_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWifiPositionPublicInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - 95
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWifiPositionRssiThreshold_check_value( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, long wtpWifiPositionRssiThreshold_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionRssiThreshold_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWifiPositionRssiThreshold value.
     */

    return MFD_SUCCESS; /* wtpWifiPositionRssiThreshold value not illegal */
} /* wtpWifiPositionRssiThreshold_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWifiPositionPublicInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWifiPositionRssiThreshold_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionRssiThreshold_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWifiPositionRssiThreshold undo.
     */
    /*
     * copy wtpWifiPositionRssiThreshold data
     * set rowreq_ctx->undo->wtpWifiPositionRssiThreshold from rowreq_ctx->data.wtpWifiPositionRssiThreshold
     */
    rowreq_ctx->undo->wtpWifiPositionRssiThreshold = rowreq_ctx->data.wtpWifiPositionRssiThreshold;


    return MFD_SUCCESS;
} /* wtpWifiPositionRssiThreshold_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWifiPositionRssiThreshold_val
 *        A long containing the new value.
 */
int
wtpWifiPositionRssiThreshold_set( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, long wtpWifiPositionRssiThreshold_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionRssiThreshold_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpWifiPositionRssiThreshold value.
     * set wtpWifiPositionRssiThreshold value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	int ret = 0;
	void *connection = NULL;

    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;	
						
	ret = set_wifi_locate_change_rssi_cmd(rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.instanceID,rowreq_ctx->data.wtp_mac,wtpWifiPositionRssiThreshold_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection); 
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpWifiPositionRssiThreshold = wtpWifiPositionRssiThreshold_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWifiPositionRssiThreshold_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWifiPositionRssiThreshold_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionRssiThreshold_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWifiPositionRssiThreshold undo.
     */
    /*
     * copy wtpWifiPositionRssiThreshold data
     * set rowreq_ctx->data.wtpWifiPositionRssiThreshold from rowreq_ctx->undo->wtpWifiPositionRssiThreshold
     */
    rowreq_ctx->data.wtpWifiPositionRssiThreshold = rowreq_ctx->undo->wtpWifiPositionRssiThreshold;

    
    return MFD_SUCCESS;
} /* wtpWifiPositionRssiThreshold_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWifiPositionPublicInfoEntry.wtpWifiPositionScanSwitch
 * wtpWifiPositionScanSwitch is subid 10 of dot11WtpWifiPositionPublicInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.6.1.10
 * Description:
wtp wifi position scan switch.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  off(0), on(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWifiPositionScanSwitch_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWifiPositionPublicInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  off(0), on(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWifiPositionScanSwitch_check_value( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpWifiPositionScanSwitch_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanSwitch_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWifiPositionScanSwitch value.
     */

    return MFD_SUCCESS; /* wtpWifiPositionScanSwitch value not illegal */
} /* wtpWifiPositionScanSwitch_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWifiPositionPublicInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWifiPositionPublicInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWifiPositionScanSwitch_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanSwitch_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWifiPositionScanSwitch undo.
     */
    /*
     * copy wtpWifiPositionScanSwitch data
     * set rowreq_ctx->undo->wtpWifiPositionScanSwitch from rowreq_ctx->data.wtpWifiPositionScanSwitch
     */
    rowreq_ctx->undo->wtpWifiPositionScanSwitch = rowreq_ctx->data.wtpWifiPositionScanSwitch;


    return MFD_SUCCESS;
} /* wtpWifiPositionScanSwitch_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWifiPositionScanSwitch_val
 *        A long containing the new value.
 */
int
wtpWifiPositionScanSwitch_set( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpWifiPositionScanSwitch_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanSwitch_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpWifiPositionScanSwitch value.
     * set wtpWifiPositionScanSwitch value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	int ret = 0;
	void *connection = NULL;

    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;		
	
	ret = set_wifi_locate_change_on_off_cmd(rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.instanceID,rowreq_ctx->data.wtp_mac,wtpWifiPositionScanSwitch_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection); 
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpWifiPositionScanSwitch = wtpWifiPositionScanSwitch_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWifiPositionScanSwitch_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWifiPositionScanSwitch_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionScanSwitch_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWifiPositionScanSwitch undo.
     */
    /*
     * copy wtpWifiPositionScanSwitch data
     * set rowreq_ctx->data.wtpWifiPositionScanSwitch from rowreq_ctx->undo->wtpWifiPositionScanSwitch
     */
    rowreq_ctx->data.wtpWifiPositionScanSwitch = rowreq_ctx->undo->wtpWifiPositionScanSwitch;

    
    return MFD_SUCCESS;
} /* wtpWifiPositionScanSwitch_undo */




/*************wangchao add for wifi_locate ********/

int
wtpWifiPositionVersion_check_value( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpWifiPositionScanVersion_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionVersion_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWifiPositionScanSwitch value.
     */

    return MFD_SUCCESS; /* wtpWifiPositionScanSwitch value not illegal */
} /* wtpWifiPositionScanSwitch_check_value */



int
wtpWifiPositionVersion_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionVersion_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWifiPositionScanSwitch undo.
     */
    /*
     * copy wtpWifiPositionScanSwitch data
     * set rowreq_ctx->undo->wtpWifiPositionScanSwitch from rowreq_ctx->data.wtpWifiPositionScanSwitch
     */
    rowreq_ctx->undo->wtpWifiPositionVersion = rowreq_ctx->data.wtpWifiPositionVersion;


    return MFD_SUCCESS;
} /* wtpWifiPositionScanSwitch_undo_setup */


int
wtpWifiPositionVersion_set( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpWifiPositionVersion_val)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionVersion_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpWifiPositionScanSwitch value.
     * set wtpWifiPositionScanSwitch value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	int ret = 0;
	void *connection = NULL;

    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;		

	ret = set_wifi_locate_change_version_cmd(rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.instanceID,rowreq_ctx->data.wtp_mac,wtpWifiPositionVersion_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection); 
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpWifiPositionVersion = wtpWifiPositionVersion_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWifiPositionScanSwitch_set */


int
wtpWifiPositionVersion_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionVersion_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWifiPositionScanSwitch undo.
     */
    /*
     * copy wtpWifiPositionScanSwitch data
     * set rowreq_ctx->data.wtpWifiPositionScanSwitch from rowreq_ctx->undo->wtpWifiPositionScanSwitch
     */
    rowreq_ctx->data.wtpWifiPositionVersion = rowreq_ctx->undo->wtpWifiPositionVersion;

    
    return MFD_SUCCESS;
} /* wtpWifiPositionScanSwitch_undo */


int
wtpWifiPositionFilter_check_value( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpWifiPositionScanVersion_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionFilter_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWifiPositionScanSwitch value.
     */

    return MFD_SUCCESS; /* wtpWifiPositionScanSwitch value not illegal */
} /* wtpWifiPositionScanSwitch_check_value */



int
wtpWifiPositionFilter_undo_setup( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionFilter_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWifiPositionScanSwitch undo.
     */
    /*
     * copy wtpWifiPositionScanSwitch data
     * set rowreq_ctx->undo->wtpWifiPositionScanSwitch from rowreq_ctx->data.wtpWifiPositionScanSwitch
     */
    rowreq_ctx->undo->wtpWifiPositionFilter = rowreq_ctx->data.wtpWifiPositionFilter;


    return MFD_SUCCESS;
} /* wtpWifiPositionScanSwitch_undo_setup */


int
wtpWifiPositionFilter_set( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpWifiPositionFilter_val)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionFilter_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpWifiPositionScanSwitch value.
     * set wtpWifiPositionScanSwitch value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	int ret = 0;
    void *connection = NULL;

    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;		

	
	ret = set_wifi_locate_change_filter_cmd(rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.instanceID,rowreq_ctx->data.wtp_mac,wtpWifiPositionFilter_val,rowreq_ctx->tbl_idx.WifiPositionFrequency,connection); 
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpWifiPositionFilter = wtpWifiPositionFilter_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWifiPositionScanSwitch_set */


int
wtpWifiPositionFilter_undo( dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWifiPositionPublicInfoTable:wtpWifiPositionFilter_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWifiPositionScanSwitch undo.
     */
    /*
     * copy wtpWifiPositionScanSwitch data
     * set rowreq_ctx->data.wtpWifiPositionScanSwitch from rowreq_ctx->undo->wtpWifiPositionScanSwitch
     */
    rowreq_ctx->data.wtpWifiPositionFilter = rowreq_ctx->undo->wtpWifiPositionFilter;

    
    return MFD_SUCCESS;
} /* wtpWifiPositionScanSwitch_undo */



/*****************************************************************/


/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11WtpWifiPositionPublicInfoTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11WtpWifiPositionPublicInfoTable if you don't have dependencies)
 */
int
dot11WtpWifiPositionPublicInfoTable_check_dependencies(dot11WtpWifiPositionPublicInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11WtpWifiPositionPublicInfoTable:dot11WtpWifiPositionPublicInfoTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11WtpWifiPositionPublicInfoTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11WtpWifiPositionPublicInfoTable_check_dependencies */

/** @} */
