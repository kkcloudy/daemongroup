/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11AcPhyPortTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11AcPhyPortTable get routines.
 * TODO:240:M: Implement dot11AcPhyPortTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11AcPhyPortTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11AcPhyPortTable is subid 6 of dot11AcInterface.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.6, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11AcPhyPortTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param PhyPortIndex_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11AcPhyPortTable_indexes_set_tbl_idx(dot11AcPhyPortTable_mib_index *tbl_idx, long PhyPortIndex_val)
{
    DEBUGMSGTL(("verbose:dot11AcPhyPortTable:dot11AcPhyPortTable_indexes_set_tbl_idx","called\n"));

    /* PhyPortIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->PhyPortIndex = PhyPortIndex_val;
    

    return MFD_SUCCESS;
} /* dot11AcPhyPortTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11AcPhyPortTable_indexes_set(dot11AcPhyPortTable_rowreq_ctx *rowreq_ctx, long PhyPortIndex_val)
{
    DEBUGMSGTL(("verbose:dot11AcPhyPortTable:dot11AcPhyPortTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11AcPhyPortTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , PhyPortIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11AcPhyPortTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11AcPhyPortTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcPhyPortEntry.PhyPortDescr
 * PhyPortDescr is subid 2 of dot11AcPhyPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.6.1.2
 * Description:
Physical port description.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the PhyPortDescr data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PhyPortDescr_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param PhyPortDescr_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by PhyPortDescr.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*PhyPortDescr_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update PhyPortDescr_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
PhyPortDescr_get( dot11AcPhyPortTable_rowreq_ctx *rowreq_ctx, char **PhyPortDescr_val_ptr_ptr, size_t *PhyPortDescr_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != PhyPortDescr_val_ptr_ptr) && (NULL != *PhyPortDescr_val_ptr_ptr));
   netsnmp_assert( NULL != PhyPortDescr_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11AcPhyPortTable:PhyPortDescr_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PhyPortDescr data.
 * copy (* PhyPortDescr_val_ptr_ptr ) data and (* PhyPortDescr_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for PhyPortDescr data
     */
    if ((NULL == (* PhyPortDescr_val_ptr_ptr )) ||
        ((* PhyPortDescr_val_ptr_len_ptr ) <
         (rowreq_ctx->data.PhyPortDescr_len* sizeof(rowreq_ctx->data.PhyPortDescr[0])))) {
        /*
         * allocate space for PhyPortDescr data
         */
        (* PhyPortDescr_val_ptr_ptr ) = malloc(rowreq_ctx->data.PhyPortDescr_len* sizeof(rowreq_ctx->data.PhyPortDescr[0]));
        if(NULL == (* PhyPortDescr_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* PhyPortDescr_val_ptr_len_ptr ) = rowreq_ctx->data.PhyPortDescr_len* sizeof(rowreq_ctx->data.PhyPortDescr[0]);
    memcpy( (* PhyPortDescr_val_ptr_ptr ), rowreq_ctx->data.PhyPortDescr, rowreq_ctx->data.PhyPortDescr_len* sizeof(rowreq_ctx->data.PhyPortDescr[0]) );

    return MFD_SUCCESS;
} /* PhyPortDescr_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcPhyPortEntry.PhyPortType
 * PhyPortType is subid 3 of dot11AcPhyPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.6.1.3
 * Description:
Physical port type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 8/16. Values:  ETH_INVALID(1), ETH_FE_TX(2), ETH_FE_FIBER(3), ETH_GTX(4), ETH_GE_FIBER(5), ETH_GE_SFP(6), ETH_XGE_XFP(7), ETH_XGTX(8), ETH_XGE_FIBER(9)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the PhyPortType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PhyPortType_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
PhyPortType_get( dot11AcPhyPortTable_rowreq_ctx *rowreq_ctx, u_long * PhyPortType_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != PhyPortType_val_ptr );


    DEBUGMSGTL(("verbose:dot11AcPhyPortTable:PhyPortType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PhyPortType data.
 * copy (* PhyPortType_val_ptr ) from rowreq_ctx->data
 */
    (* PhyPortType_val_ptr ) = rowreq_ctx->data.PhyPortType;

    return MFD_SUCCESS;
} /* PhyPortType_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcPhyPortEntry.PhyPortMTU
 * PhyPortMTU is subid 4 of dot11AcPhyPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.6.1.4
 * Description:
Physical port MTU.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the PhyPortMTU data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PhyPortMTU_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
PhyPortMTU_get( dot11AcPhyPortTable_rowreq_ctx *rowreq_ctx, long * PhyPortMTU_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != PhyPortMTU_val_ptr );


    DEBUGMSGTL(("verbose:dot11AcPhyPortTable:PhyPortMTU_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PhyPortMTU data.
 * copy (* PhyPortMTU_val_ptr ) from rowreq_ctx->data
 */
    (* PhyPortMTU_val_ptr ) = rowreq_ctx->data.PhyPortMTU;

    return MFD_SUCCESS;
} /* PhyPortMTU_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcPhyPortEntry.PhyPortSpeed
 * PhyPortSpeed is subid 5 of dot11AcPhyPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.6.1.5
 * Description:
Physical port speed.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is GAUGE (based on perltype GAUGE)
 * The net-snmp type is ASN_GAUGE. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the PhyPortSpeed data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PhyPortSpeed_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
PhyPortSpeed_get( dot11AcPhyPortTable_rowreq_ctx *rowreq_ctx, u_long * PhyPortSpeed_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != PhyPortSpeed_val_ptr );


    DEBUGMSGTL(("verbose:dot11AcPhyPortTable:PhyPortSpeed_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PhyPortSpeed data.
 * copy (* PhyPortSpeed_val_ptr ) from rowreq_ctx->data
 */
    (* PhyPortSpeed_val_ptr ) = rowreq_ctx->data.PhyPortSpeed;

    return MFD_SUCCESS;
} /* PhyPortSpeed_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcPhyPortEntry.PhyPortPhysAddress
 * PhyPortPhysAddress is subid 6 of dot11AcPhyPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.6.1.6
 * Description:
Physical port physic address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   1
 *   settable   0
 *   hint: 1x:
 *
 *
 * Its syntax is PhysAddress (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the PhyPortPhysAddress data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PhyPortPhysAddress_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param PhyPortPhysAddress_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by PhyPortPhysAddress.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*PhyPortPhysAddress_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update PhyPortPhysAddress_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
PhyPortPhysAddress_get( dot11AcPhyPortTable_rowreq_ctx *rowreq_ctx, char **PhyPortPhysAddress_val_ptr_ptr, size_t *PhyPortPhysAddress_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != PhyPortPhysAddress_val_ptr_ptr) && (NULL != *PhyPortPhysAddress_val_ptr_ptr));
   netsnmp_assert( NULL != PhyPortPhysAddress_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11AcPhyPortTable:PhyPortPhysAddress_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PhyPortPhysAddress data.
 * copy (* PhyPortPhysAddress_val_ptr_ptr ) data and (* PhyPortPhysAddress_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for PhyPortPhysAddress data
     */
    if ((NULL == (* PhyPortPhysAddress_val_ptr_ptr )) ||
        ((* PhyPortPhysAddress_val_ptr_len_ptr ) <
         (rowreq_ctx->data.PhyPortPhysAddress_len* sizeof(rowreq_ctx->data.PhyPortPhysAddress[0])))) {
        /*
         * allocate space for PhyPortPhysAddress data
         */
        (* PhyPortPhysAddress_val_ptr_ptr ) = malloc(rowreq_ctx->data.PhyPortPhysAddress_len* sizeof(rowreq_ctx->data.PhyPortPhysAddress[0]));
        if(NULL == (* PhyPortPhysAddress_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* PhyPortPhysAddress_val_ptr_len_ptr ) = rowreq_ctx->data.PhyPortPhysAddress_len* sizeof(rowreq_ctx->data.PhyPortPhysAddress[0]);
    memcpy( (* PhyPortPhysAddress_val_ptr_ptr ), rowreq_ctx->data.PhyPortPhysAddress, rowreq_ctx->data.PhyPortPhysAddress_len* sizeof(rowreq_ctx->data.PhyPortPhysAddress[0]) );

    return MFD_SUCCESS;
} /* PhyPortPhysAddress_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcPhyPortEntry.PhyPortAdminStatus
 * PhyPortAdminStatus is subid 7 of dot11AcPhyPortEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.6.1.7
 * Description:
Physical port admin status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 1/8. Values:  up(1), down(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the PhyPortAdminStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PhyPortAdminStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
PhyPortAdminStatus_get( dot11AcPhyPortTable_rowreq_ctx *rowreq_ctx, u_long * PhyPortAdminStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != PhyPortAdminStatus_val_ptr );


    DEBUGMSGTL(("verbose:dot11AcPhyPortTable:PhyPortAdminStatus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PhyPortAdminStatus data.
 * copy (* PhyPortAdminStatus_val_ptr ) from rowreq_ctx->data
 */
    (* PhyPortAdminStatus_val_ptr ) = rowreq_ctx->data.PhyPortAdminStatus;

    return MFD_SUCCESS;
} /* PhyPortAdminStatus_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcPhyPortEntry.PhyPortOperStatus
 * PhyPortOperStatus is subid 8 of dot11AcPhyPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.6.1.8
 * Description:
Physical port current status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  up(1), down(2), adminDown(3)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the PhyPortOperStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PhyPortOperStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
PhyPortOperStatus_get( dot11AcPhyPortTable_rowreq_ctx *rowreq_ctx, u_long * PhyPortOperStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != PhyPortOperStatus_val_ptr );


    DEBUGMSGTL(("verbose:dot11AcPhyPortTable:PhyPortOperStatus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PhyPortOperStatus data.
 * copy (* PhyPortOperStatus_val_ptr ) from rowreq_ctx->data
 */
    (* PhyPortOperStatus_val_ptr ) = rowreq_ctx->data.PhyPortOperStatus;

    return MFD_SUCCESS;
} /* PhyPortOperStatus_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcPhyPortEntry.PhyPortLastChange
 * PhyPortLastChange is subid 9 of dot11AcPhyPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.6.1.9
 * Description:
The last time that physical port into current state.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the PhyPortLastChange data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PhyPortLastChange_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
PhyPortLastChange_get( dot11AcPhyPortTable_rowreq_ctx *rowreq_ctx, u_long * PhyPortLastChange_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != PhyPortLastChange_val_ptr );


    DEBUGMSGTL(("verbose:dot11AcPhyPortTable:PhyPortLastChange_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PhyPortLastChange data.
 * copy (* PhyPortLastChange_val_ptr ) from rowreq_ctx->data
 */
    (* PhyPortLastChange_val_ptr ) = rowreq_ctx->data.PhyPortLastChange;

    return MFD_SUCCESS;
} /* PhyPortLastChange_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcPhyPortEntry.PhyPortUpDwnTimes
 * PhyPortUpDwnTimes is subid 10 of dot11AcPhyPortEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.6.1.10
 * Description:
The Physical port up-down times.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the PhyPortUpDwnTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PhyPortUpDwnTimes_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
PhyPortUpDwnTimes_get( dot11AcPhyPortTable_rowreq_ctx *rowreq_ctx, long * PhyPortUpDwnTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != PhyPortUpDwnTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11AcPhyPortTable:PhyPortUpDwnTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the PhyPortUpDwnTimes data.
 * copy (* PhyPortUpDwnTimes_val_ptr ) from rowreq_ctx->data
 */
    (* PhyPortUpDwnTimes_val_ptr ) = rowreq_ctx->data.PhyPortUpDwnTimes;

    return MFD_SUCCESS;
} /* PhyPortUpDwnTimes_get */



/** @} */
