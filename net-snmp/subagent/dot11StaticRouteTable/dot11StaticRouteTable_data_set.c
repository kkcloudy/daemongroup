/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11StaticRouteTable.h"
#include "autelanWtpGroup.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11StaticRouteTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11StaticRouteTable is subid 1 of dot11RouteGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.32.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11StaticRouteTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11StaticRouteTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11StaticRouteTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11StaticRouteTable_undo_setup( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11StaticRouteTable undo.
     * set up dot11StaticRouteTable undo information, in preparation for a set.
     * Undo storage is in (* ipRouteStaticRowStatus_val_ptr )*
     */

    return rc;
} /* dot11StaticRouteTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11StaticRouteTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11StaticRouteTable_undo( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11StaticRouteTable undo.
     * dot11StaticRouteTable undo information, in response to a failed set.
     * Undo storage is in (* ipRouteStaticRowStatus_val_ptr )*
     */

    return rc;
} /* dot11StaticRouteTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11StaticRouteTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11StaticRouteTable_undo_cleanup( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11StaticRouteTable undo.
     * Undo storage is in (* ipRouteStaticRowStatus_val_ptr )*
     */

    return rc;
} /* dot11StaticRouteTable_undo_cleanup */

static int
check_create_ready(dot11StaticRouteTable_rowreq_ctx *rowreq_ctx) {
    if((0 != rowreq_ctx->data.ipRouteStaticMask) && (0 != rowreq_ctx->data.ipRouteStaticMode)) {
        return MFD_SUCCESS;
    }

    return MFD_ERROR;
}

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11StaticRouteTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11StaticRouteTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11StaticRouteTable_commit( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    if(RS_NOTREADY == rowreq_ctx->data.ipRouteStaticRowStatus && MFD_SUCCESS == check_create_ready(rowreq_ctx)) {
        rowreq_ctx->data.ipRouteStaticRowStatus = RS_NOTINSERVICE;
    }
    /*
     * commit dot11StaticRouteTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_IPROUTESTATICDEST_FLAG) {
       save_flags &= ~COLUMN_IPROUTESTATICDEST_FLAG; /* clear ipRouteStaticDest */
       /*
        * TODO:482:o: |-> commit column ipRouteStaticDest.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11StaticRouteTable column ipRouteStaticDest commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo ipRouteStaticDest
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPROUTESTATICDEST_FLAG;
       }
    }

    if (save_flags & COLUMN_IPROUTESTATICMASK_FLAG) {
       save_flags &= ~COLUMN_IPROUTESTATICMASK_FLAG; /* clear ipRouteStaticMask */
       /*
        * TODO:482:o: |-> commit column ipRouteStaticMask.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11StaticRouteTable column ipRouteStaticMask commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo ipRouteStaticMask
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPROUTESTATICMASK_FLAG;
       }
    }

    if (save_flags & COLUMN_IPROUTESTATICMODE_FLAG) {
       save_flags &= ~COLUMN_IPROUTESTATICMODE_FLAG; /* clear ipRouteStaticMode */
       /*
        * TODO:482:o: |-> commit column ipRouteStaticMode.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11StaticRouteTable column ipRouteStaticMode commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo ipRouteStaticMode
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPROUTESTATICMODE_FLAG;
       }
    }

    if (save_flags & COLUMN_IPROUTESTATICSUBNET_FLAG) {
       save_flags &= ~COLUMN_IPROUTESTATICSUBNET_FLAG; /* clear ipRouteStaticSubnet */
       /*
        * TODO:482:o: |-> commit column ipRouteStaticSubnet.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11StaticRouteTable column ipRouteStaticSubnet commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo ipRouteStaticSubnet
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPROUTESTATICSUBNET_FLAG;
       }
    }

    if (save_flags & COLUMN_IPROUTESTATICINTERFACE_FLAG) {
       save_flags &= ~COLUMN_IPROUTESTATICINTERFACE_FLAG; /* clear ipRouteStaticInterface */
       /*
        * TODO:482:o: |-> commit column ipRouteStaticInterface.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11StaticRouteTable column ipRouteStaticInterface commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo ipRouteStaticInterface
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPROUTESTATICINTERFACE_FLAG;
       }
    }

    if (save_flags & COLUMN_IPROUTESTATICACTIVE_FLAG) {
       save_flags &= ~COLUMN_IPROUTESTATICACTIVE_FLAG; /* clear ipRouteStaticActive */
       /*
        * TODO:482:o: |-> commit column ipRouteStaticActive.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11StaticRouteTable column ipRouteStaticActive commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo ipRouteStaticActive
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPROUTESTATICACTIVE_FLAG;
       }
    }

    if (save_flags & COLUMN_IPROUTESTATICMETRIC_FLAG) {
       save_flags &= ~COLUMN_IPROUTESTATICMETRIC_FLAG; /* clear ipRouteStaticMetric */
       /*
        * TODO:482:o: |-> commit column ipRouteStaticMetric.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11StaticRouteTable column ipRouteStaticMetric commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo ipRouteStaticMetric
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPROUTESTATICMETRIC_FLAG;
       }
    }

    if (save_flags & COLUMN_IPROUTESTATICROWSTATUS_FLAG) {
        save_flags &= ~COLUMN_IPROUTESTATICROWSTATUS_FLAG; /* clear ipRouteStaticRowStatus */
        switch(rowreq_ctx->data.ipRouteStaticRowStatus) {
            case RS_ACTIVE:
                {					
					rc = -1;
					int ip_addr = 0;					
					int ret = 1,status = 0;
					char sysShell[100] = { 0 };
					memset(sysShell,0,100);
					char target_IP[20] = { 0 };
					memset(target_IP,0,20);																				   
					char route_mask[20] = { 0 };
					memset(route_mask,0,20);																				   
					char next_hop[20] = { 0 };
					memset(next_hop,0,20);
					char staticRoute_mode[10] = { 0 };
					memset(staticRoute_mode,0,10);
					char distance_SR[5] = { 0 };
					memset(distance_SR,0,5);																			 

					if(rowreq_ctx->data.ipRouteStaticDest!=0)
					{
						switch(rowreq_ctx->data.ipRouteStaticMode)
						{
							case 1:	{/*network*/
										if((rowreq_ctx->data.ipRouteStaticSubnet != 0)&&(rowreq_ctx->data.ipRouteStaticActive>0)&&(rowreq_ctx->data.ipRouteStaticActive<5))
										{
											if((rowreq_ctx->data.ipRouteStaticActive!=4)&&(rowreq_ctx->data.ipRouteStaticMetric == 0))
											{
												break;
											}
											ip_addr = rowreq_ctx->data.ipRouteStaticDest;
											INET_NTOA(ip_addr,target_IP);

											ip_addr = rowreq_ctx->data.ipRouteStaticMask;
											INET_NTOA(ip_addr,route_mask);

											ip_addr = rowreq_ctx->data.ipRouteStaticSubnet;
											INET_NTOA(ip_addr,next_hop);
											
											switch(rowreq_ctx->data.ipRouteStaticActive)
											{
												case 1: strncpy(staticRoute_mode,"none",sizeof(staticRoute_mode)-1);
													   	break;
												case 2: strncpy(staticRoute_mode,"reject",sizeof(staticRoute_mode)-1);
													   	break;
												case 3: strncpy(staticRoute_mode,"blackhole",sizeof(staticRoute_mode)-1);
													   	break;
												case 4: strncpy(staticRoute_mode,"equalize",sizeof(staticRoute_mode)-1);
													   	break;
											}
											
											strncat(sysShell,"set_static_route.sh",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,target_IP,sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,route_mask,sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,next_hop,sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,staticRoute_mode,sizeof(sysShell)-strlen(sysShell)-1);
											if( rowreq_ctx->data.ipRouteStaticActive != 4 )
											{
												snprintf(distance_SR,sizeof(distance_SR)-1,"%d",rowreq_ctx->data.ipRouteStaticMetric);
												strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
												strncat(sysShell,distance_SR,sizeof(sysShell)-strlen(sysShell)-1);
											}
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,">/var/run/apache2/addroute.txt",sizeof(sysShell)-strlen(sysShell)-1);
											status = system(sysShell);	 
											ret = WEXITSTATUS(status);
										}
									}
									break;
							case 2:	{/*interface*/	
										if((strcmp(rowreq_ctx->data.ipRouteStaticInterface,"") != 0)&&(rowreq_ctx->data.ipRouteStaticMetric!=0))
										{
											ip_addr = rowreq_ctx->data.ipRouteStaticDest;
											INET_NTOA(ip_addr,target_IP);

											ip_addr = rowreq_ctx->data.ipRouteStaticMask;
											INET_NTOA(ip_addr,route_mask);
											
											snprintf(distance_SR,sizeof(distance_SR)-1,"%d",rowreq_ctx->data.ipRouteStaticMetric);
											
											strncat(sysShell,"set_static_route.sh",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,target_IP,sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,route_mask,sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);											
											strncat(sysShell,rowreq_ctx->data.ipRouteStaticInterface,sizeof(sysShell)-strlen(sysShell)-1);											
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,distance_SR,sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,">/var/run/apache2/addroute.txt",sizeof(sysShell)-strlen(sysShell)-1);

											status = system(sysShell);	 
											ret = WEXITSTATUS(status);
										}
									}
									break;
							case 3:	{/*null0*/
										if((rowreq_ctx->data.ipRouteStaticActive>1)&&(rowreq_ctx->data.ipRouteStaticActive<4)&&(rowreq_ctx->data.ipRouteStaticMetric!=0))
										{
											ip_addr = rowreq_ctx->data.ipRouteStaticDest;
											INET_NTOA(ip_addr,target_IP);

											ip_addr = rowreq_ctx->data.ipRouteStaticMask;
											INET_NTOA(ip_addr,route_mask);

											switch(rowreq_ctx->data.ipRouteStaticActive)
											{
												case 2: strncpy(staticRoute_mode,"reject",sizeof(staticRoute_mode)-1);
													   	break;
												case 3: strncpy(staticRoute_mode,"blackhole",sizeof(staticRoute_mode)-1);
											}

											snprintf(distance_SR,sizeof(distance_SR)-1,"%d",rowreq_ctx->data.ipRouteStaticMetric);
												
											strncat(sysShell,"set_static_route.sh",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,target_IP,sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,route_mask,sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);											
											strncat(sysShell,staticRoute_mode,sizeof(sysShell)-strlen(sysShell)-1);											
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,distance_SR,sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell," ",sizeof(sysShell)-strlen(sysShell)-1);
											strncat(sysShell,">/var/run/apache2/addroute.txt",sizeof(sysShell)-strlen(sysShell)-1);

											status = system(sysShell);	 
											ret = WEXITSTATUS(status);
										}
									}
									break;
						}						
					}
						
					if(ret == 0)
					{    
					    rc = MFD_SUCCESS;
                        break;							
					}							
                }
                break;

            case RS_CREATEANDWAIT:
                rowreq_ctx->data.ipRouteStaticRowStatus = RS_NOTREADY;
                rc = MFD_SUCCESS;
                break;
                
            case RS_DESTROY:  
                {
					if((rowreq_ctx->data.ipRouteStaticDest!=0)||((rowreq_ctx->data.ipRouteStaticDest==0)&&(rowreq_ctx->data.ipRouteStaticSubnet!=0)))
					{
						char command[100] = { 0 };
						memset(command,0,100);
						int ip_addr = 0;
						char ipRouteStaticDest[32] = { 0 };
						char param1[32] = { 0 };
						u_long ipRouteStaticMask = 0;
						int mask = 0;
						char param2[256] = { 0 };
						int ret,state;

						ip_addr = rowreq_ctx->data.ipRouteStaticDest;
						memset(ipRouteStaticDest,0,32);
						INET_NTOA(ip_addr,ipRouteStaticDest);	
						mask = 0;
						ipRouteStaticMask = rowreq_ctx->data.ipRouteStaticMask;
						while(ipRouteStaticMask>0)
						{
							ipRouteStaticMask = ipRouteStaticMask<<1;
							mask++;
						}		
						
						memset(param1,0,32);
						snprintf(param1,sizeof(param1)-1,"%s/%d",ipRouteStaticDest,mask);

						memset(param2,0,32);
						if(rowreq_ctx->data.ipRouteStaticSubnet == 0)
						{
							if(rowreq_ctx->data.ipRouteStaticActive == 2)/*reject*/
							{
								strncpy(param2,"reject",sizeof(param2)-1);
							}
							else
							{
								if(strcmp(rowreq_ctx->data.ipRouteStaticInterface,""))
									strncpy(param2,rowreq_ctx->data.ipRouteStaticInterface,sizeof(param2)-1);
								else
									strncpy(param2,"Null0",sizeof(param2)-1);
							}
						}
						else
						{
							ip_addr = rowreq_ctx->data.ipRouteStaticSubnet;
							INET_NTOA(ip_addr,param2);
						}
						
						strncat(command,"del_route.sh",sizeof(command)-strlen(command)-1);
						strncat(command," ",sizeof(command)-strlen(command)-1);
						strncat(command,param1,sizeof(command)-strlen(command)-1);
						strncat(command," ",sizeof(command)-strlen(command)-1);
						strncat(command,param2,sizeof(command)-strlen(command)-1);
						if(rowreq_ctx->data.ipRouteStaticActive == 4)
						{
							strncat(command," ",sizeof(command)-strlen(command)-1);
							strncat(command,"equalize",sizeof(command)-strlen(command)-1);
						}
						strncat(command," ",sizeof(command)-strlen(command)-1);
						strncat(command,">/dev/null",sizeof(command)-strlen(command)-1);
						state=system(command);
						ret = WEXITSTATUS(state);

						if(ret == 0)
						{    
						    rc = MFD_SUCCESS;
	                        break;							
						}
					}
                }
                rowreq_ctx->rowreq_flags &= ~MFD_ROW_DELETED;
                rc = -1;
                break;
                
            default:
                break;
        }
       /*
        * TODO:482:o: |-> commit column ipRouteStaticRowStatus.
        */
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11StaticRouteTable column ipRouteStaticRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo ipRouteStaticRowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPROUTESTATICROWSTATUS_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11StaticRouteTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11StaticRouteTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11StaticRouteTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11StaticRouteTable_undo_commit( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11StaticRouteTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11StaticRouteTable_undo_commit */

/*
 * TODO:440:M: Implement dot11StaticRouteTable node value checks.
 * TODO:450:M: Implement dot11StaticRouteTable undo functions.
 * TODO:460:M: Implement dot11StaticRouteTable set functions.
 * TODO:480:M: Implement dot11StaticRouteTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11StaticRouteEntry.ipRouteStaticDest
 * ipRouteStaticDest is subid 2 of dot11StaticRouteEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.32.1.1.2
 * Description:
ip route decsript
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ipRouteStaticDest_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11StaticRouteTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
ipRouteStaticDest_check_value( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticDest_val)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticDest_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid ipRouteStaticDest value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.ipRouteStaticRowStatus || RS_NOTREADY == rowreq_ctx->data.ipRouteStaticRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* ipRouteStaticDest value not illegal */
} /* ipRouteStaticDest_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11StaticRouteTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11StaticRouteTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
ipRouteStaticDest_undo_setup( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticDest_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup ipRouteStaticDest undo.
     */
    /*
     * copy ipRouteStaticDest data
     * set rowreq_ctx->undo->ipRouteStaticDest from rowreq_ctx->data.ipRouteStaticDest
     */
    rowreq_ctx->undo->ipRouteStaticDest = rowreq_ctx->data.ipRouteStaticDest;


    return MFD_SUCCESS;
} /* ipRouteStaticDest_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param ipRouteStaticDest_val
 *        A u_long containing the new value.
 */
int
ipRouteStaticDest_set( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticDest_val )
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticDest_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set ipRouteStaticDest value.
     * set ipRouteStaticDest value in rowreq_ctx->data
     */
    rowreq_ctx->data.ipRouteStaticDest = ipRouteStaticDest_val;

    return MFD_SUCCESS;
} /* ipRouteStaticDest_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
ipRouteStaticDest_undo( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticDest_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up ipRouteStaticDest undo.
     */
    /*
     * copy ipRouteStaticDest data
     * set rowreq_ctx->data.ipRouteStaticDest from rowreq_ctx->undo->ipRouteStaticDest
     */
    rowreq_ctx->data.ipRouteStaticDest = rowreq_ctx->undo->ipRouteStaticDest;

    
    return MFD_SUCCESS;
} /* ipRouteStaticDest_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11StaticRouteEntry.ipRouteStaticMask
 * ipRouteStaticMask is subid 3 of dot11StaticRouteEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.32.1.1.3
 * Description:
ip route decsript
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ipRouteStaticMask_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11StaticRouteTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
ipRouteStaticMask_check_value( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticMask_val)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMask_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid ipRouteStaticMask value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.ipRouteStaticRowStatus || RS_NOTREADY == rowreq_ctx->data.ipRouteStaticRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* ipRouteStaticMask value not illegal */
} /* ipRouteStaticMask_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11StaticRouteTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11StaticRouteTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
ipRouteStaticMask_undo_setup( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMask_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup ipRouteStaticMask undo.
     */
    /*
     * copy ipRouteStaticMask data
     * set rowreq_ctx->undo->ipRouteStaticMask from rowreq_ctx->data.ipRouteStaticMask
     */
    rowreq_ctx->undo->ipRouteStaticMask = rowreq_ctx->data.ipRouteStaticMask;


    return MFD_SUCCESS;
} /* ipRouteStaticMask_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param ipRouteStaticMask_val
 *        A u_long containing the new value.
 */
int
ipRouteStaticMask_set( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticMask_val )
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMask_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set ipRouteStaticMask value.
     * set ipRouteStaticMask value in rowreq_ctx->data
     */
    rowreq_ctx->data.ipRouteStaticMask = ipRouteStaticMask_val;

    return MFD_SUCCESS;
} /* ipRouteStaticMask_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
ipRouteStaticMask_undo( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMask_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up ipRouteStaticMask undo.
     */
    /*
     * copy ipRouteStaticMask data
     * set rowreq_ctx->data.ipRouteStaticMask from rowreq_ctx->undo->ipRouteStaticMask
     */
    rowreq_ctx->data.ipRouteStaticMask = rowreq_ctx->undo->ipRouteStaticMask;

    
    return MFD_SUCCESS;
} /* ipRouteStaticMask_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11StaticRouteEntry.ipRouteStaticMode
 * ipRouteStaticMode is subid 4 of dot11StaticRouteEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.32.1.1.4
 * Description:
ip route decsript
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  network(1), interface(2), null0(3)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ipRouteStaticMode_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11StaticRouteTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  network(1), interface(2), null0(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
ipRouteStaticMode_check_value( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticMode_val)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMode_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid ipRouteStaticMode value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.ipRouteStaticRowStatus || RS_NOTREADY == rowreq_ctx->data.ipRouteStaticRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* ipRouteStaticMode value not illegal */
} /* ipRouteStaticMode_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11StaticRouteTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11StaticRouteTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
ipRouteStaticMode_undo_setup( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMode_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup ipRouteStaticMode undo.
     */
    /*
     * copy ipRouteStaticMode data
     * set rowreq_ctx->undo->ipRouteStaticMode from rowreq_ctx->data.ipRouteStaticMode
     */
    rowreq_ctx->undo->ipRouteStaticMode = rowreq_ctx->data.ipRouteStaticMode;


    return MFD_SUCCESS;
} /* ipRouteStaticMode_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param ipRouteStaticMode_val
 *        A long containing the new value.
 */
int
ipRouteStaticMode_set( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticMode_val )
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMode_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set ipRouteStaticMode value.
     * set ipRouteStaticMode value in rowreq_ctx->data
     */
    rowreq_ctx->data.ipRouteStaticMode = ipRouteStaticMode_val;

    return MFD_SUCCESS;
} /* ipRouteStaticMode_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
ipRouteStaticMode_undo( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMode_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up ipRouteStaticMode undo.
     */
    /*
     * copy ipRouteStaticMode data
     * set rowreq_ctx->data.ipRouteStaticMode from rowreq_ctx->undo->ipRouteStaticMode
     */
    rowreq_ctx->data.ipRouteStaticMode = rowreq_ctx->undo->ipRouteStaticMode;

    
    return MFD_SUCCESS;
} /* ipRouteStaticMode_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11StaticRouteEntry.ipRouteStaticSubnet
 * ipRouteStaticSubnet is subid 5 of dot11StaticRouteEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.32.1.1.5
 * Description:
ip route Priority decsript
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ipRouteStaticSubnet_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11StaticRouteTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
ipRouteStaticSubnet_check_value( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticSubnet_val)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticSubnet_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid ipRouteStaticSubnet value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.ipRouteStaticRowStatus || RS_NOTREADY == rowreq_ctx->data.ipRouteStaticRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* ipRouteStaticSubnet value not illegal */
} /* ipRouteStaticSubnet_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11StaticRouteTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11StaticRouteTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
ipRouteStaticSubnet_undo_setup( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticSubnet_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup ipRouteStaticSubnet undo.
     */
    /*
     * copy ipRouteStaticSubnet data
     * set rowreq_ctx->undo->ipRouteStaticSubnet from rowreq_ctx->data.ipRouteStaticSubnet
     */
    rowreq_ctx->undo->ipRouteStaticSubnet = rowreq_ctx->data.ipRouteStaticSubnet;


    return MFD_SUCCESS;
} /* ipRouteStaticSubnet_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param ipRouteStaticSubnet_val
 *        A u_long containing the new value.
 */
int
ipRouteStaticSubnet_set( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticSubnet_val )
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticSubnet_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set ipRouteStaticSubnet value.
     * set ipRouteStaticSubnet value in rowreq_ctx->data
     */
    rowreq_ctx->data.ipRouteStaticSubnet = ipRouteStaticSubnet_val;

    return MFD_SUCCESS;
} /* ipRouteStaticSubnet_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
ipRouteStaticSubnet_undo( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticSubnet_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up ipRouteStaticSubnet undo.
     */
    /*
     * copy ipRouteStaticSubnet data
     * set rowreq_ctx->data.ipRouteStaticSubnet from rowreq_ctx->undo->ipRouteStaticSubnet
     */
    rowreq_ctx->data.ipRouteStaticSubnet = rowreq_ctx->undo->ipRouteStaticSubnet;

    
    return MFD_SUCCESS;
} /* ipRouteStaticSubnet_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11StaticRouteEntry.ipRouteStaticInterface
 * ipRouteStaticInterface is subid 6 of dot11StaticRouteEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.32.1.1.6
 * Description:
ip route decsript
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ipRouteStaticInterface_val_ptr
 *        A char containing the new value.
 * @param ipRouteStaticInterface_val_ptr_len
 *        The size (in bytes) of the data pointed to by ipRouteStaticInterface_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11StaticRouteTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.ipRouteStaticInterface).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
ipRouteStaticInterface_check_value( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, char *ipRouteStaticInterface_val_ptr,  size_t ipRouteStaticInterface_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticInterface_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != ipRouteStaticInterface_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid ipRouteStaticInterface value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.ipRouteStaticRowStatus || RS_NOTREADY == rowreq_ctx->data.ipRouteStaticRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* ipRouteStaticInterface value not illegal */
} /* ipRouteStaticInterface_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11StaticRouteTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11StaticRouteTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
ipRouteStaticInterface_undo_setup( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticInterface_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup ipRouteStaticInterface undo.
     */
    /*
     * copy ipRouteStaticInterface and ipRouteStaticInterface_len data
     * set rowreq_ctx->undo->ipRouteStaticInterface from rowreq_ctx->data.ipRouteStaticInterface
     */
    memcpy( rowreq_ctx->undo->ipRouteStaticInterface, rowreq_ctx->data.ipRouteStaticInterface,
            (rowreq_ctx->data.ipRouteStaticInterface_len * sizeof(rowreq_ctx->undo->ipRouteStaticInterface[0])));
    rowreq_ctx->undo->ipRouteStaticInterface_len = rowreq_ctx->data.ipRouteStaticInterface_len;


    return MFD_SUCCESS;
} /* ipRouteStaticInterface_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param ipRouteStaticInterface_val_ptr
 *        A char containing the new value.
 * @param ipRouteStaticInterface_val_ptr_len
 *        The size (in bytes) of the data pointed to by ipRouteStaticInterface_val_ptr
 */
int
ipRouteStaticInterface_set( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, char *ipRouteStaticInterface_val_ptr,  size_t ipRouteStaticInterface_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticInterface_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != ipRouteStaticInterface_val_ptr);

    /*
     * TODO:461:M: |-> Set ipRouteStaticInterface value.
     * set ipRouteStaticInterface value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.ipRouteStaticInterface, ipRouteStaticInterface_val_ptr, ipRouteStaticInterface_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.ipRouteStaticInterface_len = ipRouteStaticInterface_val_ptr_len / sizeof(ipRouteStaticInterface_val_ptr[0]);

    return MFD_SUCCESS;
} /* ipRouteStaticInterface_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
ipRouteStaticInterface_undo( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticInterface_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up ipRouteStaticInterface undo.
     */
    /*
     * copy ipRouteStaticInterface and ipRouteStaticInterface_len data
     * set rowreq_ctx->data.ipRouteStaticInterface from rowreq_ctx->undo->ipRouteStaticInterface
     */
    memcpy( rowreq_ctx->data.ipRouteStaticInterface, rowreq_ctx->undo->ipRouteStaticInterface,
            (rowreq_ctx->undo->ipRouteStaticInterface_len * sizeof(rowreq_ctx->data.ipRouteStaticInterface[0])));
    rowreq_ctx->data.ipRouteStaticInterface_len = rowreq_ctx->undo->ipRouteStaticInterface_len;

    
    return MFD_SUCCESS;
} /* ipRouteStaticInterface_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11StaticRouteEntry.ipRouteStaticActive
 * ipRouteStaticActive is subid 7 of dot11StaticRouteEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.32.1.1.7
 * Description:
ip route decsript
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  none(1), reject(2), blackhole(3), equalize(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ipRouteStaticActive_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11StaticRouteTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  none(1), reject(2), blackhole(3), equalize(4)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
ipRouteStaticActive_check_value( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticActive_val)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticActive_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid ipRouteStaticActive value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.ipRouteStaticRowStatus || RS_NOTREADY == rowreq_ctx->data.ipRouteStaticRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* ipRouteStaticActive value not illegal */
} /* ipRouteStaticActive_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11StaticRouteTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11StaticRouteTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
ipRouteStaticActive_undo_setup( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticActive_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup ipRouteStaticActive undo.
     */
    /*
     * copy ipRouteStaticActive data
     * set rowreq_ctx->undo->ipRouteStaticActive from rowreq_ctx->data.ipRouteStaticActive
     */
    rowreq_ctx->undo->ipRouteStaticActive = rowreq_ctx->data.ipRouteStaticActive;


    return MFD_SUCCESS;
} /* ipRouteStaticActive_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param ipRouteStaticActive_val
 *        A long containing the new value.
 */
int
ipRouteStaticActive_set( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticActive_val )
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticActive_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set ipRouteStaticActive value.
     * set ipRouteStaticActive value in rowreq_ctx->data
     */
    rowreq_ctx->data.ipRouteStaticActive = ipRouteStaticActive_val;

    return MFD_SUCCESS;
} /* ipRouteStaticActive_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
ipRouteStaticActive_undo( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticActive_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up ipRouteStaticActive undo.
     */
    /*
     * copy ipRouteStaticActive data
     * set rowreq_ctx->data.ipRouteStaticActive from rowreq_ctx->undo->ipRouteStaticActive
     */
    rowreq_ctx->data.ipRouteStaticActive = rowreq_ctx->undo->ipRouteStaticActive;

    
    return MFD_SUCCESS;
} /* ipRouteStaticActive_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11StaticRouteEntry.ipRouteStaticMetric
 * ipRouteStaticMetric is subid 8 of dot11StaticRouteEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.32.1.1.8
 * Description:
ip route decsript
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ipRouteStaticMetric_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11StaticRouteTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
ipRouteStaticMetric_check_value( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, long ipRouteStaticMetric_val)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMetric_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid ipRouteStaticMetric value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.ipRouteStaticRowStatus || RS_NOTREADY == rowreq_ctx->data.ipRouteStaticRowStatus)
	    return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* ipRouteStaticMetric value not illegal */
} /* ipRouteStaticMetric_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11StaticRouteTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11StaticRouteTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
ipRouteStaticMetric_undo_setup( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMetric_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup ipRouteStaticMetric undo.
     */
    /*
     * copy ipRouteStaticMetric data
     * set rowreq_ctx->undo->ipRouteStaticMetric from rowreq_ctx->data.ipRouteStaticMetric
     */
    rowreq_ctx->undo->ipRouteStaticMetric = rowreq_ctx->data.ipRouteStaticMetric;


    return MFD_SUCCESS;
} /* ipRouteStaticMetric_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param ipRouteStaticMetric_val
 *        A long containing the new value.
 */
int
ipRouteStaticMetric_set( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, long ipRouteStaticMetric_val )
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMetric_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set ipRouteStaticMetric value.
     * set ipRouteStaticMetric value in rowreq_ctx->data
     */
    rowreq_ctx->data.ipRouteStaticMetric = ipRouteStaticMetric_val;

    return MFD_SUCCESS;
} /* ipRouteStaticMetric_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
ipRouteStaticMetric_undo( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticMetric_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up ipRouteStaticMetric undo.
     */
    /*
     * copy ipRouteStaticMetric data
     * set rowreq_ctx->data.ipRouteStaticMetric from rowreq_ctx->undo->ipRouteStaticMetric
     */
    rowreq_ctx->data.ipRouteStaticMetric = rowreq_ctx->undo->ipRouteStaticMetric;

    
    return MFD_SUCCESS;
} /* ipRouteStaticMetric_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11StaticRouteEntry.ipRouteStaticRowStatus
 * ipRouteStaticRowStatus is subid 9 of dot11StaticRouteEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.32.1.1.9
 * Description:
ip route decsript
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ipRouteStaticRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11StaticRouteTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
ipRouteStaticRowStatus_check_value( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticRowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid ipRouteStaticRowStatus value.
     */

    return MFD_SUCCESS; /* ipRouteStaticRowStatus value not illegal */
} /* ipRouteStaticRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11StaticRouteTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11StaticRouteTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
ipRouteStaticRowStatus_undo_setup( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup ipRouteStaticRowStatus undo.
     */
    /*
     * copy ipRouteStaticRowStatus data
     * set rowreq_ctx->undo->ipRouteStaticRowStatus from rowreq_ctx->data.ipRouteStaticRowStatus
     */
    rowreq_ctx->undo->ipRouteStaticRowStatus = rowreq_ctx->data.ipRouteStaticRowStatus;


    return MFD_SUCCESS;
} /* ipRouteStaticRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param ipRouteStaticRowStatus_val
 *        A long containing the new value.
 */
int
ipRouteStaticRowStatus_set( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx, u_long ipRouteStaticRowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set ipRouteStaticRowStatus value.
     * set ipRouteStaticRowStatus value in rowreq_ctx->data
     */
    
    int ret = MFD_SUCCESS;
    
    switch(ipRouteStaticRowStatus_val) {
    
        case RS_ACTIVE:         
        case RS_DESTROY:
            rowreq_ctx->data.ipRouteStaticRowStatus = ipRouteStaticRowStatus_val;
            ret = MFD_SUCCESS;
            break;
    
        case RS_NOTINSERVICE:
        case RS_NOTREADY:
        case RS_CREATEANDGO:
            ret = MFD_ERROR;
            break;
    
        case RS_CREATEANDWAIT:
            if(RS_NONEXISTENT == rowreq_ctx->data.ipRouteStaticRowStatus) {
                rowreq_ctx->data.ipRouteStaticRowStatus = ipRouteStaticRowStatus_val;
                ret = MFD_SUCCESS;
            }
            else {
                ret = MFD_ERROR;
            }
            break;
    
        default:
            snmp_log(LOG_WARNING, "unknow %s in ipRouteStaticRowStatus_set", ipRouteStaticRowStatus_val);
            ret = MFD_ERROR;
            break;
    }

    return ret;
    
} /* ipRouteStaticRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
ipRouteStaticRowStatus_undo( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:ipRouteStaticRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up ipRouteStaticRowStatus undo.
     */
    /*
     * copy ipRouteStaticRowStatus data
     * set rowreq_ctx->data.ipRouteStaticRowStatus from rowreq_ctx->undo->ipRouteStaticRowStatus
     */
    rowreq_ctx->data.ipRouteStaticRowStatus = rowreq_ctx->undo->ipRouteStaticRowStatus;

    
    return MFD_SUCCESS;
} /* ipRouteStaticRowStatus_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11StaticRouteTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11StaticRouteTable if you don't have dependencies)
 */
int
dot11StaticRouteTable_check_dependencies(dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11StaticRouteTable:dot11StaticRouteTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11StaticRouteTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & COLUMN_IPROUTESTATICROWSTATUS_FLAG) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->ipRouteStaticRowStatus,
                                         rowreq_ctx->data.ipRouteStaticRowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_DESTROY ==  rowreq_ctx->data.ipRouteStaticRowStatus) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;
            }
            else if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.ipRouteStaticRowStatus) {
                if ((rowreq_ctx->column_set_flags & DOT11STATICROUTETABLE_REQUIRED_COLS)
                    != DOT11STATICROUTETABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11StaticRouteTable",
                                "required columns missing (0x%0x != 0x%0x)\n",
                                rowreq_ctx->column_set_flags, DOT11STATICROUTETABLE_REQUIRED_COLS));
                    return MFD_CANNOT_CREATE_NOW;
                }
                rowreq_ctx->data.ipRouteStaticRowStatus = ROWSTATUS_ACTIVE;
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.ipRouteStaticRowStatus) {
                if (rowreq_ctx->column_set_flags & ~COLUMN_IPROUTESTATICROWSTATUS_FLAG) {
                    DEBUGMSGTL(("dot11StaticRouteTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;

            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11StaticRouteTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11StaticRouteTable_check_dependencies */

/** @} */
