/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 14170 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11StaticRouteTable.h"


#include "dot11StaticRouteTable_data_access.h"
#include "autelanWtpGroup.h"

/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11StaticRouteTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11StaticRouteTable is subid 1 of dot11RouteGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.32.1, length: 12
*/

/**
 * initialization for dot11StaticRouteTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11StaticRouteTable_reg
 *        Pointer to dot11StaticRouteTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11StaticRouteTable_init_data(dot11StaticRouteTable_registration * dot11StaticRouteTable_reg)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11StaticRouteTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11StaticRouteTable_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11StaticRouteTable_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to dot11StaticRouteTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to dot11StaticRouteTable_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up dot11StaticRouteTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11STATICROUTETABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11StaticRouteTable_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before dot11StaticRouteTable_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
dot11StaticRouteTable_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to dot11StaticRouteTable_container_shutdown\n");
        return;
    }

} /* dot11StaticRouteTable_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement dot11StaticRouteTable data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11StaticRouteTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11StaticRouteTable_container_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11StaticRouteTable_container_load\n");
	
    dot11StaticRouteTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * staticRouteIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   staticRouteIndex = 0;

    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_container_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
      //  return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11StaticRouteTable container.
     * loop over your dot11StaticRouteTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    int ret;
	int ipRouteStaticDest = 0;
	int ipRouteStaticMask = 0;
	int ipRouteStaticMode = 0;
	int ipRouteStaticSubnet = 0;
	char ipRouteStaticInterface[256] = { 0 };
	int ipRouteStaticActive = 1;
	int ipRouteStaticMetric = 0;
	int route_num = 0,i,j;
	char showRoute[350] = { 0 };
	FILE *fd = NULL;
	char temp[60] = { 0 };
	memset(temp,0,60);
	char * routeInfo[4096];	
	char *revRouteInfo[7] = {"","","","","","",""};
	char *dest_ip = NULL;
	char *dest_mask = NULL;
	char *endptr = NULL;  
	int mask = 0;
	for(i=0;i<4096;i++)
 	{
		routeInfo[i]=(char *)malloc(60);
		if(routeInfo[i])
		{
			memset(routeInfo[i],0,60);
		}
 	}
																  
    memset(showRoute,0,350);
	strncat(showRoute,"show_route.sh 2>/dev/null | awk '{OFS=\"#\"}NR==4,NR==0{if($1~/S/){print $1,$2,$5,$6,$3,$7,$8} }'>/var/run/apache2/ip_route.txt",sizeof(showRoute)-strlen(showRoute)-1);
	system(showRoute);																  
	if((fd=fopen("/var/run/apache2/ip_route.txt","r"))!=NULL)
	{
		route_num=0;
		while((fgets(temp,60,fd)) != NULL)
		{
			if(routeInfo[route_num])
			{
				strcat(routeInfo[route_num],temp);
				route_num++;
			}
			memset(temp,0,60);			
		}	
		fclose(fd);
	}
	for(i=0;i<route_num;i++)
	{
		if(routeInfo[i])
		{
			revRouteInfo[0]=strtok(routeInfo[i],"#,");							
		}
		j=0;
		while(revRouteInfo[j]!=NULL && j<5)
		{                    									
			revRouteInfo[j+1]=strtok(NULL,"#,");
			j++;
		}
		if((revRouteInfo[0])&&(strstr(revRouteInfo[0],"S")!=NULL))
	    {
			if((revRouteInfo[2])&&(strcmp(revRouteInfo[2],"directly")==0))
			{
				revRouteInfo[6]=strtok(NULL,"#,");

				if(revRouteInfo[1])
				{
					dest_ip=strtok(revRouteInfo[1],"/");
				}
				dest_mask=strtok(NULL,"/");
				if(dest_mask)
				{
					mask= strtoul(dest_mask,&endptr,10); 
				}
				if(dest_ip)
				{
					INET_ATON(ipRouteStaticDest,dest_ip);
				}
				ipRouteStaticMask = 0xffffffff^((1<<(32-mask))-1);

				memset(ipRouteStaticInterface,0,256);
				if((revRouteInfo[5])&&(strcasecmp(revRouteInfo[5],"Null0") == 0))
				{
					ipRouteStaticMode = 3;
				}
				else
				{
					ipRouteStaticMode = 2;
					if(revRouteInfo[5])
					{
						strncpy(ipRouteStaticInterface,revRouteInfo[5],sizeof(ipRouteStaticInterface)-1);
					}
				}
				
				INET_ATON(ipRouteStaticSubnet,"0.0.0.0");					

				if((revRouteInfo[6])&&(strstr(revRouteInfo[6],"rej")!=NULL))
					ipRouteStaticActive = 2;
				else if((revRouteInfo[6])&&(strstr(revRouteInfo[6],"bh")!=NULL))
					ipRouteStaticActive = 3;
				else
					ipRouteStaticActive = 1;

				if(revRouteInfo[4])
				{
					sscanf(revRouteInfo[4],"[%d/*]",&ipRouteStaticMetric);		
				}
				staticRouteIndex++;
			}
			else
			{
				if(revRouteInfo[1])
				{
					dest_ip=strtok(revRouteInfo[1],"/");
				}
				dest_mask=strtok(NULL,"/");
				if(dest_mask)
				{
					mask= strtoul(dest_mask,&endptr,10); 
				}
				if(dest_ip)
				{
					INET_ATON(ipRouteStaticDest,dest_ip);
				}
				ipRouteStaticMask = 0xffffffff^((1<<(32-mask))-1);

				memset(ipRouteStaticInterface,0,256);
				if((revRouteInfo[3])&&(strcasecmp(revRouteInfo[3],"inactive") == 0))
				{
					ipRouteStaticMode = 1;
				}
				else
				{
					ipRouteStaticMode = 2;
					if(revRouteInfo[3])
					{
						strncpy(ipRouteStaticInterface,revRouteInfo[3],sizeof(ipRouteStaticInterface)-1);
					}
				}

				if(revRouteInfo[2])
				{
					INET_ATON(ipRouteStaticSubnet,revRouteInfo[2]);
				}

				if((revRouteInfo[5])&&(strstr(revRouteInfo[5],"rej")!=NULL))
					ipRouteStaticActive = 2;
				else if((revRouteInfo[5])&&(strstr(revRouteInfo[5],"bh")!=NULL))
					ipRouteStaticActive = 3;
				else if((revRouteInfo[5])&&(strstr(revRouteInfo[5],"equalize")!=NULL))
					ipRouteStaticActive = 4;
				else
					ipRouteStaticActive = 1;

				if(revRouteInfo[4])
				{
					sscanf(revRouteInfo[4],"[%d/*]",&ipRouteStaticMetric);		
				}
				staticRouteIndex++;
			}
	    /*
	    ***************************************************
	    ***             START EXAMPLE CODE              ***
	    ***---------------------------------------------***/
	    /*
	     * get a line (skip blank lines)
	     */
	    //do {
	      //  if (!fgets(line, sizeof(line), filep)) {
	            /* we're done */
	        //    fclose(filep);
	          //  filep = NULL;
	        //}
	    //} while (filep && (line[0] == '\n'));

	    /*
	     * check for end of data
	     */
	    //if(NULL == filep)
	      //  break;

	    /*
	     * parse line into variables
	     */
	    /*
	    ***---------------------------------------------***
	    ***              END  EXAMPLE CODE              ***
	    ***************************************************/

	        /*
	         * TODO:352:M: |   |-> set indexes in new dot11StaticRouteTable rowreq context.
	         * data context will be set from the param (unless NULL,
	         *      in which case a new data context will be allocated)
	         */
	        rowreq_ctx = dot11StaticRouteTable_allocate_rowreq_ctx(NULL);
	        if (NULL == rowreq_ctx) {
	            snmp_log(LOG_ERR, "memory allocation failed\n");
				for(i=0;i<4096;i++)
			 	{
					FREE_OBJECT(routeInfo[i]);
			 	}
	            return MFD_RESOURCE_UNAVAILABLE;
	        }
	        if(MFD_SUCCESS != dot11StaticRouteTable_indexes_set(rowreq_ctx
	                               , staticRouteIndex
	               )) {
	            snmp_log(LOG_ERR,"error setting index while loading "
	                     "dot11StaticRouteTable data.\n");
	            dot11StaticRouteTable_release_rowreq_ctx(rowreq_ctx);
	            continue;
	        }

	        /*
	         * TODO:352:r: |   |-> populate dot11StaticRouteTable data context.
	         * Populate data context here. (optionally, delay until row prep)
	         */
	    /*
	     * TRANSIENT or semi-TRANSIENT data:
	     * copy data or save any info needed to do it in row_prep.
	     */
	    /*
	     * setup/save data for ipRouteStaticDest
	     * ipRouteStaticDest(2)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.ipRouteStaticDest = ipRouteStaticDest;
	    
	    /*
	     * setup/save data for ipRouteStaticMask
	     * ipRouteStaticMask(3)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.ipRouteStaticMask = ipRouteStaticMask;
	    
	    /*
	     * setup/save data for ipRouteStaticMode
	     * ipRouteStaticMode(4)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.ipRouteStaticMode = ipRouteStaticMode;
	    
	    /*
	     * setup/save data for ipRouteStaticSubnet
	     * ipRouteStaticSubnet(5)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.ipRouteStaticSubnet = ipRouteStaticSubnet;
	    
	    /*
	     * setup/save data for ipRouteStaticInterface
	     * ipRouteStaticInterface(6)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H
	     */
	    /** no mapping */
	    /*
	     * make sure there is enough space for ipRouteStaticInterface data
	     */
	    /*if ((NULL == rowreq_ctx->data.ipRouteStaticInterface) ||
	        (rowreq_ctx->data.ipRouteStaticInterface_len <
	         (ipRouteStaticInterface_len* sizeof(ipRouteStaticInterface[0])))) {
	        snmp_log(LOG_ERR,"not enough space for value\n");
	        return MFD_ERROR;
	    }*/
	    rowreq_ctx->data.ipRouteStaticInterface_len = MIN(sizeof(rowreq_ctx->data.ipRouteStaticInterface) - 1, strlen(ipRouteStaticInterface));
	    memcpy( rowreq_ctx->data.ipRouteStaticInterface, ipRouteStaticInterface, rowreq_ctx->data.ipRouteStaticInterface_len );
	    
	    /*
	     * setup/save data for ipRouteStaticActive
	     * ipRouteStaticActive(7)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.ipRouteStaticActive = ipRouteStaticActive;
	    
	    /*
	     * setup/save data for ipRouteStaticMetric
	     * ipRouteStaticMetric(8)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.ipRouteStaticMetric = ipRouteStaticMetric;
	    
	    /*
	     * setup/save data for ipRouteStaticRowStatus
	     * ipRouteStaticRowStatus(9)/RowStatus/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
	     */
	    /** no mapping */
	    rowreq_ctx->data.ipRouteStaticRowStatus = RS_ACTIVE;
	    
	        
	        /*
	         * insert into table container
	         */
	        if(CONTAINER_INSERT(container, rowreq_ctx))
	        {
	        	dot11StaticRouteTable_release_rowreq_ctx(rowreq_ctx);
	        }
	        ++count;
	    }	
	}
	for(i=0;i<4096;i++)
 	{
		FREE_OBJECT(routeInfo[i]);
 	}


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
      //  fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_container_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11StaticRouteTable_container_load\n");
	
    return MFD_SUCCESS;
} /* dot11StaticRouteTable_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11StaticRouteTable_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_container_free","called\n"));

    /*
     * TODO:380:M: Free dot11StaticRouteTable container data.
     */
} /* dot11StaticRouteTable_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11StaticRouteTable_row_prep( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11StaticRouteTable_row_prep */

/*
 * TODO:420:r: Implement dot11StaticRouteTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11StaticRouteEntry.staticRouteIndex
 * staticRouteIndex is subid 1 of dot11StaticRouteEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.32.1.1.1
 * Description:
ip route index
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * check validity of staticRouteIndex index portion
 *
 * @retval MFD_SUCCESS   : the incoming value is legal
 * @retval MFD_ERROR     : the incoming value is NOT legal
 *
 * @note this is not the place to do any checks for the sanity
 *       of multiple indexes. Those types of checks should be done in the
 *       dot11StaticRouteTable_validate_index() function.
 *
 * @note Also keep in mind that if the index refers to a row in this or
 *       some other table, you can't check for that row here to make
 *       decisions, since that row might not be created yet, but may
 *       be created during the processing this request. If you have
 *       such checks, they should be done in the check_dependencies
 *       function, because any new/deleted/changed rows should be
 *       available then.
 *
 * The following checks have already been done for you:
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 */
int
staticRouteIndex_check_index( dot11StaticRouteTable_rowreq_ctx *rowreq_ctx )
{
    DEBUGMSGTL(("verbose:dot11StaticRouteTable:staticRouteIndex_check_index","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:426:M: |-> Check dot11StaticRouteTable index staticRouteIndex.
     * check that index value in the table context is legal.
     * (rowreq_ctx->tbl_index.staticRouteIndex)
     */

    return MFD_SUCCESS; /* staticRouteIndex index ok */
} /* staticRouteIndex_check_index */

/**
 * verify specified index is valid.
 *
 * This check is independent of whether or not the values specified for
 * the columns of the new row are valid. Column values and row consistency
 * will be checked later. At this point, only the index values should be
 * checked.
 *
 * All of the individual index validation functions have been called, so this
 * is the place to make sure they are valid as a whole when combined. If
 * you only have one index, then you probably don't need to do anything else
 * here.
 * 
 * @note Keep in mind that if the indexes refer to a row in this or
 *       some other table, you can't check for that row here to make
 *       decisions, since that row might not be created yet, but may
 *       be created during the processing this request. If you have
 *       such checks, they should be done in the check_dependencies
 *       function, because any new/deleted/changed rows should be
 *       available then.
 *
 *
 * @param dot11StaticRouteTable_reg
 *        Pointer to the user registration data
 * @param dot11StaticRouteTable_rowreq_ctx
 *        Pointer to the users context.
 * @retval MFD_SUCCESS            : success
 * @retval MFD_CANNOT_CREATE_NOW  : index not valid right now
 * @retval MFD_CANNOT_CREATE_EVER : index never valid
 */
int
dot11StaticRouteTable_validate_index( dot11StaticRouteTable_registration * dot11StaticRouteTable_reg,
                           dot11StaticRouteTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11StaticRouteTable:dot11StaticRouteTable_validate_index","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:430:M: |-> Validate potential dot11StaticRouteTable index.
     */
    if(0) {
        snmp_log(LOG_WARNING,"invalid index for a new row in the "
                 "dot11StaticRouteTable table.\n");
        /*
         * determine failure type.
         *
         * If the index could not ever be created, return MFD_NOT_EVER
         * If the index can not be created under the present circumstances
         * (even though it could be created under other circumstances),
         * return MFD_NOT_NOW.
         */
        if(0) {
            return MFD_CANNOT_CREATE_EVER;
        }
        else {
            return MFD_CANNOT_CREATE_NOW;
        }
    }

    return rc;
} /* dot11StaticRouteTable_validate_index */

/** @} */
