/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpWirelessIfTable.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"



/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpWirelessIfTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpWirelessIfTable is subid 3 of wtpInterface.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11WtpWirelessIfTable_undo_setup( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:dot11WtpWirelessIfTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11WtpWirelessIfTable undo.
     * set up dot11WtpWirelessIfTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11WtpWirelessIfTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWirelessIfTable_undo_cleanup( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:dot11WtpWirelessIfTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11WtpWirelessIfTable undo.
     */

    return rc;
} /* dot11WtpWirelessIfTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpWirelessIfTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpWirelessIfTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWirelessIfTable_commit( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:dot11WtpWirelessIfTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11WtpWirelessIfTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WTPWIRELESSIFADMINSTATUS) {
       save_flags &= ~FLAG_WTPWIRELESSIFADMINSTATUS; /* clear wtpWirelessIfAdminStatus */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfAdminStatus.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfTable column wtpWirelessIfAdminStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfAdminStatus
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFADMINSTATUS;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSIFRADIOCHANNELAUTOSELECTENABLE) {
       save_flags &= ~FLAG_WTPWIRELESSIFRADIOCHANNELAUTOSELECTENABLE; /* clear wtpWirelessIfRadioChannelAutoSelectEnable */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfRadioChannelAutoSelectEnable.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfTable column wtpWirelessIfRadioChannelAutoSelectEnable commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfRadioChannelAutoSelectEnable
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFRADIOCHANNELAUTOSELECTENABLE;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSIFRADIOCHANNELCONFIG) {
       save_flags &= ~FLAG_WTPWIRELESSIFRADIOCHANNELCONFIG; /* clear wtpWirelessIfRadioChannelConfig */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfRadioChannelConfig.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfTable column wtpWirelessIfRadioChannelConfig commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfRadioChannelConfig
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFRADIOCHANNELCONFIG;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSIFTRANSMITSPEEDCONFIG) {
       save_flags &= ~FLAG_WTPWIRELESSIFTRANSMITSPEEDCONFIG; /* clear wtpWirelessIfTransmitSpeedConfig */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfTransmitSpeedConfig.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfTable column wtpWirelessIfTransmitSpeedConfig commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfTransmitSpeedConfig
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFTRANSMITSPEEDCONFIG;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSIFPWRATTVALUE) {
       save_flags &= ~FLAG_WTPWIRELESSIFPWRATTVALUE; /* clear wtpWirelessIfPwrAttValue */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfPwrAttValue.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfTable column wtpWirelessIfPwrAttValue commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfPwrAttValue
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFPWRATTVALUE;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSIFPOWERMGMTENABLE) {
       save_flags &= ~FLAG_WTPWIRELESSIFPOWERMGMTENABLE; /* clear wtpWirelessIfPowerMgmtEnable */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfPowerMgmtEnable.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfTable column wtpWirelessIfPowerMgmtEnable commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfPowerMgmtEnable
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFPOWERMGMTENABLE;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSIFMAXSTATIONNUMPERMITTED) {
       save_flags &= ~FLAG_WTPWIRELESSIFMAXSTATIONNUMPERMITTED; /* clear wtpWirelessIfMaxStationNumPermitted */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfMaxStationNumPermitted.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfTable column wtpWirelessIfMaxStationNumPermitted commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfMaxStationNumPermitted
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFMAXSTATIONNUMPERMITTED;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSIFTXPWRSTEP) {
       save_flags &= ~FLAG_WTPWIRELESSIFTXPWRSTEP; /* clear wtpWirelessIfTxPwrStep */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfTxPwrStep.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfTable column wtpWirelessIfTxPwrStep commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfTxPwrStep
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFTXPWRSTEP;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11WtpWirelessIfTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpWirelessIfTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpWirelessIfTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWirelessIfTable_undo_commit( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:dot11WtpWirelessIfTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11WtpWirelessIfTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11WtpWirelessIfTable_undo_commit */

/*
 * TODO:420:r: Implement dot11WtpWirelessIfTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/*
 * TODO:440:M: Implement dot11WtpWirelessIfTable node value checks.
 * TODO:450:M: Implement dot11WtpWirelessIfTable undo functions.
 * TODO:460:M: Implement dot11WtpWirelessIfTable set functions.
 * TODO:480:M: Implement dot11WtpWirelessIfTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfAdminStatus
 * wtpWirelessIfAdminStatus is subid 7 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.7
 * Description:
Wireless interface admin status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 1/8. Values:  up(1), down(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfAdminStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  up(1), down(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfAdminStatus_check_value( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, u_long wtpWirelessIfAdminStatus_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfAdminStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfAdminStatus value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfAdminStatus value not illegal */
} /* wtpWirelessIfAdminStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfAdminStatus_undo_setup( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfAdminStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfAdminStatus undo.
     */
    /*
     * copy wtpWirelessIfAdminStatus data
     * set rowreq_ctx->undo->wtpWirelessIfAdminStatus from rowreq_ctx->data.wtpWirelessIfAdminStatus
     */
    rowreq_ctx->undo->wtpWirelessIfAdminStatus = rowreq_ctx->data.wtpWirelessIfAdminStatus;


    return MFD_SUCCESS;
} /* wtpWirelessIfAdminStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfAdminStatus_val
 *        A long containing the new value.
 */
int
wtpWirelessIfAdminStatus_set( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, u_long wtpWirelessIfAdminStatus_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfAdminStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfAdminStatus reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpWirelessIfAdminStatus_val) {
        case WTPWIRELESSIFADMINSTATUS_UP:
             wtpWirelessIfAdminStatus_val = INTERNAL_WTPWIRELESSIFADMINSTATUS_UP;
             break;

        case WTPWIRELESSIFADMINSTATUS_DOWN:
             wtpWirelessIfAdminStatus_val = INTERNAL_WTPWIRELESSIFADMINSTATUS_DOWN;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpWirelessIfAdminStatus\n", wtpWirelessIfAdminStatus_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpWirelessIfAdminStatus value.
     * set wtpWirelessIfAdminStatus value in rowreq_ctx->data
     */   
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	if(wtpWirelessIfAdminStatus_val == INTERNAL_WTPWIRELESSIFADMINSTATUS_UP)
	{
		ret=config_radio_service(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrRadioID,"enable");
	}
	else
	{
		ret=config_radio_service(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrRadioID,"disable");
	}
	if(ret==1)
	{
		rowreq_ctx->data.wtpWirelessIfAdminStatus = wtpWirelessIfAdminStatus_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}    

    return rc;
} /* wtpWirelessIfAdminStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfAdminStatus_undo( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfAdminStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfAdminStatus undo.
     */
    /*
     * copy wtpWirelessIfAdminStatus data
     * set rowreq_ctx->data.wtpWirelessIfAdminStatus from rowreq_ctx->undo->wtpWirelessIfAdminStatus
     */
    rowreq_ctx->data.wtpWirelessIfAdminStatus = rowreq_ctx->undo->wtpWirelessIfAdminStatus;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfAdminStatus_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfRadioChannelAutoSelectEnable
 * wtpWirelessIfRadioChannelAutoSelectEnable is subid 10 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.10
 * Description:
Whether select and config wireless channel .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 1/8. Values:  auto(1), manual(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfRadioChannelAutoSelectEnable_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  auto(1), manual(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfRadioChannelAutoSelectEnable_check_value( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, u_long wtpWirelessIfRadioChannelAutoSelectEnable_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfRadioChannelAutoSelectEnable_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfRadioChannelAutoSelectEnable value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfRadioChannelAutoSelectEnable value not illegal */
} /* wtpWirelessIfRadioChannelAutoSelectEnable_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfRadioChannelAutoSelectEnable_undo_setup( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfRadioChannelAutoSelectEnable_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfRadioChannelAutoSelectEnable undo.
     */
    /*
     * copy wtpWirelessIfRadioChannelAutoSelectEnable data
     * set rowreq_ctx->undo->wtpWirelessIfRadioChannelAutoSelectEnable from rowreq_ctx->data.wtpWirelessIfRadioChannelAutoSelectEnable
     */
    rowreq_ctx->undo->wtpWirelessIfRadioChannelAutoSelectEnable = rowreq_ctx->data.wtpWirelessIfRadioChannelAutoSelectEnable;


    return MFD_SUCCESS;
} /* wtpWirelessIfRadioChannelAutoSelectEnable_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfRadioChannelAutoSelectEnable_val
 *        A long containing the new value.
 */
int
wtpWirelessIfRadioChannelAutoSelectEnable_set( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, u_long wtpWirelessIfRadioChannelAutoSelectEnable_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfRadioChannelAutoSelectEnable_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfRadioChannelAutoSelectEnable reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpWirelessIfRadioChannelAutoSelectEnable_val) {
        case WTPWIRELESSIFRADIOCHANNELAUTOSELECTENABLE_AUTO:
             wtpWirelessIfRadioChannelAutoSelectEnable_val = INTERNAL_WTPWIRELESSIFRADIOCHANNELAUTOSELECTENABLE_AUTO;
             break;

        case WTPWIRELESSIFRADIOCHANNELAUTOSELECTENABLE_MANUAL:
             wtpWirelessIfRadioChannelAutoSelectEnable_val = INTERNAL_WTPWIRELESSIFRADIOCHANNELAUTOSELECTENABLE_MANUAL;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpWirelessIfRadioChannelAutoSelectEnable\n", wtpWirelessIfRadioChannelAutoSelectEnable_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpWirelessIfRadioChannelAutoSelectEnable value.
     * set wtpWirelessIfRadioChannelAutoSelectEnable value in rowreq_ctx->data
     */ 
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char auto_channel_state[10];
	memset(auto_channel_state,0,10);	
	if(wtpWirelessIfRadioChannelAutoSelectEnable_val == INTERNAL_WTPWIRELESSIFRADIOCHANNELAUTOSELECTENABLE_AUTO)
	{
		strncpy(auto_channel_state,"auto",sizeof(auto_channel_state)-1);
	}
	else if(wtpWirelessIfRadioChannelAutoSelectEnable_val == INTERNAL_WTPWIRELESSIFRADIOCHANNELAUTOSELECTENABLE_MANUAL)
	{
		strncpy(auto_channel_state,"1",sizeof(auto_channel_state)-1);
	}

	ret = config_radio_channel(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrRadioID,auto_channel_state);
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWirelessIfRadioChannelAutoSelectEnable = wtpWirelessIfRadioChannelAutoSelectEnable_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWirelessIfRadioChannelAutoSelectEnable_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfRadioChannelAutoSelectEnable_undo( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfRadioChannelAutoSelectEnable_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfRadioChannelAutoSelectEnable undo.
     */
    /*
     * copy wtpWirelessIfRadioChannelAutoSelectEnable data
     * set rowreq_ctx->data.wtpWirelessIfRadioChannelAutoSelectEnable from rowreq_ctx->undo->wtpWirelessIfRadioChannelAutoSelectEnable
     */
    rowreq_ctx->data.wtpWirelessIfRadioChannelAutoSelectEnable = rowreq_ctx->undo->wtpWirelessIfRadioChannelAutoSelectEnable;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfRadioChannelAutoSelectEnable_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfRadioChannelConfig
 * wtpWirelessIfRadioChannelConfig is subid 11 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.11
 * Description:
If wireless channel select switch is closed,it needs to config wireless channel manually.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 11;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfRadioChannelConfig_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 11
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfRadioChannelConfig_check_value( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfRadioChannelConfig_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfRadioChannelConfig_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfRadioChannelConfig value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfRadioChannelConfig value not illegal */
} /* wtpWirelessIfRadioChannelConfig_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfRadioChannelConfig_undo_setup( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfRadioChannelConfig_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfRadioChannelConfig undo.
     */
    /*
     * copy wtpWirelessIfRadioChannelConfig data
     * set rowreq_ctx->undo->wtpWirelessIfRadioChannelConfig from rowreq_ctx->data.wtpWirelessIfRadioChannelConfig
     */
    rowreq_ctx->undo->wtpWirelessIfRadioChannelConfig = rowreq_ctx->data.wtpWirelessIfRadioChannelConfig;


    return MFD_SUCCESS;
} /* wtpWirelessIfRadioChannelConfig_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfRadioChannelConfig_val
 *        A long containing the new value.
 */
int
wtpWirelessIfRadioChannelConfig_set( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfRadioChannelConfig_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfRadioChannelConfig_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfRadioChannelConfig reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpWirelessIfRadioChannelConfig value.
     * set wtpWirelessIfRadioChannelConfig value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char channel_value[10];
	memset(channel_value,0,10);	
	snprintf(channel_value,sizeof(channel_value)-1,"%d",wtpWirelessIfRadioChannelConfig_val);

	ret = config_radio_channel(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrRadioID,channel_value);
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWirelessIfRadioChannelConfig = wtpWirelessIfRadioChannelConfig_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWirelessIfRadioChannelConfig_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfRadioChannelConfig_undo( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfRadioChannelConfig_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfRadioChannelConfig undo.
     */
    /*
     * copy wtpWirelessIfRadioChannelConfig data
     * set rowreq_ctx->data.wtpWirelessIfRadioChannelConfig from rowreq_ctx->undo->wtpWirelessIfRadioChannelConfig
     */
    rowreq_ctx->data.wtpWirelessIfRadioChannelConfig = rowreq_ctx->undo->wtpWirelessIfRadioChannelConfig;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfRadioChannelConfig_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfTransmitSpeedConfig
 * wtpWirelessIfTransmitSpeedConfig is subid 14 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.14
 * Description:
Wireless interface transport speed.(The value you input must be 10,20,55,60,90,110,120,180,240,360,480,540,otherwise you will not set the value)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfTransmitSpeedConfig_val_ptr
 *        A char containing the new value.
 * @param wtpWirelessIfTransmitSpeedConfig_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpWirelessIfTransmitSpeedConfig_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfTransmitSpeedConfig_check_value( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, char *wtpWirelessIfTransmitSpeedConfig_val_ptr,  size_t wtpWirelessIfTransmitSpeedConfig_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfTransmitSpeedConfig_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpWirelessIfTransmitSpeedConfig_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfTransmitSpeedConfig value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfTransmitSpeedConfig value not illegal */
} /* wtpWirelessIfTransmitSpeedConfig_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfTransmitSpeedConfig_undo_setup( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfTransmitSpeedConfig_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfTransmitSpeedConfig undo.
     */
    /*
     * copy wtpWirelessIfTransmitSpeedConfig and wtpWirelessIfTransmitSpeedConfig_len data
     * set rowreq_ctx->undo->wtpWirelessIfTransmitSpeedConfig from rowreq_ctx->data.wtpWirelessIfTransmitSpeedConfig
     */
    memcpy( rowreq_ctx->undo->wtpWirelessIfTransmitSpeedConfig, rowreq_ctx->data.wtpWirelessIfTransmitSpeedConfig,
            (rowreq_ctx->data.wtpWirelessIfTransmitSpeedConfig_len * sizeof(rowreq_ctx->undo->wtpWirelessIfTransmitSpeedConfig[0])));
    rowreq_ctx->undo->wtpWirelessIfTransmitSpeedConfig_len = rowreq_ctx->data.wtpWirelessIfTransmitSpeedConfig_len;


    return MFD_SUCCESS;
} /* wtpWirelessIfTransmitSpeedConfig_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfTransmitSpeedConfig_val_ptr
 *        A char containing the new value.
 * @param wtpWirelessIfTransmitSpeedConfig_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpWirelessIfTransmitSpeedConfig_val_ptr
 */
int
wtpWirelessIfTransmitSpeedConfig_set( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, char *wtpWirelessIfTransmitSpeedConfig_val_ptr,  size_t wtpWirelessIfTransmitSpeedConfig_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfTransmitSpeedConfig_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpWirelessIfTransmitSpeedConfig_val_ptr);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfTransmitSpeedConfig reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpWirelessIfTransmitSpeedConfig value.
     * set wtpWirelessIfTransmitSpeedConfig value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_rate(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrRadioID,wtpWirelessIfTransmitSpeedConfig_val_ptr);
	if(ret == 1)
	{
		memcpy( rowreq_ctx->data.wtpWirelessIfTransmitSpeedConfig, wtpWirelessIfTransmitSpeedConfig_val_ptr, wtpWirelessIfTransmitSpeedConfig_val_ptr_len );
	    rowreq_ctx->data.wtpWirelessIfTransmitSpeedConfig_len = wtpWirelessIfTransmitSpeedConfig_val_ptr_len / sizeof(wtpWirelessIfTransmitSpeedConfig_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWirelessIfTransmitSpeedConfig_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfTransmitSpeedConfig_undo( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfTransmitSpeedConfig_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfTransmitSpeedConfig undo.
     */
    /*
     * copy wtpWirelessIfTransmitSpeedConfig and wtpWirelessIfTransmitSpeedConfig_len data
     * set rowreq_ctx->data.wtpWirelessIfTransmitSpeedConfig from rowreq_ctx->undo->wtpWirelessIfTransmitSpeedConfig
     */
    memcpy( rowreq_ctx->data.wtpWirelessIfTransmitSpeedConfig, rowreq_ctx->undo->wtpWirelessIfTransmitSpeedConfig,
            (rowreq_ctx->undo->wtpWirelessIfTransmitSpeedConfig_len * sizeof(rowreq_ctx->data.wtpWirelessIfTransmitSpeedConfig[0])));
    rowreq_ctx->data.wtpWirelessIfTransmitSpeedConfig_len = rowreq_ctx->undo->wtpWirelessIfTransmitSpeedConfig_len;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfTransmitSpeedConfig_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfPwrAttValue
 * wtpWirelessIfPwrAttValue is subid 16 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.16
 * Description:
Accuracy 0.5dB,step length 1dB.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfPwrAttValue_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfPwrAttValue_check_value( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfPwrAttValue_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfPwrAttValue_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfPwrAttValue value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfPwrAttValue value not illegal */
} /* wtpWirelessIfPwrAttValue_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfPwrAttValue_undo_setup( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfPwrAttValue_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfPwrAttValue undo.
     */
    /*
     * copy wtpWirelessIfPwrAttValue data
     * set rowreq_ctx->undo->wtpWirelessIfPwrAttValue from rowreq_ctx->data.wtpWirelessIfPwrAttValue
     */
    rowreq_ctx->undo->wtpWirelessIfPwrAttValue = rowreq_ctx->data.wtpWirelessIfPwrAttValue;


    return MFD_SUCCESS;
} /* wtpWirelessIfPwrAttValue_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfPwrAttValue_val
 *        A long containing the new value.
 */
int
wtpWirelessIfPwrAttValue_set( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfPwrAttValue_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfPwrAttValue_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfPwrAttValue reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpWirelessIfPwrAttValue value.
     * set wtpWirelessIfPwrAttValue value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_txpower(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrRadioID,wtpWirelessIfPwrAttValue_val);
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWirelessIfPwrAttValue = wtpWirelessIfPwrAttValue_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWirelessIfPwrAttValue_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfPwrAttValue_undo( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfPwrAttValue_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfPwrAttValue undo.
     */
    /*
     * copy wtpWirelessIfPwrAttValue data
     * set rowreq_ctx->data.wtpWirelessIfPwrAttValue from rowreq_ctx->undo->wtpWirelessIfPwrAttValue
     */
    rowreq_ctx->data.wtpWirelessIfPwrAttValue = rowreq_ctx->undo->wtpWirelessIfPwrAttValue;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfPwrAttValue_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfPowerMgmtEnable
 * wtpWirelessIfPowerMgmtEnable is subid 18 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.18
 * Description:
ON:AP configs transmission power automatically,power attenuation configuration invalid;OFF:manual adjustment,power attenuation configuration valid.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  OFF(0), ON(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfPowerMgmtEnable_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  OFF(0), ON(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfPowerMgmtEnable_check_value( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, u_long wtpWirelessIfPowerMgmtEnable_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfPowerMgmtEnable_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfPowerMgmtEnable value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfPowerMgmtEnable value not illegal */
} /* wtpWirelessIfPowerMgmtEnable_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfPowerMgmtEnable_undo_setup( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfPowerMgmtEnable_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfPowerMgmtEnable undo.
     */
    /*
     * copy wtpWirelessIfPowerMgmtEnable data
     * set rowreq_ctx->undo->wtpWirelessIfPowerMgmtEnable from rowreq_ctx->data.wtpWirelessIfPowerMgmtEnable
     */
    rowreq_ctx->undo->wtpWirelessIfPowerMgmtEnable = rowreq_ctx->data.wtpWirelessIfPowerMgmtEnable;


    return MFD_SUCCESS;
} /* wtpWirelessIfPowerMgmtEnable_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfPowerMgmtEnable_val
 *        A long containing the new value.
 */
int
wtpWirelessIfPowerMgmtEnable_set( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, u_long wtpWirelessIfPowerMgmtEnable_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfPowerMgmtEnable_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfPowerMgmtEnable reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpWirelessIfPowerMgmtEnable_val) {
        case WTPWIRELESSIFPOWERMGMTENABLE_OFF:
             wtpWirelessIfPowerMgmtEnable_val = INTERNAL_WTPWIRELESSIFPOWERMGMTENABLE_OFF;
             break;

        case WTPWIRELESSIFPOWERMGMTENABLE_ON:
             wtpWirelessIfPowerMgmtEnable_val = INTERNAL_WTPWIRELESSIFPOWERMGMTENABLE_ON;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpWirelessIfPowerMgmtEnable\n", wtpWirelessIfPowerMgmtEnable_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpWirelessIfPowerMgmtEnable value.
     * set wtpWirelessIfPowerMgmtEnable value in rowreq_ctx->data
     */  
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int rc = MFD_ERROR;
	int ret = 0;

	if(wtpWirelessIfPowerMgmtEnable_val == INTERNAL_WTPWIRELESSIFPOWERMGMTENABLE_ON)
	{
		ret = config_radio_txpower(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrRadioID,100);
	}
	else
	{
		ret = config_radio_txpower(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrRadioID,20);
	}
	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWirelessIfPowerMgmtEnable = wtpWirelessIfPowerMgmtEnable_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	        
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWirelessIfPowerMgmtEnable_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfPowerMgmtEnable_undo( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfPowerMgmtEnable_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfPowerMgmtEnable undo.
     */
    /*
     * copy wtpWirelessIfPowerMgmtEnable data
     * set rowreq_ctx->data.wtpWirelessIfPowerMgmtEnable from rowreq_ctx->undo->wtpWirelessIfPowerMgmtEnable
     */
    rowreq_ctx->data.wtpWirelessIfPowerMgmtEnable = rowreq_ctx->undo->wtpWirelessIfPowerMgmtEnable;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfPowerMgmtEnable_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfMaxStationNumPermitted
 * wtpWirelessIfMaxStationNumPermitted is subid 19 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.19
 * Description:
support max station number
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 64;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfMaxStationNumPermitted_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - 64
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfMaxStationNumPermitted_check_value( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfMaxStationNumPermitted_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfMaxStationNumPermitted_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfMaxStationNumPermitted value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfMaxStationNumPermitted value not illegal */
} /* wtpWirelessIfMaxStationNumPermitted_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfMaxStationNumPermitted_undo_setup( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfMaxStationNumPermitted_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfMaxStationNumPermitted undo.
     */
    /*
     * copy wtpWirelessIfMaxStationNumPermitted data
     * set rowreq_ctx->undo->wtpWirelessIfMaxStationNumPermitted from rowreq_ctx->data.wtpWirelessIfMaxStationNumPermitted
     */
    rowreq_ctx->undo->wtpWirelessIfMaxStationNumPermitted = rowreq_ctx->data.wtpWirelessIfMaxStationNumPermitted;


    return MFD_SUCCESS;
} /* wtpWirelessIfMaxStationNumPermitted_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfMaxStationNumPermitted_val
 *        A long containing the new value.
 */
int
wtpWirelessIfMaxStationNumPermitted_set( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfMaxStationNumPermitted_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfMaxStationNumPermitted_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfMaxStationNumPermitted reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpWirelessIfMaxStationNumPermitted value.
     * set wtpWirelessIfMaxStationNumPermitted value in rowreq_ctx->data
     */ 
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int rc = MFD_ERROR;
	int ret = 0;
	char max_sta[10];
	memset(max_sta,0,10);
	snprintf(max_sta,sizeof(max_sta)-1,"%d",wtpWirelessIfMaxStationNumPermitted_val);

	ret = config_wtp_max_sta_num(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrID,max_sta);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWirelessIfMaxStationNumPermitted = wtpWirelessIfMaxStationNumPermitted_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	        
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWirelessIfMaxStationNumPermitted_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfMaxStationNumPermitted_undo( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfMaxStationNumPermitted_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfMaxStationNumPermitted undo.
     */
    /*
     * copy wtpWirelessIfMaxStationNumPermitted data
     * set rowreq_ctx->data.wtpWirelessIfMaxStationNumPermitted from rowreq_ctx->undo->wtpWirelessIfMaxStationNumPermitted
     */
    rowreq_ctx->data.wtpWirelessIfMaxStationNumPermitted = rowreq_ctx->undo->wtpWirelessIfMaxStationNumPermitted;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfMaxStationNumPermitted_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfTxPwrStep
 * wtpWirelessIfTxPwrStep is subid 21 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.3.3.1.21
 * Description:
txpower adjustment step
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfTxPwrStep_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfTxPwrStep_check_value( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfTxPwrStep_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfTxPwrStep_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfTxPwrStep value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfTxPwrStep value not illegal */
} /* wtpWirelessIfTxPwrStep_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfTxPwrStep_undo_setup( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfTxPwrStep_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfTxPwrStep undo.
     */
    /*
     * copy wtpWirelessIfTxPwrStep data
     * set rowreq_ctx->undo->wtpWirelessIfTxPwrStep from rowreq_ctx->data.wtpWirelessIfTxPwrStep
     */
    rowreq_ctx->undo->wtpWirelessIfTxPwrStep = rowreq_ctx->data.wtpWirelessIfTxPwrStep;


    return MFD_SUCCESS;
} /* wtpWirelessIfTxPwrStep_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfTxPwrStep_val
 *        A long containing the new value.
 */
int
wtpWirelessIfTxPwrStep_set( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfTxPwrStep_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfTxPwrStep_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfTxPwrStep reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpWirelessIfTxPwrStep value.
     * set wtpWirelessIfTxPwrStep value in rowreq_ctx->data
     */   
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char txp_step[10] = { 0 };
	
	memset(txp_step,0,10);
	snprintf(txp_step,sizeof(txp_step)-1,"%d",wtpWirelessIfTxPwrStep_val);
	ret=set_radio_txpowerstep_cmd(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrRadioID,txp_step);
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWirelessIfTxPwrStep = wtpWirelessIfTxPwrStep_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpWirelessIfTxPwrStep_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfTxPwrStep_undo( dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfTable:wtpWirelessIfTxPwrStep_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfTxPwrStep undo.
     */
    /*
     * copy wtpWirelessIfTxPwrStep data
     * set rowreq_ctx->data.wtpWirelessIfTxPwrStep from rowreq_ctx->undo->wtpWirelessIfTxPwrStep
     */
    rowreq_ctx->data.wtpWirelessIfTxPwrStep = rowreq_ctx->undo->wtpWirelessIfTxPwrStep;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfTxPwrStep_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpWirelessIfTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11WtpWirelessIfTable if you don't have dependencies)
 */
int
dot11WtpWirelessIfTable_check_dependencies(dot11WtpWirelessIfTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11WtpWirelessIfTable:dot11WtpWirelessIfTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11WtpWirelessIfTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11WtpWirelessIfTable_check_dependencies */

/** @} */
