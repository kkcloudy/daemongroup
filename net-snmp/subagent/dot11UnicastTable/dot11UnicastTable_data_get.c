/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11UnicastTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11UnicastTable get routines.
 * TODO:240:M: Implement dot11UnicastTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11UnicastTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11UnicastTable is subid 9 of wtpWAPI.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.9, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11UnicastTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UnicastEntry.UnicastWlanID
 * UnicastWlanID is subid 1 of dot11UnicastEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.9.1.1
 * Description:
.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-UnicastWlanID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
UnicastWlanID_map(long *mib_UnicastWlanID_val_ptr, long raw_UnicastWlanID_val)
{
    netsnmp_assert(NULL != mib_UnicastWlanID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UnicastTable:UnicastWlanID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement UnicastWlanID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_UnicastWlanID_val_ptr) = raw_UnicastWlanID_val;

    return MFD_SUCCESS;
} /* UnicastWlanID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11UnicastTable_indexes_set_tbl_idx(dot11UnicastTable_mib_index *tbl_idx, long UnicastWlanID_val)
{
    DEBUGMSGTL(("verbose:dot11UnicastTable:dot11UnicastTable_indexes_set_tbl_idx","called\n"));

    /* UnicastWlanID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->UnicastWlanID = UnicastWlanID_val;
    

    return MFD_SUCCESS;
} /* dot11UnicastTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11UnicastTable_indexes_set(dot11UnicastTable_rowreq_ctx *rowreq_ctx, long UnicastWlanID_val)
{
    DEBUGMSGTL(("verbose:dot11UnicastTable:dot11UnicastTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11UnicastTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , UnicastWlanID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11UnicastTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11UnicastTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UnicastEntry.NewUnicastCipher
 * NewUnicastCipher is subid 2 of dot11UnicastEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.9.1.2
 * Description:
.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewUnicastCipher.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewUnicastCipher_map(char **mib_NewUnicastCipher_val_ptr_ptr, size_t *mib_NewUnicastCipher_val_ptr_len_ptr, char *raw_NewUnicastCipher_val_ptr, size_t raw_NewUnicastCipher_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_NewUnicastCipher_val_ptr);
    netsnmp_assert((NULL != mib_NewUnicastCipher_val_ptr_ptr) && (NULL != mib_NewUnicastCipher_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11UnicastTable:NewUnicastCipher_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement NewUnicastCipher non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_NewUnicastCipher_val_ptr_len; /* assume equal */
    if((NULL == *mib_NewUnicastCipher_val_ptr_ptr) || (*mib_NewUnicastCipher_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_NewUnicastCipher_val_ptr_ptr = realloc( *mib_NewUnicastCipher_val_ptr_ptr, converted_len * sizeof(**mib_NewUnicastCipher_val_ptr_ptr));
        if(NULL == *mib_NewUnicastCipher_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_NewUnicastCipher_val_ptr_len_ptr = converted_len;
    memcpy( *mib_NewUnicastCipher_val_ptr_ptr, raw_NewUnicastCipher_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* NewUnicastCipher_map */

/**
 * Extract the current value of the NewUnicastCipher data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewUnicastCipher_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param NewUnicastCipher_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by NewUnicastCipher.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*NewUnicastCipher_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update NewUnicastCipher_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
NewUnicastCipher_get( dot11UnicastTable_rowreq_ctx *rowreq_ctx, char **NewUnicastCipher_val_ptr_ptr, size_t *NewUnicastCipher_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != NewUnicastCipher_val_ptr_ptr) && (NULL != *NewUnicastCipher_val_ptr_ptr));
   netsnmp_assert( NULL != NewUnicastCipher_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11UnicastTable:NewUnicastCipher_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewUnicastCipher data.
 * set (* NewUnicastCipher_val_ptr_ptr ) and (* NewUnicastCipher_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for NewUnicastCipher data
     */
    if ((NULL == (* NewUnicastCipher_val_ptr_ptr )) ||
        ((* NewUnicastCipher_val_ptr_len_ptr ) < (rowreq_ctx->data.NewUnicastCipher_len * sizeof((* NewUnicastCipher_val_ptr_ptr )[0])))) {
        /*
         * allocate space for NewUnicastCipher data
         */
        (* NewUnicastCipher_val_ptr_ptr ) = malloc(rowreq_ctx->data.NewUnicastCipher_len * sizeof((* NewUnicastCipher_val_ptr_ptr )[0]));
        if(NULL == (* NewUnicastCipher_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* NewUnicastCipher_val_ptr_len_ptr ) = rowreq_ctx->data.NewUnicastCipher_len * sizeof((* NewUnicastCipher_val_ptr_ptr )[0]);
    memcpy( (* NewUnicastCipher_val_ptr_ptr ), rowreq_ctx->data.NewUnicastCipher, (* NewUnicastCipher_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* NewUnicastCipher_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UnicastEntry.NewUnicastCipherEnabled
 * NewUnicastCipherEnabled is subid 3 of dot11UnicastEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.9.1.3
 * Description:
.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewUnicastCipherEnabled.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewUnicastCipherEnabled_map(u_long *mib_NewUnicastCipherEnabled_val_ptr, u_long raw_NewUnicastCipherEnabled_val)
{
    netsnmp_assert(NULL != mib_NewUnicastCipherEnabled_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UnicastTable:NewUnicastCipherEnabled_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewUnicastCipherEnabled enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_NewUnicastCipherEnabled_val) {
        case INTERNAL_NEWUNICASTCIPHERENABLED_TRUE:
             *mib_NewUnicastCipherEnabled_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_NEWUNICASTCIPHERENABLED_FALSE:
             *mib_NewUnicastCipherEnabled_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for NewUnicastCipherEnabled\n", raw_NewUnicastCipherEnabled_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* NewUnicastCipherEnabled_map */

/**
 * Extract the current value of the NewUnicastCipherEnabled data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewUnicastCipherEnabled_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewUnicastCipherEnabled_get( dot11UnicastTable_rowreq_ctx *rowreq_ctx, u_long * NewUnicastCipherEnabled_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewUnicastCipherEnabled_val_ptr );


    DEBUGMSGTL(("verbose:dot11UnicastTable:NewUnicastCipherEnabled_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewUnicastCipherEnabled data.
 * set (* NewUnicastCipherEnabled_val_ptr ) from rowreq_ctx->data
 */
    (* NewUnicastCipherEnabled_val_ptr ) = rowreq_ctx->data.NewUnicastCipherEnabled;

    return MFD_SUCCESS;
} /* NewUnicastCipherEnabled_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11UnicastEntry.NewUnicastCipherSize
 * NewUnicastCipherSize is subid 4 of dot11UnicastEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.10.9.1.4
 * Description:
.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-NewUnicastCipherSize.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
NewUnicastCipherSize_map(u_long *mib_NewUnicastCipherSize_val_ptr, u_long raw_NewUnicastCipherSize_val)
{
    netsnmp_assert(NULL != mib_NewUnicastCipherSize_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11UnicastTable:NewUnicastCipherSize_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement NewUnicastCipherSize mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_NewUnicastCipherSize_val_ptr) = raw_NewUnicastCipherSize_val;

    return MFD_SUCCESS;
} /* NewUnicastCipherSize_map */

/**
 * Extract the current value of the NewUnicastCipherSize data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewUnicastCipherSize_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
NewUnicastCipherSize_get( dot11UnicastTable_rowreq_ctx *rowreq_ctx, u_long * NewUnicastCipherSize_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != NewUnicastCipherSize_val_ptr );


    DEBUGMSGTL(("verbose:dot11UnicastTable:NewUnicastCipherSize_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the NewUnicastCipherSize data.
 * set (* NewUnicastCipherSize_val_ptr ) from rowreq_ctx->data
 */
    (* NewUnicastCipherSize_val_ptr ) = rowreq_ctx->data.NewUnicastCipherSize;

    return MFD_SUCCESS;
} /* NewUnicastCipherSize_get */



/** @} */
