/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11CreateWtpTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11CreateWtpTable get routines.
 * TODO:240:M: Implement dot11CreateWtpTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11CreateWtpTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11CreateWtpTable is subid 1 of dot11wtpConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11CreateWtpTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param wtpMac_ptr
 * @param wtpMac_ptr_len
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11CreateWtpTable_indexes_set_tbl_idx(dot11CreateWtpTable_mib_index *tbl_idx, char *wtpMac_val_ptr,  size_t wtpMac_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11CreateWtpTable:dot11CreateWtpTable_indexes_set_tbl_idx","called\n"));

    /* wtpMac(4)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    tbl_idx->wtpMac_len = sizeof(tbl_idx->wtpMac)/sizeof(tbl_idx->wtpMac[0]); /* max length */
    /*
     * make sure there is enough space for wtpMac data
     */
    if ((NULL == tbl_idx->wtpMac) ||
        (tbl_idx->wtpMac_len <
         (wtpMac_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMac_len = wtpMac_val_ptr_len;
    memcpy( tbl_idx->wtpMac, wtpMac_val_ptr, wtpMac_val_ptr_len* sizeof(wtpMac_val_ptr[0]) );
    

    return MFD_SUCCESS;
} /* dot11CreateWtpTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11CreateWtpTable_indexes_set(dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, char *wtpMac_val_ptr,  size_t wtpMac_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11CreateWtpTable:dot11CreateWtpTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11CreateWtpTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMac_val_ptr, wtpMac_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11CreateWtpTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11CreateWtpTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11CreateWtpEntry.wtpID
 * wtpID is subid 1 of dot11CreateWtpEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1.1.1
 * Description:
Create AP ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 1023;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the wtpID data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpID_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpID_get( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, long * wtpID_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpID_val_ptr );


    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpID_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpID data.
 * copy (* wtpID_val_ptr ) from rowreq_ctx->data
 */
    (* wtpID_val_ptr ) = rowreq_ctx->data.wtpID;

    return MFD_SUCCESS;
} /* wtpID_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11CreateWtpEntry.wtpName
 * wtpName is subid 2 of dot11CreateWtpEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1.1.2
 * Description:
AP name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 15)
 */
/**
 * Extract the current value of the wtpName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpName_get( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, char **wtpName_val_ptr_ptr, size_t *wtpName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpName_val_ptr_ptr) && (NULL != *wtpName_val_ptr_ptr));
   netsnmp_assert( NULL != wtpName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpName data.
 * copy (* wtpName_val_ptr_ptr ) data and (* wtpName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpName data
     */
    if ((NULL == (* wtpName_val_ptr_ptr )) ||
        ((* wtpName_val_ptr_len_ptr ) <
         (rowreq_ctx->data.wtpName_len* sizeof(rowreq_ctx->data.wtpName[0])))) {
        /*
         * allocate space for wtpName data
         */
        (* wtpName_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpName_len* sizeof(rowreq_ctx->data.wtpName[0]));
        if(NULL == (* wtpName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpName_val_ptr_len_ptr ) = rowreq_ctx->data.wtpName_len* sizeof(rowreq_ctx->data.wtpName[0]);
    memcpy( (* wtpName_val_ptr_ptr ), rowreq_ctx->data.wtpName, rowreq_ctx->data.wtpName_len* sizeof(rowreq_ctx->data.wtpName[0]) );

    return MFD_SUCCESS;
} /* wtpName_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11CreateWtpEntry.wtpModel
 * wtpModel is subid 3 of dot11CreateWtpEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1.1.3
 * Description:
AP model.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the wtpModel data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpModel_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpModel_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpModel.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpModel_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpModel_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpModel_get( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, char **wtpModel_val_ptr_ptr, size_t *wtpModel_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpModel_val_ptr_ptr) && (NULL != *wtpModel_val_ptr_ptr));
   netsnmp_assert( NULL != wtpModel_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpModel_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpModel data.
 * copy (* wtpModel_val_ptr_ptr ) data and (* wtpModel_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpModel data
     */
    if ((NULL == (* wtpModel_val_ptr_ptr )) ||
        ((* wtpModel_val_ptr_len_ptr ) <
         (rowreq_ctx->data.wtpModel_len* sizeof(rowreq_ctx->data.wtpModel[0])))) {
        /*
         * allocate space for wtpModel data
         */
        (* wtpModel_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpModel_len* sizeof(rowreq_ctx->data.wtpModel[0]));
        if(NULL == (* wtpModel_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpModel_val_ptr_len_ptr ) = rowreq_ctx->data.wtpModel_len* sizeof(rowreq_ctx->data.wtpModel[0]);
    memcpy( (* wtpModel_val_ptr_ptr ), rowreq_ctx->data.wtpModel, rowreq_ctx->data.wtpModel_len* sizeof(rowreq_ctx->data.wtpModel[0]) );

    return MFD_SUCCESS;
} /* wtpModel_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11CreateWtpEntry.wtpRowStatus
 * wtpRowStatus is subid 5 of dot11CreateWtpEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1.1.5
 * Description:
This variable is used to create, modify, and/or delete a row
		        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the wtpRowStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRowStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpRowStatus_get( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, u_long * wtpRowStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpRowStatus_val_ptr );

    (* wtpRowStatus_val_ptr ) = rowreq_ctx->data.wtpRowStatus;

    return MFD_SUCCESS;
} /* wtpRowStatus_get */



/** @} */
