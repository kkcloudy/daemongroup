/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11CreateWtpTable.h"

#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11CreateWtpTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11CreateWtpTable is subid 1 of dot11wtpConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11CreateWtpTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11CreateWtpTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11CreateWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11CreateWtpTable_undo_setup( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:dot11CreateWtpTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11CreateWtpTable undo.
     * set up dot11CreateWtpTable undo information, in preparation for a set.
     * Undo storage is in (* wtpRowStatus_val_ptr )*
     */

    return rc;
} /* dot11CreateWtpTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11CreateWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11CreateWtpTable_undo( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:dot11CreateWtpTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11CreateWtpTable undo.
     * dot11CreateWtpTable undo information, in response to a failed set.
     * Undo storage is in (* wtpRowStatus_val_ptr )*
     */

    return rc;
} /* dot11CreateWtpTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11CreateWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11CreateWtpTable_undo_cleanup( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:dot11CreateWtpTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11CreateWtpTable undo.
     * Undo storage is in (* wtpRowStatus_val_ptr )*
     */

    return rc;
} /* dot11CreateWtpTable_undo_cleanup */

static int
check_create_ready(dot11CreateWtpTable_rowreq_ctx *rowreq_ctx) {
    if((rowreq_ctx->data.wtpID > 0) && strlen(rowreq_ctx->data.wtpName) && strlen(rowreq_ctx->data.wtpModel)) {
        return MFD_SUCCESS;
    }

    return MFD_ERROR;
}

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11CreateWtpTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11CreateWtpTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11CreateWtpTable_commit( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:dot11CreateWtpTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    if(RS_NOTREADY == rowreq_ctx->data.wtpRowStatus && MFD_SUCCESS == check_create_ready(rowreq_ctx)) {
        rowreq_ctx->data.wtpRowStatus = RS_NOTINSERVICE;
    }
    
    /*
     * commit dot11CreateWtpTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_WTPID_FLAG) {
       save_flags &= ~COLUMN_WTPID_FLAG; /* clear wtpID */
       /*
        * TODO:482:o: |-> commit column wtpID.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11CreateWtpTable column wtpID commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpID
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPID_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPNAME_FLAG) {
       save_flags &= ~COLUMN_WTPNAME_FLAG; /* clear wtpName */
       /*
        * TODO:482:o: |-> commit column wtpName.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11CreateWtpTable column wtpName commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpName
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPNAME_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPMODEL_FLAG) {
       save_flags &= ~COLUMN_WTPMODEL_FLAG; /* clear wtpModel */
       /*
        * TODO:482:o: |-> commit column wtpModel.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11CreateWtpTable column wtpModel commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpModel
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPMODEL_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPROWSTATUS_FLAG) {
       save_flags &= ~COLUMN_WTPROWSTATUS_FLAG; /* clear wtpRowStatus */

        switch(rowreq_ctx->data.wtpRowStatus) {
            case RS_ACTIVE:
                {
                    char id[10] = { 0 };
                    memset(id,0,10);
                    snprintf(id, sizeof(id)-1, "%d", rowreq_ctx->data.wtpID);
                     
                    void *connection = NULL;
                    if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3)) {
                        int ret = 0; 
                        ret = create_wtp_bymac_cmd_func(rowreq_ctx->data.parameter, connection, id, rowreq_ctx->data.wtpName, rowreq_ctx->data.wtpModel, rowreq_ctx->tbl_idx.wtpMac);
                        snmp_log(LOG_DEBUG, "dot11CreateWtpTable_commit: create_wtp_bymac_cmd_func: ret = %d\n", ret);
                        if(1 == ret) {
                            rc = MFD_SUCCESS;
                            break;
                        }
                        else if(SNMPD_CONNECTION_ERROR == ret) {
                            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
                        }        
                    }
                }
                rc = -1;
                break;

            case RS_CREATEANDWAIT:
                rowreq_ctx->data.wtpRowStatus = RS_NOTREADY;
                rc = MFD_SUCCESS;
                break;
                
            case RS_DESTROY:  
                {
                    int ret = 0;
                    void *connection = NULL;
                    if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3)) {
                    
                        DCLI_WTP_API_GROUP_ONE *wtpInfo = NULL;
                        int show_ret = show_wtp_one(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpID, &wtpInfo);
                        if(1 == show_ret && wtpInfo) {
                            if(7 == wtpInfo->WTP[0]->WTPStat) { /*only wtp is offline, can delete it*/
                                if(1 == wtpInfo->WTP[0]->isused) {  /* if wtp is used, unused it*/
                                    wtp_used(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpID, 1); /* unused wtp*/
                                }
                                
                                ret = delete_wtp(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpID);
                                snmp_log(LOG_DEBUG, "dot11CreateWtpTable_commit: delete_wtp: wtp id %d: ret = %d\n", rowreq_ctx->data.wtpID, ret);
                                if(1 == ret || -2 == ret) {
                                    Free_one_wtp_head(wtpInfo);
                                    rc = MFD_SUCCESS;
                                    break;
                                }
                            }
                            Free_one_wtp_head(wtpInfo);
                        }
                        else if(-2 == show_ret) { /*delete wtp is exist in mib, but not exist in wid*/
                            rc = MFD_SUCCESS;                             
                            break;
                        }
                    
                        if(SNMPD_CONNECTION_ERROR == show_ret ||  SNMPD_CONNECTION_ERROR == ret) {
                            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
                        }
                    }
                }
                rowreq_ctx->rowreq_flags &= ~MFD_ROW_DELETED;
                rc = -1;
                break;
                
            default:
                break;
        }
           
       /*
        * TODO:482:o: |-> commit column wtpRowStatus.
        */
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11CreateWtpTable column wtpRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpRowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPROWSTATUS_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11CreateWtpTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11CreateWtpTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11CreateWtpTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11CreateWtpTable_undo_commit( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:dot11CreateWtpTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11CreateWtpTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11CreateWtpTable_undo_commit */

/*
 * TODO:440:M: Implement dot11CreateWtpTable node value checks.
 * TODO:450:M: Implement dot11CreateWtpTable undo functions.
 * TODO:460:M: Implement dot11CreateWtpTable set functions.
 * TODO:480:M: Implement dot11CreateWtpTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11CreateWtpEntry.wtpID
 * wtpID is subid 1 of dot11CreateWtpEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1.1.1
 * Description:
Create AP ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 1023;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpID_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11CreateWtpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 1023
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpID_check_value( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, long wtpID_val)
{
    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpID_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:441:o: |-> Check for valid wtpID value.
     */    
    if(wtpID_val < 1) {
        return MFD_ERROR;
    }

	void *connection = NULL;
	if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
	{
		DCLI_WTP_API_GROUP_ONE *wtpInfo = NULL; 				   
		int show_ret = show_wtp_one(rowreq_ctx->data.parameter, connection, wtpID_val, &wtpInfo);
		snmp_log(LOG_DEBUG, "wtpID_check_value: show_wtp_one %d, show_ret = %d\n", wtpID_val, show_ret);
		if(NULL != wtpInfo) {
			Free_one_wtp_head(wtpInfo);
		}		
		if(SNMPD_CONNECTION_ERROR == show_ret) 
		{
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
        }
		if(-2 != show_ret) { /*wtpid exist or dbus get error*/
			return MFD_ERROR;
		}
	}
    
    return MFD_SUCCESS; /* wtpID value not illegal */
} /* wtpID_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11CreateWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11CreateWtpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpID_undo_setup( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpID_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpID undo.
     */
    /*
     * copy wtpID data
     * set rowreq_ctx->undo->wtpID from rowreq_ctx->data.wtpID
     */
    rowreq_ctx->undo->wtpID = rowreq_ctx->data.wtpID;


    return MFD_SUCCESS;
} /* wtpID_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpID_val
 *        A long containing the new value.
 */
int
wtpID_set( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, long wtpID_val )
{

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpID_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpID value.
     * set wtpID value in rowreq_ctx->data
     */
    rowreq_ctx->data.wtpID = wtpID_val;

    return MFD_SUCCESS;
} /* wtpID_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpID_undo( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpID_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpID undo.
     */
    /*
     * copy wtpID data
     * set rowreq_ctx->data.wtpID from rowreq_ctx->undo->wtpID
     */
    rowreq_ctx->data.wtpID = rowreq_ctx->undo->wtpID;

    
    return MFD_SUCCESS;
} /* wtpID_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11CreateWtpEntry.wtpName
 * wtpName is subid 2 of dot11CreateWtpEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1.1.2
 * Description:
AP name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 15)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpName_val_ptr
 *        A char containing the new value.
 * @param wtpName_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpName_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11CreateWtpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.wtpName).
 *    The length is in (one of) the range set(s):  1 - 15
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpName_check_value( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, char *wtpName_val_ptr,  size_t wtpName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpName_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpName_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wtpName value.
     */
    if(RS_NOTINSERVICE == rowreq_ctx->data.wtpRowStatus || RS_NOTREADY == rowreq_ctx->data.wtpRowStatus)
        return MFD_SUCCESS;
    else     
        return MFD_ERROR; /* wtpName value not illegal */
} /* wtpName_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11CreateWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11CreateWtpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpName_undo_setup( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpName_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpName undo.
     */
    /*
     * copy wtpName and wtpName_len data
     * set rowreq_ctx->undo->wtpName from rowreq_ctx->data.wtpName
     */
    memcpy( rowreq_ctx->undo->wtpName, rowreq_ctx->data.wtpName,
            (rowreq_ctx->data.wtpName_len * sizeof(rowreq_ctx->undo->wtpName[0])));
    rowreq_ctx->undo->wtpName_len = rowreq_ctx->data.wtpName_len;


    return MFD_SUCCESS;
} /* wtpName_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpName_val_ptr
 *        A char containing the new value.
 * @param wtpName_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpName_val_ptr
 */
int
wtpName_set( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, char *wtpName_val_ptr,  size_t wtpName_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpName_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpName_val_ptr);

    /*
     * TODO:461:M: |-> Set wtpName value.
     * set wtpName value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.wtpName, wtpName_val_ptr, wtpName_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.wtpName_len = wtpName_val_ptr_len / sizeof(wtpName_val_ptr[0]);

    return MFD_SUCCESS;
} /* wtpName_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpName_undo( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpName_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpName undo.
     */
    /*
     * copy wtpName and wtpName_len data
     * set rowreq_ctx->data.wtpName from rowreq_ctx->undo->wtpName
     */
    memcpy( rowreq_ctx->data.wtpName, rowreq_ctx->undo->wtpName,
            (rowreq_ctx->undo->wtpName_len * sizeof(rowreq_ctx->data.wtpName[0])));
    rowreq_ctx->data.wtpName_len = rowreq_ctx->undo->wtpName_len;

    
    return MFD_SUCCESS;
} /* wtpName_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11CreateWtpEntry.wtpModel
 * wtpModel is subid 3 of dot11CreateWtpEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1.1.3
 * Description:
AP model.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpModel_val_ptr
 *        A char containing the new value.
 * @param wtpModel_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpModel_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11CreateWtpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.wtpModel).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpModel_check_value( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, char *wtpModel_val_ptr,  size_t wtpModel_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpModel_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpModel_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wtpModel value.
     */
    if(RS_NOTINSERVICE == rowreq_ctx->data.wtpRowStatus || RS_NOTREADY == rowreq_ctx->data.wtpRowStatus)
        return MFD_SUCCESS;
    else     
        return MFD_ERROR; /* wtpModel value not illegal */
} /* wtpModel_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11CreateWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11CreateWtpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpModel_undo_setup( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpModel_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpModel undo.
     */
    /*
     * copy wtpModel and wtpModel_len data
     * set rowreq_ctx->undo->wtpModel from rowreq_ctx->data.wtpModel
     */
    memcpy( rowreq_ctx->undo->wtpModel, rowreq_ctx->data.wtpModel,
            (rowreq_ctx->data.wtpModel_len * sizeof(rowreq_ctx->undo->wtpModel[0])));
    rowreq_ctx->undo->wtpModel_len = rowreq_ctx->data.wtpModel_len;


    return MFD_SUCCESS;
} /* wtpModel_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpModel_val_ptr
 *        A char containing the new value.
 * @param wtpModel_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpModel_val_ptr
 */
int
wtpModel_set( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, char *wtpModel_val_ptr,  size_t wtpModel_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpModel_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpModel_val_ptr);

    /*
     * TODO:461:M: |-> Set wtpModel value.
     * set wtpModel value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.wtpModel, wtpModel_val_ptr, wtpModel_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.wtpModel_len = wtpModel_val_ptr_len / sizeof(wtpModel_val_ptr[0]);

    return MFD_SUCCESS;
} /* wtpModel_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpModel_undo( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpModel_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpModel undo.
     */
    /*
     * copy wtpModel and wtpModel_len data
     * set rowreq_ctx->data.wtpModel from rowreq_ctx->undo->wtpModel
     */
    memcpy( rowreq_ctx->data.wtpModel, rowreq_ctx->undo->wtpModel,
            (rowreq_ctx->undo->wtpModel_len * sizeof(rowreq_ctx->data.wtpModel[0])));
    rowreq_ctx->data.wtpModel_len = rowreq_ctx->undo->wtpModel_len;

    
    return MFD_SUCCESS;
} /* wtpModel_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11CreateWtpEntry.wtpRowStatus
 * wtpRowStatus is subid 5 of dot11CreateWtpEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1.1.5
 * Description:
This variable is used to create, modify, and/or delete a row
		        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11CreateWtpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpRowStatus_check_value( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, u_long wtpRowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpRowStatus value.
     */

    return MFD_SUCCESS; /* wtpRowStatus value not illegal */
} /* wtpRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11CreateWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11CreateWtpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpRowStatus_undo_setup( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpRowStatus undo.
     */
    /*
     * copy wtpRowStatus data
     * set rowreq_ctx->undo->wtpRowStatus from rowreq_ctx->data.wtpRowStatus
     */
    rowreq_ctx->undo->wtpRowStatus = rowreq_ctx->data.wtpRowStatus;


    return MFD_SUCCESS;
} /* wtpRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpRowStatus_val
 *        A long containing the new value.
 */
int
wtpRowStatus_set( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx, u_long wtpRowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wtpRowStatus value.
     * set wtpRowStatus value in rowreq_ctx->data
     */
    int ret = MFD_SUCCESS;
    
    switch(wtpRowStatus_val) {
    
        case RS_ACTIVE:         
        case RS_DESTROY:
            rowreq_ctx->data.wtpRowStatus = wtpRowStatus_val;
            ret = MFD_SUCCESS;
            break;
    
        case RS_NOTINSERVICE:
        case RS_NOTREADY:
        case RS_CREATEANDGO:
            ret = MFD_ERROR;
            break;
    
        case RS_CREATEANDWAIT:
            if(RS_NONEXISTENT == rowreq_ctx->data.wtpRowStatus) {
				instance_parameter *paraHead = NULL;
				list_instance_parameter(&paraHead, SNMPD_INSTANCE_MASTER); 
				if(paraHead)
				{
					memcpy(&(rowreq_ctx->data.parameter), &(paraHead->parameter), sizeof(rowreq_ctx->data.parameter));
					free_instance_parameter_list(&paraHead);
					
					rowreq_ctx->data.wtpRowStatus = wtpRowStatus_val;
					ret = MFD_SUCCESS;
				}
				else
				{
					ret = MFD_ERROR;
				}
            }
            else {
                ret = MFD_ERROR;
            }
            break;
    
        default:
            snmp_log(LOG_WARNING, "unknow %s in wtpRowStatus_set", wtpRowStatus_val);
            ret = MFD_ERROR;
            break;
    }

    return ret;
} /* wtpRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpRowStatus_undo( dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11CreateWtpTable:wtpRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpRowStatus undo.
     */
    /*
     * copy wtpRowStatus data
     * set rowreq_ctx->data.wtpRowStatus from rowreq_ctx->undo->wtpRowStatus
     */
    rowreq_ctx->data.wtpRowStatus = rowreq_ctx->undo->wtpRowStatus;

    
    return MFD_SUCCESS;
} /* wtpRowStatus_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11CreateWtpTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11CreateWtpTable if you don't have dependencies)
 */
int
dot11CreateWtpTable_check_dependencies(dot11CreateWtpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11CreateWtpTable:dot11CreateWtpTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11CreateWtpTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & COLUMN_WTPROWSTATUS_FLAG) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->wtpRowStatus,
                                         rowreq_ctx->data.wtpRowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_DESTROY ==  rowreq_ctx->data.wtpRowStatus) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;
            }
            else if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.wtpRowStatus) {
                if ((rowreq_ctx->column_set_flags & DOT11CREATEWTPTABLE_REQUIRED_COLS)
                    != DOT11CREATEWTPTABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11CreateWtpTable",
                                "required columns missing (0x%0x != 0x%0x)\n",
                                rowreq_ctx->column_set_flags, DOT11CREATEWTPTABLE_REQUIRED_COLS));
                    return MFD_CANNOT_CREATE_NOW;
                }
                rowreq_ctx->data.wtpRowStatus = ROWSTATUS_ACTIVE;
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.wtpRowStatus) {
                if (rowreq_ctx->column_set_flags & ~COLUMN_WTPROWSTATUS_FLAG) {
                    DEBUGMSGTL(("dot11CreateWtpTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;

            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11CreateWtpTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11CreateWtpTable_check_dependencies */

/** @} */
