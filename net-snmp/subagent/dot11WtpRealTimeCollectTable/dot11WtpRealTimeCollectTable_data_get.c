/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpRealTimeCollectTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpRealTimeCollectTable get routines.
 * TODO:240:M: Implement dot11WtpRealTimeCollectTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpRealTimeCollectTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpRealTimeCollectTable is subid 6 of wtpGeneralInfo.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.6, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpRealTimeCollectTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11WtpRealTimeCollectTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpRealTimeCollectTable_indexes_set_tbl_idx(dot11WtpRealTimeCollectTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:dot11WtpRealTimeCollectTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11WtpRealTimeCollectTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpRealTimeCollectTable_indexes_set(dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:dot11WtpRealTimeCollectTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpRealTimeCollectTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpRealTimeCollectTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpRealTimeCollectTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpRealTimeCollectEntry.wtpRtCollectOnOff
 * wtpRtCollectOnOff is subid 1 of dot11WtpRealTimeCollectEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.6.1.1
 * Description:
AP Information Switch.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  on(1), off(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpRtCollectOnOff.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpRtCollectOnOff_map(u_long *mib_wtpRtCollectOnOff_val_ptr, u_long raw_wtpRtCollectOnOff_val)
{
    netsnmp_assert(NULL != mib_wtpRtCollectOnOff_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpRtCollectOnOff_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpRtCollectOnOff enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpRtCollectOnOff_val) {
        case INTERNAL_WTPRTCOLLECTONOFF_ON:
             *mib_wtpRtCollectOnOff_val_ptr = WTPRTCOLLECTONOFF_ON;
             break;

        case INTERNAL_WTPRTCOLLECTONOFF_OFF:
             *mib_wtpRtCollectOnOff_val_ptr = WTPRTCOLLECTONOFF_OFF;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpRtCollectOnOff\n", raw_wtpRtCollectOnOff_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpRtCollectOnOff_map */

/**
 * Extract the current value of the wtpRtCollectOnOff data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRtCollectOnOff_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpRtCollectOnOff_get( dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, u_long * wtpRtCollectOnOff_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpRtCollectOnOff_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpRtCollectOnOff_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpRtCollectOnOff data.
 * set (* wtpRtCollectOnOff_val_ptr ) from rowreq_ctx->data
 */
    (* wtpRtCollectOnOff_val_ptr ) = rowreq_ctx->data.wtpRtCollectOnOff;

    return MFD_SUCCESS;
} /* wtpRtCollectOnOff_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpRealTimeCollectEntry.CPURTUsage
 * CPURTUsage is subid 2 of dot11WtpRealTimeCollectEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.6.1.2
 * Description:
AP Cpu RTUsage.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-CPURTUsage.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
CPURTUsage_map(long *mib_CPURTUsage_val_ptr, long raw_CPURTUsage_val)
{
    netsnmp_assert(NULL != mib_CPURTUsage_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:CPURTUsage_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement CPURTUsage mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_CPURTUsage_val_ptr) = raw_CPURTUsage_val;

    return MFD_SUCCESS;
} /* CPURTUsage_map */

/**
 * Extract the current value of the CPURTUsage data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param CPURTUsage_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
CPURTUsage_get( dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, long * CPURTUsage_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != CPURTUsage_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:CPURTUsage_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the CPURTUsage data.
 * set (* CPURTUsage_val_ptr ) from rowreq_ctx->data
 */
    (* CPURTUsage_val_ptr ) = rowreq_ctx->data.CPURTUsage;

    return MFD_SUCCESS;
} /* CPURTUsage_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpRealTimeCollectEntry.MemRTUsage
 * MemRTUsage is subid 3 of dot11WtpRealTimeCollectEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.6.1.3
 * Description:
AP MemRTUage.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-MemRTUsage.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
MemRTUsage_map(long *mib_MemRTUsage_val_ptr, long raw_MemRTUsage_val)
{
    netsnmp_assert(NULL != mib_MemRTUsage_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:MemRTUsage_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement MemRTUsage mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_MemRTUsage_val_ptr) = raw_MemRTUsage_val;

    return MFD_SUCCESS;
} /* MemRTUsage_map */

/**
 * Extract the current value of the MemRTUsage data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param MemRTUsage_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
MemRTUsage_get( dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, long * MemRTUsage_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != MemRTUsage_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:MemRTUsage_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the MemRTUsage data.
 * set (* MemRTUsage_val_ptr ) from rowreq_ctx->data
 */
    (* MemRTUsage_val_ptr ) = rowreq_ctx->data.MemRTUsage;

    return MFD_SUCCESS;
} /* MemRTUsage_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpRealTimeCollectEntry.wtpAssocTimes
 * wtpAssocTimes is subid 4 of dot11WtpRealTimeCollectEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.6.1.4
 * Description:
AP Assoc Times .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpAssocTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpAssocTimes_map(u_long *mib_wtpAssocTimes_val_ptr, u_long raw_wtpAssocTimes_val)
{
    netsnmp_assert(NULL != mib_wtpAssocTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpAssocTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpAssocTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpAssocTimes_val_ptr) = raw_wtpAssocTimes_val;

    return MFD_SUCCESS;
} /* wtpAssocTimes_map */

/**
 * Extract the current value of the wtpAssocTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAssocTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpAssocTimes_get( dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, u_long * wtpAssocTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAssocTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpAssocTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpAssocTimes data.
 * set (* wtpAssocTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAssocTimes_val_ptr ) = rowreq_ctx->data.wtpAssocTimes;

    return MFD_SUCCESS;
} /* wtpAssocTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpRealTimeCollectEntry.wtpAssocFailtimes
 * wtpAssocFailtimes is subid 5 of dot11WtpRealTimeCollectEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.6.1.5
 * Description:
AP Assoc Fail times .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpAssocFailtimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpAssocFailtimes_map(u_long *mib_wtpAssocFailtimes_val_ptr, u_long raw_wtpAssocFailtimes_val)
{
    netsnmp_assert(NULL != mib_wtpAssocFailtimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpAssocFailtimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpAssocFailtimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpAssocFailtimes_val_ptr) = raw_wtpAssocFailtimes_val;

    return MFD_SUCCESS;
} /* wtpAssocFailtimes_map */

/**
 * Extract the current value of the wtpAssocFailtimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAssocFailtimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpAssocFailtimes_get( dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, u_long * wtpAssocFailtimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAssocFailtimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpAssocFailtimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpAssocFailtimes data.
 * set (* wtpAssocFailtimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAssocFailtimes_val_ptr ) = rowreq_ctx->data.wtpAssocFailtimes;

    return MFD_SUCCESS;
} /* wtpAssocFailtimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpRealTimeCollectEntry.wtpReassocTimes
 * wtpReassocTimes is subid 6 of dot11WtpRealTimeCollectEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.6.1.6
 * Description:
AP Reassoc Times.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReassocTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReassocTimes_map(u_long *mib_wtpReassocTimes_val_ptr, u_long raw_wtpReassocTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReassocTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpReassocTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReassocTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReassocTimes_val_ptr) = raw_wtpReassocTimes_val;

    return MFD_SUCCESS;
} /* wtpReassocTimes_map */

/**
 * Extract the current value of the wtpReassocTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReassocTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReassocTimes_get( dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, u_long * wtpReassocTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReassocTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpReassocTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReassocTimes data.
 * set (* wtpReassocTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReassocTimes_val_ptr ) = rowreq_ctx->data.wtpReassocTimes;

    return MFD_SUCCESS;
} /* wtpReassocTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpRealTimeCollectEntry.wtpIfIndiscardPkts
 * wtpIfIndiscardPkts is subid 7 of dot11WtpRealTimeCollectEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.6.1.7
 * Description:
AP Receive Drop .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIfIndiscardPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIfIndiscardPkts_map(u_long *mib_wtpIfIndiscardPkts_val_ptr, u_long raw_wtpIfIndiscardPkts_val)
{
    netsnmp_assert(NULL != mib_wtpIfIndiscardPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpIfIndiscardPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpIfIndiscardPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpIfIndiscardPkts_val_ptr) = raw_wtpIfIndiscardPkts_val;

    return MFD_SUCCESS;
} /* wtpIfIndiscardPkts_map */

/**
 * Extract the current value of the wtpIfIndiscardPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIfIndiscardPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpIfIndiscardPkts_get( dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, u_long * wtpIfIndiscardPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIfIndiscardPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpIfIndiscardPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIfIndiscardPkts data.
 * set (* wtpIfIndiscardPkts_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIfIndiscardPkts_val_ptr ) = rowreq_ctx->data.wtpIfIndiscardPkts;

    return MFD_SUCCESS;
} /* wtpIfIndiscardPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpRealTimeCollectEntry.wtpStaTxBytes
 * wtpStaTxBytes is subid 8 of dot11WtpRealTimeCollectEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.6.1.8
 * Description:
AP Sta Send Bytes.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaTxBytes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTxBytes_map(unsigned long long *mib_wtpTxBytes_val_ptr, unsigned long long raw_wtpTxBytes_val)
{
    netsnmp_assert(NULL != mib_wtpTxBytes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpTxBytes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpTxBytes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpTxBytes_val_ptr) = raw_wtpTxBytes_val;

    return MFD_SUCCESS;
} /* wtpTxBytes_map */

/**
 * Extract the current value of the wtpTxBytes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTxBytes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpTxBytes_get( dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, unsigned long long * wtpTxBytes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpTxBytes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpTxBytes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTxBytes data.
 * set (* wtpTxBytes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpTxBytes_val_ptr ) = rowreq_ctx->data.wtpTxBytes;

    return MFD_SUCCESS;
} /* wtpTxBytes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpRealTimeCollectEntry.wtpStaRxBytes
 * wtpStaRxBytes is subid 9 of dot11WtpRealTimeCollectEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.1.1.6.1.9
 * Description:
AP Sta receive Bytes.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpStaRxBytes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpRxBytes_map(unsigned long long *mib_wtpRxBytes_val_ptr, unsigned long long raw_wtpRxBytes_val)
{
    netsnmp_assert(NULL != mib_wtpRxBytes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpRxBytes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpRxBytes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpRxBytes_val_ptr) = raw_wtpRxBytes_val;

    return MFD_SUCCESS;
} /* wtpRxBytes_map */

/**
 * Extract the current value of the wtpRxBytes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRxBytes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpRxBytes_get( dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, unsigned long long * wtpRxBytes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpRxBytes_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpRxBytes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpRxBytes data.
 * set (* wtpRxBytes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpRxBytes_val_ptr ) = rowreq_ctx->data.wtpRxBytes;

    return MFD_SUCCESS;
} /* wtpRxBytes_get */



/*
	lixiang add at 2011-1-3
*/
int
wtpReassocFailure_map(u_long *mib_wtpReassocFailure_val_ptr, u_long raw_wtpReassocFailure_val)
{
    netsnmp_assert(NULL != raw_wtpReassocFailure_val);
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpReassocFailure_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReassocFailure mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReassocFailure_val_ptr) = raw_wtpReassocFailure_val;

    return MFD_SUCCESS;
} /* wtpReassocFailure_map */

/**
 * Extract the current value of the wtpReassocFailure data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReassocFailure_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReassocFailure_get( dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, u_long * wtpReassocFailure_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReassocFailure_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpReassocFailure_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReassocFailure data.
 * set (* wtpReassocFailure_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReassocFailure_val_ptr ) = rowreq_ctx->data.wtpReassocFailure;

    return MFD_SUCCESS;
} /* wtpReassocFailure_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpRealTimeCollectEntry.wtpSuccAssociatedNum
 * wtpSuccAssociatedNum is subid 11 of dot11WtpRealTimeCollectEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.6.1.11
 * Description:
AP Reassoc Failure times.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSuccAssociatedNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSuccAssociatedNum_map(u_long *mib_wtpSuccAssociatedNum_val_ptr, u_long raw_wtpSuccAssociatedNum_val)
{
    netsnmp_assert(NULL != mib_wtpSuccAssociatedNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpSuccAssociatedNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSuccAssociatedNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSuccAssociatedNum_val_ptr) = raw_wtpSuccAssociatedNum_val;

    return MFD_SUCCESS;
} /* wtpSuccAssociatedNum_map */

/**
 * Extract the current value of the wtpSuccAssociatedNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSuccAssociatedNum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSuccAssociatedNum_get( dot11WtpRealTimeCollectTable_rowreq_ctx *rowreq_ctx, u_long * wtpSuccAssociatedNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSuccAssociatedNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpRealTimeCollectTable:wtpSuccAssociatedNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSuccAssociatedNum data.
 * set (* wtpSuccAssociatedNum_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSuccAssociatedNum_val_ptr ) = rowreq_ctx->data.wtpSuccAssociatedNum;

    return MFD_SUCCESS;
} /* wtpSuccAssociatedNum_get */



/** @} */
