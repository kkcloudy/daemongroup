/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 14170 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "mibs_public.h"
#include "autelanWtpGroup.h"
#include "ws_user_manage.h"
#include "ws_conf_engine.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "nm/app/eag/eag_conf.h"
#include "nm/public/nm_list.h"
#include "nm/app/eag/eag_interface.h"
#include "ws_dcli_wlans.h"
#include "ws_dcli_ac.h"
#include "dot11SsidTeminalTable.h"


#include "dot11SsidTeminalTable_data_access.h"

/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11SsidTeminalTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-WTP-MIB::dot11SsidTeminalTable is subid 6 of wlanPortal.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.6, length: 12
*/

/**
 * initialization for dot11SsidTeminalTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11SsidTeminalTable_reg
 *        Pointer to dot11SsidTeminalTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11SsidTeminalTable_init_data(dot11SsidTeminalTable_registration * dot11SsidTeminalTable_reg)
{
    DEBUGMSGTL(("verbose:dot11SsidTeminalTable:dot11SsidTeminalTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11SsidTeminalTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11SsidTeminalTable_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11SsidTeminalTable_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11SsidTeminalTable:dot11SsidTeminalTable_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to dot11SsidTeminalTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to dot11SsidTeminalTable_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up dot11SsidTeminalTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11SSIDTEMINALTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11SsidTeminalTable_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before dot11SsidTeminalTable_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
dot11SsidTeminalTable_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:dot11SsidTeminalTable:dot11SsidTeminalTable_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to dot11SsidTeminalTable_container_shutdown\n");
        return;
    }

} /* dot11SsidTeminalTable_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement dot11SsidTeminalTable data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11SsidTeminalTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11SsidTeminalTable_container_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11SsidTeminalTable_container_load\n");
	
    dot11SsidTeminalTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   wlanCurrID;

    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];

    DEBUGMSGTL(("verbose:dot11SsidTeminalTable:dot11SsidTeminalTable_container_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
        //return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11SsidTeminalTable container.
     * loop over your dot11SsidTeminalTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
	struct dot11SsidTeminalTable_info
	{
		long wlan_id;
		unsigned long pSsidUserRedirReqNum;
		unsigned long pSsidUserRedirReqSuccNum;
		unsigned long pSsidRadiusAuthReqNum;
		unsigned long pSsidRadiusAuthReqSuccNum;
		unsigned long pSsidChallengeReqNum;
		unsigned long pSsidChallengeReqSuccNum;
		unsigned long pSsidReqnum;
		unsigned long pSsidReqsuccnum;
		struct dot11SsidTeminalTable_info *next;
	};

	instance_parameter *paraHead = NULL;
	instance_parameter *pq = NULL;
	int i = 0;
	struct dot11SsidTeminalTable_info *Count_Info = NULL;
	struct dot11SsidTeminalTable_info *p = NULL,*tail = NULL;
	struct dot11SsidTeminalTable_info *q = NULL;
	int exist_flag = 0;
	struct list_head bss_stat;
	struct eag_bss_stat *tmp = NULL;
	
	snmp_log(LOG_DEBUG, "enter list_instance_parameter\n");
	list_instance_parameter(&paraHead, SNMPD_INSTANCE_MASTER); 
	snmp_log(LOG_DEBUG, "exit list_instance_parameter,paraHead=%p\n", paraHead);
	for(pq = paraHead; (NULL != pq); pq = pq->next)
	{
		eag_get_bss_statistics(pq->connection,pq->parameter.local_id,pq->parameter.instance_id,&bss_stat);
		
		list_for_each_entry(tmp, &bss_stat, node) {
			if(0 == i) {					
				Count_Info = (struct dot11SsidTeminalTable_info *)malloc(sizeof(struct dot11SsidTeminalTable_info));
				if(Count_Info) {
					memset(Count_Info, 0, sizeof(struct dot11SsidTeminalTable_info));
					Count_Info->wlan_id = local_to_global_ID(pq->parameter, tmp->wlanid, WIRELESS_MAX_NUM);
					Count_Info->pSsidUserRedirReqNum = tmp->http_redir_request_count;
					Count_Info->pSsidUserRedirReqSuccNum = tmp->http_redir_success_count;
					Count_Info->pSsidRadiusAuthReqNum = tmp->access_request_count;
					Count_Info->pSsidRadiusAuthReqSuccNum = tmp->access_accept_count;
					Count_Info->pSsidChallengeReqNum = tmp->challenge_req_count;
					Count_Info->pSsidChallengeReqSuccNum = tmp->challenge_ack_0_count;
					Count_Info->pSsidReqnum = tmp->auth_req_count;
					Count_Info->pSsidReqsuccnum = tmp->auth_ack_0_count;
					tail = Count_Info;
					i++;
					continue;
				}					
			}
			exist_flag = 0;
			for(p = Count_Info; p; p = p->next) {
				if (p->wlan_id == tmp->wlanid) {
					p->pSsidUserRedirReqNum += tmp->http_redir_request_count;
					p->pSsidUserRedirReqSuccNum += tmp->http_redir_success_count;
					p->pSsidRadiusAuthReqNum += tmp->access_request_count;
					p->pSsidRadiusAuthReqSuccNum += tmp->access_accept_count;
					p->pSsidChallengeReqNum += tmp->challenge_req_count;
					p->pSsidChallengeReqSuccNum += tmp->challenge_ack_0_count;
					p->pSsidReqnum += tmp->auth_req_count;
					p->pSsidReqsuccnum += tmp->auth_ack_0_count;
					exist_flag = 1;
					continue;
				}
			}
			if (0 == exist_flag) {
				q = (struct dot11SsidTeminalTable_info *)malloc(sizeof(struct dot11SsidTeminalTable_info));
				if(q) {
					memset(q, 0, sizeof(struct dot11SsidTeminalTable_info));
					q->wlan_id = local_to_global_ID(pq->parameter, tmp->wlanid, WIRELESS_MAX_NUM);
					q->pSsidUserRedirReqNum = tmp->http_redir_request_count;
					q->pSsidUserRedirReqSuccNum = tmp->http_redir_success_count;
					q->pSsidRadiusAuthReqNum = tmp->access_request_count;
					q->pSsidRadiusAuthReqSuccNum = tmp->access_accept_count;
					q->pSsidChallengeReqNum = tmp->challenge_req_count;
					q->pSsidChallengeReqSuccNum = tmp->challenge_ack_0_count;
					q->pSsidReqnum = tmp->auth_req_count;
					q->pSsidReqsuccnum = tmp->auth_ack_0_count;
					if(tail)
					{
						tail->next = q;
						tail = q;
					}
				}
			}
		}	
		eag_free_bss_statistics(&bss_stat);
	}
	free_instance_parameter_list(&paraHead);
	
	for(q = Count_Info; q; q = q->next)
	{
		wlanCurrID = q->wlan_id;
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * get a line (skip blank lines)
     */
    //do {
        //if (!fgets(line, sizeof(line), filep)) {
            /* we're done */
            //fclose(filep);
            //filep = NULL;
        //}
    //} while (filep && (line[0] == '\n'));

    /*
     * check for end of data
     */
    //if(NULL == filep)
        //break;

    /*
     * parse line into variables
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

        /*
         * TODO:352:M: |   |-> set indexes in new dot11SsidTeminalTable rowreq context.
         * data context will be set from the param (unless NULL,
         *      in which case a new data context will be allocated)
         */
        rowreq_ctx = dot11SsidTeminalTable_allocate_rowreq_ctx(NULL);
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
			if(Count_Info)
			{
				p = Count_Info;
				q = p->next;
				while(p)
				{
					FREE_OBJECT(p);
					p = q;
					if(q)
					{
						q = q->next;
					}
				}
			}
            return MFD_RESOURCE_UNAVAILABLE;
        }
        if(MFD_SUCCESS != dot11SsidTeminalTable_indexes_set(rowreq_ctx
                               , wlanCurrID
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "dot11SsidTeminalTable data.\n");
            dot11SsidTeminalTable_release_rowreq_ctx(rowreq_ctx);
            continue;
        }

        /*
         * TODO:352:r: |   |-> populate dot11SsidTeminalTable data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for pSsidUserRedirReqNum
     * pSsidUserRedirReqNum(1)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.pSsidUserRedirReqNum = q->pSsidUserRedirReqNum;
    /*
     * setup/save data for pSsidUserRedirReqSuccNum
     * pSsidUserRedirReqSuccNum(2)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.pSsidUserRedirReqSuccNum = q->pSsidUserRedirReqSuccNum;
    
    /*
     * setup/save data for pSsidUserRedirReqSuccRate
     * pSsidUserRedirReqSuccRate(3)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /** no mapping */
	if(0 == q->pSsidUserRedirReqNum)
		rowreq_ctx->data.pSsidUserRedirReqSuccRate = 0;
	else						
	    rowreq_ctx->data.pSsidUserRedirReqSuccRate = (q->pSsidUserRedirReqSuccNum * 100) / q->pSsidUserRedirReqNum;
    /*
     * setup/save data for pSsidRadiusAuthReqNum
     * pSsidRadiusAuthReqNum(4)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.pSsidRadiusAuthReqNum = q->pSsidRadiusAuthReqNum;
    
    /*
     * setup/save data for pSsidRadiusAuthReqSuccNum
     * pSsidRadiusAuthReqSuccNum(5)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.pSsidRadiusAuthReqSuccNum = q->pSsidRadiusAuthReqSuccNum;
    
    /*
     * setup/save data for pSsidRadiusAuthReqSuccRate
     * pSsidRadiusAuthReqSuccRate(6)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /** no mapping */
	if(0 == q->pSsidRadiusAuthReqNum)
		rowreq_ctx->data.pSsidRadiusAuthReqSuccRate = 0;
	else						
	    rowreq_ctx->data.pSsidRadiusAuthReqSuccRate = (q->pSsidRadiusAuthReqSuccNum * 100) / q->pSsidRadiusAuthReqNum;
    
    /*
     * setup/save data for pSsidChallengeReqNum
     * pSsidChallengeReqNum(7)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.pSsidChallengeReqNum = q->pSsidChallengeReqNum;
    
    /*
     * setup/save data for pSsidChallengeReqSuccNum
     * pSsidChallengeReqSuccNum(8)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.pSsidChallengeReqSuccNum = q->pSsidChallengeReqSuccNum;
    
    /*
     * setup/save data for pSsidChallengeReqSuccRate
     * pSsidChallengeReqSuccRate(9)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /** no mapping */
	if(0 == q->pSsidChallengeReqNum)
		rowreq_ctx->data.pSsidChallengeReqSuccRate = 0;
	else						
	    rowreq_ctx->data.pSsidChallengeReqSuccRate = (q->pSsidChallengeReqSuccNum * 100) / q->pSsidChallengeReqNum;
    
    /*
     * setup/save data for pSsidReqnum
     * pSsidReqnum(10)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.pSsidReqnum = q->pSsidReqnum;
    
    /*
     * setup/save data for pSsidReqsuccnum
     * pSsidReqsuccnum(11)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.pSsidReqsuccnum = q->pSsidReqsuccnum;
    
    /*
     * setup/save data for pSsidReqSuccRate
     * pSsidReqSuccRate(12)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /** no mapping */
	if(0 == q->pSsidReqnum)
		rowreq_ctx->data.pSsidReqSuccRate = 0;
	else						
	    rowreq_ctx->data.pSsidReqSuccRate = (q->pSsidReqsuccnum * 100) / q->pSsidReqnum;
        
        /*
         * insert into table container
         */
        if(CONTAINER_INSERT(container, rowreq_ctx))
        {
			dot11SsidTeminalTable_release_rowreq_ctx(rowreq_ctx);
        }
        ++count;
    }
	if(Count_Info)
	{
		p = Count_Info;
		q = p->next;
		while(p)
		{
			FREE_OBJECT(p);
			p = q;
			if(q)
			{
				q = q->next;
			}
		}
	}

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
        //fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11SsidTeminalTable:dot11SsidTeminalTable_container_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11SsidTeminalTable_container_load\n");
    return MFD_SUCCESS;
} /* dot11SsidTeminalTable_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11SsidTeminalTable_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11SsidTeminalTable:dot11SsidTeminalTable_container_free","called\n"));

    /*
     * TODO:380:M: Free dot11SsidTeminalTable container data.
     */
} /* dot11SsidTeminalTable_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11SsidTeminalTable_row_prep( dot11SsidTeminalTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SsidTeminalTable:dot11SsidTeminalTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11SsidTeminalTable_row_prep */

/** @} */
