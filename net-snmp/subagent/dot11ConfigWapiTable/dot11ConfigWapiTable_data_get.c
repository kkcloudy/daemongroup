/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigWapiTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11ConfigWapiTable get routines.
 * TODO:240:M: Implement dot11ConfigWapiTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigWapiTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigWapiTable is subid 5 of dot11securityConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.5, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11ConfigWapiTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWapiEntry.WapiSSID
 * WapiSSID is subid 1 of dot11ConfigWapiEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.5.1.1
 * Description:
WAPIASIPAddress	.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-WapiSSID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
WapiSSID_map_wapi(long *mib_WapiSSID_val_ptr, long raw_WapiSSID_val)
{
    netsnmp_assert(NULL != mib_WapiSSID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigWapiTable:WapiSSID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement WapiSSID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_WapiSSID_val_ptr) = raw_WapiSSID_val;

    return MFD_SUCCESS;
} /* WapiSSID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11ConfigWapiTable_indexes_set_tbl_idx(dot11ConfigWapiTable_mib_index *tbl_idx, long WapiSSID_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWapiTable:dot11ConfigWapiTable_indexes_set_tbl_idx","called\n"));

    /* WapiSSID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->WapiSSID = WapiSSID_val;
    

    return MFD_SUCCESS;
} /* dot11ConfigWapiTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11ConfigWapiTable_indexes_set(dot11ConfigWapiTable_rowreq_ctx *rowreq_ctx, long WapiSSID_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWapiTable:dot11ConfigWapiTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11ConfigWapiTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , WapiSSID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11ConfigWapiTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11ConfigWapiTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWapiEntry.WAPIASIPAddress
 * WAPIASIPAddress is subid 2 of dot11ConfigWapiEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.5.1.2
 * Description:
WAPIASIPAddress	.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-WAPIASIPAddress.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
WAPIASIPAddress_map(u_long *mib_WAPIASIPAddress_val_ptr, u_long raw_WAPIASIPAddress_val)
{
    netsnmp_assert(NULL != mib_WAPIASIPAddress_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigWapiTable:WAPIASIPAddress_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement WAPIASIPAddress mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_WAPIASIPAddress_val_ptr) = raw_WAPIASIPAddress_val;

    return MFD_SUCCESS;
} /* WAPIASIPAddress_map */

/**
 * Extract the current value of the WAPIASIPAddress data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param WAPIASIPAddress_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
WAPIASIPAddress_get( dot11ConfigWapiTable_rowreq_ctx *rowreq_ctx, u_long * WAPIASIPAddress_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != WAPIASIPAddress_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigWapiTable:WAPIASIPAddress_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the WAPIASIPAddress data.
 * set (* WAPIASIPAddress_val_ptr ) from rowreq_ctx->data
 */
    (* WAPIASIPAddress_val_ptr ) = rowreq_ctx->data.WAPIASIPAddress;

    return MFD_SUCCESS;
} /* WAPIASIPAddress_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWapiEntry.WapiIsInstalledCer
 * WapiIsInstalledCer is subid 3 of dot11ConfigWapiEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.2.14.5.1.3
 * Description:
Whether installed WAPI certificate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-WapiIsInstalledCer.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
WapiIsInstalledCer_map(u_long *mib_WapiIsInstalledCer_val_ptr, u_long raw_WapiIsInstalledCer_val)
{
    netsnmp_assert(NULL != mib_WapiIsInstalledCer_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigWapiTable:WapiIsInstalledCer_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement WapiIsInstalledCer enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_WapiIsInstalledCer_val) {
        case INTERNAL_WAPIISINSTALLEDCER_TRUE:
             *mib_WapiIsInstalledCer_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_WAPIISINSTALLEDCER_FALSE:
             *mib_WapiIsInstalledCer_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for WapiIsInstalledCer\n", raw_WapiIsInstalledCer_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* WapiIsInstalledCer_map */

/**
 * Extract the current value of the WapiIsInstalledCer data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param WapiIsInstalledCer_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
WapiIsInstalledCer_get( dot11ConfigWapiTable_rowreq_ctx *rowreq_ctx, u_long * WapiIsInstalledCer_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != WapiIsInstalledCer_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigWapiTable:WapiIsInstalledCer_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the WapiIsInstalledCer data.
 * set (* WapiIsInstalledCer_val_ptr ) from rowreq_ctx->data
 */
    (* WapiIsInstalledCer_val_ptr ) = rowreq_ctx->data.WapiIsInstalledCer;

    return MFD_SUCCESS;
} /* WapiIsInstalledCer_get */



/** @} */
