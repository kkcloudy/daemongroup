/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11SSIDExternStatsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11SSIDExternStatsTable get routines.
 * TODO:240:M: Implement dot11SSIDExternStatsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11SSIDExternStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11SSIDExternStatsTable is subid 7 of wtpWlan.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.7, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11SSIDExternStatsTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_ssidexternstats_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wlanCurrID
 * wlanCurrID is subid 1 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.3.1.1
 * Description:
The wlan's ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wlanCurrID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wlanCurrID_ssidexternstats_map(long *mib_wlanCurrID_val_ptr, long raw_wlanCurrID_val)
{
    netsnmp_assert(NULL != mib_wlanCurrID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:wlanCurrID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wlanCurrID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wlanCurrID_val_ptr) = raw_wlanCurrID_val;

    return MFD_SUCCESS;
} /* wlanCurrID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11SSIDExternStatsTable_indexes_set_tbl_idx(dot11SSIDExternStatsTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:dot11SSIDExternStatsTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wlanCurrID = wlanCurrID_val;
    

    return MFD_SUCCESS;
} /* dot11SSIDExternStatsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11SSIDExternStatsTable_indexes_set(dot11SSIDExternStatsTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:dot11SSIDExternStatsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11SSIDExternStatsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wlanCurrID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11SSIDExternStatsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11SSIDExternStatsTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDExternStatsEntry.SSIDDwErrPkts
 * SSIDDwErrPkts is subid 1 of dot11SSIDExternStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.7.1.1
 * Description:
The total number of down error packet.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDDwErrPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDDwErrPkts_map(u_long *mib_SSIDDwErrPkts_val_ptr, u_long raw_SSIDDwErrPkts_val)
{
    netsnmp_assert(NULL != mib_SSIDDwErrPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDDwErrPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDDwErrPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDDwErrPkts_val_ptr) = raw_SSIDDwErrPkts_val;

    return MFD_SUCCESS;
} /* SSIDDwErrPkts_map */

/**
 * Extract the current value of the SSIDDwErrPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDDwErrPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDDwErrPkts_get( dot11SSIDExternStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDDwErrPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDDwErrPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDDwErrPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDDwErrPkts data.
 * set (* SSIDDwErrPkts_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDDwErrPkts_val_ptr ) = rowreq_ctx->data.SSIDDwErrPkts;

    return MFD_SUCCESS;
} /* SSIDDwErrPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDExternStatsEntry.SSIDDwDropPkts
 * SSIDDwDropPkts is subid 2 of dot11SSIDExternStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.7.1.2
 * Description:
The total number of down drop  packet.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDDwDropPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDDwDropPkts_map(u_long *mib_SSIDDwDropPkts_val_ptr, u_long raw_SSIDDwDropPkts_val)
{
    netsnmp_assert(NULL != mib_SSIDDwDropPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDDwDropPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDDwDropPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDDwDropPkts_val_ptr) = raw_SSIDDwDropPkts_val;

    return MFD_SUCCESS;
} /* SSIDDwDropPkts_map */

/**
 * Extract the current value of the SSIDDwDropPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDDwDropPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDDwDropPkts_get( dot11SSIDExternStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDDwDropPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDDwDropPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDDwDropPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDDwDropPkts data.
 * set (* SSIDDwDropPkts_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDDwDropPkts_val_ptr ) = rowreq_ctx->data.SSIDDwDropPkts;

    return MFD_SUCCESS;
} /* SSIDDwDropPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDExternStatsEntry.SSIDDwTotErrFrames
 * SSIDDwTotErrFrames is subid 3 of dot11SSIDExternStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.7.1.3
 * Description:
The total number of down err frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDDwTotErrFrames.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDDwTotErrFrames_map(u_long *mib_SSIDDwTotErrFrames_val_ptr, u_long raw_SSIDDwTotErrFrames_val)
{
    netsnmp_assert(NULL != mib_SSIDDwTotErrFrames_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDDwTotErrFrames_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDDwTotErrFrames mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDDwTotErrFrames_val_ptr) = raw_SSIDDwTotErrFrames_val;

    return MFD_SUCCESS;
} /* SSIDDwTotErrFrames_map */

/**
 * Extract the current value of the SSIDDwTotErrFrames data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDDwTotErrFrames_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDDwTotErrFrames_get( dot11SSIDExternStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDDwTotErrFrames_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDDwTotErrFrames_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDDwTotErrFrames_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDDwTotErrFrames data.
 * set (* SSIDDwTotErrFrames_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDDwTotErrFrames_val_ptr ) = rowreq_ctx->data.SSIDDwTotErrFrames;

    return MFD_SUCCESS;
} /* SSIDDwTotErrFrames_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDExternStatsEntry.SSIDUpErrPkts
 * SSIDUpErrPkts is subid 4 of dot11SSIDExternStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.7.1.4
 * Description:
The total number of up error packet.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDUpErrPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDUpErrPkts_map(u_long *mib_SSIDUpErrPkts_val_ptr, u_long raw_SSIDUpErrPkts_val)
{
    netsnmp_assert(NULL != mib_SSIDUpErrPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDUpErrPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDUpErrPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDUpErrPkts_val_ptr) = raw_SSIDUpErrPkts_val;

    return MFD_SUCCESS;
} /* SSIDUpErrPkts_map */

/**
 * Extract the current value of the SSIDUpErrPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDUpErrPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDUpErrPkts_get( dot11SSIDExternStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDUpErrPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDUpErrPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDUpErrPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDUpErrPkts data.
 * set (* SSIDUpErrPkts_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDUpErrPkts_val_ptr ) = rowreq_ctx->data.SSIDUpErrPkts;

    return MFD_SUCCESS;
} /* SSIDUpErrPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDExternStatsEntry.SSIDUpDropPkts
 * SSIDUpDropPkts is subid 5 of dot11SSIDExternStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.7.1.5
 * Description:
The total number of up drop packet.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDUpDropPkts.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDUpDropPkts_map(u_long *mib_SSIDUpDropPkts_val_ptr, u_long raw_SSIDUpDropPkts_val)
{
    netsnmp_assert(NULL != mib_SSIDUpDropPkts_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDUpDropPkts_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDUpDropPkts mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDUpDropPkts_val_ptr) = raw_SSIDUpDropPkts_val;

    return MFD_SUCCESS;
} /* SSIDUpDropPkts_map */

/**
 * Extract the current value of the SSIDUpDropPkts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDUpDropPkts_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDUpDropPkts_get( dot11SSIDExternStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDUpDropPkts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDUpDropPkts_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDUpDropPkts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDUpDropPkts data.
 * set (* SSIDUpDropPkts_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDUpDropPkts_val_ptr ) = rowreq_ctx->data.SSIDUpDropPkts;

    return MFD_SUCCESS;
} /* SSIDUpDropPkts_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11SSIDExternStatsEntry.SSIDUpTotErrFrames
 * SSIDUpTotErrFrames is subid 6 of dot11SSIDExternStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.7.1.6
 * Description:
The total number of up error frames.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-SSIDUpTotErrFrames.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
SSIDUpTotErrFrames_map(u_long *mib_SSIDUpTotErrFrames_val_ptr, u_long raw_SSIDUpTotErrFrames_val)
{
    netsnmp_assert(NULL != mib_SSIDUpTotErrFrames_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDUpTotErrFrames_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement SSIDUpTotErrFrames mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_SSIDUpTotErrFrames_val_ptr) = raw_SSIDUpTotErrFrames_val;

    return MFD_SUCCESS;
} /* SSIDUpTotErrFrames_map */

/**
 * Extract the current value of the SSIDUpTotErrFrames data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SSIDUpTotErrFrames_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
SSIDUpTotErrFrames_get( dot11SSIDExternStatsTable_rowreq_ctx *rowreq_ctx, u_long * SSIDUpTotErrFrames_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != SSIDUpTotErrFrames_val_ptr );


    DEBUGMSGTL(("verbose:dot11SSIDExternStatsTable:SSIDUpTotErrFrames_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the SSIDUpTotErrFrames data.
 * set (* SSIDUpTotErrFrames_val_ptr ) from rowreq_ctx->data
 */
    (* SSIDUpTotErrFrames_val_ptr ) = rowreq_ctx->data.SSIDUpTotErrFrames;

    return MFD_SUCCESS;
} /* SSIDUpTotErrFrames_get */



/** @} */
