/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpParaTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpParaTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpParaTable is subid 5 of wtpStats.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.5, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11WtpParaTable_undo_setup( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpParaTable:dot11WtpParaTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11WtpParaTable undo.
     * set up dot11WtpParaTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11WtpParaTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpParaTable_undo_cleanup( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpParaTable:dot11WtpParaTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11WtpParaTable undo.
     */

    return rc;
} /* dot11WtpParaTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpParaTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpParaTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpParaTable_commit( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11WtpParaTable:dot11WtpParaTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11WtpParaTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WTPNETELEMENTCODE) {
       save_flags &= ~FLAG_WTPNETELEMENTCODE; /* clear wtpNetElementCode */
       /*
        * TODO:482:o: |-> commit column wtpNetElementCode.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpParaTable column wtpNetElementCode commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpNetElementCode
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPNETELEMENTCODE;
       }
    }

    if (save_flags & FLAG_WTPREMOTERESTARTFUN) {
       save_flags &= ~FLAG_WTPREMOTERESTARTFUN; /* clear wtpRemoteRestartFun */
       /*
        * TODO:482:o: |-> commit column wtpRemoteRestartFun.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpParaTable column wtpRemoteRestartFun commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpRemoteRestartFun
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPREMOTERESTARTFUN;
       }
    }

    if (save_flags & FLAG_WTPDEFENCEDOSATTACK) {
       save_flags &= ~FLAG_WTPDEFENCEDOSATTACK; /* clear wtpDefenceDOSAttack */
       /*
        * TODO:482:o: |-> commit column wtpDefenceDOSAttack.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpParaTable column wtpDefenceDOSAttack commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpDefenceDOSAttack
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPDEFENCEDOSATTACK;
       }
    }

    if (save_flags & FLAG_WTPIGMPSWITCH) {
       save_flags &= ~FLAG_WTPIGMPSWITCH; /* clear wtpIGMPSwitch */
       /*
        * TODO:482:o: |-> commit column wtpIGMPSwitch.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpParaTable column wtpIGMPSwitch commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpIGMPSwitch
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPIGMPSWITCH;
       }
    }

    if (save_flags & FLAG_WTPBRIDGINGWORKMODE) {
       save_flags &= ~FLAG_WTPBRIDGINGWORKMODE; /* clear wtpBridgingWorkMode */
       /*
        * TODO:482:o: |-> commit column wtpBridgingWorkMode.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpParaTable column wtpBridgingWorkMode commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpBridgingWorkMode
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPBRIDGINGWORKMODE;
       }
    }

    if (save_flags & FLAG_WTPCURRAPMODE) {
       save_flags &= ~FLAG_WTPCURRAPMODE; /* clear wtpCurrAPMode */
       /*
        * TODO:482:o: |-> commit column wtpCurrAPMode.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpParaTable column wtpCurrAPMode commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpCurrAPMode
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPCURRAPMODE;
       }
    }

    if (save_flags & FLAG_WTPSUPPORT11B) {
       save_flags &= ~FLAG_WTPSUPPORT11B; /* clear wtpSupport11B */
       /*
        * TODO:482:o: |-> commit column wtpSupport11B.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpParaTable column wtpSupport11B commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpSupport11B
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPSUPPORT11B;
       }
    }

    if (save_flags & FLAG_WTPSUPPORT11G) {
       save_flags &= ~FLAG_WTPSUPPORT11G; /* clear wtpSupport11G */
       /*
        * TODO:482:o: |-> commit column wtpSupport11G.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpParaTable column wtpSupport11G commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpSupport11G
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPSUPPORT11G;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11WtpParaTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpParaTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpParaTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpParaTable_undo_commit( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpParaTable:dot11WtpParaTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11WtpParaTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11WtpParaTable_undo_commit */

/*
 * TODO:420:r: Implement dot11WtpParaTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/*
 * TODO:440:M: Implement dot11WtpParaTable node value checks.
 * TODO:450:M: Implement dot11WtpParaTable undo functions.
 * TODO:460:M: Implement dot11WtpParaTable set functions.
 * TODO:480:M: Implement dot11WtpParaTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpParaEntry.wtpNetElementCode
 * wtpNetElementCode is subid 3 of dot11WtpParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.5.1.3
 * Description:
The net element of AP coding.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  1 - 32;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 32)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpNetElementCode_val_ptr
 *        A char containing the new value.
 * @param wtpNetElementCode_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpNetElementCode_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  1 - 32
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpNetElementCode_check_value( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, char *wtpNetElementCode_val_ptr,  size_t wtpNetElementCode_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpNetElementCode_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpNetElementCode_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wtpNetElementCode value.
     */

    return MFD_SUCCESS; /* wtpNetElementCode value not illegal */
} /* wtpNetElementCode_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpNetElementCode_undo_setup( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpNetElementCode_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpNetElementCode undo.
     */
    /*
     * copy wtpNetElementCode and wtpNetElementCode_len data
     * set rowreq_ctx->undo->wtpNetElementCode from rowreq_ctx->data.wtpNetElementCode
     */
    memcpy( rowreq_ctx->undo->wtpNetElementCode, rowreq_ctx->data.wtpNetElementCode,
            (rowreq_ctx->data.wtpNetElementCode_len * sizeof(rowreq_ctx->undo->wtpNetElementCode[0])));
    rowreq_ctx->undo->wtpNetElementCode_len = rowreq_ctx->data.wtpNetElementCode_len;


    return MFD_SUCCESS;
} /* wtpNetElementCode_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpNetElementCode_val_ptr
 *        A char containing the new value.
 * @param wtpNetElementCode_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpNetElementCode_val_ptr
 */
int
wtpNetElementCode_set( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, char *wtpNetElementCode_val_ptr,  size_t wtpNetElementCode_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpNetElementCode_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpNetElementCode_val_ptr);

/*
 * TODO:245:o: |-> Implement wtpNetElementCode reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpNetElementCode value.
     * set wtpNetElementCode value in rowreq_ctx->data
     */    
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = set_wtp_netid(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrID,wtpNetElementCode_val_ptr);	
	if(ret == 1)
	{
		memcpy( rowreq_ctx->data.wtpNetElementCode, wtpNetElementCode_val_ptr, wtpNetElementCode_val_ptr_len );
	    rowreq_ctx->data.wtpNetElementCode_len = wtpNetElementCode_val_ptr_len / sizeof(wtpNetElementCode_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpNetElementCode_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpNetElementCode_undo( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpNetElementCode_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpNetElementCode undo.
     */
    /*
     * copy wtpNetElementCode and wtpNetElementCode_len data
     * set rowreq_ctx->data.wtpNetElementCode from rowreq_ctx->undo->wtpNetElementCode
     */
    memcpy( rowreq_ctx->data.wtpNetElementCode, rowreq_ctx->undo->wtpNetElementCode,
            (rowreq_ctx->undo->wtpNetElementCode_len * sizeof(rowreq_ctx->data.wtpNetElementCode[0])));
    rowreq_ctx->data.wtpNetElementCode_len = rowreq_ctx->undo->wtpNetElementCode_len;

    
    return MFD_SUCCESS;
} /* wtpNetElementCode_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpParaEntry.wtpRemoteRestartFun
 * wtpRemoteRestartFun is subid 6 of dot11WtpParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.5.1.6
 * Description:
AP remote reset function.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  unused(0), used(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRemoteRestartFun_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  unused(0), used(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpRemoteRestartFun_check_value( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpRemoteRestartFun_val)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpRemoteRestartFun_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpRemoteRestartFun value.
     */

    return MFD_SUCCESS; /* wtpRemoteRestartFun value not illegal */
} /* wtpRemoteRestartFun_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpRemoteRestartFun_undo_setup( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpRemoteRestartFun_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpRemoteRestartFun undo.
     */
    /*
     * copy wtpRemoteRestartFun data
     * set rowreq_ctx->undo->wtpRemoteRestartFun from rowreq_ctx->data.wtpRemoteRestartFun
     */
    rowreq_ctx->undo->wtpRemoteRestartFun = rowreq_ctx->data.wtpRemoteRestartFun;


    return MFD_SUCCESS;
} /* wtpRemoteRestartFun_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpRemoteRestartFun_val
 *        A long containing the new value.
 */
int
wtpRemoteRestartFun_set( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpRemoteRestartFun_val )
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpRemoteRestartFun_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpRemoteRestartFun reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpRemoteRestartFun_val) {
        case WTPREMOTERESTARTFUN_UNUSED:
             wtpRemoteRestartFun_val = INTERNAL_WTPREMOTERESTARTFUN_UNUSED;
             break;

        case WTPREMOTERESTARTFUN_USED:
             wtpRemoteRestartFun_val = INTERNAL_WTPREMOTERESTARTFUN_USED;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpRemoteRestartFun\n", wtpRemoteRestartFun_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpRemoteRestartFun value.
     * set wtpRemoteRestartFun value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	int use_sta = 0;
	if(wtpRemoteRestartFun_val == INTERNAL_WTPREMOTERESTARTFUN_UNUSED)
	{
		use_sta = 1;
	}
	else if(wtpRemoteRestartFun_val == INTERNAL_WTPREMOTERESTARTFUN_USED)
	{
		use_sta = 0;
	}	

	ret = wtp_used(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrID,use_sta);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpRemoteRestartFun = wtpRemoteRestartFun_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpRemoteRestartFun_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpRemoteRestartFun_undo( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpRemoteRestartFun_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpRemoteRestartFun undo.
     */
    /*
     * copy wtpRemoteRestartFun data
     * set rowreq_ctx->data.wtpRemoteRestartFun from rowreq_ctx->undo->wtpRemoteRestartFun
     */
    rowreq_ctx->data.wtpRemoteRestartFun = rowreq_ctx->undo->wtpRemoteRestartFun;

    
    return MFD_SUCCESS;
} /* wtpRemoteRestartFun_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpParaEntry.wtpDefenceDOSAttack
 * wtpDefenceDOSAttack is subid 11 of dot11WtpParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.5.1.11
 * Description:
Defence DOS attack.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  disable(0), enable(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpDefenceDOSAttack_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  disable(0), enable(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpDefenceDOSAttack_check_value( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpDefenceDOSAttack_val)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpDefenceDOSAttack_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpDefenceDOSAttack value.
     */

    return MFD_SUCCESS; /* wtpDefenceDOSAttack value not illegal */
} /* wtpDefenceDOSAttack_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpDefenceDOSAttack_undo_setup( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpDefenceDOSAttack_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpDefenceDOSAttack undo.
     */
    /*
     * copy wtpDefenceDOSAttack data
     * set rowreq_ctx->undo->wtpDefenceDOSAttack from rowreq_ctx->data.wtpDefenceDOSAttack
     */
    rowreq_ctx->undo->wtpDefenceDOSAttack = rowreq_ctx->data.wtpDefenceDOSAttack;


    return MFD_SUCCESS;
} /* wtpDefenceDOSAttack_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpDefenceDOSAttack_val
 *        A long containing the new value.
 */
int
wtpDefenceDOSAttack_set( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpDefenceDOSAttack_val )
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpDefenceDOSAttack_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpDefenceDOSAttack reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpDefenceDOSAttack_val) {
        case WTPDEFENCEDOSATTACK_DISABLE:
             wtpDefenceDOSAttack_val = INTERNAL_WTPDEFENCEDOSATTACK_DISABLE;
             break;

        case WTPDEFENCEDOSATTACK_ENABLE:
             wtpDefenceDOSAttack_val = INTERNAL_WTPDEFENCEDOSATTACK_ENABLE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpDefenceDOSAttack\n", wtpDefenceDOSAttack_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpDefenceDOSAttack value.
     * set wtpDefenceDOSAttack value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = set_ap_dos_def_func(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrID,wtpDefenceDOSAttack_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpDefenceDOSAttack = wtpDefenceDOSAttack_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpDefenceDOSAttack_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpDefenceDOSAttack_undo( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpDefenceDOSAttack_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpDefenceDOSAttack undo.
     */
    /*
     * copy wtpDefenceDOSAttack data
     * set rowreq_ctx->data.wtpDefenceDOSAttack from rowreq_ctx->undo->wtpDefenceDOSAttack
     */
    rowreq_ctx->data.wtpDefenceDOSAttack = rowreq_ctx->undo->wtpDefenceDOSAttack;

    
    return MFD_SUCCESS;
} /* wtpDefenceDOSAttack_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpParaEntry.wtpIGMPSwitch
 * wtpIGMPSwitch is subid 12 of dot11WtpParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.5.1.12
 * Description:
IGMP switch.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  disable(0), enable(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIGMPSwitch_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  disable(0), enable(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpIGMPSwitch_check_value( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpIGMPSwitch_val)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpIGMPSwitch_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpIGMPSwitch value.
     */

    return MFD_SUCCESS; /* wtpIGMPSwitch value not illegal */
} /* wtpIGMPSwitch_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpIGMPSwitch_undo_setup( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpIGMPSwitch_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpIGMPSwitch undo.
     */
    /*
     * copy wtpIGMPSwitch data
     * set rowreq_ctx->undo->wtpIGMPSwitch from rowreq_ctx->data.wtpIGMPSwitch
     */
    rowreq_ctx->undo->wtpIGMPSwitch = rowreq_ctx->data.wtpIGMPSwitch;


    return MFD_SUCCESS;
} /* wtpIGMPSwitch_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpIGMPSwitch_val
 *        A long containing the new value.
 */
int
wtpIGMPSwitch_set( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpIGMPSwitch_val )
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpIGMPSwitch_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpIGMPSwitch reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpIGMPSwitch_val) {
        case WTPIGMPSWITCH_DISABLE:
             wtpIGMPSwitch_val = INTERNAL_WTPIGMPSWITCH_DISABLE;
             break;

        case WTPIGMPSWITCH_ENABLE:
             wtpIGMPSwitch_val = INTERNAL_WTPIGMPSWITCH_ENABLE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpIGMPSwitch\n", wtpIGMPSwitch_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpIGMPSwitch value.
     * set wtpIGMPSwitch value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = set_ap_igmp_snoop_func(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrID,wtpIGMPSwitch_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpIGMPSwitch = wtpIGMPSwitch_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpIGMPSwitch_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpIGMPSwitch_undo( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpIGMPSwitch_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpIGMPSwitch undo.
     */
    /*
     * copy wtpIGMPSwitch data
     * set rowreq_ctx->data.wtpIGMPSwitch from rowreq_ctx->undo->wtpIGMPSwitch
     */
    rowreq_ctx->data.wtpIGMPSwitch = rowreq_ctx->undo->wtpIGMPSwitch;

    
    return MFD_SUCCESS;
} /* wtpIGMPSwitch_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpParaEntry.wtpBridgingWorkMode
 * wtpBridgingWorkMode is subid 13 of dot11WtpParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.33050.6.1.1.2.5.1.13
 * Description:
AP bridging work mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  normal(1), Bridge(2), SemiBridge(3)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpBridgingWorkMode_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  normal(1), Bridge(2), SemiBridge(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpBridgingWorkMode_check_value( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpBridgingWorkMode_val)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpBridgingWorkMode_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpBridgingWorkMode value.
     */

    return MFD_SUCCESS; /* wtpBridgingWorkMode value not illegal */
} /* wtpBridgingWorkMode_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpBridgingWorkMode_undo_setup( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpBridgingWorkMode_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpBridgingWorkMode undo.
     */
    /*
     * copy wtpBridgingWorkMode data
     * set rowreq_ctx->undo->wtpBridgingWorkMode from rowreq_ctx->data.wtpBridgingWorkMode
     */
    rowreq_ctx->undo->wtpBridgingWorkMode = rowreq_ctx->data.wtpBridgingWorkMode;


    return MFD_SUCCESS;
} /* wtpBridgingWorkMode_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpBridgingWorkMode_val
 *        A long containing the new value.
 */
int
wtpBridgingWorkMode_set( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpBridgingWorkMode_val )
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpBridgingWorkMode_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpBridgingWorkMode reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpBridgingWorkMode_val) {
        case WTPBRIDGINGWORKMODE_NORMAL:
             wtpBridgingWorkMode_val = INTERNAL_WTPBRIDGINGWORKMODE_NORMAL;
             break;

        case WTPBRIDGINGWORKMODE_BRIDGE:
             wtpBridgingWorkMode_val = INTERNAL_WTPBRIDGINGWORKMODE_BRIDGE;
             break;

        case WTPBRIDGINGWORKMODE_SEMIBRIDGE:
             wtpBridgingWorkMode_val = INTERNAL_WTPBRIDGINGWORKMODE_SEMIBRIDGE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpBridgingWorkMode\n", wtpBridgingWorkMode_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpBridgingWorkMode value.
     * set wtpBridgingWorkMode value in rowreq_ctx->data
     */
    rowreq_ctx->data.wtpBridgingWorkMode = wtpBridgingWorkMode_val;

    return MFD_SUCCESS;
} /* wtpBridgingWorkMode_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpBridgingWorkMode_undo( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpBridgingWorkMode_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpBridgingWorkMode undo.
     */
    /*
     * copy wtpBridgingWorkMode data
     * set rowreq_ctx->data.wtpBridgingWorkMode from rowreq_ctx->undo->wtpBridgingWorkMode
     */
    rowreq_ctx->data.wtpBridgingWorkMode = rowreq_ctx->undo->wtpBridgingWorkMode;

    
    return MFD_SUCCESS;
} /* wtpBridgingWorkMode_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpParaEntry.wtpCurrAPMode
 * wtpCurrAPMode is subid 14 of dot11WtpParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.5.1.14
 * Description:
AP work mode includes:
							normal:AP support wlan login service
							monitor:AP monitor wireless envirment
							semimonitor:semi-monitor mode,doing net monitor while disengagement.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  normal(1), monitor(2), SemiMonitor(3)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCurrAPMode_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  normal(1), monitor(2), SemiMonitor(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpCurrAPMode_check_value( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpCurrAPMode_val)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpCurrAPMode_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpCurrAPMode value.
     */

    return MFD_SUCCESS; /* wtpCurrAPMode value not illegal */
} /* wtpCurrAPMode_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpCurrAPMode_undo_setup( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpCurrAPMode_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpCurrAPMode undo.
     */
    /*
     * copy wtpCurrAPMode data
     * set rowreq_ctx->undo->wtpCurrAPMode from rowreq_ctx->data.wtpCurrAPMode
     */
    rowreq_ctx->undo->wtpCurrAPMode = rowreq_ctx->data.wtpCurrAPMode;


    return MFD_SUCCESS;
} /* wtpCurrAPMode_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpCurrAPMode_val
 *        A long containing the new value.
 */
int
wtpCurrAPMode_set( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpCurrAPMode_val )
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpCurrAPMode_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpCurrAPMode reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpCurrAPMode_val) {
        case WTPCURRAPMODE_NORMAL:
             wtpCurrAPMode_val = INTERNAL_WTPCURRAPMODE_NORMAL;
             break;

        case WTPCURRAPMODE_MONITOR:
             wtpCurrAPMode_val = INTERNAL_WTPCURRAPMODE_MONITOR;
             break;

        case WTPCURRAPMODE_SEMIMONITOR:
             wtpCurrAPMode_val = INTERNAL_WTPCURRAPMODE_SEMIMONITOR;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpCurrAPMode\n", wtpCurrAPMode_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpCurrAPMode value.
     * set wtpCurrAPMode value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	int ret = 0;
	ret = set_wids_monitor_mode_cmd(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpCurrID, wtpCurrAPMode_val);	
	if(ret == 1) {	
	    rowreq_ctx->data.wtpCurrAPMode = wtpCurrAPMode_val;
		return MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		return MFD_ERROR;
	}
} /* wtpCurrAPMode_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpCurrAPMode_undo( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpCurrAPMode_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpCurrAPMode undo.
     */
    /*
     * copy wtpCurrAPMode data
     * set rowreq_ctx->data.wtpCurrAPMode from rowreq_ctx->undo->wtpCurrAPMode
     */
    rowreq_ctx->data.wtpCurrAPMode = rowreq_ctx->undo->wtpCurrAPMode;

    
    return MFD_SUCCESS;
} /* wtpCurrAPMode_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpParaEntry.wtpSupport11B
 * wtpSupport11B is subid 15 of dot11WtpParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.5.1.15
 * Description:
AP work mode includes:
							yes:AP support 11B
							no:AP not support 11B.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  yes(1), no(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSupport11B_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  yes(1), no(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpSupport11B_check_value( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpSupport11B_val)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpSupport11B_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpSupport11B value.
     */

    return MFD_SUCCESS; /* wtpSupport11B value not illegal */
} /* wtpSupport11B_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpSupport11B_undo_setup( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpSupport11B_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpSupport11B undo.
     */
    /*
     * copy wtpSupport11B data
     * set rowreq_ctx->undo->wtpSupport11B from rowreq_ctx->data.wtpSupport11B
     */
    rowreq_ctx->undo->wtpSupport11B = rowreq_ctx->data.wtpSupport11B;


    return MFD_SUCCESS;
} /* wtpSupport11B_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpSupport11B_val
 *        A long containing the new value.
 */
int
wtpSupport11B_set( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpSupport11B_val )
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpSupport11B_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpSupport11B reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpSupport11B_val) {
        case WTPSUPPORT11B_YES:
             wtpSupport11B_val = INTERNAL_WTPSUPPORT11B_YES;
             break;

        case WTPSUPPORT11B_NO:
             wtpSupport11B_val = INTERNAL_WTPSUPPORT11B_NO;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpSupport11B\n", wtpSupport11B_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpSupport11B value.
     * set wtpSupport11B value in rowreq_ctx->data
     */
    rowreq_ctx->data.wtpSupport11B = wtpSupport11B_val;

    return MFD_SUCCESS;
} /* wtpSupport11B_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpSupport11B_undo( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpSupport11B_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpSupport11B undo.
     */
    /*
     * copy wtpSupport11B data
     * set rowreq_ctx->data.wtpSupport11B from rowreq_ctx->undo->wtpSupport11B
     */
    rowreq_ctx->data.wtpSupport11B = rowreq_ctx->undo->wtpSupport11B;

    
    return MFD_SUCCESS;
} /* wtpSupport11B_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpParaEntry.wtpSupport11G
 * wtpSupport11G is subid 16 of dot11WtpParaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.5.1.16
 * Description:
AP work mode includes:
							yes:AP support 11G
							no:AP not support 11G.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  yes(1), no(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSupport11G_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpParaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  yes(1), no(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpSupport11G_check_value( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpSupport11G_val)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpSupport11G_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpSupport11G value.
     */

    return MFD_SUCCESS; /* wtpSupport11G value not illegal */
} /* wtpSupport11G_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpParaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpParaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpSupport11G_undo_setup( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpSupport11G_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpSupport11G undo.
     */
    /*
     * copy wtpSupport11G data
     * set rowreq_ctx->undo->wtpSupport11G from rowreq_ctx->data.wtpSupport11G
     */
    rowreq_ctx->undo->wtpSupport11G = rowreq_ctx->data.wtpSupport11G;


    return MFD_SUCCESS;
} /* wtpSupport11G_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpSupport11G_val
 *        A long containing the new value.
 */
int
wtpSupport11G_set( dot11WtpParaTable_rowreq_ctx *rowreq_ctx, u_long wtpSupport11G_val )
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpSupport11G_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpSupport11G reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpSupport11G_val) {
        case WTPSUPPORT11G_YES:
             wtpSupport11G_val = INTERNAL_WTPSUPPORT11G_YES;
             break;

        case WTPSUPPORT11G_NO:
             wtpSupport11G_val = INTERNAL_WTPSUPPORT11G_NO;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpSupport11G\n", wtpSupport11G_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpSupport11G value.
     * set wtpSupport11G value in rowreq_ctx->data
     */
    rowreq_ctx->data.wtpSupport11G = wtpSupport11G_val;

    return MFD_SUCCESS;
} /* wtpSupport11G_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpSupport11G_undo( dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpParaTable:wtpSupport11G_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpSupport11G undo.
     */
    /*
     * copy wtpSupport11G data
     * set rowreq_ctx->data.wtpSupport11G from rowreq_ctx->undo->wtpSupport11G
     */
    rowreq_ctx->data.wtpSupport11G = rowreq_ctx->undo->wtpSupport11G;

    
    return MFD_SUCCESS;
} /* wtpSupport11G_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpParaTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11WtpParaTable if you don't have dependencies)
 */
int
dot11WtpParaTable_check_dependencies(dot11WtpParaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11WtpParaTable:dot11WtpParaTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11WtpParaTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11WtpParaTable_check_dependencies */

/** @} */
