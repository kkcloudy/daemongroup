/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpBssidTeminalTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpBssidTeminalTable get routines.
 * TODO:240:M: Implement dot11WtpBssidTeminalTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpBssidTeminalTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpBssidTeminalTable is subid 3 of wlanPortal.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpBssidTeminalTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_map_wtpbssid(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:wtpMacAddr_map_wtpbssid","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map_wtpbssid */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfIndex
 * wtpWirelessIfIndex is subid 1 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.1
 * Description:
Wireless interface index.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWirelessIfIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWirelessIfIndex_dot11WtpBssidTeminalTable_map(long *mib_wtpWirelessIfIndex_val_ptr, long raw_wtpWirelessIfIndex_val)
{
    netsnmp_assert(NULL != mib_wtpWirelessIfIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:wtpWirelessIfIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWirelessIfIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpWirelessIfIndex_val_ptr) = raw_wtpWirelessIfIndex_val;

    return MFD_SUCCESS;
} /* wtpWirelessIfIndex_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWlanStatsEntry.wlanCurrID
 * wlanCurrID is subid 1 of dot11WtpWlanStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.5.3.1.1
 * Description:
The wlan's ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wlanCurrID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wlanCurrID_map_wtpbssid(long *mib_wlanCurrID_val_ptr, long raw_wlanCurrID_val)
{
    netsnmp_assert(NULL != mib_wlanCurrID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:wlanCurrID_map_wtpbssid","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wlanCurrID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wlanCurrID_val_ptr) = raw_wlanCurrID_val;

    return MFD_SUCCESS;
} /* wlanCurrID_map_wtpbssid */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpBssidTeminalTable_indexes_set_tbl_idx(dot11WtpBssidTeminalTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpWirelessIfIndex_val, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:dot11WtpBssidTeminalTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wtpWirelessIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wtpWirelessIfIndex = wtpWirelessIfIndex_val;
    
    /* wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wlanCurrID = wlanCurrID_val;
    

    return MFD_SUCCESS;
} /* dot11WtpBssidTeminalTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpBssidTeminalTable_indexes_set(dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpWirelessIfIndex_val, long wlanCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:dot11WtpBssidTeminalTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpBssidTeminalTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpWirelessIfIndex_val
                                   , wlanCurrID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpBssidTeminalTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpBssidTeminalTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidCurusernum
 * pBssidCurusernum is subid 1 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.1
 * Description:
The ap depend on portal current user total number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-pBssidCurusernum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
pBssidCurusernum_map(u_long *mib_pBssidCurusernum_val_ptr, u_long raw_pBssidCurusernum_val)
{
    netsnmp_assert(NULL != mib_pBssidCurusernum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidCurusernum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement pBssidCurusernum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_pBssidCurusernum_val_ptr) = raw_pBssidCurusernum_val;

    return MFD_SUCCESS;
} /* pBssidCurusernum_map */

/**
 * Extract the current value of the pBssidCurusernum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidCurusernum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidCurusernum_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pBssidCurusernum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidCurusernum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidCurusernum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidCurusernum data.
 * set (* pBssidCurusernum_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidCurusernum_val_ptr ) = rowreq_ctx->data.pBssidCurusernum;

    return MFD_SUCCESS;
} /* pBssidCurusernum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidLogoffnum
 * pBssidLogoffnum is subid 2 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.2
 * Description:
The ap depend on portal fail number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-pBssidLogoffnum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
pBssidLogoffnum_map(u_long *mib_pBssidLogoffnum_val_ptr, u_long raw_pBssidLogoffnum_val)
{
    netsnmp_assert(NULL != mib_pBssidLogoffnum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidLogoffnum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement pBssidLogoffnum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_pBssidLogoffnum_val_ptr) = raw_pBssidLogoffnum_val;

    return MFD_SUCCESS;
} /* pBssidLogoffnum_map */

/**
 * Extract the current value of the pBssidLogoffnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidLogoffnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidLogoffnum_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pBssidLogoffnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidLogoffnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidLogoffnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidLogoffnum data.
 * set (* pBssidLogoffnum_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidLogoffnum_val_ptr ) = rowreq_ctx->data.pBssidLogoffnum;

    return MFD_SUCCESS;
} /* pBssidLogoffnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidReqnum
 * pBssidReqnum is subid 3 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.3
 * Description:
The ap depend on portal request number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-pBssidReqnum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
pBssidReqnum_map(u_long *mib_pBssidReqnum_val_ptr, u_long raw_pBssidReqnum_val)
{
    netsnmp_assert(NULL != mib_pBssidReqnum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidReqnum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement pBssidReqnum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_pBssidReqnum_val_ptr) = raw_pBssidReqnum_val;

    return MFD_SUCCESS;
} /* pBssidReqnum_map */

/**
 * Extract the current value of the pBssidReqnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidReqnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidReqnum_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pBssidReqnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidReqnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidReqnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidReqnum data.
 * set (* pBssidReqnum_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidReqnum_val_ptr ) = rowreq_ctx->data.pBssidReqnum;

    return MFD_SUCCESS;
} /* pBssidReqnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidReqsuccnum
 * pBssidReqsuccnum is subid 4 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.4
 * Description:
The ap depend on portal request success number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-pBssidReqsuccnum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
pBssidReqsuccnum_map(u_long *mib_pBssidReqsuccnum_val_ptr, u_long raw_pBssidReqsuccnum_val)
{
    netsnmp_assert(NULL != mib_pBssidReqsuccnum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidReqsuccnum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement pBssidReqsuccnum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_pBssidReqsuccnum_val_ptr) = raw_pBssidReqsuccnum_val;

    return MFD_SUCCESS;
} /* pBssidReqsuccnum_map */

/**
 * Extract the current value of the pBssidReqsuccnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidReqsuccnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidReqsuccnum_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pBssidReqsuccnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidReqsuccnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidReqsuccnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidReqsuccnum data.
 * set (* pBssidReqsuccnum_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidReqsuccnum_val_ptr ) = rowreq_ctx->data.pBssidReqsuccnum;

    return MFD_SUCCESS;
} /* pBssidReqsuccnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidReqfailnum
 * pBssidReqfailnum is subid 5 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.5
 * Description:
The ap depend on bssid portal request fail number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-pBssidReqfailnum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
pBssidReqfailnum_map(u_long *mib_pBssidReqfailnum_val_ptr, u_long raw_pBssidReqfailnum_val)
{
    netsnmp_assert(NULL != mib_pBssidReqfailnum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidReqfailnum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement pBssidReqfailnum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_pBssidReqfailnum_val_ptr) = raw_pBssidReqfailnum_val;

    return MFD_SUCCESS;
} /* pBssidReqfailnum_map */

/**
 * Extract the current value of the pBssidReqfailnum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidReqfailnum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidReqfailnum_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pBssidReqfailnum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidReqfailnum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidReqfailnum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidReqfailnum data.
 * set (* pBssidReqfailnum_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidReqfailnum_val_ptr ) = rowreq_ctx->data.pBssidReqfailnum;

    return MFD_SUCCESS;
} /* pBssidReqfailnum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidReqSuccRate
 * pBssidReqSuccRate is subid 6 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.6
 * Description:
The ap depend on bssid portal request success rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the pBssidReqSuccRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidReqSuccRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidReqSuccRate_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, long * pBssidReqSuccRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidReqSuccRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidReqSuccRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidReqSuccRate data.
 * copy (* pBssidReqSuccRate_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidReqSuccRate_val_ptr ) = rowreq_ctx->data.pBssidReqSuccRate;

    return MFD_SUCCESS;
} /* pBssidReqSuccRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidUserRedirReqNum
 * pBssidUserRedirReqNum is subid 7 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.7
 * Description:
The ap depend on bssid portal page request number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the pBssidUserRedirReqNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidUserRedirReqNum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidUserRedirReqNum_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pBssidUserRedirReqNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidUserRedirReqNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidUserRedirReqNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidUserRedirReqNum data.
 * copy (* pBssidUserRedirReqNum_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidUserRedirReqNum_val_ptr ) = rowreq_ctx->data.pBssidUserRedirReqNum;

    return MFD_SUCCESS;
} /* pBssidUserRedirReqNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidUserRedirReqSuccNum
 * pBssidUserRedirReqSuccNum is subid 8 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.8
 * Description:
The ap depend on bssid portal page request success number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the pBssidUserRedirReqSuccNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidUserRedirReqSuccNum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidUserRedirReqSuccNum_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pBssidUserRedirReqSuccNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidUserRedirReqSuccNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidUserRedirReqSuccNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidUserRedirReqSuccNum data.
 * copy (* pBssidUserRedirReqSuccNum_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidUserRedirReqSuccNum_val_ptr ) = rowreq_ctx->data.pBssidUserRedirReqSuccNum;

    return MFD_SUCCESS;
} /* pBssidUserRedirReqSuccNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidUserRedirReqSuccRate
 * pBssidUserRedirReqSuccRate is subid 9 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.9
 * Description:
The ap depend on bssid portal page request success rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the pBssidUserRedirReqSuccRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidUserRedirReqSuccRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidUserRedirReqSuccRate_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, long * pBssidUserRedirReqSuccRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidUserRedirReqSuccRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidUserRedirReqSuccRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidUserRedirReqSuccRate data.
 * copy (* pBssidUserRedirReqSuccRate_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidUserRedirReqSuccRate_val_ptr ) = rowreq_ctx->data.pBssidUserRedirReqSuccRate;

    return MFD_SUCCESS;
} /* pBssidUserRedirReqSuccRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidRadiusAuthReqNum
 * pBssidRadiusAuthReqNum is subid 10 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.10
 * Description:
The ap depend on bssid radius authentication request number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the pBssidRadiusAuthReqNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidRadiusAuthReqNum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidRadiusAuthReqNum_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pBssidRadiusAuthReqNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidRadiusAuthReqNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidRadiusAuthReqNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidRadiusAuthReqNum data.
 * copy (* pBssidRadiusAuthReqNum_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidRadiusAuthReqNum_val_ptr ) = rowreq_ctx->data.pBssidRadiusAuthReqNum;

    return MFD_SUCCESS;
} /* pBssidRadiusAuthReqNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidRadiusAuthReqSuccNum
 * pBssidRadiusAuthReqSuccNum is subid 11 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.11
 * Description:
The ap depend on bssid radius authentication request success number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the pBssidRadiusAuthReqSuccNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidRadiusAuthReqSuccNum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidRadiusAuthReqSuccNum_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pBssidRadiusAuthReqSuccNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidRadiusAuthReqSuccNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidRadiusAuthReqSuccNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidRadiusAuthReqSuccNum data.
 * copy (* pBssidRadiusAuthReqSuccNum_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidRadiusAuthReqSuccNum_val_ptr ) = rowreq_ctx->data.pBssidRadiusAuthReqSuccNum;

    return MFD_SUCCESS;
} /* pBssidRadiusAuthReqSuccNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidRadiusAuthReqSuccRate
 * pBssidRadiusAuthReqSuccRate is subid 12 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.12
 * Description:
The ap depend on bssid radius authentication request success rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the pBssidRadiusAuthReqSuccRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidRadiusAuthReqSuccRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidRadiusAuthReqSuccRate_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, long * pBssidRadiusAuthReqSuccRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidRadiusAuthReqSuccRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidRadiusAuthReqSuccRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidRadiusAuthReqSuccRate data.
 * copy (* pBssidRadiusAuthReqSuccRate_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidRadiusAuthReqSuccRate_val_ptr ) = rowreq_ctx->data.pBssidRadiusAuthReqSuccRate;

    return MFD_SUCCESS;
} /* pBssidRadiusAuthReqSuccRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidChallengeReqNum
 * pBssidChallengeReqNum is subid 13 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.13
 * Description:
The ap depend on bssid challenge request number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the pBssidChallengeReqNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidChallengeReqNum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidChallengeReqNum_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pBssidChallengeReqNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidChallengeReqNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidChallengeReqNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidChallengeReqNum data.
 * copy (* pBssidChallengeReqNum_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidChallengeReqNum_val_ptr ) = rowreq_ctx->data.pBssidChallengeReqNum;

    return MFD_SUCCESS;
} /* pBssidChallengeReqNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidChallengeReqSuccNum
 * pBssidChallengeReqSuccNum is subid 14 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.14
 * Description:
The ap depend on bssid challenge request success number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the pBssidChallengeReqSuccNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidChallengeReqSuccNum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidChallengeReqSuccNum_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, u_long * pBssidChallengeReqSuccNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidChallengeReqSuccNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidChallengeReqSuccNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidChallengeReqSuccNum data.
 * copy (* pBssidChallengeReqSuccNum_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidChallengeReqSuccNum_val_ptr ) = rowreq_ctx->data.pBssidChallengeReqSuccNum;

    return MFD_SUCCESS;
} /* pBssidChallengeReqSuccNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpBssidTeminalEntry.pBssidChallengeReqSuccRate
 * pBssidChallengeReqSuccRate is subid 15 of dot11WtpBssidTeminalEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3.1.15
 * Description:
The ap depend on bssid challenge request success rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the pBssidChallengeReqSuccRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param pBssidChallengeReqSuccRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
pBssidChallengeReqSuccRate_get( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx, long * pBssidChallengeReqSuccRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != pBssidChallengeReqSuccRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:pBssidChallengeReqSuccRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the pBssidChallengeReqSuccRate data.
 * copy (* pBssidChallengeReqSuccRate_val_ptr ) from rowreq_ctx->data
 */
    (* pBssidChallengeReqSuccRate_val_ptr ) = rowreq_ctx->data.pBssidChallengeReqSuccRate;

    return MFD_SUCCESS;
} /* pBssidChallengeReqSuccRate_get */



/** @} */
