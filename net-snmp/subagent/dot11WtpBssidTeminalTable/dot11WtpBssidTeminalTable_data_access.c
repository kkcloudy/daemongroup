/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

#include "mibs_public.h"
#include "autelanWtpGroup.h"
#include "ws_user_manage.h"
#include "ws_conf_engine.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "nm/app/eag/eag_conf.h"
#include "nm/public/nm_list.h"
#include "nm/app/eag/eag_interface.h"
#include "ws_dcli_wlans.h"
#include "ws_dcli_ac.h"
/* include our parent header */
#include "dot11WtpBssidTeminalTable.h"


#include "dot11WtpBssidTeminalTable_data_access.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpBssidTeminalTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpBssidTeminalTable is subid 3 of wlanPortal.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.17.3, length: 12
*/

/**
 * initialization for dot11WtpBssidTeminalTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11WtpBssidTeminalTable_reg
 *        Pointer to dot11WtpBssidTeminalTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11WtpBssidTeminalTable_init_data(dot11WtpBssidTeminalTable_registration_ptr dot11WtpBssidTeminalTable_reg)
{
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:dot11WtpBssidTeminalTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11WtpBssidTeminalTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11WtpBssidTeminalTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11WtpBssidTeminalTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:dot11WtpBssidTeminalTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11WtpBssidTeminalTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11WtpBssidTeminalTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11WTPBSSIDTEMINALTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11WtpBssidTeminalTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11WtpBssidTeminalTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11WtpBssidTeminalTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11WtpBssidTeminalTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11WtpBssidTeminalTable_cache_load\n");

    dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
         */
        /** 128 - 1(entry) - 1(col) - 2(other indexes) = 110 */
   char   wtpMacAddr[20];
   size_t      wtpMacAddr_len;
        /*
         * wtpWirelessIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   wtpWirelessIfIndex = 0;
        /*
         * wlanCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   wlanCurrID = 0;

    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:dot11WtpBssidTeminalTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
        //return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11WtpBssidTeminalTable container.
     * loop over your dot11WtpBssidTeminalTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    instance_parameter *paraHead = NULL;
	instance_parameter *pq = NULL;
	int result = 0;
	struct SSIDStatsInfo *WtpHead = NULL;
	struct SSIDStatsInfo *WtpShowNode = NULL;
	struct SSIDStatsInfo_Radioid_info *Radioid_info_node = NULL;
	int i = 0;
	int j = 0;
	struct list_head bss_stat;
	struct eag_bss_stat *tmp = NULL;
	unsigned long pBssidCurusernum = 0;
	unsigned long pBssidLogoffnum = 0;
	unsigned long pBssidReqnum = 0;
	unsigned long pBssidReqsuccnum = 0;
	unsigned long pBssidReqfailnum = 0;
	unsigned long pBssidUserRedirReqNum = 0;
	unsigned long pBssidUserRedirReqSuccNum = 0;
	unsigned long pBssidRadiusAuthReqNum = 0;
	unsigned long pBssidRadiusAuthReqSuccNum = 0;
	unsigned long pBssidChallengeReqNum = 0;
	unsigned long pBssidChallengeReqSuccNum = 0;

	snmp_log(LOG_DEBUG, "enter list_instance_parameter\n");
	list_instance_parameter(&paraHead, SNMPD_INSTANCE_MASTER); 
	snmp_log(LOG_DEBUG, "exit list_instance_parameter,paraHead=%p\n", paraHead);
	for(pq = paraHead; (NULL != pq); pq = pq->next)
	{
		eag_get_bss_statistics(pq->connection,pq->parameter.local_id,pq->parameter.instance_id,&bss_stat);
		
		result=show_all_wlan_ssid_stats_information_cmd(pq->parameter,pq->connection,&WtpHead);
		if((result == 1)&&(WtpHead))
		{
			for(WtpShowNode = WtpHead->SSIDStatsInfo_list; (NULL != WtpShowNode); WtpShowNode = WtpShowNode->next)
			{
				memset(wtpMacAddr, 0, sizeof(wtpMacAddr));
				if(WtpShowNode->wtpMacAddr)
				{
					snprintf(wtpMacAddr, 
							 sizeof(wtpMacAddr)-1, 
							 "%02X:%02X:%02X:%02X:%02X:%02X",
							 WtpShowNode->wtpMacAddr[0],
							 WtpShowNode->wtpMacAddr[1], 
							 WtpShowNode->wtpMacAddr[2],
							 WtpShowNode->wtpMacAddr[3], 
							 WtpShowNode->wtpMacAddr[4],
							 WtpShowNode->wtpMacAddr[5]);
				}
				
				for(i=0,Radioid_info_node=WtpShowNode->SSIDStatsInfo_Radioid_info_head;
					((i<WtpShowNode->wtpBwlanRadioNum)&&(NULL != Radioid_info_node));
					i++,Radioid_info_node=Radioid_info_node->next)
				{
					wtpWirelessIfIndex = (Radioid_info_node->radioId)%4 + 1;

					for(j=0; j<Radioid_info_node->radioBwlanNum; j++)
					{
						wlanCurrID = local_to_global_ID(pq->parameter, Radioid_info_node->RadioidSupportSSID[j], WIRELESS_MAX_NUM);
						
						pBssidCurusernum = 0;
						pBssidLogoffnum = 0;
						pBssidReqnum = 0;
						pBssidReqsuccnum = 0;
						pBssidReqfailnum = 0;
						
						list_for_each_entry(tmp,&bss_stat,node)
						{
							if(((WtpShowNode->wtpMacAddr)&&(tmp)&&(memcmp(tmp->ap_mac, WtpShowNode->wtpMacAddr, 6) == 0))
								&&(wtpWirelessIfIndex == (tmp->radioid+1))
								&&(Radioid_info_node->RadioidSupportSSID[j] == tmp->wlanid)
							  )
							{
								pBssidCurusernum = tmp->online_user_num;
								pBssidLogoffnum = tmp->abnormal_logoff_count;
								pBssidReqnum = tmp->auth_req_count;
								pBssidReqsuccnum = tmp->auth_ack_0_count;
								pBssidReqfailnum = (tmp->auth_ack_1_count + tmp->auth_ack_2_count
										+ tmp->auth_ack_3_count + tmp->auth_ack_4_count);
								pBssidUserRedirReqNum = tmp->http_redir_request_count;
								pBssidUserRedirReqSuccNum = tmp->http_redir_success_count;
								pBssidRadiusAuthReqNum = tmp->access_request_count;
								pBssidRadiusAuthReqSuccNum = tmp->access_accept_count;
								pBssidChallengeReqNum = tmp->challenge_req_count;
								pBssidChallengeReqSuccNum = tmp->challenge_ack_0_count;
								break;
							}
						}	
					//	pBssidReqfailnum = tmp->auth_ack_1_count + tmp->auth_ack_2_count
					//	+ tmp->auth_ack_3_count + tmp->auth_ack_4_count;
		    {
	    /*
	    ***************************************************
	    ***             START EXAMPLE CODE              ***
	    ***---------------------------------------------***/
	    /*
	     * get a line (skip blank lines)
	     */
	    //do {
	      //  if (!fgets(line, sizeof(line), filep)) {
	            /* we're done */
	        //    fclose(filep);
	          //  filep = NULL;
	        //}
	    //} while (filep && (line[0] == '\n'));

	    /*
	     * check for end of data
	     */
	    //if(NULL == filep)
	      //  break;

	    /*
	     * parse line into variables
	     */
	    /*
	    ***---------------------------------------------***
	    ***              END  EXAMPLE CODE              ***
	    ***************************************************/

	        /*
	         * TODO:352:M: |   |-> set indexes in new dot11WtpBssidTeminalTable rowreq context.
	         */
	        rowreq_ctx = dot11WtpBssidTeminalTable_allocate_rowreq_ctx();
	        if (NULL == rowreq_ctx) {
	            snmp_log(LOG_ERR, "memory allocation failed\n");
				Free_show_all_wlan_ssid_stats_information_cmd(WtpHead);
				eag_free_bss_statistics(&bss_stat);
				free_instance_parameter_list(&paraHead);
	            return MFD_RESOURCE_UNAVAILABLE;
	        }
			wtpMacAddr_len = strlen(wtpMacAddr);
	        if(MFD_SUCCESS != dot11WtpBssidTeminalTable_indexes_set(rowreq_ctx
	                               , wtpMacAddr, wtpMacAddr_len
	                               , wtpWirelessIfIndex
	                               , wlanCurrID
	               )) {
	            snmp_log(LOG_ERR,"error setting index while loading "
	                     "dot11WtpBssidTeminalTable cache.\n");
	            dot11WtpBssidTeminalTable_release_rowreq_ctx(rowreq_ctx);
	            continue;
	        }

	        /*
	         * TODO:352:r: |   |-> populate dot11WtpBssidTeminalTable data context.
	         * Populate data context here. (optionally, delay until row prep)
	         */
	    /*
	     * TRANSIENT or semi-TRANSIENT data:
	     * copy data or save any info needed to do it in row_prep.
	     */
	    /*
	     * setup/save data for pBssidCurusernum
	     * pBssidCurusernum(1)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define pBssidCurusernum mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.pBssidCurusernum = pBssidCurusernum;
	    
	    /*
	     * setup/save data for pBssidLogoffnum
	     * pBssidLogoffnum(2)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define pBssidLogoffnum mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.pBssidLogoffnum = pBssidLogoffnum;
	    
	    /*
	     * setup/save data for pBssidReqnum
	     * pBssidReqnum(3)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define pBssidReqnum mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.pBssidReqnum = pBssidReqnum;
	    
	    /*
	     * setup/save data for pBssidReqsuccnum
	     * pBssidReqsuccnum(4)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define pBssidReqsuccnum mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.pBssidReqsuccnum = pBssidReqsuccnum;
	    
	    /*
	     * setup/save data for pBssidReqfailnum
	     * pBssidReqfailnum(5)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define pBssidReqfailnum mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.pBssidReqfailnum = pBssidReqfailnum;
		if(0 == pBssidReqnum){
			rowreq_ctx->data.pBssidReqSuccRate = 0;
		}
		else{
			rowreq_ctx->data.pBssidReqSuccRate = pBssidReqsuccnum * 100 / pBssidReqnum;

		}
					
		rowreq_ctx->data.pBssidUserRedirReqNum = pBssidUserRedirReqNum;
		rowreq_ctx->data.pBssidUserRedirReqSuccNum = pBssidUserRedirReqSuccNum;
		if(0 == pBssidUserRedirReqNum){
			rowreq_ctx->data.pBssidUserRedirReqSuccRate = 0;
		}
		else{
			rowreq_ctx->data.pBssidUserRedirReqSuccRate = pBssidUserRedirReqSuccNum * 100 / pBssidUserRedirReqNum;

		}
					
		rowreq_ctx->data.pBssidRadiusAuthReqNum = pBssidRadiusAuthReqNum;
		rowreq_ctx->data.pBssidRadiusAuthReqSuccNum = pBssidRadiusAuthReqSuccNum;
		if(0 == pBssidRadiusAuthReqNum){
			rowreq_ctx->data.pBssidRadiusAuthReqSuccRate = 0;
		}
		else{
			rowreq_ctx->data.pBssidRadiusAuthReqSuccRate = pBssidRadiusAuthReqSuccNum * 100 / pBssidRadiusAuthReqNum;

		}
					
		rowreq_ctx->data.pBssidChallengeReqNum = pBssidChallengeReqNum;
		rowreq_ctx->data.pBssidChallengeReqSuccNum = pBssidChallengeReqSuccNum;
		if(0 == pBssidChallengeReqNum){
			rowreq_ctx->data.pBssidChallengeReqSuccRate = 0;
		}
		else{
			rowreq_ctx->data.pBssidChallengeReqSuccRate = pBssidChallengeReqSuccNum * 100 / pBssidChallengeReqNum;

		}
	        /*
	         * insert into table container
	         */
	        if(CONTAINER_INSERT(container, rowreq_ctx))
			{
				dot11WtpBssidTeminalTable_release_rowreq_ctx(rowreq_ctx);
			}
	        ++count;
	    }			}
				}
			}		
		}
		Free_show_all_wlan_ssid_stats_information_cmd(WtpHead);
		eag_free_bss_statistics(&bss_stat);
	}
	free_instance_parameter_list(&paraHead);
	


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
      //  fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11WtpBssidTeminalTable:dot11WtpBssidTeminalTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11WtpBssidTeminalTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11WtpBssidTeminalTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11WtpBssidTeminalTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:dot11WtpBssidTeminalTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11WtpBssidTeminalTable cache.
     */
} /* dot11WtpBssidTeminalTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11WtpBssidTeminalTable_row_prep( dot11WtpBssidTeminalTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpBssidTeminalTable:dot11WtpBssidTeminalTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11WtpBssidTeminalTable_row_prep */

/** @} */
