/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11DHCPIpv6PoolConfigTable.h"
#include "ws_dbus_def.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11DHCPIpv6PoolConfigTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11DHCPIpv6PoolConfigTable is subid 7 of dot11Service.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.7, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11DHCPIpv6PoolConfigTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11DHCPIpv6PoolConfigTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpv6PoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11DHCPIpv6PoolConfigTable_undo_setup( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:dot11DHCPIpv6PoolConfigTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11DHCPIpv6PoolConfigTable undo.
     * set up dot11DHCPIpv6PoolConfigTable undo information, in preparation for a set.
     * Undo storage is in (* Ipv6DHCPRowStatus_val_ptr )*
     */

    return rc;
} /* dot11DHCPIpv6PoolConfigTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpv6PoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11DHCPIpv6PoolConfigTable_undo( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:dot11DHCPIpv6PoolConfigTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11DHCPIpv6PoolConfigTable undo.
     * dot11DHCPIpv6PoolConfigTable undo information, in response to a failed set.
     * Undo storage is in (* Ipv6DHCPRowStatus_val_ptr )*
     */

    return rc;
} /* dot11DHCPIpv6PoolConfigTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpv6PoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11DHCPIpv6PoolConfigTable_undo_cleanup( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:dot11DHCPIpv6PoolConfigTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11DHCPIpv6PoolConfigTable undo.
     * Undo storage is in (* Ipv6DHCPRowStatus_val_ptr )*
     */

    return rc;
} /* dot11DHCPIpv6PoolConfigTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11DHCPIpv6PoolConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11DHCPIpv6PoolConfigTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
	
static int
check_create_ready(dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx) {
	if(strlen(rowreq_ctx->data.IPv6PoolStartAddr) && strlen(rowreq_ctx->data.IPv6PoolStopAddr) && (rowreq_ctx->data.IPv6NetPrefixLen!=0) && strlen(rowreq_ctx->data.Ipv6DHCPPoolName)){
	return MFD_SUCCESS;
    }
    return MFD_ERROR;
}


int
dot11DHCPIpv6PoolConfigTable_commit( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
	snmp_log(LOG_DEBUG, "enter dot11DHCPIpv6PoolConfigTable_commit\n");
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:dot11DHCPIpv6PoolConfigTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    
	
    if(RS_NOTREADY == rowreq_ctx->data.Ipv6DHCPRowStatus && MFD_SUCCESS == check_create_ready(rowreq_ctx)) {
        rowreq_ctx->data.Ipv6DHCPRowStatus = RS_NOTINSERVICE;
    }
    

    /*
     * commit dot11DHCPIpv6PoolConfigTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_IPV6DHCPPOOLNAME_FLAG) {
       save_flags &= ~COLUMN_IPV6DHCPPOOLNAME_FLAG; /* clear Ipv6DHCPPoolName */
       /*
        * TODO:482:o: |-> commit column Ipv6DHCPPoolName.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpv6PoolConfigTable column Ipv6DHCPPoolName commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo Ipv6DHCPPoolName
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPV6DHCPPOOLNAME_FLAG;
       }
    }

    if (save_flags & COLUMN_IPV6POOLSTARTADDR_FLAG) {
       save_flags &= ~COLUMN_IPV6POOLSTARTADDR_FLAG; /* clear IPv6PoolStartAddr */
       /*
        * TODO:482:o: |-> commit column IPv6PoolStartAddr.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpv6PoolConfigTable column IPv6PoolStartAddr commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo IPv6PoolStartAddr
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPV6POOLSTARTADDR_FLAG;
       }
    }

    if (save_flags & COLUMN_IPV6POOLSTOPADDR_FLAG) {
       save_flags &= ~COLUMN_IPV6POOLSTOPADDR_FLAG; /* clear IPv6PoolStopAddr */
       /*
        * TODO:482:o: |-> commit column IPv6PoolStopAddr.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpv6PoolConfigTable column IPv6PoolStopAddr commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo IPv6PoolStopAddr
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPV6POOLSTOPADDR_FLAG;
       }
    }

    if (save_flags & COLUMN_IPV6NETPREFIXLEN_FLAG) {
       save_flags &= ~COLUMN_IPV6NETPREFIXLEN_FLAG; /* clear IPv6NetPrefixLen */
       /*
        * TODO:482:o: |-> commit column IPv6NetPrefixLen.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpv6PoolConfigTable column IPv6NetPrefixLen commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo IPv6NetPrefixLen
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPV6NETPREFIXLEN_FLAG;
       }
    }

    if (save_flags & COLUMN_FIRDNSSERVERIPV6ADDRESS_FLAG) {
       save_flags &= ~COLUMN_FIRDNSSERVERIPV6ADDRESS_FLAG; /* clear FirDNSServerIPv6Address */
       /*
        * TODO:482:o: |-> commit column FirDNSServerIPv6Address.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpv6PoolConfigTable column FirDNSServerIPv6Address commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo FirDNSServerIPv6Address
             */
            rowreq_ctx->column_set_flags |= COLUMN_FIRDNSSERVERIPV6ADDRESS_FLAG;
       }
    }

    if (save_flags & COLUMN_SECONDNSSERVERIPV6ADDRESS_FLAG) {
       save_flags &= ~COLUMN_SECONDNSSERVERIPV6ADDRESS_FLAG; /* clear SeconDNSServerIPv6Address */
       /*
        * TODO:482:o: |-> commit column SeconDNSServerIPv6Address.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpv6PoolConfigTable column SeconDNSServerIPv6Address commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo SeconDNSServerIPv6Address
             */
            rowreq_ctx->column_set_flags |= COLUMN_SECONDNSSERVERIPV6ADDRESS_FLAG;
       }
    }

    if (save_flags & COLUMN_IPV6LEASEPERIOD_FLAG) {
       save_flags &= ~COLUMN_IPV6LEASEPERIOD_FLAG; /* clear Ipv6LeasePeriod */
       /*
        * TODO:482:o: |-> commit column Ipv6LeasePeriod.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpv6PoolConfigTable column Ipv6LeasePeriod commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo Ipv6LeasePeriod
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPV6LEASEPERIOD_FLAG;
       }
    }

    if (save_flags & COLUMN_IPV6DHCPROWSTATUS_FLAG) {
       save_flags &= ~COLUMN_IPV6DHCPROWSTATUS_FLAG; /* clear Ipv6DHCPRowStatus */
       	   switch(rowreq_ctx->data.Ipv6DHCPRowStatus) 
	   {
	       case RS_ACTIVE:
	       {
		       int ret = -1;
		       int retconfig = 0;
		       char pool_name[128] = { 0 };
		       char len[5]={0};
		       unsigned int index = 0,mode = 1,ip_ret=0,time=0;
		       unsigned int bkindex = 0;
		       char *dns=(char *)malloc(280);
		       memset(dns,0, sizeof(dns));
			
			strcat(dns,rowreq_ctx->data.FirDNSServerIPv6Address);
			strcat(dns,",");
			strcat(dns,rowreq_ctx->data.SeconDNSServerIPv6Address);
			//snprintf(dns,sizeof(dns)-1,"%s,%s",rowreq_ctx->data.FirDNSServerIPv6Address,rowreq_ctx->data.SeconDNSServerIPv6Address);
			snprintf(len,sizeof(len)-1,"%d",rowreq_ctx->data.IPv6NetPrefixLen);
				
		       if(strcmp(rowreq_ctx->data.Ipv6DHCPPoolName,""))
		       {
			       strncpy(pool_name,rowreq_ctx->data.Ipv6DHCPPoolName,sizeof(pool_name)-1);
		       }
		       
		       /*ip pool name*/
		       ret=ccgi_create_ipv6_pool_name(0,pool_name, &index, rowreq_ctx->data.slot_id);
		       snmp_log(LOG_DEBUG, "ret=%d\n",ret);
		       retconfig = ccgi_config_ipv6_pool_name(pool_name, &bkindex, rowreq_ctx->data.slot_id);
		       snmp_log(LOG_DEBUG, "retconfig=%d\n",retconfig);
		       if ((1 == ret)||(1 == retconfig))
		       {
			   if(1 == retconfig)
			       {
				       index = bkindex;
			       }
			       /*add ip pool range*/
			       ret=ccgi_addordel_ipv6pool("add", rowreq_ctx->data.IPv6PoolStartAddr, rowreq_ctx->data.IPv6PoolStopAddr, len, index , rowreq_ctx->data.slot_id);
			       snmp_log(LOG_DEBUG, "add ip pool range ------ret=%d\n",ret);
			       /*add dns*/     
			       if(strcmp(dns,"")!=0)
				       ret=ccgi_set_server_name_servers_ipv6(dns,mode, index, 0, rowreq_ctx->data.slot_id);
			       snmp_log(LOG_DEBUG, "add ip dns ------ret=%d\n",ret);
			       /*add lease*/
			       time=rowreq_ctx->data.Ipv6LeasePeriod/100;
			       ret=ccgi_set_server_lease_default_ipv6(time, mode, index,0, rowreq_ctx->data.slot_id);
			       snmp_log(LOG_DEBUG, "add ip lease ------ret=%d\n",ret);
		       }
		       rc = -1;
		       break;
	       }
	  
	       case RS_CREATEANDWAIT:
		       rowreq_ctx->data.Ipv6DHCPRowStatus = RS_NOTREADY;
		       rc = MFD_SUCCESS;
		       break;
			  
	       case RS_DESTROY:  
		       {
			       int ret = 0;
			       unsigned int index = 0;
			       ret = ccgi_create_ipv6_pool_name(1,rowreq_ctx->data.Ipv6DHCPPoolName, &index, rowreq_ctx->data.slot_id);
			       snmp_log(LOG_DEBUG, "del ip ret ------ret=%d\n",ret);
			       if(1 == ret)
			       {
				       rc = MFD_SUCCESS;							
				       break;
			       }
			       else
			       	{

			       }
		       }
		       rowreq_ctx->rowreq_flags &= ~MFD_ROW_DELETED;
		       rc = -1;
		       break;
		  
	       default:
		       break;
	}

       /*
        * TODO:482:o: |-> commit column Ipv6DHCPRowStatus.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11DHCPIpv6PoolConfigTable column Ipv6DHCPRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo Ipv6DHCPRowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_IPV6DHCPROWSTATUS_FLAG;
	    
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11DHCPIpv6PoolConfigTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11DHCPIpv6PoolConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11DHCPIpv6PoolConfigTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11DHCPIpv6PoolConfigTable_undo_commit( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:dot11DHCPIpv6PoolConfigTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11DHCPIpv6PoolConfigTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11DHCPIpv6PoolConfigTable_undo_commit */

/*
 * TODO:440:M: Implement dot11DHCPIpv6PoolConfigTable node value checks.
 * TODO:450:M: Implement dot11DHCPIpv6PoolConfigTable undo functions.
 * TODO:460:M: Implement dot11DHCPIpv6PoolConfigTable set functions.
 * TODO:480:M: Implement dot11DHCPIpv6PoolConfigTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpv6PoolConfigEntry.Ipv6DHCPPoolName
 * Ipv6DHCPPoolName is subid 2 of dot11DHCPIpv6PoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.7.1.2
 * Description:
DHCP IPV6 pool name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param Ipv6DHCPPoolName_val_ptr
 *        A char containing the new value.
 * @param Ipv6DHCPPoolName_val_ptr_len
 *        The size (in bytes) of the data pointed to by Ipv6DHCPPoolName_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpv6PoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.Ipv6DHCPPoolName).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
Ipv6DHCPPoolName_check_value( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, char *Ipv6DHCPPoolName_val_ptr,  size_t Ipv6DHCPPoolName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6DHCPPoolName_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != Ipv6DHCPPoolName_val_ptr);
    

    /*
     * TODO:441:o: |-> Check for valid Ipv6DHCPPoolName value.
     */
    if(RS_NOTINSERVICE == rowreq_ctx->data.Ipv6DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.Ipv6DHCPRowStatus)
	return MFD_SUCCESS; 
    else
	    return MFD_ERROR;

} /* Ipv6DHCPPoolName_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpv6PoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpv6PoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
Ipv6DHCPPoolName_undo_setup( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6DHCPPoolName_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup Ipv6DHCPPoolName undo.
     */
    /*
     * copy Ipv6DHCPPoolName and Ipv6DHCPPoolName_len data
     * set rowreq_ctx->undo->Ipv6DHCPPoolName from rowreq_ctx->data.Ipv6DHCPPoolName
     */
    memcpy( rowreq_ctx->undo->Ipv6DHCPPoolName, rowreq_ctx->data.Ipv6DHCPPoolName,
            (rowreq_ctx->data.Ipv6DHCPPoolName_len * sizeof(rowreq_ctx->undo->Ipv6DHCPPoolName[0])));
    rowreq_ctx->undo->Ipv6DHCPPoolName_len = rowreq_ctx->data.Ipv6DHCPPoolName_len;


    return MFD_SUCCESS;
} /* Ipv6DHCPPoolName_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param Ipv6DHCPPoolName_val_ptr
 *        A char containing the new value.
 * @param Ipv6DHCPPoolName_val_ptr_len
 *        The size (in bytes) of the data pointed to by Ipv6DHCPPoolName_val_ptr
 */
int
Ipv6DHCPPoolName_set( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, char *Ipv6DHCPPoolName_val_ptr,  size_t Ipv6DHCPPoolName_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6DHCPPoolName_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != Ipv6DHCPPoolName_val_ptr);

    /*
     * TODO:461:M: |-> Set Ipv6DHCPPoolName value.
     * set Ipv6DHCPPoolName value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.Ipv6DHCPPoolName, Ipv6DHCPPoolName_val_ptr, Ipv6DHCPPoolName_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.Ipv6DHCPPoolName_len = Ipv6DHCPPoolName_val_ptr_len / sizeof(Ipv6DHCPPoolName_val_ptr[0]);

    return MFD_SUCCESS;
} /* Ipv6DHCPPoolName_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
Ipv6DHCPPoolName_undo( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6DHCPPoolName_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up Ipv6DHCPPoolName undo.
     */
    /*
     * copy Ipv6DHCPPoolName and Ipv6DHCPPoolName_len data
     * set rowreq_ctx->data.Ipv6DHCPPoolName from rowreq_ctx->undo->Ipv6DHCPPoolName
     */
    memcpy( rowreq_ctx->data.Ipv6DHCPPoolName, rowreq_ctx->undo->Ipv6DHCPPoolName,
            (rowreq_ctx->undo->Ipv6DHCPPoolName_len * sizeof(rowreq_ctx->data.Ipv6DHCPPoolName[0])));
    rowreq_ctx->data.Ipv6DHCPPoolName_len = rowreq_ctx->undo->Ipv6DHCPPoolName_len;

    
    return MFD_SUCCESS;
} /* Ipv6DHCPPoolName_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpv6PoolConfigEntry.IPv6PoolStartAddr
 * IPv6PoolStartAddr is subid 3 of dot11DHCPIpv6PoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.7.1.3
 * Description:
IPV6 Address pool starting address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 2x:
 *
 * Ranges:  16;
 *
 * Its syntax is InetAddressIPv6 (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 16)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param IPv6PoolStartAddr_val_ptr
 *        A char containing the new value.
 * @param IPv6PoolStartAddr_val_ptr_len
 *        The size (in bytes) of the data pointed to by IPv6PoolStartAddr_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpv6PoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.IPv6PoolStartAddr).
 *    The length is in (one of) the range set(s):  16
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
IPv6PoolStartAddr_check_value( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, char *IPv6PoolStartAddr_val_ptr,  size_t IPv6PoolStartAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6PoolStartAddr_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != IPv6PoolStartAddr_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid IPv6PoolStartAddr value.
     */
    if(RS_NOTINSERVICE == rowreq_ctx->data.Ipv6DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.Ipv6DHCPRowStatus)
	return MFD_SUCCESS; 
    else
	    return MFD_ERROR;

} /* IPv6PoolStartAddr_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpv6PoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpv6PoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
IPv6PoolStartAddr_undo_setup( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6PoolStartAddr_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup IPv6PoolStartAddr undo.
     */
    /*
     * copy IPv6PoolStartAddr and IPv6PoolStartAddr_len data
     * set rowreq_ctx->undo->IPv6PoolStartAddr from rowreq_ctx->data.IPv6PoolStartAddr
     */
    memcpy( rowreq_ctx->undo->IPv6PoolStartAddr, rowreq_ctx->data.IPv6PoolStartAddr,
            (rowreq_ctx->data.IPv6PoolStartAddr_len * sizeof(rowreq_ctx->undo->IPv6PoolStartAddr[0])));
    rowreq_ctx->undo->IPv6PoolStartAddr_len = rowreq_ctx->data.IPv6PoolStartAddr_len;


    return MFD_SUCCESS;
} /* IPv6PoolStartAddr_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param IPv6PoolStartAddr_val_ptr
 *        A char containing the new value.
 * @param IPv6PoolStartAddr_val_ptr_len
 *        The size (in bytes) of the data pointed to by IPv6PoolStartAddr_val_ptr
 */
int
IPv6PoolStartAddr_set( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, char *IPv6PoolStartAddr_val_ptr,  size_t IPv6PoolStartAddr_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6PoolStartAddr_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != IPv6PoolStartAddr_val_ptr);

    /*
     * TODO:461:M: |-> Set IPv6PoolStartAddr value.
     * set IPv6PoolStartAddr value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.IPv6PoolStartAddr, IPv6PoolStartAddr_val_ptr, IPv6PoolStartAddr_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.IPv6PoolStartAddr_len = IPv6PoolStartAddr_val_ptr_len / sizeof(IPv6PoolStartAddr_val_ptr[0]);

    return MFD_SUCCESS;
} /* IPv6PoolStartAddr_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
IPv6PoolStartAddr_undo( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6PoolStartAddr_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up IPv6PoolStartAddr undo.
     */
    /*
     * copy IPv6PoolStartAddr and IPv6PoolStartAddr_len data
     * set rowreq_ctx->data.IPv6PoolStartAddr from rowreq_ctx->undo->IPv6PoolStartAddr
     */
    memcpy( rowreq_ctx->data.IPv6PoolStartAddr, rowreq_ctx->undo->IPv6PoolStartAddr,
            (rowreq_ctx->undo->IPv6PoolStartAddr_len * sizeof(rowreq_ctx->data.IPv6PoolStartAddr[0])));
    rowreq_ctx->data.IPv6PoolStartAddr_len = rowreq_ctx->undo->IPv6PoolStartAddr_len;

    
    return MFD_SUCCESS;
} /* IPv6PoolStartAddr_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpv6PoolConfigEntry.IPv6PoolStopAddr
 * IPv6PoolStopAddr is subid 4 of dot11DHCPIpv6PoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.7.1.4
 * Description:
IPV6 Address pool ending address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 2x:
 *
 * Ranges:  16;
 *
 * Its syntax is InetAddressIPv6 (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 16)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param IPv6PoolStopAddr_val_ptr
 *        A char containing the new value.
 * @param IPv6PoolStopAddr_val_ptr_len
 *        The size (in bytes) of the data pointed to by IPv6PoolStopAddr_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpv6PoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.IPv6PoolStopAddr).
 *    The length is in (one of) the range set(s):  16
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
IPv6PoolStopAddr_check_value( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, char *IPv6PoolStopAddr_val_ptr,  size_t IPv6PoolStopAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6PoolStopAddr_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != IPv6PoolStopAddr_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid IPv6PoolStopAddr value.
     */
    if(RS_NOTINSERVICE == rowreq_ctx->data.Ipv6DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.Ipv6DHCPRowStatus)
	return MFD_SUCCESS; 
    else
	    return MFD_ERROR;

} /* IPv6PoolStopAddr_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpv6PoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpv6PoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
IPv6PoolStopAddr_undo_setup( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6PoolStopAddr_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup IPv6PoolStopAddr undo.
     */
    /*
     * copy IPv6PoolStopAddr and IPv6PoolStopAddr_len data
     * set rowreq_ctx->undo->IPv6PoolStopAddr from rowreq_ctx->data.IPv6PoolStopAddr
     */
    memcpy( rowreq_ctx->undo->IPv6PoolStopAddr, rowreq_ctx->data.IPv6PoolStopAddr,
            (rowreq_ctx->data.IPv6PoolStopAddr_len * sizeof(rowreq_ctx->undo->IPv6PoolStopAddr[0])));
    rowreq_ctx->undo->IPv6PoolStopAddr_len = rowreq_ctx->data.IPv6PoolStopAddr_len;


    return MFD_SUCCESS;
} /* IPv6PoolStopAddr_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param IPv6PoolStopAddr_val_ptr
 *        A char containing the new value.
 * @param IPv6PoolStopAddr_val_ptr_len
 *        The size (in bytes) of the data pointed to by IPv6PoolStopAddr_val_ptr
 */
int
IPv6PoolStopAddr_set( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, char *IPv6PoolStopAddr_val_ptr,  size_t IPv6PoolStopAddr_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6PoolStopAddr_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != IPv6PoolStopAddr_val_ptr);

    /*
     * TODO:461:M: |-> Set IPv6PoolStopAddr value.
     * set IPv6PoolStopAddr value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.IPv6PoolStopAddr, IPv6PoolStopAddr_val_ptr, IPv6PoolStopAddr_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.IPv6PoolStopAddr_len = IPv6PoolStopAddr_val_ptr_len / sizeof(IPv6PoolStopAddr_val_ptr[0]);

    return MFD_SUCCESS;
} /* IPv6PoolStopAddr_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
IPv6PoolStopAddr_undo( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6PoolStopAddr_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up IPv6PoolStopAddr undo.
     */
    /*
     * copy IPv6PoolStopAddr and IPv6PoolStopAddr_len data
     * set rowreq_ctx->data.IPv6PoolStopAddr from rowreq_ctx->undo->IPv6PoolStopAddr
     */
    memcpy( rowreq_ctx->data.IPv6PoolStopAddr, rowreq_ctx->undo->IPv6PoolStopAddr,
            (rowreq_ctx->undo->IPv6PoolStopAddr_len * sizeof(rowreq_ctx->data.IPv6PoolStopAddr[0])));
    rowreq_ctx->data.IPv6PoolStopAddr_len = rowreq_ctx->undo->IPv6PoolStopAddr_len;

    
    return MFD_SUCCESS;
} /* IPv6PoolStopAddr_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpv6PoolConfigEntry.IPv6NetPrefixLen
 * IPv6NetPrefixLen is subid 5 of dot11DHCPIpv6PoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.7.1.5
 * Description:
Prefix length.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 128;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param IPv6NetPrefixLen_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpv6PoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 128
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
IPv6NetPrefixLen_check_value( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, long IPv6NetPrefixLen_val)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6NetPrefixLen_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid IPv6NetPrefixLen value.
     */
    if(RS_NOTINSERVICE == rowreq_ctx->data.Ipv6DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.Ipv6DHCPRowStatus)
	return MFD_SUCCESS; 
    else
	    return MFD_ERROR;

} /* IPv6NetPrefixLen_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpv6PoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpv6PoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
IPv6NetPrefixLen_undo_setup( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6NetPrefixLen_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup IPv6NetPrefixLen undo.
     */
    /*
     * copy IPv6NetPrefixLen data
     * set rowreq_ctx->undo->IPv6NetPrefixLen from rowreq_ctx->data.IPv6NetPrefixLen
     */
    rowreq_ctx->undo->IPv6NetPrefixLen = rowreq_ctx->data.IPv6NetPrefixLen;


    return MFD_SUCCESS;
} /* IPv6NetPrefixLen_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param IPv6NetPrefixLen_val
 *        A long containing the new value.
 */
int
IPv6NetPrefixLen_set( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, long IPv6NetPrefixLen_val )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6NetPrefixLen_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set IPv6NetPrefixLen value.
     * set IPv6NetPrefixLen value in rowreq_ctx->data
     */
    rowreq_ctx->data.IPv6NetPrefixLen = IPv6NetPrefixLen_val;

    return MFD_SUCCESS;
} /* IPv6NetPrefixLen_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
IPv6NetPrefixLen_undo( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:IPv6NetPrefixLen_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up IPv6NetPrefixLen undo.
     */
    /*
     * copy IPv6NetPrefixLen data
     * set rowreq_ctx->data.IPv6NetPrefixLen from rowreq_ctx->undo->IPv6NetPrefixLen
     */
    rowreq_ctx->data.IPv6NetPrefixLen = rowreq_ctx->undo->IPv6NetPrefixLen;

    
    return MFD_SUCCESS;
} /* IPv6NetPrefixLen_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpv6PoolConfigEntry.FirDNSServerIPv6Address
 * FirDNSServerIPv6Address is subid 6 of dot11DHCPIpv6PoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.7.1.6
 * Description:
IP Address first DNS IP Address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 2x:
 *
 * Ranges:  16;
 *
 * Its syntax is InetAddressIPv6 (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 16)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param FirDNSServerIPv6Address_val_ptr
 *        A char containing the new value.
 * @param FirDNSServerIPv6Address_val_ptr_len
 *        The size (in bytes) of the data pointed to by FirDNSServerIPv6Address_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpv6PoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.FirDNSServerIPv6Address).
 *    The length is in (one of) the range set(s):  16
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
FirDNSServerIPv6Address_check_value( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, char *FirDNSServerIPv6Address_val_ptr,  size_t FirDNSServerIPv6Address_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:FirDNSServerIPv6Address_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != FirDNSServerIPv6Address_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid FirDNSServerIPv6Address value.
     */
    if(RS_NOTINSERVICE == rowreq_ctx->data.Ipv6DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.Ipv6DHCPRowStatus)
	return MFD_SUCCESS; 
    else
	    return MFD_ERROR;

} /* FirDNSServerIPv6Address_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpv6PoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpv6PoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
FirDNSServerIPv6Address_undo_setup( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:FirDNSServerIPv6Address_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup FirDNSServerIPv6Address undo.
     */
    /*
     * copy FirDNSServerIPv6Address and FirDNSServerIPv6Address_len data
     * set rowreq_ctx->undo->FirDNSServerIPv6Address from rowreq_ctx->data.FirDNSServerIPv6Address
     */
    memcpy( rowreq_ctx->undo->FirDNSServerIPv6Address, rowreq_ctx->data.FirDNSServerIPv6Address,
            (rowreq_ctx->data.FirDNSServerIPv6Address_len * sizeof(rowreq_ctx->undo->FirDNSServerIPv6Address[0])));
    rowreq_ctx->undo->FirDNSServerIPv6Address_len = rowreq_ctx->data.FirDNSServerIPv6Address_len;


    return MFD_SUCCESS;
} /* FirDNSServerIPv6Address_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param FirDNSServerIPv6Address_val_ptr
 *        A char containing the new value.
 * @param FirDNSServerIPv6Address_val_ptr_len
 *        The size (in bytes) of the data pointed to by FirDNSServerIPv6Address_val_ptr
 */
int
FirDNSServerIPv6Address_set( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, char *FirDNSServerIPv6Address_val_ptr,  size_t FirDNSServerIPv6Address_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:FirDNSServerIPv6Address_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != FirDNSServerIPv6Address_val_ptr);

    /*
     * TODO:461:M: |-> Set FirDNSServerIPv6Address value.
     * set FirDNSServerIPv6Address value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.FirDNSServerIPv6Address, FirDNSServerIPv6Address_val_ptr, FirDNSServerIPv6Address_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.FirDNSServerIPv6Address_len = FirDNSServerIPv6Address_val_ptr_len / sizeof(FirDNSServerIPv6Address_val_ptr[0]);

    return MFD_SUCCESS;
} /* FirDNSServerIPv6Address_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
FirDNSServerIPv6Address_undo( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:FirDNSServerIPv6Address_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up FirDNSServerIPv6Address undo.
     */
    /*
     * copy FirDNSServerIPv6Address and FirDNSServerIPv6Address_len data
     * set rowreq_ctx->data.FirDNSServerIPv6Address from rowreq_ctx->undo->FirDNSServerIPv6Address
     */
    memcpy( rowreq_ctx->data.FirDNSServerIPv6Address, rowreq_ctx->undo->FirDNSServerIPv6Address,
            (rowreq_ctx->undo->FirDNSServerIPv6Address_len * sizeof(rowreq_ctx->data.FirDNSServerIPv6Address[0])));
    rowreq_ctx->data.FirDNSServerIPv6Address_len = rowreq_ctx->undo->FirDNSServerIPv6Address_len;

    
    return MFD_SUCCESS;
} /* FirDNSServerIPv6Address_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpv6PoolConfigEntry.SeconDNSServerIPv6Address
 * SeconDNSServerIPv6Address is subid 7 of dot11DHCPIpv6PoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.7.1.7
 * Description:
IP Address second DNS IP Address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 2x:
 *
 * Ranges:  16;
 *
 * Its syntax is InetAddressIPv6 (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 16)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param SeconDNSServerIPv6Address_val_ptr
 *        A char containing the new value.
 * @param SeconDNSServerIPv6Address_val_ptr_len
 *        The size (in bytes) of the data pointed to by SeconDNSServerIPv6Address_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpv6PoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.SeconDNSServerIPv6Address).
 *    The length is in (one of) the range set(s):  16
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
SeconDNSServerIPv6Address_check_value( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, char *SeconDNSServerIPv6Address_val_ptr,  size_t SeconDNSServerIPv6Address_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:SeconDNSServerIPv6Address_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != SeconDNSServerIPv6Address_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid SeconDNSServerIPv6Address value.
     */
    if(RS_NOTINSERVICE == rowreq_ctx->data.Ipv6DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.Ipv6DHCPRowStatus)
	return MFD_SUCCESS; 
    else
	    return MFD_ERROR;

} /* SeconDNSServerIPv6Address_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpv6PoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpv6PoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
SeconDNSServerIPv6Address_undo_setup( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:SeconDNSServerIPv6Address_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup SeconDNSServerIPv6Address undo.
     */
    /*
     * copy SeconDNSServerIPv6Address and SeconDNSServerIPv6Address_len data
     * set rowreq_ctx->undo->SeconDNSServerIPv6Address from rowreq_ctx->data.SeconDNSServerIPv6Address
     */
    memcpy( rowreq_ctx->undo->SeconDNSServerIPv6Address, rowreq_ctx->data.SeconDNSServerIPv6Address,
            (rowreq_ctx->data.SeconDNSServerIPv6Address_len * sizeof(rowreq_ctx->undo->SeconDNSServerIPv6Address[0])));
    rowreq_ctx->undo->SeconDNSServerIPv6Address_len = rowreq_ctx->data.SeconDNSServerIPv6Address_len;


    return MFD_SUCCESS;
} /* SeconDNSServerIPv6Address_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param SeconDNSServerIPv6Address_val_ptr
 *        A char containing the new value.
 * @param SeconDNSServerIPv6Address_val_ptr_len
 *        The size (in bytes) of the data pointed to by SeconDNSServerIPv6Address_val_ptr
 */
int
SeconDNSServerIPv6Address_set( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, char *SeconDNSServerIPv6Address_val_ptr,  size_t SeconDNSServerIPv6Address_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:SeconDNSServerIPv6Address_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != SeconDNSServerIPv6Address_val_ptr);

    /*
     * TODO:461:M: |-> Set SeconDNSServerIPv6Address value.
     * set SeconDNSServerIPv6Address value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.SeconDNSServerIPv6Address, SeconDNSServerIPv6Address_val_ptr, SeconDNSServerIPv6Address_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.SeconDNSServerIPv6Address_len = SeconDNSServerIPv6Address_val_ptr_len / sizeof(SeconDNSServerIPv6Address_val_ptr[0]);

    return MFD_SUCCESS;
} /* SeconDNSServerIPv6Address_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
SeconDNSServerIPv6Address_undo( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:SeconDNSServerIPv6Address_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up SeconDNSServerIPv6Address undo.
     */
    /*
     * copy SeconDNSServerIPv6Address and SeconDNSServerIPv6Address_len data
     * set rowreq_ctx->data.SeconDNSServerIPv6Address from rowreq_ctx->undo->SeconDNSServerIPv6Address
     */
    memcpy( rowreq_ctx->data.SeconDNSServerIPv6Address, rowreq_ctx->undo->SeconDNSServerIPv6Address,
            (rowreq_ctx->undo->SeconDNSServerIPv6Address_len * sizeof(rowreq_ctx->data.SeconDNSServerIPv6Address[0])));
    rowreq_ctx->data.SeconDNSServerIPv6Address_len = rowreq_ctx->undo->SeconDNSServerIPv6Address_len;

    
    return MFD_SUCCESS;
} /* SeconDNSServerIPv6Address_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpv6PoolConfigEntry.Ipv6LeasePeriod
 * Ipv6LeasePeriod is subid 8 of dot11DHCPIpv6PoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.7.1.8
 * Description:
IPv6 Address Lease Periods.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param Ipv6LeasePeriod_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpv6PoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_TIMETICKS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
Ipv6LeasePeriod_check_value( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, u_long Ipv6LeasePeriod_val)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6LeasePeriod_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid Ipv6LeasePeriod value.
     */
    if(RS_NOTINSERVICE == rowreq_ctx->data.Ipv6DHCPRowStatus || RS_NOTREADY == rowreq_ctx->data.Ipv6DHCPRowStatus)
	return MFD_SUCCESS; 
    else
	    return MFD_ERROR;

} /* Ipv6LeasePeriod_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpv6PoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpv6PoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
Ipv6LeasePeriod_undo_setup( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6LeasePeriod_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup Ipv6LeasePeriod undo.
     */
    /*
     * copy Ipv6LeasePeriod data
     * set rowreq_ctx->undo->Ipv6LeasePeriod from rowreq_ctx->data.Ipv6LeasePeriod
     */
    rowreq_ctx->undo->Ipv6LeasePeriod = rowreq_ctx->data.Ipv6LeasePeriod;


    return MFD_SUCCESS;
} /* Ipv6LeasePeriod_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param Ipv6LeasePeriod_val
 *        A u_long containing the new value.
 */
int
Ipv6LeasePeriod_set( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, u_long Ipv6LeasePeriod_val )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6LeasePeriod_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set Ipv6LeasePeriod value.
     * set Ipv6LeasePeriod value in rowreq_ctx->data
     */
    rowreq_ctx->data.Ipv6LeasePeriod = Ipv6LeasePeriod_val;

    return MFD_SUCCESS;
} /* Ipv6LeasePeriod_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
Ipv6LeasePeriod_undo( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6LeasePeriod_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up Ipv6LeasePeriod undo.
     */
    /*
     * copy Ipv6LeasePeriod data
     * set rowreq_ctx->data.Ipv6LeasePeriod from rowreq_ctx->undo->Ipv6LeasePeriod
     */
    rowreq_ctx->data.Ipv6LeasePeriod = rowreq_ctx->undo->Ipv6LeasePeriod;

    
    return MFD_SUCCESS;
} /* Ipv6LeasePeriod_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11DHCPIpv6PoolConfigEntry.Ipv6DHCPRowStatus
 * Ipv6DHCPRowStatus is subid 9 of dot11DHCPIpv6PoolConfigEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.7.1.9
 * Description:
This variable is used to create, modify, and/or delete a row
			        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param Ipv6DHCPRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11DHCPIpv6PoolConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
Ipv6DHCPRowStatus_check_value( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, u_long Ipv6DHCPRowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6DHCPRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid Ipv6DHCPRowStatus value.
     */

    return MFD_SUCCESS; /* Ipv6DHCPRowStatus value not illegal */
} /* Ipv6DHCPRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11DHCPIpv6PoolConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11DHCPIpv6PoolConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
Ipv6DHCPRowStatus_undo_setup( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6DHCPRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup Ipv6DHCPRowStatus undo.
     */
    /*
     * copy Ipv6DHCPRowStatus data
     * set rowreq_ctx->undo->Ipv6DHCPRowStatus from rowreq_ctx->data.Ipv6DHCPRowStatus
     */
    rowreq_ctx->undo->Ipv6DHCPRowStatus = rowreq_ctx->data.Ipv6DHCPRowStatus;


    return MFD_SUCCESS;
} /* Ipv6DHCPRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param Ipv6DHCPRowStatus_val
 *        A long containing the new value.
 */
int
Ipv6DHCPRowStatus_set( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx, u_long Ipv6DHCPRowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6DHCPRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set Ipv6DHCPRowStatus value.
     * set Ipv6DHCPRowStatus value in rowreq_ctx->data
     */
     int ret = MFD_SUCCESS;
    
    switch(Ipv6DHCPRowStatus_val) {
    
        case RS_ACTIVE:         
        case RS_DESTROY:
            rowreq_ctx->data.Ipv6DHCPRowStatus = Ipv6DHCPRowStatus_val;
            ret = MFD_SUCCESS;
            break;
    
        case RS_NOTINSERVICE:
        case RS_NOTREADY:
        case RS_CREATEANDGO:
            ret = MFD_ERROR;
            break;
    
        case RS_CREATEANDWAIT:
            if(RS_NONEXISTENT == rowreq_ctx->data.Ipv6DHCPRowStatus) 
	    {
                rowreq_ctx->data.Ipv6DHCPRowStatus = Ipv6DHCPRowStatus_val;
                rowreq_ctx->data.slot_id = (rowreq_ctx->tbl_idx.Ipv6DHCPID - 1) / WIRELESS_MAX_NUM + 1;
                ret = MFD_SUCCESS;
            }
            else 
	   {
                ret = MFD_ERROR;
            }
            break;
    
        default:
            snmp_log(LOG_WARNING, "unknow %s in DHCPRowStatus_set", Ipv6DHCPRowStatus_val);
            ret = MFD_ERROR;
            break;
    }
    return ret;
} /* Ipv6DHCPRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
Ipv6DHCPRowStatus_undo( dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11DHCPIpv6PoolConfigTable:Ipv6DHCPRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up Ipv6DHCPRowStatus undo.
     */
    /*
     * copy Ipv6DHCPRowStatus data
     * set rowreq_ctx->data.Ipv6DHCPRowStatus from rowreq_ctx->undo->Ipv6DHCPRowStatus
     */
    rowreq_ctx->data.Ipv6DHCPRowStatus = rowreq_ctx->undo->Ipv6DHCPRowStatus;

    
    return MFD_SUCCESS;
} /* Ipv6DHCPRowStatus_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11DHCPIpv6PoolConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11DHCPIpv6PoolConfigTable if you don't have dependencies)
 */
int
dot11DHCPIpv6PoolConfigTable_check_dependencies(dot11DHCPIpv6PoolConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11DHCPIpv6PoolConfigTable:dot11DHCPIpv6PoolConfigTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11DHCPIpv6PoolConfigTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & COLUMN_IPV6DHCPROWSTATUS_FLAG) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->Ipv6DHCPRowStatus,
                                         rowreq_ctx->data.Ipv6DHCPRowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_DESTROY ==  rowreq_ctx->data.Ipv6DHCPRowStatus) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;
            }
            else if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.Ipv6DHCPRowStatus) {
                if ((rowreq_ctx->column_set_flags & DOT11DHCPIPV6POOLCONFIGTABLE_REQUIRED_COLS)
                    != DOT11DHCPIPV6POOLCONFIGTABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11DHCPIpv6PoolConfigTable",
                                "required columns missing (0x%0x != 0x%0x)\n",
                                rowreq_ctx->column_set_flags, DOT11DHCPIPV6POOLCONFIGTABLE_REQUIRED_COLS));
                    return MFD_CANNOT_CREATE_NOW;
                }
                rowreq_ctx->data.Ipv6DHCPRowStatus = ROWSTATUS_ACTIVE;
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.Ipv6DHCPRowStatus) {
                if (rowreq_ctx->column_set_flags & ~COLUMN_IPV6DHCPROWSTATUS_FLAG) {
                    DEBUGMSGTL(("dot11DHCPIpv6PoolConfigTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;

            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11DHCPIpv6PoolConfigTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11DHCPIpv6PoolConfigTable_check_dependencies */

/** @} */
