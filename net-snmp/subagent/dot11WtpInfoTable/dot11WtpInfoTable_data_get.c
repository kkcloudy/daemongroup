/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpInfoTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpInfoTable get routines.
 * TODO:240:M: Implement dot11WtpInfoTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpInfoTable is subid 1 of wtpGeneralInfo.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpInfoTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11WtpInfoTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpInfoTable_indexes_set_tbl_idx(dot11WtpInfoTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:dot11WtpInfoTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11WtpInfoTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpInfoTable_indexes_set(dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:dot11WtpInfoTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpInfoTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpInfoTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpInfoTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpDevName
 * wtpDevName is subid 2 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.2
 * Description:
AP device's name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpDevName.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpDevName_map(char **mib_wtpDevName_val_ptr_ptr, size_t *mib_wtpDevName_val_ptr_len_ptr, char *raw_wtpDevName_val_ptr, size_t raw_wtpDevName_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpDevName_val_ptr);
    netsnmp_assert((NULL != mib_wtpDevName_val_ptr_ptr) && (NULL != mib_wtpDevName_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpDevName_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpDevName non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpDevName_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpDevName_val_ptr_ptr) || (*mib_wtpDevName_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpDevName_val_ptr_ptr = realloc( *mib_wtpDevName_val_ptr_ptr, converted_len * sizeof(**mib_wtpDevName_val_ptr_ptr));
        if(NULL == *mib_wtpDevName_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpDevName_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpDevName_val_ptr_ptr, raw_wtpDevName_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpDevName_map */

/**
 * Extract the current value of the wtpDevName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpDevName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpDevName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpDevName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpDevName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpDevName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpDevName_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char **wtpDevName_val_ptr_ptr, size_t *wtpDevName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpDevName_val_ptr_ptr) && (NULL != *wtpDevName_val_ptr_ptr));
   netsnmp_assert( NULL != wtpDevName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpDevName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpDevName data.
 * set (* wtpDevName_val_ptr_ptr ) and (* wtpDevName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpDevName data
     */
    if ((NULL == (* wtpDevName_val_ptr_ptr )) ||
        ((* wtpDevName_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpDevName_len * sizeof((* wtpDevName_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpDevName data
         */
        (* wtpDevName_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpDevName_len * sizeof((* wtpDevName_val_ptr_ptr )[0]));
        if(NULL == (* wtpDevName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpDevName_val_ptr_len_ptr ) = rowreq_ctx->data.wtpDevName_len * sizeof((* wtpDevName_val_ptr_ptr )[0]);
    memcpy( (* wtpDevName_val_ptr_ptr ), rowreq_ctx->data.wtpDevName, (* wtpDevName_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpDevName_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpPosInfo
 * wtpPosInfo is subid 3 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.3
 * Description:
The location of the AP device.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 15)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpPosInfo.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpPosInfo_map(char **mib_wtpPosInfo_val_ptr_ptr, size_t *mib_wtpPosInfo_val_ptr_len_ptr, char *raw_wtpPosInfo_val_ptr, size_t raw_wtpPosInfo_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpPosInfo_val_ptr);
    netsnmp_assert((NULL != mib_wtpPosInfo_val_ptr_ptr) && (NULL != mib_wtpPosInfo_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpPosInfo_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpPosInfo non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpPosInfo_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpPosInfo_val_ptr_ptr) || (*mib_wtpPosInfo_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpPosInfo_val_ptr_ptr = realloc( *mib_wtpPosInfo_val_ptr_ptr, converted_len * sizeof(**mib_wtpPosInfo_val_ptr_ptr));
        if(NULL == *mib_wtpPosInfo_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpPosInfo_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpPosInfo_val_ptr_ptr, raw_wtpPosInfo_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpPosInfo_map */

/**
 * Extract the current value of the wtpPosInfo data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpPosInfo_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpPosInfo_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpPosInfo.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpPosInfo_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpPosInfo_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpPosInfo_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char **wtpPosInfo_val_ptr_ptr, size_t *wtpPosInfo_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpPosInfo_val_ptr_ptr) && (NULL != *wtpPosInfo_val_ptr_ptr));
   netsnmp_assert( NULL != wtpPosInfo_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpPosInfo_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpPosInfo data.
 * set (* wtpPosInfo_val_ptr_ptr ) and (* wtpPosInfo_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpPosInfo data
     */
    if ((NULL == (* wtpPosInfo_val_ptr_ptr )) ||
        ((* wtpPosInfo_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpPosInfo_len * sizeof((* wtpPosInfo_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpPosInfo data
         */
        (* wtpPosInfo_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpPosInfo_len * sizeof((* wtpPosInfo_val_ptr_ptr )[0]));
        if(NULL == (* wtpPosInfo_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpPosInfo_val_ptr_len_ptr ) = rowreq_ctx->data.wtpPosInfo_len * sizeof((* wtpPosInfo_val_ptr_ptr )[0]);
    memcpy( (* wtpPosInfo_val_ptr_ptr ), rowreq_ctx->data.wtpPosInfo, (* wtpPosInfo_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpPosInfo_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpProduct
 * wtpProduct is subid 4 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.4
 * Description:
The AP device's manufacturer.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpProduct.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpProduct_map(char **mib_wtpProduct_val_ptr_ptr, size_t *mib_wtpProduct_val_ptr_len_ptr, char *raw_wtpProduct_val_ptr, size_t raw_wtpProduct_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpProduct_val_ptr);
    netsnmp_assert((NULL != mib_wtpProduct_val_ptr_ptr) && (NULL != mib_wtpProduct_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpProduct_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpProduct non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpProduct_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpProduct_val_ptr_ptr) || (*mib_wtpProduct_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpProduct_val_ptr_ptr = realloc( *mib_wtpProduct_val_ptr_ptr, converted_len * sizeof(**mib_wtpProduct_val_ptr_ptr));
        if(NULL == *mib_wtpProduct_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpProduct_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpProduct_val_ptr_ptr, raw_wtpProduct_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpProduct_map */

/**
 * Extract the current value of the wtpProduct data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpProduct_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpProduct_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpProduct.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpProduct_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpProduct_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpProduct_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char **wtpProduct_val_ptr_ptr, size_t *wtpProduct_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpProduct_val_ptr_ptr) && (NULL != *wtpProduct_val_ptr_ptr));
   netsnmp_assert( NULL != wtpProduct_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpProduct_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpProduct data.
 * set (* wtpProduct_val_ptr_ptr ) and (* wtpProduct_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpProduct data
     */
    if ((NULL == (* wtpProduct_val_ptr_ptr )) ||
        ((* wtpProduct_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpProduct_len * sizeof((* wtpProduct_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpProduct data
         */
        (* wtpProduct_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpProduct_len * sizeof((* wtpProduct_val_ptr_ptr )[0]));
        if(NULL == (* wtpProduct_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpProduct_val_ptr_len_ptr ) = rowreq_ctx->data.wtpProduct_len * sizeof((* wtpProduct_val_ptr_ptr )[0]);
    memcpy( (* wtpProduct_val_ptr_ptr ), rowreq_ctx->data.wtpProduct, (* wtpProduct_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpProduct_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpDevTypeNum
 * wtpDevTypeNum is subid 5 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.5
 * Description:
The AP device type number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpDevTypeNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpDevTypeNum_map(char **mib_wtpDevTypeNum_val_ptr_ptr, size_t *mib_wtpDevTypeNum_val_ptr_len_ptr, char *raw_wtpDevTypeNum_val_ptr, size_t raw_wtpDevTypeNum_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpDevTypeNum_val_ptr);
    netsnmp_assert((NULL != mib_wtpDevTypeNum_val_ptr_ptr) && (NULL != mib_wtpDevTypeNum_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpDevTypeNum_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpDevTypeNum non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpDevTypeNum_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpDevTypeNum_val_ptr_ptr) || (*mib_wtpDevTypeNum_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpDevTypeNum_val_ptr_ptr = realloc( *mib_wtpDevTypeNum_val_ptr_ptr, converted_len * sizeof(**mib_wtpDevTypeNum_val_ptr_ptr));
        if(NULL == *mib_wtpDevTypeNum_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpDevTypeNum_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpDevTypeNum_val_ptr_ptr, raw_wtpDevTypeNum_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpDevTypeNum_map */

/**
 * Extract the current value of the wtpDevTypeNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpDevTypeNum_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpDevTypeNum_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpDevTypeNum.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpDevTypeNum_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpDevTypeNum_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpDevTypeNum_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char **wtpDevTypeNum_val_ptr_ptr, size_t *wtpDevTypeNum_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpDevTypeNum_val_ptr_ptr) && (NULL != *wtpDevTypeNum_val_ptr_ptr));
   netsnmp_assert( NULL != wtpDevTypeNum_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpDevTypeNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpDevTypeNum data.
 * set (* wtpDevTypeNum_val_ptr_ptr ) and (* wtpDevTypeNum_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpDevTypeNum data
     */
    if ((NULL == (* wtpDevTypeNum_val_ptr_ptr )) ||
        ((* wtpDevTypeNum_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpDevTypeNum_len * sizeof((* wtpDevTypeNum_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpDevTypeNum data
         */
        (* wtpDevTypeNum_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpDevTypeNum_len * sizeof((* wtpDevTypeNum_val_ptr_ptr )[0]));
        if(NULL == (* wtpDevTypeNum_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpDevTypeNum_val_ptr_len_ptr ) = rowreq_ctx->data.wtpDevTypeNum_len * sizeof((* wtpDevTypeNum_val_ptr_ptr )[0]);
    memcpy( (* wtpDevTypeNum_val_ptr_ptr ), rowreq_ctx->data.wtpDevTypeNum, (* wtpDevTypeNum_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpDevTypeNum_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpSoftVersionInfo
 * wtpSoftVersionInfo is subid 6 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.6
 * Description:
The software version of the AP device.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSoftVersionInfo.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSoftVersionInfo_map(char **mib_wtpSoftVersionInfo_val_ptr_ptr, size_t *mib_wtpSoftVersionInfo_val_ptr_len_ptr, char *raw_wtpSoftVersionInfo_val_ptr, size_t raw_wtpSoftVersionInfo_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpSoftVersionInfo_val_ptr);
    netsnmp_assert((NULL != mib_wtpSoftVersionInfo_val_ptr_ptr) && (NULL != mib_wtpSoftVersionInfo_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSoftVersionInfo_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpSoftVersionInfo non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpSoftVersionInfo_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpSoftVersionInfo_val_ptr_ptr) || (*mib_wtpSoftVersionInfo_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpSoftVersionInfo_val_ptr_ptr = realloc( *mib_wtpSoftVersionInfo_val_ptr_ptr, converted_len * sizeof(**mib_wtpSoftVersionInfo_val_ptr_ptr));
        if(NULL == *mib_wtpSoftVersionInfo_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpSoftVersionInfo_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpSoftVersionInfo_val_ptr_ptr, raw_wtpSoftVersionInfo_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpSoftVersionInfo_map */

/**
 * Extract the current value of the wtpSoftVersionInfo data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSoftVersionInfo_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpSoftVersionInfo_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpSoftVersionInfo.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpSoftVersionInfo_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpSoftVersionInfo_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpSoftVersionInfo_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char **wtpSoftVersionInfo_val_ptr_ptr, size_t *wtpSoftVersionInfo_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpSoftVersionInfo_val_ptr_ptr) && (NULL != *wtpSoftVersionInfo_val_ptr_ptr));
   netsnmp_assert( NULL != wtpSoftVersionInfo_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSoftVersionInfo_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSoftVersionInfo data.
 * set (* wtpSoftVersionInfo_val_ptr_ptr ) and (* wtpSoftVersionInfo_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpSoftVersionInfo data
     */
    if ((NULL == (* wtpSoftVersionInfo_val_ptr_ptr )) ||
        ((* wtpSoftVersionInfo_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpSoftVersionInfo_len * sizeof((* wtpSoftVersionInfo_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpSoftVersionInfo data
         */
        (* wtpSoftVersionInfo_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpSoftVersionInfo_len * sizeof((* wtpSoftVersionInfo_val_ptr_ptr )[0]));
        if(NULL == (* wtpSoftVersionInfo_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpSoftVersionInfo_val_ptr_len_ptr ) = rowreq_ctx->data.wtpSoftVersionInfo_len * sizeof((* wtpSoftVersionInfo_val_ptr_ptr )[0]);
    memcpy( (* wtpSoftVersionInfo_val_ptr_ptr ), rowreq_ctx->data.wtpSoftVersionInfo, (* wtpSoftVersionInfo_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpSoftVersionInfo_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpUpTime
 * wtpUpTime is subid 7 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.7
 * Description:
The runtime of AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpUpTime.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpUpTime_map(u_long *mib_wtpUpTime_val_ptr, u_long raw_wtpUpTime_val)
{
    netsnmp_assert(NULL != mib_wtpUpTime_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpUpTime_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpUpTime mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpUpTime_val_ptr) = raw_wtpUpTime_val;

    return MFD_SUCCESS;
} /* wtpUpTime_map */

/**
 * Extract the current value of the wtpUpTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUpTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpUpTime_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpUpTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpUpTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpUpTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpUpTime data.
 * set (* wtpUpTime_val_ptr ) from rowreq_ctx->data
 */
    (* wtpUpTime_val_ptr ) = rowreq_ctx->data.wtpUpTime;

    return MFD_SUCCESS;
} /* wtpUpTime_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpOnlineTime
 * wtpOnlineTime is subid 8 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.8
 * Description:
Length of time on-line.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpOnlineTime.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpOnlineTime_map(u_long *mib_wtpOnlineTime_val_ptr, u_long raw_wtpOnlineTime_val)
{
    netsnmp_assert(NULL != mib_wtpOnlineTime_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpOnlineTime_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpOnlineTime mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpOnlineTime_val_ptr) = raw_wtpOnlineTime_val;

    return MFD_SUCCESS;
} /* wtpOnlineTime_map */

/**
 * Extract the current value of the wtpOnlineTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpOnlineTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpOnlineTime_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpOnlineTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpOnlineTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpOnlineTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpOnlineTime data.
 * set (* wtpOnlineTime_val_ptr ) from rowreq_ctx->data
 */
    (* wtpOnlineTime_val_ptr ) = rowreq_ctx->data.wtpOnlineTime;

    return MFD_SUCCESS;
} /* wtpOnlineTime_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpSysSoftName
 * wtpSysSoftName is subid 9 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.9
 * Description:
The system software name or type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSysSoftName.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSysSoftName_map(char **mib_wtpSysSoftName_val_ptr_ptr, size_t *mib_wtpSysSoftName_val_ptr_len_ptr, char *raw_wtpSysSoftName_val_ptr, size_t raw_wtpSysSoftName_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpSysSoftName_val_ptr);
    netsnmp_assert((NULL != mib_wtpSysSoftName_val_ptr_ptr) && (NULL != mib_wtpSysSoftName_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysSoftName_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpSysSoftName non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpSysSoftName_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpSysSoftName_val_ptr_ptr) || (*mib_wtpSysSoftName_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpSysSoftName_val_ptr_ptr = realloc( *mib_wtpSysSoftName_val_ptr_ptr, converted_len * sizeof(**mib_wtpSysSoftName_val_ptr_ptr));
        if(NULL == *mib_wtpSysSoftName_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpSysSoftName_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpSysSoftName_val_ptr_ptr, raw_wtpSysSoftName_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpSysSoftName_map */

/**
 * Extract the current value of the wtpSysSoftName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSysSoftName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpSysSoftName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpSysSoftName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpSysSoftName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpSysSoftName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpSysSoftName_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char **wtpSysSoftName_val_ptr_ptr, size_t *wtpSysSoftName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpSysSoftName_val_ptr_ptr) && (NULL != *wtpSysSoftName_val_ptr_ptr));
   netsnmp_assert( NULL != wtpSysSoftName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysSoftName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSysSoftName data.
 * set (* wtpSysSoftName_val_ptr_ptr ) and (* wtpSysSoftName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpSysSoftName data
     */
    if ((NULL == (* wtpSysSoftName_val_ptr_ptr )) ||
        ((* wtpSysSoftName_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpSysSoftName_len * sizeof((* wtpSysSoftName_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpSysSoftName data
         */
        (* wtpSysSoftName_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpSysSoftName_len * sizeof((* wtpSysSoftName_val_ptr_ptr )[0]));
        if(NULL == (* wtpSysSoftName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpSysSoftName_val_ptr_len_ptr ) = rowreq_ctx->data.wtpSysSoftName_len * sizeof((* wtpSysSoftName_val_ptr_ptr )[0]);
    memcpy( (* wtpSysSoftName_val_ptr_ptr ), rowreq_ctx->data.wtpSysSoftName, (* wtpSysSoftName_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpSysSoftName_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpSysHWVersion
 * wtpSysHWVersion is subid 10 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.10
 * Description:
The system hardware version number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSysHWVersion.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSysHWVersion_map(char **mib_wtpSysHWVersion_val_ptr_ptr, size_t *mib_wtpSysHWVersion_val_ptr_len_ptr, char *raw_wtpSysHWVersion_val_ptr, size_t raw_wtpSysHWVersion_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpSysHWVersion_val_ptr);
    netsnmp_assert((NULL != mib_wtpSysHWVersion_val_ptr_ptr) && (NULL != mib_wtpSysHWVersion_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysHWVersion_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpSysHWVersion non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpSysHWVersion_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpSysHWVersion_val_ptr_ptr) || (*mib_wtpSysHWVersion_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpSysHWVersion_val_ptr_ptr = realloc( *mib_wtpSysHWVersion_val_ptr_ptr, converted_len * sizeof(**mib_wtpSysHWVersion_val_ptr_ptr));
        if(NULL == *mib_wtpSysHWVersion_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpSysHWVersion_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpSysHWVersion_val_ptr_ptr, raw_wtpSysHWVersion_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpSysHWVersion_map */

/**
 * Extract the current value of the wtpSysHWVersion data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSysHWVersion_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpSysHWVersion_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpSysHWVersion.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpSysHWVersion_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpSysHWVersion_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpSysHWVersion_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char **wtpSysHWVersion_val_ptr_ptr, size_t *wtpSysHWVersion_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpSysHWVersion_val_ptr_ptr) && (NULL != *wtpSysHWVersion_val_ptr_ptr));
   netsnmp_assert( NULL != wtpSysHWVersion_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysHWVersion_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSysHWVersion data.
 * set (* wtpSysHWVersion_val_ptr_ptr ) and (* wtpSysHWVersion_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpSysHWVersion data
     */
    if ((NULL == (* wtpSysHWVersion_val_ptr_ptr )) ||
        ((* wtpSysHWVersion_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpSysHWVersion_len * sizeof((* wtpSysHWVersion_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpSysHWVersion data
         */
        (* wtpSysHWVersion_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpSysHWVersion_len * sizeof((* wtpSysHWVersion_val_ptr_ptr )[0]));
        if(NULL == (* wtpSysHWVersion_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpSysHWVersion_val_ptr_len_ptr ) = rowreq_ctx->data.wtpSysHWVersion_len * sizeof((* wtpSysHWVersion_val_ptr_ptr )[0]);
    memcpy( (* wtpSysHWVersion_val_ptr_ptr ), rowreq_ctx->data.wtpSysHWVersion, (* wtpSysHWVersion_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpSysHWVersion_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpSN
 * wtpSN is subid 11 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.11
 * Description:
The system serial-number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSN.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSN_map(char **mib_wtpSN_val_ptr_ptr, size_t *mib_wtpSN_val_ptr_len_ptr, char *raw_wtpSN_val_ptr, size_t raw_wtpSN_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpSN_val_ptr);
    netsnmp_assert((NULL != mib_wtpSN_val_ptr_ptr) && (NULL != mib_wtpSN_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSN_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpSN non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpSN_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpSN_val_ptr_ptr) || (*mib_wtpSN_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpSN_val_ptr_ptr = realloc( *mib_wtpSN_val_ptr_ptr, converted_len * sizeof(**mib_wtpSN_val_ptr_ptr));
        if(NULL == *mib_wtpSN_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpSN_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpSN_val_ptr_ptr, raw_wtpSN_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpSN_map */

/**
 * Extract the current value of the wtpSN data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSN_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpSN_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpSN.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpSN_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpSN_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpSN_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char **wtpSN_val_ptr_ptr, size_t *wtpSN_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpSN_val_ptr_ptr) && (NULL != *wtpSN_val_ptr_ptr));
   netsnmp_assert( NULL != wtpSN_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSN_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSN data.
 * set (* wtpSN_val_ptr_ptr ) and (* wtpSN_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpSN data
     */
    if ((NULL == (* wtpSN_val_ptr_ptr )) ||
        ((* wtpSN_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpSN_len * sizeof((* wtpSN_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpSN data
         */
        (* wtpSN_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpSN_len * sizeof((* wtpSN_val_ptr_ptr )[0]));
        if(NULL == (* wtpSN_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpSN_val_ptr_len_ptr ) = rowreq_ctx->data.wtpSN_len * sizeof((* wtpSN_val_ptr_ptr )[0]);
    memcpy( (* wtpSN_val_ptr_ptr ), rowreq_ctx->data.wtpSN, (* wtpSN_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpSN_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpSysSoftProductor
 * wtpSysSoftProductor is subid 12 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.12
 * Description:
The system software manufacturer.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSysSoftProductor.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSysSoftProductor_map(char **mib_wtpSysSoftProductor_val_ptr_ptr, size_t *mib_wtpSysSoftProductor_val_ptr_len_ptr, char *raw_wtpSysSoftProductor_val_ptr, size_t raw_wtpSysSoftProductor_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpSysSoftProductor_val_ptr);
    netsnmp_assert((NULL != mib_wtpSysSoftProductor_val_ptr_ptr) && (NULL != mib_wtpSysSoftProductor_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysSoftProductor_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpSysSoftProductor non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpSysSoftProductor_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpSysSoftProductor_val_ptr_ptr) || (*mib_wtpSysSoftProductor_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpSysSoftProductor_val_ptr_ptr = realloc( *mib_wtpSysSoftProductor_val_ptr_ptr, converted_len * sizeof(**mib_wtpSysSoftProductor_val_ptr_ptr));
        if(NULL == *mib_wtpSysSoftProductor_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpSysSoftProductor_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpSysSoftProductor_val_ptr_ptr, raw_wtpSysSoftProductor_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpSysSoftProductor_map */

/**
 * Extract the current value of the wtpSysSoftProductor data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSysSoftProductor_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpSysSoftProductor_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpSysSoftProductor.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpSysSoftProductor_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpSysSoftProductor_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpSysSoftProductor_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char **wtpSysSoftProductor_val_ptr_ptr, size_t *wtpSysSoftProductor_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpSysSoftProductor_val_ptr_ptr) && (NULL != *wtpSysSoftProductor_val_ptr_ptr));
   netsnmp_assert( NULL != wtpSysSoftProductor_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysSoftProductor_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSysSoftProductor data.
 * set (* wtpSysSoftProductor_val_ptr_ptr ) and (* wtpSysSoftProductor_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpSysSoftProductor data
     */
    if ((NULL == (* wtpSysSoftProductor_val_ptr_ptr )) ||
        ((* wtpSysSoftProductor_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpSysSoftProductor_len * sizeof((* wtpSysSoftProductor_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpSysSoftProductor data
         */
        (* wtpSysSoftProductor_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpSysSoftProductor_len * sizeof((* wtpSysSoftProductor_val_ptr_ptr )[0]));
        if(NULL == (* wtpSysSoftProductor_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpSysSoftProductor_val_ptr_len_ptr ) = rowreq_ctx->data.wtpSysSoftProductor_len * sizeof((* wtpSysSoftProductor_val_ptr_ptr )[0]);
    memcpy( (* wtpSysSoftProductor_val_ptr_ptr ), rowreq_ctx->data.wtpSysSoftProductor, (* wtpSysSoftProductor_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpSysSoftProductor_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpDomain
 * wtpDomain is subid 13 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.13
 * Description:
The country or district the device support.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpDomain.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpDomain_map(char **mib_wtpDomain_val_ptr_ptr, size_t *mib_wtpDomain_val_ptr_len_ptr, char *raw_wtpDomain_val_ptr, size_t raw_wtpDomain_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpDomain_val_ptr);
    netsnmp_assert((NULL != mib_wtpDomain_val_ptr_ptr) && (NULL != mib_wtpDomain_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpDomain_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpDomain non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpDomain_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpDomain_val_ptr_ptr) || (*mib_wtpDomain_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpDomain_val_ptr_ptr = realloc( *mib_wtpDomain_val_ptr_ptr, converted_len * sizeof(**mib_wtpDomain_val_ptr_ptr));
        if(NULL == *mib_wtpDomain_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpDomain_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpDomain_val_ptr_ptr, raw_wtpDomain_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpDomain_map */

/**
 * Extract the current value of the wtpDomain data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpDomain_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpDomain_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpDomain.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpDomain_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpDomain_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpDomain_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char **wtpDomain_val_ptr_ptr, size_t *wtpDomain_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpDomain_val_ptr_ptr) && (NULL != *wtpDomain_val_ptr_ptr));
   netsnmp_assert( NULL != wtpDomain_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpDomain_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpDomain data.
 * set (* wtpDomain_val_ptr_ptr ) and (* wtpDomain_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpDomain data
     */
    if ((NULL == (* wtpDomain_val_ptr_ptr )) ||
        ((* wtpDomain_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpDomain_len * sizeof((* wtpDomain_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpDomain data
         */
        (* wtpDomain_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpDomain_len * sizeof((* wtpDomain_val_ptr_ptr )[0]));
        if(NULL == (* wtpDomain_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpDomain_val_ptr_len_ptr ) = rowreq_ctx->data.wtpDomain_len * sizeof((* wtpDomain_val_ptr_ptr )[0]);
    memcpy( (* wtpDomain_val_ptr_ptr ), rowreq_ctx->data.wtpDomain, (* wtpDomain_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpDomain_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpSysRestart
 * wtpSysRestart is subid 14 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.14
 * Description:
Restart AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  Normal(1), Restart(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSysRestart.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSysRestart_map(u_long *mib_wtpSysRestart_val_ptr, u_long raw_wtpSysRestart_val)
{
    netsnmp_assert(NULL != mib_wtpSysRestart_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysRestart_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSysRestart enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpSysRestart_val) {
        case INTERNAL_WTPSYSRESTART_NORMAL:
             *mib_wtpSysRestart_val_ptr = WTPSYSRESTART_NORMAL;
             break;

        case INTERNAL_WTPSYSRESTART_RESTART:
             *mib_wtpSysRestart_val_ptr = WTPSYSRESTART_RESTART;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpSysRestart\n", raw_wtpSysRestart_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpSysRestart_map */

/**
 * Extract the current value of the wtpSysRestart data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSysRestart_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSysRestart_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpSysRestart_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSysRestart_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysRestart_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSysRestart data.
 * set (* wtpSysRestart_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSysRestart_val_ptr ) = rowreq_ctx->data.wtpSysRestart;

    return MFD_SUCCESS;
} /* wtpSysRestart_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpSysReset
 * wtpSysReset is subid 15 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.15
 * Description:
Reset AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 1/8. Values:  Normal(1), Reset(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSysReset.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSysReset_map(u_long *mib_wtpSysReset_val_ptr, u_long raw_wtpSysReset_val)
{
    netsnmp_assert(NULL != mib_wtpSysReset_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysReset_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSysReset enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpSysReset_val) {
        case INTERNAL_WTPSYSRESET_NORMAL:
             *mib_wtpSysReset_val_ptr = WTPSYSRESET_NORMAL;
             break;

        case INTERNAL_WTPSYSRESET_RESET:
             *mib_wtpSysReset_val_ptr = WTPSYSRESET_RESET;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpSysReset\n", raw_wtpSysReset_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpSysReset_map */

/**
 * Extract the current value of the wtpSysReset data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSysReset_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSysReset_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpSysReset_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSysReset_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysReset_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSysReset data.
 * set (* wtpSysReset_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSysReset_val_ptr ) = rowreq_ctx->data.wtpSysReset;

    return MFD_SUCCESS;
} /* wtpSysReset_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpTrapDesIPAddress
 * wtpTrapDesIPAddress is subid 16 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.16
 * Description:
The target IP of net-management broadcast information.£¨AP£©
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTrapDesIPAddress.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpColdReboot_map(u_long *mib_wtpColdReboot_val_ptr, u_long raw_wtpColdReboot_val)
{
    netsnmp_assert(NULL != mib_wtpColdReboot_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpColdReboot_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpColdReboot enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpColdReboot_val) {
        case INTERNAL_WTPCOLDREBOOT_REBOOT:
             *mib_wtpColdReboot_val_ptr = WTPCOLDREBOOT_REBOOT;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpColdReboot\n", raw_wtpColdReboot_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpColdReboot_map */

/**
 * Extract the current value of the wtpColdReboot data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpColdReboot_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpColdReboot_get( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpColdReboot_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpColdReboot_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpColdReboot_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpColdReboot data.
 * set (* wtpColdReboot_val_ptr ) from rowreq_ctx->data
 */
    (* wtpColdReboot_val_ptr ) = rowreq_ctx->data.wtpColdReboot;

    return MFD_SUCCESS;
} /* wtpColdReboot_get */



/** @} */
