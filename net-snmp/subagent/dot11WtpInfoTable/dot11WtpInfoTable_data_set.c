/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpInfoTable.h"

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpInfoTable is subid 1 of wtpGeneralInfo.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11WtpInfoTable_undo_setup( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:dot11WtpInfoTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11WtpInfoTable undo.
     * set up dot11WtpInfoTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11WtpInfoTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpInfoTable_undo_cleanup( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:dot11WtpInfoTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11WtpInfoTable undo.
     */

    return rc;
} /* dot11WtpInfoTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpInfoTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpInfoTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpInfoTable_commit( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:dot11WtpInfoTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11WtpInfoTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WTPDEVNAME) {
       save_flags &= ~FLAG_WTPDEVNAME; /* clear wtpDevName */
       /*
        * TODO:482:o: |-> commit column wtpDevName.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpInfoTable column wtpDevName commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpDevName
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPDEVNAME;
       }
    }

    if (save_flags & FLAG_WTPPOSINFO) {
       save_flags &= ~FLAG_WTPPOSINFO; /* clear wtpPosInfo */
       /*
        * TODO:482:o: |-> commit column wtpPosInfo.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpInfoTable column wtpPosInfo commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpPosInfo
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPPOSINFO;
       }
    }

    if (save_flags & FLAG_WTPSYSRESTART) {
       save_flags &= ~FLAG_WTPSYSRESTART; /* clear wtpSysRestart */
       /*
        * TODO:482:o: |-> commit column wtpSysRestart.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpInfoTable column wtpSysRestart commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpSysRestart
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPSYSRESTART;
       }
    }

    if (save_flags & FLAG_WTPSYSRESET) {
       save_flags &= ~FLAG_WTPSYSRESET; /* clear wtpSysReset */
       /*
        * TODO:482:o: |-> commit column wtpSysReset.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpInfoTable column wtpSysReset commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpSysReset
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPSYSRESET;
       }
    }

    if (save_flags & FLAG_WTPCOLDREBOOT) {
       save_flags &= ~FLAG_WTPCOLDREBOOT; /* clear wtpColdReboot */
       /*
        * TODO:482:o: |-> commit column wtpColdReboot.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpInfoTable column wtpColdReboot commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpColdReboot
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPCOLDREBOOT;
       }
    }

    if (save_flags & COLUMN_WTPLONGITUDE_FLAG) {
       save_flags &= ~COLUMN_WTPLONGITUDE_FLAG; /* clear wtplongitude */
       /*
        * TODO:482:o: |-> commit column wtplongitude.
        */
 //      rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpInfoTable column wtplongitude commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtplongitude
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPLONGITUDE_FLAG;
       }
    }

    if (save_flags & COLUMN_WTPLATITUDE_FLAG) {
       save_flags &= ~COLUMN_WTPLATITUDE_FLAG; /* clear wtplatitude */
       /*
        * TODO:482:o: |-> commit column wtplatitude.
        */
//       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpInfoTable column wtplatitude commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtplatitude
             */
            rowreq_ctx->column_set_flags |= COLUMN_WTPLATITUDE_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11WtpInfoTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpInfoTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpInfoTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpInfoTable_undo_commit( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:dot11WtpInfoTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11WtpInfoTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11WtpInfoTable_undo_commit */

/*
 * TODO:420:r: Implement dot11WtpInfoTable index validation.
 */
/*
 * TODO:440:M: Implement dot11WtpInfoTable node value checks.
 * TODO:450:M: Implement dot11WtpInfoTable undo functions.
 * TODO:460:M: Implement dot11WtpInfoTable set functions.
 * TODO:480:M: Implement dot11WtpInfoTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpDevName
 * wtpDevName is subid 2 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.2
 * Description:
AP device's name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpDevName_val_ptr
 *        A char containing the new value.
 * @param wtpDevName_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpDevName_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpDevName_check_value( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char *wtpDevName_val_ptr,  size_t wtpDevName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpDevName_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpDevName_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wtpDevName value.
     */

    return MFD_SUCCESS; /* wtpDevName value not illegal */
} /* wtpDevName_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpDevName_undo_setup( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpDevName_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpDevName undo.
     */
    /*
     * copy wtpDevName and wtpDevName_len data
     * set rowreq_ctx->undo->wtpDevName from rowreq_ctx->data.wtpDevName
     */
    memcpy( rowreq_ctx->undo->wtpDevName, rowreq_ctx->data.wtpDevName,
            (rowreq_ctx->data.wtpDevName_len * sizeof(rowreq_ctx->undo->wtpDevName[0])));
    rowreq_ctx->undo->wtpDevName_len = rowreq_ctx->data.wtpDevName_len;


    return MFD_SUCCESS;
} /* wtpDevName_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpDevName_val_ptr
 *        A char containing the new value.
 * @param wtpDevName_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpDevName_val_ptr
 */
int
wtpDevName_set( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char *wtpDevName_val_ptr,  size_t wtpDevName_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpDevName_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpDevName_val_ptr);

/*
 * TODO:245:o: |-> Implement wtpDevName reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpDevName value.
     * set wtpDevName value in rowreq_ctx->data
     */    
    int rc = MFD_ERROR;
	int ret = 0;
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	ret = set_wtp_wtpname(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrID, wtpDevName_val_ptr);
	if(ret == 1)
	{
		memcpy( rowreq_ctx->data.wtpDevName, wtpDevName_val_ptr, wtpDevName_val_ptr_len );
	    rowreq_ctx->data.wtpDevName_len = wtpDevName_val_ptr_len / sizeof(wtpDevName_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}   

    return rc;
} /* wtpDevName_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpDevName_undo( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpDevName_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpDevName undo.
     */
    /*
     * copy wtpDevName and wtpDevName_len data
     * set rowreq_ctx->data.wtpDevName from rowreq_ctx->undo->wtpDevName
     */
    memcpy( rowreq_ctx->data.wtpDevName, rowreq_ctx->undo->wtpDevName,
            (rowreq_ctx->undo->wtpDevName_len * sizeof(rowreq_ctx->data.wtpDevName[0])));
    rowreq_ctx->data.wtpDevName_len = rowreq_ctx->undo->wtpDevName_len;

    
    return MFD_SUCCESS;
} /* wtpDevName_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpPosInfo
 * wtpPosInfo is subid 3 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.3
 * Description:
The location of the AP device.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 15)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpPosInfo_val_ptr
 *        A char containing the new value.
 * @param wtpPosInfo_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpPosInfo_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  1 - 15
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpPosInfo_check_value( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char *wtpPosInfo_val_ptr,  size_t wtpPosInfo_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpPosInfo_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpPosInfo_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wtpPosInfo value.
     */

    return MFD_SUCCESS; /* wtpPosInfo value not illegal */
} /* wtpPosInfo_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpPosInfo_undo_setup( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpPosInfo_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpPosInfo undo.
     */
    /*
     * copy wtpPosInfo and wtpPosInfo_len data
     * set rowreq_ctx->undo->wtpPosInfo from rowreq_ctx->data.wtpPosInfo
     */
    memcpy( rowreq_ctx->undo->wtpPosInfo, rowreq_ctx->data.wtpPosInfo,
            (rowreq_ctx->data.wtpPosInfo_len * sizeof(rowreq_ctx->undo->wtpPosInfo[0])));
    rowreq_ctx->undo->wtpPosInfo_len = rowreq_ctx->data.wtpPosInfo_len;


    return MFD_SUCCESS;
} /* wtpPosInfo_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpPosInfo_val_ptr
 *        A char containing the new value.
 * @param wtpPosInfo_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpPosInfo_val_ptr
 */
int
wtpPosInfo_set( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char *wtpPosInfo_val_ptr,  size_t wtpPosInfo_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpPosInfo_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpPosInfo_val_ptr);

/*
 * TODO:245:o: |-> Implement wtpPosInfo reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpPosInfo value.
     * set wtpPosInfo value in rowreq_ctx->data
     */
    int rc = MFD_ERROR;
	int ret = 0;
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

        
	ret = set_wtp_location(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpCurrID, wtpPosInfo_val_ptr);
	if(ret == 1)
	{
		memcpy( rowreq_ctx->data.wtpPosInfo, wtpPosInfo_val_ptr, wtpPosInfo_val_ptr_len );
	    rowreq_ctx->data.wtpPosInfo_len = wtpPosInfo_val_ptr_len / sizeof(wtpPosInfo_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}   

    return rc;
} /* wtpPosInfo_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpPosInfo_undo( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpPosInfo_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpPosInfo undo.
     */
    /*
     * copy wtpPosInfo and wtpPosInfo_len data
     * set rowreq_ctx->data.wtpPosInfo from rowreq_ctx->undo->wtpPosInfo
     */
    memcpy( rowreq_ctx->data.wtpPosInfo, rowreq_ctx->undo->wtpPosInfo,
            (rowreq_ctx->undo->wtpPosInfo_len * sizeof(rowreq_ctx->data.wtpPosInfo[0])));
    rowreq_ctx->data.wtpPosInfo_len = rowreq_ctx->undo->wtpPosInfo_len;

    
    return MFD_SUCCESS;
} /* wtpPosInfo_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpSysRestart
 * wtpSysRestart is subid 14 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.14
 * Description:
Restart AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  Normal(1), Restart(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSysRestart_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  Normal(1), Restart(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpSysRestart_check_value( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpSysRestart_val)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysRestart_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpSysRestart value.
     */

    return MFD_SUCCESS; /* wtpSysRestart value not illegal */
} /* wtpSysRestart_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpSysRestart_undo_setup( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysRestart_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpSysRestart undo.
     */
    /*
     * copy wtpSysRestart data
     * set rowreq_ctx->undo->wtpSysRestart from rowreq_ctx->data.wtpSysRestart
     */
    rowreq_ctx->undo->wtpSysRestart = rowreq_ctx->data.wtpSysRestart;


    return MFD_SUCCESS;
} /* wtpSysRestart_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpSysRestart_val
 *        A long containing the new value.
 */
int
wtpSysRestart_set( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpSysRestart_val )
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysRestart_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpSysRestart reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpSysRestart_val) {
        case WTPSYSRESTART_NORMAL:
             wtpSysRestart_val = INTERNAL_WTPSYSRESTART_NORMAL;
             break;

        case WTPSYSRESTART_RESTART:
             wtpSysRestart_val = INTERNAL_WTPSYSRESTART_RESTART;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpSysRestart\n", wtpSysRestart_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpSysRestart value.
     * set wtpSysRestart value in rowreq_ctx->data
     */
    int rc = MFD_ERROR;
	int ret = 0;
    void *connection = NULL;

	if(wtpSysRestart_val == INTERNAL_WTPSYSRESTART_RESTART)
	{ 
        if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
            return MFD_ERROR;
		ret = set_ap_reboot_func(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpCurrID);
	}
	
	if(ret == 1)
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rowreq_ctx->data.wtpSysRestart = wtpSysRestart_val;
		rc = MFD_SUCCESS;
	}
	else
	{						
		rc = MFD_ERROR;
	}      

    return rc;
} /* wtpSysRestart_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpSysRestart_undo( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysRestart_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpSysRestart undo.
     */
    /*
     * copy wtpSysRestart data
     * set rowreq_ctx->data.wtpSysRestart from rowreq_ctx->undo->wtpSysRestart
     */
    rowreq_ctx->data.wtpSysRestart = rowreq_ctx->undo->wtpSysRestart;

    
    return MFD_SUCCESS;
} /* wtpSysRestart_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpSysReset
 * wtpSysReset is subid 15 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.15
 * Description:
Reset AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 1/8. Values:  Normal(1), Reset(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSysReset_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  Normal(1), Reset(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpSysReset_check_value( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpSysReset_val)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysReset_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpSysReset value.
     */

    return MFD_SUCCESS; /* wtpSysReset value not illegal */
} /* wtpSysReset_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpSysReset_undo_setup( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysReset_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpSysReset undo.
     */
    /*
     * copy wtpSysReset data
     * set rowreq_ctx->undo->wtpSysReset from rowreq_ctx->data.wtpSysReset
     */
    rowreq_ctx->undo->wtpSysReset = rowreq_ctx->data.wtpSysReset;


    return MFD_SUCCESS;
} /* wtpSysReset_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpSysReset_val
 *        A long containing the new value.
 */
int
wtpSysReset_set( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpSysReset_val )
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysReset_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpSysReset reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpSysReset_val) {
        case WTPSYSRESET_NORMAL:
             wtpSysReset_val = INTERNAL_WTPSYSRESET_NORMAL;
             break;

        case WTPSYSRESET_RESET:
             wtpSysReset_val = INTERNAL_WTPSYSRESET_RESET;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpSysReset\n", wtpSysReset_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpSysReset value.
     * set wtpSysReset value in rowreq_ctx->data
     */
    int rc = MFD_ERROR;
	int ret = 0;
	int radio_id = 0;
    
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
	if(wtpSysReset_val == INTERNAL_WTPSYSRESET_RESET)
	{
	    ret = set_ap_extension_command_func(rowreq_ctx->data.parameter, connection, rowreq_ctx->data.wtpCurrID, "sysfactoryreset");
	    snmp_log(LOG_DEBUG, "set_ap_extension_command_func sysfactoryreset, ret = %d\n", ret);
	}
	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpSysReset = wtpSysReset_val;
		set_ap_reboot_func(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrID);
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}        

    return rc;
} /* wtpSysReset_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpSysReset_undo( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpSysReset_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpSysReset undo.
     */
    /*
     * copy wtpSysReset data
     * set rowreq_ctx->data.wtpSysReset from rowreq_ctx->undo->wtpSysReset
     */
    rowreq_ctx->data.wtpSysReset = rowreq_ctx->undo->wtpSysReset;

    
    return MFD_SUCCESS;
} /* wtpSysReset_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpTrapDesIPAddress
 * wtpTrapDesIPAddress is subid 16 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.16
 * Description:
The target IP of net-management broadcast information.£¨AP£©
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTrapDesIPAddress_val_ptr
 *        A char containing the new value.
 * @param wtpTrapDesIPAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpTrapDesIPAddress_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpColdReboot_check_value( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpColdReboot_val)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpColdReboot_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpColdReboot value.
     */

    return MFD_SUCCESS; /* wtpColdReboot value not illegal */
} /* wtpColdReboot_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpColdReboot_undo_setup( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpColdReboot_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpColdReboot undo.
     */
    /*
     * copy wtpColdReboot data
     * set rowreq_ctx->undo->wtpColdReboot from rowreq_ctx->data.wtpColdReboot
     */
    rowreq_ctx->undo->wtpColdReboot = rowreq_ctx->data.wtpColdReboot;


    return MFD_SUCCESS;
} /* wtpColdReboot_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpColdReboot_val
 *        A long containing the new value.
 */
int
wtpColdReboot_set( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, u_long wtpColdReboot_val )
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpColdReboot_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpColdReboot reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpColdReboot_val) {
        case WTPCOLDREBOOT_REBOOT:
             wtpColdReboot_val = INTERNAL_WTPCOLDREBOOT_REBOOT;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpColdReboot\n", wtpColdReboot_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpColdReboot value.
     * set wtpColdReboot value in rowreq_ctx->data
     */
    int rc = MFD_ERROR;
	int ret = 0;
    void *connection = NULL;
    
	if(wtpColdReboot_val == INTERNAL_WTPCOLDREBOOT_REBOOT)
	{
        if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
            return MFD_ERROR;
		ret = set_ap_extension_command_func(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpCurrID,"syscoldreboot");
	}
	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpColdReboot = wtpColdReboot_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}            

    return rc;
} /* wtpColdReboot_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpColdReboot_undo( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtpColdReboot_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpColdReboot undo.
     */
    /*
     * copy wtpColdReboot data
     * set rowreq_ctx->data.wtpColdReboot from rowreq_ctx->undo->wtpColdReboot
     */
    rowreq_ctx->data.wtpColdReboot = rowreq_ctx->undo->wtpColdReboot;

    
    return MFD_SUCCESS;
} /* wtpColdReboot_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtplongitude
 * wtplongitude is subid 17 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.17
 * Description:
The location of AP longitude.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtplongitude_val_ptr
 *        A char containing the new value.
 * @param wtplongitude_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtplongitude_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.wtplongitude).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtplongitude_check_value( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char *wtplongitude_val_ptr,  size_t wtplongitude_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtplongitude_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtplongitude_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wtplongitude value.
     */

    return MFD_SUCCESS; /* wtplongitude value not illegal */
} /* wtplongitude_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtplongitude_undo_setup( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtplongitude_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtplongitude undo.
     */
    /*
     * copy wtplongitude and wtplongitude_len data
     * set rowreq_ctx->undo->wtplongitude from rowreq_ctx->data.wtplongitude
     */
    memcpy( rowreq_ctx->undo->wtplongitude, rowreq_ctx->data.wtplongitude,
            (rowreq_ctx->data.wtplongitude_len * sizeof(rowreq_ctx->undo->wtplongitude[0])));
    rowreq_ctx->undo->wtplongitude_len = rowreq_ctx->data.wtplongitude_len;


    return MFD_SUCCESS;
} /* wtplongitude_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtplongitude_val_ptr
 *        A char containing the new value.
 * @param wtplongitude_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtplongitude_val_ptr
 */
int
wtplongitude_set( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char *wtplongitude_val_ptr,  size_t wtplongitude_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtplongitude_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtplongitude_val_ptr);

    /*
     * TODO:461:M: |-> Set wtplongitude value.
     * set wtplongitude value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	int ret = 0;
	void *connection = NULL;
	int len =0;
	if(SNMPD_DBUS_ERROR == get_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id, &connection, SNMPD_INSTANCE_MASTER_V3))
	{
		return MFD_ERROR;
	}
	printf("rowreq_ctx->data.parameter:%d-%d-%d\n",rowreq_ctx->data.parameter.slot_id,rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.parameter.instance_id);
	
	printf("rowreq_ctx->data.wtplatitude=%s\n",rowreq_ctx->data.wtplatitude);
	printf("wtplongitude_val_ptr=%s\n",wtplongitude_val_ptr);
	printf("rowreq_ctx->data.wtpCurrID=%d\n",rowreq_ctx->data.wtpCurrID);

	ret = ccgi_set_ap_longitude_latitude_cmd(rowreq_ctx->data.parameter,connection, rowreq_ctx->data.wtpCurrID, wtplongitude_val_ptr, rowreq_ctx->data.wtplatitude);
	printf("ret=%d\n",ret);
	if(ret == 0)
	{
		len = MIN(strlen(wtplongitude_val_ptr), 15);			
	    memcpy( rowreq_ctx->data.wtplongitude, wtplongitude_val_ptr, len );
	    rowreq_ctx->data.wtplongitude_len = len;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}   
	printf("rc=%d\n",rc);

    return rc;

} /* wtplongitude_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtplongitude_undo( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtplongitude_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtplongitude undo.
     */
    /*
     * copy wtplongitude and wtplongitude_len data
     * set rowreq_ctx->data.wtplongitude from rowreq_ctx->undo->wtplongitude
     */
    memcpy( rowreq_ctx->data.wtplongitude, rowreq_ctx->undo->wtplongitude,
            (rowreq_ctx->undo->wtplongitude_len * sizeof(rowreq_ctx->data.wtplongitude[0])));
    rowreq_ctx->data.wtplongitude_len = rowreq_ctx->undo->wtplongitude_len;

    
    return MFD_SUCCESS;
} /* wtplongitude_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtplatitude
 * wtplatitude is subid 18 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.18
 * Description:
The location of AP latitude.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtplatitude_val_ptr
 *        A char containing the new value.
 * @param wtplatitude_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtplatitude_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.wtplatitude).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtplatitude_check_value( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char *wtplatitude_val_ptr,  size_t wtplatitude_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtplatitude_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtplatitude_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wtplatitude value.
     */

    return MFD_SUCCESS; /* wtplatitude value not illegal */
} /* wtplatitude_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtplatitude_undo_setup( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtplatitude_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtplatitude undo.
     */
    /*
     * copy wtplatitude and wtplatitude_len data
     * set rowreq_ctx->undo->wtplatitude from rowreq_ctx->data.wtplatitude
     */
    memcpy( rowreq_ctx->undo->wtplatitude, rowreq_ctx->data.wtplatitude,
            (rowreq_ctx->data.wtplatitude_len * sizeof(rowreq_ctx->undo->wtplatitude[0])));
    rowreq_ctx->undo->wtplatitude_len = rowreq_ctx->data.wtplatitude_len;


    return MFD_SUCCESS;
} /* wtplatitude_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtplatitude_val_ptr
 *        A char containing the new value.
 * @param wtplatitude_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtplatitude_val_ptr
 */
int
wtplatitude_set( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx, char *wtplatitude_val_ptr,  size_t wtplatitude_val_ptr_len )
{
	printf("has enter wtplatitude_set!!\n");

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtplatitude_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtplatitude_val_ptr);

    /*
     * TODO:461:M: |-> Set wtplatitude value.
     * set wtplatitude value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	int ret = 0;
	void *connection = NULL;
	int len =0;
	if(SNMPD_DBUS_ERROR == get_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id, &connection, SNMPD_INSTANCE_MASTER_V3))
	{
		return MFD_ERROR;
	}
	
	printf("rowreq_ctx->data.parameter:%d-%d-%d\n",rowreq_ctx->data.parameter.slot_id,rowreq_ctx->data.parameter.local_id,rowreq_ctx->data.parameter.instance_id);
	
	printf("rowreq_ctx->data.wtplongitude=%s\n",rowreq_ctx->data.wtplongitude);
	printf("wtplatitude_val_ptr=%s\n",wtplatitude_val_ptr);
	ret = ccgi_set_ap_longitude_latitude_cmd(rowreq_ctx->data.parameter,connection, rowreq_ctx->data.wtpCurrID, rowreq_ctx->data.wtplongitude, wtplatitude_val_ptr);
	printf("ret=%d\n",ret);
	if(ret == 0)
	{
		len = MIN(strlen(wtplatitude_val_ptr), 15);			
		memcpy( rowreq_ctx->data.wtplongitude, wtplatitude_val_ptr, len );
		rowreq_ctx->data.wtplongitude_len = len;
		rc = MFD_SUCCESS;
	}
	else
	{	
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}	
	
	printf("rc=%d\n",rc);
	return rc;

} /* wtplatitude_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtplatitude_undo( dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpInfoTable:wtplatitude_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtplatitude undo.
     */
    /*
     * copy wtplatitude and wtplatitude_len data
     * set rowreq_ctx->data.wtplatitude from rowreq_ctx->undo->wtplatitude
     */
    memcpy( rowreq_ctx->data.wtplatitude, rowreq_ctx->undo->wtplatitude,
            (rowreq_ctx->undo->wtplatitude_len * sizeof(rowreq_ctx->data.wtplatitude[0])));
    rowreq_ctx->data.wtplatitude_len = rowreq_ctx->undo->wtplatitude_len;

    
    return MFD_SUCCESS;
} /* wtplatitude_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpInfoTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11WtpInfoTable if you don't have dependencies)
 */
int
dot11WtpInfoTable_check_dependencies(dot11WtpInfoTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11WtpInfoTable:dot11WtpInfoTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11WtpInfoTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11WtpInfoTable_check_dependencies */

/** @} */
