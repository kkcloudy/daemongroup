/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpDataPktsTable.h"


#include "dot11WtpDataPktsTable_data_access.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "../mibs_public.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpDataPktsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpDataPktsTable is subid 3 of wtpStats.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.2.3, length: 12
*/

/**
 * initialization for dot11WtpDataPktsTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11WtpDataPktsTable_reg
 *        Pointer to dot11WtpDataPktsTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11WtpDataPktsTable_init_data(dot11WtpDataPktsTable_registration_ptr dot11WtpDataPktsTable_reg)
{
    DEBUGMSGTL(("verbose:dot11WtpDataPktsTable:dot11WtpDataPktsTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11WtpDataPktsTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11WtpDataPktsTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11WtpDataPktsTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11WtpDataPktsTable:dot11WtpDataPktsTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11WtpDataPktsTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11WtpDataPktsTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11WTPDATAPKTSTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11WtpDataPktsTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11WtpDataPktsTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11WtpDataPktsTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11WtpDataPktsTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11WtpDataPktsTable_cache_load\n");

    dot11WtpDataPktsTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
         */
        /** 128 - 1(entry) - 1(col) - 0(other indexes) = 112 */
   char   wtpMacAddr[20] = { 0 };
   size_t      wtpMacAddr_len = 0;

    DEBUGMSGTL(("verbose:dot11WtpDataPktsTable:dot11WtpDataPktsTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
      //  return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11WtpDataPktsTable container.
     * loop over your dot11WtpDataPktsTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
	

    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_all_wtp_data_pkts_information_cmd\n");
    messageHead = list_connection_call_dbus_method(show_all_wtp_data_pkts_information_cmd, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_all_wtp_data_pkts_information_cmd,messageHead=%p\n", messageHead);

	
	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    struct WtpDataPktsInfo *WtpHead = messageNode->message;
		    if(WtpHead)
		    {
		        struct WtpDataPktsInfo *q =	NULL;
        		for(q = WtpHead->WtpDataPktsInfo_list; q != NULL; q = q->next)
        		{
        			memset(wtpMacAddr,0,20);
					if(q->wtpMacAddr)
					{
						snprintf(wtpMacAddr,sizeof(wtpMacAddr)-1,
								 "%02X:%02X:%02X:%02X:%02X:%02X",
								 q->wtpMacAddr[0],q->wtpMacAddr[1],
								 q->wtpMacAddr[2],q->wtpMacAddr[3],
								 q->wtpMacAddr[4],q->wtpMacAddr[5]);
					}
	    
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * get a line (skip blank lines)
     */
    //do {
      //  if (!fgets(line, sizeof(line), filep)) {
            /* we're done */
        //    fclose(filep);
          //  filep = NULL;
        //}
    //} while (filep && (line[0] == '\n'));

    /*
     * check for end of data
     */
    //if(NULL == filep)
      //  break;

    /*
     * parse line into variables
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

        /*
         * TODO:352:M: |   |-> set indexes in new dot11WtpDataPktsTable rowreq context.
         */
        rowreq_ctx = dot11WtpDataPktsTable_allocate_rowreq_ctx();
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
            free_dbus_message_list(&messageHead, Free_show_all_wtp_data_pkts_information_cmd);
            return MFD_RESOURCE_UNAVAILABLE;
        }
		
		wtpMacAddr_len = strlen(wtpMacAddr);
        if(MFD_SUCCESS != dot11WtpDataPktsTable_indexes_set(rowreq_ctx
                               , wtpMacAddr, wtpMacAddr_len
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "dot11WtpDataPktsTable cache.\n");
            dot11WtpDataPktsTable_release_rowreq_ctx(rowreq_ctx);
            continue;
        }

        memcpy(&(rowreq_ctx->data.parameter), &(messageNode->parameter), sizeof(dbus_parameter));
        /*
         * TODO:352:r: |   |-> populate dot11WtpDataPktsTable data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for wtpWirelessMacRxAllByte
     * wtpWirelessMacRxAllByte(1)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessMacRxAllByte mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessMacRxAllByte = q->wtpWirelessMacRecvAllByte;
    
    /*
     * setup/save data for wtpWirelessMacTxAllByte
     * wtpWirelessMacTxAllByte(2)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessMacTxAllByte mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessMacTxAllByte = q->wtpWirelessMacSendAllByte;
    
    /*
     * setup/save data for wtpWirelessMacRxPack
     * wtpWirelessMacRxPack(3)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessMacRxPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessMacRxPack = q->wtpWirelessMacRecvPack;
    
    /*
     * setup/save data for wtpWirelessMacRxErrPack
     * wtpWirelessMacRxErrPack(4)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessMacRxErrPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessMacRxErrPack = q->wtpWirelessMacRecvWrongPack;
    
    /*
     * setup/save data for wtpWiredMacRxAllByte
     * wtpWiredMacRxAllByte(5)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWiredMacRxAllByte mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWiredMacRxAllByte = q->wtpWiredMacRecvRightByte;
    
    /*
     * setup/save data for wtpWiredMacTxAllByte
     * wtpWiredMacTxAllByte(6)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWiredMacTxAllByte mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWiredMacTxAllByte = q->wtpWiredMacSendAllByte;
    
    /*
     * setup/save data for wtpWiredMacRxErrPack
     * wtpWiredMacRxErrPack(7)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWiredMacRxErrPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWiredMacRxErrPack = q->wtpWiredMacRecvWrongPack;
    
    /*
     * setup/save data for wtpWirelessMacTxDropPkts
     * wtpWirelessMacTxDropPkts(8)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessMacTxDropPkts mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessMacTxDropPkts = q->wtpDropPkts;
    
    /*
     * setup/save data for wtpWirelessMacTxPack
     * wtpWirelessMacTxPack(9)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessMacTxPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessMacTxPack = q->wtpWirelessMacSendPack;
    
    /*
     * setup/save data for wtpWirelessMacTxErrPack
     * wtpWirelessMacTxErrPack(10)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessMacTxErrPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessMacTxErrPack = q->wtpWirelessMacSendErrPack;
    
    /*
     * setup/save data for wtpWirelessMacRxRightPack
     * wtpWirelessMacRxRightPack(11)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessMacRxRightPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessMacRxRightPack = q->wtpWirelessMacRecvRightPack;
    
    /*
     * setup/save data for wtpWirelessMacTxRightPack
     * wtpWirelessMacTxRightPack(12)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessMacTxRightPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessMacTxRightPack = q->wtpWirelessMacSendRightPack;
    
    /*
     * setup/save data for wtpWiredMacRxPack
     * wtpWiredMacRxPack(13)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWiredMacRxPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWiredMacRxPack = q->wtpWiredMacRecvPack;
    
    /*
     * setup/save data for wtpWiredMacRxRightPack
     * wtpWiredMacRxRightPack(14)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWiredMacRxRightPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWiredMacRxRightPack = q->wtpWiredMacRecvRightPack;
    
    /*
     * setup/save data for wtpWiredMacTxPack
     * wtpWiredMacTxPack(15)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWiredMacTxPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWiredMacTxPack = q->wtpWiredMacSendPack;
    
    /*
     * setup/save data for wtpWiredMacTxErrPack
     * wtpWiredMacTxErrPack(16)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWiredMacTxErrPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWiredMacTxErrPack = q->wtpWiredMacSendErrPack;
    
    /*
     * setup/save data for wtpWiredMacTxRightPack
     * wtpWiredMacTxRightPack(17)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWiredMacTxRightPack mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWiredMacTxRightPack = q->wtpWiredMacSendRightPack;
    
    /*
     * setup/save data for wtpWirelessradioRecvBytes
     * wtpWirelessradioRecvBytes(18)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessradioRecvBytes mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelessradioRecvBytes = q->wtpWirelessradioRecvBytes;
    
    /*
     * setup/save data for wtpWirelesscoreRecvBytes
     * wtpWirelesscoreRecvBytes(19)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelesscoreRecvBytes mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpWirelesscoreRecvBytes = q->wtpWirelesscoreRecvBytes;
    
    /*
     * setup/save data for wtpWirelessRxFlowByte
     * wtpWirelessRxFlowByte(20)/COUNTER64/ASN_COUNTER64/U64(U64)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessRxFlowByte mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
	
    rowreq_ctx->data.wtpWirelessRxFlowByte = q->wtpWirelessRecvFlowByte;
   // rowreq_ctx->data.wtpWirelessRxFlowByte.low = wtpWirelessRxFlowByte.low;
    
    /*
     * setup/save data for wtpWirelessTXFlowByte
     * wtpWirelessTXFlowByte(21)/COUNTER64/ASN_COUNTER64/U64(U64)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWirelessTXFlowByte mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    
    rowreq_ctx->data.wtpWirelessTxFlowByte = q->wtpWirelessSendFlowByte;
    //rowreq_ctx->data.wtpWirelessTXFlowByte.low = wtpWirelessTXFlowByte.low;
    
        
        /*
         * insert into table container
         */
    		        if(CONTAINER_INSERT(container, rowreq_ctx))
    		        {
    					dot11WtpDataPktsTable_release_rowreq_ctx(rowreq_ctx);
    		        }
    		        ++count;
        		}		
    		}
    	}  	
		free_dbus_message_list(&messageHead, Free_show_all_wtp_data_pkts_information_cmd);
	}


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
      //  fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11WtpDataPktsTable:dot11WtpDataPktsTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11WtpDataPktsTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11WtpDataPktsTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11WtpDataPktsTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11WtpDataPktsTable:dot11WtpDataPktsTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11WtpDataPktsTable cache.
     */
} /* dot11WtpDataPktsTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11WtpDataPktsTable_row_prep( dot11WtpDataPktsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpDataPktsTable:dot11WtpDataPktsTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11WtpDataPktsTable_row_prep */

/** @} */
