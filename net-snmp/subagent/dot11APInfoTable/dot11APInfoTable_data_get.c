/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11APInfoTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11APInfoTable get routines.
 * TODO:240:M: Implement dot11APInfoTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11APInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11APInfoTable is subid 5 of dot11HiddenInfo.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.6.1.5, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11APInfoTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-HIDDEN-MIB::dot11APInfoEntry.WtpID
 * WtpID is subid 1 of dot11APInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.6.1.5.1.1
 * Description:
AP ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-WtpID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
WtpIDHidden_map(long *mib_WtpIDHidden_val_ptr, long raw_WtpIDHidden_val)
{
    netsnmp_assert(NULL != mib_WtpIDHidden_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11APInfoTable:WtpIDHidden_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement WtpIDHidden mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_WtpIDHidden_val_ptr) = raw_WtpIDHidden_val;

    return MFD_SUCCESS;
} /* WtpIDHidden_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11APInfoTable_indexes_set_tbl_idx(dot11APInfoTable_mib_index *tbl_idx, long WtpIDHidden_val)
{
    DEBUGMSGTL(("verbose:dot11APInfoTable:dot11APInfoTable_indexes_set_tbl_idx","called\n"));

    /* WtpIDHidden(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->WtpIDHidden = WtpIDHidden_val;
    

    return MFD_SUCCESS;
} /* dot11APInfoTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11APInfoTable_indexes_set(dot11APInfoTable_rowreq_ctx *rowreq_ctx, long WtpIDHidden_val)
{
    DEBUGMSGTL(("verbose:dot11APInfoTable:dot11APInfoTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11APInfoTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , WtpIDHidden_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11APInfoTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11APInfoTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-HIDDEN-MIB::dot11APInfoEntry.WtpIPAddress
 * WtpIPAddress is subid 2 of dot11APInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.6.1.5.1.2
 * Description:
AP ipaddress.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-WtpIPAddress.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
WtpIPAddress_map(char **mib_WtpIPAddress_val_ptr_ptr, size_t *mib_WtpIPAddress_val_ptr_len_ptr, char *raw_WtpIPAddress_val_ptr, size_t raw_WtpIPAddress_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_WtpIPAddress_val_ptr);
    netsnmp_assert((NULL != mib_WtpIPAddress_val_ptr_ptr) && (NULL != mib_WtpIPAddress_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11APInfoTable:WtpIPAddress_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement WtpIPAddress non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_WtpIPAddress_val_ptr_len; /* assume equal */
    if((NULL == *mib_WtpIPAddress_val_ptr_ptr) || (*mib_WtpIPAddress_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_WtpIPAddress_val_ptr_ptr = realloc( *mib_WtpIPAddress_val_ptr_ptr, converted_len * sizeof(**mib_WtpIPAddress_val_ptr_ptr));
        if(NULL == *mib_WtpIPAddress_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_WtpIPAddress_val_ptr_len_ptr = converted_len;
    memcpy( *mib_WtpIPAddress_val_ptr_ptr, raw_WtpIPAddress_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* WtpIPAddress_map */

/**
 * Extract the current value of the WtpIPAddress data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param WtpIPAddress_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param WtpIPAddress_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by WtpIPAddress.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*WtpIPAddress_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update WtpIPAddress_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
WtpIPAddress_get( dot11APInfoTable_rowreq_ctx *rowreq_ctx, char **WtpIPAddress_val_ptr_ptr, size_t *WtpIPAddress_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != WtpIPAddress_val_ptr_ptr) && (NULL != *WtpIPAddress_val_ptr_ptr));
   netsnmp_assert( NULL != WtpIPAddress_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11APInfoTable:WtpIPAddress_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the WtpIPAddress data.
 * set (* WtpIPAddress_val_ptr_ptr ) and (* WtpIPAddress_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for WtpIPAddress data
     */
    if ((NULL == (* WtpIPAddress_val_ptr_ptr )) ||
        ((* WtpIPAddress_val_ptr_len_ptr ) < (rowreq_ctx->data.WtpIPAddress_len * sizeof((* WtpIPAddress_val_ptr_ptr )[0])))) {
        /*
         * allocate space for WtpIPAddress data
         */
        (* WtpIPAddress_val_ptr_ptr ) = malloc(rowreq_ctx->data.WtpIPAddress_len * sizeof((* WtpIPAddress_val_ptr_ptr )[0]));
        if(NULL == (* WtpIPAddress_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* WtpIPAddress_val_ptr_len_ptr ) = rowreq_ctx->data.WtpIPAddress_len * sizeof((* WtpIPAddress_val_ptr_ptr )[0]);
    memcpy( (* WtpIPAddress_val_ptr_ptr ), rowreq_ctx->data.WtpIPAddress, (* WtpIPAddress_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* WtpIPAddress_get */

/*---------------------------------------------------------------------
 * DOT11-HIDDEN-MIB::dot11APInfoEntry.WtpNetMask
 * WtpNetMask is subid 3 of dot11APInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.6.1.5.1.3
 * Description:
AP netmask.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-WtpNetMask.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
WtpNetMask_map(char **mib_WtpNetMask_val_ptr_ptr, size_t *mib_WtpNetMask_val_ptr_len_ptr, char *raw_WtpNetMask_val_ptr, size_t raw_WtpNetMask_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_WtpNetMask_val_ptr);
    netsnmp_assert((NULL != mib_WtpNetMask_val_ptr_ptr) && (NULL != mib_WtpNetMask_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11APInfoTable:WtpNetMask_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement WtpNetMask non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_WtpNetMask_val_ptr_len; /* assume equal */
    if((NULL == *mib_WtpNetMask_val_ptr_ptr) || (*mib_WtpNetMask_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_WtpNetMask_val_ptr_ptr = realloc( *mib_WtpNetMask_val_ptr_ptr, converted_len * sizeof(**mib_WtpNetMask_val_ptr_ptr));
        if(NULL == *mib_WtpNetMask_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_WtpNetMask_val_ptr_len_ptr = converted_len;
    memcpy( *mib_WtpNetMask_val_ptr_ptr, raw_WtpNetMask_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* WtpNetMask_map */

/**
 * Extract the current value of the WtpNetMask data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param WtpNetMask_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param WtpNetMask_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by WtpNetMask.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*WtpNetMask_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update WtpNetMask_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
WtpNetMask_get( dot11APInfoTable_rowreq_ctx *rowreq_ctx, char **WtpNetMask_val_ptr_ptr, size_t *WtpNetMask_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != WtpNetMask_val_ptr_ptr) && (NULL != *WtpNetMask_val_ptr_ptr));
   netsnmp_assert( NULL != WtpNetMask_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11APInfoTable:WtpNetMask_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the WtpNetMask data.
 * set (* WtpNetMask_val_ptr_ptr ) and (* WtpNetMask_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for WtpNetMask data
     */
    if ((NULL == (* WtpNetMask_val_ptr_ptr )) ||
        ((* WtpNetMask_val_ptr_len_ptr ) < (rowreq_ctx->data.WtpNetMask_len * sizeof((* WtpNetMask_val_ptr_ptr )[0])))) {
        /*
         * allocate space for WtpNetMask data
         */
        (* WtpNetMask_val_ptr_ptr ) = malloc(rowreq_ctx->data.WtpNetMask_len * sizeof((* WtpNetMask_val_ptr_ptr )[0]));
        if(NULL == (* WtpNetMask_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* WtpNetMask_val_ptr_len_ptr ) = rowreq_ctx->data.WtpNetMask_len * sizeof((* WtpNetMask_val_ptr_ptr )[0]);
    memcpy( (* WtpNetMask_val_ptr_ptr ), rowreq_ctx->data.WtpNetMask, (* WtpNetMask_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* WtpNetMask_get */

/*---------------------------------------------------------------------
 * DOT11-HIDDEN-MIB::dot11APInfoEntry.WtpMACAddress
 * WtpMACAddress is subid 4 of dot11APInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.6.1.5.1.4
 * Description:
AP macaddress.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-WtpMACAddress.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
WtpMACAddress_map(char **mib_WtpMACAddress_val_ptr_ptr, size_t *mib_WtpMACAddress_val_ptr_len_ptr, char *raw_WtpMACAddress_val_ptr, size_t raw_WtpMACAddress_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_WtpMACAddress_val_ptr);
    netsnmp_assert((NULL != mib_WtpMACAddress_val_ptr_ptr) && (NULL != mib_WtpMACAddress_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11APInfoTable:WtpMACAddress_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement WtpMACAddress non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_WtpMACAddress_val_ptr_len; /* assume equal */
    if((NULL == *mib_WtpMACAddress_val_ptr_ptr) || (*mib_WtpMACAddress_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_WtpMACAddress_val_ptr_ptr = realloc( *mib_WtpMACAddress_val_ptr_ptr, converted_len * sizeof(**mib_WtpMACAddress_val_ptr_ptr));
        if(NULL == *mib_WtpMACAddress_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_WtpMACAddress_val_ptr_len_ptr = converted_len;
    memcpy( *mib_WtpMACAddress_val_ptr_ptr, raw_WtpMACAddress_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* WtpMACAddress_map */

/**
 * Extract the current value of the WtpMACAddress data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param WtpMACAddress_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param WtpMACAddress_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by WtpMACAddress.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*WtpMACAddress_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update WtpMACAddress_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
WtpMACAddress_get( dot11APInfoTable_rowreq_ctx *rowreq_ctx, char **WtpMACAddress_val_ptr_ptr, size_t *WtpMACAddress_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != WtpMACAddress_val_ptr_ptr) && (NULL != *WtpMACAddress_val_ptr_ptr));
   netsnmp_assert( NULL != WtpMACAddress_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11APInfoTable:WtpMACAddress_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the WtpMACAddress data.
 * set (* WtpMACAddress_val_ptr_ptr ) and (* WtpMACAddress_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for WtpMACAddress data
     */
    if ((NULL == (* WtpMACAddress_val_ptr_ptr )) ||
        ((* WtpMACAddress_val_ptr_len_ptr ) < (rowreq_ctx->data.WtpMACAddress_len * sizeof((* WtpMACAddress_val_ptr_ptr )[0])))) {
        /*
         * allocate space for WtpMACAddress data
         */
        (* WtpMACAddress_val_ptr_ptr ) = malloc(rowreq_ctx->data.WtpMACAddress_len * sizeof((* WtpMACAddress_val_ptr_ptr )[0]));
        if(NULL == (* WtpMACAddress_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* WtpMACAddress_val_ptr_len_ptr ) = rowreq_ctx->data.WtpMACAddress_len * sizeof((* WtpMACAddress_val_ptr_ptr )[0]);
    memcpy( (* WtpMACAddress_val_ptr_ptr ), rowreq_ctx->data.WtpMACAddress, (* WtpMACAddress_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* WtpMACAddress_get */

/*---------------------------------------------------------------------
 * DOT11-HIDDEN-MIB::dot11APInfoEntry.WtpName
 * WtpName is subid 5 of dot11APInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.33050.6.1.6.1.5.1.5
 * Description:
AP name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-WtpName.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
WtpName_map(char **mib_WtpName_val_ptr_ptr, size_t *mib_WtpName_val_ptr_len_ptr, char *raw_WtpName_val_ptr, size_t raw_WtpName_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_WtpName_val_ptr);
    netsnmp_assert((NULL != mib_WtpName_val_ptr_ptr) && (NULL != mib_WtpName_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11APInfoTable:WtpName_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement WtpName non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_WtpName_val_ptr_len; /* assume equal */
    if((NULL == *mib_WtpName_val_ptr_ptr) || (*mib_WtpName_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_WtpName_val_ptr_ptr = realloc( *mib_WtpName_val_ptr_ptr, converted_len * sizeof(**mib_WtpName_val_ptr_ptr));
        if(NULL == *mib_WtpName_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_WtpName_val_ptr_len_ptr = converted_len;
    memcpy( *mib_WtpName_val_ptr_ptr, raw_WtpName_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* WtpName_map */

/**
 * Extract the current value of the WtpName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param WtpName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param WtpName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by WtpName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*WtpName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update WtpName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
WtpName_get( dot11APInfoTable_rowreq_ctx *rowreq_ctx, char **WtpName_val_ptr_ptr, size_t *WtpName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != WtpName_val_ptr_ptr) && (NULL != *WtpName_val_ptr_ptr));
   netsnmp_assert( NULL != WtpName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11APInfoTable:WtpName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the WtpName data.
 * set (* WtpName_val_ptr_ptr ) and (* WtpName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for WtpName data
     */
    if ((NULL == (* WtpName_val_ptr_ptr )) ||
        ((* WtpName_val_ptr_len_ptr ) < (rowreq_ctx->data.WtpName_len * sizeof((* WtpName_val_ptr_ptr )[0])))) {
        /*
         * allocate space for WtpName data
         */
        (* WtpName_val_ptr_ptr ) = malloc(rowreq_ctx->data.WtpName_len * sizeof((* WtpName_val_ptr_ptr )[0]));
        if(NULL == (* WtpName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* WtpName_val_ptr_len_ptr ) = rowreq_ctx->data.WtpName_len * sizeof((* WtpName_val_ptr_ptr )[0]);
    memcpy( (* WtpName_val_ptr_ptr ), rowreq_ctx->data.WtpName, (* WtpName_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* WtpName_get */



/** @} */
