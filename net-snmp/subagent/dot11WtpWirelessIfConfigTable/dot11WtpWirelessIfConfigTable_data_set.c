/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpWirelessIfConfigTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpWirelessIfConfigTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpWirelessIfConfigTable is subid 4 of wtpInterface.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.4, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11WtpWirelessIfConfigTable_undo_setup( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:dot11WtpWirelessIfConfigTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11WtpWirelessIfConfigTable undo.
     * set up dot11WtpWirelessIfConfigTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11WtpWirelessIfConfigTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWirelessIfConfigTable_undo_cleanup( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:dot11WtpWirelessIfConfigTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11WtpWirelessIfConfigTable undo.
     */

    return rc;
} /* dot11WtpWirelessIfConfigTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpWirelessIfConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpWirelessIfConfigTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWirelessIfConfigTable_commit( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:dot11WtpWirelessIfConfigTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11WtpWirelessIfConfigTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WTPWIRELESSIFBEACONINTVL) {
       save_flags &= ~FLAG_WTPWIRELESSIFBEACONINTVL; /* clear wtpWirelessIfBeaconIntvl */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfBeaconIntvl.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfConfigTable column wtpWirelessIfBeaconIntvl commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfBeaconIntvl
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFBEACONINTVL;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSIFDTIMINTVL) {
       save_flags &= ~FLAG_WTPWIRELESSIFDTIMINTVL; /* clear wtpWirelessIfDtimIntvl */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfDtimIntvl.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfConfigTable column wtpWirelessIfDtimIntvl commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfDtimIntvl
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFDTIMINTVL;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSIFSHTRETRYTHLD) {
       save_flags &= ~FLAG_WTPWIRELESSIFSHTRETRYTHLD; /* clear wtpWirelessIfShtRetryThld */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfShtRetryThld.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfConfigTable column wtpWirelessIfShtRetryThld commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfShtRetryThld
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFSHTRETRYTHLD;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSIFLONGRETRYTHLD) {
       save_flags &= ~FLAG_WTPWIRELESSIFLONGRETRYTHLD; /* clear wtpWirelessIfLongRetryThld */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfLongRetryThld.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfConfigTable column wtpWirelessIfLongRetryThld commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfLongRetryThld
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFLONGRETRYTHLD;
       }
    }

    if (save_flags & FLAG_WTPWIRELESSIFMAXRXLIFETIME) {
       save_flags &= ~FLAG_WTPWIRELESSIFMAXRXLIFETIME; /* clear wtpWirelessIfMaxRxLifetime */
       /*
        * TODO:482:o: |-> commit column wtpWirelessIfMaxRxLifetime.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11WtpWirelessIfConfigTable column wtpWirelessIfMaxRxLifetime commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpWirelessIfMaxRxLifetime
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPWIRELESSIFMAXRXLIFETIME;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11WtpWirelessIfConfigTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpWirelessIfConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11WtpWirelessIfConfigTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11WtpWirelessIfConfigTable_undo_commit( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:dot11WtpWirelessIfConfigTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11WtpWirelessIfConfigTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11WtpWirelessIfConfigTable_undo_commit */

/*
 * TODO:420:r: Implement dot11WtpWirelessIfConfigTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfEntry.wtpWirelessIfIndex
 * wtpWirelessIfIndex is subid 1 of dot11WtpWirelessIfEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.3.1.1
 * Description:
Wireless interface index.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/*
 * TODO:440:M: Implement dot11WtpWirelessIfConfigTable node value checks.
 * TODO:450:M: Implement dot11WtpWirelessIfConfigTable undo functions.
 * TODO:460:M: Implement dot11WtpWirelessIfConfigTable set functions.
 * TODO:480:M: Implement dot11WtpWirelessIfConfigTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfConfigEntry.wtpWirelessIfBeaconIntvl
 * wtpWirelessIfBeaconIntvl is subid 1 of dot11WtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.4.1.1
 * Description:
Beacon frame interval.(Range25 - 1000ms)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  25 - 1000;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfBeaconIntvl_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  25 - 1000
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfBeaconIntvl_check_value( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfBeaconIntvl_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfBeaconIntvl_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfBeaconIntvl value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfBeaconIntvl value not illegal */
} /* wtpWirelessIfBeaconIntvl_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfBeaconIntvl_undo_setup( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfBeaconIntvl_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfBeaconIntvl undo.
     */
    /*
     * copy wtpWirelessIfBeaconIntvl data
     * set rowreq_ctx->undo->wtpWirelessIfBeaconIntvl from rowreq_ctx->data.wtpWirelessIfBeaconIntvl
     */
    rowreq_ctx->undo->wtpWirelessIfBeaconIntvl = rowreq_ctx->data.wtpWirelessIfBeaconIntvl;


    return MFD_SUCCESS;
} /* wtpWirelessIfBeaconIntvl_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfBeaconIntvl_val
 *        A long containing the new value.
 */
int
wtpWirelessIfBeaconIntvl_set( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfBeaconIntvl_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfBeaconIntvl_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfBeaconIntvl reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpWirelessIfBeaconIntvl value.
     * set wtpWirelessIfBeaconIntvl value in rowreq_ctx->data
     */  
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_beaconinterval(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpRadCurrID,wtpWirelessIfBeaconIntvl_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWirelessIfBeaconIntvl = wtpWirelessIfBeaconIntvl_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}    

    return rc;
} /* wtpWirelessIfBeaconIntvl_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfBeaconIntvl_undo( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfBeaconIntvl_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfBeaconIntvl undo.
     */
    /*
     * copy wtpWirelessIfBeaconIntvl data
     * set rowreq_ctx->data.wtpWirelessIfBeaconIntvl from rowreq_ctx->undo->wtpWirelessIfBeaconIntvl
     */
    rowreq_ctx->data.wtpWirelessIfBeaconIntvl = rowreq_ctx->undo->wtpWirelessIfBeaconIntvl;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfBeaconIntvl_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfConfigEntry.wtpWirelessIfDtimIntvl
 * wtpWirelessIfDtimIntvl is subid 2 of dot11WtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.4.1.2
 * Description:
DTIM interval.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfDtimIntvl_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 15
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfDtimIntvl_check_value( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfDtimIntvl_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfDtimIntvl_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfDtimIntvl value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfDtimIntvl value not illegal */
} /* wtpWirelessIfDtimIntvl_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfDtimIntvl_undo_setup( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfDtimIntvl_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfDtimIntvl undo.
     */
    /*
     * copy wtpWirelessIfDtimIntvl data
     * set rowreq_ctx->undo->wtpWirelessIfDtimIntvl from rowreq_ctx->data.wtpWirelessIfDtimIntvl
     */
    rowreq_ctx->undo->wtpWirelessIfDtimIntvl = rowreq_ctx->data.wtpWirelessIfDtimIntvl;


    return MFD_SUCCESS;
} /* wtpWirelessIfDtimIntvl_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfDtimIntvl_val
 *        A long containing the new value.
 */
int
wtpWirelessIfDtimIntvl_set( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfDtimIntvl_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfDtimIntvl_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfDtimIntvl reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpWirelessIfDtimIntvl value.
     * set wtpWirelessIfDtimIntvl value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_dtim(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpRadCurrID,wtpWirelessIfDtimIntvl_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWirelessIfDtimIntvl = wtpWirelessIfDtimIntvl_val;
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}        

    return rc;
} /* wtpWirelessIfDtimIntvl_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfDtimIntvl_undo( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfDtimIntvl_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfDtimIntvl undo.
     */
    /*
     * copy wtpWirelessIfDtimIntvl data
     * set rowreq_ctx->data.wtpWirelessIfDtimIntvl from rowreq_ctx->undo->wtpWirelessIfDtimIntvl
     */
    rowreq_ctx->data.wtpWirelessIfDtimIntvl = rowreq_ctx->undo->wtpWirelessIfDtimIntvl;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfDtimIntvl_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfConfigEntry.wtpWirelessIfShtRetryThld
 * wtpWirelessIfShtRetryThld is subid 3 of dot11WtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.4.1.3
 * Description:
The total times that retry times when frame length less than RTS threshold  .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfShtRetryThld_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 15
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfShtRetryThld_check_value( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfShtRetryThld_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfShtRetryThld_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfShtRetryThld value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfShtRetryThld value not illegal */
} /* wtpWirelessIfShtRetryThld_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfShtRetryThld_undo_setup( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfShtRetryThld_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfShtRetryThld undo.
     */
    /*
     * copy wtpWirelessIfShtRetryThld data
     * set rowreq_ctx->undo->wtpWirelessIfShtRetryThld from rowreq_ctx->data.wtpWirelessIfShtRetryThld
     */
    rowreq_ctx->undo->wtpWirelessIfShtRetryThld = rowreq_ctx->data.wtpWirelessIfShtRetryThld;


    return MFD_SUCCESS;
} /* wtpWirelessIfShtRetryThld_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfShtRetryThld_val
 *        A long containing the new value.
 */
int
wtpWirelessIfShtRetryThld_set( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfShtRetryThld_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfShtRetryThld_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfShtRetryThld reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpWirelessIfShtRetryThld value.
     * set wtpWirelessIfShtRetryThld value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_shortretry(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpRadCurrID,wtpWirelessIfShtRetryThld_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWirelessIfShtRetryThld = wtpWirelessIfShtRetryThld_val;
		rc = MFD_SUCCESS;
	}
	else
	{		
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}   

    return rc;
} /* wtpWirelessIfShtRetryThld_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfShtRetryThld_undo( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfShtRetryThld_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfShtRetryThld undo.
     */
    /*
     * copy wtpWirelessIfShtRetryThld data
     * set rowreq_ctx->data.wtpWirelessIfShtRetryThld from rowreq_ctx->undo->wtpWirelessIfShtRetryThld
     */
    rowreq_ctx->data.wtpWirelessIfShtRetryThld = rowreq_ctx->undo->wtpWirelessIfShtRetryThld;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfShtRetryThld_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfConfigEntry.wtpWirelessIfLongRetryThld
 * wtpWirelessIfLongRetryThld is subid 4 of dot11WtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.4.1.4
 * Description:
The total times that retry times when frame length over RTS threshold  .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 15;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfLongRetryThld_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 15
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfLongRetryThld_check_value( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfLongRetryThld_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfLongRetryThld_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfLongRetryThld value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfLongRetryThld value not illegal */
} /* wtpWirelessIfLongRetryThld_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfLongRetryThld_undo_setup( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfLongRetryThld_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfLongRetryThld undo.
     */
    /*
     * copy wtpWirelessIfLongRetryThld data
     * set rowreq_ctx->undo->wtpWirelessIfLongRetryThld from rowreq_ctx->data.wtpWirelessIfLongRetryThld
     */
    rowreq_ctx->undo->wtpWirelessIfLongRetryThld = rowreq_ctx->data.wtpWirelessIfLongRetryThld;


    return MFD_SUCCESS;
} /* wtpWirelessIfLongRetryThld_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfLongRetryThld_val
 *        A long containing the new value.
 */
int
wtpWirelessIfLongRetryThld_set( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, long wtpWirelessIfLongRetryThld_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfLongRetryThld_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfLongRetryThld reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpWirelessIfLongRetryThld value.
     * set wtpWirelessIfLongRetryThld value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_longretry(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.wtpRadCurrID,wtpWirelessIfLongRetryThld_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpWirelessIfLongRetryThld = wtpWirelessIfLongRetryThld_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	} 

    return rc;
} /* wtpWirelessIfLongRetryThld_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfLongRetryThld_undo( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfLongRetryThld_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfLongRetryThld undo.
     */
    /*
     * copy wtpWirelessIfLongRetryThld data
     * set rowreq_ctx->data.wtpWirelessIfLongRetryThld from rowreq_ctx->undo->wtpWirelessIfLongRetryThld
     */
    rowreq_ctx->data.wtpWirelessIfLongRetryThld = rowreq_ctx->undo->wtpWirelessIfLongRetryThld;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfLongRetryThld_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpWirelessIfConfigEntry.wtpWirelessIfMaxRxLifetime
 * wtpWirelessIfMaxRxLifetime is subid 5 of dot11WtpWirelessIfConfigEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.3.4.1.5
 * Description:
Received data packets lifetime(units ms).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWirelessIfMaxRxLifetime_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11WtpWirelessIfConfigTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpWirelessIfMaxRxLifetime_check_value( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, u_long wtpWirelessIfMaxRxLifetime_val)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfMaxRxLifetime_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpWirelessIfMaxRxLifetime value.
     */

    return MFD_SUCCESS; /* wtpWirelessIfMaxRxLifetime value not illegal */
} /* wtpWirelessIfMaxRxLifetime_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11WtpWirelessIfConfigTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11WtpWirelessIfConfigTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpWirelessIfMaxRxLifetime_undo_setup( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfMaxRxLifetime_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpWirelessIfMaxRxLifetime undo.
     */
    /*
     * copy wtpWirelessIfMaxRxLifetime data
     * set rowreq_ctx->undo->wtpWirelessIfMaxRxLifetime from rowreq_ctx->data.wtpWirelessIfMaxRxLifetime
     */
    rowreq_ctx->undo->wtpWirelessIfMaxRxLifetime = rowreq_ctx->data.wtpWirelessIfMaxRxLifetime;


    return MFD_SUCCESS;
} /* wtpWirelessIfMaxRxLifetime_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpWirelessIfMaxRxLifetime_val
 *        A u_long containing the new value.
 */
int
wtpWirelessIfMaxRxLifetime_set( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx, u_long wtpWirelessIfMaxRxLifetime_val )
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfMaxRxLifetime_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpWirelessIfMaxRxLifetime reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpWirelessIfMaxRxLifetime value.
     * set wtpWirelessIfMaxRxLifetime value in rowreq_ctx->data
     */
    rowreq_ctx->data.wtpWirelessIfMaxRxLifetime = wtpWirelessIfMaxRxLifetime_val;

    return MFD_SUCCESS;
} /* wtpWirelessIfMaxRxLifetime_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpWirelessIfMaxRxLifetime_undo( dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11WtpWirelessIfConfigTable:wtpWirelessIfMaxRxLifetime_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpWirelessIfMaxRxLifetime undo.
     */
    /*
     * copy wtpWirelessIfMaxRxLifetime data
     * set rowreq_ctx->data.wtpWirelessIfMaxRxLifetime from rowreq_ctx->undo->wtpWirelessIfMaxRxLifetime
     */
    rowreq_ctx->data.wtpWirelessIfMaxRxLifetime = rowreq_ctx->undo->wtpWirelessIfMaxRxLifetime;

    
    return MFD_SUCCESS;
} /* wtpWirelessIfMaxRxLifetime_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11WtpWirelessIfConfigTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11WtpWirelessIfConfigTable if you don't have dependencies)
 */
int
dot11WtpWirelessIfConfigTable_check_dependencies(dot11WtpWirelessIfConfigTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11WtpWirelessIfConfigTable:dot11WtpWirelessIfConfigTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11WtpWirelessIfConfigTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11WtpWirelessIfConfigTable_check_dependencies */

/** @} */
