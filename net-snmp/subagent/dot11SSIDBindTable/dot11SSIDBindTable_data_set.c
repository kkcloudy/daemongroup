/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11SSIDBindTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11SSIDBindTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11SSIDBindTable is subid 4 of dot11wtpConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.4, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * verify specified index is valid.
 *
 * This check is independent of whether or not the values specified for
 * the columns of the new row are valid. Column values and row consistency
 * will be checked later. At this point, only the index values should be
 * checked.
 *
 * All of the individual index validation functions have been called, so this
 * is the place to make sure they are valid as a whole when combined. If
 * you only have one index, 
 * 
 *
 *
 * @param dot11SSIDBindTable_reg
 *        Pointer to the user registration data
 * @param dot11SSIDBindTable_rowreq_ctx
 *        Pointer to the users context.
 * @retval MFD_SUCCESS            : success
 * @retval MFD_CANNOT_CREATE_NOW  : index not valid right now
 * @retval MFD_CANNOT_CREATE_EVER : index never valid
 */
int
dot11SSIDBindTable_validate_index( dot11SSIDBindTable_registration_ptr dot11SSIDBindTable_reg,
                           dot11SSIDBindTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11SSIDBindTable:dot11SSIDBindTable_validate_index","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:430:M: |-> Validate potential dot11SSIDBindTable index.
     */
    if(0) {
        snmp_log(LOG_WARNING,"invalid index for a new row in the "
                 "dot11SSIDBindTable table.\n");
        /*
         * determine failure type.
         *
         * If the index could not ever be created, return MFD_NOT_EVER
         * If the index can not be created under the present circumstances
         * (even though it could be created under other circumstances),
         * return MFD_NOT_NOW.
         */
        if(0) {
            return MFD_CANNOT_CREATE_EVER;
        }
        else {
            return MFD_CANNOT_CREATE_NOW;
        }
    }

    return rc;
} /* dot11SSIDBindTable_validate_index */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SSIDBindTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11SSIDBindTable_undo_setup( dot11SSIDBindTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11SSIDBindTable:dot11SSIDBindTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11SSIDBindTable undo.
     * set up dot11SSIDBindTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11SSIDBindTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SSIDBindTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11SSIDBindTable_undo_cleanup( dot11SSIDBindTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11SSIDBindTable:dot11SSIDBindTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11SSIDBindTable undo.
     */

    return rc;
} /* dot11SSIDBindTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11SSIDBindTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11SSIDBindTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11SSIDBindTable_commit( dot11SSIDBindTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11SSIDBindTable:dot11SSIDBindTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11SSIDBindTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_BSSIDBINDROWSTATUS) {
       save_flags &= ~FLAG_BSSIDBINDROWSTATUS; /* clear BssIDBindRowStatus */
       /*
        * TODO:482:o: |-> commit column BssIDBindRowStatus.
        */
       rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11SSIDBindTable column BssIDBindRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo BssIDBindRowStatus
             */
            rowreq_ctx->column_set_flags |= FLAG_BSSIDBINDROWSTATUS;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11SSIDBindTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11SSIDBindTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11SSIDBindTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11SSIDBindTable_undo_commit( dot11SSIDBindTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11SSIDBindTable:dot11SSIDBindTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11SSIDBindTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11SSIDBindTable_undo_commit */

/*
 * TODO:420:r: Implement dot11SSIDBindTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDBindEntry.NewWtpMac
 * NewWtpMac is subid 1 of dot11SSIDBindEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.4.1.1
 * Description:
 wtpMac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * check validity of NewWtpMac index portion
 *
 * @retval MFD_SUCCESS   : the incoming value is legal
 * @retval MFD_BAD_VALUE : the incoming value is NOT legal
 *
 * @note this is not the place to do any checks for the sanity
 *       of multiple indexes. Those types of checks should be done in the
 *       dot11SSIDBindTable_validate_index() function.
 */
int
NewWtpMac_check_index( dot11SSIDBindTable_rowreq_ctx *rowreq_ctx )
{
    DEBUGMSGTL(("verbose:dot11SSIDBindTable:NewWtpMac_check_index","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:426:M: |-> Check dot11SSIDBindTable index NewWtpMac.
     * check that index value in the table context (rowreq_ctx)
     * for NewWtpMac is legal.
     */

    return MFD_SUCCESS; /* NewWtpMac index ok */
} /* NewWtpMac_check_index */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDBindEntry.NewInterface
 * NewInterface is subid 2 of dot11SSIDBindEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.4.1.2
 * Description:
Interface.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * check validity of NewInterface index portion
 *
 * @retval MFD_SUCCESS   : the incoming value is legal
 * @retval MFD_BAD_VALUE : the incoming value is NOT legal
 *
 * @note this is not the place to do any checks for the sanity
 *       of multiple indexes. Those types of checks should be done in the
 *       dot11SSIDBindTable_validate_index() function.
 */
int
NewInterface_check_index( dot11SSIDBindTable_rowreq_ctx *rowreq_ctx )
{
    DEBUGMSGTL(("verbose:dot11SSIDBindTable:NewInterface_check_index","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:426:M: |-> Check dot11SSIDBindTable index NewInterface.
     * check that index value in the table context (rowreq_ctx)
     * for NewInterface is legal.
     */

    return MFD_SUCCESS; /* NewInterface index ok */
} /* NewInterface_check_index */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDBindEntry.NewWlanId
 * NewWlanId is subid 3 of dot11SSIDBindEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.4.1.3
 * Description:
Wlan Id.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * check validity of NewWlanId index portion
 *
 * @retval MFD_SUCCESS   : the incoming value is legal
 * @retval MFD_BAD_VALUE : the incoming value is NOT legal
 *
 * @note this is not the place to do any checks for the sanity
 *       of multiple indexes. Those types of checks should be done in the
 *       dot11SSIDBindTable_validate_index() function.
 */
int
NewWlanId_check_index( dot11SSIDBindTable_rowreq_ctx *rowreq_ctx )
{
    DEBUGMSGTL(("verbose:dot11SSIDBindTable:NewWlanId_check_index","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:426:M: |-> Check dot11SSIDBindTable index NewWlanId.
     * check that index value in the table context (rowreq_ctx)
     * for NewWlanId is legal.
     */

    return MFD_SUCCESS; /* NewWlanId index ok */
} /* NewWlanId_check_index */

/*
 * TODO:440:M: Implement dot11SSIDBindTable node value checks.
 * TODO:450:M: Implement dot11SSIDBindTable undo functions.
 * TODO:460:M: Implement dot11SSIDBindTable set functions.
 * TODO:480:M: Implement dot11SSIDBindTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11SSIDBindEntry.BssIDBindRowStatus
 * BssIDBindRowStatus is subid 5 of dot11SSIDBindEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.4.1.5
 * Description:
BssIDMac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param BssIDBindRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11SSIDBindTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
BssIDBindRowStatus_check_value( dot11SSIDBindTable_rowreq_ctx *rowreq_ctx, u_long BssIDBindRowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11SSIDBindTable:BssIDBindRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid BssIDBindRowStatus value.
     */

    return MFD_SUCCESS; /* BssIDBindRowStatus value not illegal */
} /* BssIDBindRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11SSIDBindTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11SSIDBindTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
BssIDBindRowStatus_undo_setup( dot11SSIDBindTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11SSIDBindTable:BssIDBindRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup BssIDBindRowStatus undo.
     */
    /*
     * copy BssIDBindRowStatus data
     * set rowreq_ctx->undo->BssIDBindRowStatus from rowreq_ctx->data.BssIDBindRowStatus
     */
    rowreq_ctx->undo->BssIDBindRowStatus = rowreq_ctx->data.BssIDBindRowStatus;


    return MFD_SUCCESS;
} /* BssIDBindRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param BssIDBindRowStatus_val
 *        A long containing the new value.
 */
int
BssIDBindRowStatus_set( dot11SSIDBindTable_rowreq_ctx *rowreq_ctx, u_long BssIDBindRowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11SSIDBindTable:BssIDBindRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement BssIDBindRowStatus reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(BssIDBindRowStatus_val) {
        case ROWSTATUS_ACTIVE:
             BssIDBindRowStatus_val = INTERNAL_BSSIDBINDROWSTATUS_ACTIVE;
             break;

        case ROWSTATUS_NOTINSERVICE:
             BssIDBindRowStatus_val = INTERNAL_BSSIDBINDROWSTATUS_NOTINSERVICE;
             break;

        case ROWSTATUS_NOTREADY:
             BssIDBindRowStatus_val = INTERNAL_BSSIDBINDROWSTATUS_NOTREADY;
             break;

        case ROWSTATUS_CREATEANDGO:
             BssIDBindRowStatus_val = INTERNAL_BSSIDBINDROWSTATUS_CREATEANDGO;
             break;

        case ROWSTATUS_CREATEANDWAIT:
             BssIDBindRowStatus_val = INTERNAL_BSSIDBINDROWSTATUS_CREATEANDWAIT;
             break;

        case ROWSTATUS_DESTROY:
             BssIDBindRowStatus_val = INTERNAL_BSSIDBINDROWSTATUS_DESTROY;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for BssIDBindRowStatus\n", BssIDBindRowStatus_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set BssIDBindRowStatus value.
     * set BssIDBindRowStatus value in rowreq_ctx->data
     */
    rowreq_ctx->data.BssIDBindRowStatus = BssIDBindRowStatus_val;

    return MFD_SUCCESS;
} /* BssIDBindRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
BssIDBindRowStatus_undo( dot11SSIDBindTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11SSIDBindTable:BssIDBindRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up BssIDBindRowStatus undo.
     */
    /*
     * copy BssIDBindRowStatus data
     * set rowreq_ctx->data.BssIDBindRowStatus from rowreq_ctx->undo->BssIDBindRowStatus
     */
    rowreq_ctx->data.BssIDBindRowStatus = rowreq_ctx->undo->BssIDBindRowStatus;

    
    return MFD_SUCCESS;
} /* BssIDBindRowStatus_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11SSIDBindTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11SSIDBindTable if you don't have dependencies)
 */
int
dot11SSIDBindTable_check_dependencies(dot11SSIDBindTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11SSIDBindTable:dot11SSIDBindTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11SSIDBindTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & FLAG_BSSIDBINDROWSTATUS) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->BssIDBindRowStatus,
                                         rowreq_ctx->data.BssIDBindRowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.BssIDBindRowStatus) {
                if (rowreq_ctx->column_set_flags != DOT11SSIDBINDTABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11SSIDBindTable",
                                "required columns missing\n"));
                    return MFD_CANNOT_CREATE_NOW;
                }
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.BssIDBindRowStatus) {
                if (rowreq_ctx->column_set_flags & ~FLAG_BSSIDBINDROWSTATUS) {
                    DEBUGMSGTL(("dot11SSIDBindTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11SSIDBindTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11SSIDBindTable_check_dependencies */

/** @} */
