/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11IllegalDeviceTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11IllegalDeviceTable get routines.
 * TODO:240:M: Implement dot11IllegalDeviceTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11IllegalDeviceTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11IllegalDeviceTable is subid 2 of dot11WhiteBlackList.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11IllegalDeviceTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param blackListDeviceMacID_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11IllegalDeviceTable_indexes_set_tbl_idx(dot11IllegalDeviceTable_mib_index *tbl_idx, long blackListDeviceMacID_val)
{
    DEBUGMSGTL(("verbose:dot11IllegalDeviceTable:dot11IllegalDeviceTable_indexes_set_tbl_idx","called\n"));

    /* blackListDeviceMacID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->blackListDeviceMacID = blackListDeviceMacID_val;
    

    return MFD_SUCCESS;
} /* dot11IllegalDeviceTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11IllegalDeviceTable_indexes_set(dot11IllegalDeviceTable_rowreq_ctx *rowreq_ctx, long blackListDeviceMacID_val)
{
    DEBUGMSGTL(("verbose:dot11IllegalDeviceTable:dot11IllegalDeviceTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11IllegalDeviceTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , blackListDeviceMacID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11IllegalDeviceTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11IllegalDeviceTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11IllegalDeviceEntry.blackListDeviceMAC
 * blackListDeviceMAC is subid 2 of dot11IllegalDeviceEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.2.1.2
 * Description:
Black list MAC address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the blackListDeviceMAC data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param blackListDeviceMAC_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param blackListDeviceMAC_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by blackListDeviceMAC.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*blackListDeviceMAC_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update blackListDeviceMAC_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
blackListDeviceMAC_get( dot11IllegalDeviceTable_rowreq_ctx *rowreq_ctx, char **blackListDeviceMAC_val_ptr_ptr, size_t *blackListDeviceMAC_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != blackListDeviceMAC_val_ptr_ptr) && (NULL != *blackListDeviceMAC_val_ptr_ptr));
   netsnmp_assert( NULL != blackListDeviceMAC_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11IllegalDeviceTable:blackListDeviceMAC_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the blackListDeviceMAC data.
 * copy (* blackListDeviceMAC_val_ptr_ptr ) data and (* blackListDeviceMAC_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for blackListDeviceMAC data
     */
    if ((NULL == (* blackListDeviceMAC_val_ptr_ptr )) ||
        ((* blackListDeviceMAC_val_ptr_len_ptr ) <
         (rowreq_ctx->data.blackListDeviceMAC_len* sizeof(rowreq_ctx->data.blackListDeviceMAC[0])))) {
        /*
         * allocate space for blackListDeviceMAC data
         */
        (* blackListDeviceMAC_val_ptr_ptr ) = malloc(rowreq_ctx->data.blackListDeviceMAC_len* sizeof(rowreq_ctx->data.blackListDeviceMAC[0]));
        if(NULL == (* blackListDeviceMAC_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* blackListDeviceMAC_val_ptr_len_ptr ) = rowreq_ctx->data.blackListDeviceMAC_len* sizeof(rowreq_ctx->data.blackListDeviceMAC[0]);
    memcpy( (* blackListDeviceMAC_val_ptr_ptr ), rowreq_ctx->data.blackListDeviceMAC, rowreq_ctx->data.blackListDeviceMAC_len* sizeof(rowreq_ctx->data.blackListDeviceMAC[0]) );

    return MFD_SUCCESS;
} /* blackListDeviceMAC_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11IllegalDeviceEntry.blackListAddReason
 * blackListAddReason is subid 3 of dot11IllegalDeviceEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.2.1.3
 * Description:
 The reason why add to black list.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the blackListAddReason data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param blackListAddReason_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param blackListAddReason_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by blackListAddReason.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*blackListAddReason_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update blackListAddReason_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
blackListAddReason_get( dot11IllegalDeviceTable_rowreq_ctx *rowreq_ctx, char **blackListAddReason_val_ptr_ptr, size_t *blackListAddReason_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != blackListAddReason_val_ptr_ptr) && (NULL != *blackListAddReason_val_ptr_ptr));
   netsnmp_assert( NULL != blackListAddReason_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11IllegalDeviceTable:blackListAddReason_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the blackListAddReason data.
 * copy (* blackListAddReason_val_ptr_ptr ) data and (* blackListAddReason_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for blackListAddReason data
     */
    if ((NULL == (* blackListAddReason_val_ptr_ptr )) ||
        ((* blackListAddReason_val_ptr_len_ptr ) <
         (rowreq_ctx->data.blackListAddReason_len* sizeof(rowreq_ctx->data.blackListAddReason[0])))) {
        /*
         * allocate space for blackListAddReason data
         */
        (* blackListAddReason_val_ptr_ptr ) = malloc(rowreq_ctx->data.blackListAddReason_len* sizeof(rowreq_ctx->data.blackListAddReason[0]));
        if(NULL == (* blackListAddReason_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* blackListAddReason_val_ptr_len_ptr ) = rowreq_ctx->data.blackListAddReason_len* sizeof(rowreq_ctx->data.blackListAddReason[0]);
    memcpy( (* blackListAddReason_val_ptr_ptr ), rowreq_ctx->data.blackListAddReason, rowreq_ctx->data.blackListAddReason_len* sizeof(rowreq_ctx->data.blackListAddReason[0]) );

    return MFD_SUCCESS;
} /* blackListAddReason_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11IllegalDeviceEntry.blackListDuration
 * blackListDuration is subid 4 of dot11IllegalDeviceEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.2.1.4
 * Description:
 How long it keeps in black list.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the blackListDuration data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param blackListDuration_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
blackListDuration_get( dot11IllegalDeviceTable_rowreq_ctx *rowreq_ctx, u_long * blackListDuration_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != blackListDuration_val_ptr );


    DEBUGMSGTL(("verbose:dot11IllegalDeviceTable:blackListDuration_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the blackListDuration data.
 * copy (* blackListDuration_val_ptr ) from rowreq_ctx->data
 */
    (* blackListDuration_val_ptr ) = rowreq_ctx->data.blackListDuration;

    return MFD_SUCCESS;
} /* blackListDuration_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11IllegalDeviceEntry.blackListAddType
 * blackListAddType is subid 5 of dot11IllegalDeviceEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.2.1.5
 * Description:
 Black list add type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  static(1), dynamic(2)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the blackListAddType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param blackListAddType_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
blackListAddType_get( dot11IllegalDeviceTable_rowreq_ctx *rowreq_ctx, u_long * blackListAddType_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != blackListAddType_val_ptr );


    DEBUGMSGTL(("verbose:dot11IllegalDeviceTable:blackListAddType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the blackListAddType data.
 * copy (* blackListAddType_val_ptr ) from rowreq_ctx->data
 */
    (* blackListAddType_val_ptr ) = rowreq_ctx->data.blackListAddType;

    return MFD_SUCCESS;
} /* blackListAddType_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11IllegalDeviceEntry.IllegalRowStatus
 * IllegalRowStatus is subid 6 of dot11IllegalDeviceEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.8.2.1.6
 * Description:
This variable is used to create, modify, and/or delete a row
	        	 	in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the IllegalRowStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param IllegalRowStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
IllegalRowStatus_get( dot11IllegalDeviceTable_rowreq_ctx *rowreq_ctx, u_long * IllegalRowStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != IllegalRowStatus_val_ptr );

    (* IllegalRowStatus_val_ptr ) = rowreq_ctx->data.IllegalRowStatus;

    return MFD_SUCCESS;
} /* IllegalRowStatus_get */



/** @} */
