/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11AcInterfaceTable.h"

#include "mibs_public.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11AcInterfaceTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11AcInterfaceTable is subid 4 of dot11AcInterface.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.4, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11AcInterfaceTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11AcInterfaceTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11AcInterfaceTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11AcInterfaceTable_undo_setup( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:dot11AcInterfaceTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11AcInterfaceTable undo.
     * set up dot11AcInterfaceTable undo information, in preparation for a set.
     * Undo storage is in (* AcInterfaceNetMask_val_ptr )*
     */

    return rc;
} /* dot11AcInterfaceTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11AcInterfaceTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11AcInterfaceTable_undo( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:dot11AcInterfaceTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11AcInterfaceTable undo.
     * dot11AcInterfaceTable undo information, in response to a failed set.
     * Undo storage is in (* AcInterfaceNetMask_val_ptr )*
     */

    return rc;
} /* dot11AcInterfaceTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11AcInterfaceTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11AcInterfaceTable_undo_cleanup( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:dot11AcInterfaceTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11AcInterfaceTable undo.
     * Undo storage is in (* AcInterfaceNetMask_val_ptr )*
     */

    return rc;
} /* dot11AcInterfaceTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11AcInterfaceTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11AcInterfaceTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11AcInterfaceTable_commit( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:dot11AcInterfaceTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11AcInterfaceTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_ACINTERFACEIP_FLAG) {
       save_flags &= ~COLUMN_ACINTERFACEIP_FLAG; /* clear AcInterfaceIP */
       /*
        * TODO:482:o: |-> commit column AcInterfaceIP.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11AcInterfaceTable column AcInterfaceIP commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo AcInterfaceIP
             */
            rowreq_ctx->column_set_flags |= COLUMN_ACINTERFACEIP_FLAG;
       }
    }

    if (save_flags & COLUMN_ACINTERFACENETMASK_FLAG) {
       save_flags &= ~COLUMN_ACINTERFACENETMASK_FLAG; /* clear AcInterfaceNetMask */
       /*
        * TODO:482:o: |-> commit column AcInterfaceNetMask.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11AcInterfaceTable column AcInterfaceNetMask commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo AcInterfaceNetMask
             */
            rowreq_ctx->column_set_flags |= COLUMN_ACINTERFACENETMASK_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11AcInterfaceTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11AcInterfaceTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11AcInterfaceTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11AcInterfaceTable_undo_commit( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:dot11AcInterfaceTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11AcInterfaceTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11AcInterfaceTable_undo_commit */

/*
 * TODO:440:M: Implement dot11AcInterfaceTable node value checks.
 * TODO:450:M: Implement dot11AcInterfaceTable undo functions.
 * TODO:460:M: Implement dot11AcInterfaceTable set functions.
 * TODO:480:M: Implement dot11AcInterfaceTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcInterfaceEntry.AcInterfaceIP
 * AcInterfaceIP is subid 2 of dot11AcInterfaceEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.4.1.2
 * Description:
AC Interface IP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param AcInterfaceIP_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11AcInterfaceTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
AcInterfaceIP_check_value( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx, u_long AcInterfaceIP_val)
{
    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:AcInterfaceIP_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid AcInterfaceIP value.
     */

    return MFD_SUCCESS; /* AcInterfaceIP value not illegal */
} /* AcInterfaceIP_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11AcInterfaceTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11AcInterfaceTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
AcInterfaceIP_undo_setup( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:AcInterfaceIP_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup AcInterfaceIP undo.
     */
    /*
     * copy AcInterfaceIP data
     * set rowreq_ctx->undo->AcInterfaceIP from rowreq_ctx->data.AcInterfaceIP
     */
    rowreq_ctx->undo->AcInterfaceIP = rowreq_ctx->data.AcInterfaceIP;


    return MFD_SUCCESS;
} /* AcInterfaceIP_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param AcInterfaceIP_val
 *        A u_long containing the new value.
 */
int
AcInterfaceIP_set( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx, u_long AcInterfaceIP_val )
{

    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:AcInterfaceIP_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set AcInterfaceIP value.
     * set AcInterfaceIP value in rowreq_ctx->data
     */

    char command[128]   = { 0 };
    char oldip[32]      = { 0 };
    char mask[32]       = { 0 };
    char newip[32]      = { 0 };
    
    INET_NTOA(rowreq_ctx->data.AcInterfaceIP, oldip);
    INET_NTOA(rowreq_ctx->data.AcInterfaceNetMask, mask);
    INET_NTOA(AcInterfaceIP_val, newip);
    
    int Maskbit = mask_bit(mask);
    
    int ret = -1;    
    int status = 0;
    
    memset(command, 0, sizeof(command));
    snprintf(command, sizeof(command) - 1, "sudo del_intf_ip.sh %s %s/%d 1>/dev/null 2>/dev/null", 
             rowreq_ctx->data.AcInterfaceName, oldip, Maskbit);
             
    status = system(command);
    ret = WEXITSTATUS(status);
    if(0 != ret) {
        snmp_log(LOG_WARNING, "delete old ipaddr %s/%d fail\n", oldip, Maskbit);
        return MFD_ERROR;
    }

    ret = -1;
    status = 0;
    
    memset(command, 0, sizeof(command));
    snprintf(command, sizeof(command) - 1, "sudo /usr/bin/set_intf_ip.sh %s %s/%d > /dev/null 2>&1",
                rowreq_ctx->data.AcInterfaceName,newip,Maskbit);
                
    status = system(command);
    ret = WEXITSTATUS(status);
    if(0 == ret) {
        rowreq_ctx->data.AcInterfaceIP = AcInterfaceIP_val;
        return MFD_SUCCESS;
    }
        
    return MFD_ERROR;
} /* AcInterfaceIP_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
AcInterfaceIP_undo( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:AcInterfaceIP_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up AcInterfaceIP undo.
     */
    /*
     * copy AcInterfaceIP data
     * set rowreq_ctx->data.AcInterfaceIP from rowreq_ctx->undo->AcInterfaceIP
     */
    rowreq_ctx->data.AcInterfaceIP = rowreq_ctx->undo->AcInterfaceIP;

    
    return MFD_SUCCESS;
} /* AcInterfaceIP_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11AcInterfaceEntry.AcInterfaceNetMask
 * AcInterfaceNetMask is subid 3 of dot11AcInterfaceEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.4.4.1.3
 * Description:
AC Interface NetMask.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param AcInterfaceNetMask_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11AcInterfaceTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
AcInterfaceNetMask_check_value( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx, u_long AcInterfaceNetMask_val)
{
    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:AcInterfaceNetMask_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid AcInterfaceNetMask value.
     */

    return MFD_SUCCESS; /* AcInterfaceNetMask value not illegal */
} /* AcInterfaceNetMask_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11AcInterfaceTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11AcInterfaceTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
AcInterfaceNetMask_undo_setup( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:AcInterfaceNetMask_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup AcInterfaceNetMask undo.
     */
    /*
     * copy AcInterfaceNetMask data
     * set rowreq_ctx->undo->AcInterfaceNetMask from rowreq_ctx->data.AcInterfaceNetMask
     */
    rowreq_ctx->undo->AcInterfaceNetMask = rowreq_ctx->data.AcInterfaceNetMask;


    return MFD_SUCCESS;
} /* AcInterfaceNetMask_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param AcInterfaceNetMask_val
 *        A u_long containing the new value.
 */
int
AcInterfaceNetMask_set( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx, u_long AcInterfaceNetMask_val )
{

    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:AcInterfaceNetMask_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set AcInterfaceNetMask value.
     * set AcInterfaceNetMask value in rowreq_ctx->data
     */

    
    char command[128]   = { 0 };
    char oldmask[32]    = { 0 };
    char newmask[32]    = { 0 };
    char ip[32]         = { 0 };
    
    INET_NTOA(rowreq_ctx->data.AcInterfaceIP, ip);
    INET_NTOA(rowreq_ctx->data.AcInterfaceNetMask, oldmask);
    INET_NTOA(AcInterfaceNetMask_val,newmask);

    int oldMaskbit = 0, newmaskbit = 0;
    oldMaskbit = mask_bit(oldmask);
    newmaskbit = mask_bit(newmask);
    
    int ret = -1;    
    int status = 0;
    
    //delete old info
    memset(command, 0, sizeof(command));
    snprintf(command, sizeof(command) - 1, "sudo del_intf_ip.sh %s %s/%d 1>/dev/null 2>/dev/null",
                rowreq_ctx->data.AcInterfaceName, ip, oldMaskbit);
                
    status = system(command);
    ret = WEXITSTATUS(status);
    if(0 != ret) {
        snmp_log(LOG_WARNING, "delete old mask %s/%d fail\n", ip, oldMaskbit);
        return MFD_ERROR;
    }


    ret = -1;    
    status = 0;
    
    //add new info
    memset(command, 0, sizeof(command));
    snprintf(command, sizeof(command) - 1, "sudo /usr/bin/set_intf_ip.sh %s %s/%d > /dev/null 2>&1",
                rowreq_ctx->data.AcInterfaceName, ip ,newmaskbit);
                
    ret  = system(command);
    if(ret == 0) {
        rowreq_ctx->data.AcInterfaceNetMask = AcInterfaceNetMask_val;
        return  MFD_SUCCESS;
    }

    return MFD_ERROR;
} /* AcInterfaceNetMask_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
AcInterfaceNetMask_undo( dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11AcInterfaceTable:AcInterfaceNetMask_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up AcInterfaceNetMask undo.
     */
    /*
     * copy AcInterfaceNetMask data
     * set rowreq_ctx->data.AcInterfaceNetMask from rowreq_ctx->undo->AcInterfaceNetMask
     */
    rowreq_ctx->data.AcInterfaceNetMask = rowreq_ctx->undo->AcInterfaceNetMask;

    
    return MFD_SUCCESS;
} /* AcInterfaceNetMask_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11AcInterfaceTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11AcInterfaceTable if you don't have dependencies)
 */
int
dot11AcInterfaceTable_check_dependencies(dot11AcInterfaceTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11AcInterfaceTable:dot11AcInterfaceTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11AcInterfaceTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11AcInterfaceTable_check_dependencies */

/** @} */
