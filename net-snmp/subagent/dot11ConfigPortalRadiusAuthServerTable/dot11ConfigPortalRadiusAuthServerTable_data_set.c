/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigPortalRadiusAuthServerTable.h"
#include "nm/app/eag/eag_errcode.h"
#include "nm/app/eag/eag_conf.h"
#include "nm/app/eag/eag_interface.h"
#include "ws_dbus_list_interface.h"
#include "dot11ConfigPortalRadiusAuthServerTable.h"
#include "ws_eag_conf.h"
#include "autelanWtpGroup.h"

/*if eag is running, return 1*/
/*static int 
eag_ins_running_state(DBusConnection *connection, int hansitype, int insid)
{
	int ret = 0;
	struct eag_base_conf baseconf;		
	memset( &baseconf, 0, sizeof(baseconf));

	ret = eag_get_base_conf( connection, hansitype, insid, &baseconf );


	if( (EAG_RETURN_OK == ret) && (1 == baseconf.status) )
		ret = 1;
	else
		ret = 0;
	return ret;
}*/

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigPortalRadiusAuthServerTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerTable is subid 1 of dot11ConfigRadiusServerGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11ConfigPortalRadiusAuthServerTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigPortalRadiusAuthServerTable_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigPortalRadiusAuthServerTable undo.
     * set up dot11ConfigPortalRadiusAuthServerTable undo information, in preparation for a set.
     * Undo storage is in (* NewPortalRadiusServerRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigPortalRadiusAuthServerTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigPortalRadiusAuthServerTable_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11ConfigPortalRadiusAuthServerTable undo.
     * dot11ConfigPortalRadiusAuthServerTable undo information, in response to a failed set.
     * Undo storage is in (* NewPortalRadiusServerRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigPortalRadiusAuthServerTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigPortalRadiusAuthServerTable_undo_cleanup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigPortalRadiusAuthServerTable undo.
     * Undo storage is in (* NewPortalRadiusServerRowStatus_val_ptr )*
     */

    return rc;
} /* dot11ConfigPortalRadiusAuthServerTable_undo_cleanup */

static int
check_create_ready(dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx) {
    if(strlen(rowreq_ctx->data.PortalRadiusAuthDomain) && (0 != rowreq_ctx->data.NewPortalAuthServerIPAdd) 
		&& (0 != rowreq_ctx->data.NewPortalAuthServerPort) && strlen(rowreq_ctx->data.NewPortalAuthServerSharedkey)
		&& (0 != rowreq_ctx->data.NewPortalAccServerIPAdd) && (0 != rowreq_ctx->data.NewPortalAccServerPort)
		&& strlen(rowreq_ctx->data.NewPortalAccServerSharedkey)) {
        return MFD_SUCCESS;
    }

    return MFD_ERROR;
}

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigPortalRadiusAuthServerTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigPortalRadiusAuthServerTable_commit( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;
	
    if(RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus && MFD_SUCCESS == check_create_ready(rowreq_ctx)) {
        rowreq_ctx->data.NewPortalRadiusServerRowStatus = RS_NOTINSERVICE;
    }
    /*
     * commit dot11ConfigPortalRadiusAuthServerTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_PORTALRADIUSAUTHDOMAIN_FLAG) {
       save_flags &= ~COLUMN_PORTALRADIUSAUTHDOMAIN_FLAG; /* clear PortalRadiusAuthDomain */
       /*
        * TODO:482:o: |-> commit column PortalRadiusAuthDomain.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column PortalRadiusAuthDomain commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo PortalRadiusAuthDomain
             */
            rowreq_ctx->column_set_flags |= COLUMN_PORTALRADIUSAUTHDOMAIN_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALAUTHSERVERIPADD_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALAUTHSERVERIPADD_FLAG; /* clear NewPortalAuthServerIPAdd */
       /*
        * TODO:482:o: |-> commit column NewPortalAuthServerIPAdd.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalAuthServerIPAdd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalAuthServerIPAdd
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALAUTHSERVERIPADD_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALAUTHSERVERPORT_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALAUTHSERVERPORT_FLAG; /* clear NewPortalAuthServerPort */
       /*
        * TODO:482:o: |-> commit column NewPortalAuthServerPort.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalAuthServerPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalAuthServerPort
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALAUTHSERVERPORT_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALAUTHSERVERSHAREDKEY_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALAUTHSERVERSHAREDKEY_FLAG; /* clear NewPortalAuthServerSharedkey */
       /*
        * TODO:482:o: |-> commit column NewPortalAuthServerSharedkey.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalAuthServerSharedkey commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalAuthServerSharedkey
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALAUTHSERVERSHAREDKEY_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALRADIUSAUTHSERVERIPADD_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALRADIUSAUTHSERVERIPADD_FLAG; /* clear NewPortalRadiusAuthServerIPAdd */
       /*
        * TODO:482:o: |-> commit column NewPortalRadiusAuthServerIPAdd.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalRadiusAuthServerIPAdd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalRadiusAuthServerIPAdd
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALRADIUSAUTHSERVERIPADD_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALRADIUSAUTHSERVERPORT_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALRADIUSAUTHSERVERPORT_FLAG; /* clear NewPortalRadiusAuthServerPort */
       /*
        * TODO:482:o: |-> commit column NewPortalRadiusAuthServerPort.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalRadiusAuthServerPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalRadiusAuthServerPort
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALRADIUSAUTHSERVERPORT_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALRADIUSAUTHSERVERSHAREDKEY_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALRADIUSAUTHSERVERSHAREDKEY_FLAG; /* clear NewPortalRadiusAuthServerSharedkey */
       /*
        * TODO:482:o: |-> commit column NewPortalRadiusAuthServerSharedkey.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalRadiusAuthServerSharedkey commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalRadiusAuthServerSharedkey
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALRADIUSAUTHSERVERSHAREDKEY_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALACCSERVERIPADD_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALACCSERVERIPADD_FLAG; /* clear NewPortalAccServerIPAdd */
       /*
        * TODO:482:o: |-> commit column NewPortalAccServerIPAdd.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalAccServerIPAdd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalAccServerIPAdd
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALACCSERVERIPADD_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALACCSERVERPORT_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALACCSERVERPORT_FLAG; /* clear NewPortalAccServerPort */
       /*
        * TODO:482:o: |-> commit column NewPortalAccServerPort.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalAccServerPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalAccServerPort
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALACCSERVERPORT_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALACCSERVERSHAREDKEY_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALACCSERVERSHAREDKEY_FLAG; /* clear NewPortalAccServerSharedkey */
       /*
        * TODO:482:o: |-> commit column NewPortalAccServerSharedkey.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalAccServerSharedkey commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalAccServerSharedkey
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALACCSERVERSHAREDKEY_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALRADIUSACCSERVERIPADD_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALRADIUSACCSERVERIPADD_FLAG; /* clear NewPortalRadiusAccServerIPAdd */
       /*
        * TODO:482:o: |-> commit column NewPortalRadiusAccServerIPAdd.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalRadiusAccServerIPAdd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalRadiusAccServerIPAdd
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALRADIUSACCSERVERIPADD_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALRADIUSACCSERVERPORT_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALRADIUSACCSERVERPORT_FLAG; /* clear NewPortalRadiusAccServerPort */
       /*
        * TODO:482:o: |-> commit column NewPortalRadiusAccServerPort.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalRadiusAccServerPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalRadiusAccServerPort
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALRADIUSACCSERVERPORT_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALRADIUSACCSERVERSHAREDKEY_FLAG) {
       save_flags &= ~COLUMN_NEWPORTALRADIUSACCSERVERSHAREDKEY_FLAG; /* clear NewPortalRadiusAccServerSharedkey */
       /*
        * TODO:482:o: |-> commit column NewPortalRadiusAccServerSharedkey.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalRadiusAccServerSharedkey commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalRadiusAccServerSharedkey
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALRADIUSACCSERVERSHAREDKEY_FLAG;
       }
    }

    if (save_flags & COLUMN_NEWPORTALRADIUSSERVERROWSTATUS_FLAG) {
        save_flags &= ~COLUMN_NEWPORTALRADIUSSERVERROWSTATUS_FLAG; /* clear NewPortalRadiusServerRowStatus */
        switch(rowreq_ctx->data.NewPortalRadiusServerRowStatus) {
            case RS_ACTIVE:
                {
					void *connection = NULL;
				    if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3)) 
					{
						int ret = EAG_ERR_DBUS_FAILED;
						char *domain;
						unsigned long auth_ip=0;
						unsigned short auth_port=0;
						char *auth_secret="";
						unsigned long acct_ip=0;
						unsigned short acct_port=0;
						char *acct_secret="";
						
						unsigned long backup_auth_ip=0;
						unsigned short backup_auth_port=0;
						char *backup_auth_secret="";
						unsigned long backup_acct_ip=0;
						unsigned short backup_acct_port=0;
						char *backup_acct_secret="";	
						
						domain = rowreq_ctx->data.PortalRadiusAuthDomain;
						auth_ip = rowreq_ctx->data.NewPortalAuthServerIPAdd;
						auth_port = rowreq_ctx->data.NewPortalAuthServerPort;
						auth_secret = rowreq_ctx->data.NewPortalAuthServerSharedkey;
						
						acct_ip = rowreq_ctx->data.NewPortalAccServerIPAdd;
						acct_port = rowreq_ctx->data.NewPortalAccServerPort;
						acct_secret = rowreq_ctx->data.NewPortalAccServerSharedkey;
						
						if((rowreq_ctx->data.NewPortalRadiusAuthServerIPAdd != 0)&&
						   (rowreq_ctx->data.NewPortalRadiusAuthServerPort != 0)&&
						   (strcmp(rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey,"")!=0)&&
						   (rowreq_ctx->data.NewPortalRadiusAccServerIPAdd != 0)&&
						   (rowreq_ctx->data.NewPortalRadiusAccServerPort != 0)&&
						   (strcmp(rowreq_ctx->data.NewPortalRadiusAccServerSharedkey,"")!=0))
						{
							backup_auth_ip = rowreq_ctx->data.NewPortalRadiusAuthServerIPAdd;
							backup_auth_port = rowreq_ctx->data.NewPortalRadiusAuthServerPort;
							backup_auth_secret = rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey;
							
							backup_acct_ip = rowreq_ctx->data.NewPortalRadiusAccServerIPAdd;
							backup_acct_port = rowreq_ctx->data.NewPortalRadiusAccServerPort;
							backup_acct_secret = rowreq_ctx->data.NewPortalRadiusAccServerSharedkey;		
						}
						
//						if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 0)
//						{
							ret = eag_add_radius( connection, 
												  rowreq_ctx->data.parameter.local_id, 
												  rowreq_ctx->data.parameter.instance_id,
												  domain,
												  auth_ip,
												  auth_port,
												  auth_secret,
												  acct_ip,
												  acct_port,
												  acct_secret,
												  backup_auth_ip,
												  backup_auth_port,
												  backup_auth_secret,
												  backup_acct_ip,
												  backup_acct_port,
												  backup_acct_secret );
							snmp_log(LOG_DEBUG, "dot11ConfigPortalRadiusAuthServerTable_commit: eag_add_radius: ret = %d\n", ret);
							if(ret == EAG_RETURN_OK)
							{
								 rc = MFD_SUCCESS;
	                             break;
							}
							else if(EAG_ERR_DBUS_FAILED == ret)
							{
								close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
							}
//						}	
				    }    					    
                }
                rc = -1;
                break;

            case RS_CREATEANDWAIT:
                rowreq_ctx->data.NewPortalRadiusServerRowStatus = RS_NOTREADY;
                rc = MFD_SUCCESS;
                break;
                
            case RS_DESTROY:  
                {
					void *connection = NULL;
                    if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
                    {
						int ret = EAG_ERR_DBUS_FAILED;
						char *domain=NULL;
						
						domain = rowreq_ctx->data.PortalRadiusAuthDomain;
						
//						if(eag_ins_running_state(connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id) == 0)
//						{
							ret = eag_del_radius( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id,domain );
							snmp_log(LOG_DEBUG, "dot11ConfigPortalRadiusAuthServerTable_commit: eag_del_radius: ret = %d\n", ret);
							if(ret == EAG_RETURN_OK)
							{
								 rc = MFD_SUCCESS;
	                             break;
							}
							else if(EAG_ERR_DBUS_FAILED == ret)
							{
								close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
							}
//						}
                    }
                }
                rowreq_ctx->rowreq_flags &= ~MFD_ROW_DELETED;
                rc = -1;
                break;
                
            default:
                break;
        }
       /*
        * TODO:482:o: |-> commit column NewPortalRadiusServerRowStatus.
        */
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigPortalRadiusAuthServerTable column NewPortalRadiusServerRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewPortalRadiusServerRowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_NEWPORTALRADIUSSERVERROWSTATUS_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigPortalRadiusAuthServerTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigPortalRadiusAuthServerTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigPortalRadiusAuthServerTable_undo_commit( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigPortalRadiusAuthServerTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11ConfigPortalRadiusAuthServerTable_undo_commit */

/*
 * TODO:440:M: Implement dot11ConfigPortalRadiusAuthServerTable node value checks.
 * TODO:450:M: Implement dot11ConfigPortalRadiusAuthServerTable undo functions.
 * TODO:460:M: Implement dot11ConfigPortalRadiusAuthServerTable set functions.
 * TODO:480:M: Implement dot11ConfigPortalRadiusAuthServerTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.PortalRadiusAuthDomain
 * PortalRadiusAuthDomain is subid 1 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.1
 * Description:
RadiusDomain.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param PortalRadiusAuthDomain_val_ptr
 *        A char containing the new value.
 * @param PortalRadiusAuthDomain_val_ptr_len
 *        The size (in bytes) of the data pointed to by PortalRadiusAuthDomain_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.PortalRadiusAuthDomain).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
PortalRadiusAuthDomain_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *PortalRadiusAuthDomain_val_ptr,  size_t PortalRadiusAuthDomain_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:PortalRadiusAuthDomain_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != PortalRadiusAuthDomain_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid PortalRadiusAuthDomain value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* PortalRadiusAuthDomain value not illegal */
} /* PortalRadiusAuthDomain_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
PortalRadiusAuthDomain_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:PortalRadiusAuthDomain_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup PortalRadiusAuthDomain undo.
     */
    /*
     * copy PortalRadiusAuthDomain and PortalRadiusAuthDomain_len data
     * set rowreq_ctx->undo->PortalRadiusAuthDomain from rowreq_ctx->data.PortalRadiusAuthDomain
     */
    memcpy( rowreq_ctx->undo->PortalRadiusAuthDomain, rowreq_ctx->data.PortalRadiusAuthDomain,
            (rowreq_ctx->data.PortalRadiusAuthDomain_len * sizeof(rowreq_ctx->undo->PortalRadiusAuthDomain[0])));
    rowreq_ctx->undo->PortalRadiusAuthDomain_len = rowreq_ctx->data.PortalRadiusAuthDomain_len;


    return MFD_SUCCESS;
} /* PortalRadiusAuthDomain_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param PortalRadiusAuthDomain_val_ptr
 *        A char containing the new value.
 * @param PortalRadiusAuthDomain_val_ptr_len
 *        The size (in bytes) of the data pointed to by PortalRadiusAuthDomain_val_ptr
 */
int
PortalRadiusAuthDomain_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *PortalRadiusAuthDomain_val_ptr,  size_t PortalRadiusAuthDomain_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:PortalRadiusAuthDomain_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != PortalRadiusAuthDomain_val_ptr);

    /*
     * TODO:461:M: |-> Set PortalRadiusAuthDomain value.
     * set PortalRadiusAuthDomain value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.PortalRadiusAuthDomain, PortalRadiusAuthDomain_val_ptr, PortalRadiusAuthDomain_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.PortalRadiusAuthDomain_len = PortalRadiusAuthDomain_val_ptr_len / sizeof(PortalRadiusAuthDomain_val_ptr[0]);

    return MFD_SUCCESS;
} /* PortalRadiusAuthDomain_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
PortalRadiusAuthDomain_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:PortalRadiusAuthDomain_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up PortalRadiusAuthDomain undo.
     */
    /*
     * copy PortalRadiusAuthDomain and PortalRadiusAuthDomain_len data
     * set rowreq_ctx->data.PortalRadiusAuthDomain from rowreq_ctx->undo->PortalRadiusAuthDomain
     */
    memcpy( rowreq_ctx->data.PortalRadiusAuthDomain, rowreq_ctx->undo->PortalRadiusAuthDomain,
            (rowreq_ctx->undo->PortalRadiusAuthDomain_len * sizeof(rowreq_ctx->data.PortalRadiusAuthDomain[0])));
    rowreq_ctx->data.PortalRadiusAuthDomain_len = rowreq_ctx->undo->PortalRadiusAuthDomain_len;

    
    return MFD_SUCCESS;
} /* PortalRadiusAuthDomain_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalAuthServerIPAdd
 * NewPortalAuthServerIPAdd is subid 2 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.2
 * Description:
NewAuthServerIPAdd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalAuthServerIPAdd_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalAuthServerIPAdd_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewPortalAuthServerIPAdd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerIPAdd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewPortalAuthServerIPAdd value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalAuthServerIPAdd value not illegal */
} /* NewPortalAuthServerIPAdd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalAuthServerIPAdd_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerIPAdd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalAuthServerIPAdd undo.
     */
    /*
     * copy NewPortalAuthServerIPAdd data
     * set rowreq_ctx->undo->NewPortalAuthServerIPAdd from rowreq_ctx->data.NewPortalAuthServerIPAdd
     */
    rowreq_ctx->undo->NewPortalAuthServerIPAdd = rowreq_ctx->data.NewPortalAuthServerIPAdd;


    return MFD_SUCCESS;
} /* NewPortalAuthServerIPAdd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalAuthServerIPAdd_val
 *        A u_long containing the new value.
 */
int
NewPortalAuthServerIPAdd_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewPortalAuthServerIPAdd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerIPAdd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NewPortalAuthServerIPAdd value.
     * set NewPortalAuthServerIPAdd value in rowreq_ctx->data
     */
    rowreq_ctx->data.NewPortalAuthServerIPAdd = NewPortalAuthServerIPAdd_val;

    return MFD_SUCCESS;
} /* NewPortalAuthServerIPAdd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalAuthServerIPAdd_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerIPAdd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalAuthServerIPAdd undo.
     */
    /*
     * copy NewPortalAuthServerIPAdd data
     * set rowreq_ctx->data.NewPortalAuthServerIPAdd from rowreq_ctx->undo->NewPortalAuthServerIPAdd
     */
    rowreq_ctx->data.NewPortalAuthServerIPAdd = rowreq_ctx->undo->NewPortalAuthServerIPAdd;

    
    return MFD_SUCCESS;
} /* NewPortalAuthServerIPAdd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalAuthServerPort
 * NewPortalAuthServerPort is subid 3 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.3
 * Description:
NewAuthServerPort.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalAuthServerPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalAuthServerPort_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewPortalAuthServerPort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewPortalAuthServerPort value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalAuthServerPort value not illegal */
} /* NewPortalAuthServerPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalAuthServerPort_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalAuthServerPort undo.
     */
    /*
     * copy NewPortalAuthServerPort data
     * set rowreq_ctx->undo->NewPortalAuthServerPort from rowreq_ctx->data.NewPortalAuthServerPort
     */
    rowreq_ctx->undo->NewPortalAuthServerPort = rowreq_ctx->data.NewPortalAuthServerPort;


    return MFD_SUCCESS;
} /* NewPortalAuthServerPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalAuthServerPort_val
 *        A long containing the new value.
 */
int
NewPortalAuthServerPort_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewPortalAuthServerPort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NewPortalAuthServerPort value.
     * set NewPortalAuthServerPort value in rowreq_ctx->data
     */
    rowreq_ctx->data.NewPortalAuthServerPort = NewPortalAuthServerPort_val;

    return MFD_SUCCESS;
} /* NewPortalAuthServerPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalAuthServerPort_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalAuthServerPort undo.
     */
    /*
     * copy NewPortalAuthServerPort data
     * set rowreq_ctx->data.NewPortalAuthServerPort from rowreq_ctx->undo->NewPortalAuthServerPort
     */
    rowreq_ctx->data.NewPortalAuthServerPort = rowreq_ctx->undo->NewPortalAuthServerPort;

    
    return MFD_SUCCESS;
} /* NewPortalAuthServerPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalAuthServerSharedkey
 * NewPortalAuthServerSharedkey is subid 4 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.4
 * Description:
NewAuthServerSharedkey.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalAuthServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewPortalAuthServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewPortalAuthServerSharedkey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.NewPortalAuthServerSharedkey).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalAuthServerSharedkey_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewPortalAuthServerSharedkey_val_ptr,  size_t NewPortalAuthServerSharedkey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerSharedkey_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewPortalAuthServerSharedkey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid NewPortalAuthServerSharedkey value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalAuthServerSharedkey value not illegal */
} /* NewPortalAuthServerSharedkey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalAuthServerSharedkey_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerSharedkey_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalAuthServerSharedkey undo.
     */
    /*
     * copy NewPortalAuthServerSharedkey and NewPortalAuthServerSharedkey_len data
     * set rowreq_ctx->undo->NewPortalAuthServerSharedkey from rowreq_ctx->data.NewPortalAuthServerSharedkey
     */
    memcpy( rowreq_ctx->undo->NewPortalAuthServerSharedkey, rowreq_ctx->data.NewPortalAuthServerSharedkey,
            (rowreq_ctx->data.NewPortalAuthServerSharedkey_len * sizeof(rowreq_ctx->undo->NewPortalAuthServerSharedkey[0])));
    rowreq_ctx->undo->NewPortalAuthServerSharedkey_len = rowreq_ctx->data.NewPortalAuthServerSharedkey_len;


    return MFD_SUCCESS;
} /* NewPortalAuthServerSharedkey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalAuthServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewPortalAuthServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewPortalAuthServerSharedkey_val_ptr
 */
int
NewPortalAuthServerSharedkey_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewPortalAuthServerSharedkey_val_ptr,  size_t NewPortalAuthServerSharedkey_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerSharedkey_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewPortalAuthServerSharedkey_val_ptr);

    /*
     * TODO:461:M: |-> Set NewPortalAuthServerSharedkey value.
     * set NewPortalAuthServerSharedkey value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.NewPortalAuthServerSharedkey, NewPortalAuthServerSharedkey_val_ptr, NewPortalAuthServerSharedkey_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.NewPortalAuthServerSharedkey_len = NewPortalAuthServerSharedkey_val_ptr_len / sizeof(NewPortalAuthServerSharedkey_val_ptr[0]);

    return MFD_SUCCESS;
} /* NewPortalAuthServerSharedkey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalAuthServerSharedkey_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAuthServerSharedkey_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalAuthServerSharedkey undo.
     */
    /*
     * copy NewPortalAuthServerSharedkey and NewPortalAuthServerSharedkey_len data
     * set rowreq_ctx->data.NewPortalAuthServerSharedkey from rowreq_ctx->undo->NewPortalAuthServerSharedkey
     */
    memcpy( rowreq_ctx->data.NewPortalAuthServerSharedkey, rowreq_ctx->undo->NewPortalAuthServerSharedkey,
            (rowreq_ctx->undo->NewPortalAuthServerSharedkey_len * sizeof(rowreq_ctx->data.NewPortalAuthServerSharedkey[0])));
    rowreq_ctx->data.NewPortalAuthServerSharedkey_len = rowreq_ctx->undo->NewPortalAuthServerSharedkey_len;

    
    return MFD_SUCCESS;
} /* NewPortalAuthServerSharedkey_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalRadiusAuthServerIPAdd
 * NewPortalRadiusAuthServerIPAdd is subid 5 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.5
 * Description:
NewRadiusAuthServerIPAdd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalRadiusAuthServerIPAdd_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalRadiusAuthServerIPAdd_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewPortalRadiusAuthServerIPAdd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerIPAdd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewPortalRadiusAuthServerIPAdd value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalRadiusAuthServerIPAdd value not illegal */
} /* NewPortalRadiusAuthServerIPAdd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalRadiusAuthServerIPAdd_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerIPAdd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalRadiusAuthServerIPAdd undo.
     */
    /*
     * copy NewPortalRadiusAuthServerIPAdd data
     * set rowreq_ctx->undo->NewPortalRadiusAuthServerIPAdd from rowreq_ctx->data.NewPortalRadiusAuthServerIPAdd
     */
    rowreq_ctx->undo->NewPortalRadiusAuthServerIPAdd = rowreq_ctx->data.NewPortalRadiusAuthServerIPAdd;


    return MFD_SUCCESS;
} /* NewPortalRadiusAuthServerIPAdd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalRadiusAuthServerIPAdd_val
 *        A u_long containing the new value.
 */
int
NewPortalRadiusAuthServerIPAdd_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewPortalRadiusAuthServerIPAdd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerIPAdd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NewPortalRadiusAuthServerIPAdd value.
     * set NewPortalRadiusAuthServerIPAdd value in rowreq_ctx->data
     */
    rowreq_ctx->data.NewPortalRadiusAuthServerIPAdd = NewPortalRadiusAuthServerIPAdd_val;

    return MFD_SUCCESS;
} /* NewPortalRadiusAuthServerIPAdd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalRadiusAuthServerIPAdd_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerIPAdd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalRadiusAuthServerIPAdd undo.
     */
    /*
     * copy NewPortalRadiusAuthServerIPAdd data
     * set rowreq_ctx->data.NewPortalRadiusAuthServerIPAdd from rowreq_ctx->undo->NewPortalRadiusAuthServerIPAdd
     */
    rowreq_ctx->data.NewPortalRadiusAuthServerIPAdd = rowreq_ctx->undo->NewPortalRadiusAuthServerIPAdd;

    
    return MFD_SUCCESS;
} /* NewPortalRadiusAuthServerIPAdd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalRadiusAuthServerPort
 * NewPortalRadiusAuthServerPort is subid 6 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.6
 * Description:
NewRadiusAuthServerPort.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalRadiusAuthServerPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalRadiusAuthServerPort_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewPortalRadiusAuthServerPort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewPortalRadiusAuthServerPort value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalRadiusAuthServerPort value not illegal */
} /* NewPortalRadiusAuthServerPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalRadiusAuthServerPort_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalRadiusAuthServerPort undo.
     */
    /*
     * copy NewPortalRadiusAuthServerPort data
     * set rowreq_ctx->undo->NewPortalRadiusAuthServerPort from rowreq_ctx->data.NewPortalRadiusAuthServerPort
     */
    rowreq_ctx->undo->NewPortalRadiusAuthServerPort = rowreq_ctx->data.NewPortalRadiusAuthServerPort;


    return MFD_SUCCESS;
} /* NewPortalRadiusAuthServerPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalRadiusAuthServerPort_val
 *        A long containing the new value.
 */
int
NewPortalRadiusAuthServerPort_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewPortalRadiusAuthServerPort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NewPortalRadiusAuthServerPort value.
     * set NewPortalRadiusAuthServerPort value in rowreq_ctx->data
     */
    rowreq_ctx->data.NewPortalRadiusAuthServerPort = NewPortalRadiusAuthServerPort_val;

    return MFD_SUCCESS;
} /* NewPortalRadiusAuthServerPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalRadiusAuthServerPort_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalRadiusAuthServerPort undo.
     */
    /*
     * copy NewPortalRadiusAuthServerPort data
     * set rowreq_ctx->data.NewPortalRadiusAuthServerPort from rowreq_ctx->undo->NewPortalRadiusAuthServerPort
     */
    rowreq_ctx->data.NewPortalRadiusAuthServerPort = rowreq_ctx->undo->NewPortalRadiusAuthServerPort;

    
    return MFD_SUCCESS;
} /* NewPortalRadiusAuthServerPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalRadiusAuthServerSharedkey
 * NewPortalRadiusAuthServerSharedkey is subid 7 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.7
 * Description:
NewRadiusAuthServerSharedkey.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalRadiusAuthServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewPortalRadiusAuthServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewPortalRadiusAuthServerSharedkey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalRadiusAuthServerSharedkey_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewPortalRadiusAuthServerSharedkey_val_ptr,  size_t NewPortalRadiusAuthServerSharedkey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerSharedkey_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewPortalRadiusAuthServerSharedkey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid NewPortalRadiusAuthServerSharedkey value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalRadiusAuthServerSharedkey value not illegal */
} /* NewPortalRadiusAuthServerSharedkey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalRadiusAuthServerSharedkey_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerSharedkey_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalRadiusAuthServerSharedkey undo.
     */
    /*
     * copy NewPortalRadiusAuthServerSharedkey and NewPortalRadiusAuthServerSharedkey_len data
     * set rowreq_ctx->undo->NewPortalRadiusAuthServerSharedkey from rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey
     */
    memcpy( rowreq_ctx->undo->NewPortalRadiusAuthServerSharedkey, rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey,
            (rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey_len * sizeof(rowreq_ctx->undo->NewPortalRadiusAuthServerSharedkey[0])));
    rowreq_ctx->undo->NewPortalRadiusAuthServerSharedkey_len = rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey_len;


    return MFD_SUCCESS;
} /* NewPortalRadiusAuthServerSharedkey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalRadiusAuthServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewPortalRadiusAuthServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewPortalRadiusAuthServerSharedkey_val_ptr
 */
int
NewPortalRadiusAuthServerSharedkey_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewPortalRadiusAuthServerSharedkey_val_ptr,  size_t NewPortalRadiusAuthServerSharedkey_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerSharedkey_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewPortalRadiusAuthServerSharedkey_val_ptr);

    /*
     * TODO:461:M: |-> Set NewPortalRadiusAuthServerSharedkey value.
     * set NewPortalRadiusAuthServerSharedkey value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey, NewPortalRadiusAuthServerSharedkey_val_ptr, NewPortalRadiusAuthServerSharedkey_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey_len = NewPortalRadiusAuthServerSharedkey_val_ptr_len / sizeof(NewPortalRadiusAuthServerSharedkey_val_ptr[0]);

    return MFD_SUCCESS;
} /* NewPortalRadiusAuthServerSharedkey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalRadiusAuthServerSharedkey_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAuthServerSharedkey_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalRadiusAuthServerSharedkey undo.
     */
    /*
     * copy NewPortalRadiusAuthServerSharedkey and NewPortalRadiusAuthServerSharedkey_len data
     * set rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey from rowreq_ctx->undo->NewPortalRadiusAuthServerSharedkey
     */
    memcpy( rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey, rowreq_ctx->undo->NewPortalRadiusAuthServerSharedkey,
            (rowreq_ctx->undo->NewPortalRadiusAuthServerSharedkey_len * sizeof(rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey[0])));
    rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey_len = rowreq_ctx->undo->NewPortalRadiusAuthServerSharedkey_len;

    
    return MFD_SUCCESS;
} /* NewPortalRadiusAuthServerSharedkey_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalAccServerIPAdd
 * NewPortalAccServerIPAdd is subid 8 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.8
 * Description:
NewAccServerIPAdd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalAccServerIPAdd_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalAccServerIPAdd_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewPortalAccServerIPAdd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerIPAdd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewPortalAccServerIPAdd value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalAccServerIPAdd value not illegal */
} /* NewPortalAccServerIPAdd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalAccServerIPAdd_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerIPAdd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalAccServerIPAdd undo.
     */
    /*
     * copy NewPortalAccServerIPAdd data
     * set rowreq_ctx->undo->NewPortalAccServerIPAdd from rowreq_ctx->data.NewPortalAccServerIPAdd
     */
    rowreq_ctx->undo->NewPortalAccServerIPAdd = rowreq_ctx->data.NewPortalAccServerIPAdd;


    return MFD_SUCCESS;
} /* NewPortalAccServerIPAdd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalAccServerIPAdd_val
 *        A u_long containing the new value.
 */
int
NewPortalAccServerIPAdd_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewPortalAccServerIPAdd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerIPAdd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NewPortalAccServerIPAdd value.
     * set NewPortalAccServerIPAdd value in rowreq_ctx->data
     */
    rowreq_ctx->data.NewPortalAccServerIPAdd = NewPortalAccServerIPAdd_val;

    return MFD_SUCCESS;
} /* NewPortalAccServerIPAdd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalAccServerIPAdd_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerIPAdd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalAccServerIPAdd undo.
     */
    /*
     * copy NewPortalAccServerIPAdd data
     * set rowreq_ctx->data.NewPortalAccServerIPAdd from rowreq_ctx->undo->NewPortalAccServerIPAdd
     */
    rowreq_ctx->data.NewPortalAccServerIPAdd = rowreq_ctx->undo->NewPortalAccServerIPAdd;

    
    return MFD_SUCCESS;
} /* NewPortalAccServerIPAdd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalAccServerPort
 * NewPortalAccServerPort is subid 9 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.9
 * Description:
NewAccServerPort.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalAccServerPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalAccServerPort_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewPortalAccServerPort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewPortalAccServerPort value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalAccServerPort value not illegal */
} /* NewPortalAccServerPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalAccServerPort_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalAccServerPort undo.
     */
    /*
     * copy NewPortalAccServerPort data
     * set rowreq_ctx->undo->NewPortalAccServerPort from rowreq_ctx->data.NewPortalAccServerPort
     */
    rowreq_ctx->undo->NewPortalAccServerPort = rowreq_ctx->data.NewPortalAccServerPort;


    return MFD_SUCCESS;
} /* NewPortalAccServerPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalAccServerPort_val
 *        A long containing the new value.
 */
int
NewPortalAccServerPort_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewPortalAccServerPort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NewPortalAccServerPort value.
     * set NewPortalAccServerPort value in rowreq_ctx->data
     */
    rowreq_ctx->data.NewPortalAccServerPort = NewPortalAccServerPort_val;

    return MFD_SUCCESS;
} /* NewPortalAccServerPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalAccServerPort_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalAccServerPort undo.
     */
    /*
     * copy NewPortalAccServerPort data
     * set rowreq_ctx->data.NewPortalAccServerPort from rowreq_ctx->undo->NewPortalAccServerPort
     */
    rowreq_ctx->data.NewPortalAccServerPort = rowreq_ctx->undo->NewPortalAccServerPort;

    
    return MFD_SUCCESS;
} /* NewPortalAccServerPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalAccServerSharedkey
 * NewPortalAccServerSharedkey is subid 10 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.10
 * Description:
NewAccServerSharedkey.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalAccServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewPortalAccServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewPortalAccServerSharedkey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.NewPortalAccServerSharedkey).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalAccServerSharedkey_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewPortalAccServerSharedkey_val_ptr,  size_t NewPortalAccServerSharedkey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerSharedkey_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewPortalAccServerSharedkey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid NewPortalAccServerSharedkey value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalAccServerSharedkey value not illegal */
} /* NewPortalAccServerSharedkey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalAccServerSharedkey_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerSharedkey_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalAccServerSharedkey undo.
     */
    /*
     * copy NewPortalAccServerSharedkey and NewPortalAccServerSharedkey_len data
     * set rowreq_ctx->undo->NewPortalAccServerSharedkey from rowreq_ctx->data.NewPortalAccServerSharedkey
     */
    memcpy( rowreq_ctx->undo->NewPortalAccServerSharedkey, rowreq_ctx->data.NewPortalAccServerSharedkey,
            (rowreq_ctx->data.NewPortalAccServerSharedkey_len * sizeof(rowreq_ctx->undo->NewPortalAccServerSharedkey[0])));
    rowreq_ctx->undo->NewPortalAccServerSharedkey_len = rowreq_ctx->data.NewPortalAccServerSharedkey_len;


    return MFD_SUCCESS;
} /* NewPortalAccServerSharedkey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalAccServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewPortalAccServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewPortalAccServerSharedkey_val_ptr
 */
int
NewPortalAccServerSharedkey_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewPortalAccServerSharedkey_val_ptr,  size_t NewPortalAccServerSharedkey_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerSharedkey_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewPortalAccServerSharedkey_val_ptr);

    /*
     * TODO:461:M: |-> Set NewPortalAccServerSharedkey value.
     * set NewPortalAccServerSharedkey value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.NewPortalAccServerSharedkey, NewPortalAccServerSharedkey_val_ptr, NewPortalAccServerSharedkey_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.NewPortalAccServerSharedkey_len = NewPortalAccServerSharedkey_val_ptr_len / sizeof(NewPortalAccServerSharedkey_val_ptr[0]);

    return MFD_SUCCESS;
} /* NewPortalAccServerSharedkey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalAccServerSharedkey_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalAccServerSharedkey_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalAccServerSharedkey undo.
     */
    /*
     * copy NewPortalAccServerSharedkey and NewPortalAccServerSharedkey_len data
     * set rowreq_ctx->data.NewPortalAccServerSharedkey from rowreq_ctx->undo->NewPortalAccServerSharedkey
     */
    memcpy( rowreq_ctx->data.NewPortalAccServerSharedkey, rowreq_ctx->undo->NewPortalAccServerSharedkey,
            (rowreq_ctx->undo->NewPortalAccServerSharedkey_len * sizeof(rowreq_ctx->data.NewPortalAccServerSharedkey[0])));
    rowreq_ctx->data.NewPortalAccServerSharedkey_len = rowreq_ctx->undo->NewPortalAccServerSharedkey_len;

    
    return MFD_SUCCESS;
} /* NewPortalAccServerSharedkey_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalRadiusAccServerIPAdd
 * NewPortalRadiusAccServerIPAdd is subid 11 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.11
 * Description:
NewRadiusAccServerIPAdd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalRadiusAccServerIPAdd_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalRadiusAccServerIPAdd_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewPortalRadiusAccServerIPAdd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerIPAdd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewPortalRadiusAccServerIPAdd value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalRadiusAccServerIPAdd value not illegal */
} /* NewPortalRadiusAccServerIPAdd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalRadiusAccServerIPAdd_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerIPAdd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalRadiusAccServerIPAdd undo.
     */
    /*
     * copy NewPortalRadiusAccServerIPAdd data
     * set rowreq_ctx->undo->NewPortalRadiusAccServerIPAdd from rowreq_ctx->data.NewPortalRadiusAccServerIPAdd
     */
    rowreq_ctx->undo->NewPortalRadiusAccServerIPAdd = rowreq_ctx->data.NewPortalRadiusAccServerIPAdd;


    return MFD_SUCCESS;
} /* NewPortalRadiusAccServerIPAdd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalRadiusAccServerIPAdd_val
 *        A u_long containing the new value.
 */
int
NewPortalRadiusAccServerIPAdd_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewPortalRadiusAccServerIPAdd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerIPAdd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NewPortalRadiusAccServerIPAdd value.
     * set NewPortalRadiusAccServerIPAdd value in rowreq_ctx->data
     */
    rowreq_ctx->data.NewPortalRadiusAccServerIPAdd = NewPortalRadiusAccServerIPAdd_val;

    return MFD_SUCCESS;
} /* NewPortalRadiusAccServerIPAdd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalRadiusAccServerIPAdd_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerIPAdd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalRadiusAccServerIPAdd undo.
     */
    /*
     * copy NewPortalRadiusAccServerIPAdd data
     * set rowreq_ctx->data.NewPortalRadiusAccServerIPAdd from rowreq_ctx->undo->NewPortalRadiusAccServerIPAdd
     */
    rowreq_ctx->data.NewPortalRadiusAccServerIPAdd = rowreq_ctx->undo->NewPortalRadiusAccServerIPAdd;

    
    return MFD_SUCCESS;
} /* NewPortalRadiusAccServerIPAdd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalRadiusAccServerPort
 * NewPortalRadiusAccServerPort is subid 12 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.12
 * Description:
NewRadiusAccServerPort.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalRadiusAccServerPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalRadiusAccServerPort_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewPortalRadiusAccServerPort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewPortalRadiusAccServerPort value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalRadiusAccServerPort value not illegal */
} /* NewPortalRadiusAccServerPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalRadiusAccServerPort_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalRadiusAccServerPort undo.
     */
    /*
     * copy NewPortalRadiusAccServerPort data
     * set rowreq_ctx->undo->NewPortalRadiusAccServerPort from rowreq_ctx->data.NewPortalRadiusAccServerPort
     */
    rowreq_ctx->undo->NewPortalRadiusAccServerPort = rowreq_ctx->data.NewPortalRadiusAccServerPort;


    return MFD_SUCCESS;
} /* NewPortalRadiusAccServerPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalRadiusAccServerPort_val
 *        A long containing the new value.
 */
int
NewPortalRadiusAccServerPort_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, long NewPortalRadiusAccServerPort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NewPortalRadiusAccServerPort value.
     * set NewPortalRadiusAccServerPort value in rowreq_ctx->data
     */
    rowreq_ctx->data.NewPortalRadiusAccServerPort = NewPortalRadiusAccServerPort_val;

    return MFD_SUCCESS;
} /* NewPortalRadiusAccServerPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalRadiusAccServerPort_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalRadiusAccServerPort undo.
     */
    /*
     * copy NewPortalRadiusAccServerPort data
     * set rowreq_ctx->data.NewPortalRadiusAccServerPort from rowreq_ctx->undo->NewPortalRadiusAccServerPort
     */
    rowreq_ctx->data.NewPortalRadiusAccServerPort = rowreq_ctx->undo->NewPortalRadiusAccServerPort;

    
    return MFD_SUCCESS;
} /* NewPortalRadiusAccServerPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalRadiusAccServerSharedkey
 * NewPortalRadiusAccServerSharedkey is subid 13 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.13
 * Description:
NewRadiusAccServerSharedkey.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalRadiusAccServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewPortalRadiusAccServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewPortalRadiusAccServerSharedkey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.NewPortalRadiusAccServerSharedkey).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalRadiusAccServerSharedkey_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewPortalRadiusAccServerSharedkey_val_ptr,  size_t NewPortalRadiusAccServerSharedkey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerSharedkey_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewPortalRadiusAccServerSharedkey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid NewPortalRadiusAccServerSharedkey value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NewPortalRadiusServerRowStatus || RS_NOTREADY == rowreq_ctx->data.NewPortalRadiusServerRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NewPortalRadiusAccServerSharedkey value not illegal */
} /* NewPortalRadiusAccServerSharedkey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalRadiusAccServerSharedkey_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerSharedkey_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalRadiusAccServerSharedkey undo.
     */
    /*
     * copy NewPortalRadiusAccServerSharedkey and NewPortalRadiusAccServerSharedkey_len data
     * set rowreq_ctx->undo->NewPortalRadiusAccServerSharedkey from rowreq_ctx->data.NewPortalRadiusAccServerSharedkey
     */
    memcpy( rowreq_ctx->undo->NewPortalRadiusAccServerSharedkey, rowreq_ctx->data.NewPortalRadiusAccServerSharedkey,
            (rowreq_ctx->data.NewPortalRadiusAccServerSharedkey_len * sizeof(rowreq_ctx->undo->NewPortalRadiusAccServerSharedkey[0])));
    rowreq_ctx->undo->NewPortalRadiusAccServerSharedkey_len = rowreq_ctx->data.NewPortalRadiusAccServerSharedkey_len;


    return MFD_SUCCESS;
} /* NewPortalRadiusAccServerSharedkey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalRadiusAccServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewPortalRadiusAccServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewPortalRadiusAccServerSharedkey_val_ptr
 */
int
NewPortalRadiusAccServerSharedkey_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, char *NewPortalRadiusAccServerSharedkey_val_ptr,  size_t NewPortalRadiusAccServerSharedkey_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerSharedkey_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewPortalRadiusAccServerSharedkey_val_ptr);

    /*
     * TODO:461:M: |-> Set NewPortalRadiusAccServerSharedkey value.
     * set NewPortalRadiusAccServerSharedkey value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.NewPortalRadiusAccServerSharedkey, NewPortalRadiusAccServerSharedkey_val_ptr, NewPortalRadiusAccServerSharedkey_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.NewPortalRadiusAccServerSharedkey_len = NewPortalRadiusAccServerSharedkey_val_ptr_len / sizeof(NewPortalRadiusAccServerSharedkey_val_ptr[0]);

    return MFD_SUCCESS;
} /* NewPortalRadiusAccServerSharedkey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalRadiusAccServerSharedkey_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusAccServerSharedkey_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalRadiusAccServerSharedkey undo.
     */
    /*
     * copy NewPortalRadiusAccServerSharedkey and NewPortalRadiusAccServerSharedkey_len data
     * set rowreq_ctx->data.NewPortalRadiusAccServerSharedkey from rowreq_ctx->undo->NewPortalRadiusAccServerSharedkey
     */
    memcpy( rowreq_ctx->data.NewPortalRadiusAccServerSharedkey, rowreq_ctx->undo->NewPortalRadiusAccServerSharedkey,
            (rowreq_ctx->undo->NewPortalRadiusAccServerSharedkey_len * sizeof(rowreq_ctx->data.NewPortalRadiusAccServerSharedkey[0])));
    rowreq_ctx->data.NewPortalRadiusAccServerSharedkey_len = rowreq_ctx->undo->NewPortalRadiusAccServerSharedkey_len;

    
    return MFD_SUCCESS;
} /* NewPortalRadiusAccServerSharedkey_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerEntry.NewPortalRadiusServerRowStatus
 * NewPortalRadiusServerRowStatus is subid 14 of dot11ConfigPortalRadiusAuthServerEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1.1.14
 * Description:
NewRadiusAuthServerRowStatus.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewPortalRadiusServerRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigPortalRadiusAuthServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewPortalRadiusServerRowStatus_check_value( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewPortalRadiusServerRowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusServerRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewPortalRadiusServerRowStatus value.
     */

    return MFD_SUCCESS; /* NewPortalRadiusServerRowStatus value not illegal */
} /* NewPortalRadiusServerRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigPortalRadiusAuthServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewPortalRadiusServerRowStatus_undo_setup( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusServerRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewPortalRadiusServerRowStatus undo.
     */
    /*
     * copy NewPortalRadiusServerRowStatus data
     * set rowreq_ctx->undo->NewPortalRadiusServerRowStatus from rowreq_ctx->data.NewPortalRadiusServerRowStatus
     */
    rowreq_ctx->undo->NewPortalRadiusServerRowStatus = rowreq_ctx->data.NewPortalRadiusServerRowStatus;


    return MFD_SUCCESS;
} /* NewPortalRadiusServerRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewPortalRadiusServerRowStatus_val
 *        A long containing the new value.
 */
int
NewPortalRadiusServerRowStatus_set( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx, u_long NewPortalRadiusServerRowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusServerRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NewPortalRadiusServerRowStatus value.
     * set NewPortalRadiusServerRowStatus value in rowreq_ctx->data
     */
    
    int ret = MFD_SUCCESS;
    
    switch(NewPortalRadiusServerRowStatus_val) {
    
        case RS_ACTIVE:         
        case RS_DESTROY:
            rowreq_ctx->data.NewPortalRadiusServerRowStatus = NewPortalRadiusServerRowStatus_val;
            ret = MFD_SUCCESS;
            break;
    
        case RS_NOTINSERVICE:
        case RS_NOTREADY:
        case RS_CREATEANDGO:
            ret = MFD_ERROR;
            break;
    
        case RS_CREATEANDWAIT:
            if(RS_NONEXISTENT == rowreq_ctx->data.NewPortalRadiusServerRowStatus) {
                global_to_local_ID(&(rowreq_ctx->data.parameter), 
                                    rowreq_ctx->tbl_idx.InstanceId, 
                                    WIRELESS_MAX_NUM);
                rowreq_ctx->data.NewPortalRadiusServerRowStatus = NewPortalRadiusServerRowStatus_val;
                ret = MFD_SUCCESS;
            }
            else {
                ret = MFD_ERROR;
            }
            break;
    
        default:
            snmp_log(LOG_WARNING, "unknow %s in NewPortalRadiusServerRowStatus_set", NewPortalRadiusServerRowStatus_val);
            ret = MFD_ERROR;
            break;
    }

    return ret;
    ;
} /* NewPortalRadiusServerRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewPortalRadiusServerRowStatus_undo( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:NewPortalRadiusServerRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewPortalRadiusServerRowStatus undo.
     */
    /*
     * copy NewPortalRadiusServerRowStatus data
     * set rowreq_ctx->data.NewPortalRadiusServerRowStatus from rowreq_ctx->undo->NewPortalRadiusServerRowStatus
     */
    rowreq_ctx->data.NewPortalRadiusServerRowStatus = rowreq_ctx->undo->NewPortalRadiusServerRowStatus;

    
    return MFD_SUCCESS;
} /* NewPortalRadiusServerRowStatus_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigPortalRadiusAuthServerTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigPortalRadiusAuthServerTable if you don't have dependencies)
 */
int
dot11ConfigPortalRadiusAuthServerTable_check_dependencies(dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigPortalRadiusAuthServerTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & COLUMN_NEWPORTALRADIUSSERVERROWSTATUS_FLAG) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->NewPortalRadiusServerRowStatus,
                                         rowreq_ctx->data.NewPortalRadiusServerRowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_DESTROY ==  rowreq_ctx->data.NewPortalRadiusServerRowStatus) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;
            }
            else if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.NewPortalRadiusServerRowStatus) {
                if ((rowreq_ctx->column_set_flags & DOT11CONFIGPORTALRADIUSAUTHSERVERTABLE_REQUIRED_COLS)
                    != DOT11CONFIGPORTALRADIUSAUTHSERVERTABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11ConfigPortalRadiusAuthServerTable",
                                "required columns missing (0x%0x != 0x%0x)\n",
                                rowreq_ctx->column_set_flags, DOT11CONFIGPORTALRADIUSAUTHSERVERTABLE_REQUIRED_COLS));
                    return MFD_CANNOT_CREATE_NOW;
                }
                rowreq_ctx->data.NewPortalRadiusServerRowStatus = ROWSTATUS_ACTIVE;
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.NewPortalRadiusServerRowStatus) {
                if (rowreq_ctx->column_set_flags & ~COLUMN_NEWPORTALRADIUSSERVERROWSTATUS_FLAG) {
                    DEBUGMSGTL(("dot11ConfigPortalRadiusAuthServerTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;

            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11ConfigPortalRadiusAuthServerTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11ConfigPortalRadiusAuthServerTable_check_dependencies */

/** @} */
