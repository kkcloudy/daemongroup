/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 14170 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigPortalRadiusAuthServerTable.h"


#include "dot11ConfigPortalRadiusAuthServerTable_data_access.h"
#include "nm/app/eag/eag_errcode.h"
#include "nm/app/eag/eag_conf.h"
#include "nm/app/eag/eag_interface.h"
#include "ws_dbus_list_interface.h"
#include "dot11ConfigPortalRadiusAuthServerTable.h"
#include "ws_eag_conf.h"
#include "autelanWtpGroup.h"

/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigPortalRadiusAuthServerTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11ConfigPortalRadiusAuthServerTable is subid 1 of dot11ConfigRadiusServerGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.27.1, length: 12
*/

/**
 * initialization for dot11ConfigPortalRadiusAuthServerTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11ConfigPortalRadiusAuthServerTable_reg
 *        Pointer to dot11ConfigPortalRadiusAuthServerTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11ConfigPortalRadiusAuthServerTable_init_data(dot11ConfigPortalRadiusAuthServerTable_registration * dot11ConfigPortalRadiusAuthServerTable_reg)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11ConfigPortalRadiusAuthServerTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11ConfigPortalRadiusAuthServerTable_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11ConfigPortalRadiusAuthServerTable_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to dot11ConfigPortalRadiusAuthServerTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to dot11ConfigPortalRadiusAuthServerTable_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up dot11ConfigPortalRadiusAuthServerTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11CONFIGPORTALRADIUSAUTHSERVERTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11ConfigPortalRadiusAuthServerTable_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before dot11ConfigPortalRadiusAuthServerTable_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
dot11ConfigPortalRadiusAuthServerTable_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to dot11ConfigPortalRadiusAuthServerTable_container_shutdown\n");
        return;
    }

} /* dot11ConfigPortalRadiusAuthServerTable_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement dot11ConfigPortalRadiusAuthServerTable data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11ConfigPortalRadiusAuthServerTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11ConfigPortalRadiusAuthServerTable_container_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11ConfigPortalRadiusAuthServerTable_container_load\n");
	
    dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * InstanceId(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   InstanceId;

    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_container_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
      //  return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11ConfigPortalRadiusAuthServerTable container.
     * loop over your dot11ConfigPortalRadiusAuthServerTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    instance_parameter *paraHead = NULL;
	instance_parameter *pq = NULL;		
	int ret;
	struct radius_conf radiusconf;
	int i;

	snmp_log(LOG_DEBUG, "enter list_instance_parameter\n");
	list_instance_parameter(&paraHead, SNMPD_INSTANCE_MASTER); 
	snmp_log(LOG_DEBUG, "exit list_instance_parameter,paraHead=%p\n", paraHead);
	for(pq = paraHead; (NULL != pq); pq = pq->next)
	{		
		memset( &radiusconf, 0, sizeof(struct radius_conf) );			
		ret = eag_get_radius_conf( pq->connection,pq->parameter.local_id,pq->parameter.instance_id,"",&radiusconf );
		if(EAG_RETURN_OK == ret)
		{
			for( i=0; i<radiusconf.current_num; i++ )
		    {
				InstanceId = local_to_global_ID(pq->parameter, i+1, WIRELESS_MAX_NUM);
				
		    /*
		    ***************************************************
		    ***             START EXAMPLE CODE              ***
		    ***---------------------------------------------***/
		    /*
		     * get a line (skip blank lines)
		     */
		    //do {
		      //  if (!fgets(line, sizeof(line), filep)) {
		            /* we're done */
		        //    fclose(filep);
		          //  filep = NULL;
		        //}
		   // } while (filep && (line[0] == '\n'));

		    /*
		     * check for end of data
		     */
		    //if(NULL == filep)
		      //  break;

		    /*
		     * parse line into variables
		     */
		    /*
		    ***---------------------------------------------***
		    ***              END  EXAMPLE CODE              ***
		    ***************************************************/

		        /*
		         * TODO:352:M: |   |-> set indexes in new dot11ConfigPortalRadiusAuthServerTable rowreq context.
		         * data context will be set from the param (unless NULL,
		         *      in which case a new data context will be allocated)
		         */
		        rowreq_ctx = dot11ConfigPortalRadiusAuthServerTable_allocate_rowreq_ctx(NULL);
		        if (NULL == rowreq_ctx) {
		            snmp_log(LOG_ERR, "memory allocation failed\n");
					free_instance_parameter_list(&paraHead);
		            return MFD_RESOURCE_UNAVAILABLE;
		        }
		        if(MFD_SUCCESS != dot11ConfigPortalRadiusAuthServerTable_indexes_set(rowreq_ctx
		                               , InstanceId
		               )) {
		            snmp_log(LOG_ERR,"error setting index while loading "
		                     "dot11ConfigPortalRadiusAuthServerTable data.\n");
		            dot11ConfigPortalRadiusAuthServerTable_release_rowreq_ctx(rowreq_ctx);
		            continue;
		        }

				memcpy(&(rowreq_ctx->data.parameter), &(pq->parameter), sizeof(rowreq_ctx->data.parameter));
		        /*
		         * TODO:352:r: |   |-> populate dot11ConfigPortalRadiusAuthServerTable data context.
		         * Populate data context here. (optionally, delay until row prep)
		         */
		    /*
		     * TRANSIENT or semi-TRANSIENT data:
		     * copy data or save any info needed to do it in row_prep.
		     */
		    /*
		     * setup/save data for PortalRadiusAuthDomain
		     * PortalRadiusAuthDomain(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H
		     */
		    /** no mapping */
		    /*
		     * make sure there is enough space for PortalRadiusAuthDomain data
		     */
		    /*if ((NULL == rowreq_ctx->data.PortalRadiusAuthDomain) ||
		        (rowreq_ctx->data.PortalRadiusAuthDomain_len <
		         (PortalRadiusAuthDomain_len* sizeof(PortalRadiusAuthDomain[0])))) {
		        snmp_log(LOG_ERR,"not enough space for value\n");
		        return MFD_ERROR;
		    }*/
		    rowreq_ctx->data.PortalRadiusAuthDomain_len = MIN(sizeof(rowreq_ctx->data.PortalRadiusAuthDomain) - 1, strlen(radiusconf.radius_srv[i].domain));
		    memcpy( rowreq_ctx->data.PortalRadiusAuthDomain, radiusconf.radius_srv[i].domain, rowreq_ctx->data.PortalRadiusAuthDomain_len );
		    
		    /*
		     * setup/save data for NewPortalAuthServerIPAdd
		     * NewPortalAuthServerIPAdd(2)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.NewPortalAuthServerIPAdd = radiusconf.radius_srv[i].auth_ip;
		    
		    /*
		     * setup/save data for NewPortalAuthServerPort
    		     * NewPortalAuthServerPort(3)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.NewPortalAuthServerPort = radiusconf.radius_srv[i].auth_port;
		    
		    /*
		     * setup/save data for NewPortalAuthServerSharedkey
		     * NewPortalAuthServerSharedkey(4)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H
		     */
		    /** no mapping */
		    /*
		     * make sure there is enough space for NewPortalAuthServerSharedkey data
		     */
		    /*if ((NULL == rowreq_ctx->data.NewPortalAuthServerSharedkey) ||
		        (rowreq_ctx->data.NewPortalAuthServerSharedkey_len <
		         (NewPortalAuthServerSharedkey_len* sizeof(NewPortalAuthServerSharedkey[0])))) {
		        snmp_log(LOG_ERR,"not enough space for value\n");
		        return MFD_ERROR;
		    }*/
		    rowreq_ctx->data.NewPortalAuthServerSharedkey_len = MIN(sizeof(rowreq_ctx->data.NewPortalAuthServerSharedkey) - 1, strlen(radiusconf.radius_srv[i].auth_secret));
		    memcpy( rowreq_ctx->data.NewPortalAuthServerSharedkey, radiusconf.radius_srv[i].auth_secret, rowreq_ctx->data.NewPortalAuthServerSharedkey_len );
		    
		    /*
		     * setup/save data for NewPortalRadiusAuthServerIPAdd
		     * NewPortalRadiusAuthServerIPAdd(5)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.NewPortalRadiusAuthServerIPAdd = radiusconf.radius_srv[i].backup_auth_ip;
		    
		    /*
		     * setup/save data for NewPortalRadiusAuthServerPort
     		     * NewPortalRadiusAuthServerPort(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.NewPortalRadiusAuthServerPort = radiusconf.radius_srv[i].backup_auth_port;
		    
		    /*
		     * setup/save data for NewPortalRadiusAuthServerSharedkey
		     * NewPortalRadiusAuthServerSharedkey(7)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H
		     */
		    /** no mapping */
		    /*
		     * make sure there is enough space for NewPortalRadiusAuthServerSharedkey data
		     */
		    /*if ((NULL == rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey) ||
		        (rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey_len <
		         (NewPortalRadiusAuthServerSharedkey_len* sizeof(NewPortalRadiusAuthServerSharedkey[0])))) {
		        snmp_log(LOG_ERR,"not enough space for value\n");
		        return MFD_ERROR;
		    }*/
		    rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey_len = MIN(sizeof(rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey) - 1, strlen(radiusconf.radius_srv[i].backup_auth_secret));
		    memcpy( rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey, radiusconf.radius_srv[i].backup_auth_secret, rowreq_ctx->data.NewPortalRadiusAuthServerSharedkey_len );
		    
		    /*
		     * setup/save data for NewPortalAccServerIPAdd
		     * NewPortalAccServerIPAdd(8)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.NewPortalAccServerIPAdd = radiusconf.radius_srv[i].acct_ip;
		    
		    /*
		     * setup/save data for NewPortalAccServerPort
		     * NewPortalAccServerPort(9)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.NewPortalAccServerPort = radiusconf.radius_srv[i].acct_port;
		    
		    /*
		     * setup/save data for NewPortalAccServerSharedkey
		     * NewPortalAccServerSharedkey(10)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H
		     */
		    /** no mapping */
		    /*
		     * make sure there is enough space for NewPortalAccServerSharedkey data
		     */
		    /*if ((NULL == rowreq_ctx->data.NewPortalAccServerSharedkey) ||
		        (rowreq_ctx->data.NewPortalAccServerSharedkey_len <
		         (NewPortalAccServerSharedkey_len* sizeof(NewPortalAccServerSharedkey[0])))) {
		        snmp_log(LOG_ERR,"not enough space for value\n");
		        return MFD_ERROR;
		    }*/
		    rowreq_ctx->data.NewPortalAccServerSharedkey_len = MIN(sizeof(rowreq_ctx->data.NewPortalAccServerSharedkey) - 1, strlen(radiusconf.radius_srv[i].acct_secret));
		    memcpy( rowreq_ctx->data.NewPortalAccServerSharedkey, radiusconf.radius_srv[i].acct_secret, rowreq_ctx->data.NewPortalAccServerSharedkey_len );
		    
		    /*
		     * setup/save data for NewPortalRadiusAccServerIPAdd
		     * NewPortalRadiusAccServerIPAdd(11)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/W/e/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.NewPortalRadiusAccServerIPAdd = radiusconf.radius_srv[i].backup_acct_ip;
		    
		    /*
		     * setup/save data for NewPortalRadiusAccServerPort
 		     * NewPortalRadiusAccServerPort(12)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.NewPortalRadiusAccServerPort = radiusconf.radius_srv[i].backup_acct_port;
		    
		    /*
		     * setup/save data for NewPortalRadiusAccServerSharedkey
		     * NewPortalRadiusAccServerSharedkey(13)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H
		     */
		    /** no mapping */
		    /*
		     * make sure there is enough space for NewPortalRadiusAccServerSharedkey data
		     */
		    /*if ((NULL == rowreq_ctx->data.NewPortalRadiusAccServerSharedkey) ||
		        (rowreq_ctx->data.NewPortalRadiusAccServerSharedkey_len <
		         (NewPortalRadiusAccServerSharedkey_len* sizeof(NewPortalRadiusAccServerSharedkey[0])))) {
		        snmp_log(LOG_ERR,"not enough space for value\n");
		        return MFD_ERROR;
		    }*/
		    rowreq_ctx->data.NewPortalRadiusAccServerSharedkey_len = MIN(sizeof(rowreq_ctx->data.NewPortalRadiusAccServerSharedkey) - 1, strlen(radiusconf.radius_srv[i].backup_acct_secret));
		    memcpy( rowreq_ctx->data.NewPortalRadiusAccServerSharedkey, radiusconf.radius_srv[i].backup_acct_secret, rowreq_ctx->data.NewPortalRadiusAccServerSharedkey_len );
		    
		    /*
		     * setup/save data for NewPortalRadiusServerRowStatus
		     * NewPortalRadiusServerRowStatus(14)/RowStatus/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
		     */
		    /** no mapping */
		    rowreq_ctx->data.NewPortalRadiusServerRowStatus = RS_ACTIVE;
		    
		        
		        /*
		         * insert into table container
		         */
		        if(CONTAINER_INSERT(container, rowreq_ctx))
		        {
		        	dot11ConfigPortalRadiusAuthServerTable_release_rowreq_ctx(rowreq_ctx);
		        }
		        ++count;
		    }		
		}
	}
	free_instance_parameter_list(&paraHead);


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
        //fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_container_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11ConfigPortalRadiusAuthServerTable_container_load\n");
	
    return MFD_SUCCESS;
} /* dot11ConfigPortalRadiusAuthServerTable_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11ConfigPortalRadiusAuthServerTable_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_container_free","called\n"));

    /*
     * TODO:380:M: Free dot11ConfigPortalRadiusAuthServerTable container data.
     */
} /* dot11ConfigPortalRadiusAuthServerTable_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11ConfigPortalRadiusAuthServerTable_row_prep( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11ConfigPortalRadiusAuthServerTable_row_prep */

/*
 * TODO:420:r: Implement dot11ConfigPortalRadiusAuthServerTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigEagServerGroupEntry.InstanceId
 * InstanceId is subid 1 of dot11ConfigEagServerGroupEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.23.4.1.1
 * Description:
eag instance index.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * check validity of InstanceId external index portion
 *
 * NOTE: this is not the place to do any checks for the sanity
 *       of multiple indexes. Those types of checks should be done in the
 *       dot11ConfigPortalRadiusAuthServerTable_validate_index() function.
 *
 * @retval MFD_SUCCESS   : the incoming value is legal
 * @retval MFD_ERROR     : the incoming value is NOT legal
 */
int
dot11ConfigPortalRadiusAuthServerTable_InstanceId_check_index( dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx )
{
    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_InstanceId_check_index","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:424:M: |-> Check dot11ConfigPortalRadiusAuthServerTable external index InstanceId.
     * check that index value in the table context (rowreq_ctx)
     * for the external index InstanceId is legal.
     */

    return MFD_SUCCESS; /*  external index InstanceId ok */
} /* dot11ConfigPortalRadiusAuthServerTable_InstanceId_check_index */

/**
 * verify specified index is valid.
 *
 * This check is independent of whether or not the values specified for
 * the columns of the new row are valid. Column values and row consistency
 * will be checked later. At this point, only the index values should be
 * checked.
 *
 * All of the individual index validation functions have been called, so this
 * is the place to make sure they are valid as a whole when combined. If
 * you only have one index, then you probably don't need to do anything else
 * here.
 * 
 * @note Keep in mind that if the indexes refer to a row in this or
 *       some other table, you can't check for that row here to make
 *       decisions, since that row might not be created yet, but may
 *       be created during the processing this request. If you have
 *       such checks, they should be done in the check_dependencies
 *       function, because any new/deleted/changed rows should be
 *       available then.
 *
 *
 * @param dot11ConfigPortalRadiusAuthServerTable_reg
 *        Pointer to the user registration data
 * @param dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx
 *        Pointer to the users context.
 * @retval MFD_SUCCESS            : success
 * @retval MFD_CANNOT_CREATE_NOW  : index not valid right now
 * @retval MFD_CANNOT_CREATE_EVER : index never valid
 */
int
dot11ConfigPortalRadiusAuthServerTable_validate_index( dot11ConfigPortalRadiusAuthServerTable_registration * dot11ConfigPortalRadiusAuthServerTable_reg,
                           dot11ConfigPortalRadiusAuthServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigPortalRadiusAuthServerTable:dot11ConfigPortalRadiusAuthServerTable_validate_index","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:430:M: |-> Validate potential dot11ConfigPortalRadiusAuthServerTable index.
     */
    if(0) {
        snmp_log(LOG_WARNING,"invalid index for a new row in the "
                 "dot11ConfigPortalRadiusAuthServerTable table.\n");
        /*
         * determine failure type.
         *
         * If the index could not ever be created, return MFD_NOT_EVER
         * If the index can not be created under the present circumstances
         * (even though it could be created under other circumstances),
         * return MFD_NOT_NOW.
         */
        if(0) {
            return MFD_CANNOT_CREATE_EVER;
        }
        else {
            return MFD_CANNOT_CREATE_NOW;
        }
    }

    return rc;
} /* dot11ConfigPortalRadiusAuthServerTable_validate_index */

/** @} */
