/*
 * Note: this file originally auto-generated by mib2c using
 *  : mib2c.iterate.conf,v 5.14.2.2 2005/05/09 08:13:01 dts12 Exp $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "dot11WtpBssIDNumTable.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "ws_sysinfo.h"
#include "ws_init_dbus.h"
#include "autelanWtpGroup.h"
#include "ws_dbus_list_interface.h"

#define BSSIDNUM "1.14.1"
struct dot11WtpBssIDNumTable_entry {
    /* Index values */
	dbus_parameter parameter;
    long wtpID;
    long radioCount;
    /* Column values */
    u_long BssIDTatolNum;

    /* Illustrate using a simple linked list */
    int   valid;
    struct dot11WtpBssIDNumTable_entry *next;
};

void dot11WtpBssIDNumTable_load();
void dot11WtpBssIDNumTable_removeEntry( struct dot11WtpBssIDNumTable_entry *entry );

/** Initializes the dot11WtpBssIDNumTable module */
void
init_dot11WtpBssIDNumTable(void)
{
  /* here we initialize all the tables we're planning on supporting */
    initialize_table_dot11WtpBssIDNumTable();
}

/** Initialize the dot11WtpBssIDNumTable table by defining its contents and how it's structured */
void
initialize_table_dot11WtpBssIDNumTable(void)
{   
    static oid dot11WtpBssIDNumTable_oid[128] = {0};
    size_t dot11WtpBssIDNumTable_oid_len   = 0;
	mad_dev_oid(dot11WtpBssIDNumTable_oid,BSSIDNUM,&dot11WtpBssIDNumTable_oid_len,enterprise_pvivate_oid);
    netsnmp_handler_registration    *reg;
    netsnmp_iterator_info           *iinfo;
    netsnmp_table_registration_info *table_info;
    reg = netsnmp_create_handler_registration(
              "dot11WtpBssIDNumTable",     dot11WtpBssIDNumTable_handler,
              dot11WtpBssIDNumTable_oid, dot11WtpBssIDNumTable_oid_len,
              HANDLER_CAN_RWRITE
              );
    table_info = SNMP_MALLOC_TYPEDEF( netsnmp_table_registration_info );
    netsnmp_table_helper_add_indexes(table_info,
                           ASN_INTEGER,  /* index: wtpID */
                           0);
    table_info->min_column = BSSIDNUMMIN;
    table_info->max_column = BSSIDNUMMAX;
    
    iinfo = SNMP_MALLOC_TYPEDEF( netsnmp_iterator_info );
    iinfo->get_first_data_point = dot11WtpBssIDNumTable_get_first_data_point;
    iinfo->get_next_data_point  = dot11WtpBssIDNumTable_get_next_data_point;
    iinfo->table_reginfo        = table_info;
    
    netsnmp_register_table_iterator( reg, iinfo );

	netsnmp_inject_handler(reg,netsnmp_get_cache_handler(DOT1DTPFDBTABLE_CACHE_TIMEOUT,dot11WtpBssIDNumTable_load, dot11WtpBssIDNumTable_removeEntry,
							dot11WtpBssIDNumTable_oid, dot11WtpBssIDNumTable_oid_len));

    /* Initialise the contents of the table here */
}

    /* Typical data structure for a row entry */

struct dot11WtpBssIDNumTable_entry  *dot11WtpBssIDNumTable_head;

/* create a new row in the (unsorted) table */
struct dot11WtpBssIDNumTable_entry *
dot11WtpBssIDNumTable_createEntry(
				 dbus_parameter parameter,
                 long  wtpID,
                 long radioCount,
                 u_long BssIDTatolNum
                ) {
    struct dot11WtpBssIDNumTable_entry *entry;

    entry = SNMP_MALLOC_TYPEDEF(struct dot11WtpBssIDNumTable_entry);
    if (!entry)
        return NULL;

	memcpy(&entry->parameter, &parameter, sizeof(dbus_parameter));
    entry->wtpID = wtpID;
	entry->radioCount = radioCount;
	entry->BssIDTatolNum = BssIDTatolNum;
    entry->next = dot11WtpBssIDNumTable_head;
    dot11WtpBssIDNumTable_head = entry;
    return entry;
}

/* remove a row from the table */
void
dot11WtpBssIDNumTable_removeEntry( struct dot11WtpBssIDNumTable_entry *entry ) {
    struct dot11WtpBssIDNumTable_entry *ptr, *prev;

    if (!entry)
        return;    /* Nothing to remove */

    for ( ptr  = dot11WtpBssIDNumTable_head, prev = NULL;
          ptr != NULL;
          prev = ptr, ptr = ptr->next ) {
        if ( ptr == entry )
            break;
    }
    if ( !ptr )
        return;    /* Can't find it */

    if ( prev == NULL )
        dot11WtpBssIDNumTable_head = ptr->next;
    else
        prev->next = ptr->next;

    SNMP_FREE( entry );   /* XXX - release any other internal resources */
}


void dot11WtpBssIDNumTable_load()
{
	snmp_log(LOG_DEBUG, "enter dot11WtpBssIDNumTable_load\n");

	struct dot11WtpBssIDNumTable_entry *temp = NULL;
	while( dot11WtpBssIDNumTable_head )
	{
		temp=dot11WtpBssIDNumTable_head->next;
		dot11WtpBssIDNumTable_removeEntry(dot11WtpBssIDNumTable_head);
		dot11WtpBssIDNumTable_head=temp;
	}
	
	snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_wtp_list_by_mac_cmd_func\n");
    messageHead = list_connection_call_dbus_method(show_wtp_list_by_mac_cmd_func, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_wtp_list_by_mac_cmd_func,messageHead=%p\n", messageHead);

	
	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    DCLI_WTP_API_GROUP_ONE *head = messageNode->message;
    		if((head)&&(head->WTP_INFO)&&(head->WTP_INFO->WTP_LIST))
    		{
    		    void *connection = NULL;
                if(SNMPD_DBUS_ERROR == get_slot_dbus_connection(messageNode->parameter.slot_id, &connection, SNMPD_INSTANCE_MASTER_V3))
                    continue;
                
                int i = 0;
                WID_WTP *q = NULL;
    			for(i = 0, q = head->WTP_INFO->WTP_LIST; (i < head->WTP_INFO->list_len)&&(NULL != q); i++, q = q->next)
    			{
    			    unsigned long AllBSSIDNum = 0;
    			    unsigned long wtpid = local_to_global_ID(messageNode->parameter, 
    			                                             q->WTPID, 
    			                                             WIRELESS_MAX_NUM);
    			    
    			    int j = 0;
    			    for (j = 0; j < q->RadioCount; j++)
    			    {
                        int ret = 0;  
    			        unsigned int radio_id = q->WTPID*4 + j;
    			        DCLI_RADIO_API_GROUP_ONE  *rad = NULL;
    					ret=show_radio_one(messageNode->parameter, connection,radio_id,&rad);
    					if(ret == 1)
    					{
    						AllBSSIDNum += rad->bss_num;
    						
    						Free_radio_one_head(rad);
    					}
    					else if(SNMPD_CONNECTION_ERROR == ret) {
                            close_slot_dbus_connection(messageNode->parameter.slot_id);
                            break;
                	    }
    			    }
    			    
					dot11WtpBssIDNumTable_createEntry(messageNode->parameter,
													  wtpid,	
						                              q->RadioCount,
						                              AllBSSIDNum);
					FREE_OBJECT(q->WTPMAC);
    			}
    		}
    	}
    	free_dbus_message_list(&messageHead, Free_wtp_list_by_mac_head);
	}
	
	snmp_log(LOG_DEBUG, "exit dot11WtpBssIDNumTable_load\n");

}

/* Example iterator hook routines - using 'get_next' to do most of the work */
netsnmp_variable_list *
dot11WtpBssIDNumTable_get_first_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{   
    if (dot11WtpBssIDNumTable_head == NULL)
			return NULL;
    *my_loop_context = dot11WtpBssIDNumTable_head;
    return dot11WtpBssIDNumTable_get_next_data_point(my_loop_context, my_data_context,
                                    put_index_data,  mydata );
}

netsnmp_variable_list *
dot11WtpBssIDNumTable_get_next_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    struct dot11WtpBssIDNumTable_entry *entry = (struct dot11WtpBssIDNumTable_entry *)*my_loop_context;
    netsnmp_variable_list *idx = put_index_data;

    if ( entry ) {
        snmp_set_var_value( idx, (u_char*)&entry->wtpID, sizeof(entry->wtpID) );
        idx = idx->next_variable;
        *my_data_context = (void *)entry;
        *my_loop_context = (void *)entry->next;
    } else {
        return NULL;
    }
	return put_index_data;
}


/** handles requests for the dot11WtpBssIDNumTable table */
int
dot11WtpBssIDNumTable_handler(
    netsnmp_mib_handler               *handler,
    netsnmp_handler_registration      *reginfo,
    netsnmp_agent_request_info        *reqinfo,
    netsnmp_request_info              *requests) {

    netsnmp_request_info       *request;
    netsnmp_table_request_info *table_info;
    struct dot11WtpBssIDNumTable_entry          *table_entry;

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:
        for (request=requests; request; request=request->next) {
            table_entry = (struct dot11WtpBssIDNumTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);

		 if( !table_entry ){
        		netsnmp_set_request_error(reqinfo,request,SNMP_NOSUCHINSTANCE);
				continue;
	    	}   
            switch (table_info->colnum) {
            case COLUMN_WTPID:
                snmp_set_var_typed_value( request->requestvb, ASN_INTEGER,
                                          (u_char*)&table_entry->wtpID,
                                          sizeof(table_entry->wtpID));
                break;
            case COLUMN_BSSIDTATOLNUM:
              {
                snmp_set_var_typed_value( request->requestvb, ASN_INTEGER,
                                          (u_char*)&table_entry->BssIDTatolNum,
                                          sizeof(table_entry->BssIDTatolNum));
             }
                break;
            }
        }
        break;

        /*
         * Write-support
         */
    case MODE_SET_RESERVE1:
        for (request=requests; request; request=request->next) {
            table_entry = (struct dot11WtpBssIDNumTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
    
            switch (table_info->colnum) {
            case COLUMN_WTPID:
                if ( request->requestvb->type != ASN_INTEGER ) {
                    netsnmp_set_request_error( reqinfo, request,
                                               SNMP_ERR_WRONGTYPE );
                    return SNMP_ERR_NOERROR;
                }
                /* Also may need to check size/value */
                break;
            default:
                netsnmp_set_request_error( reqinfo, request,
                                           SNMP_ERR_NOTWRITABLE );
                return SNMP_ERR_NOERROR;
            }
        }
        break;

    case MODE_SET_RESERVE2:

        break;

    case MODE_SET_FREE:
        break;

    case MODE_SET_ACTION:
        for (request=requests; request; request=request->next) {
            table_entry = (struct dot11WtpBssIDNumTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);

			 if( !table_entry ){
        		netsnmp_set_request_error(reqinfo,request,SNMP_NOSUCHINSTANCE);
				continue;
	    	}   
            switch (table_info->colnum) {
            case COLUMN_WTPID:
                /* Need to save old 'table_entry->wtpID' value.
                   May need to use 'memcpy' */
                break;
            }
        }
        break;

    case MODE_SET_UNDO:
        for (request=requests; request; request=request->next) {
            table_entry = (struct dot11WtpBssIDNumTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
    
            switch (table_info->colnum) {
            case COLUMN_WTPID:
                break;
            }
        }
        break;

    case MODE_SET_COMMIT:
        break;
    }
    return SNMP_ERR_NOERROR;
}
