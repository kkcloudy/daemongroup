/*
 * Note: this file originally auto-generated by mib2c using
 *  : mib2c.iterate.conf,v 5.14.2.2 2005/05/09 08:13:01 dts12 Exp $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "dot11NewWtpVersionFileUpdateTable.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "autelanWtpGroup.h"
#include "ws_init_dbus.h"
#include <stdio.h>
#include "mibs_public.h"
#include "ws_dbus_list_interface.h"

#define NEWVEROID	"1.1.7"
struct dot11NewWtpVersionFileUpdateTable_entry {
    /* Index values */
    char *wtpMacAddr;

    /* Column values */
    char *setWtpVersionFile;
	dbus_parameter parameter;
    long wtpCurrID;

    /* Illustrate using a simple linked list */
    int   valid;
    struct dot11NewWtpVersionFileUpdateTable_entry *next;
};
void dot11NewWtpVersionFileUpdateTable_load();

void
dot11NewWtpVersionFileUpdateTable_removeEntry( struct dot11NewWtpVersionFileUpdateTable_entry *entry );

/** Initializes the dot11NewWtpVersionFileUpdateTable module */
void
init_dot11NewWtpVersionFileUpdateTable(void)
{
  /* here we initialize all the tables we're planning on supporting */
    initialize_table_dot11NewWtpVersionFileUpdateTable();
}

/** Initialize the dot11NewWtpVersionFileUpdateTable table by defining its contents and how it's structured */
void
initialize_table_dot11NewWtpVersionFileUpdateTable(void)
{
    static oid dot11NewWtpVersionFileUpdateTable_oid[128] = {0};
    size_t dot11NewWtpVersionFileUpdateTable_oid_len   = 0;
	mad_dev_oid(dot11NewWtpVersionFileUpdateTable_oid,NEWVEROID,&dot11NewWtpVersionFileUpdateTable_oid_len,enterprise_pvivate_oid);
    netsnmp_handler_registration    *reg;
    netsnmp_iterator_info           *iinfo;
    netsnmp_table_registration_info *table_info;

    reg = netsnmp_create_handler_registration(
              "dot11NewWtpVersionFileUpdateTable",     dot11NewWtpVersionFileUpdateTable_handler,
              dot11NewWtpVersionFileUpdateTable_oid, dot11NewWtpVersionFileUpdateTable_oid_len,
              HANDLER_CAN_RWRITE
              );

    table_info = SNMP_MALLOC_TYPEDEF( netsnmp_table_registration_info );
    netsnmp_table_helper_add_indexes(table_info,
                           ASN_OCTET_STR,  /* index: wtpMacAddr */
                           0);
    table_info->min_column = NEWVERMIN;
    table_info->max_column = NEWVERMAX;
    
    iinfo = SNMP_MALLOC_TYPEDEF( netsnmp_iterator_info );
    iinfo->get_first_data_point = dot11NewWtpVersionFileUpdateTable_get_first_data_point;
    iinfo->get_next_data_point  = dot11NewWtpVersionFileUpdateTable_get_next_data_point;
    iinfo->table_reginfo        = table_info;
    
    netsnmp_register_table_iterator( reg, iinfo );
	netsnmp_inject_handler(reg,netsnmp_get_cache_handler(DOT1DTPFDBTABLE_CACHE_TIMEOUT,dot11NewWtpVersionFileUpdateTable_load, dot11NewWtpVersionFileUpdateTable_removeEntry,
							dot11NewWtpVersionFileUpdateTable_oid, dot11NewWtpVersionFileUpdateTable_oid_len));

    /* Initialise the contents of the table here */
}

    /* Typical data structure for a row entry */

struct dot11NewWtpVersionFileUpdateTable_entry  *dot11NewWtpVersionFileUpdateTable_head;

/* create a new row in the (unsorted) table */
struct dot11NewWtpVersionFileUpdateTable_entry *
dot11NewWtpVersionFileUpdateTable_createEntry(
                char  *wtpMacAddr,
                char *setWtpVersionFile,
				dbus_parameter parameter,
				long wtpCurrID
                ) {
    struct dot11NewWtpVersionFileUpdateTable_entry *entry;

    entry = SNMP_MALLOC_TYPEDEF(struct dot11NewWtpVersionFileUpdateTable_entry);
    if (!entry)
        return NULL;
	memcpy(&(entry->parameter), &parameter, sizeof(dbus_parameter));
    entry->wtpCurrID = wtpCurrID;
    entry->wtpMacAddr = strdup(wtpMacAddr);
	entry->setWtpVersionFile = strdup(setWtpVersionFile);
    entry->next = dot11NewWtpVersionFileUpdateTable_head;
    dot11NewWtpVersionFileUpdateTable_head = entry;
    return entry;
}

/* remove a row from the table */
void
dot11NewWtpVersionFileUpdateTable_removeEntry( struct dot11NewWtpVersionFileUpdateTable_entry *entry ) {
    struct dot11NewWtpVersionFileUpdateTable_entry *ptr, *prev;

    if (!entry)
        return;    /* Nothing to remove */

    for ( ptr  = dot11NewWtpVersionFileUpdateTable_head, prev = NULL;
          ptr != NULL;
          prev = ptr, ptr = ptr->next ) {
        if ( ptr == entry )
            break;
    }
    if ( !ptr )
        return;    /* Can't find it */

    if ( prev == NULL )
        dot11NewWtpVersionFileUpdateTable_head = ptr->next;
    else
        prev->next = ptr->next;
    FREE_OBJECT(entry->wtpMacAddr);
    FREE_OBJECT(entry->setWtpVersionFile);
    SNMP_FREE( entry );   /* XXX - release any other internal resources */
}

void dot11NewWtpVersionFileUpdateTable_load()
{
	snmp_log(LOG_DEBUG, "enter dot11NewWtpVersionFileUpdateTable_load\n");
	
    char temp_mac[20] = {0};
    
    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_wtp_list_by_mac_cmd_func\n");
    messageHead = list_connection_call_dbus_method(show_wtp_list_by_mac_cmd_func, SHOW_ALL_WTP_TABLE_METHOD);
    snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_wtp_list_by_mac_cmd_func,messageHead=%p\n", messageHead);
	
	if(messageHead)
	{
	    for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
        {
            DCLI_WTP_API_GROUP_ONE *head = messageNode->message;
            if((head)&&(head->WTP_INFO)&&(head->WTP_INFO->WTP_LIST))
        	{
        	    int i = 0;
        	    WID_WTP *q = NULL;
        		for(i = 0,q = head->WTP_INFO->WTP_LIST; (i < head->WTP_INFO->list_len)&&(NULL != q); i++,q=q->next)
        		{
        			memset(temp_mac,0,20);
					if(q->WTPMAC)
					{
						snprintf(temp_mac,sizeof(temp_mac)-1,"%02X:%02X:%02X:%02X:%02X:%02X",q->WTPMAC[0],q->WTPMAC[1],q->WTPMAC[2],q->WTPMAC[3],q->WTPMAC[4],q->WTPMAC[5]);
					}

        			dot11NewWtpVersionFileUpdateTable_createEntry(temp_mac,
        														"",
        														messageNode->parameter,
        														q->WTPID); 	
					FREE_OBJECT(q->WTPMAC);
        		}
        	}
    	}
        free_dbus_message_list(&messageHead, Free_wtp_list_by_mac_head);
    }

	snmp_log(LOG_DEBUG, "exit dot11NewWtpVersionFileUpdateTable_load\n");

}
/* Example iterator hook routines - using 'get_next' to do most of the work */
netsnmp_variable_list *
dot11NewWtpVersionFileUpdateTable_get_first_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{

		if(dot11NewWtpVersionFileUpdateTable_head==NULL)
		{
			return NULL;
		}

	*my_loop_context = dot11NewWtpVersionFileUpdateTable_head;		
	*my_data_context =   dot11NewWtpVersionFileUpdateTable_head;


    return dot11NewWtpVersionFileUpdateTable_get_next_data_point(my_loop_context, my_data_context,
                                    put_index_data,  mydata );
}

netsnmp_variable_list *
dot11NewWtpVersionFileUpdateTable_get_next_data_point(void **my_loop_context,
                          void **my_data_context,
                          netsnmp_variable_list *put_index_data,
                          netsnmp_iterator_info *mydata)
{
    struct dot11NewWtpVersionFileUpdateTable_entry *entry = (struct dot11NewWtpVersionFileUpdateTable_entry *)*my_loop_context;
    netsnmp_variable_list *idx = put_index_data;

    if ( entry ) {
        snmp_set_var_value( idx, (u_char*)entry->wtpMacAddr, strlen(entry->wtpMacAddr) );
        idx = idx->next_variable;
        *my_data_context = (void *)entry;
        *my_loop_context = (void *)entry->next;
    } else {
        return NULL;
    }
	return put_index_data;
}


/** handles requests for the dot11NewWtpVersionFileUpdateTable table */
int
dot11NewWtpVersionFileUpdateTable_handler(
    netsnmp_mib_handler               *handler,
    netsnmp_handler_registration      *reginfo,
    netsnmp_agent_request_info        *reqinfo,
    netsnmp_request_info              *requests) {

    netsnmp_request_info       *request;
    netsnmp_table_request_info *table_info;
    struct dot11NewWtpVersionFileUpdateTable_entry          *table_entry;

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:
        for (request=requests; request; request=request->next) {
            table_entry = (struct dot11NewWtpVersionFileUpdateTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
			
    		if( !table_entry ){
				netsnmp_set_request_error(reqinfo,request,SNMP_NOSUCHINSTANCE);
				continue;
			}	 
            switch (table_info->colnum) {
            case COLUMN_SETWTPVERSIONFILE:
                snmp_set_var_typed_value( request->requestvb, ASN_OCTET_STR,
                                          (u_char*)table_entry->setWtpVersionFile,
                                          strlen(table_entry->setWtpVersionFile));
                break;
            }
        }
        break;

        /*
         * Write-support
         */
    case MODE_SET_RESERVE1:
        for (request=requests; request; request=request->next) {
            table_entry = (struct dot11NewWtpVersionFileUpdateTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
    
            switch (table_info->colnum) {
            case COLUMN_SETWTPVERSIONFILE:
                if ( request->requestvb->type != ASN_OCTET_STR ) {
                    netsnmp_set_request_error( reqinfo, request,
                                               SNMP_ERR_WRONGTYPE );
                    return SNMP_ERR_NOERROR;
                }
                /* Also may need to check size/value */
                break;
            default:
                netsnmp_set_request_error( reqinfo, request,
                                           SNMP_ERR_NOTWRITABLE );
                return SNMP_ERR_NOERROR;
            }
        }
        break;

    case MODE_SET_RESERVE2:
        break;

    case MODE_SET_FREE:
        break;

    case MODE_SET_ACTION:
        for (request=requests; request; request=request->next) {
            table_entry = (struct dot11NewWtpVersionFileUpdateTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
    
	      if( !table_entry ){
		   netsnmp_set_request_error(reqinfo,request,SNMP_NOSUCHINSTANCE);
		   continue;
		   }	
            switch (table_info->colnum) {
            case COLUMN_SETWTPVERSIONFILE:
                /* Need to save old 'table_entry->setWtpVersionFile' value.
                   May need to use 'memcpy' */
                {
                    void *connection = NULL;
                    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(table_entry->parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
                        return MFD_ERROR;
                        
					int ret = 0;
					int i = 0,j = 0,k = 0,m = 0;
					char input_string[255] = {0};
					memset(input_string,0,255);
					strncpy(input_string,request->requestvb->val.string,request->requestvb->val_len);
					//char filename[256] = {0};
					char version[255] = {0};
					memset(version,0,255);
					for(i = 0; i<255; i++)
					{
					   if(input_string[i]=='.') break;
					}
					for(j = 255; j>0; j--)
					{
					   if(input_string[j]=='.') break;
					}
					
					for(k = (i-1); k<j; k++)
					{  
					   version[m] = input_string[k];
					   m++;
					   
					}
					
				    ret = update_wtp_img_cmd_func(table_entry->parameter, connection, table_entry->wtpCurrID, input_string, version, "now");					
					if(1 == ret) {
					    table_entry->setWtpVersionFile = 1;
					}
					else if (SNMPD_CONNECTION_ERROR == ret) {
                        close_slot_dbus_connection(table_entry->parameter.slot_id);
            	    }
				}
                break;
            }
        }
        break;

    case MODE_SET_UNDO:
        for (request=requests; request; request=request->next) {
            table_entry = (struct dot11NewWtpVersionFileUpdateTable_entry *)
                              netsnmp_extract_iterator_context(request);
            table_info  =     netsnmp_extract_table_info(      request);
    
            switch (table_info->colnum) {
            case COLUMN_SETWTPVERSIONFILE:
                /* Need to restore old 'table_entry->setWtpVersionFile' value.
                   May need to use 'memcpy' */
                //table_entry->setWtpVersionFile = table_entry->old_setWtpVersionFile;
                break;
            }
        }
        break;

    case MODE_SET_COMMIT:
        break;
    }
    return SNMP_ERR_NOERROR;
}
