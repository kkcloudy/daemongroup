/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11BssStaInfoTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11BssStaInfoTable get routines.
 * TODO:240:M: Implement dot11BssStaInfoTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11BssStaInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-WTP-MIB::dot11BssStaInfoTable is subid 3 of wtpStation.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.3, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11BssStaInfoTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param wtpMacAddr_ptr
 * @param wtpMacAddr_ptr_len
 * @param wtpWirelessIfIndex_val
 * @param NewWirelessWlanID_val
 * @param StaMAC_ptr
 * @param StaMAC_ptr_len
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11BssStaInfoTable_indexes_set_tbl_idx(dot11BssStaInfoTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpWirelessIfIndex_val, long NewWirelessWlanID_val, char *StaMAC_val_ptr,  size_t StaMAC_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11BssStaInfoTable:dot11BssStaInfoTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
    tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr)/sizeof(tbl_idx->wtpMacAddr[0]); /* max length */
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len <
         (wtpMacAddr_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len;
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len* sizeof(wtpMacAddr_val_ptr[0]) );
    
    /* wtpWirelessIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wtpWirelessIfIndex = wtpWirelessIfIndex_val;
    
    /* NewWirelessWlanID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->NewWirelessWlanID = NewWirelessWlanID_val;
    
    /* StaMAC(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
    tbl_idx->StaMAC_len = sizeof(tbl_idx->StaMAC)/sizeof(tbl_idx->StaMAC[0]); /* max length */
    /*
     * make sure there is enough space for StaMAC data
     */
    if ((NULL == tbl_idx->StaMAC) ||
        (tbl_idx->StaMAC_len <
         (StaMAC_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->StaMAC_len = StaMAC_val_ptr_len;
    memcpy( tbl_idx->StaMAC, StaMAC_val_ptr, StaMAC_val_ptr_len* sizeof(StaMAC_val_ptr[0]) );
    

    return MFD_SUCCESS;
} /* dot11BssStaInfoTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11BssStaInfoTable_indexes_set(dot11BssStaInfoTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpWirelessIfIndex_val, long NewWirelessWlanID_val, char *StaMAC_val_ptr,  size_t StaMAC_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11BssStaInfoTable:dot11BssStaInfoTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11BssStaInfoTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpWirelessIfIndex_val
                                   , NewWirelessWlanID_val
                                   , StaMAC_val_ptr, StaMAC_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11BssStaInfoTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11BssStaInfoTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssStaInfoEntry.StaESSID
 * StaESSID is subid 2 of dot11BssStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.3.1.2
 * Description:
station access ESSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the StaESSID data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param StaESSID_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param StaESSID_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by StaESSID.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*StaESSID_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update StaESSID_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
StaESSID_get( dot11BssStaInfoTable_rowreq_ctx *rowreq_ctx, char **StaESSID_val_ptr_ptr, size_t *StaESSID_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != StaESSID_val_ptr_ptr) && (NULL != *StaESSID_val_ptr_ptr));
   netsnmp_assert( NULL != StaESSID_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11BssStaInfoTable:StaESSID_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the StaESSID data.
 * copy (* StaESSID_val_ptr_ptr ) data and (* StaESSID_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for StaESSID data
     */
    if ((NULL == (* StaESSID_val_ptr_ptr )) ||
        ((* StaESSID_val_ptr_len_ptr ) <
         (rowreq_ctx->data.StaESSID_len* sizeof(rowreq_ctx->data.StaESSID[0])))) {
        /*
         * allocate space for StaESSID data
         */
        (* StaESSID_val_ptr_ptr ) = malloc(rowreq_ctx->data.StaESSID_len* sizeof(rowreq_ctx->data.StaESSID[0]));
        if(NULL == (* StaESSID_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* StaESSID_val_ptr_len_ptr ) = rowreq_ctx->data.StaESSID_len* sizeof(rowreq_ctx->data.StaESSID[0]);
    memcpy( (* StaESSID_val_ptr_ptr ), rowreq_ctx->data.StaESSID, rowreq_ctx->data.StaESSID_len* sizeof(rowreq_ctx->data.StaESSID[0]) );

    return MFD_SUCCESS;
} /* StaESSID_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11BssStaInfoEntry.StaIPAddress
 * StaIPAddress is subid 3 of dot11BssStaInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.3.1.3
 * Description:
station IP address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the StaIPAddress data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param StaIPAddress_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
StaIPAddress_get( dot11BssStaInfoTable_rowreq_ctx *rowreq_ctx, u_long * StaIPAddress_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != StaIPAddress_val_ptr );


    DEBUGMSGTL(("verbose:dot11BssStaInfoTable:StaIPAddress_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the StaIPAddress data.
 * copy (* StaIPAddress_val_ptr ) from rowreq_ctx->data
 */
    (* StaIPAddress_val_ptr ) = rowreq_ctx->data.StaIPAddress;

    return MFD_SUCCESS;
} /* StaIPAddress_get */



/** @} */
