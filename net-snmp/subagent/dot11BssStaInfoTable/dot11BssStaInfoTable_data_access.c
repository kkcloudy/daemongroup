/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 14170 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11BssStaInfoTable.h"


#include "dot11BssStaInfoTable_data_access.h"
#include "mibs_public.h"
#include "wcpss/wid/WID.h"
#include "ws_dcli_wlans.h"
#include "wcpss/asd/asd.h"
#include "ws_sta.h"
#include "autelanWtpGroup.h"
#include "ws_dbus_list_interface.h"

/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11BssStaInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-WTP-MIB::dot11BssStaInfoTable is subid 3 of wtpStation.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.8.3, length: 12
*/

/**
 * initialization for dot11BssStaInfoTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11BssStaInfoTable_reg
 *        Pointer to dot11BssStaInfoTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11BssStaInfoTable_init_data(dot11BssStaInfoTable_registration * dot11BssStaInfoTable_reg)
{
    DEBUGMSGTL(("verbose:dot11BssStaInfoTable:dot11BssStaInfoTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11BssStaInfoTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11BssStaInfoTable_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11BssStaInfoTable_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11BssStaInfoTable:dot11BssStaInfoTable_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to dot11BssStaInfoTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to dot11BssStaInfoTable_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up dot11BssStaInfoTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11BSSSTAINFOTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11BssStaInfoTable_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before dot11BssStaInfoTable_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
dot11BssStaInfoTable_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:dot11BssStaInfoTable:dot11BssStaInfoTable_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to dot11BssStaInfoTable_container_shutdown\n");
        return;
    }

} /* dot11BssStaInfoTable_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement dot11BssStaInfoTable data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11BssStaInfoTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11BssStaInfoTable_container_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11BssStaInfoTable_container_load\n");

    dot11BssStaInfoTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
         */
        /** 128 - 1(entry) - 1(col) - 3(other indexes) = 109 */
   char   wtpMacAddr[20];
   size_t      wtpMacAddr_len;
        /*
         * wtpWirelessIfIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   wtpWirelessIfIndex;
        /*
         * NewWirelessWlanID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   NewWirelessWlanID;
        /*
         * StaMAC(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
         */
        /** 128 - 1(entry) - 1(col) - 3(other indexes) = 109 */
   char   StaMAC[20];
   size_t      StaMAC_len;

    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];

    DEBUGMSGTL(("verbose:dot11BssStaInfoTable:dot11BssStaInfoTable_container_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
      //  return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11BssStaInfoTable container.
     * loop over your dot11BssStaInfoTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    instance_parameter *paraHead = NULL;
	instance_parameter *pq = NULL;
	int ret = 0;
	struct dcli_base_bss_info *bsshead = NULL, *bss = NULL;
	struct dcli_sta_base_info *sta = NULL;
	int i = 0;
	long StaIPAddress = 0;

	snmp_log(LOG_DEBUG, "enter list_instance_parameter\n");
    list_instance_parameter(&paraHead, SNMPD_INSTANCE_MASTER); 
	snmp_log(LOG_DEBUG, "exit list_instance_parameter,paraHead=%p\n", paraHead);
	for(pq = paraHead; (NULL != pq); pq = pq->next)
	{
		ret = show_all_sta_base_info_cmd(pq->parameter, pq->connection, &bsshead);
		if(1 == ret)
		{
			for(bss = bsshead; (NULL != bss); bss = bss->next)
			{
				for(i=0,sta = bss->sta_list; ((i<bss->num_sta)&&(NULL != sta)); i++,sta = sta->next)
				{
					wtpMacAddr_len = 0;
					wtpWirelessIfIndex = 0;
					NewWirelessWlanID = 0;
					StaMAC_len = 0;
					
					memset(wtpMacAddr, 0, sizeof(wtpMacAddr));
					snprintf(wtpMacAddr, sizeof(wtpMacAddr)-1, 
							 "%02X:%02X:%02X:%02X:%02X:%02X",
							 sta->wtp_addr[0],sta->wtp_addr[1], 
							 sta->wtp_addr[2],sta->wtp_addr[3], 
							 sta->wtp_addr[4],sta->wtp_addr[5]);

					wtpWirelessIfIndex = sta->radio_l_id + 1;

					NewWirelessWlanID = local_to_global_ID(pq->parameter, sta->wlan_id, WIRELESS_MAX_NUM);

					memset(StaMAC, 0, sizeof(StaMAC));
					snprintf(StaMAC, sizeof(StaMAC)-1, 
							 "%02X:%02X:%02X:%02X:%02X:%02X",
							 sta->addr[0],sta->addr[1], 
							 sta->addr[2],sta->addr[3], 
							 sta->addr[4],sta->addr[5]);
				
				    /*
				    ***************************************************
				    ***             START EXAMPLE CODE              ***
				    ***---------------------------------------------***/
				    /*
				     * get a line (skip blank lines)
				     */
				    //do {
				      //  if (!fgets(line, sizeof(line), filep)) {
				            /* we're done */
				        //    fclose(filep);
				          //  filep = NULL;
				        //}
				    //} while (filep && (line[0] == '\n'));

				    /*
				     * check for end of data
				     */
				    //if(NULL == filep)
				      //  break;

				    /*
				     * parse line into variables
				     */
				    /*
				    ***---------------------------------------------***
				    ***              END  EXAMPLE CODE              ***
				    ***************************************************/

				        /*
				         * TODO:352:M: |   |-> set indexes in new dot11BssStaInfoTable rowreq context.
				         * data context will be set from the param (unless NULL,
				         *      in which case a new data context will be allocated)
				         */
				        rowreq_ctx = dot11BssStaInfoTable_allocate_rowreq_ctx(NULL);
				        if (NULL == rowreq_ctx) {
				            snmp_log(LOG_ERR, "memory allocation failed\n");
							Free_show_all_sta_base_info_cmd(bsshead);
							free_instance_parameter_list(&paraHead);
				            return MFD_RESOURCE_UNAVAILABLE;
				        }
						wtpMacAddr_len = strlen(wtpMacAddr);
						StaMAC_len = strlen(StaMAC);
				        if(MFD_SUCCESS != dot11BssStaInfoTable_indexes_set(rowreq_ctx
				                               , wtpMacAddr, wtpMacAddr_len
				                               , wtpWirelessIfIndex
				                               , NewWirelessWlanID
				                               , StaMAC, StaMAC_len
				               )) {
				            snmp_log(LOG_ERR,"error setting index while loading "
				                     "dot11BssStaInfoTable data.\n");
				            dot11BssStaInfoTable_release_rowreq_ctx(rowreq_ctx);
				            continue;
				        }

				        /*
				         * TODO:352:r: |   |-> populate dot11BssStaInfoTable data context.
				         * Populate data context here. (optionally, delay until row prep)
				         */
				    /*
				     * TRANSIENT or semi-TRANSIENT data:
				     * copy data or save any info needed to do it in row_prep.
				     */
				    /*
				     * setup/save data for StaESSID
				     * StaESSID(2)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
				     */
				    /** no mapping */
				    /*
				     * make sure there is enough space for StaESSID data
				     */
				    /*if ((NULL == rowreq_ctx->data.StaESSID) ||
				        (rowreq_ctx->data.StaESSID_len <
				         (StaESSID_len* sizeof(StaESSID[0])))) {
				        snmp_log(LOG_ERR,"not enough space for value\n");
				        return MFD_ERROR;
				    }*/
				    if(sta->essid)
				    {
						rowreq_ctx->data.StaESSID_len = MIN(sizeof(rowreq_ctx->data.StaESSID) - 1, strlen(sta->essid));
						memcpy( rowreq_ctx->data.StaESSID, sta->essid, rowreq_ctx->data.StaESSID_len );
				    }
				    
				    /*
				     * setup/save data for StaIPAddress
				     * StaIPAddress(3)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/w/e/r/d/h
				     */
				    /** no mapping */
					StaIPAddress = 0;
					if(sta->ip)
				    {
						INET_ATON(StaIPAddress, sta->ip);
				    }
				    rowreq_ctx->data.StaIPAddress = StaIPAddress;
				    
				        
				        /*
				         * insert into table container
				         */
				        if(CONTAINER_INSERT(container, rowreq_ctx))
				        {
				        	dot11BssStaInfoTable_release_rowreq_ctx(rowreq_ctx);
				        }
				        ++count;
				    }
			}
			Free_show_all_sta_base_info_cmd(bsshead);
		}
	}
	free_instance_parameter_list(&paraHead);


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
      //  fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11BssStaInfoTable:dot11BssStaInfoTable_container_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11BssStaInfoTable_container_load\n");
    return MFD_SUCCESS;
} /* dot11BssStaInfoTable_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11BssStaInfoTable_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11BssStaInfoTable:dot11BssStaInfoTable_container_free","called\n"));

    /*
     * TODO:380:M: Free dot11BssStaInfoTable container data.
     */
} /* dot11BssStaInfoTable_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11BssStaInfoTable_row_prep( dot11BssStaInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11BssStaInfoTable:dot11BssStaInfoTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11BssStaInfoTable_row_prep */

/** @} */
