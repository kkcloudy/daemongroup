/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11RadioStatsTable.h"
#include "dot11RadioStatsTable_data_access.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "dbus/wcpss/ACDbusDef1.h"
#include "../mibs_public.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11RadioStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11RadioStatsTable is subid 1 of wtpRadio.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1, length: 12
*/

/**
 * initialization for dot11RadioStatsTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11RadioStatsTable_reg
 *        Pointer to dot11RadioStatsTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11RadioStatsTable_init_data(dot11RadioStatsTable_registration_ptr dot11RadioStatsTable_reg)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11RadioStatsTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11RadioStatsTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11RadioStatsTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11RadioStatsTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11RadioStatsTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11RADIOSTATSTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11RadioStatsTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11RadioStatsTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11RadioStatsTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11RadioStatsTable_cache_load(netsnmp_container *container)
{
	
	snmp_log(LOG_DEBUG, "enter dot11RadioStatsTable_cache_load\n");
	
    dot11RadioStatsTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
         */
        /** 128 - 1(entry) - 1(col) - 1(other indexes) = 111 */
   char   wtpMacAddr[20] = { 0 };
   size_t      wtpMacAddr_len = 0;
        /*
         * wtpRadCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
      //  return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11RadioStatsTable container.
     * loop over your dot11RadioStatsTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */

	
	int wtpCurTakeFreqPoint = 0;
	char wtpConfigAvailSpeed[255] = { 0 };
	size_t wtpConfigAvailSpeed_len = 0;
	char wtpConfigForceSpeed[255] = { 0 };
	size_t wtpConfigForceSpeed_len = 0;
    
    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_all_wtp_radio_stats_information_cmd\n");
    messageHead = list_connection_call_dbus_method(show_all_wtp_radio_stats_information_cmd, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_all_wtp_radio_stats_information_cmd,messageHead=%p\n", messageHead);
	
	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    struct RadioStatsInfo *WtpHead = messageNode->message;
		    if(WtpHead)
		    {
		        struct RadioStatsInfo *q = NULL;
            	for(q = WtpHead->RadioStatsInfo_list; q != NULL; q = q->next)
            	{
            		memset(wtpMacAddr,0,20);
					if(q->wtpMacAddr)
					{
						snprintf(wtpMacAddr,sizeof(wtpMacAddr)-1,
								 "%02X:%02X:%02X:%02X:%02X:%02X",
								 q->wtpMacAddr[0],q->wtpMacAddr[1],
								 q->wtpMacAddr[2],q->wtpMacAddr[3],
								 q->wtpMacAddr[4],q->wtpMacAddr[5]);
					}

                    int i = 0;
            		struct Sub_RadioStatsInfo *sub_radio = NULL;
            		for(i=0,sub_radio = q->Sub_RadioStatsInfo_head;((i<q->wtp_radio_num)&&(sub_radio));i++,sub_radio = sub_radio->next)
            		{
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * get a line (skip blank lines)
     */
    //do {
      //  if (!fgets(line, sizeof(line), filep)) {
            /* we're done */
        //    fclose(filep);
          //  filep = NULL;
        //}
   // } while (filep && (line[0] == '\n'));

    /*
     * check for end of data
     */
    //if(NULL == filep)
      //  break;

    /*
     * parse line into variables
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

        /*
         * TODO:352:M: |   |-> set indexes in new dot11RadioStatsTable rowreq context.
         */
        rowreq_ctx = dot11RadioStatsTable_allocate_rowreq_ctx();
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
            free_dbus_message_list(&messageHead, Free_show_all_wtp_radio_stats_information_cmd);
            return MFD_RESOURCE_UNAVAILABLE;
        }
		
		wtpMacAddr_len = strlen(wtpMacAddr);
        if(MFD_SUCCESS != dot11RadioStatsTable_indexes_set(rowreq_ctx
                               , wtpMacAddr, wtpMacAddr_len
                               , sub_radio->wtpRadLocalID+1
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "dot11RadioStatsTable cache.\n");
            dot11RadioStatsTable_release_rowreq_ctx(rowreq_ctx);
            continue;
        }
		
    memcpy(&(rowreq_ctx->data.parameter), &(messageNode->parameter), sizeof(dbus_parameter));
	rowreq_ctx->data.setwtpRadCurrID = sub_radio->wtpRadCurrID;

	rowreq_ctx->data.wtpRadCurrID = sub_radio->wtpRadCurrID;
        /*
         * TODO:352:r: |   |-> populate dot11RadioStatsTable data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for wtpCurTxPower
     * wtpCurTxPower(2)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpCurTxPower mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
	
		
    rowreq_ctx->data.wtpCurTxPower = sub_radio->wtpCurSendPower;
    
    /*
     * setup/save data for wtpPowerManage
     * wtpPowerManage(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpPowerManage mapping.
     * Map values between raw/native values and MIB values
     *
    * enums usually need mapping.
    */
    /*if(MFD_SUCCESS !=
       wtpPowerManage_map(&rowreq_ctx->data.wtpPowerManage, wtpPowerManage )) {
        return MFD_ERROR;
    }*/
    rowreq_ctx->data.wtpPowerManage = q->wtpPowerManage;
    /*
     * setup/save data for wtpRxPower
     * wtpRxPower(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpRxPower mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpRxPower = sub_radio->wtpRecvPower;
    
    /*
     * setup/save data for wtpCurTakeFreqPoint
     * wtpCurTakeFreqPoint(5)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpCurTakeFreqPoint mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */    
	switch(sub_radio->wtpCurConfChannel)
	{
		case 1:wtpCurTakeFreqPoint = 2412;
			   break;
		case 2:wtpCurTakeFreqPoint = 2417;
			   break;
		case 3:wtpCurTakeFreqPoint = 2422;
			   break;
		case 4:wtpCurTakeFreqPoint = 2427;
			   break;
		case 5:wtpCurTakeFreqPoint = 2432;
			   break;	   
		case 6:wtpCurTakeFreqPoint = 2437;
			   break;	   
		case 7:wtpCurTakeFreqPoint = 2442;
			   break;	   
		case 8:wtpCurTakeFreqPoint = 2447;
			   break;	   
		case 9:wtpCurTakeFreqPoint = 2452;
			   break;	   
		case 10:wtpCurTakeFreqPoint = 2457;
			    break;
		case 11:wtpCurTakeFreqPoint = 2462;
			    break;		
		case 12:wtpCurTakeFreqPoint = 2467;
			    break;	
		case 13:wtpCurTakeFreqPoint = 2472;
			    break;
	}
	
    rowreq_ctx->data.wtpCurTakeFreqPoint = wtpCurTakeFreqPoint;
    
    /*
     * setup/save data for wtpCurConfChannel
     * wtpCurConfChannel(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpCurConfChannel mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpCurConfChannel = sub_radio->wtpCurConfChannel;
    
    /*
     * setup/save data for wtpProtoMode
     * wtpProtoMode(7)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpProtoMode mapping.
     * Map values between raw/native values and MIB values
     *
    * enums usually need mapping.
    */
    /*if(MFD_SUCCESS !=
       wtpProtoMode_map(&rowreq_ctx->data.wtpProtoMode, wtpProtoMode )) {
        return MFD_ERROR;
    }*/
    unsigned long wtpProtoMode = 0;
    unsigned int flag_11n = 0;
    
    if(sub_radio->radio_type & IEEE80211_11N){
        flag_11n = 1;
    }
    
    if(sub_radio->radio_type & IEEE80211_11A) {
#if 0
        wtpProtoMode |= 0x1;           //a    
        if(flag_11n) {
            wtpProtoMode |= 0x1 << 3;      //an
        }
#else        
        if(flag_11n) {
            if(sub_radio->radio_type & IEEE80211_11AN) {
                wtpProtoMode |= 0x1;           //a    
            }
            wtpProtoMode |= 0x1 << 3;      //an
        }
#endif    
    }

    if(sub_radio->radio_type & IEEE80211_11B) {
        wtpProtoMode |= 0x1 << 1;      //b        
    }

    if(sub_radio->radio_type & IEEE80211_11G) {
#if 0
        wtpProtoMode |= 0x1 << 2;      //g    
        if(flag_11n) {
            wtpProtoMode |= 0x1 << 4;      //gn
        }
#else
        if(flag_11n) {
            if((sub_radio->radio_type & IEEE80211_11GN)
			|| (wtpProtoMode & (0x1 << 1))) {                
                wtpProtoMode |= 0x1 << 2;      //g        
            }
            wtpProtoMode |= 0x1 << 4;      //gn
        }
        else {
            wtpProtoMode |= 0x1 << 2;      //g        
        }
#endif
    }
    
					
    rowreq_ctx->data.wtpProtoMode = wtpProtoMode;
    /*
     * setup/save data for wtpSampleTime
     * wtpSampleTime(8)/TICKS/ASN_TIMETICKS/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpSampleTime mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpSampleTime = q->wtpSampleTime*100;
    
    /*
     * setup/save data for wtpTerminalConRate
     * wtpTerminalConRate(9)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpTerminalConRate mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpTerminalConRate = sub_radio->wtpTerminalConRate;
    
    /*
     * setup/save data for wtpConfigAvailSpeed
     * wtpConfigAvailSpeed(10)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpConfigAvailSpeed mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpConfigAvailSpeed_map(&rowreq_ctx->data.wtpConfigAvailSpeed, &rowreq_ctx->data.wtpConfigAvailSpeed_len,
     *                wtpConfigAvailSpeed, wtpConfigAvailSpeed_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpConfigAvailSpeed data
     */
    /*if ((NULL == rowreq_ctx->data.wtpConfigAvailSpeed) ||
        (rowreq_ctx->data.wtpConfigAvailSpeed_len < (wtpConfigAvailSpeed_len * sizeof(rowreq_ctx->data.wtpConfigAvailSpeed[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/
    wtpConfigAvailSpeed_len = MIN(strlen("54.0 48.0 36.0 24.0 18.0 12.0 11.0 9.0 6.0 5.5 2.0 1.0 M/bps"),sizeof(wtpConfigAvailSpeed)-1);
	memset(wtpConfigAvailSpeed,0,sizeof(wtpConfigAvailSpeed));
	memcpy(wtpConfigAvailSpeed,"54.0 48.0 36.0 24.0 18.0 12.0 11.0 9.0 6.0 5.5 2.0 1.0 M/bps",wtpConfigAvailSpeed_len);
				
    rowreq_ctx->data.wtpConfigAvailSpeed_len = wtpConfigAvailSpeed_len * sizeof(rowreq_ctx->data.wtpConfigAvailSpeed[0]);
    memcpy( rowreq_ctx->data.wtpConfigAvailSpeed, wtpConfigAvailSpeed, rowreq_ctx->data.wtpConfigAvailSpeed_len );
    
    /*
     * setup/save data for wtpConfigForceSpeed
     * wtpConfigForceSpeed(11)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpConfigForceSpeed mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpConfigForceSpeed_map(&rowreq_ctx->data.wtpConfigForceSpeed, &rowreq_ctx->data.wtpConfigForceSpeed_len,
     *                wtpConfigForceSpeed, wtpConfigForceSpeed_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpConfigForceSpeed data
     */
    /*if ((NULL == rowreq_ctx->data.wtpConfigForceSpeed) ||
        (rowreq_ctx->data.wtpConfigForceSpeed_len < (wtpConfigForceSpeed_len * sizeof(rowreq_ctx->data.wtpConfigForceSpeed[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/
    wtpConfigForceSpeed_len = MIN(strlen("54.0 48.0 36.0 24.0 18.0 12.0 11.0 9.0 6.0 5.5 2.0 1.0 M/bps"),sizeof(wtpConfigForceSpeed)-1);
	memset(wtpConfigForceSpeed,0,sizeof(wtpConfigForceSpeed));
	memcpy(wtpConfigForceSpeed,"54.0 48.0 36.0 24.0 18.0 12.0 11.0 9.0 6.0 5.5 2.0 1.0 M/bps",wtpConfigForceSpeed_len);
	
    rowreq_ctx->data.wtpConfigForceSpeed_len = wtpConfigForceSpeed_len * sizeof(rowreq_ctx->data.wtpConfigForceSpeed[0]);
    memcpy( rowreq_ctx->data.wtpConfigForceSpeed, wtpConfigForceSpeed, rowreq_ctx->data.wtpConfigForceSpeed_len );
    
    /*
     * setup/save data for wtpRTSCTSEnable
     * wtpRTSCTSEnable(12)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpRTSCTSEnable mapping.
     * Map values between raw/native values and MIB values
     *
    * enums usually need mapping.
    */
    /*if(MFD_SUCCESS !=
       wtpRTSCTSEnable_map(&rowreq_ctx->data.wtpRTSCTSEnable, wtpRTSCTSEnable )) {
        return MFD_ERROR;
    }*/
    rowreq_ctx->data.wtpRTSCTSEnable = 1;
    
    /*
     * setup/save data for wtpconfigBeaconFrameBlank
     * wtpconfigBeaconFrameBlank(13)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpconfigBeaconFrameBlank mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpconfigBeaconFrameBlank = sub_radio->wtpconfigBeaconFrameBlank;
    
        
        /*
         * insert into table container
         */
    			        if(CONTAINER_INSERT(container, rowreq_ctx))
    			        {
    						dot11RadioStatsTable_release_rowreq_ctx(rowreq_ctx);
    			        }
    			        ++count;
        			}				
    			}
    		}
    	}
        free_dbus_message_list(&messageHead, Free_show_all_wtp_radio_stats_information_cmd);
	}


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
      //  fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11RadioStatsTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11RadioStatsTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11RadioStatsTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11RadioStatsTable cache.
     */
} /* dot11RadioStatsTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11RadioStatsTable_row_prep( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11RadioStatsTable_row_prep */

/** @} */
