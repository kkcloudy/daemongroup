/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11RadioStatsTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11RadioStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11RadioStatsTable is subid 1 of wtpRadio.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11RadioStatsTable_undo_setup( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11RadioStatsTable undo.
     * set up dot11RadioStatsTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11RadioStatsTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11RadioStatsTable_undo_cleanup( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11RadioStatsTable undo.
     */

    return rc;
} /* dot11RadioStatsTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11RadioStatsTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11RadioStatsTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11RadioStatsTable_commit( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11RadioStatsTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WTPCURTXPOWER) {
       save_flags &= ~FLAG_WTPCURTXPOWER; /* clear wtpCurTxPower */
       /*
        * TODO:482:o: |-> commit column wtpCurTxPower.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioStatsTable column wtpCurTxPower commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpCurTxPower
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPCURTXPOWER;
       }
    }

    if (save_flags & FLAG_WTPRXPOWER) {
       save_flags &= ~FLAG_WTPRXPOWER; /* clear wtpRxPower */
       /*
        * TODO:482:o: |-> commit column wtpRxPower.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioStatsTable column wtpRxPower commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpRxPower
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPRXPOWER;
       }
    }

    if (save_flags & FLAG_WTPCURCONFCHANNEL) {
       save_flags &= ~FLAG_WTPCURCONFCHANNEL; /* clear wtpCurConfChannel */
       /*
        * TODO:482:o: |-> commit column wtpCurConfChannel.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioStatsTable column wtpCurConfChannel commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpCurConfChannel
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPCURCONFCHANNEL;
       }
    }

    if (save_flags & FLAG_WTPPROTOMODE) {
       save_flags &= ~FLAG_WTPPROTOMODE; /* clear wtpProtoMode */
       /*
        * TODO:482:o: |-> commit column wtpProtoMode.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioStatsTable column wtpProtoMode commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpProtoMode
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPPROTOMODE;
       }
    }

    if (save_flags & FLAG_WTPTERMINALCONRATE) {
       save_flags &= ~FLAG_WTPTERMINALCONRATE; /* clear wtpTerminalConRate */
       /*
        * TODO:482:o: |-> commit column wtpTerminalConRate.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioStatsTable column wtpTerminalConRate commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpTerminalConRate
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPTERMINALCONRATE;
       }
    }

    if (save_flags & FLAG_WTPRTSCTSENABLE) {
       save_flags &= ~FLAG_WTPRTSCTSENABLE; /* clear wtpRTSCTSEnable */
       /*
        * TODO:482:o: |-> commit column wtpRTSCTSEnable.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioStatsTable column wtpRTSCTSEnable commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpRTSCTSEnable
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPRTSCTSENABLE;
       }
    }

    if (save_flags & FLAG_WTPCONFIGBEACONFRAMEBLANK) {
       save_flags &= ~FLAG_WTPCONFIGBEACONFRAMEBLANK; /* clear wtpconfigBeaconFrameBlank */
       /*
        * TODO:482:o: |-> commit column wtpconfigBeaconFrameBlank.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11RadioStatsTable column wtpconfigBeaconFrameBlank commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpconfigBeaconFrameBlank
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPCONFIGBEACONFRAMEBLANK;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11RadioStatsTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11RadioStatsTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11RadioStatsTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11RadioStatsTable_undo_commit( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11RadioStatsTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11RadioStatsTable_undo_commit */

/*
 * TODO:420:r: Implement dot11RadioStatsTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/*
 * TODO:440:M: Implement dot11RadioStatsTable node value checks.
 * TODO:450:M: Implement dot11RadioStatsTable undo functions.
 * TODO:460:M: Implement dot11RadioStatsTable set functions.
 * TODO:480:M: Implement dot11RadioStatsTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpCurTxPower
 * wtpCurTxPower is subid 2 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.2
 * Description:
The  current transmission power value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCurTxPower_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpCurTxPower_check_value( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long wtpCurTxPower_val)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurTxPower_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpCurTxPower value.
     */

    return MFD_SUCCESS; /* wtpCurTxPower value not illegal */
} /* wtpCurTxPower_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpCurTxPower_undo_setup( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurTxPower_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpCurTxPower undo.
     */
    /*
     * copy wtpCurTxPower data
     * set rowreq_ctx->undo->wtpCurTxPower from rowreq_ctx->data.wtpCurTxPower
     */
    rowreq_ctx->undo->wtpCurTxPower = rowreq_ctx->data.wtpCurTxPower;


    return MFD_SUCCESS;
} /* wtpCurTxPower_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpCurTxPower_val
 *        A long containing the new value.
 */
int
wtpCurTxPower_set( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long wtpCurTxPower_val )
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurTxPower_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpCurTxPower reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpCurTxPower value.
     * set wtpCurTxPower value in rowreq_ctx->data
     */    
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_txpower(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,wtpCurTxPower_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpCurTxPower = wtpCurTxPower_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}    

    return MFD_SUCCESS;
} /* wtpCurTxPower_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpCurTxPower_undo( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurTxPower_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpCurTxPower undo.
     */
    /*
     * copy wtpCurTxPower data
     * set rowreq_ctx->data.wtpCurTxPower from rowreq_ctx->undo->wtpCurTxPower
     */
    rowreq_ctx->data.wtpCurTxPower = rowreq_ctx->undo->wtpCurTxPower;

    
    return MFD_SUCCESS;
} /* wtpCurTxPower_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpRxPower
 * wtpRxPower is subid 4 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.4
 * Description:
The the received power value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRxPower_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpRxPower_check_value( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long wtpRxPower_val)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRxPower_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpRxPower value.
     */

    return MFD_SUCCESS; /* wtpRxPower value not illegal */
} /* wtpRxPower_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpRxPower_undo_setup( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRxPower_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpRxPower undo.
     */
    /*
     * copy wtpRxPower data
     * set rowreq_ctx->undo->wtpRxPower from rowreq_ctx->data.wtpRxPower
     */
    rowreq_ctx->undo->wtpRxPower = rowreq_ctx->data.wtpRxPower;


    return MFD_SUCCESS;
} /* wtpRxPower_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpRxPower_val
 *        A long containing the new value.
 */
int
wtpRxPower_set( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long wtpRxPower_val )
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRxPower_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpRxPower reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpRxPower value.
     * set wtpRxPower value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;

	ret = config_radio_txpower(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,wtpRxPower_val);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpRxPower = wtpRxPower_val;
		rc = MFD_SUCCESS;
	}
	else
	{		
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}        

    return MFD_SUCCESS;
} /* wtpRxPower_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpRxPower_undo( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRxPower_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpRxPower undo.
     */
    /*
     * copy wtpRxPower data
     * set rowreq_ctx->data.wtpRxPower from rowreq_ctx->undo->wtpRxPower
     */
    rowreq_ctx->data.wtpRxPower = rowreq_ctx->undo->wtpRxPower;

    
    return MFD_SUCCESS;
} /* wtpRxPower_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpCurConfChannel
 * wtpCurConfChannel is subid 6 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.6
 * Description:
The channel of the configuration.(0 means auto)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 14;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCurConfChannel_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - 14
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpCurConfChannel_check_value( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long wtpCurConfChannel_val)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurConfChannel_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpCurConfChannel value.
     */

    return MFD_SUCCESS; /* wtpCurConfChannel value not illegal */
} /* wtpCurConfChannel_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpCurConfChannel_undo_setup( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurConfChannel_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpCurConfChannel undo.
     */
    /*
     * copy wtpCurConfChannel data
     * set rowreq_ctx->undo->wtpCurConfChannel from rowreq_ctx->data.wtpCurConfChannel
     */
    rowreq_ctx->undo->wtpCurConfChannel = rowreq_ctx->data.wtpCurConfChannel;


    return MFD_SUCCESS;
} /* wtpCurConfChannel_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpCurConfChannel_val
 *        A long containing the new value.
 */
int
wtpCurConfChannel_set( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long wtpCurConfChannel_val )
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurConfChannel_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpCurConfChannel reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpCurConfChannel value.
     * set wtpCurConfChannel value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char channel_value[10] = { 0 };
	memset(channel_value, 0, 10);
	
	if(wtpCurConfChannel_val != 0)
	{
		snprintf(channel_value,sizeof(channel_value)-1,"%d",wtpCurConfChannel_val);
	}
	else if(wtpCurConfChannel_val == 0)
	{
		strncpy(channel_value,"auto",sizeof(channel_value)-1);
	}

	ret = config_radio_channel(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,channel_value);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpCurConfChannel = wtpCurConfChannel_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
	    
		rc = MFD_ERROR;
	}     

    return MFD_SUCCESS;
} /* wtpCurConfChannel_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpCurConfChannel_undo( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurConfChannel_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpCurConfChannel undo.
     */
    /*
     * copy wtpCurConfChannel data
     * set rowreq_ctx->data.wtpCurConfChannel from rowreq_ctx->undo->wtpCurConfChannel
     */
    rowreq_ctx->data.wtpCurConfChannel = rowreq_ctx->undo->wtpCurConfChannel;

    
    return MFD_SUCCESS;
} /* wtpCurConfChannel_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpProtoMode
 * wtpProtoMode is subid 7 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.7
 * Description:
AP protocol mode.(bg,b,g)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  bg(1), g(2), b(3), a(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpProtoMode_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  bg(1), g(2), b(3), a(4)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpProtoMode_check_value( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, u_long wtpProtoMode_val)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpProtoMode_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpProtoMode value.
     */

    return MFD_SUCCESS; /* wtpProtoMode value not illegal */
} /* wtpProtoMode_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpProtoMode_undo_setup( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpProtoMode_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpProtoMode undo.
     */
    /*
     * copy wtpProtoMode data
     * set rowreq_ctx->undo->wtpProtoMode from rowreq_ctx->data.wtpProtoMode
     */
    rowreq_ctx->undo->wtpProtoMode = rowreq_ctx->data.wtpProtoMode;


    return MFD_SUCCESS;
} /* wtpProtoMode_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpProtoMode_val
 *        A long containing the new value.
 */
int
wtpProtoMode_set( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, u_long wtpProtoMode_val )
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpProtoMode_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpProtoMode reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/    
	char radioMode[10] = { 0 };
	
    switch(wtpProtoMode_val) {
        case WTPPROTOMODE_A:
             strncpy(radioMode, "11a", sizeof(radioMode) - 1);
             wtpProtoMode_val = INTERNAL_WTPPROTOMODE_A;
             break;

        case WTPPROTOMODE_B:
             strncpy(radioMode, "11b", sizeof(radioMode) - 1);
             wtpProtoMode_val = INTERNAL_WTPPROTOMODE_B;
             break;

        case WTPPROTOMODE_G:
             strncpy(radioMode, "11g", sizeof(radioMode) - 1);
             wtpProtoMode_val = INTERNAL_WTPPROTOMODE_G;
             break;

        case WTPPROTOMODE_BG:
             strncpy(radioMode, "11b/g", sizeof(radioMode) - 1);
             wtpProtoMode_val = INTERNAL_WTPPROTOMODE_BG;
             break;

        case WTPPROTOMODE_AN:
             strncpy(radioMode, "11an", sizeof(radioMode) - 1);
             wtpProtoMode_val = INTERNAL_WTPPROTOMODE_AN;
             break;
             
        case WTPPROTOMODE_AAN:
            strncpy(radioMode, "11a/an", sizeof(radioMode) - 1);
            wtpProtoMode_val = INTERNAL_WTPPROTOMODE_AAN;
            break;

        case WTPPROTOMODE_GN:
            strncpy(radioMode, "11gn", sizeof(radioMode) - 1);
            wtpProtoMode_val = INTERNAL_WTPPROTOMODE_GN;
            break;

        case WTPPROTOMODE_GGN:
            strncpy(radioMode, "11g/gn", sizeof(radioMode) - 1);
            wtpProtoMode_val = INTERNAL_WTPPROTOMODE_GGN;
            break;

        case WTPPROTOMODE_BGGN:
			strncpy(radioMode, "11b/g/n", sizeof(radioMode) - 1);
            wtpProtoMode_val = INTERNAL_WTPPROTOMODE_BGGN;
            break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpProtoMode\n", wtpProtoMode_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpProtoMode value.
     * set wtpProtoMode value in rowreq_ctx->data
     */
	void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
		return MFD_ERROR;
        
	int ret = 0;
	ret = config_radio_mode(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,radioMode);	
	if(1 == ret){
		rowreq_ctx->data.wtpProtoMode = wtpProtoMode_val;
		return MFD_SUCCESS;
	}

	if(SNMPD_CONNECTION_ERROR == ret) {
		close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	}

    return MFD_ERROR;
} /* wtpProtoMode_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpProtoMode_undo( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpProtoMode_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpProtoMode undo.
     */
    /*
     * copy wtpProtoMode data
     * set rowreq_ctx->data.wtpProtoMode from rowreq_ctx->undo->wtpProtoMode
     */
    rowreq_ctx->data.wtpProtoMode = rowreq_ctx->undo->wtpProtoMode;

    
    return MFD_SUCCESS;
} /* wtpProtoMode_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpTerminalConRate
 * wtpTerminalConRate is subid 9 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.9
 * Description:
The rate that configed terminal switch in.Select input value 540,480,360,240,120,110,90,60,55,20,10.While getting value,the value will be devided by 10bps(Units M/bps) 
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTerminalConRate_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpTerminalConRate_check_value( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long wtpTerminalConRate_val)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpTerminalConRate_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpTerminalConRate value.
     */

    return MFD_SUCCESS; /* wtpTerminalConRate value not illegal */
} /* wtpTerminalConRate_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpTerminalConRate_undo_setup( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpTerminalConRate_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpTerminalConRate undo.
     */
    /*
     * copy wtpTerminalConRate data
     * set rowreq_ctx->undo->wtpTerminalConRate from rowreq_ctx->data.wtpTerminalConRate
     */
    rowreq_ctx->undo->wtpTerminalConRate = rowreq_ctx->data.wtpTerminalConRate;


    return MFD_SUCCESS;
} /* wtpTerminalConRate_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpTerminalConRate_val
 *        A long containing the new value.
 */
int
wtpTerminalConRate_set( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long wtpTerminalConRate_val )
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpTerminalConRate_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpTerminalConRate reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpTerminalConRate value.
     * set wtpTerminalConRate value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
	char rad_rate[10] = { 0 };
	memset(rad_rate, 0, 10);
	snprintf(rad_rate,sizeof(rad_rate)-1,"%d",wtpTerminalConRate_val);
	struct RadioList *RadioList_Head = NULL;
		
	ret = config_max_rate(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,rad_rate,&RadioList_Head);	
	if(ret == 1)
	{
		rowreq_ctx->data.wtpTerminalConRate = wtpTerminalConRate_val;
		rc = MFD_SUCCESS;
	}
	else
	{	
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}      

    return MFD_SUCCESS;
} /* wtpTerminalConRate_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpTerminalConRate_undo( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpTerminalConRate_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpTerminalConRate undo.
     */
    /*
     * copy wtpTerminalConRate data
     * set rowreq_ctx->data.wtpTerminalConRate from rowreq_ctx->undo->wtpTerminalConRate
     */
    rowreq_ctx->data.wtpTerminalConRate = rowreq_ctx->undo->wtpTerminalConRate;

    
    return MFD_SUCCESS;
} /* wtpTerminalConRate_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpRTSCTSEnable
 * wtpRTSCTSEnable is subid 12 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.12
 * Description:
RTS/CTS whether start using.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  disable(0), enable(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRTSCTSEnable_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  disable(0), enable(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpRTSCTSEnable_check_value( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, u_long wtpRTSCTSEnable_val)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRTSCTSEnable_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpRTSCTSEnable value.
     */

    return MFD_SUCCESS; /* wtpRTSCTSEnable value not illegal */
} /* wtpRTSCTSEnable_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpRTSCTSEnable_undo_setup( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRTSCTSEnable_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpRTSCTSEnable undo.
     */
    /*
     * copy wtpRTSCTSEnable data
     * set rowreq_ctx->undo->wtpRTSCTSEnable from rowreq_ctx->data.wtpRTSCTSEnable
     */
    rowreq_ctx->undo->wtpRTSCTSEnable = rowreq_ctx->data.wtpRTSCTSEnable;


    return MFD_SUCCESS;
} /* wtpRTSCTSEnable_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpRTSCTSEnable_val
 *        A long containing the new value.
 */
int
wtpRTSCTSEnable_set( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, u_long wtpRTSCTSEnable_val )
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRTSCTSEnable_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpRTSCTSEnable reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpRTSCTSEnable_val) {
        case WTPRTSCTSENABLE_DISABLE:
             wtpRTSCTSEnable_val = INTERNAL_WTPRTSCTSENABLE_DISABLE;
             break;

        case WTPRTSCTSENABLE_ENABLE:
             wtpRTSCTSEnable_val = INTERNAL_WTPRTSCTSENABLE_ENABLE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpRTSCTSEnable\n", wtpRTSCTSEnable_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpRTSCTSEnable value.
     * set wtpRTSCTSEnable value in rowreq_ctx->data
     */

	
    rowreq_ctx->data.wtpRTSCTSEnable = wtpRTSCTSEnable_val;

    return MFD_SUCCESS;
} /* wtpRTSCTSEnable_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpRTSCTSEnable_undo( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRTSCTSEnable_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpRTSCTSEnable undo.
     */
    /*
     * copy wtpRTSCTSEnable data
     * set rowreq_ctx->data.wtpRTSCTSEnable from rowreq_ctx->undo->wtpRTSCTSEnable
     */
    rowreq_ctx->data.wtpRTSCTSEnable = rowreq_ctx->undo->wtpRTSCTSEnable;

    
    return MFD_SUCCESS;
} /* wtpRTSCTSEnable_undo */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpconfigBeaconFrameBlank
 * wtpconfigBeaconFrameBlank is subid 13 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.13
 * Description:
The configed beacon frame blank.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  25 - 100;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpconfigBeaconFrameBlank_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11RadioStatsTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  25 - 100
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpconfigBeaconFrameBlank_check_value( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long wtpconfigBeaconFrameBlank_val)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpconfigBeaconFrameBlank_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpconfigBeaconFrameBlank value.
     */

    return MFD_SUCCESS; /* wtpconfigBeaconFrameBlank value not illegal */
} /* wtpconfigBeaconFrameBlank_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11RadioStatsTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11RadioStatsTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpconfigBeaconFrameBlank_undo_setup( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpconfigBeaconFrameBlank_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpconfigBeaconFrameBlank undo.
     */
    /*
     * copy wtpconfigBeaconFrameBlank data
     * set rowreq_ctx->undo->wtpconfigBeaconFrameBlank from rowreq_ctx->data.wtpconfigBeaconFrameBlank
     */
    rowreq_ctx->undo->wtpconfigBeaconFrameBlank = rowreq_ctx->data.wtpconfigBeaconFrameBlank;


    return MFD_SUCCESS;
} /* wtpconfigBeaconFrameBlank_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpconfigBeaconFrameBlank_val
 *        A long containing the new value.
 */
int
wtpconfigBeaconFrameBlank_set( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long wtpconfigBeaconFrameBlank_val )
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpconfigBeaconFrameBlank_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpconfigBeaconFrameBlank reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpconfigBeaconFrameBlank value.
     * set wtpconfigBeaconFrameBlank value in rowreq_ctx->data
     */
    void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;
        
    int rc = MFD_ERROR;
	int ret = 0;
		
	ret = config_radio_beaconinterval(rowreq_ctx->data.parameter, connection,rowreq_ctx->data.setwtpRadCurrID,wtpconfigBeaconFrameBlank_val);	
	if(ret == 1)
	{		
		rowreq_ctx->data.wtpconfigBeaconFrameBlank = wtpconfigBeaconFrameBlank_val;		
		rc = MFD_SUCCESS;
	}
	else
	{
	    if(SNMPD_CONNECTION_ERROR == ret) {
            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
	    }
		rc = MFD_ERROR;
	}      

    return MFD_SUCCESS;
} /* wtpconfigBeaconFrameBlank_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpconfigBeaconFrameBlank_undo( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpconfigBeaconFrameBlank_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpconfigBeaconFrameBlank undo.
     */
    /*
     * copy wtpconfigBeaconFrameBlank data
     * set rowreq_ctx->data.wtpconfigBeaconFrameBlank from rowreq_ctx->undo->wtpconfigBeaconFrameBlank
     */
    rowreq_ctx->data.wtpconfigBeaconFrameBlank = rowreq_ctx->undo->wtpconfigBeaconFrameBlank;

    
    return MFD_SUCCESS;
} /* wtpconfigBeaconFrameBlank_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11RadioStatsTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11RadioStatsTable if you don't have dependencies)
 */
int
dot11RadioStatsTable_check_dependencies(dot11RadioStatsTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11RadioStatsTable:dot11RadioStatsTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11RadioStatsTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11RadioStatsTable_check_dependencies */

/** @} */
