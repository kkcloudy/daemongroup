/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11RadioStatsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11RadioStatsTable get routines.
 * TODO:240:M: Implement dot11RadioStatsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11RadioStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11RadioStatsTable is subid 1 of wtpRadio.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11RadioStatsTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAdd_dot11RadioStatsTabler_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpRadCurrID
 * wtpRadCurrID is subid 1 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.1
 * Description:
Current radio ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpRadCurrID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpRadCurrID_dot11RadioStatsTable_map(long *mib_wtpRadCurrID_val_ptr, long raw_wtpRadCurrID_val)
{
    netsnmp_assert(NULL != mib_wtpRadCurrID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRadCurrID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpRadCurrID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpRadCurrID_val_ptr) = raw_wtpRadCurrID_val;

    return MFD_SUCCESS;
} /* wtpRadCurrID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11RadioStatsTable_indexes_set_tbl_idx(dot11RadioStatsTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpRadCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    
    /* wtpRadCurrID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->wtpRadCurrID = wtpRadCurrID_val;
    

    return MFD_SUCCESS;
} /* dot11RadioStatsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11RadioStatsTable_indexes_set(dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len, long wtpRadCurrID_val)
{
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:dot11RadioStatsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11RadioStatsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
                                   , wtpRadCurrID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11RadioStatsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11RadioStatsTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpCurTxPower
 * wtpCurTxPower is subid 2 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.2
 * Description:
The  current transmission power value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpCurTxPower.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpRadCurrId_stats_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long * wtpRadCurrId_val_ptr )
{
   netsnmp_assert( NULL != wtpRadCurrId_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRadCurrId_val_ptr","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    (* wtpRadCurrId_val_ptr ) = rowreq_ctx->data.wtpRadCurrID;
    return MFD_SUCCESS;
} 
int
wtpCurTxPower_map(long *mib_wtpCurTxPower_val_ptr, long raw_wtpCurTxPower_val)
{
    netsnmp_assert(NULL != mib_wtpCurTxPower_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurTxPower_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpCurTxPower mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpCurTxPower_val_ptr) = raw_wtpCurTxPower_val;

    return MFD_SUCCESS;
} /* wtpCurTxPower_map */

/**
 * Extract the current value of the wtpCurTxPower data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCurTxPower_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpCurTxPower_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long * wtpCurTxPower_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpCurTxPower_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurTxPower_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpCurTxPower data.
 * set (* wtpCurTxPower_val_ptr ) from rowreq_ctx->data
 */
    (* wtpCurTxPower_val_ptr ) = rowreq_ctx->data.wtpCurTxPower;

    return MFD_SUCCESS;
} /* wtpCurTxPower_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpPowerManage
 * wtpPowerManage is subid 3 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.3
 * Description:
If power management is open,the AP will adjust the launching power;
						If it is closed,it needs manual adjustment.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  disable(0), enable(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpPowerManage.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpPowerManage_map(u_long *mib_wtpPowerManage_val_ptr, u_long raw_wtpPowerManage_val)
{
    netsnmp_assert(NULL != mib_wtpPowerManage_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpPowerManage_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpPowerManage enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpPowerManage_val) {
        case INTERNAL_WTPPOWERMANAGE_DISABLE:
             *mib_wtpPowerManage_val_ptr = WTPPOWERMANAGE_DISABLE;
             break;

        case INTERNAL_WTPPOWERMANAGE_ENABLE:
             *mib_wtpPowerManage_val_ptr = WTPPOWERMANAGE_ENABLE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpPowerManage\n", raw_wtpPowerManage_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpPowerManage_map */

/**
 * Extract the current value of the wtpPowerManage data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpPowerManage_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpPowerManage_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpPowerManage_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpPowerManage_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpPowerManage_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpPowerManage data.
 * set (* wtpPowerManage_val_ptr ) from rowreq_ctx->data
 */
    (* wtpPowerManage_val_ptr ) = rowreq_ctx->data.wtpPowerManage;

    return MFD_SUCCESS;
} /* wtpPowerManage_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpRxPower
 * wtpRxPower is subid 4 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.4
 * Description:
The the received power value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpRxPower.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpRxPower_map(long *mib_wtpRxPower_val_ptr, long raw_wtpRxPower_val)
{
    netsnmp_assert(NULL != mib_wtpRxPower_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRxPower_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpRxPower mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpRxPower_val_ptr) = raw_wtpRxPower_val;

    return MFD_SUCCESS;
} /* wtpRxPower_map */

/**
 * Extract the current value of the wtpRxPower data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRxPower_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpRxPower_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long * wtpRxPower_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpRxPower_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRxPower_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpRxPower data.
 * set (* wtpRxPower_val_ptr ) from rowreq_ctx->data
 */
    (* wtpRxPower_val_ptr ) = rowreq_ctx->data.wtpRxPower;

    return MFD_SUCCESS;
} /* wtpRxPower_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpCurTakeFreqPoint
 * wtpCurTakeFreqPoint is subid 5 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.5
 * Description:
The frequency occupied by the AP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpCurTakeFreqPoint.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpCurTakeFreqPoint_map(long *mib_wtpCurTakeFreqPoint_val_ptr, long raw_wtpCurTakeFreqPoint_val)
{
    netsnmp_assert(NULL != mib_wtpCurTakeFreqPoint_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurTakeFreqPoint_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpCurTakeFreqPoint mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpCurTakeFreqPoint_val_ptr) = raw_wtpCurTakeFreqPoint_val;

    return MFD_SUCCESS;
} /* wtpCurTakeFreqPoint_map */

/**
 * Extract the current value of the wtpCurTakeFreqPoint data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCurTakeFreqPoint_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpCurTakeFreqPoint_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long * wtpCurTakeFreqPoint_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpCurTakeFreqPoint_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurTakeFreqPoint_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpCurTakeFreqPoint data.
 * set (* wtpCurTakeFreqPoint_val_ptr ) from rowreq_ctx->data
 */
    (* wtpCurTakeFreqPoint_val_ptr ) = rowreq_ctx->data.wtpCurTakeFreqPoint;

    return MFD_SUCCESS;
} /* wtpCurTakeFreqPoint_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpCurConfChannel
 * wtpCurConfChannel is subid 6 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.6
 * Description:
The channel of the configuration.(0 means auto)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 14;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpCurConfChannel.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpCurConfChannel_map(long *mib_wtpCurConfChannel_val_ptr, long raw_wtpCurConfChannel_val)
{
    netsnmp_assert(NULL != mib_wtpCurConfChannel_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurConfChannel_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpCurConfChannel mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpCurConfChannel_val_ptr) = raw_wtpCurConfChannel_val;

    return MFD_SUCCESS;
} /* wtpCurConfChannel_map */

/**
 * Extract the current value of the wtpCurConfChannel data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCurConfChannel_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpCurConfChannel_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long * wtpCurConfChannel_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpCurConfChannel_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpCurConfChannel_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpCurConfChannel data.
 * set (* wtpCurConfChannel_val_ptr ) from rowreq_ctx->data
 */
    (* wtpCurConfChannel_val_ptr ) = rowreq_ctx->data.wtpCurConfChannel;

    return MFD_SUCCESS;
} /* wtpCurConfChannel_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpProtoMode
 * wtpProtoMode is subid 7 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.7
 * Description:
AP protocol mode.(bg,b,g)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  bg(1), g(2), b(3), a(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpProtoMode.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpProtoMode_map(u_long *mib_wtpProtoMode_val_ptr, u_long raw_wtpProtoMode_val)
{
    netsnmp_assert(NULL != mib_wtpProtoMode_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpProtoMode_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpProtoMode enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpProtoMode_val) {
        case INTERNAL_WTPPROTOMODE_A:
             *mib_wtpProtoMode_val_ptr = WTPPROTOMODE_A;
             break;

        case INTERNAL_WTPPROTOMODE_B:
             *mib_wtpProtoMode_val_ptr = WTPPROTOMODE_B;
             break;

        case INTERNAL_WTPPROTOMODE_G:
             *mib_wtpProtoMode_val_ptr = WTPPROTOMODE_G;
             break;

        case INTERNAL_WTPPROTOMODE_BG:
             *mib_wtpProtoMode_val_ptr = WTPPROTOMODE_BG;
             break;
             
        case INTERNAL_WTPPROTOMODE_AN:
            *mib_wtpProtoMode_val_ptr = WTPPROTOMODE_AN;
            break;

        case INTERNAL_WTPPROTOMODE_AAN:
            *mib_wtpProtoMode_val_ptr = WTPPROTOMODE_AAN;
            break;

        case INTERNAL_WTPPROTOMODE_GN:
            *mib_wtpProtoMode_val_ptr = WTPPROTOMODE_GN;
            break;

        case INTERNAL_WTPPROTOMODE_GGN:
            *mib_wtpProtoMode_val_ptr = WTPPROTOMODE_GGN;
            break;
            
        case INTERNAL_WTPPROTOMODE_BGGN:
            *mib_wtpProtoMode_val_ptr = WTPPROTOMODE_BGGN;
            break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpProtoMode\n", raw_wtpProtoMode_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpProtoMode_map */

/**
 * Extract the current value of the wtpProtoMode data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpProtoMode_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpProtoMode_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpProtoMode_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpProtoMode_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpProtoMode_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpProtoMode data.
 * set (* wtpProtoMode_val_ptr ) from rowreq_ctx->data
 */
    (* wtpProtoMode_val_ptr ) = rowreq_ctx->data.wtpProtoMode;

    return MFD_SUCCESS;
} /* wtpProtoMode_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpSampleTime
 * wtpSampleTime is subid 8 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.8
 * Description:
The sample time.(s)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSampleTime.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSampleTime_map(u_long *mib_wtpSampleTime_val_ptr, u_long raw_wtpSampleTime_val)
{
    netsnmp_assert(NULL != mib_wtpSampleTime_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpSampleTime_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSampleTime mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSampleTime_val_ptr) = raw_wtpSampleTime_val;

    return MFD_SUCCESS;
} /* wtpSampleTime_map */

/**
 * Extract the current value of the wtpSampleTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSampleTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSampleTime_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpSampleTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSampleTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpSampleTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSampleTime data.
 * set (* wtpSampleTime_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSampleTime_val_ptr ) = rowreq_ctx->data.wtpSampleTime;

    return MFD_SUCCESS;
} /* wtpSampleTime_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpTerminalConRate
 * wtpTerminalConRate is subid 9 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.9
 * Description:
The rate that configed terminal switch in.Select input value 540,480,360,240,120,110,90,60,55,20,10.While getting value,the value will be devided by 10bps(Units M/bps) 
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTerminalConRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTerminalConRate_map(long *mib_wtpTerminalConRate_val_ptr, long raw_wtpTerminalConRate_val)
{
    netsnmp_assert(NULL != mib_wtpTerminalConRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpTerminalConRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpTerminalConRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpTerminalConRate_val_ptr) = raw_wtpTerminalConRate_val;

    return MFD_SUCCESS;
} /* wtpTerminalConRate_map */

/**
 * Extract the current value of the wtpTerminalConRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTerminalConRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpTerminalConRate_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long * wtpTerminalConRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpTerminalConRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpTerminalConRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTerminalConRate data.
 * set (* wtpTerminalConRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpTerminalConRate_val_ptr ) = rowreq_ctx->data.wtpTerminalConRate;

    return MFD_SUCCESS;
} /* wtpTerminalConRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpConfigAvailSpeed
 * wtpConfigAvailSpeed is subid 10 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.10
 * Description:
The configed available speed congregation.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpConfigAvailSpeed.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpConfigAvailSpeed_map(char **mib_wtpConfigAvailSpeed_val_ptr_ptr, size_t *mib_wtpConfigAvailSpeed_val_ptr_len_ptr, char *raw_wtpConfigAvailSpeed_val_ptr, size_t raw_wtpConfigAvailSpeed_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpConfigAvailSpeed_val_ptr);
    netsnmp_assert((NULL != mib_wtpConfigAvailSpeed_val_ptr_ptr) && (NULL != mib_wtpConfigAvailSpeed_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpConfigAvailSpeed_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpConfigAvailSpeed non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpConfigAvailSpeed_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpConfigAvailSpeed_val_ptr_ptr) || (*mib_wtpConfigAvailSpeed_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpConfigAvailSpeed_val_ptr_ptr = realloc( *mib_wtpConfigAvailSpeed_val_ptr_ptr, converted_len * sizeof(**mib_wtpConfigAvailSpeed_val_ptr_ptr));
        if(NULL == *mib_wtpConfigAvailSpeed_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpConfigAvailSpeed_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpConfigAvailSpeed_val_ptr_ptr, raw_wtpConfigAvailSpeed_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpConfigAvailSpeed_map */

/**
 * Extract the current value of the wtpConfigAvailSpeed data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpConfigAvailSpeed_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpConfigAvailSpeed_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpConfigAvailSpeed.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpConfigAvailSpeed_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpConfigAvailSpeed_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpConfigAvailSpeed_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, char **wtpConfigAvailSpeed_val_ptr_ptr, size_t *wtpConfigAvailSpeed_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpConfigAvailSpeed_val_ptr_ptr) && (NULL != *wtpConfigAvailSpeed_val_ptr_ptr));
   netsnmp_assert( NULL != wtpConfigAvailSpeed_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpConfigAvailSpeed_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpConfigAvailSpeed data.
 * set (* wtpConfigAvailSpeed_val_ptr_ptr ) and (* wtpConfigAvailSpeed_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpConfigAvailSpeed data
     */
    if ((NULL == (* wtpConfigAvailSpeed_val_ptr_ptr )) ||
        ((* wtpConfigAvailSpeed_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpConfigAvailSpeed_len * sizeof((* wtpConfigAvailSpeed_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpConfigAvailSpeed data
         */
        (* wtpConfigAvailSpeed_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpConfigAvailSpeed_len * sizeof((* wtpConfigAvailSpeed_val_ptr_ptr )[0]));
        if(NULL == (* wtpConfigAvailSpeed_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpConfigAvailSpeed_val_ptr_len_ptr ) = rowreq_ctx->data.wtpConfigAvailSpeed_len * sizeof((* wtpConfigAvailSpeed_val_ptr_ptr )[0]);
    memcpy( (* wtpConfigAvailSpeed_val_ptr_ptr ), rowreq_ctx->data.wtpConfigAvailSpeed, (* wtpConfigAvailSpeed_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpConfigAvailSpeed_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpConfigForceSpeed
 * wtpConfigForceSpeed is subid 11 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.11
 * Description:
The configed enforced speed congregation.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpConfigForceSpeed.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpConfigForceSpeed_map(char **mib_wtpConfigForceSpeed_val_ptr_ptr, size_t *mib_wtpConfigForceSpeed_val_ptr_len_ptr, char *raw_wtpConfigForceSpeed_val_ptr, size_t raw_wtpConfigForceSpeed_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpConfigForceSpeed_val_ptr);
    netsnmp_assert((NULL != mib_wtpConfigForceSpeed_val_ptr_ptr) && (NULL != mib_wtpConfigForceSpeed_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpConfigForceSpeed_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpConfigForceSpeed non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpConfigForceSpeed_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpConfigForceSpeed_val_ptr_ptr) || (*mib_wtpConfigForceSpeed_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpConfigForceSpeed_val_ptr_ptr = realloc( *mib_wtpConfigForceSpeed_val_ptr_ptr, converted_len * sizeof(**mib_wtpConfigForceSpeed_val_ptr_ptr));
        if(NULL == *mib_wtpConfigForceSpeed_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpConfigForceSpeed_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpConfigForceSpeed_val_ptr_ptr, raw_wtpConfigForceSpeed_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpConfigForceSpeed_map */

/**
 * Extract the current value of the wtpConfigForceSpeed data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpConfigForceSpeed_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpConfigForceSpeed_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpConfigForceSpeed.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpConfigForceSpeed_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpConfigForceSpeed_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpConfigForceSpeed_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, char **wtpConfigForceSpeed_val_ptr_ptr, size_t *wtpConfigForceSpeed_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpConfigForceSpeed_val_ptr_ptr) && (NULL != *wtpConfigForceSpeed_val_ptr_ptr));
   netsnmp_assert( NULL != wtpConfigForceSpeed_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpConfigForceSpeed_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpConfigForceSpeed data.
 * set (* wtpConfigForceSpeed_val_ptr_ptr ) and (* wtpConfigForceSpeed_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpConfigForceSpeed data
     */
    if ((NULL == (* wtpConfigForceSpeed_val_ptr_ptr )) ||
        ((* wtpConfigForceSpeed_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpConfigForceSpeed_len * sizeof((* wtpConfigForceSpeed_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpConfigForceSpeed data
         */
        (* wtpConfigForceSpeed_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpConfigForceSpeed_len * sizeof((* wtpConfigForceSpeed_val_ptr_ptr )[0]));
        if(NULL == (* wtpConfigForceSpeed_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpConfigForceSpeed_val_ptr_len_ptr ) = rowreq_ctx->data.wtpConfigForceSpeed_len * sizeof((* wtpConfigForceSpeed_val_ptr_ptr )[0]);
    memcpy( (* wtpConfigForceSpeed_val_ptr_ptr ), rowreq_ctx->data.wtpConfigForceSpeed, (* wtpConfigForceSpeed_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpConfigForceSpeed_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpRTSCTSEnable
 * wtpRTSCTSEnable is subid 12 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.12
 * Description:
RTS/CTS whether start using.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  disable(0), enable(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpRTSCTSEnable.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpRTSCTSEnable_map(u_long *mib_wtpRTSCTSEnable_val_ptr, u_long raw_wtpRTSCTSEnable_val)
{
    netsnmp_assert(NULL != mib_wtpRTSCTSEnable_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRTSCTSEnable_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpRTSCTSEnable enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpRTSCTSEnable_val) {
        case INTERNAL_WTPRTSCTSENABLE_DISABLE:
             *mib_wtpRTSCTSEnable_val_ptr = WTPRTSCTSENABLE_DISABLE;
             break;

        case INTERNAL_WTPRTSCTSENABLE_ENABLE:
             *mib_wtpRTSCTSEnable_val_ptr = WTPRTSCTSENABLE_ENABLE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpRTSCTSEnable\n", raw_wtpRTSCTSEnable_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpRTSCTSEnable_map */

/**
 * Extract the current value of the wtpRTSCTSEnable data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpRTSCTSEnable_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpRTSCTSEnable_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, u_long * wtpRTSCTSEnable_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpRTSCTSEnable_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpRTSCTSEnable_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpRTSCTSEnable data.
 * set (* wtpRTSCTSEnable_val_ptr ) from rowreq_ctx->data
 */
    (* wtpRTSCTSEnable_val_ptr ) = rowreq_ctx->data.wtpRTSCTSEnable;

    return MFD_SUCCESS;
} /* wtpRTSCTSEnable_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11RadioStatsEntry.wtpconfigBeaconFrameBlank
 * wtpconfigBeaconFrameBlank is subid 13 of dot11RadioStatsEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.4.1.1.13
 * Description:
The configed beacon frame blank.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  25 - 100;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpconfigBeaconFrameBlank.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpconfigBeaconFrameBlank_map(long *mib_wtpconfigBeaconFrameBlank_val_ptr, long raw_wtpconfigBeaconFrameBlank_val)
{
    netsnmp_assert(NULL != mib_wtpconfigBeaconFrameBlank_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpconfigBeaconFrameBlank_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpconfigBeaconFrameBlank mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpconfigBeaconFrameBlank_val_ptr) = raw_wtpconfigBeaconFrameBlank_val;

    return MFD_SUCCESS;
} /* wtpconfigBeaconFrameBlank_map */

/**
 * Extract the current value of the wtpconfigBeaconFrameBlank data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpconfigBeaconFrameBlank_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpconfigBeaconFrameBlank_get( dot11RadioStatsTable_rowreq_ctx *rowreq_ctx, long * wtpconfigBeaconFrameBlank_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpconfigBeaconFrameBlank_val_ptr );


    DEBUGMSGTL(("verbose:dot11RadioStatsTable:wtpconfigBeaconFrameBlank_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpconfigBeaconFrameBlank data.
 * set (* wtpconfigBeaconFrameBlank_val_ptr ) from rowreq_ctx->data
 */
    (* wtpconfigBeaconFrameBlank_val_ptr ) = rowreq_ctx->data.wtpconfigBeaconFrameBlank;

    return MFD_SUCCESS;
} /* wtpconfigBeaconFrameBlank_get */



/** @} */
