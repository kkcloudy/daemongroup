/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigWtpTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigWtpTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigWtpTable is subid 2 of dot11wtpConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigWtpTable_undo_setup( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigWtpTable undo.
     * set up dot11ConfigWtpTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11ConfigWtpTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigWtpTable_undo_cleanup( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigWtpTable undo.
     */

    return rc;
} /* dot11ConfigWtpTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigWtpTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigWtpTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigWtpTable_commit( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11ConfigWtpTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_WTPBINDPORT) {
       save_flags &= ~FLAG_WTPBINDPORT; /* clear wtpBindPort */
       /*
        * TODO:482:o: |-> commit column wtpBindPort.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWtpTable column wtpBindPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpBindPort
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPBINDPORT;
       }
    }

    if (save_flags & FLAG_WTPUSED) {
       save_flags &= ~FLAG_WTPUSED; /* clear wtpUsed */
       /*
        * TODO:482:o: |-> commit column wtpUsed.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWtpTable column wtpUsed commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpUsed
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPUSED;
       }
    }

    if (save_flags & FLAG_WTPMAXSTANUM) {
       save_flags &= ~FLAG_WTPMAXSTANUM; /* clear wtpMaxStaNum */
       /*
        * TODO:482:o: |-> commit column wtpMaxStaNum.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWtpTable column wtpMaxStaNum commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpMaxStaNum
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPMAXSTANUM;
       }
    }

    if (save_flags & FLAG_WTPLOADBALANCETRIGERBASEUSR) {
       save_flags &= ~FLAG_WTPLOADBALANCETRIGERBASEUSR; /* clear wtpLoadBalanceTrigerBaseUsr */
       /*
        * TODO:482:o: |-> commit column wtpLoadBalanceTrigerBaseUsr.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWtpTable column wtpLoadBalanceTrigerBaseUsr commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpLoadBalanceTrigerBaseUsr
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPLOADBALANCETRIGERBASEUSR;
       }
    }

    if (save_flags & FLAG_WTPLOADBALANCETRIGERBASEFLOW) {
       save_flags &= ~FLAG_WTPLOADBALANCETRIGERBASEFLOW; /* clear wtpLoadBalanceTrigerBaseFlow */
       /*
        * TODO:482:o: |-> commit column wtpLoadBalanceTrigerBaseFlow.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigWtpTable column wtpLoadBalanceTrigerBaseFlow commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wtpLoadBalanceTrigerBaseFlow
             */
            rowreq_ctx->column_set_flags |= FLAG_WTPLOADBALANCETRIGERBASEFLOW;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigWtpTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigWtpTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigWtpTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigWtpTable_undo_commit( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigWtpTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11ConfigWtpTable_undo_commit */

/*
 * TODO:420:r: Implement dot11ConfigWtpTable index validation.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11CreateWtpEntry.wtpID
 * wtpID is subid 1 of dot11CreateWtpEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1.1.1
 * Description:
Create AP ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 1023;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/*
 * TODO:440:M: Implement dot11ConfigWtpTable node value checks.
 * TODO:450:M: Implement dot11ConfigWtpTable undo functions.
 * TODO:460:M: Implement dot11ConfigWtpTable set functions.
 * TODO:480:M: Implement dot11ConfigWtpTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWtpEntry.wtpBindPort
 * wtpBindPort is subid 1 of dot11ConfigWtpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2.1.1
 * Description:
Bind port.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpBindPort_val_ptr
 *        A char containing the new value.
 * @param wtpBindPort_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpBindPort_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWtpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpBindPort_check_value( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, char *wtpBindPort_val_ptr,  size_t wtpBindPort_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpBindPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpBindPort_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wtpBindPort value.
     */

    return MFD_SUCCESS; /* wtpBindPort value not illegal */
} /* wtpBindPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWtpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpBindPort_undo_setup( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpBindPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpBindPort undo.
     */
    /*
     * copy wtpBindPort and wtpBindPort_len data
     * set rowreq_ctx->undo->wtpBindPort from rowreq_ctx->data.wtpBindPort
     */
    memcpy( rowreq_ctx->undo->wtpBindPort, rowreq_ctx->data.wtpBindPort,
            (rowreq_ctx->data.wtpBindPort_len * sizeof(rowreq_ctx->undo->wtpBindPort[0])));
    rowreq_ctx->undo->wtpBindPort_len = rowreq_ctx->data.wtpBindPort_len;


    return MFD_SUCCESS;
} /* wtpBindPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpBindPort_val_ptr
 *        A char containing the new value.
 * @param wtpBindPort_val_ptr_len
 *        The size (in bytes) of the data pointed to by wtpBindPort_val_ptr
 */
int
wtpBindPort_set( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, char *wtpBindPort_val_ptr,  size_t wtpBindPort_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpBindPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wtpBindPort_val_ptr);

/*
 * TODO:245:o: |-> Implement wtpBindPort reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpBindPort value.
     * set wtpBindPort value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
	    return MFD_ERROR;

    int rc = MFD_ERROR;
	int ret = 0;

	ret = wtp_apply_interface(rowreq_ctx->data.parameter,connection,rowreq_ctx->data.localwtpID,wtpBindPort_val_ptr);
	if(ret ==1)
	{
		memcpy( rowreq_ctx->data.wtpBindPort, wtpBindPort_val_ptr, wtpBindPort_val_ptr_len );
		rowreq_ctx->data.wtpBindPort_len = wtpBindPort_val_ptr_len / sizeof(wtpBindPort_val_ptr[0]);
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpBindPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpBindPort_undo( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpBindPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpBindPort undo.
     */
    /*
     * copy wtpBindPort and wtpBindPort_len data
     * set rowreq_ctx->data.wtpBindPort from rowreq_ctx->undo->wtpBindPort
     */
    memcpy( rowreq_ctx->data.wtpBindPort, rowreq_ctx->undo->wtpBindPort,
            (rowreq_ctx->undo->wtpBindPort_len * sizeof(rowreq_ctx->data.wtpBindPort[0])));
    rowreq_ctx->data.wtpBindPort_len = rowreq_ctx->undo->wtpBindPort_len;

    
    return MFD_SUCCESS;
} /* wtpBindPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWtpEntry.wtpUsed
 * wtpUsed is subid 3 of dot11ConfigWtpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2.1.3
 * Description:
AP whether used.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  unused(0), used(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUsed_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWtpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  unused(0), used(1)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpUsed_check_value( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, u_long wtpUsed_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpUsed_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpUsed value.
     */

    return MFD_SUCCESS; /* wtpUsed value not illegal */
} /* wtpUsed_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWtpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpUsed_undo_setup( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpUsed_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpUsed undo.
     */
    /*
     * copy wtpUsed data
     * set rowreq_ctx->undo->wtpUsed from rowreq_ctx->data.wtpUsed
     */
    rowreq_ctx->undo->wtpUsed = rowreq_ctx->data.wtpUsed;


    return MFD_SUCCESS;
} /* wtpUsed_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpUsed_val
 *        A long containing the new value.
 */
int
wtpUsed_set( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, u_long wtpUsed_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpUsed_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpUsed reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    switch(wtpUsed_val) {
        case WTPUSED_UNUSED:
             wtpUsed_val = INTERNAL_WTPUSED_UNUSED;
             break;

        case WTPUSED_USED:
             wtpUsed_val = INTERNAL_WTPUSED_USED;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't reverse map value %ld for wtpUsed\n", wtpUsed_val );
                 return SNMP_ERR_GENERR;
    }
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    /*
     * TODO:461:M: |-> Set wtpUsed value.
     * set wtpUsed value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
	    return MFD_ERROR;
	
    int rc = MFD_ERROR;
	int ret = 0;

	ret = wtp_used(rowreq_ctx->data.parameter,connection,rowreq_ctx->data.localwtpID,!(wtpUsed_val));
	if(ret ==1)
	{
		rowreq_ctx->data.wtpUsed = wtpUsed_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpUsed_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpUsed_undo( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpUsed_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpUsed undo.
     */
    /*
     * copy wtpUsed data
     * set rowreq_ctx->data.wtpUsed from rowreq_ctx->undo->wtpUsed
     */
    rowreq_ctx->data.wtpUsed = rowreq_ctx->undo->wtpUsed;

    
    return MFD_SUCCESS;
} /* wtpUsed_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWtpEntry.wtpMaxStaNum
 * wtpMaxStaNum is subid 4 of dot11ConfigWtpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2.1.4
 * Description:
Max station number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 64;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMaxStaNum_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWtpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - 64
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpMaxStaNum_check_value( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, long wtpMaxStaNum_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpMaxStaNum_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpMaxStaNum value.
     */

    return MFD_SUCCESS; /* wtpMaxStaNum value not illegal */
} /* wtpMaxStaNum_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWtpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpMaxStaNum_undo_setup( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpMaxStaNum_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpMaxStaNum undo.
     */
    /*
     * copy wtpMaxStaNum data
     * set rowreq_ctx->undo->wtpMaxStaNum from rowreq_ctx->data.wtpMaxStaNum
     */
    rowreq_ctx->undo->wtpMaxStaNum = rowreq_ctx->data.wtpMaxStaNum;


    return MFD_SUCCESS;
} /* wtpMaxStaNum_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpMaxStaNum_val
 *        A long containing the new value.
 */
int
wtpMaxStaNum_set( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, long wtpMaxStaNum_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpMaxStaNum_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpMaxStaNum reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpMaxStaNum value.
     * set wtpMaxStaNum value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
	    return MFD_ERROR;
	
    int rc = MFD_ERROR;
	int ret = 0;
	char max_num[20] = { 0 };
	memset(max_num,0,20);
	snprintf(max_num,sizeof(max_num)-1,"%d",wtpMaxStaNum_val);

	ret = config_wtp_max_sta_num(rowreq_ctx->data.parameter,connection,rowreq_ctx->data.localwtpID,max_num);
	if(ret ==1)
	{
		rowreq_ctx->data.wtpMaxStaNum = wtpMaxStaNum_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return rc;
} /* wtpMaxStaNum_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpMaxStaNum_undo( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpMaxStaNum_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpMaxStaNum undo.
     */
    /*
     * copy wtpMaxStaNum data
     * set rowreq_ctx->data.wtpMaxStaNum from rowreq_ctx->undo->wtpMaxStaNum
     */
    rowreq_ctx->data.wtpMaxStaNum = rowreq_ctx->undo->wtpMaxStaNum;

    
    return MFD_SUCCESS;
} /* wtpMaxStaNum_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWtpEntry.wtpLoadBalanceTrigerBaseUsr
 * wtpLoadBalanceTrigerBaseUsr is subid 5 of dot11ConfigWtpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2.1.5
 * Description:
Load balance base on user.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 64;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpLoadBalanceTrigerBaseUsr_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWtpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 64
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpLoadBalanceTrigerBaseUsr_check_value( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, long wtpLoadBalanceTrigerBaseUsr_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseUsr_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpLoadBalanceTrigerBaseUsr value.
     */

    return MFD_SUCCESS; /* wtpLoadBalanceTrigerBaseUsr value not illegal */
} /* wtpLoadBalanceTrigerBaseUsr_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWtpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpLoadBalanceTrigerBaseUsr_undo_setup( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseUsr_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpLoadBalanceTrigerBaseUsr undo.
     */
    /*
     * copy wtpLoadBalanceTrigerBaseUsr data
     * set rowreq_ctx->undo->wtpLoadBalanceTrigerBaseUsr from rowreq_ctx->data.wtpLoadBalanceTrigerBaseUsr
     */
    rowreq_ctx->undo->wtpLoadBalanceTrigerBaseUsr = rowreq_ctx->data.wtpLoadBalanceTrigerBaseUsr;


    return MFD_SUCCESS;
} /* wtpLoadBalanceTrigerBaseUsr_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpLoadBalanceTrigerBaseUsr_val
 *        A long containing the new value.
 */
int
wtpLoadBalanceTrigerBaseUsr_set( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, long wtpLoadBalanceTrigerBaseUsr_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseUsr_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpLoadBalanceTrigerBaseUsr reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpLoadBalanceTrigerBaseUsr value.
     * set wtpLoadBalanceTrigerBaseUsr value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
	    return MFD_ERROR;
	
    int rc = MFD_ERROR;
	int ret = 0;
	char number[10] = { 0 };
	memset(number,0,10);
	snprintf(number,sizeof(number)-1,"%d",wtpLoadBalanceTrigerBaseUsr_val);

	ret = config_wtp_triger_num(rowreq_ctx->data.parameter,connection,rowreq_ctx->data.localwtpID,number);
	if(ret ==1)
	{
		rowreq_ctx->data.wtpLoadBalanceTrigerBaseUsr = wtpLoadBalanceTrigerBaseUsr_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpLoadBalanceTrigerBaseUsr_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpLoadBalanceTrigerBaseUsr_undo( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseUsr_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpLoadBalanceTrigerBaseUsr undo.
     */
    /*
     * copy wtpLoadBalanceTrigerBaseUsr data
     * set rowreq_ctx->data.wtpLoadBalanceTrigerBaseUsr from rowreq_ctx->undo->wtpLoadBalanceTrigerBaseUsr
     */
    rowreq_ctx->data.wtpLoadBalanceTrigerBaseUsr = rowreq_ctx->undo->wtpLoadBalanceTrigerBaseUsr;

    
    return MFD_SUCCESS;
} /* wtpLoadBalanceTrigerBaseUsr_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWtpEntry.wtpLoadBalanceTrigerBaseFlow
 * wtpLoadBalanceTrigerBaseFlow is subid 6 of dot11ConfigWtpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2.1.6
 * Description:
Load balance base on user.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 1024;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpLoadBalanceTrigerBaseFlow_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigWtpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - 1024
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wtpLoadBalanceTrigerBaseFlow_check_value( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, long wtpLoadBalanceTrigerBaseFlow_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseFlow_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wtpLoadBalanceTrigerBaseFlow value.
     */

    return MFD_SUCCESS; /* wtpLoadBalanceTrigerBaseFlow value not illegal */
} /* wtpLoadBalanceTrigerBaseFlow_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigWtpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigWtpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wtpLoadBalanceTrigerBaseFlow_undo_setup( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseFlow_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wtpLoadBalanceTrigerBaseFlow undo.
     */
    /*
     * copy wtpLoadBalanceTrigerBaseFlow data
     * set rowreq_ctx->undo->wtpLoadBalanceTrigerBaseFlow from rowreq_ctx->data.wtpLoadBalanceTrigerBaseFlow
     */
    rowreq_ctx->undo->wtpLoadBalanceTrigerBaseFlow = rowreq_ctx->data.wtpLoadBalanceTrigerBaseFlow;


    return MFD_SUCCESS;
} /* wtpLoadBalanceTrigerBaseFlow_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wtpLoadBalanceTrigerBaseFlow_val
 *        A long containing the new value.
 */
int
wtpLoadBalanceTrigerBaseFlow_set( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, long wtpLoadBalanceTrigerBaseFlow_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseFlow_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement wtpLoadBalanceTrigerBaseFlow reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set wtpLoadBalanceTrigerBaseFlow value.
     * set wtpLoadBalanceTrigerBaseFlow value in rowreq_ctx->data
     */
    void *connection = NULL;
	if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
	    return MFD_ERROR;
	
    int rc = MFD_ERROR;
	int ret = 0;
	char number[10] = { 0 };
	memset(number,0,10);
	snprintf(number,sizeof(number)-1,"%d",wtpLoadBalanceTrigerBaseFlow_val);

	ret = set_wtp_flow_trige(rowreq_ctx->data.parameter,connection,rowreq_ctx->data.localwtpID,number);
	if(ret ==1)
	{
		rowreq_ctx->data.wtpLoadBalanceTrigerBaseFlow = wtpLoadBalanceTrigerBaseFlow_val;
		rc = MFD_SUCCESS;
	}
	else
	{
		if(SNMPD_CONNECTION_ERROR == ret) {
			close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
		}
		rc = MFD_ERROR;
	}

    return MFD_SUCCESS;
} /* wtpLoadBalanceTrigerBaseFlow_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wtpLoadBalanceTrigerBaseFlow_undo( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseFlow_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wtpLoadBalanceTrigerBaseFlow undo.
     */
    /*
     * copy wtpLoadBalanceTrigerBaseFlow data
     * set rowreq_ctx->data.wtpLoadBalanceTrigerBaseFlow from rowreq_ctx->undo->wtpLoadBalanceTrigerBaseFlow
     */
    rowreq_ctx->data.wtpLoadBalanceTrigerBaseFlow = rowreq_ctx->undo->wtpLoadBalanceTrigerBaseFlow;

    
    return MFD_SUCCESS;
} /* wtpLoadBalanceTrigerBaseFlow_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigWtpTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigWtpTable if you don't have dependencies)
 */
int
dot11ConfigWtpTable_check_dependencies(dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigWtpTable:dot11ConfigWtpTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigWtpTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11ConfigWtpTable_check_dependencies */

/** @} */
