/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigWtpTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11ConfigWtpTable get routines.
 * TODO:240:M: Implement dot11ConfigWtpTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigWtpTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigWtpTable is subid 2 of dot11wtpConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11ConfigWtpTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11CreateWtpEntry.wtpID
 * wtpID is subid 1 of dot11CreateWtpEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.1.1.1
 * Description:
Create AP ID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 1023;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpID_map(long *mib_wtpID_val_ptr, long raw_wtpID_val)
{
    netsnmp_assert(NULL != mib_wtpID_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpID_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpID mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpID_val_ptr) = raw_wtpID_val;

    return MFD_SUCCESS;
} /* wtpID_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11ConfigWtpTable_indexes_set_tbl_idx(dot11ConfigWtpTable_mib_index *tbl_idx, long wtpID_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_indexes_set_tbl_idx","called\n"));

    /* wtpID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h */
    tbl_idx->wtpID = wtpID_val;
    

    return MFD_SUCCESS;
} /* dot11ConfigWtpTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11ConfigWtpTable_indexes_set(dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, long wtpID_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11ConfigWtpTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpID_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11ConfigWtpTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11ConfigWtpTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWtpEntry.wtpBindPort
 * wtpBindPort is subid 1 of dot11ConfigWtpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2.1.1
 * Description:
Bind port.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpBindPort.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpBindPort_map(char **mib_wtpBindPort_val_ptr_ptr, size_t *mib_wtpBindPort_val_ptr_len_ptr, char *raw_wtpBindPort_val_ptr, size_t raw_wtpBindPort_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpBindPort_val_ptr);
    netsnmp_assert((NULL != mib_wtpBindPort_val_ptr_ptr) && (NULL != mib_wtpBindPort_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpBindPort_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpBindPort non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpBindPort_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpBindPort_val_ptr_ptr) || (*mib_wtpBindPort_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpBindPort_val_ptr_ptr = realloc( *mib_wtpBindPort_val_ptr_ptr, converted_len * sizeof(**mib_wtpBindPort_val_ptr_ptr));
        if(NULL == *mib_wtpBindPort_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpBindPort_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpBindPort_val_ptr_ptr, raw_wtpBindPort_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpBindPort_map */

/**
 * Extract the current value of the wtpBindPort data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpBindPort_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpBindPort_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpBindPort.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpBindPort_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpBindPort_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpBindPort_get( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, char **wtpBindPort_val_ptr_ptr, size_t *wtpBindPort_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpBindPort_val_ptr_ptr) && (NULL != *wtpBindPort_val_ptr_ptr));
   netsnmp_assert( NULL != wtpBindPort_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpBindPort_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpBindPort data.
 * set (* wtpBindPort_val_ptr_ptr ) and (* wtpBindPort_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpBindPort data
     */
    if ((NULL == (* wtpBindPort_val_ptr_ptr )) ||
        ((* wtpBindPort_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpBindPort_len * sizeof((* wtpBindPort_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpBindPort data
         */
        (* wtpBindPort_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpBindPort_len * sizeof((* wtpBindPort_val_ptr_ptr )[0]));
        if(NULL == (* wtpBindPort_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpBindPort_val_ptr_len_ptr ) = rowreq_ctx->data.wtpBindPort_len * sizeof((* wtpBindPort_val_ptr_ptr )[0]);
    memcpy( (* wtpBindPort_val_ptr_ptr ), rowreq_ctx->data.wtpBindPort, (* wtpBindPort_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpBindPort_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWtpEntry.wtpBindWlan
 * wtpBindWlan is subid 2 of dot11ConfigWtpEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2.1.2
 * Description:
Bind wlan.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpBindWlan.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpBindWlan_map(char **mib_wtpBindWlan_val_ptr_ptr, size_t *mib_wtpBindWlan_val_ptr_len_ptr, char *raw_wtpBindWlan_val_ptr, size_t raw_wtpBindWlan_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpBindWlan_val_ptr);
    netsnmp_assert((NULL != mib_wtpBindWlan_val_ptr_ptr) && (NULL != mib_wtpBindWlan_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpBindWlan_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpBindWlan non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpBindWlan_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpBindWlan_val_ptr_ptr) || (*mib_wtpBindWlan_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpBindWlan_val_ptr_ptr = realloc( *mib_wtpBindWlan_val_ptr_ptr, converted_len * sizeof(**mib_wtpBindWlan_val_ptr_ptr));
        if(NULL == *mib_wtpBindWlan_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpBindWlan_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpBindWlan_val_ptr_ptr, raw_wtpBindWlan_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpBindWlan_map */

/**
 * Extract the current value of the wtpBindWlan data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpBindWlan_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpBindWlan_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpBindWlan.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpBindWlan_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpBindWlan_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpBindWlan_get( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, char **wtpBindWlan_val_ptr_ptr, size_t *wtpBindWlan_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpBindWlan_val_ptr_ptr) && (NULL != *wtpBindWlan_val_ptr_ptr));
   netsnmp_assert( NULL != wtpBindWlan_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpBindWlan_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpBindWlan data.
 * set (* wtpBindWlan_val_ptr_ptr ) and (* wtpBindWlan_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpBindWlan data
     */
    if ((NULL == (* wtpBindWlan_val_ptr_ptr )) ||
        ((* wtpBindWlan_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpBindWlan_len * sizeof((* wtpBindWlan_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpBindWlan data
         */
        (* wtpBindWlan_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpBindWlan_len * sizeof((* wtpBindWlan_val_ptr_ptr )[0]));
        if(NULL == (* wtpBindWlan_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpBindWlan_val_ptr_len_ptr ) = rowreq_ctx->data.wtpBindWlan_len * sizeof((* wtpBindWlan_val_ptr_ptr )[0]);
    memcpy( (* wtpBindWlan_val_ptr_ptr ), rowreq_ctx->data.wtpBindWlan, (* wtpBindWlan_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpBindWlan_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWtpEntry.wtpUsed
 * wtpUsed is subid 3 of dot11ConfigWtpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2.1.3
 * Description:
AP whether used.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  unused(0), used(1)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpUsed.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpUsed_map(u_long *mib_wtpUsed_val_ptr, u_long raw_wtpUsed_val)
{
    netsnmp_assert(NULL != mib_wtpUsed_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpUsed_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpUsed enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpUsed_val) {
        case INTERNAL_WTPUSED_UNUSED:
             *mib_wtpUsed_val_ptr = WTPUSED_UNUSED;
             break;

        case INTERNAL_WTPUSED_USED:
             *mib_wtpUsed_val_ptr = WTPUSED_USED;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpUsed\n", raw_wtpUsed_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpUsed_map */

/**
 * Extract the current value of the wtpUsed data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUsed_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpUsed_get( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, u_long * wtpUsed_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpUsed_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpUsed_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpUsed data.
 * set (* wtpUsed_val_ptr ) from rowreq_ctx->data
 */
    (* wtpUsed_val_ptr ) = rowreq_ctx->data.wtpUsed;

    return MFD_SUCCESS;
} /* wtpUsed_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWtpEntry.wtpMaxStaNum
 * wtpMaxStaNum is subid 4 of dot11ConfigWtpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2.1.4
 * Description:
Max station number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 64;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMaxStaNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMaxStaNum_map(long *mib_wtpMaxStaNum_val_ptr, long raw_wtpMaxStaNum_val)
{
    netsnmp_assert(NULL != mib_wtpMaxStaNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpMaxStaNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMaxStaNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMaxStaNum_val_ptr) = raw_wtpMaxStaNum_val;

    return MFD_SUCCESS;
} /* wtpMaxStaNum_map */

/**
 * Extract the current value of the wtpMaxStaNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMaxStaNum_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMaxStaNum_get( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, long * wtpMaxStaNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMaxStaNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpMaxStaNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMaxStaNum data.
 * set (* wtpMaxStaNum_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMaxStaNum_val_ptr ) = rowreq_ctx->data.wtpMaxStaNum;

    return MFD_SUCCESS;
} /* wtpMaxStaNum_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWtpEntry.wtpLoadBalanceTrigerBaseUsr
 * wtpLoadBalanceTrigerBaseUsr is subid 5 of dot11ConfigWtpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2.1.5
 * Description:
Load balance base on user.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 64;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpLoadBalanceTrigerBaseUsr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpLoadBalanceTrigerBaseUsr_map(long *mib_wtpLoadBalanceTrigerBaseUsr_val_ptr, long raw_wtpLoadBalanceTrigerBaseUsr_val)
{
    netsnmp_assert(NULL != mib_wtpLoadBalanceTrigerBaseUsr_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseUsr_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpLoadBalanceTrigerBaseUsr mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpLoadBalanceTrigerBaseUsr_val_ptr) = raw_wtpLoadBalanceTrigerBaseUsr_val;

    return MFD_SUCCESS;
} /* wtpLoadBalanceTrigerBaseUsr_map */

/**
 * Extract the current value of the wtpLoadBalanceTrigerBaseUsr data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpLoadBalanceTrigerBaseUsr_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpLoadBalanceTrigerBaseUsr_get( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, long * wtpLoadBalanceTrigerBaseUsr_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpLoadBalanceTrigerBaseUsr_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseUsr_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpLoadBalanceTrigerBaseUsr data.
 * set (* wtpLoadBalanceTrigerBaseUsr_val_ptr ) from rowreq_ctx->data
 */
    (* wtpLoadBalanceTrigerBaseUsr_val_ptr ) = rowreq_ctx->data.wtpLoadBalanceTrigerBaseUsr;

    return MFD_SUCCESS;
} /* wtpLoadBalanceTrigerBaseUsr_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigWtpEntry.wtpLoadBalanceTrigerBaseFlow
 * wtpLoadBalanceTrigerBaseFlow is subid 6 of dot11ConfigWtpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2.1.6
 * Description:
Load balance base on user.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 1024;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpLoadBalanceTrigerBaseFlow.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpLoadBalanceTrigerBaseFlow_map(long *mib_wtpLoadBalanceTrigerBaseFlow_val_ptr, long raw_wtpLoadBalanceTrigerBaseFlow_val)
{
    netsnmp_assert(NULL != mib_wtpLoadBalanceTrigerBaseFlow_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseFlow_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpLoadBalanceTrigerBaseFlow mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpLoadBalanceTrigerBaseFlow_val_ptr) = raw_wtpLoadBalanceTrigerBaseFlow_val;

    return MFD_SUCCESS;
} /* wtpLoadBalanceTrigerBaseFlow_map */

/**
 * Extract the current value of the wtpLoadBalanceTrigerBaseFlow data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpLoadBalanceTrigerBaseFlow_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpLoadBalanceTrigerBaseFlow_get( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx, long * wtpLoadBalanceTrigerBaseFlow_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpLoadBalanceTrigerBaseFlow_val_ptr );


    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:wtpLoadBalanceTrigerBaseFlow_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpLoadBalanceTrigerBaseFlow data.
 * set (* wtpLoadBalanceTrigerBaseFlow_val_ptr ) from rowreq_ctx->data
 */
    (* wtpLoadBalanceTrigerBaseFlow_val_ptr ) = rowreq_ctx->data.wtpLoadBalanceTrigerBaseFlow;

    return MFD_SUCCESS;
} /* wtpLoadBalanceTrigerBaseFlow_get */



/** @} */
