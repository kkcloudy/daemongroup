/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigWtpTable.h"


#include "dot11ConfigWtpTable_data_access.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/ACDbusDef1.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "../mibs_public.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigWtpTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigWtpTable is subid 2 of dot11wtpConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.12.2, length: 12
*/

/**
 * initialization for dot11ConfigWtpTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11ConfigWtpTable_reg
 *        Pointer to dot11ConfigWtpTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11ConfigWtpTable_init_data(dot11ConfigWtpTable_registration_ptr dot11ConfigWtpTable_reg)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11ConfigWtpTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11ConfigWtpTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11ConfigWtpTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11ConfigWtpTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11ConfigWtpTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11CONFIGWTPTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11ConfigWtpTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11ConfigWtpTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11ConfigWtpTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11ConfigWtpTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11ConfigWtpTable_cache_load\n");

    dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wtpID(1)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h
         */
   long   wtpID = 0;

    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
        //return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11ConfigWtpTable container.
     * loop over your dot11ConfigWtpTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    instance_parameter *para_head = NULL, *para_node = NULL;
    list_instance_parameter(&para_head, SNMPD_INSTANCE_MASTER);	
    for(para_node = para_head; NULL != para_node; para_node = para_node->next)
    {
		int ret = 0;
		struct WTP_CONFIG_INFORMATION *WTPconfig = NULL;
		struct WTP_CONFIG_INFORMATION *WTPconfig_show = NULL;
		struct WLAN_INFO *wlan_config = NULL;
		char wtpBindPort[255] = { 0 };
		size_t wtpBindPort_len = 0;
		int i = 0;
		char wlan_list[255] = { 0 };
		char wtpBindWlan[255] = { 0 };
		size_t wtpBindWlan_len = 0;
		
		snmp_log(LOG_DEBUG, "enter show_wtp_config_of_all_cmd\n");
		ret = show_wtp_config_of_all_cmd(para_node->parameter, para_node->connection,&WTPconfig);
		snmp_log(LOG_DEBUG, "exit show_wtp_config_of_all_cmd,ret=%d\n", ret);
	  	if(ret == 1)
		{
			for(WTPconfig_show = WTPconfig->wtp_config_list;(NULL!=WTPconfig_show);WTPconfig_show = WTPconfig_show->next)
		    {
				wtpID = local_to_global_ID(para_node->parameter, WTPconfig_show->WTPID, WIRELESS_MAX_NUM);
	    /*
	    ***************************************************
	    ***             START EXAMPLE CODE              ***
	    ***---------------------------------------------***/
	    /*
	     * get a line (skip blank lines)
	     */
	    //do {
	        //if (!fgets(line, sizeof(line), filep)) {
	            /* we're done */
	            //fclose(filep);
	            //filep = NULL;
	        //}
	    //} while (filep && (line[0] == '\n'));

	    /*
	     * check for end of data
	     */
	    //if(NULL == filep)
	        //break;

	    /*
	     * parse line into variables
	     */
	    /*
	    ***---------------------------------------------***
	    ***              END  EXAMPLE CODE              ***
	    ***************************************************/

	        /*
	         * TODO:352:M: |   |-> set indexes in new dot11ConfigWtpTable rowreq context.
	         */
	        rowreq_ctx = dot11ConfigWtpTable_allocate_rowreq_ctx();
	        if (NULL == rowreq_ctx) {
	            snmp_log(LOG_ERR, "memory allocation failed\n");
				Free_show_wtp_config_of_all_cmd(WTPconfig);
				free_instance_parameter_list(&para_head);
	            return MFD_RESOURCE_UNAVAILABLE;
	        }
	        if(MFD_SUCCESS != dot11ConfigWtpTable_indexes_set(rowreq_ctx
	                               , wtpID
	               )) {
	            snmp_log(LOG_ERR,"error setting index while loading "
	                     "dot11ConfigWtpTable cache.\n");
	            dot11ConfigWtpTable_release_rowreq_ctx(rowreq_ctx);
	            continue;
	        }

	        /*
	         * TODO:352:r: |   |-> populate dot11ConfigWtpTable data context.
	         * Populate data context here. (optionally, delay until row prep)
	         */
	    /*
	     * TRANSIENT or semi-TRANSIENT data:
	     * copy data or save any info needed to do it in row_prep.
	     */
	    /*
	     * setup/save data for wtpBindPort
	     * wtpBindPort(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H
	     */
	    /*
	     * TODO:246:r: |-> Define wtpBindPort mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * if(MFD_SUCCESS !=
	     *    wtpBindPort_map(&rowreq_ctx->data.wtpBindPort, &rowreq_ctx->data.wtpBindPort_len,
	     *                wtpBindPort, wtpBindPort_len, 0)) {
	     *    return MFD_ERROR;
	     * }
	     */
	    /*
	     * make sure there is enough space for wtpBindPort data
	     */
	    rowreq_ctx->data.localwtpID = WTPconfig_show->WTPID;
			
		memcpy(&(rowreq_ctx->data.parameter), &(para_node->parameter), sizeof(dbus_parameter));
		
	    /*if ((NULL == rowreq_ctx->data.wtpBindPort) ||
	        (rowreq_ctx->data.wtpBindPort_len < (wtpBindPort_len * sizeof(rowreq_ctx->data.wtpBindPort[0])))) {
	        snmp_log(LOG_ERR,"not enough space for value\n");
	        return MFD_ERROR;
	    }*/    
		memset(wtpBindPort,0,sizeof(wtpBindPort));
		if(WTPconfig_show->wtpBindPort)
		{
			wtpBindPort_len = MIN(strlen(WTPconfig_show->wtpBindPort),sizeof(wtpBindPort)-1);
			memcpy(wtpBindPort,WTPconfig_show->wtpBindPort,wtpBindPort_len);
		}

	    rowreq_ctx->data.wtpBindPort_len = wtpBindPort_len * sizeof(rowreq_ctx->data.wtpBindPort[0]);
	    memcpy( rowreq_ctx->data.wtpBindPort, wtpBindPort, rowreq_ctx->data.wtpBindPort_len );
	    
	    /*
	     * setup/save data for wtpBindWlan
	     * wtpBindWlan(2)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
	     */
	    /*
	     * TODO:246:r: |-> Define wtpBindWlan mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * if(MFD_SUCCESS !=
	     *    wtpBindWlan_map(&rowreq_ctx->data.wtpBindWlan, &rowreq_ctx->data.wtpBindWlan_len,
	     *                wtpBindWlan, wtpBindWlan_len, 0)) {
	     *    return MFD_ERROR;
	     * }
	     */
	    /*
	     * make sure there is enough space for wtpBindWlan data
	     */
	    /*if ((NULL == rowreq_ctx->data.wtpBindWlan) ||
	        (rowreq_ctx->data.wtpBindWlan_len < (wtpBindWlan_len * sizeof(rowreq_ctx->data.wtpBindWlan[0])))) {
	        snmp_log(LOG_ERR,"not enough space for value\n");
	        return MFD_ERROR;
	    }*/
	    memset(wlan_list,0,sizeof(wlan_list));
	    for(i=0,wlan_config = WTPconfig_show->wlan_info_head;
			((i<WTPconfig_show->apply_wlan_num)&&(NULL != wlan_config));
			i++,wlan_config = wlan_config->next)
	    {
	    	if(i > 0 )
	    	{
				strncat(wlan_list," ",sizeof(wlan_list)-strlen(wlan_list)-1);
	    	}
			if(wlan_config->wlanname)
			{
				strncat(wlan_list,wlan_config->wlanname,sizeof(wlan_list)-strlen(wlan_list)-1);
			}
	    }
		if(strcmp(wlan_list,"")==0)
		{
			strncpy(wlan_list,"NONE",sizeof(wlan_list)-1);
		}
	    
		memset(wtpBindWlan,0,sizeof(wtpBindWlan));
		wtpBindWlan_len = MIN(strlen(wlan_list),sizeof(wtpBindWlan)-1);
		memcpy(wtpBindWlan,wlan_list,wtpBindWlan_len);

	    rowreq_ctx->data.wtpBindWlan_len = wtpBindWlan_len * sizeof(rowreq_ctx->data.wtpBindWlan[0]);
	    memcpy( rowreq_ctx->data.wtpBindWlan, wtpBindWlan, rowreq_ctx->data.wtpBindWlan_len );
	    
	    /*
	     * setup/save data for wtpUsed
	     * wtpUsed(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpUsed mapping.
	     * Map values between raw/native values and MIB values
	     *
	    * enums usually need mapping.
	    */
	    /*if(MFD_SUCCESS !=
	       wtpUsed_map(&rowreq_ctx->data.wtpUsed, wtpUsed )) {
	        return MFD_ERROR;
	    }*/
	    if(WTPconfig_show->wtpused == 1)
		    rowreq_ctx->data.wtpUsed = 1;
		else
			rowreq_ctx->data.wtpUsed = 0;
	    
	    /*
	     * setup/save data for wtpMaxStaNum
	     * wtpMaxStaNum(4)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpMaxStaNum mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.wtpMaxStaNum = WTPconfig_show->wtpMaxStaNum;
	    
	    /*
	     * setup/save data for wtpLoadBalanceTrigerBaseUsr
	     * wtpLoadBalanceTrigerBaseUsr(5)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpLoadBalanceTrigerBaseUsr mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.wtpLoadBalanceTrigerBaseUsr = WTPconfig_show->wtpLoadBalanceTrigerBaseUsr;
	    
	    /*
	     * setup/save data for wtpLoadBalanceTrigerBaseFlow
	     * wtpLoadBalanceTrigerBaseFlow(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/R/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpLoadBalanceTrigerBaseFlow mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.wtpLoadBalanceTrigerBaseFlow = WTPconfig_show->wtpLoadBalanceTrigerBaseFlow;
	    
    /*
     * setup/save data for wtpRadioPrioritySelect
     * wtpRadioPrioritySelect(7)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
     */
    /** no mapping */
    rowreq_ctx->data.wtpRadioPrioritySelect = 0;
	        
	        /*
	         * insert into table container
	         */
	        if(CONTAINER_INSERT(container, rowreq_ctx))		
			{
				dot11ConfigWtpTable_release_rowreq_ctx(rowreq_ctx);
			}
	        ++count;
	    }}
		Free_show_wtp_config_of_all_cmd(WTPconfig);
	}
	free_instance_parameter_list(&para_head);


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
        //fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11ConfigWtpTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11ConfigWtpTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11ConfigWtpTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11ConfigWtpTable cache.
     */
} /* dot11ConfigWtpTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11ConfigWtpTable_row_prep( dot11ConfigWtpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigWtpTable:dot11ConfigWtpTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11ConfigWtpTable_row_prep */

/** @} */
