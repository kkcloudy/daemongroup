/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigRadiusAccServerTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigRadiusAccServerTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11ConfigRadiusAccServerTable is subid 7 of dot11securityConfig.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.14.7, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAccServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigRadiusAccServerTable_undo_setup( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:dot11ConfigRadiusAccServerTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigRadiusAccServerTable undo.
     * set up dot11ConfigRadiusAccServerTable undo information, in preparation for a set.
     */

    return rc;
} /* dot11ConfigRadiusAccServerTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAccServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigRadiusAccServerTable_undo_cleanup( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:dot11ConfigRadiusAccServerTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigRadiusAccServerTable undo.
     */

    return rc;
} /* dot11ConfigRadiusAccServerTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigRadiusAccServerTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigRadiusAccServerTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigRadiusAccServerTable_commit( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:dot11ConfigRadiusAccServerTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit dot11ConfigRadiusAccServerTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & FLAG_NEWACCSERVERIPADD) {
       save_flags &= ~FLAG_NEWACCSERVERIPADD; /* clear NewAccServerIPAdd */
       /*
        * TODO:482:o: |-> commit column NewAccServerIPAdd.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAccServerTable column NewAccServerIPAdd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewAccServerIPAdd
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWACCSERVERIPADD;
       }
    }

    if (save_flags & FLAG_NEWACCSERVERPORT) {
       save_flags &= ~FLAG_NEWACCSERVERPORT; /* clear NewAccServerPort */
       /*
        * TODO:482:o: |-> commit column NewAccServerPort.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAccServerTable column NewAccServerPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewAccServerPort
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWACCSERVERPORT;
       }
    }

    if (save_flags & FLAG_NEWACCSERVERSHAREDKEY) {
       save_flags &= ~FLAG_NEWACCSERVERSHAREDKEY; /* clear NewAccServerSharedkey */
       /*
        * TODO:482:o: |-> commit column NewAccServerSharedkey.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAccServerTable column NewAccServerSharedkey commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewAccServerSharedkey
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWACCSERVERSHAREDKEY;
       }
    }

    if (save_flags & FLAG_NEWRADIUSACCSERVERIPADD) {
       save_flags &= ~FLAG_NEWRADIUSACCSERVERIPADD; /* clear NewRadiusAccServerIPAdd */
       /*
        * TODO:482:o: |-> commit column NewRadiusAccServerIPAdd.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAccServerTable column NewRadiusAccServerIPAdd commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewRadiusAccServerIPAdd
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWRADIUSACCSERVERIPADD;
       }
    }

    if (save_flags & FLAG_NEWRADIUSACCSERVERPORT) {
       save_flags &= ~FLAG_NEWRADIUSACCSERVERPORT; /* clear NewRadiusAccServerPort */
       /*
        * TODO:482:o: |-> commit column NewRadiusAccServerPort.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAccServerTable column NewRadiusAccServerPort commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewRadiusAccServerPort
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWRADIUSACCSERVERPORT;
       }
    }

    if (save_flags & FLAG_NEWRADIUSACCSERVERSHAREDKEY) {
       save_flags &= ~FLAG_NEWRADIUSACCSERVERSHAREDKEY; /* clear NewRadiusAccServerSharedkey */
       /*
        * TODO:482:o: |-> commit column NewRadiusAccServerSharedkey.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigRadiusAccServerTable column NewRadiusAccServerSharedkey commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NewRadiusAccServerSharedkey
             */
            rowreq_ctx->column_set_flags |= FLAG_NEWRADIUSACCSERVERSHAREDKEY;
       }
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigRadiusAccServerTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigRadiusAccServerTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigRadiusAccServerTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigRadiusAccServerTable_undo_commit( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:dot11ConfigRadiusAccServerTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigRadiusAccServerTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & FLAG_) {}
     */

    return rc;
} /* dot11ConfigRadiusAccServerTable_undo_commit */

/*
 * TODO:420:r: Implement dot11ConfigRadiusAccServerTable index validation.
 */
/*
 * TODO:440:M: Implement dot11ConfigRadiusAccServerTable node value checks.
 * TODO:450:M: Implement dot11ConfigRadiusAccServerTable undo functions.
 * TODO:460:M: Implement dot11ConfigRadiusAccServerTable set functions.
 * TODO:480:M: Implement dot11ConfigRadiusAccServerTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAccServerEntry.NewAccServerIPAdd
 * NewAccServerIPAdd is subid 2 of dot11ConfigRadiusAccServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.14.7.1.2
 * Description:
NewAccServerIPAdd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAccServerIPAdd_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAccServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewAccServerIPAdd_check_value( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, u_long NewAccServerIPAdd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerIPAdd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewAccServerIPAdd value.
     */

    return MFD_SUCCESS; /* NewAccServerIPAdd value not illegal */
} /* NewAccServerIPAdd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAccServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAccServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewAccServerIPAdd_undo_setup( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerIPAdd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewAccServerIPAdd undo.
     */
    /*
     * copy NewAccServerIPAdd data
     * set rowreq_ctx->undo->NewAccServerIPAdd from rowreq_ctx->data.NewAccServerIPAdd
     */
    rowreq_ctx->undo->NewAccServerIPAdd = rowreq_ctx->data.NewAccServerIPAdd;


    return MFD_SUCCESS;
} /* NewAccServerIPAdd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewAccServerIPAdd_val
 *        A u_long containing the new value.
 */
int
NewAccServerIPAdd_set( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, u_long NewAccServerIPAdd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerIPAdd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewAccServerIPAdd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewAccServerIPAdd value.
     * set NewAccServerIPAdd value in rowreq_ctx->data
     */
    
	int rc = MFD_ERROR;
	int ret = 0;
	char input_string[20] = {0};
	memset(input_string,0,20);
	INET_NTOA(NewAccServerIPAdd_val,input_string);

	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	ret = security_auth_acct(rowreq_ctx->data.parameter, connection, 0, rowreq_ctx->tbl_idx.RadiusAccServerID, input_string, rowreq_ctx->data.NewAccServerPort, rowreq_ctx->data.NewAccServerSharedkey);
    if(1 == ret)
    {
		rowreq_ctx->data.NewAccServerIPAdd = NewAccServerIPAdd_val;
        rc = MFD_SUCCESS;
	}
    else
    {
		rc = MFD_ERROR;
	}

    return rc;
} /* NewAccServerIPAdd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewAccServerIPAdd_undo( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerIPAdd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewAccServerIPAdd undo.
     */
    /*
     * copy NewAccServerIPAdd data
     * set rowreq_ctx->data.NewAccServerIPAdd from rowreq_ctx->undo->NewAccServerIPAdd
     */
    rowreq_ctx->data.NewAccServerIPAdd = rowreq_ctx->undo->NewAccServerIPAdd;

    
    return MFD_SUCCESS;
} /* NewAccServerIPAdd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAccServerEntry.NewAccServerPort
 * NewAccServerPort is subid 3 of dot11ConfigRadiusAccServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.14.7.1.3
 * Description:
NewAccServerPort.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAccServerPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAccServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewAccServerPort_check_value( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, long NewAccServerPort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewAccServerPort value.
     */

    return MFD_SUCCESS; /* NewAccServerPort value not illegal */
} /* NewAccServerPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAccServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAccServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewAccServerPort_undo_setup( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewAccServerPort undo.
     */
    /*
     * copy NewAccServerPort data
     * set rowreq_ctx->undo->NewAccServerPort from rowreq_ctx->data.NewAccServerPort
     */
    rowreq_ctx->undo->NewAccServerPort = rowreq_ctx->data.NewAccServerPort;


    return MFD_SUCCESS;
} /* NewAccServerPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewAccServerPort_val
 *        A long containing the new value.
 */
int
NewAccServerPort_set( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, long NewAccServerPort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewAccServerPort reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewAccServerPort value.
     * set NewAccServerPort value in rowreq_ctx->data
     */

	int rc = MFD_ERROR;
	int ret = 0;
	int NewAccServerPort = 0;
	char NewAccServerIPAdd[20] = {0};
	memset(NewAccServerIPAdd,0,20);	
	INET_NTOA(rowreq_ctx->data.NewAccServerIPAdd,NewAccServerIPAdd);
	NewAccServerPort = NewAccServerPort_val;

	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	ret = security_auth_acct(rowreq_ctx->data.parameter, connection, 0, rowreq_ctx->tbl_idx.RadiusAccServerID, NewAccServerIPAdd, NewAccServerPort, rowreq_ctx->data.NewAccServerSharedkey);
    if(1 == ret)
    {
		rowreq_ctx->data.NewAccServerPort = NewAccServerPort_val;
        rc = MFD_SUCCESS;
	}
    else
    {
		rc = MFD_ERROR;
	}

    return rc;
} /* NewAccServerPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewAccServerPort_undo( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewAccServerPort undo.
     */
    /*
     * copy NewAccServerPort data
     * set rowreq_ctx->data.NewAccServerPort from rowreq_ctx->undo->NewAccServerPort
     */
    rowreq_ctx->data.NewAccServerPort = rowreq_ctx->undo->NewAccServerPort;

    
    return MFD_SUCCESS;
} /* NewAccServerPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAccServerEntry.NewAccServerSharedkey
 * NewAccServerSharedkey is subid 4 of dot11ConfigRadiusAccServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.14.7.1.4
 * Description:
NewAccServerSharedkey.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewAccServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewAccServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewAccServerSharedkey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAccServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewAccServerSharedkey_check_value( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, char *NewAccServerSharedkey_val_ptr,  size_t NewAccServerSharedkey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerSharedkey_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewAccServerSharedkey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid NewAccServerSharedkey value.
     */

    return MFD_SUCCESS; /* NewAccServerSharedkey value not illegal */
} /* NewAccServerSharedkey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAccServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAccServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewAccServerSharedkey_undo_setup( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerSharedkey_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewAccServerSharedkey undo.
     */
    /*
     * copy NewAccServerSharedkey and NewAccServerSharedkey_len data
     * set rowreq_ctx->undo->NewAccServerSharedkey from rowreq_ctx->data.NewAccServerSharedkey
     */
    memcpy( rowreq_ctx->undo->NewAccServerSharedkey, rowreq_ctx->data.NewAccServerSharedkey,
            (rowreq_ctx->data.NewAccServerSharedkey_len * sizeof(rowreq_ctx->undo->NewAccServerSharedkey[0])));
    rowreq_ctx->undo->NewAccServerSharedkey_len = rowreq_ctx->data.NewAccServerSharedkey_len;


    return MFD_SUCCESS;
} /* NewAccServerSharedkey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewAccServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewAccServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewAccServerSharedkey_val_ptr
 */
int
NewAccServerSharedkey_set( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, char *NewAccServerSharedkey_val_ptr,  size_t NewAccServerSharedkey_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerSharedkey_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewAccServerSharedkey_val_ptr);

/*
 * TODO:245:o: |-> Implement NewAccServerSharedkey reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewAccServerSharedkey value.
     * set NewAccServerSharedkey value in rowreq_ctx->data
     */

	int rc = MFD_ERROR;
	int ret = 0;
	char NewAccServerIPAdd[20] = {0};
	memset(NewAccServerIPAdd,0,20);	
	INET_NTOA(rowreq_ctx->data.NewAccServerIPAdd,NewAccServerIPAdd);

	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	ret = security_auth_acct(rowreq_ctx->data.parameter, connection, 0, rowreq_ctx->tbl_idx.RadiusAccServerID, NewAccServerIPAdd, rowreq_ctx->data.NewAccServerPort, NewAccServerSharedkey_val_ptr);
    if(1 == ret)
    {
		memcpy( rowreq_ctx->data.NewAccServerSharedkey, NewAccServerSharedkey_val_ptr, NewAccServerSharedkey_val_ptr_len );
		rowreq_ctx->data.NewAccServerSharedkey_len = NewAccServerSharedkey_val_ptr_len / sizeof(NewAccServerSharedkey_val_ptr[0]);
        rc = MFD_SUCCESS;
	}
    else
    {
		rc = MFD_ERROR;
	}

    return rc;
} /* NewAccServerSharedkey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewAccServerSharedkey_undo( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewAccServerSharedkey_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewAccServerSharedkey undo.
     */
    /*
     * copy NewAccServerSharedkey and NewAccServerSharedkey_len data
     * set rowreq_ctx->data.NewAccServerSharedkey from rowreq_ctx->undo->NewAccServerSharedkey
     */
    memcpy( rowreq_ctx->data.NewAccServerSharedkey, rowreq_ctx->undo->NewAccServerSharedkey,
            (rowreq_ctx->undo->NewAccServerSharedkey_len * sizeof(rowreq_ctx->data.NewAccServerSharedkey[0])));
    rowreq_ctx->data.NewAccServerSharedkey_len = rowreq_ctx->undo->NewAccServerSharedkey_len;

    
    return MFD_SUCCESS;
} /* NewAccServerSharedkey_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAccServerEntry.NewRadiusAccServerIPAdd
 * NewRadiusAccServerIPAdd is subid 5 of dot11ConfigRadiusAccServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.14.7.1.5
 * Description:
NewRadiusAccServerIPAdd.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewRadiusAccServerIPAdd_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAccServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_IPADDRESS
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewRadiusAccServerIPAdd_check_value( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, u_long NewRadiusAccServerIPAdd_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerIPAdd_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewRadiusAccServerIPAdd value.
     */

    return MFD_SUCCESS; /* NewRadiusAccServerIPAdd value not illegal */
} /* NewRadiusAccServerIPAdd_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAccServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAccServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewRadiusAccServerIPAdd_undo_setup( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerIPAdd_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewRadiusAccServerIPAdd undo.
     */
    /*
     * copy NewRadiusAccServerIPAdd data
     * set rowreq_ctx->undo->NewRadiusAccServerIPAdd from rowreq_ctx->data.NewRadiusAccServerIPAdd
     */
    rowreq_ctx->undo->NewRadiusAccServerIPAdd = rowreq_ctx->data.NewRadiusAccServerIPAdd;


    return MFD_SUCCESS;
} /* NewRadiusAccServerIPAdd_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewRadiusAccServerIPAdd_val
 *        A u_long containing the new value.
 */
int
NewRadiusAccServerIPAdd_set( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, u_long NewRadiusAccServerIPAdd_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerIPAdd_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewRadiusAccServerIPAdd reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewRadiusAccServerIPAdd value.
     * set NewRadiusAccServerIPAdd value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	int ret = 0;
	char input_string[20] = {0};
	memset(input_string,0,20);
	INET_NTOA(NewRadiusAccServerIPAdd_val,input_string);

	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	ret = secondary_radius_acct(rowreq_ctx->data.parameter, connection, rowreq_ctx->tbl_idx.RadiusAccServerID, input_string, rowreq_ctx->data.NewRadiusAccServerPort, rowreq_ctx->data.NewRadiusAccServerSharedkey);
    if(1 == ret)
    {
		rowreq_ctx->data.NewRadiusAccServerIPAdd = NewRadiusAccServerIPAdd_val;
        rc = MFD_SUCCESS;
	}
    else
    {
        rc = MFD_ERROR;
	}

    return rc;
} /* NewRadiusAccServerIPAdd_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewRadiusAccServerIPAdd_undo( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerIPAdd_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewRadiusAccServerIPAdd undo.
     */
    /*
     * copy NewRadiusAccServerIPAdd data
     * set rowreq_ctx->data.NewRadiusAccServerIPAdd from rowreq_ctx->undo->NewRadiusAccServerIPAdd
     */
    rowreq_ctx->data.NewRadiusAccServerIPAdd = rowreq_ctx->undo->NewRadiusAccServerIPAdd;

    
    return MFD_SUCCESS;
} /* NewRadiusAccServerIPAdd_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAccServerEntry.NewRadiusAccServerPort
 * NewRadiusAccServerPort is subid 6 of dot11ConfigRadiusAccServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.14.7.1.6
 * Description:
NewRadiusAccServerPort.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewRadiusAccServerPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAccServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewRadiusAccServerPort_check_value( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, long NewRadiusAccServerPort_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerPort_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NewRadiusAccServerPort value.
     */

    return MFD_SUCCESS; /* NewRadiusAccServerPort value not illegal */
} /* NewRadiusAccServerPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAccServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAccServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewRadiusAccServerPort_undo_setup( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerPort_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewRadiusAccServerPort undo.
     */
    /*
     * copy NewRadiusAccServerPort data
     * set rowreq_ctx->undo->NewRadiusAccServerPort from rowreq_ctx->data.NewRadiusAccServerPort
     */
    rowreq_ctx->undo->NewRadiusAccServerPort = rowreq_ctx->data.NewRadiusAccServerPort;


    return MFD_SUCCESS;
} /* NewRadiusAccServerPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewRadiusAccServerPort_val
 *        A long containing the new value.
 */
int
NewRadiusAccServerPort_set( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, long NewRadiusAccServerPort_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerPort_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:245:o: |-> Implement NewRadiusAccServerPort reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewRadiusAccServerPort value.
     * set NewRadiusAccServerPort value in rowreq_ctx->data
     */
	int rc = MFD_ERROR;
	int ret = 0;
	int NewRadiusAccServerPort = 0;
	char NewRadiusAccServerIPAdd[20] = {0};
	memset(NewRadiusAccServerIPAdd,0,20);	
	INET_NTOA(rowreq_ctx->data.NewRadiusAccServerIPAdd,NewRadiusAccServerIPAdd);
	NewRadiusAccServerPort = NewRadiusAccServerPort_val;

	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	ret = secondary_radius_acct(rowreq_ctx->data.parameter, connection, rowreq_ctx->tbl_idx.RadiusAccServerID, NewRadiusAccServerIPAdd, NewRadiusAccServerPort, rowreq_ctx->data.NewRadiusAccServerSharedkey);
    if(1 == ret)
    {
		rowreq_ctx->data.NewRadiusAccServerPort = NewRadiusAccServerPort_val;
        rc = MFD_SUCCESS;
	}
    else
    {
		rc = MFD_ERROR;
	}

    return rc;
} /* NewRadiusAccServerPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewRadiusAccServerPort_undo( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerPort_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewRadiusAccServerPort undo.
     */
    /*
     * copy NewRadiusAccServerPort data
     * set rowreq_ctx->data.NewRadiusAccServerPort from rowreq_ctx->undo->NewRadiusAccServerPort
     */
    rowreq_ctx->data.NewRadiusAccServerPort = rowreq_ctx->undo->NewRadiusAccServerPort;

    
    return MFD_SUCCESS;
} /* NewRadiusAccServerPort_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigRadiusAccServerEntry.NewRadiusAccServerSharedkey
 * NewRadiusAccServerSharedkey is subid 7 of dot11ConfigRadiusAccServerEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.14.7.1.7
 * Description:
NewRadiusAccServerSharedkey.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NewRadiusAccServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewRadiusAccServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewRadiusAccServerSharedkey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigRadiusAccServerTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NewRadiusAccServerSharedkey_check_value( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, char *NewRadiusAccServerSharedkey_val_ptr,  size_t NewRadiusAccServerSharedkey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerSharedkey_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewRadiusAccServerSharedkey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid NewRadiusAccServerSharedkey value.
     */

    return MFD_SUCCESS; /* NewRadiusAccServerSharedkey value not illegal */
} /* NewRadiusAccServerSharedkey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigRadiusAccServerTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigRadiusAccServerTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NewRadiusAccServerSharedkey_undo_setup( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerSharedkey_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NewRadiusAccServerSharedkey undo.
     */
    /*
     * copy NewRadiusAccServerSharedkey and NewRadiusAccServerSharedkey_len data
     * set rowreq_ctx->undo->NewRadiusAccServerSharedkey from rowreq_ctx->data.NewRadiusAccServerSharedkey
     */
    memcpy( rowreq_ctx->undo->NewRadiusAccServerSharedkey, rowreq_ctx->data.NewRadiusAccServerSharedkey,
            (rowreq_ctx->data.NewRadiusAccServerSharedkey_len * sizeof(rowreq_ctx->undo->NewRadiusAccServerSharedkey[0])));
    rowreq_ctx->undo->NewRadiusAccServerSharedkey_len = rowreq_ctx->data.NewRadiusAccServerSharedkey_len;


    return MFD_SUCCESS;
} /* NewRadiusAccServerSharedkey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NewRadiusAccServerSharedkey_val_ptr
 *        A char containing the new value.
 * @param NewRadiusAccServerSharedkey_val_ptr_len
 *        The size (in bytes) of the data pointed to by NewRadiusAccServerSharedkey_val_ptr
 */
int
NewRadiusAccServerSharedkey_set( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx, char *NewRadiusAccServerSharedkey_val_ptr,  size_t NewRadiusAccServerSharedkey_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerSharedkey_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NewRadiusAccServerSharedkey_val_ptr);

/*
 * TODO:245:o: |-> Implement NewRadiusAccServerSharedkey reverse mapping.
 * If the values for your data type don't exactly match the
 * possible values defined by the mib, you should map them here.
 */
    /*
     * TODO:461:M: |-> Set NewRadiusAccServerSharedkey value.
     * set NewRadiusAccServerSharedkey value in rowreq_ctx->data
     */

	int rc = MFD_ERROR;
	int ret = 0;
	int NewRadiusAccServerPort = 0;
	char NewRadiusAccServerIPAdd[20] = {0};
	memset(NewRadiusAccServerIPAdd,0,20);	
	INET_NTOA(rowreq_ctx->data.NewRadiusAccServerIPAdd,NewRadiusAccServerIPAdd);
	NewRadiusAccServerPort = rowreq_ctx->data.NewRadiusAccServerPort;

	void *connection = NULL;
    if(SNMPD_DBUS_ERROR == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
        return MFD_ERROR;

	ret = secondary_radius_acct(rowreq_ctx->data.parameter, connection, rowreq_ctx->tbl_idx.RadiusAccServerID, NewRadiusAccServerIPAdd, NewRadiusAccServerPort, NewRadiusAccServerSharedkey_val_ptr);
    if(1 == ret)
    {
		memcpy( rowreq_ctx->data.NewRadiusAccServerSharedkey, NewRadiusAccServerSharedkey_val_ptr, NewRadiusAccServerSharedkey_val_ptr_len );
		rowreq_ctx->data.NewRadiusAccServerSharedkey_len = NewRadiusAccServerSharedkey_val_ptr_len / sizeof(NewRadiusAccServerSharedkey_val_ptr[0]);
        rc = MFD_SUCCESS;
	}
    else
    {
		rc = MFD_ERROR;
	}

    return rc;
} /* NewRadiusAccServerSharedkey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NewRadiusAccServerSharedkey_undo( dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigRadiusAccServerTable:NewRadiusAccServerSharedkey_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NewRadiusAccServerSharedkey undo.
     */
    /*
     * copy NewRadiusAccServerSharedkey and NewRadiusAccServerSharedkey_len data
     * set rowreq_ctx->data.NewRadiusAccServerSharedkey from rowreq_ctx->undo->NewRadiusAccServerSharedkey
     */
    memcpy( rowreq_ctx->data.NewRadiusAccServerSharedkey, rowreq_ctx->undo->NewRadiusAccServerSharedkey,
            (rowreq_ctx->undo->NewRadiusAccServerSharedkey_len * sizeof(rowreq_ctx->data.NewRadiusAccServerSharedkey[0])));
    rowreq_ctx->data.NewRadiusAccServerSharedkey_len = rowreq_ctx->undo->NewRadiusAccServerSharedkey_len;

    
    return MFD_SUCCESS;
} /* NewRadiusAccServerSharedkey_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the FLAG_* bits can be found in
 * dot11ConfigRadiusAccServerTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigRadiusAccServerTable if you don't have dependencies)
 */
int
dot11ConfigRadiusAccServerTable_check_dependencies(dot11ConfigRadiusAccServerTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigRadiusAccServerTable:dot11ConfigRadiusAccServerTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigRadiusAccServerTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* dot11ConfigRadiusAccServerTable_check_dependencies */

/** @} */
