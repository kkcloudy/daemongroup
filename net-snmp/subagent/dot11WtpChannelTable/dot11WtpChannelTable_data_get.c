/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpChannelTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpChannelTable get routines.
 * TODO:240:M: Implement dot11WtpChannelTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpChannelTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpChannelTable is subid 1 of wtpBaseWtps.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.7.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpChannelTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11WtpChannelTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpChannelTable_indexes_set_tbl_idx(dot11WtpChannelTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:dot11WtpChannelTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11WtpChannelTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpChannelTable_indexes_set(dot11WtpChannelTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:dot11WtpChannelTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpChannelTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpChannelTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpChannelTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpChannelEntry.wtpPathPhyErrPack
 * wtpPathPhyErrPack is subid 1 of dot11WtpChannelEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.7.1.1.1
 * Description:
The total number of mistake package of physical leve.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpPathPhyErrPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpPathPhyErrPack_map(u_long *mib_wtpPathPhyErrPack_val_ptr, u_long raw_wtpPathPhyErrPack_val)
{
    netsnmp_assert(NULL != mib_wtpPathPhyErrPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpPathPhyErrPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpPathPhyErrPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpPathPhyErrPack_val_ptr) = raw_wtpPathPhyErrPack_val;

    return MFD_SUCCESS;
} /* wtpPathPhyErrPack_map */

/**
 * Extract the current value of the wtpPathPhyErrPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpPathPhyErrPack_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpPathPhyErrPack_get( dot11WtpChannelTable_rowreq_ctx *rowreq_ctx, u_long * wtpPathPhyErrPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpPathPhyErrPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpPathPhyErrPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpPathPhyErrPack data.
 * set (* wtpPathPhyErrPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpPathPhyErrPack_val_ptr ) = rowreq_ctx->data.wtpPathPhyErrPack;

    return MFD_SUCCESS;
} /* wtpPathPhyErrPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpChannelEntry.wtpPathMacErrPack
 * wtpPathMacErrPack is subid 2 of dot11WtpChannelEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.7.1.1.2
 * Description:
The total number of mistake package of MAC level in downlink channel(down-bound).(Update time less than 2 seconds)
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpPathMacErrPack.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpPathMacErrPack_map(u_long *mib_wtpPathMacErrPack_val_ptr, u_long raw_wtpPathMacErrPack_val)
{
    netsnmp_assert(NULL != mib_wtpPathMacErrPack_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpPathMacErrPack_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpPathMacErrPack mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpPathMacErrPack_val_ptr) = raw_wtpPathMacErrPack_val;

    return MFD_SUCCESS;
} /* wtpPathMacErrPack_map */

/**
 * Extract the current value of the wtpPathMacErrPack data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpPathMacErrPack_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpPathMacErrPack_get( dot11WtpChannelTable_rowreq_ctx *rowreq_ctx, u_long * wtpPathMacErrPack_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpPathMacErrPack_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpPathMacErrPack_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpPathMacErrPack data.
 * set (* wtpPathMacErrPack_val_ptr ) from rowreq_ctx->data
 */
    (* wtpPathMacErrPack_val_ptr ) = rowreq_ctx->data.wtpPathMacErrPack;

    return MFD_SUCCESS;
} /* wtpPathMacErrPack_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpChannelEntry.wtpPathFrameResRate
 * wtpPathFrameResRate is subid 3 of dot11WtpChannelEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.7.1.1.3
 * Description:
The total number of frames which resent by wireless signal channel during runtime .
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpPathFrameResRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpPathFrameResRate_map(u_long *mib_wtpPathFrameResRate_val_ptr, u_long raw_wtpPathFrameResRate_val)
{
    netsnmp_assert(NULL != mib_wtpPathFrameResRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpPathFrameResRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpPathFrameResRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpPathFrameResRate_val_ptr) = raw_wtpPathFrameResRate_val;

    return MFD_SUCCESS;
} /* wtpPathFrameResRate_map */

/**
 * Extract the current value of the wtpPathFrameResRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpPathFrameResRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpPathFrameResRate_get( dot11WtpChannelTable_rowreq_ctx *rowreq_ctx, u_long * wtpPathFrameResRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpPathFrameResRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpPathFrameResRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpPathFrameResRate data.
 * set (* wtpPathFrameResRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpPathFrameResRate_val_ptr ) = rowreq_ctx->data.wtpPathFrameResRate;

    return MFD_SUCCESS;
} /* wtpPathFrameResRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpChannelEntry.wtpPathErrFrameRate
 * wtpPathErrFrameRate is subid 4 of dot11WtpChannelEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.7.1.1.4
 * Description:
Mistake frame rate on signal path.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpPathErrFrameRate.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpPathErrFrameRate_map(u_long *mib_wtpPathErrFrameRate_val_ptr, u_long raw_wtpPathErrFrameRate_val)
{
    netsnmp_assert(NULL != mib_wtpPathErrFrameRate_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpPathErrFrameRate_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpPathErrFrameRate mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpPathErrFrameRate_val_ptr) = raw_wtpPathErrFrameRate_val;

    return MFD_SUCCESS;
} /* wtpPathErrFrameRate_map */

/**
 * Extract the current value of the wtpPathErrFrameRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpPathErrFrameRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpPathErrFrameRate_get( dot11WtpChannelTable_rowreq_ctx *rowreq_ctx, u_long * wtpPathErrFrameRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpPathErrFrameRate_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpPathErrFrameRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpPathErrFrameRate data.
 * set (* wtpPathErrFrameRate_val_ptr ) from rowreq_ctx->data
 */
    (* wtpPathErrFrameRate_val_ptr ) = rowreq_ctx->data.wtpPathErrFrameRate;

    return MFD_SUCCESS;
} /* wtpPathErrFrameRate_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpChannelEntry.wtpMonitorTime
 * wtpMonitorTime is subid 5 of dot11WtpChannelEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.7.1.1.5
 * Description:
The pass time of monitoring.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is TICKS (based on perltype TICKS)
 * The net-snmp type is ASN_TIMETICKS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMonitorTime.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMonitorTime_map(u_long *mib_wtpMonitorTime_val_ptr, u_long raw_wtpMonitorTime_val)
{
    netsnmp_assert(NULL != mib_wtpMonitorTime_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpMonitorTime_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMonitorTime mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMonitorTime_val_ptr) = raw_wtpMonitorTime_val;

    return MFD_SUCCESS;
} /* wtpMonitorTime_map */

/**
 * Extract the current value of the wtpMonitorTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMonitorTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMonitorTime_get( dot11WtpChannelTable_rowreq_ctx *rowreq_ctx, u_long * wtpMonitorTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMonitorTime_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpMonitorTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMonitorTime data.
 * set (* wtpMonitorTime_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMonitorTime_val_ptr ) = rowreq_ctx->data.wtpMonitorTime;

    return MFD_SUCCESS;
} /* wtpMonitorTime_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpChannelEntry.wtpSignalPathOfTerm
 * wtpSignalPathOfTerm is subid 6 of dot11WtpChannelEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.7.1.1.6
 * Description:
The terminal number which use the signal path.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpSignalPathOfTerm.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpSignalPathOfTerm_map(long *mib_wtpSignalPathOfTerm_val_ptr, long raw_wtpSignalPathOfTerm_val)
{
    netsnmp_assert(NULL != mib_wtpSignalPathOfTerm_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpSignalPathOfTerm_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpSignalPathOfTerm mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpSignalPathOfTerm_val_ptr) = raw_wtpSignalPathOfTerm_val;

    return MFD_SUCCESS;
} /* wtpSignalPathOfTerm_map */

/**
 * Extract the current value of the wtpSignalPathOfTerm data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpSignalPathOfTerm_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpSignalPathOfTerm_get( dot11WtpChannelTable_rowreq_ctx *rowreq_ctx, long * wtpSignalPathOfTerm_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpSignalPathOfTerm_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpChannelTable:wtpSignalPathOfTerm_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpSignalPathOfTerm data.
 * set (* wtpSignalPathOfTerm_val_ptr ) from rowreq_ctx->data
 */
    (* wtpSignalPathOfTerm_val_ptr ) = rowreq_ctx->data.wtpSignalPathOfTerm;

    return MFD_SUCCESS;
} /* wtpSignalPathOfTerm_get */



/** @} */
