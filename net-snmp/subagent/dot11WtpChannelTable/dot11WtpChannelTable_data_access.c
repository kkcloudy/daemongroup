/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpChannelTable.h"


#include "dot11WtpChannelTable_data_access.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "../mibs_public.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpChannelTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpChannelTable is subid 1 of wtpBaseWtps.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.7.1, length: 12
*/

/**
 * initialization for dot11WtpChannelTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11WtpChannelTable_reg
 *        Pointer to dot11WtpChannelTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11WtpChannelTable_init_data(dot11WtpChannelTable_registration_ptr dot11WtpChannelTable_reg)
{
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:dot11WtpChannelTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11WtpChannelTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11WtpChannelTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11WtpChannelTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:dot11WtpChannelTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11WtpChannelTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11WtpChannelTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11WTPCHANNELTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11WtpChannelTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11WtpChannelTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11WtpChannelTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11WtpChannelTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11WtpChannelTable_cache_load\n");

    dot11WtpChannelTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
         */
        /** 128 - 1(entry) - 1(col) - 0(other indexes) = 112 */
   char   wtpMacAddr[20] = { 0 };
   size_t      wtpMacAddr_len = 0;

    DEBUGMSGTL(("verbose:dot11WtpChannelTable:dot11WtpChannelTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
        //return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11WtpChannelTable container.
     * loop over your dot11WtpChannelTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    instance_parameter *para_head = NULL, *para_node = NULL;
    list_instance_parameter(&para_head, SNMPD_INSTANCE_MASTER);	
    for(para_node = para_head; NULL != para_node; para_node = para_node->next)
    {
		int ret = 0;
		struct WtpAthStatisticInfo *WtpHead = NULL;
		struct WtpAthStatisticInfo *WtpNode = NULL;
		
		snmp_log(LOG_DEBUG, "enter show_statistcs_information_of_all_wtp_whole_cmd\n");
		ret = show_statistcs_information_of_all_wtp_whole_cmd(para_node->parameter, para_node->connection,&WtpHead);
		snmp_log(LOG_DEBUG, "exit show_statistcs_information_of_all_wtp_whole_cmd,ret=%d\n", ret);
	 	if(ret == 1)
		{
			for(WtpNode=WtpHead; (NULL != WtpNode); WtpNode=WtpNode->next)		
			{
				memset(wtpMacAddr,0,sizeof(wtpMacAddr));	
				if(WtpNode->wtpMacAddr)
				{
					strncpy(wtpMacAddr,WtpNode->wtpMacAddr,sizeof(wtpMacAddr)-1);
				}
	    /*
	    ***************************************************
	    ***             START EXAMPLE CODE              ***
	    ***---------------------------------------------***/
	    /*
	     * get a line (skip blank lines)
	     */
	    //do {
	        //if (!fgets(line, sizeof(line), filep)) {
	            /* we're done */
	            //fclose(filep);
	            //filep = NULL;
	        //}
	    //} while (filep && (line[0] == '\n'));

	    /*
	     * check for end of data
	     */
	    //if(NULL == filep)
	        //break;

	    /*
	     * parse line into variables
	     */
	    /*
	    ***---------------------------------------------***
	    ***              END  EXAMPLE CODE              ***
	    ***************************************************/

	        /*
	         * TODO:352:M: |   |-> set indexes in new dot11WtpChannelTable rowreq context.
	         */
	        rowreq_ctx = dot11WtpChannelTable_allocate_rowreq_ctx();
	        if (NULL == rowreq_ctx) {
	            snmp_log(LOG_ERR, "memory allocation failed\n");
				Free_show_statistcs_information_of_all_wtp_whole_cmd(WtpHead);
				free_instance_parameter_list(&para_head);
	            return MFD_RESOURCE_UNAVAILABLE;
	        }

			wtpMacAddr_len = strlen(wtpMacAddr);
	        if(MFD_SUCCESS != dot11WtpChannelTable_indexes_set(rowreq_ctx
	                               , wtpMacAddr, wtpMacAddr_len
	               )) {
	            snmp_log(LOG_ERR,"error setting index while loading "
	                     "dot11WtpChannelTable cache.\n");
	            dot11WtpChannelTable_release_rowreq_ctx(rowreq_ctx);
	            continue;
	        }

	        /*
	         * TODO:352:r: |   |-> populate dot11WtpChannelTable data context.
	         * Populate data context here. (optionally, delay until row prep)
	         */
	    /*
	     * TRANSIENT or semi-TRANSIENT data:
	     * copy data or save any info needed to do it in row_prep.
	     */
	    /*
	     * setup/save data for wtpPathPhyErrPack
	     * wtpPathPhyErrPack(1)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpPathPhyErrPack mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.wtpPathPhyErrPack = WtpNode->ast_rx_phyerr;
	    
	    /*
	     * setup/save data for wtpPathMacErrPack
	     * wtpPathMacErrPack(2)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpPathMacErrPack mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.wtpPathMacErrPack = WtpNode->tx_errors;
	    
	    /*
	     * setup/save data for wtpPathFrameResRate
     	     * wtpPathFrameResRate(3)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpPathFrameResRate mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    if((WtpNode->tx_retry == 0)||(WtpNode->tx_packets == 0))
			rowreq_ctx->data.wtpPathFrameResRate = 0;
		else
		    rowreq_ctx->data.wtpPathFrameResRate = (WtpNode->tx_retry*100)/WtpNode->tx_packets;
	    
	    /*
	     * setup/save data for wtpPathErrFrameRate
     	     * wtpPathErrFrameRate(4)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpPathErrFrameRate mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    if((WtpNode->tx_errors == 0)||(WtpNode->tx_packets == 0))
			rowreq_ctx->data.wtpPathErrFrameRate = 0;
		else
	    	rowreq_ctx->data.wtpPathErrFrameRate = (WtpNode->tx_errors*100)/WtpNode->tx_packets;
	    
	    /*
	     * setup/save data for wtpMonitorTime
	     * wtpMonitorTime(5)/TICKS/ASN_TIMETICKS/u_long(u_long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpMonitorTime mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.wtpMonitorTime = WtpNode->monitor_time*100;
	    
	    /*
	     * setup/save data for wtpSignalPathOfTerm
	     * wtpSignalPathOfTerm(6)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
	     */
	    /*
	     * TODO:246:r: |-> Define wtpSignalPathOfTerm mapping.
	     * Map values between raw/native values and MIB values
	     *
	     * Integer based value can usually just do a direct copy.
	     */
	    rowreq_ctx->data.wtpSignalPathOfTerm = WtpNode->sta_num;
	    
	        
	        /*
	         * insert into table container
	         */
	        if(CONTAINER_INSERT(container, rowreq_ctx))		
			{
				dot11WtpChannelTable_release_rowreq_ctx(rowreq_ctx);
			}
	        ++count;
	    	}
		}
		if(ret == 1)
		{
			Free_show_statistcs_information_of_all_wtp_whole_cmd(WtpHead);
		}
	}
	free_instance_parameter_list(&para_head);


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
        //fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11WtpChannelTable:dot11WtpChannelTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11WtpChannelTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11WtpChannelTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11WtpChannelTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:dot11WtpChannelTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11WtpChannelTable cache.
     */
} /* dot11WtpChannelTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11WtpChannelTable_row_prep( dot11WtpChannelTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpChannelTable:dot11WtpChannelTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11WtpChannelTable_row_prep */

/** @} */
