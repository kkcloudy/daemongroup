/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11RogueStaTable.h"


#include "dot11RogueStaTable_data_access.h"
#include "wcpss/wid/WID.h"
#include "ws_dcli_ac.h"
#include "mac_corp.h"
#include "../mibs_public.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11RogueStaTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11RogueStaTable is subid 2 of dot11Rogue.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2, length: 12
*/

/**
 * initialization for dot11RogueStaTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11RogueStaTable_reg
 *        Pointer to dot11RogueStaTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11RogueStaTable_init_data(dot11RogueStaTable_registration_ptr dot11RogueStaTable_reg)
{
    DEBUGMSGTL(("verbose:dot11RogueStaTable:dot11RogueStaTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11RogueStaTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11RogueStaTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11RogueStaTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11RogueStaTable:dot11RogueStaTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11RogueStaTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11RogueStaTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11ROGUESTATABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11RogueStaTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11RogueStaTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11RogueStaTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11RogueStaTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11RogueStaTable_cache_load\n");

    dot11RogueStaTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * rogueStaIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
         */
   long   rogueStaIndex = 1;

    DEBUGMSGTL(("verbose:dot11RogueStaTable:dot11RogueStaTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
        //return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11RogueStaTable container.
     * loop over your dot11RogueStaTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    instance_parameter *para_head = NULL, *para_node = NULL;
    list_instance_parameter(&para_head, SNMPD_INSTANCE_MASTER);	
    for(para_node = para_head; NULL != para_node; para_node = para_node->next)
    {
		int ret = 0;
		DCLI_AC_API_GROUP_TWO *LIST = NULL;
		struct Neighbor_AP_ELE *head = NULL;
		int len = 0;
		int i = 0;
		char mac[20] = { 0 };
		char temp_mac[20] = { 0 };
		char rogueStaMac[255] = { 0 };
		size_t rogueStaMac_len = 0;
		int rogueStaMonitorNum = 0;
		char Ftime[255] = { 0 };
		char rogueStaFirstDetectTm[255] = { 0 };
		size_t rogueStaFirstDetectTm_len = 0;
		char Ltime[255] = { 0 };
		char rogueStaLastDetectTm[255] = { 0 };
		size_t rogueStaLastDetectTm_len = 0;
		char rogueStaAccessBSSID[255] = { 0 };
		size_t rogueStaAccessBSSID_len = 0;
		char corp[255] = { 0 };
		char rogueStaManufacturer[255] = { 0 };
		size_t rogueStaManufacturer_len = 0;
		
		snmp_log(LOG_DEBUG, "enter show_rogue_ap_list_v1_func\n");
		ret = show_rogue_ap_list_v1_func(para_node->parameter, para_node->connection,&LIST);
		snmp_log(LOG_DEBUG, "exit show_rogue_ap_list_v1_func,ret=%d\n", ret);
	 	if((ret == 1)&&(LIST->rouge_ap_list))
		{
			rogueStaMonitorNum = LIST->rouge_ap_list->wtp_online_num;
			len = LIST->rouge_ap_list->neighborapInfosCount;
			init_mac_corp_data();
			for(i=0,head = LIST->rouge_ap_list->neighborapInfos;
					((i<len)&&(NULL != head));
					i++,head = head->next)
			{
				if((head->IEs_INFO)&&(strchr(head->IEs_INFO,'I')!=NULL))
			    {
		    /*
		    ***************************************************
		    ***             START EXAMPLE CODE              ***
		    ***---------------------------------------------***/
		    /*
		     * get a line (skip blank lines)
		     */
		    //do {
		        //if (!fgets(line, sizeof(line), filep)) {
		            /* we're done */
		            //fclose(filep);
		            //filep = NULL;
		        //}
		    //} while (filep && (line[0] == '\n'));

		    /*
		     * check for end of data
		     */
		    //if(NULL == filep)
		        //break;

		    /*
		     * parse line into variables
		     */
		    /*
		    ***---------------------------------------------***
		    ***              END  EXAMPLE CODE              ***
		    ***************************************************/

		        /*
		         * TODO:352:M: |   |-> set indexes in new dot11RogueStaTable rowreq context.
		         */
		        rowreq_ctx = dot11RogueStaTable_allocate_rowreq_ctx();
		        if (NULL == rowreq_ctx) {
		            snmp_log(LOG_ERR, "memory allocation failed\n");
					Free_rogue_ap_list_v1_head(LIST);
					free_instance_parameter_list(&para_head);
		            return MFD_RESOURCE_UNAVAILABLE;
		        }
		        if(MFD_SUCCESS != dot11RogueStaTable_indexes_set(rowreq_ctx
		                               , rogueStaIndex
		               )) {
		            snmp_log(LOG_ERR,"error setting index while loading "
		                     "dot11RogueStaTable cache.\n");
		            dot11RogueStaTable_release_rowreq_ctx(rowreq_ctx);
		            continue;
		        }

		        /*
		         * TODO:352:r: |   |-> populate dot11RogueStaTable data context.
		         * Populate data context here. (optionally, delay until row prep)
		         */
		    /*
		     * TRANSIENT or semi-TRANSIENT data:
		     * copy data or save any info needed to do it in row_prep.
		     */
		    /*
		     * setup/save data for rogueStaMac
		     * rogueStaMac(2)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
		     */
		    /*
		     * TODO:246:r: |-> Define rogueStaMac mapping.
		     * Map values between raw/native values and MIB values
		     *
		     * if(MFD_SUCCESS !=
		     *    rogueStaMac_map(&rowreq_ctx->data.rogueStaMac, &rowreq_ctx->data.rogueStaMac_len,
		     *                rogueStaMac, rogueStaMac_len, 0)) {
		     *    return MFD_ERROR;
		     * }
		     */
		    /*
		     * make sure there is enough space for rogueStaMac data
		     */
		    /*if ((NULL == rowreq_ctx->data.rogueStaMac) ||
		        (rowreq_ctx->data.rogueStaMac_len < (rogueStaMac_len * sizeof(rowreq_ctx->data.rogueStaMac[0])))) {
		        snmp_log(LOG_ERR,"not enough space for value\n");
		        return MFD_ERROR;
		    }*/
		    memcpy(&(rowreq_ctx->data.parameter), &(para_node->parameter), sizeof(dbus_parameter));
				
		    memset(mac,0,sizeof(mac));
			snprintf(mac,sizeof(mac)-1,
					 "%02X:%02X:%02X:%02X:%02X:%02X",
					 head->BSSID[0],head->BSSID[1],
					 head->BSSID[2],head->BSSID[3],
					 head->BSSID[4],head->BSSID[5]);
			
		    rogueStaMac_len = MIN(strlen(mac),sizeof(rogueStaMac)-1);
			memset(rogueStaMac,0,sizeof(rogueStaMac));
			memcpy(rogueStaMac,mac,rogueStaMac_len);
		
		    rowreq_ctx->data.rogueStaMac_len = rogueStaMac_len * sizeof(rowreq_ctx->data.rogueStaMac[0]);
		    memcpy( rowreq_ctx->data.rogueStaMac, rogueStaMac, rowreq_ctx->data.rogueStaMac_len );
		    
		    /*
		     * setup/save data for rogueStaMonitorNum
		     * rogueStaMonitorNum(3)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
		     */
		    /*
		     * TODO:246:r: |-> Define rogueStaMonitorNum mapping.
		     * Map values between raw/native values and MIB values
		     *
		     * Integer based value can usually just do a direct copy.
		     */
		    rowreq_ctx->data.rogueStaMonitorNum = rogueStaMonitorNum;
		    
		    /*
		     * setup/save data for rogueStaFirstDetectTm
		     * rogueStaFirstDetectTm(4)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
		     */
		    /*
		     * TODO:246:r: |-> Define rogueStaFirstDetectTm mapping.
		     * Map values between raw/native values and MIB values
		     *
		     * if(MFD_SUCCESS !=
		     *    rogueStaFirstDetectTm_map(&rowreq_ctx->data.rogueStaFirstDetectTm, &rowreq_ctx->data.rogueStaFirstDetectTm_len,
		     *                rogueStaFirstDetectTm, rogueStaFirstDetectTm_len, 0)) {
		     *    return MFD_ERROR;
		     * }
		     */
		    /*
		     * make sure there is enough space for rogueStaFirstDetectTm data
		     */
		    /*if ((NULL == rowreq_ctx->data.rogueStaFirstDetectTm) ||
		        (rowreq_ctx->data.rogueStaFirstDetectTm_len < (rogueStaFirstDetectTm_len * sizeof(rowreq_ctx->data.rogueStaFirstDetectTm[0])))) {
		        snmp_log(LOG_ERR,"not enough space for value\n");
		        return MFD_ERROR;
		    }*/
		    memset(Ftime,0,sizeof(Ftime));
			snprintf(Ftime,sizeof(Ftime)-1,"%s",ctime(&(head->fst_dtc_tm)));
			delete_enter(Ftime);

			rogueStaFirstDetectTm_len = MIN(strlen(Ftime),sizeof(rogueStaFirstDetectTm)-1);
			memset(rogueStaFirstDetectTm,0,sizeof(rogueStaFirstDetectTm));
			memcpy(rogueStaFirstDetectTm,Ftime,rogueStaFirstDetectTm_len);
		
		    rowreq_ctx->data.rogueStaFirstDetectTm_len = rogueStaFirstDetectTm_len * sizeof(rowreq_ctx->data.rogueStaFirstDetectTm[0]);
		    memcpy( rowreq_ctx->data.rogueStaFirstDetectTm, rogueStaFirstDetectTm, rowreq_ctx->data.rogueStaFirstDetectTm_len );
		    
		    /*
		     * setup/save data for rogueStaLastDetectTm
		     * rogueStaLastDetectTm(5)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
		     */
		    /*
		     * TODO:246:r: |-> Define rogueStaLastDetectTm mapping.
		     * Map values between raw/native values and MIB values
		     *
		     * if(MFD_SUCCESS !=
		     *    rogueStaLastDetectTm_map(&rowreq_ctx->data.rogueStaLastDetectTm, &rowreq_ctx->data.rogueStaLastDetectTm_len,
		     *                rogueStaLastDetectTm, rogueStaLastDetectTm_len, 0)) {
		     *    return MFD_ERROR;
		     * }
		     */
		    /*
		     * make sure there is enough space for rogueStaLastDetectTm data
		     */
		    /*if ((NULL == rowreq_ctx->data.rogueStaLastDetectTm) ||
		        (rowreq_ctx->data.rogueStaLastDetectTm_len < (rogueStaLastDetectTm_len * sizeof(rowreq_ctx->data.rogueStaLastDetectTm[0])))) {
		        snmp_log(LOG_ERR,"not enough space for value\n");
		        return MFD_ERROR;
		    }*/
		    memset(Ltime,0,sizeof(Ltime));
			snprintf(Ltime,sizeof(Ltime)-1,"%s",ctime(&(head->lst_dtc_tm)));
			delete_enter(Ltime);

			rogueStaLastDetectTm_len = MIN(strlen(Ltime),sizeof(rogueStaLastDetectTm)-1);
			memset(rogueStaLastDetectTm,0,sizeof(rogueStaLastDetectTm));
			memcpy(rogueStaLastDetectTm,Ltime,rogueStaLastDetectTm_len);
		
		    rowreq_ctx->data.rogueStaLastDetectTm_len = rogueStaLastDetectTm_len * sizeof(rowreq_ctx->data.rogueStaLastDetectTm[0]);
		    memcpy( rowreq_ctx->data.rogueStaLastDetectTm, rogueStaLastDetectTm, rowreq_ctx->data.rogueStaLastDetectTm_len );
		    
		    /*
		     * setup/save data for rogueStaAccessBSSID
		     * rogueStaAccessBSSID(6)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
		     */
		    /*
		     * TODO:246:r: |-> Define rogueStaAccessBSSID mapping.
		     * Map values between raw/native values and MIB values
		     *
		     * if(MFD_SUCCESS !=
		     *    rogueStaAccessBSSID_map(&rowreq_ctx->data.rogueStaAccessBSSID, &rowreq_ctx->data.rogueStaAccessBSSID_len,
		     *                rogueStaAccessBSSID, rogueStaAccessBSSID_len, 0)) {
		     *    return MFD_ERROR;
		     * }
		     */
		    /*
		     * make sure there is enough space for rogueStaAccessBSSID data
		     */
		    /*if ((NULL == rowreq_ctx->data.rogueStaAccessBSSID) ||
		        (rowreq_ctx->data.rogueStaAccessBSSID_len < (rogueStaAccessBSSID_len * sizeof(rowreq_ctx->data.rogueStaAccessBSSID[0])))) {
		        snmp_log(LOG_ERR,"not enough space for value\n");
		        return MFD_ERROR;
		    }*/
		    rogueStaAccessBSSID_len = MIN(strlen(mac),sizeof(rogueStaAccessBSSID)-1);
			memset(rogueStaAccessBSSID,0,sizeof(rogueStaAccessBSSID));
			memcpy(rogueStaAccessBSSID,mac,rogueStaAccessBSSID_len);
			
		    rowreq_ctx->data.rogueStaAccessBSSID_len = rogueStaAccessBSSID_len * sizeof(rowreq_ctx->data.rogueStaAccessBSSID[0]);
		    memcpy( rowreq_ctx->data.rogueStaAccessBSSID, rogueStaAccessBSSID, rowreq_ctx->data.rogueStaAccessBSSID_len );
		    
		    /*
		     * setup/save data for rogueStaMaxSigStrength
		     * rogueStaMaxSigStrength(7)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
		     */
		    /*
		     * TODO:246:r: |-> Define rogueStaMaxSigStrength mapping.
		     * Map values between raw/native values and MIB values
		     *
		     * Integer based value can usually just do a direct copy.
		     */
		    rowreq_ctx->data.rogueStaMaxSigStrength = head->RSSI;
		    
		    /*
		     * setup/save data for rogueStaChannel
		     * rogueStaChannel(8)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
		     */
		    /*
		     * TODO:246:r: |-> Define rogueStaChannel mapping.
		     * Map values between raw/native values and MIB values
		     *
		     * Integer based value can usually just do a direct copy.
		     */
		    rowreq_ctx->data.rogueStaChannel = head->Channel;
		    
		    /*
		     * setup/save data for rogueStaAdHocStatus
		     * rogueStaAdHocStatus(9)/TruthValue/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h
		     */
		    /*
		     * TODO:246:r: |-> Define rogueStaAdHocStatus mapping.
		     * Map values between raw/native values and MIB values
		     *
		    * enums usually need mapping.
		    */
		    /*if(MFD_SUCCESS !=
		       rogueStaAdHocStatus_map(&rowreq_ctx->data.rogueStaAdHocStatus, rogueStaAdHocStatus )) {
		        return MFD_ERROR;
		    }*/
			rowreq_ctx->data.rogueStaAdHocStatus = 1;
		    
		    /*
		     * setup/save data for rogueStaAttackStatus
		     * rogueStaAttackStatus(10)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
		     */
		    /*
		     * TODO:246:r: |-> Define rogueStaAttackStatus mapping.
		     * Map values between raw/native values and MIB values
		     *
		    * enums usually need mapping.
		    */
		    /*if(MFD_SUCCESS !=
		       rogueStaAttackStatus_map(&rowreq_ctx->data.rogueStaAttackStatus, rogueStaAttackStatus )) {
		        return MFD_ERROR;
		    }*/
		    if(head->RogueAPAttackedStatus == 1)
		    	rowreq_ctx->data.rogueStaAttackStatus = 1;
			else
				rowreq_ctx->data.rogueStaAttackStatus = 2;
		    /*
		     * setup/save data for rogueStaToIgnore
		     * rogueStaToIgnore(11)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h
		     */
		    /*
		     * TODO:246:r: |-> Define rogueStaToIgnore mapping.
		     * Map values between raw/native values and MIB values
		     *
		    * enums usually need mapping.
		    */
		    /*if(MFD_SUCCESS !=
		       rogueStaToIgnore_map(&rowreq_ctx->data.rogueStaToIgnore, rogueStaToIgnore )) {
		        return MFD_ERROR;
		    }*/
			if(head->RogueAPToIgnore == 1)
		    	rowreq_ctx->data.rogueStaToIgnore = 1;
			else
				rowreq_ctx->data.rogueStaToIgnore = 2;
		    
		    /*
		     * setup/save data for rogueStaManufacturer
		     * rogueStaManufacturer(12)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
		     */
		    /*
		     * TODO:246:r: |-> Define rogueStaManufacturer mapping.
		     * Map values between raw/native values and MIB values
		     *
		     * if(MFD_SUCCESS !=
		     *    rogueStaManufacturer_map(&rowreq_ctx->data.rogueStaManufacturer, &rowreq_ctx->data.rogueStaManufacturer_len,
		     *                rogueStaManufacturer, rogueStaManufacturer_len, 0)) {
		     *    return MFD_ERROR;
		     * }
		     */
		    /*
		     * make sure there is enough space for rogueStaManufacturer data
		     */
		    /*if ((NULL == rowreq_ctx->data.rogueStaManufacturer) ||
		        (rowreq_ctx->data.rogueStaManufacturer_len < (rogueStaManufacturer_len * sizeof(rowreq_ctx->data.rogueStaManufacturer[0])))) {
		        snmp_log(LOG_ERR,"not enough space for value\n");
		        return MFD_ERROR;
		    }*/
		    memset(temp_mac,0,sizeof(temp_mac));
			snprintf(temp_mac,sizeof(temp_mac)-1,
					 "%02X-%02X-%02X-%02X-%02X-%02X",
					 head->BSSID[0],head->BSSID[1],
					 head->BSSID[2],head->BSSID[3],
					 head->BSSID[4],head->BSSID[5]);

		    memset(corp, 0, sizeof(corp) );
			get_corp_by_mac( temp_mac, corp, sizeof(corp) );
			if(strcmp(corp,"")==0)
			{
				memset( corp, 0, sizeof(corp) );
				strncpy( corp, "UNKNOWN", sizeof(corp)-1);
			}
			
			rogueStaManufacturer_len = MIN(strlen(corp),sizeof(rogueStaManufacturer)-1);
			memset(rogueStaManufacturer,0,sizeof(rogueStaManufacturer));
			memcpy(rogueStaManufacturer,corp,rogueStaManufacturer_len);
		
		    rowreq_ctx->data.rogueStaManufacturer_len = rogueStaManufacturer_len * sizeof(rowreq_ctx->data.rogueStaManufacturer[0]);
		    memcpy( rowreq_ctx->data.rogueStaManufacturer, rogueStaManufacturer, rowreq_ctx->data.rogueStaManufacturer_len );
		    
		        
		        /*
		         * insert into table container
		         */
		        if(CONTAINER_INSERT(container, rowreq_ctx))
				{
					dot11RogueStaTable_release_rowreq_ctx(rowreq_ctx);
				}
		        ++count;
				rogueStaIndex++;
		    }	}
			destroy_mac_corp_data();
		}
		if(ret == 1)
		{
			Free_rogue_ap_list_v1_head(LIST);
		}
	}
	free_instance_parameter_list(&para_head);

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
        //fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11RogueStaTable:dot11RogueStaTable_cache_load",
               "inserted %d records\n", count));
	
	snmp_log(LOG_DEBUG, "exit dot11RogueStaTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11RogueStaTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11RogueStaTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11RogueStaTable:dot11RogueStaTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11RogueStaTable cache.
     */
} /* dot11RogueStaTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11RogueStaTable_row_prep( dot11RogueStaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11RogueStaTable:dot11RogueStaTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11RogueStaTable_row_prep */

/** @} */
