/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11RogueStaTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11RogueStaTable get routines.
 * TODO:240:M: Implement dot11RogueStaTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11RogueStaTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11RogueStaTable is subid 2 of dot11Rogue.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11RogueStaTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaIndex
 * rogueStaIndex is subid 1 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.1
 * Description:
Rogue station index.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaIndex.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaIndex_map(long *mib_rogueStaIndex_val_ptr, long raw_rogueStaIndex_val)
{
    netsnmp_assert(NULL != mib_rogueStaIndex_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaIndex_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueStaIndex mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_rogueStaIndex_val_ptr) = raw_rogueStaIndex_val;

    return MFD_SUCCESS;
} /* rogueStaIndex_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11RogueStaTable_indexes_set_tbl_idx(dot11RogueStaTable_mib_index *tbl_idx, long rogueStaIndex_val)
{
    DEBUGMSGTL(("verbose:dot11RogueStaTable:dot11RogueStaTable_indexes_set_tbl_idx","called\n"));

    /* rogueStaIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h */
    tbl_idx->rogueStaIndex = rogueStaIndex_val;
    

    return MFD_SUCCESS;
} /* dot11RogueStaTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11RogueStaTable_indexes_set(dot11RogueStaTable_rowreq_ctx *rowreq_ctx, long rogueStaIndex_val)
{
    DEBUGMSGTL(("verbose:dot11RogueStaTable:dot11RogueStaTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11RogueStaTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , rogueStaIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11RogueStaTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11RogueStaTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaMac
 * rogueStaMac is subid 2 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.2
 * Description:
Rogue station mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaMac.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaMac_map(char **mib_rogueStaMac_val_ptr_ptr, size_t *mib_rogueStaMac_val_ptr_len_ptr, char *raw_rogueStaMac_val_ptr, size_t raw_rogueStaMac_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_rogueStaMac_val_ptr);
    netsnmp_assert((NULL != mib_rogueStaMac_val_ptr_ptr) && (NULL != mib_rogueStaMac_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaMac_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement rogueStaMac non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_rogueStaMac_val_ptr_len; /* assume equal */
    if((NULL == *mib_rogueStaMac_val_ptr_ptr) || (*mib_rogueStaMac_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_rogueStaMac_val_ptr_ptr = realloc( *mib_rogueStaMac_val_ptr_ptr, converted_len * sizeof(**mib_rogueStaMac_val_ptr_ptr));
        if(NULL == *mib_rogueStaMac_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_rogueStaMac_val_ptr_len_ptr = converted_len;
    memcpy( *mib_rogueStaMac_val_ptr_ptr, raw_rogueStaMac_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* rogueStaMac_map */

/**
 * Extract the current value of the rogueStaMac data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueStaMac_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rogueStaMac_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rogueStaMac.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rogueStaMac_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rogueStaMac_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rogueStaMac_get( dot11RogueStaTable_rowreq_ctx *rowreq_ctx, char **rogueStaMac_val_ptr_ptr, size_t *rogueStaMac_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != rogueStaMac_val_ptr_ptr) && (NULL != *rogueStaMac_val_ptr_ptr));
   netsnmp_assert( NULL != rogueStaMac_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaMac_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueStaMac data.
 * set (* rogueStaMac_val_ptr_ptr ) and (* rogueStaMac_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for rogueStaMac data
     */
    if ((NULL == (* rogueStaMac_val_ptr_ptr )) ||
        ((* rogueStaMac_val_ptr_len_ptr ) < (rowreq_ctx->data.rogueStaMac_len * sizeof((* rogueStaMac_val_ptr_ptr )[0])))) {
        /*
         * allocate space for rogueStaMac data
         */
        (* rogueStaMac_val_ptr_ptr ) = malloc(rowreq_ctx->data.rogueStaMac_len * sizeof((* rogueStaMac_val_ptr_ptr )[0]));
        if(NULL == (* rogueStaMac_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* rogueStaMac_val_ptr_len_ptr ) = rowreq_ctx->data.rogueStaMac_len * sizeof((* rogueStaMac_val_ptr_ptr )[0]);
    memcpy( (* rogueStaMac_val_ptr_ptr ), rowreq_ctx->data.rogueStaMac, (* rogueStaMac_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* rogueStaMac_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaMonitorNum
 * rogueStaMonitorNum is subid 3 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.3
 * Description:
The station number that detect rogue station.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaMonitorNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaMonitorNum_map(long *mib_rogueStaMonitorNum_val_ptr, long raw_rogueStaMonitorNum_val)
{
    netsnmp_assert(NULL != mib_rogueStaMonitorNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaMonitorNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueStaMonitorNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_rogueStaMonitorNum_val_ptr) = raw_rogueStaMonitorNum_val;

    return MFD_SUCCESS;
} /* rogueStaMonitorNum_map */

/**
 * Extract the current value of the rogueStaMonitorNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueStaMonitorNum_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueStaMonitorNum_get( dot11RogueStaTable_rowreq_ctx *rowreq_ctx, long * rogueStaMonitorNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueStaMonitorNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaMonitorNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueStaMonitorNum data.
 * set (* rogueStaMonitorNum_val_ptr ) from rowreq_ctx->data
 */
    (* rogueStaMonitorNum_val_ptr ) = rowreq_ctx->data.rogueStaMonitorNum;

    return MFD_SUCCESS;
} /* rogueStaMonitorNum_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaFirstDetectTm
 * rogueStaFirstDetectTm is subid 4 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.4
 * Description:
The first time that detect rogue station.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaFirstDetectTm.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaFirstDetectTm_map(char **mib_rogueStaFirstDetectTm_val_ptr_ptr, size_t *mib_rogueStaFirstDetectTm_val_ptr_len_ptr, char *raw_rogueStaFirstDetectTm_val_ptr, size_t raw_rogueStaFirstDetectTm_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_rogueStaFirstDetectTm_val_ptr);
    netsnmp_assert((NULL != mib_rogueStaFirstDetectTm_val_ptr_ptr) && (NULL != mib_rogueStaFirstDetectTm_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaFirstDetectTm_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement rogueStaFirstDetectTm non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_rogueStaFirstDetectTm_val_ptr_len; /* assume equal */
    if((NULL == *mib_rogueStaFirstDetectTm_val_ptr_ptr) || (*mib_rogueStaFirstDetectTm_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_rogueStaFirstDetectTm_val_ptr_ptr = realloc( *mib_rogueStaFirstDetectTm_val_ptr_ptr, converted_len * sizeof(**mib_rogueStaFirstDetectTm_val_ptr_ptr));
        if(NULL == *mib_rogueStaFirstDetectTm_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_rogueStaFirstDetectTm_val_ptr_len_ptr = converted_len;
    memcpy( *mib_rogueStaFirstDetectTm_val_ptr_ptr, raw_rogueStaFirstDetectTm_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* rogueStaFirstDetectTm_map */

/**
 * Extract the current value of the rogueStaFirstDetectTm data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueStaFirstDetectTm_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rogueStaFirstDetectTm_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rogueStaFirstDetectTm.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rogueStaFirstDetectTm_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rogueStaFirstDetectTm_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rogueStaFirstDetectTm_get( dot11RogueStaTable_rowreq_ctx *rowreq_ctx, char **rogueStaFirstDetectTm_val_ptr_ptr, size_t *rogueStaFirstDetectTm_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != rogueStaFirstDetectTm_val_ptr_ptr) && (NULL != *rogueStaFirstDetectTm_val_ptr_ptr));
   netsnmp_assert( NULL != rogueStaFirstDetectTm_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaFirstDetectTm_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueStaFirstDetectTm data.
 * set (* rogueStaFirstDetectTm_val_ptr_ptr ) and (* rogueStaFirstDetectTm_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for rogueStaFirstDetectTm data
     */
    if ((NULL == (* rogueStaFirstDetectTm_val_ptr_ptr )) ||
        ((* rogueStaFirstDetectTm_val_ptr_len_ptr ) < (rowreq_ctx->data.rogueStaFirstDetectTm_len * sizeof((* rogueStaFirstDetectTm_val_ptr_ptr )[0])))) {
        /*
         * allocate space for rogueStaFirstDetectTm data
         */
        (* rogueStaFirstDetectTm_val_ptr_ptr ) = malloc(rowreq_ctx->data.rogueStaFirstDetectTm_len * sizeof((* rogueStaFirstDetectTm_val_ptr_ptr )[0]));
        if(NULL == (* rogueStaFirstDetectTm_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* rogueStaFirstDetectTm_val_ptr_len_ptr ) = rowreq_ctx->data.rogueStaFirstDetectTm_len * sizeof((* rogueStaFirstDetectTm_val_ptr_ptr )[0]);
    memcpy( (* rogueStaFirstDetectTm_val_ptr_ptr ), rowreq_ctx->data.rogueStaFirstDetectTm, (* rogueStaFirstDetectTm_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* rogueStaFirstDetectTm_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaLastDetectTm
 * rogueStaLastDetectTm is subid 5 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.5
 * Description:
The last time that detect rogue station.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaLastDetectTm.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaLastDetectTm_map(char **mib_rogueStaLastDetectTm_val_ptr_ptr, size_t *mib_rogueStaLastDetectTm_val_ptr_len_ptr, char *raw_rogueStaLastDetectTm_val_ptr, size_t raw_rogueStaLastDetectTm_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_rogueStaLastDetectTm_val_ptr);
    netsnmp_assert((NULL != mib_rogueStaLastDetectTm_val_ptr_ptr) && (NULL != mib_rogueStaLastDetectTm_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaLastDetectTm_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement rogueStaLastDetectTm non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_rogueStaLastDetectTm_val_ptr_len; /* assume equal */
    if((NULL == *mib_rogueStaLastDetectTm_val_ptr_ptr) || (*mib_rogueStaLastDetectTm_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_rogueStaLastDetectTm_val_ptr_ptr = realloc( *mib_rogueStaLastDetectTm_val_ptr_ptr, converted_len * sizeof(**mib_rogueStaLastDetectTm_val_ptr_ptr));
        if(NULL == *mib_rogueStaLastDetectTm_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_rogueStaLastDetectTm_val_ptr_len_ptr = converted_len;
    memcpy( *mib_rogueStaLastDetectTm_val_ptr_ptr, raw_rogueStaLastDetectTm_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* rogueStaLastDetectTm_map */

/**
 * Extract the current value of the rogueStaLastDetectTm data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueStaLastDetectTm_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rogueStaLastDetectTm_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rogueStaLastDetectTm.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rogueStaLastDetectTm_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rogueStaLastDetectTm_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rogueStaLastDetectTm_get( dot11RogueStaTable_rowreq_ctx *rowreq_ctx, char **rogueStaLastDetectTm_val_ptr_ptr, size_t *rogueStaLastDetectTm_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != rogueStaLastDetectTm_val_ptr_ptr) && (NULL != *rogueStaLastDetectTm_val_ptr_ptr));
   netsnmp_assert( NULL != rogueStaLastDetectTm_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaLastDetectTm_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueStaLastDetectTm data.
 * set (* rogueStaLastDetectTm_val_ptr_ptr ) and (* rogueStaLastDetectTm_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for rogueStaLastDetectTm data
     */
    if ((NULL == (* rogueStaLastDetectTm_val_ptr_ptr )) ||
        ((* rogueStaLastDetectTm_val_ptr_len_ptr ) < (rowreq_ctx->data.rogueStaLastDetectTm_len * sizeof((* rogueStaLastDetectTm_val_ptr_ptr )[0])))) {
        /*
         * allocate space for rogueStaLastDetectTm data
         */
        (* rogueStaLastDetectTm_val_ptr_ptr ) = malloc(rowreq_ctx->data.rogueStaLastDetectTm_len * sizeof((* rogueStaLastDetectTm_val_ptr_ptr )[0]));
        if(NULL == (* rogueStaLastDetectTm_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* rogueStaLastDetectTm_val_ptr_len_ptr ) = rowreq_ctx->data.rogueStaLastDetectTm_len * sizeof((* rogueStaLastDetectTm_val_ptr_ptr )[0]);
    memcpy( (* rogueStaLastDetectTm_val_ptr_ptr ), rowreq_ctx->data.rogueStaLastDetectTm, (* rogueStaLastDetectTm_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* rogueStaLastDetectTm_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaAccessBSSID
 * rogueStaAccessBSSID is subid 6 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.6
 * Description:
The rogue station access BSSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaAccessBSSID.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaAccessBSSID_map(char **mib_rogueStaAccessBSSID_val_ptr_ptr, size_t *mib_rogueStaAccessBSSID_val_ptr_len_ptr, char *raw_rogueStaAccessBSSID_val_ptr, size_t raw_rogueStaAccessBSSID_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_rogueStaAccessBSSID_val_ptr);
    netsnmp_assert((NULL != mib_rogueStaAccessBSSID_val_ptr_ptr) && (NULL != mib_rogueStaAccessBSSID_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaAccessBSSID_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement rogueStaAccessBSSID non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_rogueStaAccessBSSID_val_ptr_len; /* assume equal */
    if((NULL == *mib_rogueStaAccessBSSID_val_ptr_ptr) || (*mib_rogueStaAccessBSSID_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_rogueStaAccessBSSID_val_ptr_ptr = realloc( *mib_rogueStaAccessBSSID_val_ptr_ptr, converted_len * sizeof(**mib_rogueStaAccessBSSID_val_ptr_ptr));
        if(NULL == *mib_rogueStaAccessBSSID_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_rogueStaAccessBSSID_val_ptr_len_ptr = converted_len;
    memcpy( *mib_rogueStaAccessBSSID_val_ptr_ptr, raw_rogueStaAccessBSSID_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* rogueStaAccessBSSID_map */

/**
 * Extract the current value of the rogueStaAccessBSSID data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueStaAccessBSSID_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rogueStaAccessBSSID_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rogueStaAccessBSSID.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rogueStaAccessBSSID_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rogueStaAccessBSSID_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rogueStaAccessBSSID_get( dot11RogueStaTable_rowreq_ctx *rowreq_ctx, char **rogueStaAccessBSSID_val_ptr_ptr, size_t *rogueStaAccessBSSID_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != rogueStaAccessBSSID_val_ptr_ptr) && (NULL != *rogueStaAccessBSSID_val_ptr_ptr));
   netsnmp_assert( NULL != rogueStaAccessBSSID_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaAccessBSSID_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueStaAccessBSSID data.
 * set (* rogueStaAccessBSSID_val_ptr_ptr ) and (* rogueStaAccessBSSID_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for rogueStaAccessBSSID data
     */
    if ((NULL == (* rogueStaAccessBSSID_val_ptr_ptr )) ||
        ((* rogueStaAccessBSSID_val_ptr_len_ptr ) < (rowreq_ctx->data.rogueStaAccessBSSID_len * sizeof((* rogueStaAccessBSSID_val_ptr_ptr )[0])))) {
        /*
         * allocate space for rogueStaAccessBSSID data
         */
        (* rogueStaAccessBSSID_val_ptr_ptr ) = malloc(rowreq_ctx->data.rogueStaAccessBSSID_len * sizeof((* rogueStaAccessBSSID_val_ptr_ptr )[0]));
        if(NULL == (* rogueStaAccessBSSID_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* rogueStaAccessBSSID_val_ptr_len_ptr ) = rowreq_ctx->data.rogueStaAccessBSSID_len * sizeof((* rogueStaAccessBSSID_val_ptr_ptr )[0]);
    memcpy( (* rogueStaAccessBSSID_val_ptr_ptr ), rowreq_ctx->data.rogueStaAccessBSSID, (* rogueStaAccessBSSID_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* rogueStaAccessBSSID_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaMaxSigStrength
 * rogueStaMaxSigStrength is subid 7 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.7
 * Description:
WIDS receive from the suspect the maximum station signal strength.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaMaxSigStrength.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaMaxSigStrength_map(long *mib_rogueStaMaxSigStrength_val_ptr, long raw_rogueStaMaxSigStrength_val)
{
    netsnmp_assert(NULL != mib_rogueStaMaxSigStrength_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaMaxSigStrength_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueStaMaxSigStrength mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_rogueStaMaxSigStrength_val_ptr) = raw_rogueStaMaxSigStrength_val;

    return MFD_SUCCESS;
} /* rogueStaMaxSigStrength_map */

/**
 * Extract the current value of the rogueStaMaxSigStrength data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueStaMaxSigStrength_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueStaMaxSigStrength_get( dot11RogueStaTable_rowreq_ctx *rowreq_ctx, long * rogueStaMaxSigStrength_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueStaMaxSigStrength_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaMaxSigStrength_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueStaMaxSigStrength data.
 * set (* rogueStaMaxSigStrength_val_ptr ) from rowreq_ctx->data
 */
    (* rogueStaMaxSigStrength_val_ptr ) = rowreq_ctx->data.rogueStaMaxSigStrength;

    return MFD_SUCCESS;
} /* rogueStaMaxSigStrength_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaChannel
 * rogueStaChannel is subid 8 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.8
 * Description:
Rogue station number.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaChannel.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaChannel_map(long *mib_rogueStaChannel_val_ptr, long raw_rogueStaChannel_val)
{
    netsnmp_assert(NULL != mib_rogueStaChannel_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaChannel_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueStaChannel mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_rogueStaChannel_val_ptr) = raw_rogueStaChannel_val;

    return MFD_SUCCESS;
} /* rogueStaChannel_map */

/**
 * Extract the current value of the rogueStaChannel data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueStaChannel_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueStaChannel_get( dot11RogueStaTable_rowreq_ctx *rowreq_ctx, long * rogueStaChannel_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueStaChannel_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaChannel_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueStaChannel data.
 * set (* rogueStaChannel_val_ptr ) from rowreq_ctx->data
 */
    (* rogueStaChannel_val_ptr ) = rowreq_ctx->data.rogueStaChannel;

    return MFD_SUCCESS;
} /* rogueStaChannel_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaAdHocStatus
 * rogueStaAdHocStatus is subid 9 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.9
 * Description:
Rogue station whether works in Ad-Hoc mode.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaAdHocStatus.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaAdHocStatus_map(u_long *mib_rogueStaAdHocStatus_val_ptr, u_long raw_rogueStaAdHocStatus_val)
{
    netsnmp_assert(NULL != mib_rogueStaAdHocStatus_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaAdHocStatus_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueStaAdHocStatus enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_rogueStaAdHocStatus_val) {
        case INTERNAL_ROGUESTAADHOCSTATUS_TRUE:
             *mib_rogueStaAdHocStatus_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_ROGUESTAADHOCSTATUS_FALSE:
             *mib_rogueStaAdHocStatus_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for rogueStaAdHocStatus\n", raw_rogueStaAdHocStatus_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* rogueStaAdHocStatus_map */

/**
 * Extract the current value of the rogueStaAdHocStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueStaAdHocStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueStaAdHocStatus_get( dot11RogueStaTable_rowreq_ctx *rowreq_ctx, u_long * rogueStaAdHocStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueStaAdHocStatus_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaAdHocStatus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueStaAdHocStatus data.
 * set (* rogueStaAdHocStatus_val_ptr ) from rowreq_ctx->data
 */
    (* rogueStaAdHocStatus_val_ptr ) = rowreq_ctx->data.rogueStaAdHocStatus;

    return MFD_SUCCESS;
} /* rogueStaAdHocStatus_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaAttackStatus
 * rogueStaAttackStatus is subid 10 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.10
 * Description:
Whether strategies to the rogue station.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaAttackStatus.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaAttackStatus_map(u_long *mib_rogueStaAttackStatus_val_ptr, u_long raw_rogueStaAttackStatus_val)
{
    netsnmp_assert(NULL != mib_rogueStaAttackStatus_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaAttackStatus_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueStaAttackStatus enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_rogueStaAttackStatus_val) {
        case INTERNAL_ROGUESTAATTACKSTATUS_TRUE:
             *mib_rogueStaAttackStatus_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_ROGUESTAATTACKSTATUS_FALSE:
             *mib_rogueStaAttackStatus_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for rogueStaAttackStatus\n", raw_rogueStaAttackStatus_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* rogueStaAttackStatus_map */

/**
 * Extract the current value of the rogueStaAttackStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueStaAttackStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueStaAttackStatus_get( dot11RogueStaTable_rowreq_ctx *rowreq_ctx, u_long * rogueStaAttackStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueStaAttackStatus_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaAttackStatus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueStaAttackStatus data.
 * set (* rogueStaAttackStatus_val_ptr ) from rowreq_ctx->data
 */
    (* rogueStaAttackStatus_val_ptr ) = rowreq_ctx->data.rogueStaAttackStatus;

    return MFD_SUCCESS;
} /* rogueStaAttackStatus_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaToIgnore
 * rogueStaToIgnore is subid 11 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.11
 * Description:
Whether or not ignore the rogue Station.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaToIgnore.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaToIgnore_map(u_long *mib_rogueStaToIgnore_val_ptr, u_long raw_rogueStaToIgnore_val)
{
    netsnmp_assert(NULL != mib_rogueStaToIgnore_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaToIgnore_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement rogueStaToIgnore enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_rogueStaToIgnore_val) {
        case INTERNAL_ROGUESTATOIGNORE_TRUE:
             *mib_rogueStaToIgnore_val_ptr = TRUTHVALUE_TRUE;
             break;

        case INTERNAL_ROGUESTATOIGNORE_FALSE:
             *mib_rogueStaToIgnore_val_ptr = TRUTHVALUE_FALSE;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for rogueStaToIgnore\n", raw_rogueStaToIgnore_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* rogueStaToIgnore_map */

/**
 * Extract the current value of the rogueStaToIgnore data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueStaToIgnore_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rogueStaToIgnore_get( dot11RogueStaTable_rowreq_ctx *rowreq_ctx, u_long * rogueStaToIgnore_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != rogueStaToIgnore_val_ptr );


    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaToIgnore_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueStaToIgnore data.
 * set (* rogueStaToIgnore_val_ptr ) from rowreq_ctx->data
 */
    (* rogueStaToIgnore_val_ptr ) = rowreq_ctx->data.rogueStaToIgnore;

    return MFD_SUCCESS;
} /* rogueStaToIgnore_get */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11RogueStaEntry.rogueStaManufacturer
 * rogueStaManufacturer is subid 12 of dot11RogueStaEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.5.2.1.12
 * Description:
Rogue station manufacturer.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-rogueStaManufacturer.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
rogueStaManufacturer_map(char **mib_rogueStaManufacturer_val_ptr_ptr, size_t *mib_rogueStaManufacturer_val_ptr_len_ptr, char *raw_rogueStaManufacturer_val_ptr, size_t raw_rogueStaManufacturer_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_rogueStaManufacturer_val_ptr);
    netsnmp_assert((NULL != mib_rogueStaManufacturer_val_ptr_ptr) && (NULL != mib_rogueStaManufacturer_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaManufacturer_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement rogueStaManufacturer non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_rogueStaManufacturer_val_ptr_len; /* assume equal */
    if((NULL == *mib_rogueStaManufacturer_val_ptr_ptr) || (*mib_rogueStaManufacturer_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_rogueStaManufacturer_val_ptr_ptr = realloc( *mib_rogueStaManufacturer_val_ptr_ptr, converted_len * sizeof(**mib_rogueStaManufacturer_val_ptr_ptr));
        if(NULL == *mib_rogueStaManufacturer_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_rogueStaManufacturer_val_ptr_len_ptr = converted_len;
    memcpy( *mib_rogueStaManufacturer_val_ptr_ptr, raw_rogueStaManufacturer_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* rogueStaManufacturer_map */

/**
 * Extract the current value of the rogueStaManufacturer data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rogueStaManufacturer_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rogueStaManufacturer_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rogueStaManufacturer.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rogueStaManufacturer_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rogueStaManufacturer_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rogueStaManufacturer_get( dot11RogueStaTable_rowreq_ctx *rowreq_ctx, char **rogueStaManufacturer_val_ptr_ptr, size_t *rogueStaManufacturer_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != rogueStaManufacturer_val_ptr_ptr) && (NULL != *rogueStaManufacturer_val_ptr_ptr));
   netsnmp_assert( NULL != rogueStaManufacturer_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11RogueStaTable:rogueStaManufacturer_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the rogueStaManufacturer data.
 * set (* rogueStaManufacturer_val_ptr_ptr ) and (* rogueStaManufacturer_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for rogueStaManufacturer data
     */
    if ((NULL == (* rogueStaManufacturer_val_ptr_ptr )) ||
        ((* rogueStaManufacturer_val_ptr_len_ptr ) < (rowreq_ctx->data.rogueStaManufacturer_len * sizeof((* rogueStaManufacturer_val_ptr_ptr )[0])))) {
        /*
         * allocate space for rogueStaManufacturer data
         */
        (* rogueStaManufacturer_val_ptr_ptr ) = malloc(rowreq_ctx->data.rogueStaManufacturer_len * sizeof((* rogueStaManufacturer_val_ptr_ptr )[0]));
        if(NULL == (* rogueStaManufacturer_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* rogueStaManufacturer_val_ptr_len_ptr ) = rowreq_ctx->data.rogueStaManufacturer_len * sizeof((* rogueStaManufacturer_val_ptr_ptr )[0]);
    memcpy( (* rogueStaManufacturer_val_ptr_ptr ), rowreq_ctx->data.rogueStaManufacturer, (* rogueStaManufacturer_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* rogueStaManufacturer_get */



/** @} */
