/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpDeviceInfoTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11WtpDeviceInfoTable get routines.
 * TODO:240:M: Implement dot11WtpDeviceInfoTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpDeviceInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpDeviceInfoTable is subid 2 of wtpGeneralInfo.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11WtpDeviceInfoTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpInfoEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11WtpInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.1.1.1
 * Description:
AP mac.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is Dot11BaseWtpIdTC (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max )
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11WtpDeviceInfoTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11WtpDeviceInfoTable_indexes_set_tbl_idx(dot11WtpDeviceInfoTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:dot11WtpDeviceInfoTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11WtpDeviceInfoTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11WtpDeviceInfoTable_indexes_set(dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:dot11WtpDeviceInfoTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11WtpDeviceInfoTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11WtpDeviceInfoTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11WtpDeviceInfoTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpCPUType
 * wtpCPUType is subid 1 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.1
 * Description:
AP CPU type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpCPUType.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpCPUType_map(char **mib_wtpCPUType_val_ptr_ptr, size_t *mib_wtpCPUType_val_ptr_len_ptr, char *raw_wtpCPUType_val_ptr, size_t raw_wtpCPUType_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpCPUType_val_ptr);
    netsnmp_assert((NULL != mib_wtpCPUType_val_ptr_ptr) && (NULL != mib_wtpCPUType_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPUType_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpCPUType non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpCPUType_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpCPUType_val_ptr_ptr) || (*mib_wtpCPUType_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpCPUType_val_ptr_ptr = realloc( *mib_wtpCPUType_val_ptr_ptr, converted_len * sizeof(**mib_wtpCPUType_val_ptr_ptr));
        if(NULL == *mib_wtpCPUType_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpCPUType_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpCPUType_val_ptr_ptr, raw_wtpCPUType_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpCPUType_map */

/**
 * Extract the current value of the wtpCPUType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCPUType_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpCPUType_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpCPUType.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpCPUType_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpCPUType_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpCPUType_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, char **wtpCPUType_val_ptr_ptr, size_t *wtpCPUType_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpCPUType_val_ptr_ptr) && (NULL != *wtpCPUType_val_ptr_ptr));
   netsnmp_assert( NULL != wtpCPUType_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPUType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpCPUType data.
 * set (* wtpCPUType_val_ptr_ptr ) and (* wtpCPUType_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpCPUType data
     */
    if ((NULL == (* wtpCPUType_val_ptr_ptr )) ||
        ((* wtpCPUType_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpCPUType_len * sizeof((* wtpCPUType_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpCPUType data
         */
        (* wtpCPUType_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpCPUType_len * sizeof((* wtpCPUType_val_ptr_ptr )[0]));
        if(NULL == (* wtpCPUType_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpCPUType_val_ptr_len_ptr ) = rowreq_ctx->data.wtpCPUType_len * sizeof((* wtpCPUType_val_ptr_ptr )[0]);
    memcpy( (* wtpCPUType_val_ptr_ptr ), rowreq_ctx->data.wtpCPUType, (* wtpCPUType_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpCPUType_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpCPUProcessAbility
 * wtpCPUProcessAbility is subid 2 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.2
 * Description:
The CPU process ability.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpCPUProcessAbility.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpCPUProcessAbility_map(char **mib_wtpCPUProcessAbility_val_ptr_ptr, size_t *mib_wtpCPUProcessAbility_val_ptr_len_ptr, char *raw_wtpCPUProcessAbility_val_ptr, size_t raw_wtpCPUProcessAbility_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpCPUProcessAbility_val_ptr);
    netsnmp_assert((NULL != mib_wtpCPUProcessAbility_val_ptr_ptr) && (NULL != mib_wtpCPUProcessAbility_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPUProcessAbility_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpCPUProcessAbility non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpCPUProcessAbility_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpCPUProcessAbility_val_ptr_ptr) || (*mib_wtpCPUProcessAbility_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpCPUProcessAbility_val_ptr_ptr = realloc( *mib_wtpCPUProcessAbility_val_ptr_ptr, converted_len * sizeof(**mib_wtpCPUProcessAbility_val_ptr_ptr));
        if(NULL == *mib_wtpCPUProcessAbility_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpCPUProcessAbility_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpCPUProcessAbility_val_ptr_ptr, raw_wtpCPUProcessAbility_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpCPUProcessAbility_map */

/**
 * Extract the current value of the wtpCPUProcessAbility data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCPUProcessAbility_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpCPUProcessAbility_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpCPUProcessAbility.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpCPUProcessAbility_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpCPUProcessAbility_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpCPUProcessAbility_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, char **wtpCPUProcessAbility_val_ptr_ptr, size_t *wtpCPUProcessAbility_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpCPUProcessAbility_val_ptr_ptr) && (NULL != *wtpCPUProcessAbility_val_ptr_ptr));
   netsnmp_assert( NULL != wtpCPUProcessAbility_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPUProcessAbility_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpCPUProcessAbility data.
 * set (* wtpCPUProcessAbility_val_ptr_ptr ) and (* wtpCPUProcessAbility_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpCPUProcessAbility data
     */
    if ((NULL == (* wtpCPUProcessAbility_val_ptr_ptr )) ||
        ((* wtpCPUProcessAbility_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpCPUProcessAbility_len * sizeof((* wtpCPUProcessAbility_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpCPUProcessAbility data
         */
        (* wtpCPUProcessAbility_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpCPUProcessAbility_len * sizeof((* wtpCPUProcessAbility_val_ptr_ptr )[0]));
        if(NULL == (* wtpCPUProcessAbility_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpCPUProcessAbility_val_ptr_len_ptr ) = rowreq_ctx->data.wtpCPUProcessAbility_len * sizeof((* wtpCPUProcessAbility_val_ptr_ptr )[0]);
    memcpy( (* wtpCPUProcessAbility_val_ptr_ptr ), rowreq_ctx->data.wtpCPUProcessAbility, (* wtpCPUProcessAbility_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpCPUProcessAbility_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpCPURTUsage
 * wtpCPURTUsage is subid 3 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.3
 * Description:
AP's CPU use rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpCPURTUsage.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpCPURTUsage_map(long *mib_wtpCPURTUsage_val_ptr, long raw_wtpCPURTUsage_val)
{
    netsnmp_assert(NULL != mib_wtpCPURTUsage_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPURTUsage_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpCPURTUsage mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpCPURTUsage_val_ptr) = raw_wtpCPURTUsage_val;

    return MFD_SUCCESS;
} /* wtpCPURTUsage_map */

/**
 * Extract the current value of the wtpCPURTUsage data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCPURTUsage_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpCPURTUsage_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, long * wtpCPURTUsage_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpCPURTUsage_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPURTUsage_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpCPURTUsage data.
 * set (* wtpCPURTUsage_val_ptr ) from rowreq_ctx->data
 */
    (* wtpCPURTUsage_val_ptr ) = rowreq_ctx->data.wtpCPURTUsage;

    return MFD_SUCCESS;
} /* wtpCPURTUsage_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpCPUPeakUsage
 * wtpCPUPeakUsage is subid 4 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.4
 * Description:
AP's CPU peak use rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpCPUPeakUsage.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpCPUPeakUsage_map(char **mib_wtpCPUPeakUsage_val_ptr_ptr, size_t *mib_wtpCPUPeakUsage_val_ptr_len_ptr, char *raw_wtpCPUPeakUsage_val_ptr, size_t raw_wtpCPUPeakUsage_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpCPUPeakUsage_val_ptr);
    netsnmp_assert((NULL != mib_wtpCPUPeakUsage_val_ptr_ptr) && (NULL != mib_wtpCPUPeakUsage_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPUPeakUsage_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpCPUPeakUsage non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpCPUPeakUsage_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpCPUPeakUsage_val_ptr_ptr) || (*mib_wtpCPUPeakUsage_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpCPUPeakUsage_val_ptr_ptr = realloc( *mib_wtpCPUPeakUsage_val_ptr_ptr, converted_len * sizeof(**mib_wtpCPUPeakUsage_val_ptr_ptr));
        if(NULL == *mib_wtpCPUPeakUsage_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpCPUPeakUsage_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpCPUPeakUsage_val_ptr_ptr, raw_wtpCPUPeakUsage_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpCPUPeakUsage_map */

/**
 * Extract the current value of the wtpCPUPeakUsage data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCPUPeakUsage_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpCPUPeakUsage_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpCPUPeakUsage.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpCPUPeakUsage_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpCPUPeakUsage_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpCPUPeakUsage_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, char **wtpCPUPeakUsage_val_ptr_ptr, size_t *wtpCPUPeakUsage_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpCPUPeakUsage_val_ptr_ptr) && (NULL != *wtpCPUPeakUsage_val_ptr_ptr));
   netsnmp_assert( NULL != wtpCPUPeakUsage_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPUPeakUsage_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpCPUPeakUsage data.
 * set (* wtpCPUPeakUsage_val_ptr_ptr ) and (* wtpCPUPeakUsage_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpCPUPeakUsage data
     */
    if ((NULL == (* wtpCPUPeakUsage_val_ptr_ptr )) ||
        ((* wtpCPUPeakUsage_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpCPUPeakUsage_len * sizeof((* wtpCPUPeakUsage_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpCPUPeakUsage data
         */
        (* wtpCPUPeakUsage_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpCPUPeakUsage_len * sizeof((* wtpCPUPeakUsage_val_ptr_ptr )[0]));
        if(NULL == (* wtpCPUPeakUsage_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpCPUPeakUsage_val_ptr_len_ptr ) = rowreq_ctx->data.wtpCPUPeakUsage_len * sizeof((* wtpCPUPeakUsage_val_ptr_ptr )[0]);
    memcpy( (* wtpCPUPeakUsage_val_ptr_ptr ), rowreq_ctx->data.wtpCPUPeakUsage, (* wtpCPUPeakUsage_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpCPUPeakUsage_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpCPUAvgUsage
 * wtpCPUAvgUsage is subid 5 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.5
 * Description:
AP's CPU average use rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpCPUAvgUsage.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpCPUAvgUsage_map(long *mib_wtpCPUAvgUsage_val_ptr, long raw_wtpCPUAvgUsage_val)
{
    netsnmp_assert(NULL != mib_wtpCPUAvgUsage_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPUAvgUsage_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpCPUAvgUsage mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpCPUAvgUsage_val_ptr) = raw_wtpCPUAvgUsage_val;

    return MFD_SUCCESS;
} /* wtpCPUAvgUsage_map */

/**
 * Extract the current value of the wtpCPUAvgUsage data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCPUAvgUsage_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpCPUAvgUsage_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, long * wtpCPUAvgUsage_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpCPUAvgUsage_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPUAvgUsage_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpCPUAvgUsage data.
 * set (* wtpCPUAvgUsage_val_ptr ) from rowreq_ctx->data
 */
    (* wtpCPUAvgUsage_val_ptr ) = rowreq_ctx->data.wtpCPUAvgUsage;

    return MFD_SUCCESS;
} /* wtpCPUAvgUsage_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpCPUusageThreshhd
 * wtpCPUusageThreshhd is subid 6 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.6
 * Description:
If CPU use rate too high,send trap.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpCPUusageThreshhd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpCPUusageThreshhd_map(long *mib_wtpCPUusageThreshhd_val_ptr, long raw_wtpCPUusageThreshhd_val)
{
    netsnmp_assert(NULL != mib_wtpCPUusageThreshhd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPUusageThreshhd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpCPUusageThreshhd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpCPUusageThreshhd_val_ptr) = raw_wtpCPUusageThreshhd_val;

    return MFD_SUCCESS;
} /* wtpCPUusageThreshhd_map */

/**
 * Extract the current value of the wtpCPUusageThreshhd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpCPUusageThreshhd_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpCPUusageThreshhd_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, long * wtpCPUusageThreshhd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpCPUusageThreshhd_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpCPUusageThreshhd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpCPUusageThreshhd data.
 * set (* wtpCPUusageThreshhd_val_ptr ) from rowreq_ctx->data
 */
    (* wtpCPUusageThreshhd_val_ptr ) = rowreq_ctx->data.wtpCPUusageThreshhd;

    return MFD_SUCCESS;
} /* wtpCPUusageThreshhd_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpMemoryType
 * wtpMemoryType is subid 7 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.7
 * Description:
The memory type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMemoryType.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMemoryType_map(char **mib_wtpMemoryType_val_ptr_ptr, size_t *mib_wtpMemoryType_val_ptr_len_ptr, char *raw_wtpMemoryType_val_ptr, size_t raw_wtpMemoryType_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMemoryType_val_ptr);
    netsnmp_assert((NULL != mib_wtpMemoryType_val_ptr_ptr) && (NULL != mib_wtpMemoryType_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemoryType_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMemoryType non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMemoryType_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMemoryType_val_ptr_ptr) || (*mib_wtpMemoryType_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMemoryType_val_ptr_ptr = realloc( *mib_wtpMemoryType_val_ptr_ptr, converted_len * sizeof(**mib_wtpMemoryType_val_ptr_ptr));
        if(NULL == *mib_wtpMemoryType_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMemoryType_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMemoryType_val_ptr_ptr, raw_wtpMemoryType_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMemoryType_map */

/**
 * Extract the current value of the wtpMemoryType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMemoryType_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpMemoryType_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpMemoryType.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpMemoryType_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpMemoryType_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpMemoryType_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, char **wtpMemoryType_val_ptr_ptr, size_t *wtpMemoryType_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpMemoryType_val_ptr_ptr) && (NULL != *wtpMemoryType_val_ptr_ptr));
   netsnmp_assert( NULL != wtpMemoryType_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemoryType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMemoryType data.
 * set (* wtpMemoryType_val_ptr_ptr ) and (* wtpMemoryType_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpMemoryType data
     */
    if ((NULL == (* wtpMemoryType_val_ptr_ptr )) ||
        ((* wtpMemoryType_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpMemoryType_len * sizeof((* wtpMemoryType_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpMemoryType data
         */
        (* wtpMemoryType_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpMemoryType_len * sizeof((* wtpMemoryType_val_ptr_ptr )[0]));
        if(NULL == (* wtpMemoryType_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpMemoryType_val_ptr_len_ptr ) = rowreq_ctx->data.wtpMemoryType_len * sizeof((* wtpMemoryType_val_ptr_ptr )[0]);
    memcpy( (* wtpMemoryType_val_ptr_ptr ), rowreq_ctx->data.wtpMemoryType, (* wtpMemoryType_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpMemoryType_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpMemoryCapacity
 * wtpMemoryCapacity is subid 8 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.8
 * Description:
The memory capacity.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is GAUGE (based on perltype GAUGE)
 * The net-snmp type is ASN_GAUGE. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMemoryCapacity.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMemoryCapacity_map(u_long *mib_wtpMemoryCapacity_val_ptr, u_long raw_wtpMemoryCapacity_val)
{
    netsnmp_assert(NULL != mib_wtpMemoryCapacity_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemoryCapacity_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMemoryCapacity mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMemoryCapacity_val_ptr) = raw_wtpMemoryCapacity_val;

    return MFD_SUCCESS;
} /* wtpMemoryCapacity_map */

/**
 * Extract the current value of the wtpMemoryCapacity data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMemoryCapacity_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMemoryCapacity_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpMemoryCapacity_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMemoryCapacity_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemoryCapacity_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMemoryCapacity data.
 * set (* wtpMemoryCapacity_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMemoryCapacity_val_ptr ) = rowreq_ctx->data.wtpMemoryCapacity;

    return MFD_SUCCESS;
} /* wtpMemoryCapacity_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpMemRTUsage
 * wtpMemRTUsage is subid 9 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.9
 * Description:
Memory current use rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMemRTUsage.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMemRTUsage_map(long *mib_wtpMemRTUsage_val_ptr, long raw_wtpMemRTUsage_val)
{
    netsnmp_assert(NULL != mib_wtpMemRTUsage_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemRTUsage_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMemRTUsage mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMemRTUsage_val_ptr) = raw_wtpMemRTUsage_val;

    return MFD_SUCCESS;
} /* wtpMemRTUsage_map */

/**
 * Extract the current value of the wtpMemRTUsage data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMemRTUsage_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMemRTUsage_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, long * wtpMemRTUsage_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMemRTUsage_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemRTUsage_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMemRTUsage data.
 * set (* wtpMemRTUsage_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMemRTUsage_val_ptr ) = rowreq_ctx->data.wtpMemRTUsage;

    return MFD_SUCCESS;
} /* wtpMemRTUsage_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpMemPeakUsage
 * wtpMemPeakUsage is subid 10 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.10
 * Description:
Memory peak use rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMemPeakUsage.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMemPeakUsage_map(char **mib_wtpMemPeakUsage_val_ptr_ptr, size_t *mib_wtpMemPeakUsage_val_ptr_len_ptr, char *raw_wtpMemPeakUsage_val_ptr, size_t raw_wtpMemPeakUsage_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMemPeakUsage_val_ptr);
    netsnmp_assert((NULL != mib_wtpMemPeakUsage_val_ptr_ptr) && (NULL != mib_wtpMemPeakUsage_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemPeakUsage_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMemPeakUsage non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMemPeakUsage_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMemPeakUsage_val_ptr_ptr) || (*mib_wtpMemPeakUsage_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMemPeakUsage_val_ptr_ptr = realloc( *mib_wtpMemPeakUsage_val_ptr_ptr, converted_len * sizeof(**mib_wtpMemPeakUsage_val_ptr_ptr));
        if(NULL == *mib_wtpMemPeakUsage_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMemPeakUsage_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMemPeakUsage_val_ptr_ptr, raw_wtpMemPeakUsage_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMemPeakUsage_map */

/**
 * Extract the current value of the wtpMemPeakUsage data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMemPeakUsage_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpMemPeakUsage_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpMemPeakUsage.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpMemPeakUsage_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpMemPeakUsage_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpMemPeakUsage_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, char **wtpMemPeakUsage_val_ptr_ptr, size_t *wtpMemPeakUsage_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpMemPeakUsage_val_ptr_ptr) && (NULL != *wtpMemPeakUsage_val_ptr_ptr));
   netsnmp_assert( NULL != wtpMemPeakUsage_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemPeakUsage_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMemPeakUsage data.
 * set (* wtpMemPeakUsage_val_ptr_ptr ) and (* wtpMemPeakUsage_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpMemPeakUsage data
     */
    if ((NULL == (* wtpMemPeakUsage_val_ptr_ptr )) ||
        ((* wtpMemPeakUsage_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpMemPeakUsage_len * sizeof((* wtpMemPeakUsage_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpMemPeakUsage data
         */
        (* wtpMemPeakUsage_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpMemPeakUsage_len * sizeof((* wtpMemPeakUsage_val_ptr_ptr )[0]));
        if(NULL == (* wtpMemPeakUsage_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpMemPeakUsage_val_ptr_len_ptr ) = rowreq_ctx->data.wtpMemPeakUsage_len * sizeof((* wtpMemPeakUsage_val_ptr_ptr )[0]);
    memcpy( (* wtpMemPeakUsage_val_ptr_ptr ), rowreq_ctx->data.wtpMemPeakUsage, (* wtpMemPeakUsage_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpMemPeakUsage_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpMemAvgUsage
 * wtpMemAvgUsage is subid 11 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.11
 * Description:
Memory average use rate.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMemAvgUsage.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMemAvgUsage_map(long *mib_wtpMemAvgUsage_val_ptr, long raw_wtpMemAvgUsage_val)
{
    netsnmp_assert(NULL != mib_wtpMemAvgUsage_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemAvgUsage_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMemAvgUsage mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMemAvgUsage_val_ptr) = raw_wtpMemAvgUsage_val;

    return MFD_SUCCESS;
} /* wtpMemAvgUsage_map */

/**
 * Extract the current value of the wtpMemAvgUsage data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMemAvgUsage_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMemAvgUsage_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, long * wtpMemAvgUsage_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMemAvgUsage_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemAvgUsage_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMemAvgUsage data.
 * set (* wtpMemAvgUsage_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMemAvgUsage_val_ptr ) = rowreq_ctx->data.wtpMemAvgUsage;

    return MFD_SUCCESS;
} /* wtpMemAvgUsage_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpMemUsageThreshhd
 * wtpMemUsageThreshhd is subid 12 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.12
 * Description:
If memory use rate too high ,send trap.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMemUsageThreshhd.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMemUsageThreshhd_map(long *mib_wtpMemUsageThreshhd_val_ptr, long raw_wtpMemUsageThreshhd_val)
{
    netsnmp_assert(NULL != mib_wtpMemUsageThreshhd_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemUsageThreshhd_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpMemUsageThreshhd mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpMemUsageThreshhd_val_ptr) = raw_wtpMemUsageThreshhd_val;

    return MFD_SUCCESS;
} /* wtpMemUsageThreshhd_map */

/**
 * Extract the current value of the wtpMemUsageThreshhd data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpMemUsageThreshhd_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpMemUsageThreshhd_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, long * wtpMemUsageThreshhd_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpMemUsageThreshhd_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpMemUsageThreshhd_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpMemUsageThreshhd data.
 * set (* wtpMemUsageThreshhd_val_ptr ) from rowreq_ctx->data
 */
    (* wtpMemUsageThreshhd_val_ptr ) = rowreq_ctx->data.wtpMemUsageThreshhd;

    return MFD_SUCCESS;
} /* wtpMemUsageThreshhd_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpFlashType
 * wtpFlashType is subid 13 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.13
 * Description:
Flash type.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpFlashType.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpFlashType_map(char **mib_wtpFlashType_val_ptr_ptr, size_t *mib_wtpFlashType_val_ptr_len_ptr, char *raw_wtpFlashType_val_ptr, size_t raw_wtpFlashType_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpFlashType_val_ptr);
    netsnmp_assert((NULL != mib_wtpFlashType_val_ptr_ptr) && (NULL != mib_wtpFlashType_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpFlashType_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpFlashType non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpFlashType_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpFlashType_val_ptr_ptr) || (*mib_wtpFlashType_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpFlashType_val_ptr_ptr = realloc( *mib_wtpFlashType_val_ptr_ptr, converted_len * sizeof(**mib_wtpFlashType_val_ptr_ptr));
        if(NULL == *mib_wtpFlashType_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpFlashType_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpFlashType_val_ptr_ptr, raw_wtpFlashType_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpFlashType_map */

/**
 * Extract the current value of the wtpFlashType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpFlashType_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpFlashType_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpFlashType.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpFlashType_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpFlashType_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpFlashType_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, char **wtpFlashType_val_ptr_ptr, size_t *wtpFlashType_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpFlashType_val_ptr_ptr) && (NULL != *wtpFlashType_val_ptr_ptr));
   netsnmp_assert( NULL != wtpFlashType_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpFlashType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpFlashType data.
 * set (* wtpFlashType_val_ptr_ptr ) and (* wtpFlashType_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpFlashType data
     */
    if ((NULL == (* wtpFlashType_val_ptr_ptr )) ||
        ((* wtpFlashType_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpFlashType_len * sizeof((* wtpFlashType_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpFlashType data
         */
        (* wtpFlashType_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpFlashType_len * sizeof((* wtpFlashType_val_ptr_ptr )[0]));
        if(NULL == (* wtpFlashType_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpFlashType_val_ptr_len_ptr ) = rowreq_ctx->data.wtpFlashType_len * sizeof((* wtpFlashType_val_ptr_ptr )[0]);
    memcpy( (* wtpFlashType_val_ptr_ptr ), rowreq_ctx->data.wtpFlashType, (* wtpFlashType_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpFlashType_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpFlashCapacity
 * wtpFlashCapacity is subid 14 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.14
 * Description:
Flash Capacity(units MB).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpFlashCapacity.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpFlashCapacity_map(long *mib_wtpFlashCapacity_val_ptr, long raw_wtpFlashCapacity_val)
{
    netsnmp_assert(NULL != mib_wtpFlashCapacity_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpFlashCapacity_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpFlashCapacity mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpFlashCapacity_val_ptr) = raw_wtpFlashCapacity_val;

    return MFD_SUCCESS;
} /* wtpFlashCapacity_map */

/**
 * Extract the current value of the wtpFlashCapacity data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpFlashCapacity_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpFlashCapacity_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, long * wtpFlashCapacity_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpFlashCapacity_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpFlashCapacity_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpFlashCapacity data.
 * set (* wtpFlashCapacity_val_ptr ) from rowreq_ctx->data
 */
    (* wtpFlashCapacity_val_ptr ) = rowreq_ctx->data.wtpFlashCapacity;

    return MFD_SUCCESS;
} /* wtpFlashCapacity_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpFlashRemain
 * wtpFlashRemain is subid 15 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.15
 * Description:
Flash surplus capacity(units KB).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpFlashRemain.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpFlashRemain_map(long *mib_wtpFlashRemain_val_ptr, long raw_wtpFlashRemain_val)
{
    netsnmp_assert(NULL != mib_wtpFlashRemain_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpFlashRemain_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpFlashRemain mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpFlashRemain_val_ptr) = raw_wtpFlashRemain_val;

    return MFD_SUCCESS;
} /* wtpFlashRemain_map */

/**
 * Extract the current value of the wtpFlashRemain data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpFlashRemain_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpFlashRemain_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, long * wtpFlashRemain_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpFlashRemain_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpFlashRemain_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpFlashRemain data.
 * set (* wtpFlashRemain_val_ptr ) from rowreq_ctx->data
 */
    (* wtpFlashRemain_val_ptr ) = rowreq_ctx->data.wtpFlashRemain;

    return MFD_SUCCESS;
} /* wtpFlashRemain_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpWayGetIP
 * wtpWayGetIP is subid 16 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.16
 * Description:
How ap get the IP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 4/8. Values:  DHCP(0), staticAllocation(1), PPPoE(2), DHCPv6(3), SLACC(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWayGetIP.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWayGetIP_map(u_long *mib_wtpWayGetIP_val_ptr, u_long raw_wtpWayGetIP_val)
{
    netsnmp_assert(NULL != mib_wtpWayGetIP_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpWayGetIP_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpWayGetIP enum mapping.
     * uses INTERNAL_* macros defined in the header files
     */
    switch(raw_wtpWayGetIP_val) {
        case INTERNAL_WTPWAYGETIP_STATICALLOCATION:
             *mib_wtpWayGetIP_val_ptr = WTPWAYGETIP_STATICALLOCATION;
             break;

        case INTERNAL_WTPWAYGETIP_DHCP:
             *mib_wtpWayGetIP_val_ptr = WTPWAYGETIP_DHCP;
             break;

        case INTERNAL_WTPWAYGETIP_PPPOE:
             *mib_wtpWayGetIP_val_ptr = WTPWAYGETIP_PPPOE;
             break;

        case INTERNAL_WTPWAYGETIP_DHCPV6:
             *mib_wtpWayGetIP_val_ptr = WTPWAYGETIP_DHCPV6;
             break;

        case INTERNAL_WTPWAYGETIP_SLACC:
             *mib_wtpWayGetIP_val_ptr = WTPWAYGETIP_SLACC;
             break;

             default:
                 snmp_log(LOG_ERR, "couldn't map value %ld for wtpWayGetIP\n", raw_wtpWayGetIP_val );
                 return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* wtpWayGetIP_map */

/**
 * Extract the current value of the wtpWayGetIP data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWayGetIP_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpWayGetIP_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpWayGetIP_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpWayGetIP_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpWayGetIP_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWayGetIP data.
 * set (* wtpWayGetIP_val_ptr ) from rowreq_ctx->data
 */
    (* wtpWayGetIP_val_ptr ) = rowreq_ctx->data.wtpWayGetIP;

    return MFD_SUCCESS;
} /* wtpWayGetIP_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpWorkTemp
 * wtpWorkTemp is subid 17 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.17
 * Description:
Working temperature.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpWorkTemp.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpWorkTemp_map(char **mib_wtpWorkTemp_val_ptr_ptr, size_t *mib_wtpWorkTemp_val_ptr_len_ptr, char *raw_wtpWorkTemp_val_ptr, size_t raw_wtpWorkTemp_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpWorkTemp_val_ptr);
    netsnmp_assert((NULL != mib_wtpWorkTemp_val_ptr_ptr) && (NULL != mib_wtpWorkTemp_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpWorkTemp_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpWorkTemp non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpWorkTemp_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpWorkTemp_val_ptr_ptr) || (*mib_wtpWorkTemp_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpWorkTemp_val_ptr_ptr = realloc( *mib_wtpWorkTemp_val_ptr_ptr, converted_len * sizeof(**mib_wtpWorkTemp_val_ptr_ptr));
        if(NULL == *mib_wtpWorkTemp_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpWorkTemp_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpWorkTemp_val_ptr_ptr, raw_wtpWorkTemp_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpWorkTemp_map */

/**
 * Extract the current value of the wtpWorkTemp data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpWorkTemp_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param wtpWorkTemp_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by wtpWorkTemp.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*wtpWorkTemp_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update wtpWorkTemp_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
wtpWorkTemp_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, char **wtpWorkTemp_val_ptr_ptr, size_t *wtpWorkTemp_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != wtpWorkTemp_val_ptr_ptr) && (NULL != *wtpWorkTemp_val_ptr_ptr));
   netsnmp_assert( NULL != wtpWorkTemp_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpWorkTemp_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpWorkTemp data.
 * set (* wtpWorkTemp_val_ptr_ptr ) and (* wtpWorkTemp_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for wtpWorkTemp data
     */
    if ((NULL == (* wtpWorkTemp_val_ptr_ptr )) ||
        ((* wtpWorkTemp_val_ptr_len_ptr ) < (rowreq_ctx->data.wtpWorkTemp_len * sizeof((* wtpWorkTemp_val_ptr_ptr )[0])))) {
        /*
         * allocate space for wtpWorkTemp data
         */
        (* wtpWorkTemp_val_ptr_ptr ) = malloc(rowreq_ctx->data.wtpWorkTemp_len * sizeof((* wtpWorkTemp_val_ptr_ptr )[0]));
        if(NULL == (* wtpWorkTemp_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* wtpWorkTemp_val_ptr_len_ptr ) = rowreq_ctx->data.wtpWorkTemp_len * sizeof((* wtpWorkTemp_val_ptr_ptr )[0]);
    memcpy( (* wtpWorkTemp_val_ptr_ptr ), rowreq_ctx->data.wtpWorkTemp, (* wtpWorkTemp_val_ptr_len_ptr ) );

    return MFD_SUCCESS;
} /* wtpWorkTemp_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11WtpDeviceInfoEntry.wtpIP
 * wtpIP is subid 18 of dot11WtpDeviceInfoEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2.1.18
 * Description:
AP IP address.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is IPADDR (based on perltype IPADDR)
 * The net-snmp type is ASN_IPADDRESS. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpIP.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpIP_map(u_long *mib_wtpIP_val_ptr, u_long raw_wtpIP_val)
{
    netsnmp_assert(NULL != mib_wtpIP_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpIP_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpIP mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpIP_val_ptr) = raw_wtpIP_val;

    return MFD_SUCCESS;
} /* wtpIP_map */

/**
 * Extract the current value of the wtpIP data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpIP_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpIP_get( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx, u_long * wtpIP_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpIP_val_ptr );


    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:wtpIP_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpIP data.
 * set (* wtpIP_val_ptr ) from rowreq_ctx->data
 */
    (* wtpIP_val_ptr ) = rowreq_ctx->data.wtpIP;

    return MFD_SUCCESS;
} /* wtpIP_get */



/** @} */
