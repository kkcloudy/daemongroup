/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11WtpDeviceInfoTable.h"
#include "dot11WtpDeviceInfoTable_data_access.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "ws_dcli_wlans.h"
#include "../mibs_public.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11WtpDeviceInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11WtpDeviceInfoTable is subid 2 of wtpGeneralInfo.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.1.2, length: 12
*/

/**
 * initialization for dot11WtpDeviceInfoTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11WtpDeviceInfoTable_reg
 *        Pointer to dot11WtpDeviceInfoTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11WtpDeviceInfoTable_init_data(dot11WtpDeviceInfoTable_registration_ptr dot11WtpDeviceInfoTable_reg)
{
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:dot11WtpDeviceInfoTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11WtpDeviceInfoTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11WtpDeviceInfoTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11WtpDeviceInfoTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:dot11WtpDeviceInfoTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11WtpDeviceInfoTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11WtpDeviceInfoTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11WTPDEVICEINFOTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11WtpDeviceInfoTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11WtpDeviceInfoTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11WtpDeviceInfoTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11WtpDeviceInfoTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11WtpDeviceInfoTable_cache_load\n");

    dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    //FILE *filep;
    //char line[MAX_LINE_SIZE];
    /*
     * temporary storage for index values
     */
        /*
         * wtpMacAddr(1)/Dot11BaseWtpIdTC/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
         */
        /** 128 - 1(entry) - 1(col) - 0(other indexes) = 112 */
   char   wtpMacAddr[20] = { 0 };
   size_t      wtpMacAddr_len = 0;

    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:dot11WtpDeviceInfoTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */
    //filep = fopen("/etc/dummy.conf", "r");
    //if(NULL ==  filep) {
      //  return MFD_RESOURCE_UNAVAILABLE;
    //}

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11WtpDeviceInfoTable container.
     * loop over your dot11WtpDeviceInfoTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
     
	char wtpCPUType[255];
	size_t wtpCPUType_len = 0;
	char cpu_ability[15] = { 0 };
	char wtpCPUProcessAbility[255] = { 0 };
	size_t wtpCPUProcessAbility_len = 0;
	char CPUPeakUsage[10] = { 0 };
	char wtpCPUPeakUsage[255] = { 0 };
	size_t wtpCPUPeakUsage_len = 0;
	char wtpMemoryType[255] = { 0 };
	size_t wtpMemoryType_len = 0;
	char MemPeakUsage[10] = { 0 };
	char wtpMemPeakUsage[255] = { 0 };
	size_t wtpMemPeakUsage_len = 0;
	char wtpFlashType[255] = { 0 };
	size_t wtpFlashType_len = 0;
	char WorkTemp[10] = { 0 };
	char wtpWorkTemp[255] = { 0 };
	size_t wtpWorkTemp_len = 0;
	int wtpIP = 0;

    snmpd_dbus_message *messageHead = NULL, *messageNode = NULL;
    
    snmp_log(LOG_DEBUG, "enter list_connection_call_dbus_method:show_all_wtp_device_information_cmd\n");
    messageHead = list_connection_call_dbus_method(show_all_wtp_device_information_cmd, SHOW_ALL_WTP_TABLE_METHOD);
	snmp_log(LOG_DEBUG, "exit list_connection_call_dbus_method:show_all_wtp_device_information_cmd,messageHead=%p\n", messageHead);

	if(messageHead)
	{
		for(messageNode = messageHead; NULL != messageNode; messageNode = messageNode->next)
		{
		    struct WtpDeviceInfo *WtpHead = messageNode->message;
		    if(WtpHead)
		    {
		        struct WtpDeviceInfo *q = NULL;
		        for(q = WtpHead->WtpDeviceInfo_list; NULL != q; q = q->next)
		        {
        			memset(wtpMacAddr,0,20);
					if(q->wtpMacAddr)
					{
						snprintf(wtpMacAddr,sizeof(wtpMacAddr)-1,
								 "%02X:%02X:%02X:%02X:%02X:%02X",
								 q->wtpMacAddr[0],q->wtpMacAddr[1],
								 q->wtpMacAddr[2],q->wtpMacAddr[3],
								 q->wtpMacAddr[4],q->wtpMacAddr[5]);
					}
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * get a line (skip blank lines)
     */
    //do {
      //  if (!fgets(line, sizeof(line), filep)) {
            /* we're done */
         //   fclose(filep);
           // filep = NULL;
        //}
    //} while (filep && (line[0] == '\n'));

    /*
     * check for end of data
     */
    //if(NULL == filep)
      //  break;

    /*
     * parse line into variables
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

        /*
         * TODO:352:M: |   |-> set indexes in new dot11WtpDeviceInfoTable rowreq context.
         */
        rowreq_ctx = dot11WtpDeviceInfoTable_allocate_rowreq_ctx();
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
            free_dbus_message_list(&messageHead, Free_show_all_wtp_device_information_cmd);
            return MFD_RESOURCE_UNAVAILABLE;
        }
		
		wtpMacAddr_len = strlen(wtpMacAddr);
        if(MFD_SUCCESS != dot11WtpDeviceInfoTable_indexes_set(rowreq_ctx
                               , wtpMacAddr, wtpMacAddr_len
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "dot11WtpDeviceInfoTable cache.\n");
            dot11WtpDeviceInfoTable_release_rowreq_ctx(rowreq_ctx);
            continue;
        }

        /*
         * TODO:352:r: |   |-> populate dot11WtpDeviceInfoTable data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for wtpCPUType
     * wtpCPUType(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpCPUType mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpCPUType_map(&rowreq_ctx->data.wtpCPUType, &rowreq_ctx->data.wtpCPUType_len,
     *                wtpCPUType, wtpCPUType_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpCPUType data
     */
    /*if ((NULL == rowreq_ctx->data.wtpCPUType) ||
        (rowreq_ctx->data.wtpCPUType_len < (wtpCPUType_len * sizeof(rowreq_ctx->data.wtpCPUType[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/
	
	memcpy(&(rowreq_ctx->data.parameter), &(messageNode->parameter), sizeof(dbus_parameter));
	rowreq_ctx->data.wtpCurrID = q->wtpCurrID;

    wtpCPUType_len = MIN(strlen(q->cpuType_str),sizeof(wtpCPUType)-1);
	memset(wtpCPUType,0,sizeof(wtpCPUType));
	memcpy(wtpCPUType,q->cpuType_str,wtpCPUType_len);
				
    rowreq_ctx->data.wtpCPUType_len = wtpCPUType_len * sizeof(rowreq_ctx->data.wtpCPUType[0]);
    memcpy( rowreq_ctx->data.wtpCPUType, wtpCPUType, rowreq_ctx->data.wtpCPUType_len );
    
    /*
     * setup/save data for wtpCPUProcessAbility
     * wtpCPUProcessAbility(2)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpCPUProcessAbility mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpCPUProcessAbility_map(&rowreq_ctx->data.wtpCPUProcessAbility, &rowreq_ctx->data.wtpCPUProcessAbility_len,
     *                wtpCPUProcessAbility, wtpCPUProcessAbility_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpCPUProcessAbility data
     */
    /*if ((NULL == rowreq_ctx->data.wtpCPUProcessAbility) ||
        (rowreq_ctx->data.wtpCPUProcessAbility_len < (wtpCPUProcessAbility_len * sizeof(rowreq_ctx->data.wtpCPUProcessAbility[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/

	memset(cpu_ability, 0, 15);	
	if(strncmp(q->cpuType_str," Atheros AR7100",strlen(" Atheros AR7100")) == 0)
		strncpy(cpu_ability,"680MHz",sizeof(cpu_ability)-1);
	else if(strncmp(q->cpuType_str," Atheros AR7240 (Python)",strlen(" Atheros AR7240 (Python)")) == 0)
		strncpy(cpu_ability,"400MHz",sizeof(cpu_ability)-1);
	else if(strncmp(q->cpuType_str," Atheros AR934x",strlen(" Atheros AR934x")) == 0)
		strncpy(cpu_ability,"560MHz",sizeof(cpu_ability)-1);
	else
		strncpy(cpu_ability,"600.33MHz",sizeof(cpu_ability)-1);
						
	wtpCPUProcessAbility_len = MIN(strlen(cpu_ability),sizeof(wtpCPUProcessAbility)-1);
	memset(wtpCPUProcessAbility,0,sizeof(wtpCPUProcessAbility));
	memcpy(wtpCPUProcessAbility,cpu_ability,wtpCPUProcessAbility_len);
	
    rowreq_ctx->data.wtpCPUProcessAbility_len = wtpCPUProcessAbility_len * sizeof(rowreq_ctx->data.wtpCPUProcessAbility[0]);
    memcpy( rowreq_ctx->data.wtpCPUProcessAbility, wtpCPUProcessAbility, rowreq_ctx->data.wtpCPUProcessAbility_len );
    
    /*
     * setup/save data for wtpCPURTUsage
     * wtpCPURTUsage(3)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpCPURTUsage mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpCPURTUsage = q->wtpCPURTUsage/100;
    
    /*
     * setup/save data for wtpCPUPeakUsage
     * wtpCPUPeakUsage(4)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpCPUPeakUsage mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpCPUPeakUsage_map(&rowreq_ctx->data.wtpCPUPeakUsage, &rowreq_ctx->data.wtpCPUPeakUsage_len,
     *                wtpCPUPeakUsage, wtpCPUPeakUsage_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpCPUPeakUsage data
     */
    /*if ((NULL == rowreq_ctx->data.wtpCPUPeakUsage) ||
        (rowreq_ctx->data.wtpCPUPeakUsage_len < (wtpCPUPeakUsage_len * sizeof(rowreq_ctx->data.wtpCPUPeakUsage[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/
	
	memset(CPUPeakUsage,0,10);
	snprintf(CPUPeakUsage,sizeof(CPUPeakUsage)-1,"%0.2f",q->wtpCPUPeakUsage/100.00);
	strncat(CPUPeakUsage,"%",sizeof(CPUPeakUsage)-strlen(CPUPeakUsage)-1);

	wtpCPUPeakUsage_len = MIN(strlen(CPUPeakUsage),sizeof(wtpCPUPeakUsage)-1);
	memset(wtpCPUPeakUsage,0,sizeof(wtpCPUPeakUsage));
	memcpy(wtpCPUPeakUsage,CPUPeakUsage,wtpCPUPeakUsage_len);
				
    rowreq_ctx->data.wtpCPUPeakUsage_len = wtpCPUPeakUsage_len * sizeof(rowreq_ctx->data.wtpCPUPeakUsage[0]);
    memcpy( rowreq_ctx->data.wtpCPUPeakUsage, wtpCPUPeakUsage, rowreq_ctx->data.wtpCPUPeakUsage_len );
    
    /*
     * setup/save data for wtpCPUAvgUsage
     * wtpCPUAvgUsage(5)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpCPUAvgUsage mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
	rowreq_ctx->data.wtpCPUAvgUsage = q->cpu_collect_average/100;
    
    /*
     * setup/save data for wtpCPUusageThreshhd
     * wtpCPUusageThreshhd(6)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpCPUusageThreshhd mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
	rowreq_ctx->data.wtpCPUusageThreshhd = q->wtpCPUusageThreshhd;
    
    /*
     * setup/save data for wtpMemoryType
     * wtpMemoryType(7)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpMemoryType mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpMemoryType_map(&rowreq_ctx->data.wtpMemoryType, &rowreq_ctx->data.wtpMemoryType_len,
     *                wtpMemoryType, wtpMemoryType_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpMemoryType data
     */
    /*if ((NULL == rowreq_ctx->data.wtpMemoryType) ||
        (rowreq_ctx->data.wtpMemoryType_len < (wtpMemoryType_len * sizeof(rowreq_ctx->data.wtpMemoryType[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/

	wtpMemoryType_len = MIN(strlen(q->memType_str),sizeof(wtpMemoryType)-1);
	memset(wtpMemoryType,0,sizeof(wtpMemoryType));
	memcpy(wtpMemoryType,q->memType_str,wtpMemoryType_len);
				
    rowreq_ctx->data.wtpMemoryType_len = wtpMemoryType_len * sizeof(rowreq_ctx->data.wtpMemoryType[0]);
    memcpy( rowreq_ctx->data.wtpMemoryType, wtpMemoryType, rowreq_ctx->data.wtpMemoryType_len );
    
    /*
     * setup/save data for wtpMemoryCapacity
     * wtpMemoryCapacity(8)/GAUGE/ASN_GAUGE/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpMemoryCapacity mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    //rowreq_ctx->data.wtpMemoryCapacity = q->wtpMemoryCapacity;
    rowreq_ctx->data.wtpMemoryCapacity = 64 * 1024;
    /*
     * setup/save data for wtpMemRTUsage
     * wtpMemRTUsage(9)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpMemRTUsage mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    rowreq_ctx->data.wtpMemRTUsage = q->wtpMemRTUsage;
    
    /*
     * setup/save data for wtpMemPeakUsage
     * wtpMemPeakUsage(10)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpMemPeakUsage mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpMemPeakUsage_map(&rowreq_ctx->data.wtpMemPeakUsage, &rowreq_ctx->data.wtpMemPeakUsage_len,
     *                wtpMemPeakUsage, wtpMemPeakUsage_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpMemPeakUsage data
     */
    /*if ((NULL == rowreq_ctx->data.wtpMemPeakUsage) ||
        (rowreq_ctx->data.wtpMemPeakUsage_len < (wtpMemPeakUsage_len * sizeof(rowreq_ctx->data.wtpMemPeakUsage[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/

	memset(MemPeakUsage,0,10);
	snprintf(MemPeakUsage,sizeof(MemPeakUsage)-1,"%d",q->wtpMemPeakUsage);						
	strncat(MemPeakUsage,"%",sizeof(MemPeakUsage)-strlen(MemPeakUsage)-1);

	wtpMemPeakUsage_len = MIN(strlen(MemPeakUsage),sizeof(wtpMemPeakUsage)-1);
	memset(wtpMemPeakUsage,0,sizeof(wtpMemPeakUsage));
	memcpy(wtpMemPeakUsage,MemPeakUsage,wtpMemPeakUsage_len);
				
    rowreq_ctx->data.wtpMemPeakUsage_len = wtpMemPeakUsage_len * sizeof(rowreq_ctx->data.wtpMemPeakUsage[0]);
    memcpy( rowreq_ctx->data.wtpMemPeakUsage, wtpMemPeakUsage, rowreq_ctx->data.wtpMemPeakUsage_len );
    
    /*
     * setup/save data for wtpMemAvgUsage
     * wtpMemAvgUsage(11)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpMemAvgUsage mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
	rowreq_ctx->data.wtpMemAvgUsage = q->wtpMemAvgUsage;
    
    /*
     * setup/save data for wtpMemUsageThreshhd
     * wtpMemUsageThreshhd(12)/INTEGER/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpMemUsageThreshhd mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
	rowreq_ctx->data.wtpMemUsageThreshhd = q->wtpMemUsageThreshhd;
    
    /*
     * setup/save data for wtpFlashType
     * wtpFlashType(13)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpFlashType mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpFlashType_map(&rowreq_ctx->data.wtpFlashType, &rowreq_ctx->data.wtpFlashType_len,
     *                wtpFlashType, wtpFlashType_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpFlashType data
     */
    /*if ((NULL == rowreq_ctx->data.wtpFlashType) ||
        (rowreq_ctx->data.wtpFlashType_len < (wtpFlashType_len * sizeof(rowreq_ctx->data.wtpFlashType[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/

	wtpFlashType_len = MIN(strlen(q->flashType_str),sizeof(wtpFlashType)-1);
	memset(wtpFlashType,0,sizeof(wtpFlashType));
	memcpy(wtpFlashType,q->flashType_str,wtpFlashType_len);
		
    rowreq_ctx->data.wtpFlashType_len = wtpFlashType_len * sizeof(rowreq_ctx->data.wtpFlashType[0]);
    memcpy( rowreq_ctx->data.wtpFlashType, wtpFlashType, rowreq_ctx->data.wtpFlashType_len );
    
    /*
     * setup/save data for wtpFlashCapacity
     * wtpFlashCapacity(14)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpFlashCapacity mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
	rowreq_ctx->data.wtpFlashCapacity = q->wtpFlashCapacity;
    
    /*
     * setup/save data for wtpFlashRemain
     * wtpFlashRemain(15)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpFlashRemain mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
	rowreq_ctx->data.wtpFlashRemain = q->wtpFlashRemain;
    
    /*
     * setup/save data for wtpWayGetIP
     * wtpWayGetIP(16)/INTEGER/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpWayGetIP mapping.
     * Map values between raw/native values and MIB values
     *
    * enums usually need mapping.
    */
    /*if(MFD_SUCCESS !=
       wtpWayGetIP_map(&rowreq_ctx->data.wtpWayGetIP, wtpWayGetIP )) {
        return MFD_ERROR;
    }*/

	rowreq_ctx->data.wtpWayGetIP = q->wtpWayGetIP;
    
    /*
     * setup/save data for wtpWorkTemp
     * wtpWorkTemp(17)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H
     */
    /*
     * TODO:246:r: |-> Define wtpWorkTemp mapping.
     * Map values between raw/native values and MIB values
     *
     * if(MFD_SUCCESS !=
     *    wtpWorkTemp_map(&rowreq_ctx->data.wtpWorkTemp, &rowreq_ctx->data.wtpWorkTemp_len,
     *                wtpWorkTemp, wtpWorkTemp_len, 0)) {
     *    return MFD_ERROR;
     * }
     */
    /*
     * make sure there is enough space for wtpWorkTemp data
     */
    /*if ((NULL == rowreq_ctx->data.wtpWorkTemp) ||
        (rowreq_ctx->data.wtpWorkTemp_len < (wtpWorkTemp_len * sizeof(rowreq_ctx->data.wtpWorkTemp[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }*/
	
	memset(WorkTemp,0,10);
	snprintf(WorkTemp,sizeof(WorkTemp)-1,"%d C",q->wtpWorkTemp);

	wtpWorkTemp_len = MIN(strlen(WorkTemp),sizeof(wtpWorkTemp)-1);
	memset(wtpWorkTemp,0,sizeof(wtpWorkTemp));
	memcpy(wtpWorkTemp,WorkTemp,wtpWorkTemp_len);
	
    rowreq_ctx->data.wtpWorkTemp_len = wtpWorkTemp_len * sizeof(rowreq_ctx->data.wtpWorkTemp[0]);
    memcpy( rowreq_ctx->data.wtpWorkTemp, wtpWorkTemp, rowreq_ctx->data.wtpWorkTemp_len );
    
    /*
     * setup/save data for wtpIP
     * wtpIP(18)/IPADDR/ASN_IPADDRESS/u_long(u_long)//l/A/w/e/r/d/h
     */
    /*
     * TODO:246:r: |-> Define wtpIP mapping.
     * Map values between raw/native values and MIB values
     *
     * Integer based value can usually just do a direct copy.
     */
    if(q->WtpIP)
    {
		INET_ATON(wtpIP,q->WtpIP);
    }
	
    rowreq_ctx->data.wtpIP = wtpIP;

   int	wtpStaIPV6Address_len= 0;
   char *p =NULL;
   if(q->wtp_ipv6_ip)
   {	
   	   char *ip=q->wtp_ipv6_ip;
   	   p= strtok(ip, "/");
	   wtpStaIPV6Address_len = MIN(strlen(p),sizeof(rowreq_ctx->data.wtpIpv6Address)-1);
	   rowreq_ctx->data.wtpIpv6Address_len = wtpStaIPV6Address_len* sizeof(rowreq_ctx->data.wtpIpv6Address[0]);
	   memcpy( rowreq_ctx->data.wtpIpv6Address, p, wtpStaIPV6Address_len );
   }

        /*
         * insert into table container
         */
    		        if(CONTAINER_INSERT(container, rowreq_ctx))
    		        {
    					dot11WtpDeviceInfoTable_release_rowreq_ctx(rowreq_ctx);
    		        }
    		        ++count;
    	        }
    		}
		}
		free_dbus_message_list(&messageHead, Free_show_all_wtp_device_information_cmd);
	}
	


    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    //if(NULL != filep)
      //  fclose(filep);
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11WtpDeviceInfoTable:dot11WtpDeviceInfoTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11WtpDeviceInfoTable_cache_load\n");
    return MFD_SUCCESS;
} /* dot11WtpDeviceInfoTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11WtpDeviceInfoTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:dot11WtpDeviceInfoTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11WtpDeviceInfoTable cache.
     */
} /* dot11WtpDeviceInfoTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11WtpDeviceInfoTable_row_prep( dot11WtpDeviceInfoTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11WtpDeviceInfoTable:dot11WtpDeviceInfoTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11WtpDeviceInfoTable_row_prep */

/** @} */
