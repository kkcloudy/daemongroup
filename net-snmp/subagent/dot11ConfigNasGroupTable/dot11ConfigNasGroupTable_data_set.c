/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11ConfigNasGroupTable.h"
#include "ws_sysinfo.h"
#include "ws_init_dbus.h"
#include "mibs_public.h"
#include "ws_log_conf.h"
#include "ws_stp.h"
#include "wcpss/asd/asd.h"
#include "wcpss/wid/WID.h"
#include "dbus/wcpss/dcli_wid_wtp.h"
#include "dbus/wcpss/dcli_wid_wlan.h"
#include "nm/app/eag/eag_errcode.h"
#include "nm/app/eag/eag_conf.h"
#include "nm/app/eag/eag_interface.h"
#include "ws_dcli_wlans.h"
#include "ws_dcli_ac.h"
#include "ws_log_conf.h"
#include "ws_dhcp_conf.h"
#include "autelanWtpGroup.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11ConfigNasGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * DOT11-AC-MIB::dot11ConfigNasGroupTable is subid 1 of dot11ConfigNasGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.21.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

static int eag_check_ip_format(const char *str)
{
	char *endptr = NULL;
	char* endptr1 = NULL;
	char c;
	int IP,i;
	
	c = str[0];
	if (c>='0'&&c<='9'){
		IP= strtoul(str,&endptr,10);
		if(IP < 0||IP > 255)
			return 1;
		else if(((IP < 10)&&((endptr - str) > 1))||((IP < 100)&&((endptr - str) > 2))||((IP < 256)&&((endptr - str) > 3)))
			return 1;
		for(i = 0; i < 3; i++){
			if(endptr[0] == '\0'||endptr[0] != '.')
				return 1;
			else{
				endptr1 = &endptr[1];
				IP= strtoul(&endptr[1],&endptr,10); 			
				if(IP < 0||IP > 255)
					return 1;				
				else if(((IP < 10)&&((endptr - endptr1) > 1))||((IP < 100)&&((endptr - endptr1) > 2))||((IP < 256)&&((endptr - endptr1) > 3)))
					return 1;
			}
		}
		if(endptr[0] == '\0' && IP >= 0)
			return 0;
		else
			return 1;
	}
	else
		return 1;		
}


/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * dot11ConfigNasGroupTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * dot11ConfigNasGroupTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigNasGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigNasGroupTable_undo_setup( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:dot11ConfigNasGroupTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup dot11ConfigNasGroupTable undo.
     * set up dot11ConfigNasGroupTable undo information, in preparation for a set.
     * Undo storage is in (* NasSyntaxisPoint_val_ptr )*
     */

    return rc;
} /* dot11ConfigNasGroupTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigNasGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
dot11ConfigNasGroupTable_undo( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:dot11ConfigNasGroupTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> dot11ConfigNasGroupTable undo.
     * dot11ConfigNasGroupTable undo information, in response to a failed set.
     * Undo storage is in (* NasSyntaxisPoint_val_ptr )*
     */

    return rc;
} /* dot11ConfigNasGroupTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigNasGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigNasGroupTable_undo_cleanup( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:dot11ConfigNasGroupTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup dot11ConfigNasGroupTable undo.
     * Undo storage is in (* NasSyntaxisPoint_val_ptr )*
     */

    return rc;
} /* dot11ConfigNasGroupTable_undo_cleanup */

static int
check_create_ready(dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx) {
    if(strlen(rowreq_ctx->data.NasName) && strlen(rowreq_ctx->data.NasKeyValue)) {
        return MFD_SUCCESS;
    }

    return MFD_ERROR;
}

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigNasGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigNasGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigNasGroupTable_commit( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:dot11ConfigNasGroupTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;
	
    if(RS_NOTREADY == rowreq_ctx->data.NasRowStatus && MFD_SUCCESS == check_create_ready(rowreq_ctx)) {
        rowreq_ctx->data.NasRowStatus = RS_NOTINSERVICE;
    }
    /*
     * commit dot11ConfigNasGroupTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_NASNAME_FLAG) {
       save_flags &= ~COLUMN_NASNAME_FLAG; /* clear NasName */
       /*
        * TODO:482:o: |-> commit column NasName.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigNasGroupTable column NasName commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NasName
             */
            rowreq_ctx->column_set_flags |= COLUMN_NASNAME_FLAG;
       }
    }

    if (save_flags & COLUMN_NASROWSTATUS_FLAG) {
        save_flags &= ~COLUMN_NASROWSTATUS_FLAG; /* clear NasRowStatus */		
        switch(rowreq_ctx->data.NasRowStatus) {
            case RS_ACTIVE:
                {
					void *connection = NULL;
				    if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3)) 
					{
						int ret = 0;
						struct nasid_map_t nasidmap;
						memset (&nasidmap,0,sizeof(struct nasid_map_t));							
						unsigned long keywd_1 = 0;
						unsigned long keywd_2 = 0;
						char *tmp_ip=NULL;
						char *ip=NULL;
						char ip_1[24]={0};
						char ip_2[24]={0};
						char *tmp = NULL;
						char *tmp_1 = NULL;
						
						switch(rowreq_ctx->data.NasKeyType)
						{						
							case NASID_KEYTYPE_WLANID:
								{
									tmp=(char *)rowreq_ctx->data.NasKeyValue;
									nasidmap.key_type = NASID_KEYTYPE_WLANID;
									if (NULL != (tmp_1 = strchr(tmp, '-'))) {
										keywd_1 = strtoul(tmp,NULL,10);
										tmp=tmp_1+1;
										keywd_2 = strtoul(tmp,NULL,10);
									} else {
										keywd_1 = strtoul(tmp,NULL,10);
										keywd_2 = keywd_1;
									}
									nasidmap.key.wlanidrange.id_begin = keywd_1;
									nasidmap.key.wlanidrange.id_end = keywd_2;
								}
								break;
							case NASID_KEYTYPE_VLANID:
								{
									tmp=(char *)rowreq_ctx->data.NasKeyValue;
									nasidmap.key_type = NASID_KEYTYPE_VLANID;
									if (NULL != (tmp_1 = strchr(tmp, '-'))) {
										keywd_1 = strtoul(tmp,NULL,10);
										tmp=tmp_1+1;
										keywd_2 = strtoul(tmp,NULL,10);
									} else {
										keywd_1 = strtoul(tmp,NULL,10);
										keywd_2 = keywd_1;
									}
									nasidmap.key.vlanidrange.id_begin = keywd_1;
									nasidmap.key.vlanidrange.id_end = keywd_2;
								}
								break;
							case NASID_KEYTYPE_WTPID:
								{
									tmp=(char *)rowreq_ctx->data.NasKeyValue;
									nasidmap.key_type = NASID_KEYTYPE_WTPID;
									if (NULL != (tmp_1 = strchr(tmp, '-'))) {
										keywd_1 = strtoul(tmp,NULL,10);
										tmp=tmp_1+1;
										keywd_2 = strtoul(tmp,NULL,10);
									} else {
										keywd_1 = strtoul(tmp,NULL,10);
										keywd_2 = keywd_1;
									}
									nasidmap.key.wtpidrange.id_begin = keywd_1;
									nasidmap.key.wtpidrange.id_end = keywd_2;
								}
								break;
							case NASID_KEYTYPE_IPRANGE:
								{
									memset (ip_1,0,sizeof(ip_1));
									memset (ip_2,0,sizeof(ip_2));
									nasidmap.key_type = NASID_KEYTYPE_IPRANGE;
									tmp_ip=rowreq_ctx->data.NasKeyValue;
									if((tmp_ip)&&(NULL==(ip = strchr(tmp_ip, '-'))))
									{
										strncpy(ip_1,tmp_ip,sizeof(ip_1)-1);
										if (eag_check_ip_format(ip_1) != 0)
										{
											rc = -1;
											break;
										}
										INET_ATON(nasidmap.key.iprange.ip_begin,ip_1);
										nasidmap.key.iprange.ip_end=nasidmap.key.iprange.ip_begin;
									}
									else if((tmp_ip)&&(ip))
									{
										strncpy(ip_1,tmp_ip,ip-tmp_ip);
										if (eag_check_ip_format(ip_1) != 0)
										{
											rc = -1;
											break;
										}
										tmp_ip=ip+1;
										strncpy(ip_2,tmp_ip,sizeof(ip_1)-1);
										if (eag_check_ip_format(ip_2) != 0)
										{
											rc = -1;
											break;
										}
										INET_ATON(nasidmap.key.iprange.ip_begin,ip_1);
										INET_ATON(nasidmap.key.iprange.ip_end,ip_2);
									}
								}
								break;
							case NASID_KEYTYPE_INTF:
								{
									nasidmap.key_type = NASID_KEYTYPE_INTF;
									strncpy(nasidmap.key.intf,rowreq_ctx->data.NasKeyValue,MAX_NASID_KEY_BUFF_LEN-1);
								}
								break;
							default:
								{
									rc = -1;
								}
								break;
						}
						strncpy(nasidmap.nasid,rowreq_ctx->data.NasName,MAX_NASID_LEN-1);
						nasidmap.conid = rowreq_ctx->data.NasSyntaxisPoint;
						ret = eag_add_nasid( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id, &nasidmap);
						snmp_log(LOG_DEBUG, "dot11ConfigNasGroupTable_commit: eag_add_nasid: ret = %d\n", ret);
						if(ret == EAG_RETURN_OK)
						{
							rc = MFD_SUCCESS;
                            break;
						}
						else if(EAG_ERR_DBUS_FAILED == ret) 
						{
							close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
						}
				    }    					    
                }
                rc = -1;
                break;

            case RS_CREATEANDWAIT:
                rowreq_ctx->data.NasRowStatus = RS_NOTREADY;
                rc = MFD_SUCCESS;
                break;
                
            case RS_DESTROY:  
                {					
					void *connection = NULL;
					if(SNMPD_DBUS_SUCCESS == get_instance_dbus_connection(rowreq_ctx->data.parameter, &connection, SNMPD_INSTANCE_MASTER_V3))
					{
						int ret = 0;
						struct nasid_map_t nasidmap;
						memset (&nasidmap,0,sizeof(struct nasid_map_t));
						unsigned long keywd_1 = 0;
						unsigned long keywd_2 = 0;
						char *tmp_ip=NULL;
						char *ip=NULL;
						char ip_1[24]={0};
						char ip_2[24]={0};
						char *tmp = NULL;
						char *tmp_1 = NULL;
						
						switch(rowreq_ctx->data.NasKeyType)
						{						
							case NASID_KEYTYPE_WLANID:
								{
									tmp=(char *)rowreq_ctx->data.NasKeyValue;
									nasidmap.key_type = NASID_KEYTYPE_WLANID;
									if (NULL != (tmp_1 = strchr(tmp, '-'))) {
										keywd_1 = strtoul(tmp,NULL,10);
										tmp=tmp_1+1;
										keywd_2 = strtoul(tmp,NULL,10);
									} else {
										keywd_1 = strtoul(tmp,NULL,10);
										keywd_2 = keywd_1;
									}
									nasidmap.key.wlanidrange.id_begin = keywd_1;
									nasidmap.key.wlanidrange.id_end = keywd_2;
								}
								break;
							case NASID_KEYTYPE_VLANID:
								{
									tmp=(char *)rowreq_ctx->data.NasKeyValue;
									nasidmap.key_type = NASID_KEYTYPE_VLANID;
									if (NULL != (tmp_1 = strchr(tmp, '-'))) {
										keywd_1 = strtoul(tmp,NULL,10);
										tmp=tmp_1+1;
										keywd_2 = strtoul(tmp,NULL,10);
									} else {
										keywd_1 = strtoul(tmp,NULL,10);
										keywd_2 = keywd_1;
									}
									nasidmap.key.vlanidrange.id_begin = keywd_1;
									nasidmap.key.vlanidrange.id_end = keywd_2;
								}
								break;
							case NASID_KEYTYPE_WTPID:
								{
									tmp=(char *)rowreq_ctx->data.NasKeyValue;
									nasidmap.key_type = NASID_KEYTYPE_WTPID;
									if (NULL != (tmp_1 = strchr(tmp, '-'))) {
										keywd_1 = strtoul(tmp,NULL,10);
										tmp=tmp_1+1;
										keywd_2 = strtoul(tmp,NULL,10);
									} else {
										keywd_1 = strtoul(tmp,NULL,10);
										keywd_2 = keywd_1;
									}
									nasidmap.key.wtpidrange.id_begin = keywd_1;
									nasidmap.key.wtpidrange.id_end = keywd_2;
								}
								break;
							case NASID_KEYTYPE_IPRANGE:
								{
									memset (ip_1,0,sizeof(ip_1));
									memset (ip_2,0,sizeof(ip_2));
									nasidmap.key_type = NASID_KEYTYPE_IPRANGE;
									tmp_ip=rowreq_ctx->data.NasKeyValue;
									if((tmp_ip)&&(NULL==(ip = strchr(tmp_ip, '-'))))
									{
										strncpy(ip_1,tmp_ip,sizeof(ip_1)-1);
										if (eag_check_ip_format(ip_1) != 0)
										{
											rc = -1;
											break;
										}
										INET_ATON(nasidmap.key.iprange.ip_begin,ip_1);
										nasidmap.key.iprange.ip_end=nasidmap.key.iprange.ip_begin;
									}
									else if((tmp_ip)&&(ip))
									{
										strncpy(ip_1,tmp_ip,ip-tmp_ip);
										if (eag_check_ip_format(ip_1) != 0)
										{
											rc = -1;
											break;
										}
										tmp_ip=ip+1;
										strncpy(ip_2,tmp_ip,24-1);
										if (eag_check_ip_format(ip_2) != 0)
										{
											rc = -1;
											break;
										}
										INET_ATON(nasidmap.key.iprange.ip_begin,ip_1);
										INET_ATON(nasidmap.key.iprange.ip_end,ip_2);
									}
								}
								break;
							case NASID_KEYTYPE_INTF:
								{
									nasidmap.key_type = NASID_KEYTYPE_INTF;
									strncpy(nasidmap.key.intf,rowreq_ctx->data.NasKeyValue,MAX_NASID_KEY_BUFF_LEN-1);
								}
								break;
							default:
								{
									rc = -1;
								}
								break;
						}
						
						ret = eag_del_nasid( connection, rowreq_ctx->data.parameter.local_id, rowreq_ctx->data.parameter.instance_id, &nasidmap);
						snmp_log(LOG_DEBUG, "dot11ConfigNasGroupTable_commit: eag_del_nasid: ret = %d\n", ret);
						if(ret == EAG_RETURN_OK)
						{
							rc = MFD_SUCCESS;
                            break;
						}
						else if(EAG_ERR_DBUS_FAILED == ret) 
						{
                            close_slot_dbus_connection(rowreq_ctx->data.parameter.slot_id);
                        }
					}
                }
                rowreq_ctx->rowreq_flags &= ~MFD_ROW_DELETED;
                rc = -1;
                break;
                
            default:
                break;
        }
       /*
        * TODO:482:o: |-> commit column NasRowStatus.
        */
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigNasGroupTable column NasRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NasRowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_NASROWSTATUS_FLAG;
       }
    }

    if (save_flags & COLUMN_NASKEYTYPE_FLAG) {
       save_flags &= ~COLUMN_NASKEYTYPE_FLAG; /* clear NasKeyType */
       /*
        * TODO:482:o: |-> commit column NasKeyType.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigNasGroupTable column NasKeyType commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NasKeyType
             */
            rowreq_ctx->column_set_flags |= COLUMN_NASKEYTYPE_FLAG;
       }
    }

    if (save_flags & COLUMN_NASKEYVALUE_FLAG) {
       save_flags &= ~COLUMN_NASKEYVALUE_FLAG; /* clear NasKeyValue */
       /*
        * TODO:482:o: |-> commit column NasKeyValue.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigNasGroupTable column NasKeyValue commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NasKeyValue
             */
            rowreq_ctx->column_set_flags |= COLUMN_NASKEYVALUE_FLAG;
       }
    }

    if (save_flags & COLUMN_NASSYNTAXISPOINT_FLAG) {
       save_flags &= ~COLUMN_NASSYNTAXISPOINT_FLAG; /* clear NasSyntaxisPoint */
       /*
        * TODO:482:o: |-> commit column NasSyntaxisPoint.
        */
       rc = MFD_SUCCESS;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"dot11ConfigNasGroupTable column NasSyntaxisPoint commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo NasSyntaxisPoint
             */
            rowreq_ctx->column_set_flags |= COLUMN_NASSYNTAXISPOINT_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* dot11ConfigNasGroupTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigNasGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param dot11ConfigNasGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
dot11ConfigNasGroupTable_undo_commit( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:dot11ConfigNasGroupTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo dot11ConfigNasGroupTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* dot11ConfigNasGroupTable_undo_commit */

/*
 * TODO:440:M: Implement dot11ConfigNasGroupTable node value checks.
 * TODO:450:M: Implement dot11ConfigNasGroupTable undo functions.
 * TODO:460:M: Implement dot11ConfigNasGroupTable set functions.
 * TODO:480:M: Implement dot11ConfigNasGroupTable commit functions.
 */
/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigNasGroupEntry.NasName
 * NasName is subid 2 of dot11ConfigNasGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.21.1.1.2
 * Description:
NasID   name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NasName_val_ptr
 *        A char containing the new value.
 * @param NasName_val_ptr_len
 *        The size (in bytes) of the data pointed to by NasName_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigNasGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.NasName).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NasName_check_value( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx, char *NasName_val_ptr,  size_t NasName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasName_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NasName_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid NasName value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NasRowStatus || RS_NOTREADY == rowreq_ctx->data.NasRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NasName value not illegal */
} /* NasName_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigNasGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigNasGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NasName_undo_setup( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasName_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NasName undo.
     */
    /*
     * copy NasName and NasName_len data
     * set rowreq_ctx->undo->NasName from rowreq_ctx->data.NasName
     */
    memcpy( rowreq_ctx->undo->NasName, rowreq_ctx->data.NasName,
            (rowreq_ctx->data.NasName_len * sizeof(rowreq_ctx->undo->NasName[0])));
    rowreq_ctx->undo->NasName_len = rowreq_ctx->data.NasName_len;


    return MFD_SUCCESS;
} /* NasName_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NasName_val_ptr
 *        A char containing the new value.
 * @param NasName_val_ptr_len
 *        The size (in bytes) of the data pointed to by NasName_val_ptr
 */
int
NasName_set( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx, char *NasName_val_ptr,  size_t NasName_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasName_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NasName_val_ptr);

    /*
     * TODO:461:M: |-> Set NasName value.
     * set NasName value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.NasName, NasName_val_ptr, NasName_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.NasName_len = NasName_val_ptr_len / sizeof(NasName_val_ptr[0]);

    return MFD_SUCCESS;
} /* NasName_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NasName_undo( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasName_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NasName undo.
     */
    /*
     * copy NasName and NasName_len data
     * set rowreq_ctx->data.NasName from rowreq_ctx->undo->NasName
     */
    memcpy( rowreq_ctx->data.NasName, rowreq_ctx->undo->NasName,
            (rowreq_ctx->undo->NasName_len * sizeof(rowreq_ctx->data.NasName[0])));
    rowreq_ctx->data.NasName_len = rowreq_ctx->undo->NasName_len;

    
    return MFD_SUCCESS;
} /* NasName_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigNasGroupEntry.NasRowStatus
 * NasRowStatus is subid 3 of dot11ConfigNasGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.21.1.1.3
 * Description:
This variable is used to create, modify, and/or delete a row
			        	 in this table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NasRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigNasGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NasRowStatus_check_value( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx, u_long NasRowStatus_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NasRowStatus value.
     */

    return MFD_SUCCESS; /* NasRowStatus value not illegal */
} /* NasRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigNasGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigNasGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NasRowStatus_undo_setup( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NasRowStatus undo.
     */
    /*
     * copy NasRowStatus data
     * set rowreq_ctx->undo->NasRowStatus from rowreq_ctx->data.NasRowStatus
     */
    rowreq_ctx->undo->NasRowStatus = rowreq_ctx->data.NasRowStatus;


    return MFD_SUCCESS;
} /* NasRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NasRowStatus_val
 *        A long containing the new value.
 */
int
NasRowStatus_set( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx, u_long NasRowStatus_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NasRowStatus value.
     * set NasRowStatus value in rowreq_ctx->data
     */
    
    int ret = MFD_SUCCESS;
    
    switch(NasRowStatus_val) {
    
        case RS_ACTIVE:         
        case RS_DESTROY:
            rowreq_ctx->data.NasRowStatus = NasRowStatus_val;
            ret = MFD_SUCCESS;
            break;
    
        case RS_NOTINSERVICE:
        case RS_NOTREADY:
        case RS_CREATEANDGO:
            ret = MFD_ERROR;
            break;
    
        case RS_CREATEANDWAIT:
            if(RS_NONEXISTENT == rowreq_ctx->data.NasRowStatus) {
                global_to_local_ID(&(rowreq_ctx->data.parameter), 
                                    rowreq_ctx->tbl_idx.NasID, 
                                    WIRELESS_MAX_NUM);
                rowreq_ctx->data.NasRowStatus = NasRowStatus_val;
                ret = MFD_SUCCESS;
            }
            else {
                ret = MFD_ERROR;
            }
            break;
    
        default:
            snmp_log(LOG_WARNING, "unknow %s in NasRowStatus_set", NasRowStatus_val);
            ret = MFD_ERROR;
            break;
    }

    return ret;
    
} /* NasRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NasRowStatus_undo( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NasRowStatus undo.
     */
    /*
     * copy NasRowStatus data
     * set rowreq_ctx->data.NasRowStatus from rowreq_ctx->undo->NasRowStatus
     */
    rowreq_ctx->data.NasRowStatus = rowreq_ctx->undo->NasRowStatus;

    
    return MFD_SUCCESS;
} /* NasRowStatus_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigNasGroupEntry.NasKeyType
 * NasKeyType is subid 4 of dot11ConfigNasGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.21.1.1.4
 * Description:
NasID KeyType.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 5/8. Values:  wlanid(0), vlanid(1), wtpid(2), iprange(3), interface(4)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NasKeyType_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigNasGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  wlanid(0), vlanid(1), wtpid(2), iprange(3), interface(4)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NasKeyType_check_value( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx, u_long NasKeyType_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasKeyType_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NasKeyType value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NasRowStatus || RS_NOTREADY == rowreq_ctx->data.NasRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NasKeyType value not illegal */
} /* NasKeyType_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigNasGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigNasGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NasKeyType_undo_setup( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasKeyType_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NasKeyType undo.
     */
    /*
     * copy NasKeyType data
     * set rowreq_ctx->undo->NasKeyType from rowreq_ctx->data.NasKeyType
     */
    rowreq_ctx->undo->NasKeyType = rowreq_ctx->data.NasKeyType;


    return MFD_SUCCESS;
} /* NasKeyType_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NasKeyType_val
 *        A long containing the new value.
 */
int
NasKeyType_set( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx, u_long NasKeyType_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasKeyType_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NasKeyType value.
     * set NasKeyType value in rowreq_ctx->data
     */
    rowreq_ctx->data.NasKeyType = NasKeyType_val;

    return MFD_SUCCESS;
} /* NasKeyType_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NasKeyType_undo( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasKeyType_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NasKeyType undo.
     */
    /*
     * copy NasKeyType data
     * set rowreq_ctx->data.NasKeyType from rowreq_ctx->undo->NasKeyType
     */
    rowreq_ctx->data.NasKeyType = rowreq_ctx->undo->NasKeyType;

    
    return MFD_SUCCESS;
} /* NasKeyType_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigNasGroupEntry.NasKeyValue
 * NasKeyValue is subid 5 of dot11ConfigNasGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.21.1.1.5
 * Description:
NasID KeyValue,key value depends on key type.If key type is iprange(3),
					the form of key value should be A.B.C.D or A1.B1.C1.D1-A2.B2.C3.D2
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NasKeyValue_val_ptr
 *        A char containing the new value.
 * @param NasKeyValue_val_ptr_len
 *        The size (in bytes) of the data pointed to by NasKeyValue_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigNasGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.NasKeyValue).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NasKeyValue_check_value( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx, char *NasKeyValue_val_ptr,  size_t NasKeyValue_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasKeyValue_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NasKeyValue_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid NasKeyValue value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NasRowStatus || RS_NOTREADY == rowreq_ctx->data.NasRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NasKeyValue value not illegal */
} /* NasKeyValue_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigNasGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigNasGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NasKeyValue_undo_setup( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasKeyValue_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NasKeyValue undo.
     */
    /*
     * copy NasKeyValue and NasKeyValue_len data
     * set rowreq_ctx->undo->NasKeyValue from rowreq_ctx->data.NasKeyValue
     */
    memcpy( rowreq_ctx->undo->NasKeyValue, rowreq_ctx->data.NasKeyValue,
            (rowreq_ctx->data.NasKeyValue_len * sizeof(rowreq_ctx->undo->NasKeyValue[0])));
    rowreq_ctx->undo->NasKeyValue_len = rowreq_ctx->data.NasKeyValue_len;


    return MFD_SUCCESS;
} /* NasKeyValue_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NasKeyValue_val_ptr
 *        A char containing the new value.
 * @param NasKeyValue_val_ptr_len
 *        The size (in bytes) of the data pointed to by NasKeyValue_val_ptr
 */
int
NasKeyValue_set( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx, char *NasKeyValue_val_ptr,  size_t NasKeyValue_val_ptr_len )
{

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasKeyValue_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != NasKeyValue_val_ptr);

    /*
     * TODO:461:M: |-> Set NasKeyValue value.
     * set NasKeyValue value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.NasKeyValue, NasKeyValue_val_ptr, NasKeyValue_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.NasKeyValue_len = NasKeyValue_val_ptr_len / sizeof(NasKeyValue_val_ptr[0]);

    return MFD_SUCCESS;
} /* NasKeyValue_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NasKeyValue_undo( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasKeyValue_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NasKeyValue undo.
     */
    /*
     * copy NasKeyValue and NasKeyValue_len data
     * set rowreq_ctx->data.NasKeyValue from rowreq_ctx->undo->NasKeyValue
     */
    memcpy( rowreq_ctx->data.NasKeyValue, rowreq_ctx->undo->NasKeyValue,
            (rowreq_ctx->undo->NasKeyValue_len * sizeof(rowreq_ctx->data.NasKeyValue[0])));
    rowreq_ctx->data.NasKeyValue_len = rowreq_ctx->undo->NasKeyValue_len;

    
    return MFD_SUCCESS;
} /* NasKeyValue_undo */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigNasGroupEntry.NasSyntaxisPoint
 * NasSyntaxisPoint is subid 6 of dot11ConfigNasGroupEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.31656.6.1.2.21.1.1.6
 * Description:
NasID   Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 99;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param NasSyntaxisPoint_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * dot11ConfigNasGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - 99
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
NasSyntaxisPoint_check_value( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx, long NasSyntaxisPoint_val)
{
    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasSyntaxisPoint_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid NasSyntaxisPoint value.
     */

    if(RS_NOTINSERVICE == rowreq_ctx->data.NasRowStatus || RS_NOTREADY == rowreq_ctx->data.NasRowStatus)
    	return MFD_SUCCESS;
	else     
        return MFD_ERROR; /* NasSyntaxisPoint value not illegal */
} /* NasSyntaxisPoint_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (dot11ConfigNasGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * dot11ConfigNasGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
NasSyntaxisPoint_undo_setup( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasSyntaxisPoint_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup NasSyntaxisPoint undo.
     */
    /*
     * copy NasSyntaxisPoint data
     * set rowreq_ctx->undo->NasSyntaxisPoint from rowreq_ctx->data.NasSyntaxisPoint
     */
    rowreq_ctx->undo->NasSyntaxisPoint = rowreq_ctx->data.NasSyntaxisPoint;


    return MFD_SUCCESS;
} /* NasSyntaxisPoint_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param NasSyntaxisPoint_val
 *        A long containing the new value.
 */
int
NasSyntaxisPoint_set( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx, long NasSyntaxisPoint_val )
{

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasSyntaxisPoint_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set NasSyntaxisPoint value.
     * set NasSyntaxisPoint value in rowreq_ctx->data
     */
    rowreq_ctx->data.NasSyntaxisPoint = NasSyntaxisPoint_val;

    return MFD_SUCCESS;
} /* NasSyntaxisPoint_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
NasSyntaxisPoint_undo( dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:dot11ConfigNasGroupTable:NasSyntaxisPoint_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up NasSyntaxisPoint undo.
     */
    /*
     * copy NasSyntaxisPoint data
     * set rowreq_ctx->data.NasSyntaxisPoint from rowreq_ctx->undo->NasSyntaxisPoint
     */
    rowreq_ctx->data.NasSyntaxisPoint = rowreq_ctx->undo->NasSyntaxisPoint;

    
    return MFD_SUCCESS;
} /* NasSyntaxisPoint_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * dot11ConfigNasGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-dot11ConfigNasGroupTable if you don't have dependencies)
 */
int
dot11ConfigNasGroupTable_check_dependencies(dot11ConfigNasGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:dot11ConfigNasGroupTable:dot11ConfigNasGroupTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check dot11ConfigNasGroupTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    /*
     * check RowStatus dependencies
     */
    if (rowreq_ctx->column_set_flags & COLUMN_NASROWSTATUS_FLAG) {
        /*
         * check for valid RowStatus transition (old, new)
         * (Note: move transition check to 
         *  to catch errors earlier)
         */
        rc = check_rowstatus_transition( rowreq_ctx->undo->NasRowStatus,
                                         rowreq_ctx->data.NasRowStatus );
        if (MFD_SUCCESS != rc)
            return rc;
        
        /*
         * row creation requirements
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            if (ROWSTATUS_DESTROY ==  rowreq_ctx->data.NasRowStatus) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;
            }
            else if (ROWSTATUS_CREATEANDGO ==  rowreq_ctx->data.NasRowStatus) {
                if ((rowreq_ctx->column_set_flags & DOT11CONFIGNASGROUPTABLE_REQUIRED_COLS)
                    != DOT11CONFIGNASGROUPTABLE_REQUIRED_COLS) {
                    DEBUGMSGTL(("dot11ConfigNasGroupTable",
                                "required columns missing (0x%0x != 0x%0x)\n",
                                rowreq_ctx->column_set_flags, DOT11CONFIGNASGROUPTABLE_REQUIRED_COLS));
                    return MFD_CANNOT_CREATE_NOW;
                }
                rowreq_ctx->data.NasRowStatus = ROWSTATUS_ACTIVE;
            }
        } /* row creation */
        else {
            /*
             * row change requirements
             */
            /*
             * don't allow a destroy if any other value was changed, since
             * that might call data access routines with bad info.
             *
             * you may or may not require the row be notInService before it
             * can be destroyed.
             */
            if (ROWSTATUS_DESTROY == rowreq_ctx->data.NasRowStatus) {
                if (rowreq_ctx->column_set_flags & ~COLUMN_NASROWSTATUS_FLAG) {
                    DEBUGMSGTL(("dot11ConfigNasGroupTable",
                                "destroy must be only varbind for row\n"));
                    return MFD_NOT_VALID_NOW;
                }
                rowreq_ctx->rowreq_flags |= MFD_ROW_DELETED;

            } /* row destroy */
        } /* row change */
    }
    else {
        /*
         * must have row status to create a row
         */
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            DEBUGMSGTL(("dot11ConfigNasGroupTable",
                        "must use RowStatus to create rows\n"));
            return MFD_CANNOT_CREATE_NOW;
        }
    } /* row status not set */

    if ( MFD_SUCCESS != rc )
        return rc;

    return rc;
} /* dot11ConfigNasGroupTable_check_dependencies */

/** @} */
