/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.12 $ of : mfd-data-access.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

#include "ws_init_dbus.h"
#include "autelanWtpGroup.h"
#include "ws_dhcp_conf.h"
#include "ws_log_conf.h"
#include "ws_dcli_dhcp.h"
/* include our parent header */
#include "dot11DHCPIpAddressPoolTable.h"

#include "dot11DHCPIpAddressPoolTable_data_access.h"

/** @defgroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11DHCPIpAddressPoolTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11DHCPIpAddressPoolTable is subid 4 of dot11Service.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.2.6.4, length: 12
*/

/**
 * initialization for dot11DHCPIpAddressPoolTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param dot11DHCPIpAddressPoolTable_reg
 *        Pointer to dot11DHCPIpAddressPoolTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
dot11DHCPIpAddressPoolTable_init_data(dot11DHCPIpAddressPoolTable_registration_ptr dot11DHCPIpAddressPoolTable_reg)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpAddressPoolTable:dot11DHCPIpAddressPoolTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize dot11DHCPIpAddressPoolTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* dot11DHCPIpAddressPoolTable_init_data */

/**
 * container-cached overview
 *
 */

/***********************************************************************
 *
 * cache
 *
 ***********************************************************************/
/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
dot11DHCPIpAddressPoolTable_container_init(netsnmp_container **container_ptr_ptr,
                        netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpAddressPoolTable:dot11DHCPIpAddressPoolTable_container_init","called\n"));
    
    if((NULL == cache) || (NULL == container_ptr_ptr)) {
        snmp_log(LOG_ERR,"bad params to dot11DHCPIpAddressPoolTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    /*
     * TODO:345:A: Set up dot11DHCPIpAddressPoolTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = DOT11DHCPIPADDRESSPOOLTABLE_CACHE_TIMEOUT; /* seconds */
} /* dot11DHCPIpAddressPoolTable_container_init */

/**
 * load cache data
 *
 * TODO:350:M: Implement dot11DHCPIpAddressPoolTable cache load
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to cache the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the cache, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  dot11DHCPIpAddressPoolTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
dot11DHCPIpAddressPoolTable_cache_load(netsnmp_container *container)
{
	snmp_log(LOG_DEBUG, "enter dot11DHCPIpAddressPoolTable_cache_load\n");
	
    dot11DHCPIpAddressPoolTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;
    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    /*
     * temporary storage for index values
     */

    DEBUGMSGTL(("verbose:dot11DHCPIpAddressPoolTable:dot11DHCPIpAddressPoolTable_cache_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * open our data file.
     */

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the dot11DHCPIpAddressPoolTable container.
     * loop over your dot11DHCPIpAddressPoolTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    instance_parameter *paraHead = NULL, *paraNode = NULL;
    list_instance_parameter(&paraHead, SNMPD_SLOT_CONNECT);

    for(paraNode = paraHead; paraNode; paraNode = paraNode->next) {
        
    	struct dhcp_pool_show_st poolshow, *q = NULL;
    	memset(&poolshow,0,sizeof(struct dhcp_pool_show_st));
    	struct dhcp_sub_show_st *pq = NULL;

    	unsigned int mode = 0, index = 0, countn = 0;
    	
        u_long  ipPoolIndex = (paraNode->parameter.slot_id - 1) * WIRELESS_MAX_NUM;
    	
    	int cflag = -1;        
    	
    	long IPAddpoolStartAdd = 0;
    	long IPAddpoolEndAdd = 0;
    	long GatewayAddress = 0;
    	int IPAddLeasePeriod = 0;
    	
    	char tmp2[32] = { 0 }; 
    	char startip[32] = {0};
    	char endip[32] = {0};
    	char gwip[32] = {0};
    	char maskip[32] = {0};

    	snmp_log(LOG_DEBUG, "enter slot %d ccgi_show_ip_pool\n", paraNode->parameter.slot_id);
    	cflag = ccgi_show_ip_pool(mode, index, &poolshow, &countn, paraNode->parameter.slot_id);
    	snmp_log(LOG_DEBUG, "exit slot %d ccgi_show_ip_pool,cflag=%d\n", paraNode->parameter.slot_id, cflag);   


    	if(1 == cflag)
    	{
    		for(q = poolshow.next; NULL!=q; q = q->next)
    		{
    			for(pq = q->sub_show.next; NULL != pq; pq = pq->next)
    			{
					ipPoolIndex++;
					
					memset(tmp2, 0, 32);
					memset(startip,0,32);
					strncpy(startip,pq->iplow ? ip_long2string(pq->iplow, tmp2) : "0.0.0.0",sizeof(startip)-1);

					memset(tmp2, 0, 32);				
					memset(endip,0,32);
					strncpy(endip,pq->iphigh ? ip_long2string(pq->iphigh, tmp2) : "0.0.0.0",sizeof(endip)-1);
					
					memset(tmp2,0,32);
					memset(gwip,0,32);
					strncpy(gwip,q->routers ? ip_long2string(q->routers, tmp2) : "0.0.0.0",sizeof(gwip)-1);	

					memset(tmp2, 0, 32);
	                memset(maskip,0,32);
					strncpy(maskip,pq->mask ? ip_long2string(pq->mask, tmp2) : "0.0.0.0",sizeof(maskip)-1);


					IPAddLeasePeriod = (q->defaulttime ? q->defaulttime : 86400);
	   				INET_ATON(IPAddpoolStartAdd,startip);
					INET_ATON(IPAddpoolEndAdd,endip);
					INET_ATON(GatewayAddress,gwip);
					
        	        rowreq_ctx = dot11DHCPIpAddressPoolTable_allocate_rowreq_ctx();
        	        if (NULL == rowreq_ctx) {
        	            snmp_log(LOG_ERR, "memory allocation failed\n");
                        free_instance_parameter_list(&paraHead);
        				Free_show_dhcp_pool(&poolshow);
        	            return MFD_RESOURCE_UNAVAILABLE;
        	        }
        	        if(MFD_SUCCESS != dot11DHCPIpAddressPoolTable_indexes_set(rowreq_ctx
        	                               , ipPoolIndex
        	               )) {
        	            snmp_log(LOG_ERR,"error setting index while loading "
        	                     "dot11DHCPIpAddressPoolTable cache.\n");
        	            dot11DHCPIpAddressPoolTable_release_rowreq_ctx(rowreq_ctx);
        	            continue;
        	        }

                    rowreq_ctx->data.slot_id = paraNode->parameter.slot_id;
                    
            		if(q->poolname) {
            			strncpy(rowreq_ctx->data.PoolName,q->poolname,sizeof(rowreq_ctx->data.PoolName)-1);
            		}
            		strncpy(rowreq_ctx->data.PoolMask,maskip,sizeof(rowreq_ctx->data.PoolMask)-1);

	                rowreq_ctx->data.IPAddpoolStartAdd = IPAddpoolStartAdd;
	    
	                rowreq_ctx->data.IPAddpoolEndAdd = IPAddpoolEndAdd;

	                rowreq_ctx->data.GatewayAddress = GatewayAddress;
	    
	                rowreq_ctx->data.IPAddLeasePeriod = IPAddLeasePeriod;

                    if(CONTAINER_INSERT(container, rowreq_ctx))
                    {
                        dot11DHCPIpAddressPoolTable_release_rowreq_ctx(rowreq_ctx);
                    }
                    ++count;
                }
            }    	  
            Free_show_dhcp_pool(&poolshow);
        }
    }

    free_instance_parameter_list(&paraHead);
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:dot11DHCPIpAddressPoolTable:dot11DHCPIpAddressPoolTable_cache_load",
               "inserted %d records\n", count));

	snmp_log(LOG_DEBUG, "exit dot11DHCPIpAddressPoolTable_cache_load\n");
	
    return MFD_SUCCESS;
} /* dot11DHCPIpAddressPoolTable_cache_load */

/**
 * cache clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
dot11DHCPIpAddressPoolTable_cache_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpAddressPoolTable:dot11DHCPIpAddressPoolTable_cache_free","called\n"));

    /*
     * TODO:380:M: Free dot11DHCPIpAddressPoolTable cache.
     */
} /* dot11DHCPIpAddressPoolTable_cache_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
dot11DHCPIpAddressPoolTable_row_prep( dot11DHCPIpAddressPoolTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:dot11DHCPIpAddressPoolTable:dot11DHCPIpAddressPoolTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* dot11DHCPIpAddressPoolTable_row_prep */

/** @} */
