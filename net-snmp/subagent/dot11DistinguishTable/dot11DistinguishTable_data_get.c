/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.18.2.1 $ of : mfd-data-get.m2c,v $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "dot11DistinguishTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement dot11DistinguishTable get routines.
 * TODO:240:M: Implement dot11DistinguishTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table dot11DistinguishTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * dot11DistinguishTable is subid 3 of wtpLink.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.3, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement dot11DistinguishTable data context functions.
 */

/*---------------------------------------------------------------------
 * DOT11-AC-MIB::dot11ConfigTrapSwitchEntry.wtpMacAddr
 * wtpMacAddr is subid 1 of dot11ConfigTrapSwitchEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.2.18.2.1.1
 * Description:
AP Mac  Address Group.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpMacAddr.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpMacAddr_dot11DistinguishTable_map(char **mib_wtpMacAddr_val_ptr_ptr, size_t *mib_wtpMacAddr_val_ptr_len_ptr, char *raw_wtpMacAddr_val_ptr, size_t raw_wtpMacAddr_val_ptr_len, int allow_realloc)
{
    int converted_len;

    netsnmp_assert(NULL != raw_wtpMacAddr_val_ptr);
    netsnmp_assert((NULL != mib_wtpMacAddr_val_ptr_ptr) && (NULL != mib_wtpMacAddr_val_ptr_len_ptr));
    
    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpMacAddr_map","called\n"));
    
    /*
     * TODO:241:r: |-> Implement wtpMacAddr non-integer mapping
     * it is hard to autogenerate code for mapping types that are not simple
     * integers, so here is an idea of what you might need to do. It will
     * probably need some tweaking to get right.
     */
    /*
     * if the length of the raw data doesn't directly correspond with
     * the length of the mib data, set converted_len to the
     * space required.
     */
    converted_len = raw_wtpMacAddr_val_ptr_len; /* assume equal */
    if((NULL == *mib_wtpMacAddr_val_ptr_ptr) || (*mib_wtpMacAddr_val_ptr_len_ptr < converted_len)) {
        if(! allow_realloc) {
            snmp_log(LOG_ERR,"not enough space for value mapping\n");
            return SNMP_ERR_GENERR;
        }
        *mib_wtpMacAddr_val_ptr_ptr = realloc( *mib_wtpMacAddr_val_ptr_ptr, converted_len * sizeof(**mib_wtpMacAddr_val_ptr_ptr));
        if(NULL == *mib_wtpMacAddr_val_ptr_ptr) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return SNMP_ERR_GENERR;
        }
    }
    *mib_wtpMacAddr_val_ptr_len_ptr = converted_len;
    memcpy( *mib_wtpMacAddr_val_ptr_ptr, raw_wtpMacAddr_val_ptr, converted_len );

    return MFD_SUCCESS;
} /* wtpMacAddr_map */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
dot11DistinguishTable_indexes_set_tbl_idx(dot11DistinguishTable_mib_index *tbl_idx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11DistinguishTable:dot11DistinguishTable_indexes_set_tbl_idx","called\n"));

    /* wtpMacAddr(1)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
     tbl_idx->wtpMacAddr_len = sizeof(tbl_idx->wtpMacAddr);
    /*
     * make sure there is enough space for wtpMacAddr data
     */
    if ((NULL == tbl_idx->wtpMacAddr) ||
        (tbl_idx->wtpMacAddr_len < (wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->wtpMacAddr_len = wtpMacAddr_val_ptr_len * sizeof(tbl_idx->wtpMacAddr[0]);
    memcpy( tbl_idx->wtpMacAddr, wtpMacAddr_val_ptr, tbl_idx->wtpMacAddr_len );
    

    return MFD_SUCCESS;
} /* dot11DistinguishTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
dot11DistinguishTable_indexes_set(dot11DistinguishTable_rowreq_ctx *rowreq_ctx, char *wtpMacAddr_val_ptr,  size_t wtpMacAddr_val_ptr_len)
{
    DEBUGMSGTL(("verbose:dot11DistinguishTable:dot11DistinguishTable_indexes_set","called\n"));

    if(MFD_SUCCESS != dot11DistinguishTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , wtpMacAddr_val_ptr, wtpMacAddr_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != dot11DistinguishTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* dot11DistinguishTable_indexes_set */


/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11DistinguishEntry.wtpTryDistinguishTimes
 * wtpTryDistinguishTimes is subid 1 of dot11DistinguishEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.3.1.1
 * Description:
The total times that trying to distinguish.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpTryDistinguishTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpTryDistinguishTimes_map(u_long *mib_wtpTryDistinguishTimes_val_ptr, u_long raw_wtpTryDistinguishTimes_val)
{
    netsnmp_assert(NULL != mib_wtpTryDistinguishTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpTryDistinguishTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpTryDistinguishTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpTryDistinguishTimes_val_ptr) = raw_wtpTryDistinguishTimes_val;

    return MFD_SUCCESS;
} /* wtpTryDistinguishTimes_map */

/**
 * Extract the current value of the wtpTryDistinguishTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpTryDistinguishTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpTryDistinguishTimes_get( dot11DistinguishTable_rowreq_ctx *rowreq_ctx, u_long * wtpTryDistinguishTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpTryDistinguishTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpTryDistinguishTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpTryDistinguishTimes data.
 * set (* wtpTryDistinguishTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpTryDistinguishTimes_val_ptr ) = rowreq_ctx->data.wtpTryDistinguishTimes;

    return MFD_SUCCESS;
} /* wtpTryDistinguishTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11DistinguishEntry.wtpDistinguishSuccessTimes
 * wtpDistinguishSuccessTimes is subid 2 of dot11DistinguishEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.3.1.2
 * Description:
The total times that distinguish successfully.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpDistinguishSuccessTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpDistinguishSuccessTimes_map(u_long *mib_wtpDistinguishSuccessTimes_val_ptr, u_long raw_wtpDistinguishSuccessTimes_val)
{
    netsnmp_assert(NULL != mib_wtpDistinguishSuccessTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpDistinguishSuccessTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpDistinguishSuccessTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpDistinguishSuccessTimes_val_ptr) = raw_wtpDistinguishSuccessTimes_val;

    return MFD_SUCCESS;
} /* wtpDistinguishSuccessTimes_map */

/**
 * Extract the current value of the wtpDistinguishSuccessTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpDistinguishSuccessTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpDistinguishSuccessTimes_get( dot11DistinguishTable_rowreq_ctx *rowreq_ctx, u_long * wtpDistinguishSuccessTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpDistinguishSuccessTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpDistinguishSuccessTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpDistinguishSuccessTimes data.
 * set (* wtpDistinguishSuccessTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpDistinguishSuccessTimes_val_ptr ) = rowreq_ctx->data.wtpDistinguishSuccessTimes;

    return MFD_SUCCESS;
} /* wtpDistinguishSuccessTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11DistinguishEntry.wtpReasonErrKeyFailDistinguishTimes
 * wtpReasonErrKeyFailDistinguishTimes is subid 3 of dot11DistinguishEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.3.1.3
 * Description:
The total times that distinguish failed because of wrong key.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonErrKeyFailDistinguishTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonErrKeyFailDistinguishTimes_map(u_long *mib_wtpReasonErrKeyFailDistinguishTimes_val_ptr, u_long raw_wtpReasonErrKeyFailDistinguishTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonErrKeyFailDistinguishTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpReasonErrKeyFailDistinguishTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonErrKeyFailDistinguishTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonErrKeyFailDistinguishTimes_val_ptr) = raw_wtpReasonErrKeyFailDistinguishTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonErrKeyFailDistinguishTimes_map */

/**
 * Extract the current value of the wtpReasonErrKeyFailDistinguishTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonErrKeyFailDistinguishTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonErrKeyFailDistinguishTimes_get( dot11DistinguishTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonErrKeyFailDistinguishTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonErrKeyFailDistinguishTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpReasonErrKeyFailDistinguishTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonErrKeyFailDistinguishTimes data.
 * set (* wtpReasonErrKeyFailDistinguishTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonErrKeyFailDistinguishTimes_val_ptr ) = rowreq_ctx->data.wtpReasonErrKeyFailDistinguishTimes;

    return MFD_SUCCESS;
} /* wtpReasonErrKeyFailDistinguishTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11DistinguishEntry.wtpReasonInvalidFailDistinguishTimes
 * wtpReasonInvalidFailDistinguishTimes is subid 4 of dot11DistinguishEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.3.1.4
 * Description:
The total times that distinguish failed because of invalided.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonInvalidFailDistinguishTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonInvalidFailDistinguishTimes_map(u_long *mib_wtpReasonInvalidFailDistinguishTimes_val_ptr, u_long raw_wtpReasonInvalidFailDistinguishTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonInvalidFailDistinguishTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpReasonInvalidFailDistinguishTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonInvalidFailDistinguishTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonInvalidFailDistinguishTimes_val_ptr) = raw_wtpReasonInvalidFailDistinguishTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonInvalidFailDistinguishTimes_map */

/**
 * Extract the current value of the wtpReasonInvalidFailDistinguishTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonInvalidFailDistinguishTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonInvalidFailDistinguishTimes_get( dot11DistinguishTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonInvalidFailDistinguishTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonInvalidFailDistinguishTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpReasonInvalidFailDistinguishTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonInvalidFailDistinguishTimes data.
 * set (* wtpReasonInvalidFailDistinguishTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonInvalidFailDistinguishTimes_val_ptr ) = rowreq_ctx->data.wtpReasonInvalidFailDistinguishTimes;

    return MFD_SUCCESS;
} /* wtpReasonInvalidFailDistinguishTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11DistinguishEntry.wtpReasonTimeOutFailDistinguishTimes
 * wtpReasonTimeOutFailDistinguishTimes is subid 5 of dot11DistinguishEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.3.1.5
 * Description:
The total times that distinguish failed because of time out.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonTimeOutFailDistinguishTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonTimeOutFailDistinguishTimes_map(u_long *mib_wtpReasonTimeOutFailDistinguishTimes_val_ptr, u_long raw_wtpReasonTimeOutFailDistinguishTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonTimeOutFailDistinguishTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpReasonTimeOutFailDistinguishTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonTimeOutFailDistinguishTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonTimeOutFailDistinguishTimes_val_ptr) = raw_wtpReasonTimeOutFailDistinguishTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonTimeOutFailDistinguishTimes_map */

/**
 * Extract the current value of the wtpReasonTimeOutFailDistinguishTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonTimeOutFailDistinguishTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonTimeOutFailDistinguishTimes_get( dot11DistinguishTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonTimeOutFailDistinguishTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonTimeOutFailDistinguishTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpReasonTimeOutFailDistinguishTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonTimeOutFailDistinguishTimes data.
 * set (* wtpReasonTimeOutFailDistinguishTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonTimeOutFailDistinguishTimes_val_ptr ) = rowreq_ctx->data.wtpReasonTimeOutFailDistinguishTimes;

    return MFD_SUCCESS;
} /* wtpReasonTimeOutFailDistinguishTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11DistinguishEntry.wtpReasonRefuseFailDistinguishTimes
 * wtpReasonRefuseFailDistinguishTimes is subid 6 of dot11DistinguishEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.3.1.6
 * Description:
The total times that distinguish failed because of refusing.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonRefuseFailDistinguishTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonRefuseFailDistinguishTimes_map(u_long *mib_wtpReasonRefuseFailDistinguishTimes_val_ptr, u_long raw_wtpReasonRefuseFailDistinguishTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonRefuseFailDistinguishTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpReasonRefuseFailDistinguishTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonRefuseFailDistinguishTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonRefuseFailDistinguishTimes_val_ptr) = raw_wtpReasonRefuseFailDistinguishTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonRefuseFailDistinguishTimes_map */

/**
 * Extract the current value of the wtpReasonRefuseFailDistinguishTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonRefuseFailDistinguishTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonRefuseFailDistinguishTimes_get( dot11DistinguishTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonRefuseFailDistinguishTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonRefuseFailDistinguishTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpReasonRefuseFailDistinguishTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonRefuseFailDistinguishTimes data.
 * set (* wtpReasonRefuseFailDistinguishTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonRefuseFailDistinguishTimes_val_ptr ) = rowreq_ctx->data.wtpReasonRefuseFailDistinguishTimes;

    return MFD_SUCCESS;
} /* wtpReasonRefuseFailDistinguishTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11DistinguishEntry.wtpReasonOthersFailDistinguishTimes
 * wtpReasonOthersFailDistinguishTimes is subid 7 of dot11DistinguishEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.3.1.7
 * Description:
The total times that distinguish failed because of other reasons.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpReasonOthersFailDistinguishTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpReasonOthersFailDistinguishTimes_map(u_long *mib_wtpReasonOthersFailDistinguishTimes_val_ptr, u_long raw_wtpReasonOthersFailDistinguishTimes_val)
{
    netsnmp_assert(NULL != mib_wtpReasonOthersFailDistinguishTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpReasonOthersFailDistinguishTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpReasonOthersFailDistinguishTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpReasonOthersFailDistinguishTimes_val_ptr) = raw_wtpReasonOthersFailDistinguishTimes_val;

    return MFD_SUCCESS;
} /* wtpReasonOthersFailDistinguishTimes_map */

/**
 * Extract the current value of the wtpReasonOthersFailDistinguishTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpReasonOthersFailDistinguishTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpReasonOthersFailDistinguishTimes_get( dot11DistinguishTable_rowreq_ctx *rowreq_ctx, u_long * wtpReasonOthersFailDistinguishTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpReasonOthersFailDistinguishTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpReasonOthersFailDistinguishTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpReasonOthersFailDistinguishTimes data.
 * set (* wtpReasonOthersFailDistinguishTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpReasonOthersFailDistinguishTimes_val_ptr ) = rowreq_ctx->data.wtpReasonOthersFailDistinguishTimes;

    return MFD_SUCCESS;
} /* wtpReasonOthersFailDistinguishTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11DistinguishEntry.wtpUserRequestTimes
 * wtpUserRequestTimes is subid 8 of dot11DistinguishEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.3.1.8
 * Description:
The total request times of user.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpUserRequestTimes.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpUserRequestTimes_map(u_long *mib_wtpUserRequestTimes_val_ptr, u_long raw_wtpUserRequestTimes_val)
{
    netsnmp_assert(NULL != mib_wtpUserRequestTimes_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpUserRequestTimes_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpUserRequestTimes mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpUserRequestTimes_val_ptr) = raw_wtpUserRequestTimes_val;

    return MFD_SUCCESS;
} /* wtpUserRequestTimes_map */

/**
 * Extract the current value of the wtpUserRequestTimes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpUserRequestTimes_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpUserRequestTimes_get( dot11DistinguishTable_rowreq_ctx *rowreq_ctx, u_long * wtpUserRequestTimes_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpUserRequestTimes_val_ptr );


    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpUserRequestTimes_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpUserRequestTimes data.
 * set (* wtpUserRequestTimes_val_ptr ) from rowreq_ctx->data
 */
    (* wtpUserRequestTimes_val_ptr ) = rowreq_ctx->data.wtpUserRequestTimes;

    return MFD_SUCCESS;
} /* wtpUserRequestTimes_get */

/*---------------------------------------------------------------------
 * DOT11-WTP-MIB::dot11DistinguishEntry.wtpAssociatedUserNum
 * wtpAssociatedUserNum is subid 9 of dot11DistinguishEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.31656.6.1.1.6.3.1.9
 * Description:
The number of users associated.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is COUNTER. The C type decl is u_long (u_long)
 */
/**
 * map a value from its original native format to the MIB format.
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_ERROR           : Any other error
 *
 * @note parameters follow the memset convention (dest, src).
 *
 * @note generation and use of this function can be turned off by re-running
 * mib2c after adding the following line to the file
 * default-node-wtpAssociatedUserNum.m2d :
 *   @eval $m2c_node_skip_mapping = 1@
 *
 * @remark
 *  If the values for your data type don't exactly match the
 *  possible values defined by the mib, you should map them here.
 *  Otherwise, just do a direct copy.
 */
int
wtpAssociatedUserNum_map(u_long *mib_wtpAssociatedUserNum_val_ptr, u_long raw_wtpAssociatedUserNum_val)
{
    netsnmp_assert(NULL != mib_wtpAssociatedUserNum_val_ptr);
    
    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpAssociatedUserNum_map","called\n"));
    
    /*
     * TODO:241:o: |-> Implement wtpAssociatedUserNum mapping.
     * If the values for your data type don't exactly match the
     * possible values defined by the mib, you should map them here.
     */
    (*mib_wtpAssociatedUserNum_val_ptr) = raw_wtpAssociatedUserNum_val;

    return MFD_SUCCESS;
} /* wtpAssociatedUserNum_map */

/**
 * Extract the current value of the wtpAssociatedUserNum data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wtpAssociatedUserNum_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
wtpAssociatedUserNum_get( dot11DistinguishTable_rowreq_ctx *rowreq_ctx, u_long * wtpAssociatedUserNum_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != wtpAssociatedUserNum_val_ptr );


    DEBUGMSGTL(("verbose:dot11DistinguishTable:wtpAssociatedUserNum_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the wtpAssociatedUserNum data.
 * set (* wtpAssociatedUserNum_val_ptr ) from rowreq_ctx->data
 */
    (* wtpAssociatedUserNum_val_ptr ) = rowreq_ctx->data.wtpAssociatedUserNum;

    return MFD_SUCCESS;
} /* wtpAssociatedUserNum_get */



/** @} */
